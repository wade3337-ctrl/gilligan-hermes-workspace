# 2026-07-12

## Asset cache-busting on the dashboards (ship-log #136)
- Skipper's mobile bar-chart fix (#135) looked broken still because his **browser cached the old chart JS** — hard refresh fixed it. Offered a durable fix; he said "yes add it".
- Added `?v=20260712` to `js/mtab-bar-chart-style.js` in the **5 iframed Executive tab pages** (ByRep$Scope, ByMarket, ClosePct$ByRep, ClosePct$ByMarket, ByCrewName — the tabs the Exec shell iframes via `data-base`), and bumped `protips.css?v=20260615→20260712` in the shared `protips-include.cfm` (one file → every protip page). Matched the existing house `?v=YYYYMMDD` convention (found on `css/gsts-protips.css?v=20260615`).
- Deployed 6 files to play (D:\ webroot). **Verified served output** on play emits both versioned URLs (curl as ZUserID=9).
- Also backfilled ship-log **#134** (action-badge orphan) + **#135** (bar-chart overlap) — deployed earlier but never got arbor-stack table rows. Log header refreshed (date + item count 125→136, though the 125 total was already stale).
- Doc: DEPLOY-PACKAGE got a "Cache-busting (no extra files)" note — the versioned refs live in files already in the deploy set; nothing new to migrate; bump the date on future asset changes.
- Committed arbor-stack `458f344`.

## Executive reconcile Q → renamed "Sales by Market" tab (ship-log #137)
- Skipper: "Rec" shows $11,825 on Sales-by-Market but $77,770 on Closing-%-by-Market — explain. **Not a bug, different metrics:** Sales-by-Market = `SUM(Invoices.NetTotal)` = billed/invoiced revenue; Closing-%-by-Market dollar = `SUM(Proposals.EstValue)` = bid/quote value of proposals written in-window (closing-rate denominator). Quoted > invoiced is expected.
- He said "yes rename it" → renamed the revenue tab to **"Invoiced Revenue by Market"** in all 4 spots (Beta shell tab button, page `<title>`, panel heading, chart title; protip-keys untouched). Deployed D:\, verified served + 0 CF errors. Committed `42bebfd`.
- **Open (offered, pending):** "Sales by Rep" uses the SAME `Invoices.NetTotal` basis (grouped by measured rep) → renaming it to "Invoiced Revenue by Rep" would keep the pair symmetric. Waiting on Skipper's call.

## Invoiced Revenue by Market — 4-group bucket drill + 2 security gaps closed (ship-log #138)
- Skipper: give the revenue tab "the same four groups we have in closing by markets with the same drill downs." Built the 2-level drill matching Closing % by Market: **L0** 4 groups (Municipal/HOA/Municipal Other/Commercial) → **L1** segments → **L2** per-sale detail. Chose **option A** for Municipal (drills to its city segments; the Closing tab's Municipal→City Budgets redirect is close-rate-specific, doesn't apply to revenue).
- Verified L0 (Municipal $8.12M / HOA $8.03M / Muni Other $748K / Commercial $3.22M T12, 0 CF errors) + L1 Commercial→10 segments + screenshot.
- **SECURITY find (surfaced to Skipper):** both `Executive$Sales$Detail.cfm` AND `Executive$ClosePercentage$Detail.cfm` returned **200 to a garbage cookie** — ungated per-record financial detail the Jul-11 crew audit MISSED (drill children not enumerated; Sales$Detail wasn't even in the repo). Gated both with `dashboard-auth-gate.cfm`, pulled Sales$Detail into repo, added both to the deploy package. Verified garbage→403, pilot→200. Committed `7a3153f`.
- **Lesson candidate:** the crew audit's "20 leak surfaces" didn't include the Exec **detail/drill** pages — enumerate ALL drill children, not just tabs, when gating.

## Where the deploy walkthrough paused
- We were walking the deploy package section by section; last covered Section 3, **Section 4 (the two DB steps) was next** when we detoured into mobile QA + this cache fix. Resume there when Skipper's ready.
- Deferred (do NOT act without explicit OK): apply `deactivate-rep-chase-PROD.sql`; ZTest-/Beta filename renames (post-pilot).
