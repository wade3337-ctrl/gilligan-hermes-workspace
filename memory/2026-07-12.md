# 2026-07-12

## Asset cache-busting on the dashboards (ship-log #136)
- Skipper's mobile bar-chart fix (#135) looked broken still because his **browser cached the old chart JS** — hard refresh fixed it. Offered a durable fix; he said "yes add it".
- Added `?v=20260712` to `js/mtab-bar-chart-style.js` in the **5 iframed Executive tab pages** (ByRep$Scope, ByMarket, ClosePct$ByRep, ClosePct$ByMarket, ByCrewName — the tabs the Exec shell iframes via `data-base`), and bumped `protips.css?v=20260615→20260712` in the shared `protips-include.cfm` (one file → every protip page). Matched the existing house `?v=YYYYMMDD` convention (found on `css/gsts-protips.css?v=20260615`).
- Deployed 6 files to play (D:\ webroot). **Verified served output** on play emits both versioned URLs (curl as ZUserID=9).
- Also backfilled ship-log **#134** (action-badge orphan) + **#135** (bar-chart overlap) — deployed earlier but never got arbor-stack table rows. Log header refreshed (date + item count 125→136, though the 125 total was already stale).
- Doc: DEPLOY-PACKAGE got a "Cache-busting (no extra files)" note — the versioned refs live in files already in the deploy set; nothing new to migrate; bump the date on future asset changes.
- Committed arbor-stack `458f344`.

## Where the deploy walkthrough paused
- We were walking the deploy package section by section; last covered Section 3, **Section 4 (the two DB steps) was next** when we detoured into mobile QA + this cache fix. Resume there when Skipper's ready.
- Deferred (do NOT act without explicit OK): apply `deactivate-rep-chase-PROD.sql`; ZTest-/Beta filename renames (post-pilot).
