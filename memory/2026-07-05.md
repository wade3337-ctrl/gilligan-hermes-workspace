
## Slice 2 (arbor-core map-overlay) — crosswalk found + opportunity sized (2026-07-05)
Skipper resumed Slice 2 = Inventory overlay UI build+verify. Reset the reporting cadence first (milestone pings, option A — see wiki async-report-rule).

**Root-cause of "detection returns 0":** NOT detection — a swallowed Postgres error. `georef_map.detect_trees` matched inventory via `coalesce(asset_number,'')` but asset_number is INTEGER → `invalid input syntax for type integer ""`. Fixed with `::text` cast + added legacy_inventory_id as a key. Gemini detection itself works (reads GSTSID markers clean).

**THE CROSSWALK (Skipper's Q: where do map numbers match tree identity?):** TRIM IT `dbo.InventoryDetail.GSTSID` = the small number printed on the map (per-site seq 1..N). Chain: map label → GSTSID → InventoryDetailID → arbor-core tree.asset_number/legacy_inventory_id → tree_id. Proven on Culver (markers 52→Palm Dactylifera, 594→Palm Queen). **We do NOT import GSTSID yet — that's the missing field.** bidqueue_import.py imports InventoryDetailID as asset_number+legacy_inventory_id.

**BIG efficiency finding — TRIM IT already stores Lat/Long for most trees.** Culver: 851/880 already GPS'd in TRIM IT; the 29 ungeo have null coords AND no GSTSID (never plotted → not on any map → AI can't place them, manual only).

**Opportunity scan (5,987 commercial sites, InventoryDetail, IsDeleted=0; rows inflate ~2x from revisions):**
- A full_gps: 362 sites (130,957 rows all GPS) — just import coords.
- B ≥90% gps: 210 sites (17,265 coordless).
- C coordless+LABELED+has map (AI TARGET): **2,004 sites, ~51,948 recoverable rows (~26K trees dedup).**
- D coordless+unlabeled: 3,379 sites — manual only, AI can't help.
- ~1.28M tree-rows already carry lat/lng in TRIM IT → coordinate-import rollout = biggest free win.

**Recommended build order:** (1) import GSTSID (crosswalk) + roll out TRIM IT coordinate import broadly [free win]; (2) rewire detect_trees match to GSTSID; (3) validate Slice 2 on a real bucket-C site (Culver is already GPS-complete = wrong test site); (4) Phase-3 Inventory UI + crew verify.
Test artifacts: /tmp/g2test/. gsql.sh = TRIM IT play read. Instrumented georef_map.py (DETECT_DEBUG env → log).

## Slice 2 validation results (2026-07-05 AM) — crosswalk PROVEN, detection is the remaining hard part
Lane A done: GSTSID crosswalk shipped (commit bb4f459, migration 0025 map_label, importer+matcher, kanban card 21). Backfilled Culver+Ashford. Coordinate import already worked (in importer).
Validated on 2 fresh bucket-C imports:
- **1273573 (Los Olivos, 150 trees, 2007 SITE MAPS INVENTORY):** georef BAILED "no work-zone outline" — it's a site-plan/CAD drawing, not a Google-aerial-with-yellow-zone. → **bucket-C maps are heterogeneous; current georef only handles aerial+zone maps.**
- **1277715 (Los Olivos 2, 174 trees, 08-11-16 Google Site Map):** ran end-to-end, ok:true, **54 markers matched via map_label (CROSSWALK PROVEN)**, 0 tree GPS written (propose-only safety validated). BUT: (a) georef **rms 64m** (poor; Culver was 16-24m), (b) **DETECTION HALLUCINATED** — Gemini returned a perfect vertical grid (id 1..17 all x=382, y+10) = it read a legend list / fabricated a sequence, NOT real marker positions. This is exactly the crew's flagged #1 risk (single-shot whole-map detect truncates/hallucinates).
**Verdict:** crosswalk + import + propose-only safety all PROVEN. Remaining Slice-2 work = the AI-quality layer: (1) robust detection = tile hi-res map + CV/template-match real plotted symbols + per-crop OCR (NOT one Gemini call); (2) georef accuracy per map-type; (3) map-type classifier (aerial+zone vs site-plan). GSTSID format varies per site (Culver 1..N numeric; Los Olivos1 = 19990431-A alphanumeric) — matcher handles either (string key).
Imported real sites left in arbor-core: Los Olivos (019f30ac...), Los Olivos 2 (019f3299...). Test artifacts /tmp/g2test/.

## ⚠️ CORRECTION + DEFINITIVE FINDING (2026-07-05 AM) — Slice 2 premise doesn't hold: maps have NO per-tree numbers
Built the robust tiled-vision detector (crew Gemini+GLM converged; detect_markers.py — two-pass map-bbox+style, tiled reads, NMS dedup, JS variance gate + grid-reject). It WORKS: its Pass-1 has_markers gate correctly returned 0/"no markers" on markerless sheets where the OLD single-shot code hallucinated a fake grid. Good engineering.
BUT visual inspection (finally LOOKED at the rendered maps via Read tool) is decisive:
- **Culver "Tree Master Plan 2019":** trees plotted as SPECIES-COLORED SYMBOLS (circles/stars per species) + a species legend. NO per-tree numbers printed. The "52"/"594" the naive code "detected" = the legend "Total Trees 52 / 594" TOTALS. **My earlier "crosswalk PROVEN (52->Palm Dactylifera, 594->Palm Queen)" was a FALSE POSITIVE — legend totals coinciding with GSTSIDs.**
- **Los Olivos 2 "Google Site Map":** plain Google Earth aerial + a property boundary line. No trees plotted at all.
- Matches 2026-07-02 Greenwood finding: "the map does NOT locate individual trees."
**Conclusion:** legacy maps encode tree POSITION + SPECIES (symbols), NOT the GSTSID number. So "detect the numbered marker, read its number, match to tree" has ~no addressable surface: where trees ARE plotted (species maps) TRIM IT already has their coordinates (free import, no AI); where coords are missing (coordless sites) the trees were never plotted on a readable map. The 2,004 "coordless+labeled" sites are labeled in the INVENTORY LIST, not on a printed map.
**Real value stands elsewhere:** (1) coordinate-import rollout (free, already works) is the big win; (2) a POSSIBLE narrow AI use = species-symbol position detection on species-master-plans that lack coords (identity via species-cluster, not number) — unquantified, needs a scan. Slice-2-as-conceived (number-marker OCR) should be PARKED. detect_markers.py kept (solid, reusable for species-symbol detection if we pivot).

## ✅ RESOLUTION (2026-07-05 AM) — Slice 2 IS viable, scoped to "WITH ID" species maps. Detector VALIDATED.
Pivot B revealed the earlier "premise dead" was too broad. THREE legacy map classes:
1. Master species-plan (Culver, LosOlivos basemap2019): species-colored symbols + legend totals, NO per-tree numbers. Not marker-readable. (These sites usually already have TRIM IT coords anyway.)
2. **Species "WITH ID" map (LosOlivos "2017 Palm Dact Map"): every tree = a blue/yellow dot with its GSTSID number printed. MARKER-OCR WORKS.**
3. Plain aerial / boundary only (LosOlivos Google Site Map): nothing plotted.
**VALIDATION (real, on coordless site 019f3299 LosOlivos2):** ran the new tiled detector (detect_markers.py) on the Palm Dact WITH-ID map → has_markers=True, style auto="blue/yellow circle w/ number", 24 tiles, **0 grid-tiles, 110 raw→81 deduped, all 81 survived the variance gate** (positions scattered/real, NOT the old fake grid). Detected IDs = 1..81. The site's Dactylifera palms have map_label distinct 1..81 EXACTLY → detect marker N -> map_label N -> tree_id. So a coordless site's 81 palms can get GPS from the map. detect_markers.py anti-hallucination design PROVEN (correctly returned 0 on the 2 markerless sheets; real 81 on the numbered one).
**Remaining to finish Slice 2:** (a) wire detect_markers into georef_map (replace naive detect_trees); (b) georef the WITH-ID aerial WITHOUT requiring a yellow zone (transform from landmark-match; zone optional for placement) — current run() hard-exits on no-zone; (c) dedup revision-duplicate map_labels (168 rows/81 palms ~2x); (d) SCAN to size how many "WITH ID" maps exist across the 5,987 sites (the true opportunity — needs map-image or filename heuristic like '%WITH ID%','%Palm%Map%').

## ✅✅ SLICE 2 END-TO-END COMPLETE (2026-07-05, commit 50e61c6, card 21 -> beta)
Wired detect_markers into georef_map + made yellow-zone OPTIONAL (WITH-ID maps have no zone; transform from landmark-match). Full run on coordless LosOlivos2 Palm-Dact WITH-ID map: **georef rms 3.8m** (19 CPs), detector read **80 numbered palms** (24 tiles, 0 grid-drops, all survived variance gate), all matched map_label 1..81 -> tree_id, **80 palms proposed with GPS** spread realistically (propose-only, 0 auto-written). Visual overlay proof (/tmp/g2test/placement_proof.png) shows red placements land on the palm dots. So a coordless site can now recover real tree GPS from its WITH-ID map.
REMAINING (not blocking): commit-trees endpoint/UI (owner review->write GPS), dedup revision-duplicate map_labels (168 rows/81 palms), guard spurious micro-zone (yellow marker dots falsely trip the yellow-zone extractor -> junk 5m polygon; harmless to placements), SCAN dbo.Maps to size how many WITH-ID maps exist across 5,987 sites = the true opportunity.

## Strategy realign (Skipper 2026-07-05) — map-overlay is ON-DEMAND per-customer, NOT a batch migration
Skipper course-corrected: don't process the whole TRIM IT history at once. The map-overlay is a per-site tool used AS we bring each customer into arbor-core; the USER decides whether to run the AI overlay + whether to commit (thin-slice/incubator way). The opportunity scan was only a one-time "is it worth building?" gut-check (yes: ~1,000+ sites / ~35-50k coordless trees, rough) — NOT a to-do. Calibration sample DROPPED (was measuring a batch we'll never run). Real next build = commit-trees endpoint/UI (the tech's apply/commit step in onboarding), NOT a scan.

## commit-trees flow BUILT + validated (2026-07-05, commit 9fd50cd)
Skipper: put commit next to the Import legacy map button, do NOT remove import (import also useful). Built:
- API main.py: GET /sites/{id}/proposed-trees (from site_area.georef proposed_trees, deduped per coordless tree) + POST /sites/{id}/commit-trees (only writes coordless trees, zone-tags, source_system='ai_map').
- UI InventoryStep: orange "review me" pins for proposals + "✨ Commit AI placements (N)" button beside the untouched Import button; confirm->commit->refresh.
Validated on LosOlivos2: GET 80 -> commit wrote 80 (ai_map), proposals->0, other 94 species left coordless. Headless load 0 JS errors. Committed. NEXT (Skipper): handle bugs he found on the inventory page.

## Inventory overlay: map PICKER + SEPARATE area/tree extraction BUILT (2026-07-05, commit 90b0aca)
Skipper confirmed his intended flow, I flagged 2 gaps, he said build it. Done:
- georef_map.run(mode='areas'|'trees'|'both'); import_service + maps_api thread mode through. maps_api POST /maps/{id}/georef?mode=... (areas validated 34s/proposed:0 = detection skipped; trees = tiled detect+propose).
- UI InventoryStep: Map picker (pulled site_map maps) + "Extract areas" + "Extract trees" buttons on the selected map; trees->orange pins->existing Commit. PDF-upload "Import legacy map" KEPT as fallback. Headless 0 JS errors, endpoints live.
Full architecture now matches Skipper's model: maps come with customer -> pick a map -> extract areas AND/OR trees (separate) -> review -> commit. NEXT: Skipper to live click-test + then the inventory-page bugs he found (still pending). Los Olivos1/2 remain as test sites (Los Olivos2 has 80 palms committed + some test zones).

## Area COLOR scheme — Skipper wants extracted areas to match GSTS colors (2026-07-05)
Current tool: stamps fixed cyan #00e5ff on every extracted area; UI colors zones by arbitrary index (zoneColor(i)) — distinct but NOT matched to records.
TRIM IT DOES store per-zone color: dbo.ZoneDefs (Desc1=name, LocationID, ColorCode, ColorDefID, Latitude/Longitude center, FusionTablePolygon). dbo.ColorDefs = canonical palette name->hex (1 Yellow #FFFF00, 2 Blue #3366FF, 3 Green #00CC33, 4 Red #FF0000, 6 Brown #663300, 9 Orange #FF6600, 11 Shale #99CC99, 15 Purple #9999FF... 53 rows, +FontColorCode).
CAVEATS: (1) 3,441 zones are just "Yellow" (mostly uniform in data — distinct colors likely a display convention); (2) zone BOUNDARY geometry essentially NOT stored (2,953 sites have ZoneDefs but only 1 has ZonePoints; FusionTablePolygon maybe) -> we still AI-extract the area shape, just paint it with the matched ZoneDef color+name.
PROPOSED FIX (pending Skipper go / after his live test): import ZoneDefs name+color on bring-over (resolve via ColorDefs), apply to extracted site_area.color, make UI honor stored color not index palette.

## Area COLOR matching BUILT (2026-07-05, commit af47682)
Skipper: do it now. Built: migration 0026 site.zone_colors jsonb; importer pull_zone_colors carries TRIM IT dbo.ZoneDefs color per zone (resolved via dbo.ColorDefs; prefer zd.ColorCode text over ColorDefID FK — they can disagree, e.g. Culver text='Yellow' but FK=Shale) into site.zone_colors; georef_map colors extracted areas from the imported zone color else GSTS palette (ColorDefs hex: Yellow #FFFF00, Blue #3366FF, Green #00CC33...) replacing hardcoded cyan; UI uses GSTS palette + honors stored z.color (zoneCol). Verified: Culver imports Yellow #FFFF00, fresh extraction stamped #00CC33 green, 0 JS errors. Exact per-zone-name match fires only where names align (rare — extracted area name=map name); practical result = distinct GSTS-palette colors. GSTS_PALETTE constant duplicated in georef_map.py + index.html (ZONE_PALETTE). Zone boundaries still AI-extracted (not stored in TRIM IT).

## Inventory bug #1 FIXED (2026-07-05, commit 6063e40) — tree-pin click dropped a duplicate
Skipper: clicking an existing imported tree changed it to the top-box species + added to inventory. Root cause: Leaflet circleMarker clicks bubble to the map -> pin click fired openTree AND the map stamp handler (POST new tree w/ tmpl species). Fix: bubblingMouseEvents:false (+stopPropagation) on tree/cluster/proposed markers. 0 JS errors. LESSON logged. (More inventory bugs pending from Skipper.)

## Inventory bug #2 FIXED (2026-07-05, commit cf72b40) — no pre-selected type/size
Skipper: don't pre-select tree type/size in top bar. species/size default '' (was 'Coast Live Oak'/'M'); size select has 'Size…' placeholder; map-tap + drop-at-GPS guard: no drop until a species is picked (prompt instead), size_class=null when unset. 0 JS errors.

## Pricing redesign Phase 1 BUILT (2026-07-05, commit cabe2c6) — group-first cohort grid
Skipper: build it, let me play. Access code = per-ZONE multiplier (same trees, harder zone → bump access ×). Built:
- API: GET /sites/{id}/cohorts (species×size×zone + count + tree_ids + history-seeded est_hours/tree + hist_tph, site_rebid_history→price_history benchmark); POST /proposals/{id}/cohort-line (one call: tree_group+line+computed service seeded from history; sets proposal_line.tree_count=N so it prices ×count — gotcha: tree_count defaults 1, must set explicitly). Verified Olive cohort → 194.5h/$25,282/blended 130.
- UI QuoteStep: replaced flat +species button wall with cohort grid (filter, count, hrs/tree, green✓/red▼ Last-TPH pill). "+ Price" → auto-priced line. Per-zone access × lives on each line's ServiceRow. 0 JS errors.
Data check passed earlier (price_history 2847 rows all w/ hours; site_rebid_history per-site). Phase 2 = Profit Lens map + map↔grid sync + evidence-confidence tiers. Design doc docs/PRICING-SHEET-REDESIGN.md, mockup docs/pricing-mockup.html, kanban card 22.

## Inventory bugs #3 + #4 FIXED (2026-07-06)
#3 (commit 119d67f): Extract Areas on a bad/residential map corner-jammed the Leaflet map (tiles stuck top-left, no escape). Fix: okBounds() guard (bad/degenerate/out-of-range bounds never reach fitBounds/imageOverlay) + invalidateSize() after extract + NEW DELETE /sites/{id}/areas/{area_id} + "↩ Undo extraction" button (untags trees, they keep GPS). LESSON logged.
#4 (commit 6cc25ac): opening First Service then inventory showed ASHFORD's maps (24/25) auto-selected, no deselect. Cause: InventoryStep/QuoteStep had NO key → reused prior customer's stale state (maps/selMap/trees). Fix: key=site_id on both (fresh remount per customer) + removed map auto-select + added "— select a map —" placeholder. Both 0 JS errors.

## Map-type classifier + preview + gating BUILT (2026-07-06 overnight, commit 442e701) — crew-built
Skipper (overnight task): add a map-type classifier so you can't feed the wrong map to the georef process + a preview popup; "have fable/codex build it, lean on the crew."
- DESIGN: Gemini+Fable+Codex converged (Fable's key rule: gate from booleans not the enum). CODEX BUILT importer/classify_map.py (render page-1 → 1 vision call → type) and tested it on 3 real maps, all correct.
- BUILT: mig 0027 (site_map map_type/is_aerial/has_markers/confidence/desc/raw/preview_path/classified_at); host classify + import_service /classify (saves page-1 preview PNG to basemaps); maps_api POST /maps/{id}/classify + GET /maps/{id}/preview.png + list_maps returns server-derived `allowed` (aerial⇒areas; aerial+markers⇒trees); georef endpoint 409-GATES wrong-type maps (force=true override); UI picker shows type + gates Extract buttons + 👁 Preview modal (image+verdict); classifies unclassified maps on inventory load. Fail-safe: unclassified/uncertain ⇒ locked.
- VERIFIED: all 5 Los Olivos 2 maps classified correctly (RFP+ClientMap=non_map, Google=aerial_base areas-only, PalmDact=species_with_id both, Base2019=master_species_plan locked). classify+preview(200/6.5MB)+gate(409) proven. 0 JS errors.
- This RESOLVES most of the pinned "area-scan on wrong maps breaks it" issue — Ashford work-order maps now classify as work_order → both buttons disabled. Still pending: Skipper's feel/feedback + confirm gating matches his judgment.

## Extract-Trees map-corner bug SOLVED + timeout fixed (2026-07-06) — end-to-end works
After MANY wrong swings (fitBounds, invalidateSize, ResizeObserver — all treating symptoms), the REAL bug: clicking Extract Trees set note='Reading trees…' which inserted a <div> ABOVE the ref-held Leaflet map div → preact RECREATED the map DOM node → .leaflet-container went 0×0 (map DESTROYED) → jammed to corner. FIX (commit 8994c10): render err+note banners ALWAYS (display:none when empty) so the map's siblings never shift. Reproduced+measured headlessly (localStorage session-inject cust={Los Olivos/Irvine Co} + click Extract-trees; container stayed 930×430 during extraction, visual = trees centered). Skipper CONFIRMED fixed. Lesson logged (preact-recreates-map-div-on-sibling-insert).
Then TIMEOUT: detection = 24 tiles × 1 sequential vision call (>6min) blew maps_api 400s. FIX (fb39c62): ThreadPoolExecutor(6) parallel tiles + timeout 400→900s. Verified 87s (was >6min), 80 palms, rms 7.4m.
Also today: map-type CLASSIFIER/preview/gating (Codex built classify_map.py, mig 0027, commit 442e701), site-name-in-header + clean-map-on-open (71b4d26). Full record: wiki [[arbor-core-onestop-ui]] RESOLVED section.
KEY META-LESSON (Skipper feedback): stop guessing — reproduce first (headless localStorage inject is the tool for this app), and lean on the crew (Codex) rather than one-man-band. This is what finally cracked it.
