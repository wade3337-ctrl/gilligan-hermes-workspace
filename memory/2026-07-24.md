# 2026-07-24 (late 7/23 PT session)

## 🔧 TRIM IT dashboard/report accuracy fixes (play, shipped + verified) — see gsts-ship-log.md
1. **Sales-by-Rep "Unattributed" drill** (`Executive$Sales$Unattributed.cfm`): view-list ignored the date range (period-ID model, default 0) → showed all-time (2015+). Fixed to filter `InvoiceDate BETWEEN ZStartDate/ZEndDate`; reconciles to $65,775/13; empty-state guard. D:-only webroot.
2. **Revenue Performance dashboard** (`Dashboard-RevenuePerformance.cfm`): reported `Calendars.EstValue` (ESTIMATE) as produced. Fixed True Produced → actual `CrewSheets.CompletedDollars` for past/today (**Option A, strict** — Skipper's call). June 1 = $84,909.71 (matches Day Sheets). Overstatement ~2.9%/mo gone.
3. **Daily COO/rep email** (`monitor.js` + `revenue-block.js`, cron-local): labeled the schedule-board ESTIMATE as "Produced." Fixed → Produced = TrueProduced actual ($1.295M July MTD); Tracking-to-goal = ScheduleBoard total (expected landing). Per-rep + Nate rollup were already accurate (use completedDollars).

## ⚠️ Big lesson (cost ~40 min) — SHADOW WEBROOT
Play serves some `.cfm` from **`C:\ColdFusion2023\cfusion\wwwroot\GSTS\`**, not just the IIS root `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` that `deploy-to-play.sh` writes. The RevenuePerformance fix had no effect (even after a **CF Application Server restart** — Skipper-approved — and cfclasses clears) until the C: shadow copy was updated. Fresh filenames served from D: fine (that's why other fixes "just worked"). → LESSONS.md. **Search whole server for dup copies before blaming CF cache; deploy to ALL live copies.**

## 📦 Deploy
Both dashboard fixes staged in **`arbor-stack/release-candidates/NEXT-DEPLOY-20260724/`** (manifest warns re: dual webroot). Held out of the live V15 package (Skipper's call) → next prod batch. Kanban: 3 shipped cards (CardID 59-61) on the TRIM IT board; backup `Workbench.dbo.WorkKanban_bak_20260724`.

## Other threads this session
- **Goodman/Boss Herman:** handed Herman two paste-ready prompts (copy-paste hand-off is the standing method now — trains him on the real chat workflow; do NOT stand up the direct line → [[herman-agent]]): (1) satellite-aerial basemap for the traveler field map (was a blank matplotlib scatter — contextily + Esri WorldImagery, reproject 4326→3857); (2) OVERLAY true-labor pricing onto the genuine TRIM IT blank Pricing Worksheet PDF (not a recreation), matching format, ~$5,406.
- **Incentive plan** (BLACK, [[key-employee-incentive-plan]]): pulled Nate; re-pegged to actual $33.4M net proceeds ($234K/1%); added Ba Tran (Inventory Mgr) + Arthur Margaryian (Fleet Manager) at 0.08% each; Garrett flipped to 40/60. 2.90% allocated / ~$679K / $101K slack. Emailed clean chart to jwade.
- **June financials review** (Dimitry pack, [[fort-point-acquisition]]): 2026 NI down mostly because 2025 was juiced by ~$320K/mo one-time income; real operating overhead +$110K/mo (5 accounts = the whole increase; Accounting/Lobbyist/Legal are add-back candidates). TTM adj EBITDA (Dimitry pack) $2.02M vs deal $3.80M → the add-back bridge Steve owes. Emailed exec talking points (Track-1 clean) to jwade.

---
## 7/24 AM continued (context-heavy session — full capture for chat refresh)

### 🟢 RESUME POINTERS (start here next session)
- **Herman is BUILDING the field-map PUBLISH step** (grey/published info-card layer) — handed off ~16:23. Blueprint = Kimi K3's reverse-engineering (verified by Gilligan). **When he reports:** verify IsNewPlot cleared, the GeoJSON written to `/GSTS/API/JSON/` matching the contract, grey `shaded_dot` dots + info-card render (blue gone), and the publish gate is in his pipeline. Full mechanism + PLAY fix → `trimit-knowledge/procedures/gps-publish-info-card-mechanism.md`.
- **Herman's fresh Eastvale build** (this morning's rundown test) STILL not in the DB as of ~15:00 — was waiting to run the full lessons rundown on it. Re-scan for an Eastvale project; run the 12-gate rundown when it lands.
- **NEXT-DEPLOY-20260724 package** ready for prod (Unattributed drill + Dashboard-RevenuePerformance.cfm w/ ALL today's upgrades). Manifest documents the **dual-webroot** requirement. Not yet deployed to prod (held per Skipper).

### 🔧 Revenue Performance dashboard — MAJOR day (all live on play, both webroots, in the deploy pkg)
1. Estimate→actual produced (True Produced = CrewSheets.CompletedDollars).
2. "Actual to date" source-independent + restore-proof (nightly restore was flipping GoalSettings.RevenueSource→ScheduleBoard → dashboard showed the estimate $1.68M vs report $1.29M).
3. 3-bucket model: Confirmed (posted actuals) / Pending (worked-but-unposted lag → estimate as projected, no $0 dip) / Scheduled (future). `row.isPostedActual`.
4. Projected days value = scheduled hours × Target TPH (not lumpy EstValue that front-loads whole-job values, e.g. the "Grid 2" $45,878-on-18hr sheet — flagged as a real TRIM IT data error for the office).
5. **FEATURE: dual TPH** — new "True TPH" tile (revenue ÷ ALL paid hrs) next to "Productive TPH"; **Non-Productive Time collapsible tab** (Yard/Safety/OJT/ModDuty/Meetings breakdown + TPH drag); **True-TPH tick on every bar** (3px/$ drag, colored by whether True clears target, shaded cap = productivity lost). `internalSubtype()` + `internalByType`.
- **Daily emails inherit all of it** (monitor.js + revenue-block.js: Produced=TrueProduced Actual, Tracking=ScheduleBoard Total). Fixed monitor.js passing `sampleDay`→`todayPT` (was inflating tracking). Resent corrected COO email to jwade only (no exec CC).

### ✅ 7/24 PM — Herman PUBLISH build VERIFIED (works) + Eastvale field-population bugs found (Gilligan, live off play; Skipper on-screen)
Resumed the two open items. **CORRECTION:** my first pass queried `InventoryDetail.ProjectID` (unset on these rows) → wrongly reported "0 trees / gate missing / refresh wiped it." Trees resolve via `Projects.LocationID → InventoryDetail.LocationID`. Real state below (STATUS block in `gps-publish-info-card-mechanism.md` rewritten; 3 wrong LESSONS lines corrected).
- **PUBLISH works.** Eastvale proj 1105467 → **LocationID 1285096**, **313 trees**, **IsNewPlot=0 on all** (clear-step ran, likely direct SQL). Skipper confirms: only grey dots + info-card, no blue = target/"complete." apiCall.cfm generator contract-correct, backup kept, PLAY-only.
- **Real bugs = field population (Skipper's punch list, doing ONE at a time):**
  1. **GSTSID=NULL on all 313 → card "tree id 0".** Customer tags present in `LegacyRef` (2043–2355, 313 distinct). Fix = `LegacyRef`→`GSTSID`; safe (GSTSID is per-site, not unique — 10,365 existing hits on those values). ← addressing first, backup-first, awaiting Skipper OK.
  2. **Height numeric NULL** (HeightRange bucket set) → "height not a number."
  3. **Observations empty + address blank** on drill-down — believed to be in the source spreadsheet; needs staging→field mapping.
- **Landmine (Skipper has no visibility, I own it):** 197 orphaned `InventoryGPSModel` staging rows for proj 1094998 "USC Contract" (LA) = contamination hazard for any re-import. NOT touched (shared box). To isolate/back-up before next import.

### ✅ Eastvale drill-down fixes (Herman executes, Gilligan verifies) — progress
- **Tree ID (GSTSID) = FIXED + verified** (LegacyRef→GSTSID, 313/313, JSON regenerated).
- **Address = FIXED + VERIFIED CLEAN.** Herman replicated Irvine's 4-field model (StreetNumber + StreetName + StreetNameID→StreetNames + LocationStreetID→LocationStreets), created 2 streets (GOODMAN WAY 284156/184512 ×112; BELLGRAVE AVE 284157/184513 ×201), per-tree join by tag. My verify: **313/313 match the source sheet on street AND number, 0 mismatch, 0 orphan FK, 0 unmatched tag**; backup `Workbench.dbo.InventoryDetail_bak_address_1285096_20260724` (313). He also self-caught 2 NULL-GSTSID stragglers (2151, 2161). (My own miss: I sampled tags 2043-2360 and misreported the street split — real set runs to 2937. → LESSONS.)
- **Observations = NOT broken — CONFIRMED by Skipper.** The records show in the real **Observations tab**; he'd been looking at the Details-tab "Observations:" field (=`LineNote001`, import ignores → blank). Closed, no fix.
- **Height = FIXED + VERIFIED.** Skipper wanted the exact spreadsheet bands shown. Dropdown/card are scoped to the location's HeightModel, and the default ("USC" ID2) is shared by 35 locations → can't touch it. Herman created a dedicated **HeightModel 11 "Goodman Survey (10ft bands)"** + 4 ranges (IDs 72-75: 0-10/11-20/21-30/31-40 ft), repointed location 1285096, set all 313 by tag. My verify: **313/313 heights match the sheet, 0 off-model, dropdown shows the 4 bands**, JSON regenerated (`i`="11 - 20 ft"). Backups: `..._bak_height_...` (313) + `Locations_bak_heightmodel_...` (old ID2).
- ✅ **Eastvale drill-down punch list COMPLETE:** Tree ID · Address · Observations · Height all done+verified.
- **Landmine backed up + assessed:** 197 USC-Contract (1094998) staging rows = **2014 leftover, all already in InventoryDetail** → safe to clear. Backed up to `Workbench.dbo.InventoryGPSModel_bak_USC1094998_20260724`. ⏳ **Awaiting Skipper nod on the DELETE** (`DELETE FROM dbo.InventoryGPSModel WHERE ProjectID=1094998`). Not urgent (only bites at next import).
- Full field-gap recipe (GSTSID/address/height/observations + drill-down page map) captured → `trimit-knowledge/procedures/gps-inventory-import-pipeline.md`. Reusable for the other 27 Goodman properties.

### 🧪 Herman front-to-back TEST #2 — El Monte Bldg 1 (STRONG PASS, 1 gap)
Skipper had Herman build a 2nd property solo (chosen to test generalization: new LA County, 15 species). Herman created **project 1105468 "GLC El Monte Bldg.1 (2026)" → new LocationID 1285097** from scratch. My tag-by-tag verify vs the sheet answer key (`inbox-pull/goodman-rfp-2026-07-22/elmonte-bldg1-verify-answerkey.md`):
- ✅ 298 trees, tags 4382-4795, 0 dup GSTSID; GSTSID=tags; IsNewPlot cleared (grey); Condition/LZR/lat-lng 298; SpeciesRef set / Desc1 NULL (correct).
- ✅ **Height: 298/298 match sheet by tag** (dedicated HeightModel 17). Nit: he made 4 bands but El Monte only uses 3 (unused "31-40 ft" #99, 0 trees — templated from Eastvale).
- ✅✅ **Species 15/15 resolved correctly incl. the hard aliases** — Christmasberry→Toyon, Afghan Pine→Pine-Eldarica, Chinese Flame Tree→Koelreuteria-Bipinnata, Goldenrain→Koelreuteria-Paniculata, African Sumac→Rhus. The standout — nailed the trickiest step.
- ❌ **ADDRESS INCOMPLETE (the one gap):** set StreetNumber+StreetName text (4300 NORTH SHIRLEY AVE, correct) but **StreetNameID=0/298 and LocationStreetID=0/298** — skipped the two FK links (didn't link a StreetNames row, didn't create LocationStreets). Regression vs his own Eastvale build (where he did all 4). Fix = the Eastvale address recipe steps 3-4. → next Herman hand-off.

### ⏳ El Monte — TWO Herman fixes IN FLIGHT (verify when he reports) — RESUME HERE
Skipper handed Herman both prompts; he's working. When he reports, verify against `inbox-pull/goodman-rfp-2026-07-22/elmonte-bldg1-verify-answerkey.md` (project 1105468 / LocationID **1285097**):
1. **Address FKs:** expect StreetNameID + LocationStreetID now 298/298 (create/link "NORTH SHIRLEY AVE" in StreetNames + LocationStreets), FK 0-orphan, drill-down shows "4300 NORTH SHIRLEY AVE, El Monte".
2. **Pricing worksheet:** was blank ("no counts, no prices"). NOTE: DB counts EXIST (InventorySummary=15, 298 trees; Price=0 is normal pre-Price-Buddy) → it's a worksheet GENERATION issue. Verify the worksheet is fetched from **ProjectID 1105468** (El Monte's own — `ReportDev/Project$PricingWorksheet$...cfr?ZProjectID=1105468`) with **El Monte's address baked in** (NOT copied from another property — that trap = `genuine-pricing-worksheet-overlay.md`), and shows the 298-tree counts by species/service.

### 🧠 BRAIN UPGRADED → Opus 5 (2026-07-24)
Anthropic released **Claude Opus 5** today (model id `claude-opus-5`). Verified real + reachable (tested on Claude CLI v2.1.161 → clean response). Updated `openclaw.json` (backup `openclaw.json.bak-preopus5-20260724T200145Z`): registered `anthropic/claude-opus-5` (alias `opus5`, claude-cli backend) + set `model.primary` = it + added provider def; JSON re-validated. Session flipped live via override (was drawing manual anthropic token; persistent default routes claude-cli). **Doing `openclaw gateway restart` now to make Opus 5 the live system-wide default.** Post-restart: I should be on Opus 5 for ALL sessions/crons/subagents. If gateway doesn't come back, restore the backup.

### 🔍 Publish/info-card — Kimi K3 crew win
K3 reverse-engineered the whole grey/card layer from ~2,500 lines: grey dots = pre-generated GeoJSON in `/GSTS/API/JSON/` (apiJS→apiCall→Taffy), gated server-side (excludes IsNewPlot=1); blue = SelectedPoints (IsNewPlot=1). windowControl/FusionTables = orphaned red herring. Publish = clear IsNewPlot + generate the GeoJSON. PLAY fix (Taffy 404 + stub proc + xp_cmdshell off): patch apiCall.cfm to generate GeoJSON via `<cffile>` locally (dir+icon already exist on play). ALL verified against code+server. Herman now building it.

### 🛠️ Tooling gotchas hit (see LESSONS)
Detached crew runs: `pkill -f "kimi-ask.py"` self-kills the launching command (matches its own cmdline). The 180s no-output watchdog SIGKILLs silent reasoning-model runs → launch DETACHED (nohup, return immediately), poll the output file. Kimi K3 crew script: `python3 ~/arbor-core/crew/kimi-ask.py <file>`; slow (~6 min on 168KB).
