# 2026-06-09

## 📨 Phase 2 (per-salesperson emails) — DATA MODEL LOCKED (probe run Jun 9/10)
- **Rep email = `flow.Users.UserEmail`; rep name = `SalesReps.FullName`** (join `SalesReps.UserID = flow.Users.UserID`). No First/Last on flow.Users; SalesReps has First/Last/FullName. (StaffMembers has WorkEmail/PersonalEmail as a fallback source.)
- **Job→salesperson:** WorkOrders.ProjectID → Projects.SalesRepID → SalesReps → flow.Users. **Spot-check of 5 recent WOs maps to real reps w/ @gstsinc.com emails + sensible future scheduled dates** (awaiting Skipper's confirm the rep = who SOLD it).
- **WorkOrders has rich fields:** StartDate, DateScheduled, HardStartDate, PercentComplete, DateCompleted, CompletedTPH/Hours/Dollars, ScheduledTPH/Hours, StatusDefID. → scheduled-start = min CrewSheets.CalDate (validated) and/or WO.StartDate; completion = PercentComplete=100 / DateCompleted.
- **VIABILITY: 140 WOs scheduled to start next 30 days, ALL 140 have a SalesRepID (100%).** Feature works.
- **Email coverage: 46/61 reps have email**; the 15 without are mostly junk/dead reps (DELETE/UNDEFINED/UNK) BUT one real rep (Carlos Alcaraz, SalesRepID 1145) has none → **plan: skip no-email reps + report to Skipper.** SalesReps table has cruft (more DB-cleanup fuel).
- Reps are **@gstsinc.com (Microsoft 365)** → same Junk issue as Skipper's work email; pilot reps mark not-junk, rollout needs IT to allowlist gilligan.gsts tenant-wide.
- **✅ MAPPING VALIDATED (Codex read app source, Jun 10):** the WorkOrder Detail page's "Sales Rep" display uses the SAME chain (Profile.WorkOrder.Detail.cfm L67 + Synch.WorkOrder.Update.cfm L255: `Projects.SalesRepID → SalesReps`). **WO 166727 = Garrett Cornish (gcornish@gstsinc.com) via both our chain AND the app — match.** So `Projects.SalesRepID` IS the authoritative selling rep. Skipper's "data seems off" was the SAMPLE (my "5 most recent WOs" = recently-CREATED, many far-future multi-year plans e.g. 166727 starts Jan 2027) — dates looked weird, reps were right.
- **Note:** Projects has multiple rep roles — SalesRepID (=app "Sales Rep", what we use), FieldRepID, InsideRepID, AccountRepID, PlannedByID. Could add field/account rep as extra recipients later if Skipper wants.
- **CONTENT SPEC (Skipper 6/10):** ONE daily email per rep, 4 sections — UPCOMING(next 2wk: job+crew+schedTPH), STARTING TODAY(job+crew+schedTPH+duration), IN PROGRESS(actual TPH + COO color bands 🔴<120/🟡120-129/🟢130+ vs $130 & vs scheduled), RECENTLY COMPLETED(last 7d: $ + final TPH). WorkOrders rolls these up at job level (ScheduledTPH/CompletedTPH/CompletedDollars/CompletedHours/PercentComplete/DateCompleted) — no crew-math needed. Defaults: 7-day completed lookback, daily cadence, skip no-email reps.
- **PILOT = Rebekah Barker + Scott Griffiths** (DB spells her Rebekah, not Rebecca). **Pilot delivery → emails go to SKIPPER first** ("here's what each rep would get") for review, THEN flip to real reps. Scott email sgriffiths@gstsinc.com ✓.
- **ENDPOINT `metric=salesperson_jobs` BUILT (Codex, Jun 10, backup `MonitorData.ReadOnly.cfm.20260609-214735.bak`):** `?metric=salesperson_jobs&date=` → JOBS[] with bucket (completed/starting_today/in_progress/upcoming) + rep+email + crews + scheduled/actual TPH + $ + dates. Verified date 2026-06-09: completed 59, starting_today 12, upcoming 63 — sane; **in_progress 1443 = TOO BROAD (stale never-completed WOs).** → sent Codex a REFINE to scope in_progress to WOs with a CrewSheet CalDate in the last 14 days (genuinely active). **Re-verified: in_progress 1443→29** (completed 59, starting_today 12, upcoming 63). Sane.
- **ENGINE BUILT: `anomaly-monitor/salesperson-monitor.js`** — fetches `metric=salesperson_jobs`, groups by rep, renders 4-section outline (📅 Starting Today / 🔧 In Progress color-coded vs $130 / ✅ Completed last 7d $+TPH / 🗓️ Upcoming 2wk). Flags: `--print` (stdout), `--reps="A,B"` (pilot filter), `--preview-to=` (default = email Jason for review), `--live` (email each rep direct). Cleans crew names (strips (x/y) codes, dedupes, drops EMPTY/PRODUCTION placeholders, caps "+N more"). Handles dirty TrimIT ScheduledTPH (0→"TBD", >600→⚠️). Skips no-email reps + reports them.
- **PILOT PREVIEW SENT to Skipper (Jun 10)** for Rebekah Barker + Scott Griffiths (both have emails; rbarker@gstsinc.com). Awaiting Skipper's approval/tweaks → then `--live` to the 2 reps (+ get IT to allowlist gilligan.gsts on the gstsinc M365 tenant so it clears Junk). **NOTE: TrimIT ScheduledTPH is unreliable on some WOs (saw a 5000, several 0) — TrimIT data issue, flagged in display.**
- **REVIEW FINDING #1 (Skipper, Jun 10):** the feed "customer" is often the property-MGMT company (Optimum, Powerstone, Seabreeze, La Perla, Walters) shared across MANY HOAs; the actual job SITE is in `workOrderDesc`. Showing only "Optimum" made jobs indistinguishable. **FIX: added `jobSite()`/`jobLabel()` to salesperson-monitor.js → each line now "Optimum · Knott-Lampson HOA".** TPH numbers were CORRECT (Knott-Lampson genuinely 55 = $2,395/43.6h). The completed TPH = CompletedDollars/CompletedHours, internally consistent.
- **TERRITORY tags added (Skipper, Jun 10):** endpoint now returns `territory` per WO (Codex; `CrewSheets→CrewNames.SiteAssigned` 1=N/2=S/else Other, weighted by ActHours; dist N67/S79/Other17). Engine tags each job line **[N]/[S]/[–]** (– = no crew assigned yet). Skipper wanted per-line tag, NOT grouping. Preview re-sent.
- **MANAGER ROLLUP added (Skipper, Jun 10):** Nate manages **Scott Griffiths + Ethan Chesley + Rebekah Barker + Garrett Cornish** — wants ONE compiled email of all 4 to Nate. Built `--manager="Nate" [--manager-to=email] [--live]` mode in salesperson-monitor.js → renders `renderManager()` = AT A GLANCE team summary (today/in-prog+reds/completed $/upcoming counts) + each rep's full section. Preview sent to Skipper. All 4 reps have @gstsinc.com emails (Ethan echesley, Garrett gcornish). Garrett has 57 jobs (31 upcoming) → long section. **RESOLVED (Skipper Jun 10): each rep ALSO gets their own email + Nate gets the rollup; CAP the rollup; Nate = nperkins@gstsinc.com.** Added `opts.cap` to renderRep (manager rollup caps each section to 5 + "…+N more"; individual emails uncapped). `--print` now honors `--manager`. **Team config: `anomaly-monitor/config/salesperson-pilot.json`** (liveEnabled:false, previewTo=Jason, reps + Nate@nperkins). Both previews (4 individual + Nate rollup) sent to Skipper.
- **CHECKPOINT + DAILY PREVIEWS (Skipper Jun 10):** Skipper put prod-endpoint deployment ON HOLD; wants to **vet the salesperson/Nate emails to his own inbox daily** before going live. Built **`run-salesperson-preview.sh`** (reads config, preview-to-Jason unless liveEnabled) + **crontab line 6:35am PT** (13:35/14:35 UTC, guarded), runs right after the 6:30 COO report. Both COO + salesperson previews now arrive daily to wade3337@gmail.com + jwade@gstsinc.com. Wrote **`anomaly-monitor/CHECKPOINT.md`** (full system snapshot). ⚠️ Reminder: all data is from **PLAY** (lags prod) — recommended Skipper deploy endpoint to PRODUCTION before salesperson go-live (on hold).
- **GO-LIVE PATH (pending):** (1) Skipper approves previews; (2) **IT allowlists gilligan.gsts@gmail.com tenant-wide on gstsinc.com M365** so it clears Junk for all @gstsinc recipients; (3) flip config liveEnabled + run `--live` (each rep → own inbox) and `--manager="Nate" --live --manager-to=nperkins@gstsinc.com`; (4) schedule daily 6:30am cron like the COO email.
- **REVIEW FINDING #2:** Skipper's screenshot job (The Oaks at Lake Forest HOA, WO 167613, ~129 TPH) is **Garrett Cornish's**, NOT Rebekah's — so it correctly wasn't in her email; he'd compared two different reps' Optimum jobs. Mapping is sound. Corrected preview re-sent.

## 🎨 Canonical GSTS UI theme captured (Jun 9)
- Skipper wanted his real built UI style as one reusable file (from `Profile$Main.HiRes.cfm`). Bot login dead on play, but the **CSS assets are publicly fetchable**: `/gsts/assets/css/gsts-tokens.css`, `/gsts/assets/protips/protips.css`, `/gsts/css/pricing-guide.css` (Price Buddy = the real component system, 15.7KB).
- **REAL palette (source of truth):** brand green **#5C743D**, gradient partner **#405528** (header gradient `135deg #5C743D→#405528`), deep **#2A3A18** (protip bg), pale **#D5EDB3**; page bg **#f7f9fb**, surface #fff, border #e5e7eb, text #1f2937; status good **#166534** / bad **#c62828**. Font = system stack. Mobile-first: **44px tap targets, 16px inputs**, 6px radius.
- **⚠️ The old v1 spec (`gsts-ui-spec-v1.0.md`) had GUESSED hexes (#5a7a3a) — superseded by the real values.** Also `/gsts/css/style.css` = OLD generic template cruft (blue #00a9ff, sliders, social footer) — NOT the brand; do not propagate.
- **Deliverables (in `workspace/files/`):** **`gsts-theme.css`** (drop-in stylesheet: tokens + `.gsts-*` component classes — appbar/section-header/card/table/btn/pill/segmented/kpi/badge/protip) + **`GSTS-UI-STYLE-GUIDE.md`** (usage ref). Apply to all TrimIT + Arbor UI; hand both to Codex/Herman when building screens.

## Herman → Gilligan file store (set up Jun 9)
- Herman needs to store larger project files with me. Chose **rsync over a locked dropbox** (Herman installed rsync; rsync = resumable/efficient for big files + no root/sshd-config changes on my secret-bearing box).
- **Setup (my server, no root):** dir `/home/wade3337/herman-store` (0700); Herman's ed25519 key (`herman-to-gilligan-backup`) added to `~/.ssh/authorized_keys` as a **FORCED-COMMAND** key: `command="/usr/bin/rrsync /home/wade3337/herman-store",restrict` → can ONLY rrsync (read+write) into that one folder, **no shell / no port-fwd / no access to my .secrets or anything else.** Connects as wade3337@76.32.188.157:22.
- **✅ VERIFIED WORKING (Jun 9):** Herman pushed his whole `arbor-ai-helper` git project (56 files, 424K — README, character/, plans/, prompts/, prototype/, docs/, scripts/) into `herman-store/arbor-ai-helper/`. Lockdown held (only that folder touched).
- **⭐ ROUTE = TAILSCALE, not public IP.** Herman's connection: user **wade3337**, dest `gilligan:arbor-ai-helper/` (path is RELATIVE to the locked store — do NOT prefix `~/herman-store/`). **My server is on Tailscale at `100.82.161.7`** (confirmed; iface tailscale0). The public IP **76.32.188.157 TIMED OUT** from Herman, so Tailscale is the working (and more private) route. First attempt failed because Herman connected as user `arduino` (no such user here) — fixed by using wade3337. `--delete` is blocked by the locked rrsync unless I enable it.
- Herman's storage strategy: Gilligan = heavy/private (screenshots/exports/backups), GitHub = version-controlled code/docs. Herman's project = the Arbor AI guide CHARACTER (Phase 1: look/feel/behavior); using my ARBOR_AI_CONTEXT doc. (Deliberately did NOT run Herman's original raw shell-access command → would've exposed gmail/trimit/monitor secrets.)

## 🐛 FIXED — multi-crew TPH accuracy bug (Skipper flagged from GSTS screenshot, Jun 9)
- **Problem:** when multiple crews work one job, TrimIT puts the call-in $ on ONE crew's sheet; other crews log hours with $0. Old monitor evaluated per-crew-sheet → one crew showed a fake huge TPH (e.g. Cushman job = 527.9 on Daniel Meza) and the others showed $0 (miscounted as "dollars-pending"). Headline TPH distorted.
- **Fix (monitor.js metric1):** aggregate per WORK ORDER (key=workOrder+customer), SUM dollars AND hours across all crews → job TPH = total$/totalHours. Crew label = "Lead +N more". **Validated: 6/6 aggregate reconciles to the penny with GSTS Production-Day page ($24,748.94 / 173.82h / 142.38 TPH).** Cushman job now 145.8 TPH (real), LB South 134.3 (was 234.8/0 split). Documented in METRICS_SPEC.md. Hardening option noted: add real workOrderId to endpoint so grouping doesn't rely on description string (string grouping reconciles exactly for now).

## Herman's real role clarified (Jun 9)
- **Herman is building an INTERACTIVE GUIDE CHARACTER that helps employees navigate Arbor AI** — NOT building the app structure/screens. He's the conversational evolution of the GSTS "Pro-Tip" help system.
- **Arbor's flagship "Municipal Tree Bid Manager" = an IMPROVED version of the existing Inventory Pricing tool ("Price Buddy")**, in the repaired TrimIT framework (NOT greenfield).
- Two context docs for Herman now in `workspace/files/`: `HERMAN_CONTEXT.md` (working style + hands-on background) + `ARBOR_AI_CONTEXT_FOR_HERMAN.md` (Arbor/TrimIT/domain/design-language background, re-aimed for the guide-character role). Open character questions for Jason: where it embeds, guide-only vs can-take-actions, AI backend + data access, persona/voice.

## ✅ BACKUP OUTAGE RESOLVED (Jun 9 ~3:50am PT)
- Codex confirmed the prod GSTS backup ran successfully again. Play caught up: data was frozen at Thu Jun 4, now flowing through **Sat Jun 6** (Jun 5=34 jobs, Jun 6=7; Jun 7 Sun no work; Jun 8 not in this restore yet — will keep catching up as daily backups now cycle).
- Sent the fresh full report to both inboxes (`--once-daily` marker set, so the 6:30 cron skips today's duplicate). Deleted the one-shot `play-freshness-check-postrestore` cron (moot — confirmed live).

## 🗄️ PROJECT KICKOFF — TrimIT Database Cleanup (AI-assisted, replacing devs' 1yr/$300k quote)
- **Why:** Skipper wants to clean up the GSTS database structure. His devs scoped it at ~1 year / ~$300k (mostly human labor of analysis). We're devising an AI-assisted plan that reuses the work already done.
- **Head start (already done, June 2–5):** read-only schema discovery, **948-table classification** ("noise map"), and the **6-item prioritized punch list** (`Arbor AI/Trim IT Repairs/TrimIT-Database-Health-Assessment.docx`). Discovery complete; cleanup NOT executed.
- **6 punch-list items:** (1) ~200 dead backup/snapshot tables (several GB) — verify code-unreferenced + zero inbound FK, batch-drop; (2) **missing foreign keys** (menu+dashboard tables) — clean orphans THEN add constraints [most important structural fix]; (3) ~180 "Unknown" tables — code-reference check each; (4) duplicate/drift artifacts (folder case, InvoiceLine vs InvoiceLines, page variants); (5) unbounded log tables — KEEP but add retention; (6) **app datasource runs as `sa`** (HIGH/LOW) — create read-only login.
- **Plan written:** `Arbor AI/Trim IT Repairs/DB_CLEANUP_PLAN_AI.md`. Phases: P0 setup/access → P1 safe wins (read-only login + log retention, zero deletions) → P2 verified cleanup (dead tables + 180 unknowns) → P3 structural (orphans→FKs, dedup) → P4 optional **data dictionary** (Arbor AI gold — TrimIT has NO plain-English schema docs).
- **AI/human split:** AI does code-reference tracing + orphan detection + dead-table proof + authors all SQL; **human owns backups + executing destructive changes + verification.** Backup-first, sandbox→play→prod. Nothing destructive automated.
- **Environments:** Skipper has **prod + dev + play** (3 DBs). Decided **NOT to copy the ~120GB DB to Gilligan's server** (size, data sensitivity, analysis needs the CF source which lives on their box). Instead → dedicated **`GSTS_cleanup` test bed**.
- **⭐ TEST-BED SERVER (Skipper offered):** a **spare server that USED TO RUN TrimIT in-house** — ideal (already Windows/SQL/CF capable). Will house the cleanup sandbox; on a throwaway sandbox we can actually TEST AI-written drops/FKs (re-restore if broken), not just draft them. **➡️ TODO: ASK SKIPPER FOR THIS SERVER'S SPECS (OS / RAM / free disk) — he's reporting them later today (Jun 9). He explicitly asked me to resurface this when we reach test-bed setup (after Codex capability report + Phase 0).**
- **CODEX CAPABILITY REPORT IN (Jun 9):** ✅ Codex can read+full-text-search ALL CF source (8,449 .cfm + 127 .cfc = 8,576 files; "CrewSheets" hit 634 files), pull ALL proc/view/trigger bodies (3,628 procs / 29 views / 157 triggers via sys.sql_modules), and read full catalog+data. Catalog: **917 tables, 22,339 columns, 1,094 FKs** (so FKs DO exist — Item 2 is about SPECIFIC tables lacking them, not total absence). **Login = `sa`/sysadmin** (confirms Item 6 from inside). **Codex reaches PLAY SQL only** (173.208.162.142:14333); prod (198.207.148.169) + dev (198.207.148.188) NOT reachable → all analysis on play. Junk already visible (aaaTravisNewTest123.cfm, CattleShipment module, $dev procs). Codex can create/restore DB (sysadmin) but treats as human-approved.
- **PASS 1 LAUNCHED:** wrote `Arbor AI/Trim IT Repairs/CODEX_PASS1_table_census.md` — read-only Table Usage Census over all 917 tables (rows, size, inbound/outbound FK, code_refs, module_refs, junk-name flags → DEAD-CANDIDATE/KEEP/REVIEW verdict; conservative bias). Output = evidence-backed verified-dead shortlist + GB reclaimable = the Phase 2 drop plan. **Codex is running it.** When results land → I turn the census into the batched drop plan. Analysis runs on play NOW, parallel to test-bed prep.
- **Test-bed server specs:** still pending from Skipper (former in-house TrimIT host).
- **PASS 1 RESULTS IN (Jun 9):** 918 tables → KEEP 620 / DEAD-CANDIDATE 298 (~5.65 GB). I pulled the census CSV **directly** (web-served at `play.greatscotttreeservice.com/discovery/trimit-table-usage-census-20260609-062007/table_usage_census.csv` — no manual upload needed; .md 404s but .csv serves). Tiered it: **A** zDelete-* 262 tbl/3.46GB, **B** dated backups 3 tbl/2.07GB, **C** import/temp 5, **D** other junk 2 → **272 high-confidence safe drops (~5.62GB)**; **26 normal-named PARKED for human review** (many look like planned/Arbor feature scaffolding — InventoryPricingHistory, MarketClusters, GSTSArborNoteSynonyms, NotificationSettings, etc. — do NOT drop without Skipper/dev OK). Codex's 14 junk-named-KEEP traps respected.
- **DROP PLAN written:** `Arbor AI/Trim IT Repairs/DB_CLEANUP_DROP_PLAN_PHASE2.md` + tiered list `dead_candidates_tiered.csv`. **Reversible method:** backup → `ALTER SCHEMA _graveyard TRANSFER` (quarantine, surfaces hidden callers) → soak → DROP. Order: test-bed → play → prod, backup-first. READY when test-bed is up.
- **Hygiene DONE (Jun 9):** Codex moved census artifacts out of the web root to `D:\trimit-analysis\discovery\trimit-table-usage-census-20260609-062007`. Old URL now 404s (verified) — schema map no longer web-accessible. My local copies (`/tmp/census.csv`, `dead_candidates_tiered.csv`) retained.
