# 2026-07-30

## OpenClaw / Codex model config
- 2026-07-30 00:49 UTC: Skipper reported `gpt-5.6-sol` stalling/failing in Discord. Local config had advertised `openai/gpt-5.6-sol`, but `openclaw infer model inspect --model openai/gpt-5.6-sol` returned model-not-found while `openai/gpt-5.5` resolved. Temporarily restored default to `openai/gpt-5.5`, removed 5.6 from the allowed picker/recents to avoid accidental selection, and backed up config/preferences as `~/.openclaw/openclaw.json.bak-pre-gpt56-disable-20260730T0049Z` and `~/.openclaw/discord/model-picker-preferences.json.bak-pre-gpt56-disable-20260730T0049Z`.
- Follow-up investigation after Skipper clarified 5.6 is available: `codex debug models` shows `gpt-5.6-sol` is available on the account. Correct OpenClaw model reference for this install is `codex/gpt-5.6-sol`, not `openai/gpt-5.6-sol`. Gateway test reached the model route but failed with: `The 'gpt-5.6-sol' model requires a newer version of Codex.` Current Codex CLI observed as `0.144.1`; OpenClaw update dry-run showed update path from `2026.6.1` to `2026.7.1-2`.
- Planned setup path for 5.6 Sol: run `openclaw update --yes --timeout 1800`, let gateway restart, add `codex/gpt-5.6-sol` back to allowed models with alias `sol`, test with `openclaw infer model run --gateway --model codex/gpt-5.6-sol --prompt 'Reply with exactly: SOL_OK' --json`, then make it selectable/default if the test passes. Current safe state at compaction: default remains `openai/gpt-5.5`; 5.6 provider catalog row is staged in config but not default/picker.
- 2026-07-30 01:35 UTC: Final working setup is `openai/gpt-5.6-sol`, not `codex/gpt-5.6-sol`, for OpenClaw model selection. Updated OpenClaw to `2026.7.1-2`, copied existing local Codex ChatGPT auth into OpenClaw's active `CODEX_HOME` so native Codex reports logged in, verified native `codex exec -m gpt-5.6-sol`, then found `codex/gpt-5.6-sol` still returns an HTML/CDN response through OpenClaw `model.run`. Switched allowlist alias `sol` to `openai/gpt-5.6-sol`, made default `openai/gpt-5.6-sol`, restored it to Discord model picker recents, and verified JSON parse plus live gateway tests for explicit `openai/gpt-5.6-sol`, alias `sol`, and default route.
- 2026-07-30 02:05 UTC: Anthropic/Claude repair after Skipper reported every Claude in the Discord picker looked like stale `claude-cli`. Root cause was picker/config entries still tied to expired Claude CLI runtime/auth, while direct `anthropic/...` gateway routes work. Current allowed Claude choices in `~/.openclaw/openclaw.json` are direct Anthropic routes: `anthropic/claude-opus-5` alias `opus5`, `anthropic/claude-sonnet-5` alias `sonnet`, `anthropic/claude-opus-4-8` alias `opus48`, plus `anthropic/claude-opus-4-7`, `anthropic/claude-sonnet-4-6`, and `anthropic/claude-opus-4-6`. Removed `agentRuntime: claude-cli` from picker-facing Claude model entries and cleaned Discord recents to direct routes only: `openai/gpt-5.6-sol`, `anthropic/claude-opus-5`, `anthropic/claude-sonnet-5`, `anthropic/claude-opus-4-8`, `openai/gpt-5.5`.
- Verification for the Claude repair: `openclaw config validate` passes; `openclaw infer model run --gateway --model opus5` returned `MEMORY_OPUS5_OK`; `--model sonnet` returned `MEMORY_SONNET_OK`. Earlier repair tests also passed for `opus48` and `anthropic/claude-sonnet-4-6`. The stale `claude-cli` auth profile/back-end still exists in config internals and may show as expired in diagnostics, but the working Discord picker routes should use direct `anthropic/...`, not `claude-cli/...`.
- Session pinning gotcha: `openclaw status` at 02:05 UTC says this Discord DM session `agent:main:discord:direct:1301226640130445323` is still pinned to `openai/gpt-5.5`, while config primary is `openai/gpt-5.6-sol`; new/unpinned sessions use Sol. To make this DM follow the config default, use `/model default` in Discord. Do not claim the current DM is on Sol until `openclaw status` shows the session selected model changed.
- 2026-07-30 02:25 UTC: Model-picker cleanup FINISHED and verified after two gateway restarts. The Skipper's complaint was that every Claude in the Discord `/model` picker was labelled "(Claude CLI)" — that was correct: the labels come from OpenClaw's *catalog augmentation*, not from config, so pruning `openclaw.json` alone could never remove them. Root cause: `dist/extensions/anthropic` registers `augmentModelCatalog: () => buildClaudeCliCatalogEntries()`, which injects `claude-cli/claude-*` rows with hard-coded "(Claude CLI)" names from `dist/cli-catalog-*.js` (`CLAUDE_CLI_MODEL_LABELS`) plus a `claude-cli` runtime choice in `dist/commands-models-*.js`. Fixed at all three layers (local hotfixes, each with a timestamped `.bak-pre-*` beside it): catalog builder early-returns `[]`, `augmentModelCatalog` returns `[]`, and the picker filters a `HIDDEN_CLI_RUNTIME_PROVIDERS` set.
- Verified this session, not from notes: `openclaw models list --plain | grep -c claude-cli` = 0; `models list --json` contains neither `claude-cli` nor the string `Claude CLI`; `grep -c claude-cli ~/.openclaw/openclaw.json` = 0 (auth.profiles/auth.order entries for `anthropic:claude-cli` are gone too); calling `buildModelsProviderData(cfg,'main')` directly returns providers `anthropic, ollama, openai` with runtime choices only for openai (`codex`, `openclaw`) and anthropic models `claude-opus-4-6/4-7/4-8/5, claude-sonnet-4-6/5`; live gateway runs returned ok=true for `anthropic/claude-opus-5`, `anthropic/claude-sonnet-5`, `anthropic/claude-opus-4-8` and for aliases `opus5`, `sonnet`, `opus48`, `sol`; `openclaw config validate` passes.
- So in Discord `/model` the Claude entries now appear only under provider **anthropic** with clean names (no "(Claude CLI)"), and choosing Opus 5 means `anthropic/claude-opus-5` (alias `opus5`).
- Billing answer for the Skipper: the working Claude routes use auth profile `anthropic:manual`, mode **token**, expires 2027-06-29 — i.e. a subscription-style setup token (`claude auth status` shows subscriptionType `max`), not a metered API key. The expired `anthropic:claude-cli` OAuth profile (expired 2026-07-29T23:00Z) still exists in the agent sqlite auth store and will keep showing "expired" in `openclaw models status`; it is now unreachable from the picker/config so it is cosmetic. There is no `models auth remove`; clearing it would need `openclaw models auth login --provider claude-cli --force` (TTY) which would re-add it — deliberately left alone.
- ⚠️ The three fixes live in `~/.npm-global/lib/node_modules/openclaw/dist/` and **will be wiped by the next `openclaw update`**. If "(Claude CLI)" reappears in the picker after an update, re-apply: `cli-catalog-*.js` → `buildClaudeCliCatalogEntries` returns `[]`; `register.runtime-*.js` → `augmentModelCatalog: () => []`; `commands-models-*.js` → add `HIDDEN_CLI_RUNTIME_PROVIDERS = new Set(["claude-cli"])` and filter it out of the visible-provider check, `cliRuntimeProviders`, and the runtime-choice list. Also still patched from earlier: `register.runtime-*.js` `applyAnthropicSonnet5Cost` needs optional chaining on `params.model.cost` or `models list` crashes.
- Also observed: two gateway restarts (02:03 and 02:09 UTC) interrupted this work; the 02:03 one killed the turn with `codex app-server client closed before turn completed`, and a config reload at 02:11 deferred its restart ~4 min waiting on the recovery task. During that window a stale request tried `claude-cli/claude-opus-5` and correctly got `Model override "claude-cli/claude-opus-5" is not allowed for agent "main"`.
