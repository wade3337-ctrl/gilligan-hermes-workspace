# 2026-07-13

_(Cockpit thread continues from `2026-07-12.md` — rename/protips/welcome, logic reconcile, running-dry rebuild, re-sell flag, map-dot + color fixes, "Jump to project" relabel are all logged there as ship-log #147–157.)_

## Sales Cockpit — split "Working" into Working + Scheduled (ship-log #158)
- **Skipper caught it:** Glenbrook Townhomes showed in **Working** but "last work 11/05/25." Asked how Working is figured. Root cause: the board put *any* account with an active work order (StatusDef 46/109) into Working — including WOs **booked for a future date** (a recurring account's next scheduled service), which isn't a crew on-site today.
- **Validated before building:** of 239 "Working" accounts, only **52 are in-progress now** vs **187 scheduled-ahead**. Glenbrook = ProjectID 1091177, WO 166632 Active with **StartDate 2026-10-12** → scheduled, not working. Confirmed my read of the data.
- **Skipper picked option 2** (tighten Working to current/in-progress; future-scheduled becomes its own state).
- **Built:** SQL `WInProgress` (active WO with StartDate ≤ today OR NULL) + `ScheduledFor` (MIN future WO StartDate). `stageOf` → in-progress = **Working**, active-but-future = new **Scheduled**. New 6th column **📅 Scheduled** (teal `#0d9488`) between Won and Working; teal map dot + legend; "Scheduled for <date>" status line; welcome-modal bullet; `cockpit.stageScheduled` protip; reworded Working copy ("a crew is on the job now"). `aisummary` counter split (`active`=52 / `scheduled`=187).
- Deployed D:\ (backup-first — server backup dir is *inside* the webroot: `.../wwwroot/GSTS/Jasonsrepairs`, not `D:/GSTS/...`; git HEAD version placed as the rollback copy). **0 CF errors**, 6 columns render with distinct colors, counts reconcile (52+187=239 = old Working lump). Committed arbor-stack.

## V1.5 deploy package — pre-ship crew audit + 3 repairs (ship-log #159)
- Skipper: reconcile the package with all our work, apply deployment rules to every dashboard, have the crew verify, **report findings before committing.**
- Did completeness + rules pass myself (found + pulled 2 play-only .cfm into repo), then ran a **3-agent adversarial audit** (completeness · security/gate · render+dual-webroot). Reported findings; Skipper said "do all three repairs."
- **🔴 Real leak fixed:** `SalesProductionMeeting$Drill.cfm` — auth gate was stranded inside the `soldApproved` `<cfcase>`, so every other `metric=` returned 200 + real customer/$ data to a garbage cookie. Moved gate above the `<cfswitch>`; all metrics now 403. **New lesson logged** (gate before the switch; test every URL-param branch).
- **🟡 SalesGoals hardened:** added shared entry gate (consistent 403) while keeping exec-only write check. **🟢 Repo completeness:** pulled 3 brand assets (`pro-tips.js`, `tree-helper-v1.png`, `gsts-tokens.css`) + the 2 .cfm into the repo.
- Crew confirmed the rest clean (24 surfaces + 5 aisummary all 403 garbage; 25 pages 0 CF errors; 3 shadowed files match doc; assets `?v=`-tagged). Committed arbor-stack + workspace lesson.

## Sales Cockpit — retired the "Won — schedule" stage (ship-log #160)
- Skipper on the map: an account bid in 2018 still showed purple "Won — need to schedule." Probe proved the whole purple column was dead data: `dbo.GoAheads` abandoned ~2012 (299 approved ever, all 2008–2012; all 73 Won cards were ghosts, 0 recent). No live "won/accepted" proposal status exists — a win today = a work order appears (→ Scheduled/Working). So Won was dead AND redundant.
- Skipper: "go with your rec" → **retired the stage.** Removed from stageOf/STAGES/buckets/column/legend/colors/statusLine/welcome + aisummary won counter; dropped go-aheads from `IsActive` so ghosts fall off. Board now **5 all-live columns**: Follow-up · Bidding · Scheduled · Working · Done. Total 740→682 (58 ghosts dropped, ~15 reclassified), reconciles, 0 CF errors. Committed.
- Tiny follow-up left: re-sell SQL still reads `ga.dt IS NULL` (harmless dead-go-ahead dep) — can drop later.

## 📌 PINNED (2026-07-14 00:10 UTC) — City Budgets "Renewals" feature (design done, build NOT started)
Skipper found: cities vanish from the Forecasting tab because it defaults to the current FY and most contracts aren't renewed yet (only 9 of 75 funded for their current FY). Design agreed over several rounds:
- **Forecast page stays clean** (current FY only). **Don't** do inline fallback-to-last-year (that's noise).
- **New 3rd tab "Renewals"** on Dashboard-CityBudgets.cfm (next to City Budgets / Forecasting): all 66 renewal candidates, **split 🏛 Municipal / 🏢 Commercial** (IsMuni = ProjectGroupDefID 11, matching other dashboards). Working-with-no-budget flagged red at top; sort urgent→dormant.
- **Main forecast (all-cities) surfacing:** accounts we're actively BILLING with no current-year budget appear with **Budgeted $0 → Remaining negative (red)** = "doing unbudgeted work" alarm. (~7 with real spend: muni Fountain Valley $16.7K, Cypress $15.5K; commercial FirstService $35K, Seabreeze, Crummack, Keystone, Mount St Marys.)
- **Detection MUST use the FY helpers** (`citybudget-fy-helpers.cfm`: cbFyStartFor + cbResolveCurrent) — a naive SQL label-match gave false positives (City of Industry has a $666K budget under calendar label "26", not "26/27"). Accurate scope: **75 accounts, 9 funded, 66 renewals — Municipal 7 (3 working/4 dormant), Commercial 59 (11 working/48 dormant).**
- Build correctly in CF reusing the forecast engine for exact per-city current-FY $. Then gate + render-verify + **screenshot for Skipper approval before committing.** Nothing built/committed yet.

## ▶ RESUME POINTER
- **Cockpit SIGNED OFF by the Skipper (2026-07-13 14:14 UTC).** Full visual/logic pass done through the Working/Scheduled split (+ same-session blank-board hotfix, #158). The whole fleet mobile + pro-tip + welcome walkthrough is complete for all 6 dashboards + the Cockpit.
- **Next up:** resume the **DEPLOY-PACKAGE-V15-DASHBOARDS.md** walkthrough at **Section 4 (the two — now three — DB steps)**, which is where we paused before the Cockpit deep-dive.
- Still deferred (need explicit OK): apply `deactivate-rep-chase-PROD.sql`; "Sales by Rep → Invoiced Revenue by Rep" rename (offered); the auto-RFP live-pipeline write (parked, kanban card #48).
- Ship-log through **#158**. All committed (arbor-stack).
