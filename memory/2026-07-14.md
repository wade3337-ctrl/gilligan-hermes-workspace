# 2026-07-14

## Steve's accrual reconcile — June unbilled revenue + report replacement (ship-log #161)
- Steve (CFO) pulled June from the live `Exec$PercentagePerformed2.cfm?ZPeriodID=333` to check unbilled revenue; saw July-dated jobs and flagged that **only municipal was off** (commercial = what he expects).
- **Traced it:** period 333 is correctly June 1–30 (no July calendar mis-mapping). The July rows are **in-progress jobs** (June crew-sheet, WO EndDate in July). The proc `Report$PercentagePerformed_npr2` selects by crew-sheet-in-period and just *displays* the WO EndDate.
- **Two findings:**
  1. **(Steve's issue) Municipal phantom accrual** — the proc accrues cycle/EOM-billed municipal cities (Irvine/Industry/Newport/Stanton) that are billed as performed. June municipal accrual **$154,334 → $8,494.71** corrected (phantom **$145,839**; upon-completion cities Aliso Viejo/Cypress/Fountain Valley/San Clemente still correctly accrue). Commercial **$285,471.14 unchanged** (matches Steve). Grand **$439,805 → $293,966**.
  2. **(separate, documented)** the accrual denominator (`WOHours`) isn't capped at period-end, so closed-month accruals **drift down** as later hours post. June accrual as-of-6/30 was **$505,005** vs **$346,694** live on the OPEN-jobs cut — a $158K swing. Not Steve's concern now; flagged for a future proc fix.
- **Action (Skipper: "replace the broken one so Steve just clicks the link"):** ported the billing-cycle-corrected INLINE logic from `Exec$PercentagePerformed2$NEW.cfm` INTO the original `Exec$PercentagePerformed2.cfm`, adapted to the original `?ZPeriodID=` param (+ picker→ZPeriodID, export link, last-closed-month default). Steve's existing link now returns the corrected report — no `$NEW`/`?period=` to know. Verified June + default + CSV + other periods, 0 CF errors. Backup-first, play D:\ only, logic inline (survives nightly refresh). Committed #161.
- Audit artifact for Steve: `steves-projects/financial-report-reconciliation/june-2026-unbilled-open-jobs-audit.csv` (13 open jobs, billing-cycle + corrected accrual columns).

## ⏳ PROD follow-up (open)
Prod still runs the old proc — Steve pulling from **prod** still sees the phantom. The fix reaches prod via the dev/IT handoff: either deploy this inline CFM to prod, or patch `dbo.Report$PercentagePerformed_npr2`. (While at it, consider capping the hours denominator at period-end to stop the drift.)

## 📌 Still pinned from 2026-07-13
City Budgets **Renewals** 3rd-tab feature — design done, build not started (see 2026-07-13.md pin).

## NEW PROJECT — RGC (Revenue Goal Close) dashboard — spec review (walkthrough in progress)
Hermes sent a big crew build spec + full technical design for a native V1.5 **Revenue Goal Close** dashboard (production-goal tool: "on track to PRODUCE $24M?" — distinct from Revenue Performance's invoiced revenue). `Calendars` = dollar authority; count-once stages (Produced > Future Scheduled > Approved-unscheduled > Pending > Municipal); read-only; custom objects in Workbench `rgc` schema. 6-phase gate ladder (Phase 0 contracts → 1A SQL → 1B CFM pilot → 2 municipal fcst → 3 capacity/targets → 4 prod). Crew: Gilligan(tech lead)/GBT(engineer)/Fable(UX)/Judge(acceptance)/Hermes(verifier)/Jason(authority).
**Decisions locked with Skipper (2026-07-14):**
1. **Architecture = proc-based `rgc`** (procs/views/config tables in Workbench) — the 30-test/snapshot/drill-reconcile framework needs it; single-source-of-truth avoids the "3 copies missed one" bug class.
2. **Sequencing = RGC deploys LATER, own track** — current V1.5 package ships now; RGC joins the PLAY fleet via a named-user pilot link (Task 10), prod is its own Phase-4 deploy.
3. **Fixtures = verified.** Keystone reconciled to the PENNY vs live Calendars: Produced (EstValue ≤7/13) = **$12,215,995.79**, Future Sched (ScheduledValue >7/13 <2027) = **$2,766,705.23** → hard coverage $14,982,701.02, gap $9,017,298.98. Foundation trusted. (Pipeline/allocation fixtures still = Phase-0 Gate-1/Judge work.)
**De-risk found:** blocker #1 (runtime reads Workbench) likely already OPEN — our dashboards read `Workbench.dbo.SalesGoal` via the GSTS DSN daily; blocker #2 (goal contract) — SalesGoal fields (FiscalYear/MonthNum/Goal) already known (we built it #123).
**Status:** walking the spec top-to-bottom with Skipper before any Phase 0 build. Nothing built yet. Files: RGC_BUILD_SPEC + RGC_TECHNICAL_DESIGN (Hermes, untrusted-external, treated as spec-to-review).
