
## TRIM IT V1.5 Roadmap session (Jun 18, morning)
- Built the master plan: `arbor-stack/TRIMIT-1.5-ROADMAP.md` + LIVE tab "▶ V1.5 Roadmap" on `Reference-RepairsAndScheme.cfm` (play; opens to it by default).
- Grounded in the **system-wide Menu Audit** (`Arbor AI/A.I/TrimIT Menu Audit — Jason Wade Profile.pdf`, May 8, all 119 links) + our logic audit + strategy.
- **Cost model:** Jordan Kim = IT mgr, SALARIED ($0 marginal) → does menu/AppForms/deploys (Tracks 2–5). Travis/Data Processing = $75/hr, ONLY deep DB/security. → whole V1.5 ≈ **$1.5–2.5K** out of pocket.
- **Priority = SALES first.** But Skipper reframed: dashboards are surface; the FLAGSHIP sales issue = **re-engineer the bid/"traveler" creation workflow** (too many people + duplicate entry + admin↔sales back-and-forth). Reference product to copy = **ArborNote** (tree-care SaaS; its proposal/estimate module is the target pattern).
- Sales funnel pages (Pipeline/Command/Overview) likely **CONSOLIDATE** into one rep/manager one-stop-shop → design-first, don't fix in isolation. Menu redesign vision: Find→SALES (Sales+Marketing), go-ahead→Production. Both = separate design talks.
- Revenue Performance = ship ~Jun 19 (standalone, near deploy). SPM suite deploy package ready (`dev-tasks/spm-prod-deploy.md`) BUT Skipper not sure SPM is ready → NOT deploying yet.
- **PINNED awaiting Skipper:** the bid-process SOPs + ArborNote proposal-flow screenshots/screen-share + an admin walkthrough. (Holding the ArborNote login — no headless browser + SaaS edge blocks my IP; screenshots/SOPs are the better path.)

## TREE-6138 WO cleanup recheck — PASSED (Jun 18 17:00 UTC)
Re-ran the 255-WO reconciliation on 100.86.97.46 (read-only diagnostic, not the close-out file — avoided the backup-table side effect). REPAIR 1 (former-rep WO cleanup) NOW landed on prod after the latest refresh:
- Revised(315)=0 (was 185), Complete-undated(48/null)=0 (was 39), Inactive(47)=216, total matched=255.
- Proof table dbo.zBackup_WO_Cleanup_20260613 present; trigger WorkOrdersPostUpdate is_disabled=0.
All PASS criteria met. No flag email to Travis needed — closed out. (REPAIR 2 / Contract Dashboard had already landed Jun 17.)

## Workspace 5-layer restructure (Jun 18 PM) — DONE
- Reorganized to Skipper's 5-layer model: 1 Identity (root .md, boot-loaded) · 2 Routing (`ROUTING.md` — read first) · 3 Stage contracts (`contracts/`: repair, dev-handoff, db-repair, external-comms) · 4 Reference (`reference/` — old `files/` moved here + INDEX.md) · 5 Artifacts (`~/arbor-stack/`, left in place). AGENTS.md now says "read ROUTING.md first"; MEMORY.md HOW-WE-WORK records the convention.
- **Resume points for next session:** (1) PINNED — bid/"traveler" re-engineering: awaiting Skipper's SOPs + ArborNote proposal-flow screenshots + admin walkthrough. (2) Revenue Performance = ship ~Jun 19. (3) Sales consolidation design + Find→SALES menu redesign = design talks pending. (4) Roadmap = `~/arbor-stack/TRIMIT-1.5-ROADMAP.md` + live tab. (5) Auto-crons still pending: WO-cleanup recheck (was due Jun 18 10am PT), Fri Jun 19 prod-deploy check, Jun 20 dev-queue, Sep 12 token rotation.

## Stale-proposal cleanup — scoping + critical schema finding (Jun 18 PM)
- Skipper approved the cleanup. Decisions LOCKED: sent-but-dead → existing **Lost (StatusDefID 141** for proposals; 156 is another entity's Lost — verified). Never-sent drafts → NEW status "Abandoned - Draft". Thresholds: sent 180d cold, drafts 365d cold. Reversible via dbo.ProposalArchiveLog. Reuse Lost per Jeanie (don't invent a parallel "Archived-Stale").
- 🚨 **SCHEMA LANDMINE (verified live on play):** Proposal **StatusDefID 149 "Locked" = WON (converted to a go-ahead)** per Skipper. `dbo.Proposals.LastGoAheadDate` is UNRELIABLE — null even on won deals. The ONLY trustworthy "was it won" test = **EXISTS a row in dbo.GoAheads (g.ProposalID = p.ProposalID)**. Proved: all 13,452 Locked-sent-cold proposals have a GoAhead row (0 without). NEVER use LastGoAheadDate or treat Locked as open. Open/sweepable status set = {41 Active,40 Pending,106 InProcess,334 Revised,243 Closed} — NOT 149.
- **Corrected verified counts (vs the brief's bogus ~57K):** Pop A sent-dead→Lost = **852** only (reps already marked the real losers Lost = existing 9,451). Pop B never-sent drafts→Abandoned = **7,716** (mostly Pending 6,905 + InProcess 799). Total sweep ≈ 8,568. Win-rate distortion = negligible; real payoff = clearing the 7,716 abandoned drafts from pipeline clutter.
- Earlier "57%→18% win rate" framing was based on the brief's broken numbers — retracted. 43,637 proposals are go-ahead-linked (won).
- NEXT: on Skipper's go, build dbo.ArchiveStaleProposals proc w/ corrected logic, test on PLAY (backup→preview→apply→verify→rollback), then hand tested script to devs for PROD. DB access = gsql.sh (read-only) on gstsdatabase play.

## 📌 PINNED — "STEVE'S PROJECTS" (Steve = the CFO; CFO-flagged report-accuracy work) (Jun 18 PM)
**Home (RENAMED Jun 18): `~/arbor-stack/steves-projects/` + `INDEX.md`.** Skipper wants CFO-flagged items collected here. Reconciliation work now at `steves-projects/financial-report-reconciliation/` (was `financial-report-truth/`). DB access = `production-dashboard/gsql.sh` (read-only play SQL).

### Project A — Dashboard reconciliation vs CFO Financial Report
**Goal:** use the CFO's `FinancialReportDashboard.cfm` (Steve says it's correct) as GROUND TRUTH to verify/fix all our dashboards' numbers.
- **CANONICAL definition captured** (`CANONICAL-DEFINITION.md`): correct revenue = SUM(Invoices.Total), invoice status IN (InProcess,Pending,Open,Paid,Locked), anchored to accounting PERIOD (Periods.StartDate month via Invoices.PeriodID), requires WO+Proposal+Project+Company (drops 0 in 2026). **Benchmark 2026:** Jan 1.618M, Feb 1.700M, Mar 1.687M, Apr 2.179M, May 1.898M, Jun(partial) 0.654M; YTD ≈ $9.73M.
- **RECON #1 — Exec$Periods$Overview.cfm = 🔴 CONFIRMED WRONG** (`RECON-01-...md`). Its Invoice column prefers stored `Periods.TotalPrice` over live invoices. `TotalPrice` = a "Period Close" snapshot, but closes are BROKEN: **April "closed" mid-month (Apr 22) → shows $1.224M vs real $2.179M = −$955,745**; Jan/Feb closed late (drift +7K/−19K); **Mar & May never closed** (fall back to live = correct). Root = inconsistent/premature period closes, NOT a clean freeze. CFO report is right because it ignores the snapshot & sums live.
- **BLAST RADIUS mapped** (`BLAST-RADIUS-PeriodClose.md`): pages reading Periods.TotalPrice — Tier1 exec dashboards (Exec-Performance-Period, Exec$Period$Snapshot, Exec$Periods$Targets, Exec$Invoices$Day), Tier2 **Exec$Commissions (pay implications!)**, Tier3 Client-Observations family (~8, client-facing), Tier4 Synch.*, Tier5 Profile$Capacity$ByPeriod. DB: year/company/contract rollups may have inherited bad April → annual figures suspect. No .cfr reports affected.
- **RESUME POINT:** Skipper approved order = work Tier 1 first, VERIFY each page's read-pattern (does it prefer stored over live?) THEN fix to compute live (= CFO), backup-first to Jasonsrepairs, UI fix we own → test on play → hand .cfm to IT for prod. A reference ≠ confirmed bug. Start with Exec$Periods$Overview (already confirmed). SEPARATELY flag broken period-close process to finance. **NOT STARTED — pinned by Skipper.**
- Source copies in `financial-report-reconciliation/src/`.

### Project B — Percentage Performed: municipal accrual (`financial-report-reconciliation/RECON-02-...md`)
`Exec$PercentagePerformed2.cfm` → proc `dbo.Report$PercentagePerformed_npr2`. Steve: report doesn't show municipal accruals right; accounting backs them out manually. 🔴 **Root-caused:** one percentage-of-completion accrual `(WOValue−PriorBilled)×(ThisMonthHours/WOHours)` applied to BOTH commercial (billed at completion → accrual valid, PriorBilled=$0) AND municipal (billed progressively/monthly → revenue already recognized; PriorBilled=$718,881 in May). Municipal then gets phantom accrual off the whole remaining contract value = **May 2026 $111,204** that accounting strips out. Fix lives in the PROC (DB → build+test on play, hand to devs). **BLOCKED on Steve's accounting rule:** zero municipal accrual, or true one-month-in-arrears? Then build. NOT STARTED.

## 📌 PINNED — Bid-process re-engineering: SOPs received + AS-IS map built (Jun 18 late)
Skipper sent 14 current SOPs/docs (5 batches) for the flagship bid/"traveler" workflow re-engineering. Home: **`~/arbor-stack/bid-process-reengineering/`** — `sops/` (originals in `sops/source-pdfs/`, full transcripts in `sops/SOP-INTAKE.md`; image-only Arborist flowchart was extracted+de-rotated to `sops/Arborist_FlowChart_left/right.png`), and **`AS-IS-WORKFLOW-MAP.md`** = end-to-end current-state map (RFP→e-traveler→inventory/arborist 3 branches→proposal→approval→go-ahead 2-step flip→scheduling→invoice→month-end accruals) with cast/owners + 7 pain points.
- **Key cross-links:** commercial vs municipal run on SEPARATE tracks (commercial=Sales Admin+Branch Mgrs; municipal=Production team Celeste/Maria/Larry); **month-end accruals (MP2)=Dimitry** = owner of the report behind [[Steve RECON-02]] municipal-accrual bug.
- **GAPS still needed before future-state design:** (1) the actual bid PRICING/estimate logic (SOPs cover packet mechanics, not how $ is built — Price Buddy may relate); (2) lead intake before RFP; (3) **ArborNote proposal-flow reference** (screenshots/walkthrough — Skipper to provide); (4) municipal scheduling + report formats.
- **RESUME:** when ArborNote reference arrives, design FUTURE-state (enter data once, in-system attachments, handoffs become status changes). "thats what i have for now" — more SOPs may follow; append as Batch 6+.

### Bid-process gaps — Skipper answers (Jun 18 23:56)
- **Pricing = judgment, not formula:** Sales Arborist prices from experience + job history; **Price Buddy** assists. Future-state: surface history/Price Buddy inline at proposal, don't auto-price.
- **Lead intake = phone or email** (no portal).
- **ArborNote walkthrough = TOMORROW (Jun 19)** with Skipper — the key remaining input before future-state design. AS-IS-WORKFLOW-MAP.md gaps section updated.

### Steve replied on the accrual question (fwd Jun 18, "Municipal Billing Cycle")
Steve Guzowski (CFO) preliminary answer = **"Both" — billing cycle is PER-CITY**, not blanket municipal. His doc (saved `steves-projects/financial-report-reconciliation/src/MunicipalBillingCycle__*.pdf`) lists each city: most bill **Upon Completion** (Aliso Viejo, Anaheim[no progress], Cypress, Fountain Valley, LB Marine, LB Parks, San Clemente, Westminster[no progress]); **monthly/cycle** = Irvine (1st&15th), Newport Beach (EOM), Stanton (EOM); Industry TBD. → RECON-02 fix REFRAMED: not "zero municipal accrual" but **billing-cycle-aware accrual driven by a per-city billing-cycle field** (TRIM IT lacks it → Steve wants a "selectable billing tab"; schema/UI + logic, "long-term problem"). Corrects my earlier "municipal billed progressively" claim (only true for the monthly cities). Steve wants to CONFIRM before formally replying → HOLD build.

### 📌 Steve's project PINNED (Jun 18 ~19:50 PT) — switched back to SPM dashboard
Accrual (Project B) now split: emailed jwade to fwd — **Brent** = confirm per-city billing cycles; **Steve** = accrual treatment (accrue to billing date vs period close). Drafts: `email-brent-billing-cycles.txt`, `email-steve-accrual-treatment.txt`. Earlier "confirm city list" Steve email superseded. HOLD build until both reply. Project A (RECON-01 exec periods + blast radius) still not started. RESUME Steve's stack when replies land.
