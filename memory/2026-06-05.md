# 2026-06-05

- Resumed after a rough handoff (subagent-noise had buried Skipper's messages). Confirmed verifier DONE: 414/414, `verification_full_results.xlsx` built. Final: 161 GREEN / 112 DATA_FIX / 96 YELLOW / 45 RED.
- Server fully patched (Skipper ran apt upgrade): 6 pkgs incl docker, nodejs 24.16, twisted/urllib3 security. No reboot needed.
- LLM inventory: Opus (primary, me), Codex (OAuth+runtime ready, NOT primary — Skipper chose to stay), Ollama llama3.2:3b (running, backs web search; gemma4 configured-not-pulled).
- Built **profile/memory** with Skipper (USER.md + MEMORY.md seeded). See those files.
- Set up **PDF reading** via Node (no python pkg mgr on box): `pdf-parse` at `/home/wade3337/arbor-stack/pdf-tools/` + `pdf2txt.js`. Also `extract_all.js` (whole-folder corpus builder, uses adm-zip for docx/pptx). xlsx via xlsx2csv.
- **Received + extracted the Arbor AI project folder** (8.1MB zip → `/home/wade3337/arbor-stack/Arbor AI/`, 99 files) and **read the entire thing**. Synthesis + deep facts written into MEMORY.md (TrimIT stack, repair workflow, dashboards shipped, business intel, Muni Bid Manager PRD, AI integration plan).
- Corpus cached at `/home/wade3337/arbor-stack/pdf-tools/_corpus.txt` for fast re-reference.

## NEW PROJECT: Anomaly Monitor (started 2026-06-05, eve)
Skipper wants Gilligan to watch tracking metrics and alert on anomalies. Specs in `/home/wade3337/arbor-stack/anomaly-monitor/`:
- `METRICS_SPEC.md` — v0.1. Feed = fully automatic (read-only JSON endpoint on TrimIT, Option A). Alerts = daily digest + ping-when-off, respect 11pm–4am PT quiet hours.
  - **Metric 1 (headline): Daily Job TPH** — reuse TrimIT's own TPH color bands vs target 130 (Amber/Red = flag). Hours of record = **Crew Sheets**.
  - **Metric 1b: Overtime** — cost flag, track separately.
  - **Metric 2: Monthly revenue pace vs $2.2M goal** — already computed by `Dashboard-RevenuePerformance.cfm`.
  - **Metric 3: City contract burn-down** — CONTEXT ONLY, no alert (cities control rebids; fiscal-year). Point at Contracts Dashboard. Big muni data dump coming later.
  - **CRITICAL guard:** hours & dollars arrive at different times — call-ins → $ is a MANUAL admin step, municipal $ lags. So "hours but no $" = "dollars pending" info flag, NOT a low-TPH alarm. True low-TPH alert only when hours AND $ both posted. (Future Arbor agent: auto-convert call-ins → dollars.)
  - Pipeline truth = Schedule Board + GoAheads COMBINED.
- `TRIMIT_ACCESS_PLAN.md` — Skipper's desktop is **Windows** → no OpenClaw screen-share node. Plan: **server-side browser (Playwright/Chromium) that Gilligan drives into TrimIT**, logged in via a **dedicated READ-ONLY TrimIT "Gilligan" account** (IT to create; never use Jason's admin login; secret in 0600 file). Doubles as the monitor's data feed. Fallback = Windows screenshot-streamer.
- **PENDING on Skipper (tomorrow):** (1) approve dedicated read-only TrimIT login, (2) confirm server can reach play.greatscotttreeservice.com, (3) green-light Playwright install. Then first live Crew Sheets walkthrough.

## Anomaly Monitor — BIG progress session (Jun 5 afternoon)
- **TrimIT live access SHIPPED:** created read-only `gilligan-bot` (UserID 376) via supported proc; built `trimit-fetch.sh` (authenticated HTTP pulls, no browser — Playwright/Puppeteer won't build on Ubuntu 26.04). Server reaches TrimIT directly over public internet (no VPN needed). See MEMORY.md.
- **Walked the live screens WITH Skipper and defined ALL 4 metrics.** Full spec: `/home/wade3337/arbor-stack/anomaly-monitor/METRICS_SPEC.md`.
  - **Metric 1 (Daily Job TPH) CLOSED:** read Progress(actual) TPH off daily crew sheets (`Profile$Performed$Day$Frame.cfm?ZCalendarID=`); skip 0%/unreported; flag every reported job daily, in-progress + complete; vs $130 target (🔴<120 ping, 🟡120–130 digest); vs Scheduled bid BOTH directions (over=win, under=behind). Cadence: pull ~4am, report ~6:30am for PRIOR day (work reported manually each evening by a manager; future real-time → config swap).
  - **Metric 1b (Overtime) CLOSED:** source = Payroll Day Sheet `ReportDev/ProductionPayroll$Day.cfr?ZCalendarID=`; OT = any daily paid hrs >8, + 6th worked day = whole day OT; WEEK-AWARE (Mon–Sun). Hours only now (pay $ = future). Daily: company total + flag any 1 man >1 OT hr (name crew+job). Monday: prior-week total.
  - **Metric 3 (Contract burn-down) CLOSED:** source = `Blue/Admin/Contracts-Overview-Content-Accounts-Slim.cfm?ZProjectGroupDefID=11` (Municipal). Cols: Budget=projected available, Call-Ins=used, Hours, TPH=CallIns/Hrs, Diff$=(TPH−130)×Hrs. Remaining=Budget−CallIns; flag <10% remaining, louder if over budget; pace vs Jul–Jun FY. Weekly Monday.
  - **Metric 2 (Revenue vs $2.2M) CLOSED:** REAL earned = **Invoiced + Accrued**. Invoiced = `dbo.Invoices.Total` by PeriodID/CustomerPeriodID, excl Deleted/Voided. Accrued = supported fn **`dbo.GetPeriodAccrual(@PeriodID).AccrualTotal`**. Territory = `dbo.CrewNames.SiteAssigned` (1=N,2=S,else Other/Shared→allocate proportionally into N/S). Report company total + N/S, weekly Monday, calendar month; also SCHEDULED pacing from `Dashboard-RevenuePerformance.cfm`. INSIGHT: May earned $2.09M = 95% of $2.2M (invoiced-only looked 84%). **Build note:** Metric 2 needs a small read-only JSON endpoint on TrimIT (Codex builds once) — no existing page; Metrics 1/1b/3 pull from existing pages.
- **ALL 4 METRICS DEFINED.** Next session = BUILD: read-only JSON endpoint for Metric 2; the pull/evaluate engine; scheduled 4am compute + 6:30am report (+ Monday weekly/Metric 3); digest + pings; respect 11pm–4am quiet hours. Nothing built yet — all definition.
- **Taught Skipper accrual accounting** (revenue earned vs billed) — he's learning the COO/finance side; keep teaching.
- **Next:** ingest Codex accrual+territory output → Metric 2 closed → then BUILD (scheduled 4am/6:30am runs, digest, pings). Nothing built yet; all definition so far.
