# 2026-07-10 — daily log

## 🎯 GOAL SET (2026-07-10 ~14:44 UTC): 4-dashboard deploy bundle — staged for a NEW chat (Skipper controlling tokens)
Skipper wants RC-02 Revenue · RC-06 Production Performance (built today) · RC-03 City Budgets · RC-04 SPM deployed to prod TOGETHER (one bundle, no piecemeal). Process: (1) complete-verify each on play, (2) run our pre-prod checks (`deploy-manifest.js` full dep-tree vs play+prod; `deploy-smoketest.sh` 404s), (3) assemble one deploy package for Jordan/Travis. Staged so the new chat opens ONE file & executes:
- Master plan/resume: `arbor-stack/release-candidates/DEPLOY-BUNDLE-2026-07-10.md` (phase checklist + per-dashboard gates).
- New RC file: `release-candidates/RC-06-production-performance-dashboard.md`; README + MEMORY.md "Fresh session" pointer updated.
- OpenClaw goal created (id 7d6ade27). Open gates to decide: RC-06 turnover-grace label; RC-03 Brent reply (ship-as-is vs hold); RC-02 dual-webroot C:\ shadow; RC-04 5 asset 404s + Go-Aheads match. Play-only until package approved.

## Production/Contract Dashboard rebuild — CREW LAUNCHED (workflow prod-dash-rebuild, ID w2s94tsjj) — deliver by Fri AM
Skipper directive (05:10 UTC): our City-Budgets live numbers = SOURCE OF TRUTH; don't trust legacy CompanyYears (may never have been right). Crew deep-dive + BUILD, deliverable by his Friday morning: a FIXED or NEW production board on PLAY giving production guys **Jobs, Dollars, TPH, drill-downs to the DAY** (like ContractDashboard.cfm), **mobile-friendly + GSTS colors + fail-proof going forward**. Legacy `ContractDashboard.cfm` reads baked `dbo.CompanyYears` (TotalBudget/TotalPrice/TotalHours/TPH; TPH=TotalPrice/TotalHours; Diff$=(TotalPrice/130−Hours)*130) → stale/missing at FY turnover = the fall-off/zeros bug. Our engine (`CityBudgets.data.cfm`) computes live → self-heals.
Workflow phases: Forensics (was it ever accurate? + TPH-logic audit + reconcile legacy vs our live truth) → Decide (fix-vs-new, lock metric defs + build spec) → Build (deploy to play, backup-first, render-verify, drill-to-day, mobile) → Verify (adversarial accuracy + UX/mobile/drill). Play-only, prod untouched. Report to Skipper on completion (async-report rule) w/ screenshots — even if it snags. Also: removed the bogus "QuickBooks divergence" framing from RECONCILIATION-2026-07-09.md + 07-09 daily (both Brent & dashboard = TRIM IT; QB API is only a future item). RC-03 still gated on Brent's reply (Newport prior-FY overage rule + master relabels).

## Brent "Dashboard issues" email (FW from Skipper, uid97) — DIAGNOSED vs live play
Screenshot = the legacy **Contract Dashboard** (Budget/Call-Ins/Hours/TPH/Diff$, "Contract Performance/Balances"
tabs), NOT our RC-03 City Budgets rebuild. It reads the **materialized CompanyYears/CompanyPeriods** layer (same
fragile layer as the June Long Beach/Anaheim fix). At the 25/26→26/27 FY turnover that layer is lagging. Root-caused
all 6 from `gsql.sh` (read-only play):
- **Anaheim** "Sep/Aug duplicate": old Sep/Aug CY 89815 (25/26, $174,421) still populated next to new Mar/Feb CY
  90020 (26/27, $81,005). → IT delete/clear 89815 (Brent OK'd).
- **Long Beach**: CYs built Apr/Mar (from EndDate Mar 31) so "current"=26/27 w/ ~3mo ($80,843). Real FY=Oct/Sep →
  should read 25/26. → IT rebuild LB contract-year calendar to Oct/Sep. (Our RC-03 map already = 10.)
- **Cypress**: 26/27 CY 89587 exists but TotalPrice NULL / periods empty. Contract budget IS entered (Year04 26/27).
  → materialization/regen not run. Brent did nothing wrong; IT must regen Cypress 26/27.
- **Newport**: budget reads ($2.08M) but CY 89909 (26/27) empty shell → call-ins/hours 0. FY started Jul 1. → same regen.
- **Fountain Valley** "fell off": NO approved contract carries a 26/27 label → no 26/27 CY. → Brent add 26/27 contract
  year, then IT generate.
- **Industry** "fell off": approved contract 1248 has single-year "26" budget ($666,666) but no "26" CY generated
  (single-year-label gotcha, ship #60). → IT generate Industry "26" CY.
Two solve paths pitched to Skipper: (1) hand IT a precise punch-list/runbook (I'm read-only on play, can't touch prod);
(2) the permanent fix = get RC-03 City Budgets signed off + deployed — it computes live from source, no materialized
layer, so none of these 6 failure modes can occur. Awaiting Skipper's call on direction.

## Find-menu customer pages → GSTS restyle (Skipper session, ~03:05–04:25 UTC)
Restyled 3 pages off the TRIM IT landing-page **Find menu** to the GSTS look, all **live on play, in Kanban
Release Candidate, prod-pending** (one handoff to Jordan when the Skipper says go — nothing touched in prod).
Full detail in the ship log (#113–#116) + `ship-log/2026-07-10-*.md`.

- **#113 Proposal Review** (`Review$Profile.Proposals.Content.cfm`, AppForm 1114) — DW6 template → GSTS card/table/badges. Look-only.
- **#114 Scott's Customers** (`Profile.Companies.My.Content.cfm`, AppForm 1076, `?ZUserID=3`) — GSTS look; then Skipper-approved: **TPH bands → 130 target**, **dollar formatting**, Scheduling sort-link fix.
- **#115 Top By Market** (`Profile.Companies.Top.ByMarket.Content.cfm`, AppForm 1070) — GSTS look + **Spry menu → clean Market dropdown**; 130 bands + dollars.
- **#116 improvement pass** (overnight) — both Scott's + Top By Market: **"Unassigned"** rep display; Scott's got a **rep switcher dropdown** + **client-side click-to-sort** on all columns.

### ⚠️ OPEN — needs Skipper decision (carry into next session)
The Rep column showed **Patrick Fringer (PF), a departed rep** — because **~149 top accounts are stale-assigned**
to departed/blank reps and were never reassigned (TRIM IT has no record of the current owner; FieldRep/InsideRep
blank, projects also still Patrick's). Interim = show "Unassigned". **Real fix = reassign those ~149 accounts**
to whoever owns them now (a data change → prod via devs). Waiting on the Skipper to say **who inherited
Patrick's book** (one rep, or a per-account map).

### 🖼️ Tooling win — screenshots now work
`.html` MEDIA attachments fail ("media failed"); Discord needs an IMAGE. Playwright/puppeteer won't install on
this host (ubuntu26.04 unsupported). ✅ Route through the running **browserless container `arbor_browser` :3010**;
helper `production-dashboard/shot-html.sh <in.html> <out.png>`. Full recipe in `PLAYBOOK.md` → `[render/screenshot-trimit-pages]`.

Note: chained `copy ... >nul & echo` over SSH intermittently errors "cannot find path" — use standalone `copy` for backups.

## Deploy-bundle Phase 1 — RC-02 + RC-04 COMPLETE-VERIFIED (Gilligan, ~14:5x UTC, new chat)
Skipper OK'd starting with the two no-decision dashboards. Verified live on play via `production-dashboard/view.sh` (ZUserID=376 read-only GET) + manifest.
- **RC-02 Revenue** ✅ — main page renders clean (61KB, 0 CF errors; welcome modal + Target-TPH bands + Pace tile + True-Produced all present). CSV export works via **form POST-back** (`exportCSV=1`) → proper CSV header + real data. **CORRECTION:** the `Dashboard-RevenuePerformance.Export.cfm` listed in the bundle/RC docs **does NOT exist** — export is self-contained in the page (`<cfif StructKeyExists(form,"exportCSV")>` line 683). Removed the phantom from the deploy set. Bundle checklist RC-02 ✅.
- **RC-04 SPM** ✅ — all 6 pages (shell + Pipeline/Sold/Production/Results/Drill) render clean (0 CF errors, 0 404s). Results CSV export (`exportCSV=1`) → clean CSV. **Go-Aheads RESOLVED** (Nate's muni "Apprvd" date = report-runtime artifact, not queryable — nothing to fix). **Manifest run → the 5 prod-missing assets CONFIRMED** (js/pro-tips.js, assets/img/tree-helper-v1.png, assets/css/gsts-tokens.css, assets/protips/protips.css, assets/protips/protips.js — 200 play / 404 prod, must ship WITH .cfm). Full deploy set mapped (6 pages + 2 includes + 3 linked-present + 2 shared-present). Bundle checklist RC-04 ✅.
  - ⚠️ OPEN (Skipper judgment, not a bug): SPM production total −1.9% (−$141K) vs Nate — definitional (actual-managing-rep attribution vs Nate's 5-col fold); dashboard penny-accurate on its own def. Accept as report-vs-report, or drill?
- **Path gotcha logged:** project work lives at `/home/wade3337/arbor-stack/` (memory paths are relative to `/home/wade3337/`, NOT the OpenClaw workspace `~/.openclaw/workspace/`). Cost a few tool calls at session start.
- **Still gating Phase 1:** RC-06 turnover-grace label (Skipper) · RC-03 Brent gate ship-as-is-vs-hold (Skipper) · RC-04 −$141K judgment (Skipper).

## ⚠️ RC-03 NTE carryover — Brent replied; carryover line is NOT in TRIM IT (QB-sourced) — surfaced to Skipper, awaiting call
Brent's answer to the Newport prior-FY overage Q: Newport & Industry are **multi-year Not-To-Exceed (NTE)** contracts; he sets each year's budget = NTE ÷ #years, and **carries over/under across years** (cumulative NTE is the real cap). City doesn't care how we track — internal. Skipper: "display this for Newport AND Industry." Chose **option A** (hard-code the 2 now; note option C = real TRIM IT NTE flag for later).
- Confirmed data: **Industry 1248** = flat $666,666.66 ×3 = **$2.0M NTE / 3yr** (textbook). **Newport 449** = escalating slices 22/23→26/27 ($1.70M/$1.75M/$1.97M/$2.03M/$2.08M) = **$9.54M / 5yr** (NOT a flat divide — NTE = sum of slices).
- **BLOCKER FOUND:** to reproduce Brent's Newport 25/26 report (Budgeted $2,030,649.30 = 25/26 slice; Invoiced $2,178,277.42; Remaining −$147,628.12) the delta is EXACTLY one carryover line: inv **#121827, 06/30/2025, $178,347.51, "FY 2024/2025 June Overages."** That invoice **does NOT exist in TRIM IT** — TRIM IT LegacyRef 121827 = a *June 2023* master ($131,140.10); no $178,347.51 / no "Overage" line anywhere in Newport TRIM IT. Nor does it equal the TRIM-IT-computed 24/25 overage ($2,073,774.55 invoiced − $1,971,504.17 slice = **$102,270.38**).
- **CONCLUSION:** Brent's Budget Reports are **QuickBooks-generated**; the carryover/overage lines are **QB adjustments not in TRIM IT**. This **contradicts the 07-10 "both read TRIM IT, no QB source" correction** — true for the plain monthly masters (penny-tie) but NOT for the carryover lines. Original ship-log #109 "Newport→QB divergence" framing was actually right for these lines.
- Dashboard invoiced buckets by `im.ProjectYearLabel` (label, not date) — 25/26 = the 12 masters = $1,999,929.91; Brent = that + $178,347.51 QB overage.
- **3 paths put to Skipper:** ① TRIM-IT-native carryover (our ~$102K number = source of truth; ships today; won't penny-match Brent's QB $178K) · ② hold for QB API · ③ back to Brent for the $178K origin. My lean = ①. **AWAITING Skipper's call — NOT building yet.**

## ✅ RC-03 NTE carryover — SOLVED via Brent's Excel workbook (uid98 "City Forecasting.xlsx")
Skipper forwarded Brent's forecasting workbook (gilligan.gsts uid98) → `sales-review/incoming/2026-07-09__City Forecasting.xlsx`. Parsed via python stdlib (no openpyxl/xlsx lib on host — unzip + ET). Sheets: "City Projections (Fiscal Year)"(main), FY23-24 TPH, Sheet1, Summary, "For Company Model", Project Data Table.
- **SMOKING GUN:** the $178,347.51 is a **hand-typed value** in Brent's sheet — cell `DH146` (Newport "Invoiced" monthly row) = 178347.51, NO formula. Remaining is a plain formula `EGx45−(Inv+Callins+Sched+Placeholder)` (e.g. EG150 = −178347.51; Summary!H9 refs it). Confirmed earlier: NOT in TRIM IT (0 rows any company/status) and NOT QuickBooks (Skipper) → it's a **manual Excel entry**.
- His invoiced figures are hand-entered & **don't tie to TRIM IT**: Newport 25/26 sheet "invoiced" $1,852,301.79 vs TRIM IT masters $1,999,929.91. Budget B145=2,030,649.30 (matches TRIM IT 25/26 slice). 2,030,649.30−1,852,301.79 = 178,347.51 (the "overage" = budget minus his hand-entered invoiced).
- **VERDICT:** cannot/should not reverse-engineer Brent's exact numbers (manual inputs, not source-derived). His LOGIC (budget spread monthly → invoiced → remaining → projected) IS reproducible live from TRIM IT and would be MORE accurate than his hand-entry.
- **Recommendation to Skipper (awaiting A/B):** **A** = ship RC-03 City Budgets as-is (clean TRIM-IT truth; NTE carryover is a manual-Excel artifact, not a dashboard bug) + open a NEW separate project **"City Forecasting dashboard — replace Brent's Excel"** (monthly budget-spread + live invoiced + remaining + projected, TRIM-IT-native). **B** = hold RC-03 to build forecasting in now (bigger scope, still won't match his manual numbers). Lean A.
- Tooling: `anomaly-monitor/inbox-recent.js` lists inbox; `grab-uid.js <uid>` saves attachments → `sales-review/incoming/` (hardcoded `2026-07-09__` prefix).

## City Forecasting build — P1 engine DONE + verified (bg sub-agent + Gilligan spot-check)
Spec: `city-budgets-review/FORECASTING-BUILD-SPEC.md` (Skipper-approved: forecast=new TAB on City Budgets dash; true annual÷12 spread; 2.5% escalation; reconcile option (a) = shared budget/FY/spread engine, produced-vs-invoiced stay distinct).
- **P1 engine built:** `production-dashboard/CityBudgetsForecast.data.cfm` (+ throwaway `ZZForecast-test.cfm`). Exposes forecastMonths[12]{monthLabel,budgeted,invoiced,callins,scheduled,remaining,projected} + forecastTotals. Reuses cbFyStartFor/cbResolveCurrent verbatim. Read-only, no existing files touched.
- **Verified to the penny (my own gsql check):** Newport 25/26 Budgeted $2,030,649.30 / Invoiced $1,999,929.91; monthly invoiced sums to FY total; months bucket to correct FY. Industry/Stanton budgets tie.
- **⚠️ WO attribution decision (surfaced to Skipper):** callins/scheduled differ by method — BY LABEL (current City Budgets view): Newport Callins $41,966.80 / Sched $222,918.80 (454 WOs); BY StartDate window (monthly grid): $23,920.07 / $73,382.25 (453 WOs). Diff = big scheduled/future jobs labeled 25/26 but dated outside Jul25-Jun26. **Gilligan rec (awaiting Skipper confirm):** FY TOTALS use label method (ties exactly to current view → tabs reconcile), dates only lay out the monthly columns, future/undated scheduled → "scheduled ahead" column. Then build P2 tab.
- Note: sub-agent couldn't render via view.sh (SSH host 100.86.97.46 = DB box, not play webroot) — proved numbers via gsql instead. For P2 rendering use browserless :3010 / view.sh from the web path.

## City Forecasting build — P2 Forecasting tab LIVE on play + verified
- **Deployed to play** (backup `Jasonsrepairs\citybudgets-forecast-tab-20260710-214616\`): engine `CityBudgetsForecast.data.cfm` (FY helpers renamed cb*→fcb* to avoid dup-declare clash) + `Dashboard-CityBudgets.cfm` (new **Forecasting** tab via `ZTab=forecast`, default budgets view UNCHANGED). Local mirror == play (md5).
- **Forecast grid:** rows Budgeted/Invoiced/Callins/Scheduled/Remaining/Projected × 12 FY months + **Scheduled Ahead** + Total. GSTS style. **CSV export** = `?...&ZTab=forecast&exportForecastCSV=1` (real text/csv; requires ZTab=forecast — button includes it).
- **Reconciles to the penny vs current City Budgets view** (my own view.sh check, Newport 25/26): Budgeted $2,030,649.30 · Invoiced $1,999,929.91 · Callins $41,966.80 · Scheduled $222,918.80 · Remaining −$234,166.21. Scheduled split: 12mo $73,382.25 + Ahead $149,536.55 = $222,918.80 (4 label WOs dated outside FY window). Industry (Jan-Dec NTE) ties too. Both tabs 0 CF errors; default view intact.
- **⚠️ FY rolled to 26/27 today (2026-07-10)** — Newport Jul-Jun FY flipped; both tabs default to 26/27 (Budgeted $2,081,415.53 = 25/26×1.025) and agree; 25/26 via ZFY picker.
- Screenshot recipe worked: `view.sh` → inject `<base href>` → `shot-html.sh` → PNG (welcome modal overlays lower rows; cosmetic).
- **NEXT: P3** — rewire `ProductionPerf.data.cfm` (RC-06) to consume the shared engine (kill the verbatim FY/budget copy). Awaiting Skipper go (touches deploy-bundle RC-06). Then P4 3-way reconcile + crew verify. Ship-log entry #118 pending.

## City Forecasting build — P3 shared FY engine LIVE + verified (drift killed)
- **Created `citybudget-fy-helpers.cfm`** (5 canonical cb* helpers) — ONE source. All 3 data layers now use a load-guarded include (`request.cbHelpersLoaded`): `CityBudgets.data.cfm`, `CityBudgetsForecast.data.cfm` (fcb* hack reverted → cb*, only a history comment mentions fcb), `ProductionPerf.data.cfm` (verbatim copy removed). Backup `Jasonsrepairs\shared-fy-engine-20260710-215743\`. Local==play (md5).
- **Regression: byte-identical before/after** on all 3 pages (agent diff + my render check). No calc logic touched — only helper defs relocated.
- **3-way spine reconciles:** RC-06 Production "Contract budget context" = **$2,030,649.30** = City Budgets Budgeted = Forecast Total. Produced $ $2,027,793.81 (CrewSheets) stays distinct from Invoiced $1,999,929.91 (option-a: labeled distinct actuals). 0 CF errors on City Budgets tab, dual-include Forecasting tab, Production dash (+ Day/all-mode smoke).
- **NEXT: P4** — broad 3-way reconcile across ALL municipal cities + adversarial edge cases (FY types Jan-Dec/Sep-Aug/Oct-Sep, 26/27 rollover, all-cities mode). Then ship-log #118, Kanban card, wrap. OPEN scope Q for Skipper: all-cities FORECAST aggregate view (Brent's full report w/ grand total) or per-city enough?

## City Forecasting build — P4 QA + P4b fixes DONE/verified
- **P4 adversarial 3-way reconcile (read-only agent):** budget/FY/callins/scheduled spine ties across ALL municipal cities; all FY types (Mar-Feb/Jan-Dec/Sep-Aug/Oct-Sep) + all-cities mode + CSV = 0 CF errors; month-split integrity clean (months+ahead=total). Found 3 issues.
- **P4b fixed 2 real bugs** in `CityBudgetsForecast.data.cfm` (backup `Jasonsrepairs\forecast-invoiced-fix-20260710-221403\`, local==play md5 fe343bf1...): (A) **Anaheim** forecast Invoiced $0→**$122,739.20** — added actual-`dbo.Invoices` FALLBACK when 0 masters (mirrors CityBudgets hybrid); (B) **Fountain Valley** forecast Invoiced $9,637.74→**$0.00** — added current-month HOLD-OUT (subtract held from Total, month cell still shows it). Verified myself: Anaheim/FV tie to Budgets tab; Newport ($1,999,929.91/−$234,166.21) + Industry unchanged; 0 CF errors.
- **3rd issue = POLICY (awaiting Skipper 1/2/3):** at Jul-Jun FY rollover, Production defaults to prior FY 25/26 (60-day production grace) while Budgets/Forecast default to calendar 26/27 → different years side-by-side on default landing (each internally correct; pinning same FY reconciles). = the RC-06 turnover-grace-label decision. Gilligan rec = Option 1 (keep natural defaults + clear FY badge on each).
- **Also awaiting Skipper:** all-cities FORECAST aggregate (Brent's full report + grand total) vs per-city enough.
- **Build essentially complete** — reconciles across all cities when pinned to same FY. Remaining: Skipper's 2 calls → then ship-log #118 + Kanban card + close.

## City Forecasting build — P5 rollover labeling (Option 1) DONE + verified; P6 all-cities running
- Skipper chose **Option 1** for the FY-rollover divergence: keep each dashboard's natural default, add a clear FY badge. Also settles the **RC-06 turnover-grace-label** decision (deploy bundle).
- **Built (Gilligan, direct):** `ProductionPerf.data.cfm` ppResolveFY now returns `calLabel` + `isGrace` (=wantCurrent AND grace-label≠calendar-label). `Dashboard-ProductionPerf.cfm` banner shows amber note **"prior FY · new year (26/27) just starting"** when isGrace. Backup `Jasonsrepairs\prodperf-fy-grace-note-20260710-222335\`; local==play md5 (3d9493dc / d3496c2c).
- **Verified:** Newport default → grace note + FY 25/26, budget $2,030,649/TPH 121.0 unchanged, 0 CF errors. Anaheim (mid-FY) → no note. Explicit ZFY → no note. Screenshot sent.
- **Skipper: "yes add the total row"** → **P6 building** (bg agent a8060b2d): all-cities forecast summary (per-city FY totals + GRAND-TOTAL row) on ZProjectID=all&ZTab=forecast, reusing CityBudgets all-mode per-city rows (reconciles by construction) + CSV. Then ship-log #118 + Kanban card = DONE.

## City Forecasting build — P6 all-cities + WRAP-UP: COMPLETE
- **P6 all-cities forecast LIVE + verified:** `Dashboard-CityBudgets.cfm?ZProjectID=all&ZTab=forecast` = per-city FY-total rows + **GRAND-TOTAL** (9 munis: Budgeted $7,317,742.97 / Invoiced $4,327,665.63 / Callins $64,730.11 / Scheduled $624,651.85 / Remaining $2,300,695.38) + separate non-City table + CSV (`exportForecastAllCSV=1`). Reuses CityBudgets all-mode per-city rows (reconciles by construction; spot-matched Anaheim/Long Beach/Newport). Backup `Jasonsrepairs\forecast-allcities-20260710-222943\`; local==play md5 d0f8843b...; 0 CF errors. Screenshot sent.
- **WRAPPED:** Ship-log **#118** added (top of table). Kanban **card 47** created (Workbench.dbo.WorkKanban, board=trimit, Col=rc, active) — City Forecasting in Release Candidates lane. Spec marked COMPLETE (`FORECASTING-BUILD-SPEC.md`).
- **BUILD DONE.** Live on play, prod-pending (would join a deploy package to Jordan/Travis when Skipper says go). Files: CityBudgetsForecast.data.cfm (NEW), citybudget-fy-helpers.cfm (NEW), Dashboard-CityBudgets.cfm, CityBudgets.data.cfm, ProductionPerf.data.cfm, Dashboard-ProductionPerf.cfm.

## V1.5 Home — Forecasting + Production Performance links added (ship #119)
Skipper: wire the 2 new dashboards into the role-gated V1.5 landing (`sales-engine/cockpit-src/Dashboard-V15Home.cfm`; live at webroot). **Forecasting** (`Dashboard-CityBudgets.cfm?ZProjectID=all&ZTab=forecast`) → **SALES** node; **Production Performance** (`Dashboard-ProductionPerf.cfm`) → **PRODUCTION** node. Backup `Jasonsrepairs\v15home-forecast-prod-links-*`; local==play md5; 0 CF errors; both links render (screenshot sent). Play-only.

## Wiki save + Boss Herman: knowledge + PLAY WRITE access granted (Skipper ask)
**1. Wiki save (DONE):** City Forecasting build captured in BOTH vaults (bg agent + verified).
- Workspace wiki: `wiki/projects/city-forecasting.md` + facts `shared-engine-kills-dashboard-drift.md` & `brent-forecast-178k-artifact.md`; `wiki/index/PROJECTS.md` MOC updated.
- Herman's shareable `trimit-knowledge` vault: `pages/Dashboard-CityBudgets-Forecast.md`, `pages/Dashboard-ProductionPerf.md`, `concepts/citybudget-fy-helpers.md` + indexes. **Committed + pushed** (wade3337-ctrl/trimit-knowledge, 6ccdcc8) so Herman gets it on sync.
**2. Herman PLAY WRITE access (DONE, Skipper chose "mint his own" = Option A):**
- Minted independent keypair `herman_trimit_rw` (ed25519). **Authorized on play** `C:\ProgramData\ssh\administrators_authorized_keys` (backup `.bak-20260710-225635`; both keys present: gilligan@openclaw + herman-trimit-rw-play-20260710; ACL intact SYSTEM+Administrators). **Independently revocable** (remove just Herman's line; Gilligan's key untouched).
- **Verified:** Herman key authenticates as `gstsdatabase\administrator`; webroot WRITE test (create+remove) OK; gsql.sh DB read OK. Same access level as Gilligan (deploy .cfm + DB write). PLAY only.
- **Tooling staged** in `~/herman-store/gilligan-outbox/trimit-play-write/` AND `~/herman-gateway/`: `herman_trimit_rw`, `gsql.sh` (rw), `view.sh`, `deploy-to-play.sh` (backup-first+scp), `install-play-write.sh`, `HERMAN-PLAY-WRITE-SETUP.md` (guardrails: backup-first, render-verify, PLAY-only, log ship-log, ask-before-destructive).
- **LAST MILE (can't do remotely — no SSH into Herman's box `arduino@100.121.177.31`):** Herman must pull the folder + run `bash install-play-write.sh` on his box. Same last-mile pattern as the RO gateway install.

## Herman RW access — DELIVERED into his container (last mile done from my side)
Herman asked me to `scp -r trimit-play-write/ herman@jdog1:/opt/data/home/`. That FAILED: **jdog1 = THIS host's own hostname** (127.0.1.1), no local `herman` user, `/opt/data/home` not on host. **Herman = the `hermes` docker container ON jdog1** (image hermes-agent, up 7d), bind-mount **host `/home/wade3337/.hermes` → container `/opt/data`** (container home owner `hermes` = host uid 1000 = me). So delivered by copying the folder to **host `/home/wade3337/.hermes/home/trimit-play-write/`** → appears in-container at `/opt/data/home/trimit-play-write/` (all 7 files, key readable by hermes). Herman runs `bash /opt/data/home/trimit-play-write/install-play-write.sh`. **KEY FACT for future Herman delivery:** don't scp to herman@jdog1 — copy to `~/.hermes/home/` on this host (= his container `/opt/data/home/`).

## Exec financials + Employee Handbook → wiki (Gilligan + Herman), for Q&A (early 2026-07-11 UTC, same session)
Skipper fwd 2 emails (uid99 5 monthly financial decks Jan–May 2026; uid100 GSTC Employee Handbook 2026 draft). Pulled → `arbor-stack/exec-financials/` + `arbor-stack/employee-handbook/`. Extracted to Obsidian notes for Q&A.
- **Workspace wiki** (my memory): `wiki/reference/gsts-employee-handbook-2026.md` (all 129 sections verbatim) + `gsts-financials-2026-{01..05}-*.md` + `gsts-financials-2026-summary.md`. PROJECTS.md "Business docs" pointer added.
- **VERIFIED** (my own reparse of May deck consolidated IS): Net Income Jan −$37,428.45 / Feb −$119,619.10 / Mar +$62,533.28 / Apr +$2,719.88 — matches notes to the penny. Annual plan $24.0M rev / $536.5K net target; segments HOA $11.0M / Cities $8.5M / Commercial $3.0M / Muni-Other $1.5M.
- **Provenance caveat:** 5 decks non-uniform; used latest restated consolidated 2026 IS (May deck) as single source; minor Jan restatement documented; **June deck NOT received** (subject said 6mo, 5 came).
- **Herman: BOTH (Skipper: "local and git"):** GIT → `trimit-knowledge/business/` (7 notes + `000-business-index.md` + HOME pointer), committed+pushed (c8cd161). LOCAL → copied into his container `/opt/data/home/gsts-business-docs/` (8 files). Herman can now answer questions on these too.

## Employee Handbook legal review + email to Roxanne (attorney) — 2026-07-11
Skipper: read the 2026 handbook draft, flag legal violations for the attorney, save it, email findings to Roxanne.
- **Review saved:** `wiki/reference/gsts-handbook-legal-review-2026.md` (linked to [[gsts-employee-handbook-2026]]). Handbook is generally well-drafted (at-will, meal/rest premiums, 72h final pay, LC2802 expenses, OT tables all CA-compliant). FLAGS — High: (1) field "Holiday & Vacation Pay Allowance" per-hour → must be in OT regular rate (LC510/§226) = biggest exposure ~100 field; (2) discipline on arrest/charge not conviction (LC432.7 + Fair Chance Act) — Criminal/Arrests + drug-alcohol self-report; (3) drug policy "measurable quantity in blood/urine" vs AB2188 + own off-duty-cannabis clause. Med: (4) forcing sick time onto unauthorized absences (LC246); (5) no-solicit "working hours" vs "working time" (NLRA); (6) monitoring phone calls w/o consent (PC632 two-party); (7) forcing vacation during FMLA/CFRA. Verify: PSL 40h/5day 2024; bereavement AB1949 + reproductive-loss SB848; NLRA savings clause on confidentiality.
- **OUTBOUND EMAIL SENT (Skipper-approved):** from gilligan.gsts → **jwade@gstsinc.com** (Skipper's work; he forwards to Roxanne himself), subj "2026 Employee Handbook - compliance items for your review", MsgID 7144512a-b107-de38-8c96-4b41edce4890. Body = clean Roxanne-addressed draft (7 priority items + 2 verifies). Not sent to Roxanne directly (Skipper forwards).
