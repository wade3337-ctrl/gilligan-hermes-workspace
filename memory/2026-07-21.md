
## TRIM IT profile picture installed on PLAY for Jason (UserID 9)
Figured out the mechanism: landing page (`Profile$Main`) reads `dbo.Profiles.ProfilePicturePath`; files live in webroot `art/profilepictures/`; blank => falls back to logo. Jason was on `blank.png`.
- Cropped his sepia "Prohibition boss" portrait to 13:10 (520x400) via jimp (no PIL/IM on host — npm i jimp in ~/imgtmp), named `jwade001.jpg`.
- Installed on play myself: scp -> `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\art\profilepictures\jwade001.jpg`; `UPDATE dbo.Profiles SET ProfilePicturePath='art/profilepictures/jwade001.jpg' WHERE UserID=9` via gsql.sh. Verified HTTP 200 (50927b) + DB row.
- **CORRECTION (Skipper caught me):** I wrongly said play was read-only. Reality: my SSH key `gstsdb_ed25519` = Administrator on play (100.86.97.46) → full write (files via scp, DB via gsql.sh `-E` integrated auth). The RO stuff (HermanRO/GSTSREADONLY, gilligan-bot view-only web login) is separate. Don't conflate.
- Rollback: `UPDATE dbo.Profiles SET ProfilePicturePath='art/profilepictures/blank.png' WHERE UserID=9`.

## Steve's Financial Report Dashboard — PROD deploy package built, crew-verified, sent to Jordan
Steve (CFO) asked (via Skipper) to deploy his diligence dashboard to prod. Found it was NOT in yesterday's V1.5 bundle (the sibling Executive/SPM dashboards + accrual report WERE; FinancialReportDashboard/Export were left out). So it's a separate deploy.
- Canonical source = winloss-fix/ (Jul 10, ship #111/#112); confirmed BYTE-IDENTICAL to play-live (no drift).
- **Key hidden dep:** dashboard LEFT JOINs `Workbench.dbo.ProposalOriginalRep` (attribution overrides) — that table exists on play but NOT prod → generated a create+seed SQL.
- **Crew review (Kimi K3 + Gemini 3.1 Pro + gpt-5.6-sol) = DO-NOT-SHIP round 1.** Caught: non-atomic reseed, RAISERROR didn't halt, no unique key (double-count), no count assert, missing GRANT for CF login, AND a real data bug — proposal 396441 override='UNDEFINED' would EXCLUDE it (sol caught). Hardened SQL: XACT_ABORT+tran, THROW asserts, UNIQUE index, dropped UNDEFINED (564→563), backup, grant step. Re-validated on a throwaway play table (clean, 563).
- Package (5 cfm + hardened SQL + DEPLOY-INSTRUCTIONS.md + CREW-REVIEW.md) staged play `D:\GSTS-Deploy\STEVE-FRD-DEPLOY-20260721\`; emailed Jordan (jkim) cc Steve (sguzowski) + Skipper, framed as Steve's fresh request today (not an add-on).
- OPEN: Jordan runs it (DB-first, then files, + the CF-login GRANT he must supply). Play still has the 564/UNDEFINED row — offered to Skipper to clean for parity. Steve email = sguzowski@gstsinc.com.

## Dimitry "Convene" financials (updated) — BLACK, banked for the EBITDA bridge
Skipper sent an updated June-2026 financials ("...for Convene meeting"). Raw held scratch `~/arbor-stack/anomaly-monitor/_pull/dimitry-convene-2026-07-21/` (NOT committed).
- **BS UNCHANGED vs the 07.23 Executive file** — cash $4.18M, same debt lines → net-debt read stands (~neutral; phantom debt-haircut ~nil). Nothing to redo.
- **NEW: Dimitry's own TTM (12-mo rolling, thru Jun-2026) EBITDA build:** revenue $21.37M · gross profit $6.41M · Net Ordinary Income **−$88K** (breakeven/slight loss) · +D&A $1.19M +interest $0.23M = **EBITDA $2.26M** · −ERC interest $0.27M = **adj EBITDA ~$2.01M (9.4%)**. Officer comp $303K = a visible add-back.
- **Gap to the deal:** deal priced on ~$4.1M adj EBITDA vs Dimitry's ~$2.0M → **~$2M of add-backs to justify** = the DD battleground; every $ trimmed flows into Skipper's phantom (NP−$10M)/30 + earnout.
- **DECISION (Skipper):** do NOT build the $2.0M→$4.1M bridge yet — **wait for Steve's data so it aligns with Fort Point's EBITDA/AGP definitions.** Ties to the pinned open item (Steve reply on adj-EBITDA bridge + AGP def). Also reconfirms incentive Gate 1 ($25M/'26) is a stretch (TTM rev $21.4M, soft).

## Segment margin analysis (municipal vs HOA vs commercial) — Skipper kickoff
Skipper launched: Herman/MuniBot surfaced TRIM IT has 2 price worlds (muni vs comm/HOA); hypothesis "municipal done at a lower rate." Analyzed live off play GSTS (HermanRO gateway).
- Segments via `Projects.ProjectTypeID`→`ProjectType` (1 Muni-Cities/2 Muni-Other/3 HOA/4 Commercial).
- **TPH equalized ~$123 across ALL segments** (GSTS prices to a target rev/hr). Price-per-tree varies (Muni $104 / Comm $97 / HOA $146) = throughput×size, not margin.
- Prevailing wage MUCH smaller than I first said: muni uses DIR **Tree Maintenance** determ (~$33-37 total), not construction Laborer ($75). Real premium = fringe top-up **~$5.80/hr/worker** (from Skipper's Pay_Rates sheet, 83/87 below DIR total). Owned the correction.
- TRIM IT holds NO job cost (DirectCost empty) → realized margin needs Steve/payroll.
- Execution fine: muni-cities MOST on-target (+0.8% vs comm +3.9%). Drag ≠ inefficiency.
- **Fixed-price erosion VISIBLE:** muni-cities TPH flat ~$116-119 (2023-25) while HOA/comm climbed $92→$122-125 (+33%); resets at renewal (2026 ptl $130). Sawtooth.
- Synthesis: muni margin ~$6/hr thinner post-renewal → ~$12/hr mid-contract. Action: escalators on muni renewals; weight growth to HOA/commercial; muni = base-load volume not margin.
- Captured → [[segment-margin-analysis]] (wiki/projects). Open: (b) cost data from Steve; confirm DIR determination per city contract; then build segment-aware pricing.

## MuniBot: vision fix + on-demand dashboard capture (Skipper build session)
- **Vision fix:** MuniBot "couldn't read screenshots" = aux vision misrouted to glm-5.2 (403). Repointed `auxiliary.vision` → Gemini 3.1 Pro + `GEMINI_API_KEY` in .env (PAID key). Restart. Verified + Skipper live-tested. Also fixes browser_vision. → [[PLAYBOOK]] infra/agents/hermes-vision.
- **Can produce screenshots:** yes — headless chromium (playwright-core) launches + captures in-container (tested).
- **On-demand City Budgets capture for Brent (WIRED):** Brent asks MuniBot → it screenshots the live dashboard → sends PNG on Telegram.
  - Auth persistence solved: play refresh had WIPED the custom munibot@ + herman/90377 flow.Users logins. Fix (Skipper): use existing PROD accounts that survive refresh → MuniBot=`GSTCTest`(205), Gilligan=`TrimIT`(1, already on dashboard allowlist). Creds in `~/.munibot/home/.secrets/trimit-web-login.env` + `~/.secrets/gilligan-trimit-login.env`.
  - Granted GSTCTest (205) dashboard access: INSERT into `Workbench.dbo.DashboardAccess` (allowlist by UserID).
  - Real URL `/GSTS/Dashboard-CityBudgets.cfm` (found via SSH `where /r`). Welcome modal auto-dismissed (`#wcGotIt`/`#welcomeOverlay`).
  - Build: `~/.munibot/home/dashboard-capture/{capture.js,capture.sh}` + skill `productivity/dashboard-capture` (reply w/ MEDIA:<path>). Restarted MuniBot to load. **Awaiting Brent live test.**
