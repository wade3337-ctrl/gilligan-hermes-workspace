# 2026-06-22 — Daily Log

## ⭐ NEW: Jordan + Travis already building a pricing tool (fold into Part B)
- Skipper (2026-06-22): **Jordan (IT) + Travis (Data Processing) have been building a pricing tool already**, housed
  on the **PRODUCTION server**. Skipper wants to **focus our efforts with what they built INCLUDED** — it may *become*
  the v1 pricing front-end (or reshape the "Price a Job" first slice). He'll share it.
- **Getting files to me:** I have SSH to the **play** server but NOT prod files (prod DB still firewalled — IP
  76.32.188.157 needs allowlisting + port from Travis). Recommended transfer: Jordan/Travis **drop a zip on the play
  box** (`gstsdatabase` 100.86.97.46, e.g. `D:\trimit-analysis\pricing-tool\`) → I pull it; or email to
  `gilligan.gsts@gmail.com`; or Discord attach. Examine as Track-1; **keep arbor-core itself confidential.**
- ⏭️ **RESUME:** get the pricing-tool files → review → decide how it fits (adopt / evolve / align) → continue Part B
  ("Price a Job" first slice). Build = still a FUTURE decision (design phase).

## Misc
- Sent Skipper a copy of **Garrett Cornish's** daily "Your Jobs" email (Jun 22). **Garrett = `gcornish`** (one of the
  4 pilot reps: Chesley/echesley, Cornish/gcornish, Barker/rbarker, Griffiths/sgriffiths → Nate/nperkins rollup).
  ⚠️ Rep daily emails are **plain-text only** (no HTML part) — extract `text/plain` when copying/forwarding them.
- I can read **`gilligan.gsts@gmail.com`** inbox via IMAP (creds `arbor-stack/anomaly-monitor/.secrets/gmail.json`),
  but **NOT** the Skipper's `jwade@gstsinc.com` work inbox (no M365 access). Inbound to jwade must be forwarded to me.

## 🧠 Knowledge-management system BUILT (the Hermes self-improvement model)
- `LESSONS.md` (what didn't work) + `PLAYBOOK.md` (what works — Hermes self-improvement file), seeded + tagged.
  **Auto-capture rule in `AGENTS.md`** (in-the-moment) + **weekly cron** (Sun 6am PT, isolated, silent, jobId
  `58f0eb65-…`) that snapshots→distills→prunes. Wired into `ROUTING.md`+`MEMORY.md`. **Rollback:** baseline snapshots
  in `memory/_backups/` + git + cron pre-edit snapshot + conservative-only edits.
- **GLM reliability:** root-caused the `claude -p --model glm-5.2` CLI — **HANGS (124) OR SIGKILLs (137)** on agentic
  tool loops (CLI orchestration broken w/ backend, NOT GLM). Built **`~/arbor-core/crew/glm-judge.py`** = GLM does
  live-DB verification via a stable Python tool-loop (`RUNSQL:`/`VERDICT:`, my code runs gsql). Tested ✓.

## 💻 GLM set up & WORKING on laptop Herman (2026-06-23)
- Root cause: GLM was **never actually configured** (no key/client/provider) — Herman's "setup" didn't land.
- Fix: **dedicated laptop key** (separate from main box) at `C:\Users\wadej\.secrets\glm.json`, `icacls`-locked
  (SYSTEM/Admins/wadej, inheritance stripped); deployed `glm-ask.py` to `C:\Users\wadej\`. **Verified live twice**
  (LAPTOP_GLM_LIVE, HERMES_GLM_OK) via Python312 + the hermes venv python.
- My laptop SSH key had been **de-authorized**; Arduino Herman re-added it to `administrators_authorized_keys`.
- ⏭️ Open: deeper **Hermes-agent model integration** (agent auto-uses GLM) not done — needs the Hermes config
  mechanism. Laptop can now reliably CALL GLM via `glm-ask.py`. (One transient z.ai 529 mid-test; client fallback handled it.)
