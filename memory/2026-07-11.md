# 2026-07-11

## V1.5 Landing Assistant — kicked off (Skipper go)
- Skipper: make the V1.5 landing-page LLM-chat placeholder real, using the **local model on this box**, grounded on live TRIM IT but scoped to **only landing-page-reachable pages**.
- Confirmed local model: **llama3.2:3b** on Ollama (`localhost:11434`, 3B Q4 — fast/tiny → grounding mandatory).
- Built the assistant's **Obsidian brain-vault** at `landing-assistant/` (8 atomic notes: HOME, identity, data-scope-contract, scope-map, architecture, guardrails, capabilities-v1, open-questions).
- Key design: model = language layer only; **truth = the dashboards' own live queries** injected at answer-time. Scope rule reuses existing role-gate → no new data/security surface.
- Registered: `wiki/projects/v15-landing-assistant.md` + PROJECTS.md pointer.
- **Network RESOLVED = Tailscale.** Boxes are in different locations but same tailnet. This box `gilligan`=100.82.161.7 (Ollama). TRIM IT/CF server likely `gstsdatabase`=100.86.97.46 (confirm). CF → `http://100.82.161.7:11434`, WireGuard-encrypted, no SSH tunnel needed.
- **PROVEN end-to-end:** live grounded chat call over the tailscale IP → correctly computed Anaheim remaining $187,550 from injected figures, obeyed "never say paid". Latency cold ~21s (model load) / **warm ~2s**. Findings: set Ollama keep_alive to avoid cold starts; **must inject metric definitions** — 3B mislabeled TPH as "tons/throughput per hour".
- **Security to harden pre-go-live:** Tailscale ACL (only CF node → gilligan:11434) + bind Ollama to 100.82.161.7.
- **Wire BUILT + deployed:** Skipper confirmed gstsdatabase=TRIM IT + green-lit. Built `AI-Chat.cfm` (cfhttp → Ollama, hand-built lowercase JSON, keep_alive 30m, reads ZUserID). scp'd to D:\ webroot (absent before, new file). Render-verified via view.sh: valid JSON, 0 CF errors, read you_are:376, graceful failure. Ship-log #118 + detail file.
- **⛔ BLOCKER for end-to-end = Tailscale ACL (Skipper's console).** gstsdatabase (tagged) not in gilligan's allowed-src list → 11434 filtered (tailnet up, ping 57ms). Grant needed: `{"action":"accept","src":["100.86.97.46"],"dst":["100.82.161.7:11434"]}` at login.tailscale.com → Access Controls. This IS also the security lock. After save → green, no code change.
- **Next after ACL:** re-verify end-to-end → ground Revenue Performance (inject its query) → role test → widen per scope-map. Did NOT register on live Reference page (not advertised until it works).
