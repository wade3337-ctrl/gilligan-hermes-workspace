# 2026-07-11

## V1.5 Landing Assistant — kicked off (Skipper go)
- Skipper: make the V1.5 landing-page LLM-chat placeholder real, using the **local model on this box**, grounded on live TRIM IT but scoped to **only landing-page-reachable pages**.
- Confirmed local model: **llama3.2:3b** on Ollama (`localhost:11434`, 3B Q4 — fast/tiny → grounding mandatory).
- Built the assistant's **Obsidian brain-vault** at `landing-assistant/` (8 atomic notes: HOME, identity, data-scope-contract, scope-map, architecture, guardrails, capabilities-v1, open-questions).
- Key design: model = language layer only; **truth = the dashboards' own live queries** injected at answer-time. Scope rule reuses existing role-gate → no new data/security surface.
- Registered: `wiki/projects/v15-landing-assistant.md` + PROJECTS.md pointer.
- **Network RESOLVED = Tailscale.** Boxes are in different locations but same tailnet. This box `gilligan`=100.82.161.7 (Ollama). TRIM IT/CF server likely `gstsdatabase`=100.86.97.46 (confirm). CF → `http://100.82.161.7:11434`, WireGuard-encrypted, no SSH tunnel needed.
- **PROVEN end-to-end:** live grounded chat call over the tailscale IP → correctly computed Anaheim remaining $187,550 from injected figures, obeyed "never say paid". Latency cold ~21s (model load) / **warm ~2s**. Findings: set Ollama keep_alive to avoid cold starts; **must inject metric definitions** — 3B mislabeled TPH as "tons/throughput per hour".
- **Security to harden pre-go-live:** Tailscale ACL (only CF node → gilligan:11434) + bind Ollama to 100.82.161.7.
- **Wire BUILT + deployed:** Skipper confirmed gstsdatabase=TRIM IT + green-lit. Built `AI-Chat.cfm` (cfhttp → Ollama, hand-built lowercase JSON, keep_alive 30m, reads ZUserID). scp'd to D:\ webroot (absent before, new file). Render-verified via view.sh: valid JSON, 0 CF errors, read you_are:376, graceful failure. Ship-log #118 + detail file.
- **⛔ BLOCKER for end-to-end = Tailscale ACL (Skipper's console).** gstsdatabase (tagged) not in gilligan's allowed-src list → 11434 filtered (tailnet up, ping 57ms). Grant needed: `{"action":"accept","src":["100.86.97.46"],"dst":["100.82.161.7:11434"]}` at login.tailscale.com → Access Controls. This IS also the security lock. After save → green, no code change.
- **✅ ACL added 03:35 UTC → GREEN end-to-end.** Root cause: tailnet ACL only let `autogroup:member` initiate; gstsdatabase carries `tag:reachonly` (reachable, can't initiate). Added grant src 100.86.97.46 → dst 100.82.161.7:11434. Via view.sh: real llama3.2 reply through the CF page; grounded test "Anaheim invoiced 312,450 / 500,000" → "$187,550 left", said *invoiced* not *paid*. Warm ≈2s / cold ≈18s. Ship-log #118 now 🟢.
- **✅ GROUNDED on Revenue Performance — live numbers end-to-end** (ship-log #119). Added `?aisummary=1` JSON branch to `Dashboard-RevenuePerformance.cfm` (emits its own computed totals, aborts before HTML, normal render byte-identical). `AI-Chat.cfm` fetches it, pre-formats a fact list in CF, llama3.2 rephrases. Live: on-pace→"ahead by $63,253"; TPH→"$179.44 ABOVE $130"; revenue→"$655,561"; out-of-scope→redirects. Warm ~1-2s.
- **🥷 Root-caused the DUAL-WEBROOT SHADOW** (cost ~40 min): CF serves `C:\ColdFusion2023\cfusion\wwwroot\GSTS\` over D:\ — dashboard was a stale 6/27 C:\ copy, D:\ edits did nothing; NEW files fall back to D:\ (masked it). Not trusted-cache/IIS-cache/CF-restart (all ruled out). Fixed → deployed to both webroots (C:\ backed up). New note [[trimit-dual-webroot-shadow]]; LESSONS + PLAYBOOK updated. **8 GSTS .cfm shadowed — audit pending.**
- **3B model lessons:** don't feed JSON / don't let it compute — pre-format facts + pre-compute verdicts in CF, model only rephrases. Then numbers exact.
- **✅ Chat box WIRED into the landing page** (`Dashboard-V15Home.cfm`, ship-log #120) — dead placeholder → real chat log + input, JS `fetch`→`AI-Chat.cfm` (mirrors their ZTest-Todo pattern), user/bot bubbles + thinking state. Backup-first, D:\ only (no shadow). Render-verified: 0 CF errors, POST returns grounded reply. Usable in-browser now at `play.greatscotttreeservice.com/GSTS/Dashboard-V15Home.cfm`.
- **✅ ANY time period now works** (ship-log #121). Was current-month-only. `AI-Chat.cfm` parses the period in CF (months/quarters/this-last-month/this-last-year/YTD/week) → POSTs date range to `?aisummary=1` (dashboard already honored form.startDate/endDate). The 3B kept overriding facts with its own date reasoning (invented months/numbers, even temp 0.8) → switched to **trust-guarded compose**: CF composes the exact sentence, model (temp 0) only rewords, reword accepted only if it keeps the number AND no wrong month. Verified: June/Mar/Q2/YTD/last-year/pace-this-month all correct. Only-trustworthy-data honored.
- **Two more small-model lessons** (in LESSONS/PLAYBOOK): Ollama default temp 0.8 → set temperature:0 for facts; never let a 3B reason about dates or emit the number — compute in CF, model is a skin.
- **Added goals intent** (Skipper testing found it couldn't state the sales goal). Now answers monthly ($2,200,000) + annual ($26,400,000). ⚠️ **OPEN DATA Q:** dashboard holds a FLAT $2.2M/month (GoalSettings default, same every month) → annual = ×12. Tree care is seasonal, so real monthly goals likely vary. Asked Skipper: are goals flat or seasonal + real annual? If seasonal, need a goals source (e.g. Workbench.dbo.SalesGoal by month/year) the assistant + dashboard read.
- **✅ REAL seasonal goals wired** (ship-log #122). Skipper pointed me to the financial reports he sent today (`wiki/reference/gsts-financials-2026-*`). Extracted the 2026 seasonalized monthly revenue budget from the exec deck (`exec-financials/January...xlsx` → `2026 Budget` row 9, hand-parsed xlsx since no openpyxl) = real sales goals, sum to $24.0M annual. Wired into `AI-Chat.cfm` (GOALS2026): monthly/annual answers + pace recomputed vs real month goal (July ahead $93,644). New source note [[gsts-2026-sales-goals-monthly]]. ⚠️ dashboard UI still flat $2.2M → follow-up to seasonalize it so UI matches assistant.
- **✅ DURABLE goals system built** (ship-log #123, Skipper chose durable + "import through the dashboard"). `Workbench.dbo.SalesGoal` table (survives refresh) seeded 2026 ($24M). New `Dashboard-SalesGoals.cfm` editor (12 fields + paste-import + save; verified via FY2099 test). Assistant + Revenue dashboard BOTH read the table → they now agree (dashboard goal tile $2,087,119 = July seasonal, was flat $2.2M). "Goals" link added to dashboard header. Deployed both webroots (dashboard shadowed). Next year = edit numbers, no code.
- **Next:** repurpose dashboard's superseded inline goal input; exec-gate the Goals editor before prod; create SalesGoal in PROD via dev; role-gating on assistant; ground City Budgets/SPM; audit 8 shadowed .cfm.

## Crew model upgrade (Skipper)
- Skipper: lock crew to newer models — we were on lesser ones. Verified both respond, then locked defaults: **gemini → gemini-3.5-flash** (was gemini-3.1-pro-preview), **kimi → kimi-k2.7** (was kimi-k2.6, also fixed hardcode in kimi-worker.py:150 + _TEMPLATE). Edited gemini-ask.py/kimi-ask.py/kimi-worker.py/_TEMPLATE-ask.py; defaults confirmed running without env override. glm-5.2 / fable claude-fable-5 / codex gpt-5.6-sol unchanged. → [[arbor-core-crew-infra]].
- **Codex = code worker too (Skipper):** not just reviews. Codex CLI (gpt-5.6-sol, logged in, headless `codex exec`) now has crew wrappers `codex-code.sh` (workspace-write sandbox, `CODEX_CD` target repo — verified writing a file) + `codex-review.sh` (`codex exec review --uncommitted`). Documented in [[arbor-core-crew-infra]].
- **Next up:** Skipper about to hand an OVERNIGHT job for the arbor helper (V1.5 landing assistant) — me + crew (now: gemini-3.5-flash, kimi-k2.7, glm-5.2, fable, codex code+review).

## OVERNIGHT JOB — Arbor Helper full rebuild (ship-log #124) ✅ SHIPPED
- Skipper (going to bed) spec: teach it every V1.5 dashboard, loosen guardrails (fun/utility), wiki-file knowledge, crew design+review, polish (mascot/chips/wording), actions. 3 must-haves: dashboard answers, off-topic fun, polish.
- **Crew genuinely used:** 3-lab design panel (gemini/glm/fable) → I synthesized → built → 3-lab adversarial review (gemini/glm/kimi) caught the CF `##`=`#` bug + header-injection + weather-thread → fixed.
- **Delivered on play:** `AI-Chat.cfm` router v2 + `ai-kb/` 7 KB files. Explainers for ALL 6 dashboards; live figures for 3 (Revenue, City Budgets, Production Performance via `?aisummary=1` hooks); meta (time/date/identity/purpose/help); fun (joke bank/poem/weather); navigate button; polished (mascot, chips, nav buttons, warm copy). CF composes facts, 3B only rewords under number+month guard — numbers never hallucinated.
- **CF lessons (in LESSONS.md):** `##`=`#` in strings (use `####`); `var` illegal outside functions; emoji needs `<cfprocessingdirective pageencoding="utf-8">`; line-loop beats `find(chr(10))`.
- **Gaps documented for the Skipper:** figures for Executive/SPM/Sales Cockpit (explainer-only tonight); write-actions add-todo/save-note/set-goal (designed, not built — navigate only); role-gating. Checkpoint: `landing-assistant/OVERNIGHT-BUILD-2026-07-11.md`.

## Arbor Helper — morning iteration (Skipper live feedback, ship-log #125)
- Fixed UX: garbled header entity, chips eating reading space, links → new tab, per-reply 🔊 removed → **mascot is now a sticky voice on/off toggle** (auto-reads replies; localStorage), male voice (browser TTS; no ElevenLabs key here — offered as paid upgrade).
- **"Still too guardrailed":** added DERIVED math answers (gap-to-goal, per-day-to-goal, ahead/behind-plan, at-this-rate, actual-vs-projected) — CF-computed exact.
- **Executive grounding (the "Scott's sales" class):** Sales by Rep (invoiced NetTotal by rep, any period — "Rebekah Barker invoiced $173,747 in June"; rep roster cached from SalesReps; two Scotts handled) + Crew Performance (added aisummary to Executive$Sales$ByCrewName.cfm, ZStartDate/ZEndDate — "most productive crew in 2025 = Luis Cuevas $138.2/hr"). Historical years work.
- **SPM fully grounded (all 4 tabs):** Production drill-down ("Scott's low jobs last week + why" — per-job TPH/variance + ReasonCode) via aisummary on `SalesProductionMeeting$Production.cfm`; Pipeline (go-aheads $/count comm+muni), Sold (newly-sold $ + backlog), Results (completed YTD vs last-year YTD, commercial) via hooks on `$Pipeline/$Sold/$Results.cfm`. Router: answerSPMJobs + answerSPMFunnel; "sell/sold" (company) → SPM Sold not Revenue; results year = explicit or current.
- **Grounded dashboards now:** Revenue, City Budgets, Production Performance, Executive (Sales-by-Rep, Crew), SPM (Pipeline/Sold/Production/Results) + derived math. $-in-filename deploy = scp temp + single-quoted cmd/PowerShell copy.
- **+ SPM Results municipal** (relocated the Results hook after muActualYTD: commercial + municipal call-ins YTD vs last year) **+ Sales Cockpit figures** (aisummary on ZTest-SalesPipeline.cfm from qCards stage flags: running dry / bidding+$ / won-pending+$ / active; "1,619 running dry of 3,338", "$40.9M in open bids"). answerCockpit + routing; funnel excludes "account" so go-ahead-accounts→Cockpit not Pipeline.
- **Grounded = EVERY V1.5 dashboard now.** Remaining: write-actions (add-todo/save-note/set-goal), role-gating. (assistant iteration, live on play)

## Arbor Helper — semantic fallback layer (16:26 UTC)
- Added `classifyIntent()` to `AI-Chat.cfm`: when the deterministic router misses (was going straight to out-of-scope), the 3B now maps the fuzzy question to ONE category from a closed list (revenue/city_budgets/production/sales_rep/crew/spm_jobs/spm_funnel/cockpit/none), then we run THAT handler's real SQL. Model only picks a category — never a number. Ollama down → "" → out-of-scope (same as before). New section `5z` before out-of-scope; intent tagged `semantic-<key>`.
- Deployed to play (backup ah-semantic-20260711-162634). Verified: deterministic paths unchanged; fuzzy paraphrases now land — "how are we doing compared to what we hoped for"→revenue, "which of my guys in the field is crushing it"→crew, "are any of my customers about to run out of work"→cockpit, "who is my rockstar closer"→sales_rep (added 'closer/rockstar' hint). "capital of france"→still out-of-scope. This is the loosen-the-middle-layer upgrade w/o giving up the no-hallucination guard.
- **Fix: annual gap-to-goal.** "what is our goal for this year and how much more to schedule" was answering with JULY's monthly goal/gap — the GAP-TO-GOAL block had no year branch (always single month). Added wantYear detection ("year/annual/yearly" + NOT single-month) → uses annualGoalFor() + full-year window (Jan1-Dec31, Monthly groupBy). Now: "$24M full-year goal, need $9.41M more, $14.59M scheduled." Monthly path unchanged. Grammar: folded article into gLabel so "To hit July's goal" / "To hit the 2026 full-year goal" both read right. Deployed (backup ah-annualgap-20260711-163606).
- **Company identity in Arbor Helper.** Skipper: it must know company name, years in business, core principles, mission & vision. Found them in the 2026 Employee Handbook (`trimit-knowledge/business/gsts-employee-handbook-2026.md`). Added Mission/Vision/Company-facts sections to `ai-kb/_site.md` + a `company-identity` router handler (name/years/founded/owner/mission/vision/principles, verbatim, no reword). Name CONFIRMED = "Great Scott Tree Care" (GSTC), founded 1976 by Scott Griffiths (still Pres/Owner), 50 yrs, 100+ employees. NOTE: handbook has NO explicit "Core Values" section — used the handbook's stated aim (outperform in employment/service/safety) as principles; flagged to Skipper to supply a formal list if one exists. Saved verbatim to wiki [[skipper-and-company]].
- **Bug caught + fixed (pre-existing): meta-count over-greedy.** "what does the city budgets page do" was hijacked by 2b meta-count ("what...page" matched) → returned "I know 6 dashboards". Tightened 2b to require count/list intent (how many + noun, OR list/which/what + noun + do-you/can-you/tell-me/to-try). Now explainers route to page-explainer; count questions still work. Deployed (backup ah-company-20260711-165934).
- **Core values confirmed** by Skipper: Quality, Integrity, Safety, Innovation, Caring. Added to `ai-kb/_site.md` (Core values section) + company-identity handler (wantPrin now recites the 5). Broadened trigger to catch "company values"/"what does the company value" (was slipping to semantic-revenue on the word "value"); guarded so monetary "value" (open bids $40.9M) still routes to figures. Deployed.
- **Gave Herman the full Track-1 picture** (Skipper: "herman should know everything, all arbor helper AND V1.5 info"). New `references/v15-arbor-helper-reference.md` in his vault — architecture (two-machine/Tailscale), deterministic-first router + all intents, `?aisummary=1` grounding, 6 dashboards + scope contract, KB, guardrails, the 2026-07-11 intelligence pass, deploy/ops (keys/webroots/dual-shadow/kbreload), state+gaps+graduation. Plus `references/gsts-company-identity.md` earlier. Both linked in Herman HOME index under new "Operational references". Note marks Track-1 vs Track-2-BLACK clearly (info flows INTO his brain; no arbor-core leak out).

## RC-02 deploy-bundle prep — seasonal goal reconciled (21:0x UTC)
Skipper asked what's left on the 4-dashboard deploy bundle, then flagged the seasonal budget-upload was added to Revenue Perf AFTER RC-02 was "verified." Went with my recs:
- **Source drift FIXED:** local `production-dashboard/Dashboard-RevenuePerformance.cfm` was the Jun-25 FLAT version; play has the seasonal `Workbench.dbo.SalesGoal` read. Building the package from local would've regressed prod. Pulled play→local (both webroots identical), backed up stale.
- **Crash-guard added:** the seasonal `<cfquery>` had no cftry; "fallback" only covered empty table, not ABSENT → prod would 500. Wrapped in cftry (missing table → flat goal, order-independent). Deployed both webroots, re-verified $2,087,119 seasonal, 0 errors.
- **Exec-gate** on `Dashboard-SalesGoals.cfm` (was ungated): flow.Users title/login, fail-closed to leadership. 4 paths verified. **KEY CATCH: jwade's flow.Users title = stale "Fleet Manager" → login allowlist (not title) is what lets the Skipper in.** CF trap: `gt` = reserved GT operator → renamed gtitle.
- **Prod DB script** `RC-02-SalesGoal-create-seed-PROD.sql` (idempotent, insert-if-missing, 12 rows/$24M) validated on play (12/$24,000,000.00).
- Bundle doc updated (RC-02 scope + status + "no DB changes" amended). Ship-log #126.
- **Ship-log numbering quirk noticed:** two overlapping series both hit 118-120 (top table = Jul-10 forecasting; lower = Jul-11 landing-assistant). Used #126 to sit on top. Cleanup candidate, not urgent.
- **Still open (Skipper decisions):** RC-06 turnover label (likely closed by #118 amber badge — confirm), RC-03 Brent gate (ship-as-is rec), RC-04 −$141K Nate basis-gap (accept+document rec). Then P2 manifest+smoketest, P3 package.
- **RC-06 turnover-label CONFIRMED CLOSED (23:5x UTC):** amber "prior FY · new year (26/27) just starting" badge live-verified across all 11 municipal cities — 7 in-grace show FY25/26+badge, 26/27 cities no badge, 0 CF errors. Reads clearly. Also fixed a 3rd drift (play had the ?aisummary=1 hook, local did not) → synced local←play (gated/inert on prod). Bundle doc RC-06 = complete-verified. Next Skipper decisions: RC-03 Brent gate, RC-04 Nate -$141K gap.
- **RC-03 NTE carryover BUILT (00:1x UTC 7/12).** Skipper: "we need the carry over; we never built it" → option B. Built Multi-year NTE panel on City Budgets single-city view for Newport(295963)+Industry(295924): carryover from prior yrs (Σ prior budget−invoiced, excl-Homeowner to match dashboard), adjusted available, contract-to-date vs cap, prior-year table; Remaining folds carryover in + Carryover tile reconciles. From TRIM IT Contracts.Year01-05. HARD-CODED the 2 (auto-detect over-triggered Irvine/LongBeach). Verified: NP 26/27 +$35,430.70, NP 25/26 +$4,711.31, Industry $0. Brent gate resolved ($178K=Brent Excel artifact). Ship-log #127. RC-03 complete-verified. **Bundle: RC-02/03/04/06 all P1 done. Remaining Skipper decision: RC-04 Nate -$141K (accept+doc rec). Then P2 manifest+smoketest all 4, P3 package.**
- **RC-04 accepted** (−$141K = report-vs-report definitional diff, documented). **Phase 2 DONE:** deploy-manifest.js ran on all 4 → full deploy set captured in bundle doc; prod-missing = 4 SPM funnel pages ($Pipeline/$Sold/$Production/$Results) + 6 assets (pro-tips.js, tree-helper png, gsts-tokens.css, protips.css/js) + Dashboard-SalesPipeline.cfm (linked off Revenue Perf — scope Q for Skipper). Smoke-test dry-run vs play = 0 missing (clean baseline). **All 4 dashboards P1-complete + Phase 2 done.** P3 package pending: Skipper wants to fold in a Boss Herman handoff first. RevenuePerf → BOTH webroots; RC-02 DB step = SalesGoal seed SQL.
- **V1.5 6-dashboard deploy package DONE (01:4x UTC 7/12).** Skipper green-lit hardening. Folded Herman handoff (Dashboards submenu on Profile$Main.HiRes.cfm, Jason-pilot). Crew found broken-access-control (garbage cookie → financials + aisummary JSON) → built shared dashboard-auth-gate.cfm on ALL 20 leak-surface files (real flow.Users user + authorized set: pilot 9/376 + exec/finance leadership; JSON/HTML 403). Fixed Cockpit XSS (SerializeJSON→script, </-escaped) + dead code. VERIFIED: garbage + non-exec → 403 everywhere; Jason/President/bot → 200; all 6 render 0 err; assistant still live. Deferred ZTest/Beta renames (wired into menu/assistant). Assembled DEPLOY-PACKAGE-V15-DASHBOARDS.md. Ship-log #128. **Deploy bundle COMPLETE — ready for dev handoff.**
- **Mobile check (02:1x UTC 7/12):** Skipper asked if all 6 dashboards are mobile-friendly. Rendered all 6 at iPhone width (390px, browserless :3010, cookie ZUserID=9). **YES** — all reflow/stack cleanly: welcome cards scale, Cockpit Kanban→single column, SPM/Production/Revenue/Exec KPI cards + shells stack. Bonus catch: Production welcome modal had `&##`-double-hash emoji entities (CF ##-entity trap, LESSONS) rendering as literal `&##127981;` text — fixed the 7 outside-cfoutput ones (left the inside-cfoutput `&##9632;`), deployed D:\, re-rendered → emojis correct, 0 CF errors. Other 5 dashboards clean.
- **Cockpit logic review + fix (02:3x UTC 7/12).** Skipper: review before deploy; "1585 bidding is wrong, each job = one column." Reviewed stageOf cascade (sound) + verified StatusDef IDs vs TRIM IT. Fixed non-exclusive aisummary → exclusive stageOf: followUp 1559/bidding 1411/won 73/active 243/done 52 = 3338 total (reconciles); runningDry 1619 = overlay. Assistant now 1,411 bidding. Ship-log #129.
- **Follow-up added to Arbor Helper (02:3x UTC 7/12).** answerCockpit now surfaces followUp (1,559, biggest bucket) + done (52); summary shows all 5 stages + dry overlay. Router 5h + classifyIntent updated for follow-up phrasings. Deployed AI-Chat.cfm. Edge: "chase" collides with rep Chase Schroeder (1135) → routes to rep (left as-is; follow-up uses unambiguous triggers).
- **Final deploy-package hardening pass (02:5x UTC 7/12).** Dual-webroot shadow AUDIT: of 24-file set, only RevenuePerformance + dashboard-auth-gate shadowed on play (both deployed both webroots ✓); Cockpit/SPM/Exec/CityBudgets/Menu D-only → my deploys are served (confirmed by 403 tests). Final render: all 6 + menu 0 errors as Jason; garbage→403 (Cockpit/Exec); smoke-test 0 missing assets. Added to package: PROD-FIRST critical check (gate FAILS CLOSED on flow.Users error → test Jason on prod BEFORE full rollout) + prod-shadow re-audit note. Remaining = prod-side execution safety + minor (assistant graceful-403, Cockpit 1.36MB weight, Exec figures not penny-verified). Chase deactivation SQL prepped (deferred).
- **Executive reconcile DONE (03:1x UTC 7/12, ship-log #130).** Sales-by-Rep 3-way penny match (per-rep sum=MeasuredTotal $1,780,342.78; render matches; RB ties to assistant). Sales-by-Market penny-tie MarketSum=BookedTotal $1,953,835.73. Closing apped ≤100- **Single access point BUILT (16:3x UTC 7/12, ship-log #131).** Skipper wanted data-driven access. Created Workbench.dbo.DashboardAccess table + Dashboard-Access.cfm admin page (search->grant/remove, gated 9/3). Rewired dashboard-auth-gate.cfm to read the table (bootstrap 9+376; leadership cftry-fallback). LIVE-verified add/remove (71 grant->200, revoke->403). Package: 2nd DB step + admin page added.
- **Mobile QA + protip fix (19:1x UTC 7/12, ship-log #132).** Skipper caught Exec filter bar showing protip "?" as big empty boxes on mobile. Root cause: badge is a <button>, page nav.filter-bar button rule out-specifies it. Fixed shared assets/protips/protips.css (badge !important sizing → stays 14px circle). Fixes all 6. QA all 6 at 390px = clean (Exec fixed, others good; SPM blank was render-timing not a bug).
