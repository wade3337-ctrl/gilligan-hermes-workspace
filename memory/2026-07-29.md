# 2026-07-29

### 23:59 UTC — Gilligan model default moved off Anthropic to GPT-5.6 Sol
Skipper reported Anthropic repeatedly failing/overloading and asked for 5.6 Sol selectable in Discord. Backed up `~/.openclaw/openclaw.json` to `openclaw.json.bak-pre-gpt56sol-20260729T2359Z`, added `openai/gpt-5.6-sol` (alias `sol`) to `agents.defaults.models`, set `agents.defaults.model.primary` to it, and put it first in this DM's Discord model-picker recents. Verified exact Codex slug from `codex debug models` (`gpt-5.6-sol`, display `GPT-5.6-Sol`) and OpenClaw status (`openclaw models status --plain` → `openai/gpt-5.6-sol`). Live session override still had stale in-memory allowlist until gateway reload, so queued a delayed `openclaw gateway restart` to apply it after the reply. Note updated: [[gilligan-session-settings]].

### 15:00 UTC — Play DB refresh STALE (automated monitor)
`play-refresh-monitor.sh` → `STALE|2026-07-26 03:03:20|83h`. **Same ongoing outage** as the 7/28 alert (vintage unchanged at 2026-07-26 03:03, aged 59h → 83h). Prod→rsync.net `GSTS_backup_*.bak` upload still not producing new files; play's `\GSTS DB RESTORE` keeps succeeding on the same stale copy. Sent Skipper a one-liner nudge (Jordan is the prod-side fix).


---

## The long session (02:19 → 18:50 UTC) — BOD dashboard, a verification gate, Arbor Helper, and the play migration

### 📊 Built the BOD Commitment Dashboard on play (his cockpit)
`Dashboard-BODCommitments.cfm`, gated to UserID 9 + 376 only (340/209 verified 403). Seven design
decisions taken up-front → [[bod-commitment-dashboard]]. Tile 1 tracks **15 NET ADDS off a frozen baseline
of 79**, landing at **94** — not the plan's 91, which came from a holiday-skewed mean of 76.

**Five wrong numbers caught before anyone saw them:**
1. The documented revenue filter `IsProForma=0 AND IsCredit=0` returns **ZERO rows** — both columns are
   100% NULL. Would have shown **$0**. Correct basis `SUM(Invoices.Total)` = H1 $11,078,311.77.
2. "Typical weekday" as a **mean** turned July 4th (18 people) into a fake staffing collapse — 74.1 vs a
   median of 78.5. Now a median.
3. The "unbooked hours" gap is **96% INTERNAL time**, not unclosed paperwork → [[pending-crewsheet-closeout-gap]].
   Third revision of that finding.
4. The board's **$2.31M/mo lands at $25.08M**, not the $25,300,976 he made authoritative — $218K short.
5. A seed script still hardcoded the old **$24.0M** goal and would have silently re-broken RGC.

### 🔎 He found four defects in work I had called "verified"
RGC showing all em-dashes · the approved-unscheduled drill listing finished jobs · the 3-people-in-October
headcount · and a claim that Revenue Performance was broken **when it was not** (I read a form-field default
as a stale goal without tracing what overrides it — he was right, I was wrong).
**None were reasoning failures. All four were scope failures.** → led directly to the gate below.

### ✅ Adopted a verification gate (his standing instruction)
`arbor-stack/production-dashboard/verify-build.sh` — auth · render · **drill total == tile** · assets ·
stale. Exit code = FAIL count. Plus two tooling-free rules now in [[repair-contract]]: *verify the
neighbours, not just the change*, and *every figure I report must come from a query I ran THIS session*.
**Then he asked whether I could reliably FIND the contract — and that exposed a worse problem:**
`ROUTING.md` pointed at `contracts/repair-contract.md`, a **5-week-stale duplicate**. All four contracts
were split that way. `contracts/*` are now stubs; `wiki/reference/` is canonical; gate blocks added to
`AGENTS.md`, `ROUTING.md`, and a `CLAUDE.md` tripwire in the build folder (subagents never load MEMORY.md).

### 🤖 Arbor Helper got a personality
Root cause of "it goes to the stock answer": nine data handlers, everything else hit one canned line.
**No download needed — llama3.2 was already installed and our code was gagging it.** Free-chat path +
`ai-kb/_persona.md` (his to edit, no redeploy). Raiders grudge, self-laughing jokes, **56 tree + 32 dad = 88**.
⚠️ **My first numeric guard FAILED live** — it was a blocklist of business nouns and the model walked past
it with *"about 80 folks in the field"*, a number **I had left in the persona file myself**. Now zero-digits
with a freeway exception. *A blocklist cannot work; enumerate what is permitted.*

### 🚚 Play is being replaced — capture executed same day
He confirmed **"we will lose the current play," end of this week** → [[play-to-infra-migration]].
Measured the exposure (8 drifted files, `Workbench` single-copy), captured everything, **PROVED the restore**
(36/36 tables, proc runs, 4/4 controls PASS), and wrote `rebuild-on-fresh-box.sh`.
Traps: SQL service account can't write to `C:\Users\`; `Compress-Archive` returns a **0-byte zip and
reports success** on `$` filenames; the webroot is **~330 GB** not the 277 MB a mid-write read reported;
`unzip` isn't installed here so an empty listing looked like an empty archive.

### ⚠️ Open / carried forward
- Re-take the `Workbench` backup on the LAST day before the wipe.
- Ask Jordan/Travis whether `Storage`/`Staging` carry over.
- Arbor Helper still mis-routes *"how many crews/trucks do we have?"* to a crew-TPH answer.
- The $218K board-vs-goal gap is noted, not resolved.
- **Play's DB restore has been stale since 2026-07-26** (see 15:00 entry) — every figure I pulled today is
  on 7/26-vintage data. Not wrong, but not "today".
