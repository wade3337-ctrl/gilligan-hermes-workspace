# 2026-06-13 (Sat) — daily log

## Sales Production Meeting Dashboard — concept + discovery + DIRECT SERVER ACCESS

### Report set complete (6 reports → one dashboard)
Skipper sent the remaining reports for the unified weekly production-meeting dashboard. Full catalog in `arbor-stack/production-dashboard/REPORTS-INVENTORY.md`:
1. Salesman Performance (weekly rev/TPH by rep, YoY)
2. Approved GoAheads last-7d (weekly inflow, by rep)
3. Sales Meeting worksheets (due dates, hot list[broken render], open proposals, close rate)
4. Production Meeting TPH Report (completed-WO perf vs $130 — ≈ our COO monitor)
5. Approved Work Awaiting Schedule (unscheduled backlog, City/Non-City)
6. 2026 Completed+Scheduled master (= Brent's Completed&Sold; June Sold Commercial $956,466 = our validated #)
→ Maps to 3 back-to-back Tue-morning meetings (Production 8:30 / Scheduling 9:00 / Sales 10:00, all Jeanie). Funnel: Pipeline → Sold → Production → Results.

### DIRECTION CHANGE (Skipper)
Not report-assembly — build it **LIVE, server-side, in TrimIT**: log in & view, no report pulls. Multi-layer; **page 1 = config/filter**; layers = the funnel. Page-1 filter = the SCALE mechanism (goal 2× revenue → division meetings → ONE dashboard filtered by area, not N dashboards). Saved Views = each meeting's filter preset. This IS the V1.5/Arbor-foundation overlap.

### Discovery (read-only Codex) — `production-dashboard/prod-dashboard-discovery-findings.md`
- **Customer Type 🟢** clean (group 11=Municipal 13,660 WO/$74.8M; Commercial=NOT 11, 38,510 WO/$281.8M).
- **Geography 🟡** NO North/South flag exists; only clean geo = county via Projects.GeoMarketID→GeoSegments (Orange/LA/Riv/SB/SD). Skipper: "county won't really work" → **SKIP geography for v1**.
- **Area Manager 🔴** not in data (closest "supervisor" mostly = Jeanie). **DEFER** (needs manual mapping).
- **v1 filter set = Customer Type + Date Range + Salesperson.** Geo/AM bolt on later via saved-Views (zero rework).
- Layer caveats to resolve at build: GoAhead approval-date sparse; "awaiting schedule" = WO StartDate IS NULL (657 vs Brent's ~dozen — tighter filter); miss-notes NOT in ReasonCodeNote (find real field).

### 🎉 DIRECT SERVER ACCESS — Tailscale + SSH (big milestone)
Set up so I can build directly (kills the Codex relay). Full detail: `arbor-stack/gstsdatabase-access.md`.
- New tailnet device **`gstsdatabase` @ 100.86.97.46** (windows). Reachable (58ms).
- Windows OpenSSH Server enabled; my ed25519 key (`~/.ssh/gstsdb_ed25519`) authorized for **Administrator**. Login verified.
- Confirmed on host: **sqlcmd** present, **web root** `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS` reachable. (PLAY/dev box — nightly copy of prod.)
- STILL NEED for SQL: the `trimit3` SQL-auth password (read from CF datasource config on box) — integrated `-E` may hit SSPI.
- Standing rules apply: backup-first to `\GSTS\Jasonsrepairs\`, default read-only, log mods in Ship Log.

### Workflow notes / lessons
- Codex email deliverables must go to **gilligan.gsts@gmail.com** (my reachable inbox), NOT "me"/wade3337/jwade. (Baked into the discovery prompt template.) Skipper forwarded this one.
- New helper: `anomaly-monitor/fetch-findings.js` (imapflow + mailparser) pulls inbound attachments from my inbox. Installed `mailparser`. Note: imapflow `search({since})` missed the forwarded msg (kept original date) — fetch newest by sequence instead.

### 🏗️ BUILD STARTED (Jun 13 late)
- **SQL access SOLVED the clean way:** `sqlcmd -S localhost,14333 -d GSTS -E` (Windows integrated auth as Administrator via SSH) WORKS — no trimit3 password needed. (The old SSPI failure was Codex's env, not my SSH-as-admin session.)
- **Helper built:** `production-dashboard/gsql.sh` — `echo "SELECT..." | bash gsql.sh` or `bash gsql.sh file.sql`. (scp's the query to server temp, runs sqlcmd -i; stdin-over-ssh failed with ReadTextLine, scp+-i is robust.)
- **Pipeline VALIDATED to the penny:** live customer-type split = Commercial 38,510/$281,808,421.40 · Municipal 13,660/$74,826,775.18 — matches discovery exactly.
- **Template reverse-engineered:** TrimIT dashboards = main `.cfm` + `*Header.cfm`(CSS/Bootstrap5/FA) + `*NavBar.cfm` + `*Selection.cfm` + `*JS.cfm` + content sub-pages. Studied `ContractDashboard.cfm` (343 lines, saved /tmp/tmpl). It ALREADY has: Commercial/Municipal toggle (URL.type→ProjectGroupDefID 14/11), KPI cards (Budget/Call-Ins/TPH) w/ up/down %-vs-target coloring, multi-page nav dropdown, drill-down links. GSTS green `##5C743C`, `LSNumberFormat`. **$130 target source = `dbo.GetSystemListItem('Targets','TargetTPH')`.**
- **Build plan:** new file family `SalesProductionDashboard*.cfm` mirroring ContractDashboard. Page-1 = filter (customer-type toggle + date range + salesperson). NavBar switches 4 layers (Pipeline/Sold/Production/Results). Each layer = content include w/ discovery-mapped queries. **Backup-first to Jasonsrepairs before writing any file to web root.**

### Design assets received (Jun 13, end of session)
- **🎨 GSTS UI Spec v1.0 = the design bible** → `arbor-stack/gsts-ui-spec-v1.0.md` (Skipper's "style folder"). Color tokens on :root (`/assets/css/gsts-tokens.css`), Pro Tip popup system (`/assets/protips/*`, `data-protip-key`, `<cfinclude /includes/protips.cfm>`), KPI tiles, header bars, totals strip, mobile-first, a11y, backup+QA rules. Build "to gsts-ui-spec v1.0".
- **🌳 "Little tree guy" = Herman's AI guide mascot** → synced to `~/herman-store/arbor-ai-helper/` (render + mascot specs + working prototype `prototype/inventory-pricing-guide-test.*`). Intent: always-available conversational guide evolving the Pro Tip pattern; code-native SVG rig preferred. ("Herman" = another OpenClaw agent on tailnet host `herman`/user arduino; synced via rsync to my box.)

### Next session (build, fast-start) — EVERYTHING is in hand
- Scaffold `SalesProductionDashboard.cfm` + Header/NavBar/Selection mirroring ContractDashboard template, BUILT TO `gsts-ui-spec-v1.0.md`. FIRST: check if `/assets/css/gsts-tokens.css` + `/assets/protips/*` already exist on server (reuse) else create per spec. Build layer-by-layer (Production/TPH first — validated, closest to monitor). Wire in the tree-guy guide (read mascot spec + prototype first). Backup-first to Jasonsrepairs; new Ship Log build entry.
- Pending from prior: Contract Dashboard prod deploy (IT) → confirm → Ship Log #11 Done; MEMORY.md tidy-up; salesperson reps M365 allowlist.

- **UI principle (Skipper):** all Sales Production Meeting dashboard pages must behave identically — user learns an interaction once, it works the same everywhere. New patterns roll to ALL applicable pages in one motion. (Now spec Section 0.) Rep-name drill-down live on Pipeline/Production/Results; KPI-box drill-down on all 4 layers; job rows open the TrimIT WO/proposal file.

- **Contract Dashboard — Long Beach $0 investigated (Jun 13):** Skipper saw City of Long Beach = $0 on the prod dashboard. NOT missing work — City of Long Beach (CompanyID 295947, the municipal acct) has ~$9.95M completed all-time (1,461 WOs) and $97,211.70 / 29 completed WOs in the CURRENT 26/27 year (Apr 1–Jun 9 2026). $0 because the dashboard reads the ACTIVE contract year (89866 = 26/27, started 2026-04-01) whose totals aren't rolled up: CompanyYears.TotalBudget=0/TotalPrice=NULL, and the 12 CompanyPeriods rows exist but CallInTotalPrice=$0. Prior years populated fine (25/26 $452K, 24/25 $851K, 23/24 $2.43M). SAME FAMILY as the Anaheim issue (current contract-year call-ins not materializing; Anaheim=missing rows, LB=empty rows). Likely needs the same call-in rollup/remediation for 26/27 on prod. (Read on play, which is pre-refresh; historical + raw-WO signals reliable; Skipper confirmed $0 on prod too.) Other "Long Beach" entities are non-municipal (Cal State LB $3.1M, LB USD $579K) so not on the muni dashboard.

- **CORRECTION (Skipper caught it):** my "active muni" list used today-BETWEEN-start/end on the CompanyYears row, which falsely flagged **Seal Beach** as active — it has ZERO completed work in 14 months (stale/placeholder year row). The right activeness test = recent completed work, not the date window. Refined affected list for the un-rolled-current-year issue: **City of Long Beach (295947)** = the real one (169 WOs/$1.17M last 14mo, 26/27 never rolled up); City of Aliso Viejo (297291) = minor ($35K, also empty); Anaheim already remediated on prod; Seal Beach/Aliso Viejo-stale = ignore. Lesson: when judging "active contract," check recent WorkOrders, not just CompanyYears date span.

- **CORRECTION #2 (Skipper, on Aliso Viejo):** Aliso Viejo is **Jul–Jun fiscal (25/26 = 2025-07-01→2026-06-30), same as Irvine/Newport/most muni** — NOT a different calendar. My "April-fiscal rollover" theory only applies to Long Beach + Anaheim (Apr–Mar). The TRUE common root of the $0s is NOT fiscal-year timing — it's that the **CURRENT contract year's periods/figures were never generated** for specific accounts: Long Beach 26/27 = 12 empty period buckets; Aliso Viejo 25/26 = 0 buckets generated (budget $50,900 entered, but TotalPrice/Hours NULL); while Irvine 25/26 (identical dates) = 12 full periods/$3.09M. No nightly job builds these (confirmed in Anaheim review) → they only populate when the figures-generation is triggered per contract; some accounts got triggered, these didn't. Remediation target (accounts w/ real recent work + un-materialized current year): **Long Beach 26/27** (big) + **Aliso Viejo 25/26** (small). Anaheim already remediated on prod. Lesson: don't theorize from fiscal-year patterns — the buckets-generated state is the signal.

- **PLAY TEST (Skipper authorized) — recalc does NOT fix Long Beach (Jun 13 eve):** Ran the scoped figures-recalc chain on play for City of Long Beach (295947), 26/27 (CompanyYearID 89866): UpdateCompanyCalendar$Figures -> UpdateCompanyPeriod$Figures -> UpdateCompanyYear$Figures for periods 331-342, then year rollup. Backed up CompanyPeriods/CompanyYears/CompanyCalendars to zBak_*_LB_20260613 first; cleaned up (dropped) after; scoped to CompanyID 295947 only; play unchanged + reverts tonight. RESULT: figures stayed NULL/$0. ROOT CAUSE PINNED: chain = WorkOrderCalendars -> CompanyCalendars -> CompanyPeriods -> CompanyYears. LB 26/27 CompanyCalendars rows EXIST (365) but are EMPTY and stayed empty after the calendar-figures proc -> the gap is UPSTREAM (work-order/contract data never built+linked to the 26/27 company-year). Recalc has nothing to sum. CONCLUSION: Long Beach needs the SAME full remediation Anaheim(90020)/Aliso Viejo(89934) got (proc fixes GenerateContractPeriod/GenerateCompanyPeriods$YearID + the data relink/regen scripts), with CompanyYearID 89866 ADDED to the target set — NOT a simple recalc. Couldn't fully replicate on play: play has OLD pre-fix procs + I don't have Codex's remediation SQLScripts. KEY: Long Beach was NOT in the original Anaheim remediation target set; Aliso Viejo 89934 WAS (acceptance $32,435.90) so it'll heal on refresh; Long Beach will NOT self-heal -> IT email needed.
