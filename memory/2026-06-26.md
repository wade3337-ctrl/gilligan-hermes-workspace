# 2026-06-26 — Crew stack hardening

Worked the crew stack with the Skipper, prompted by Gemini's "best stack for ColdFusion/DB"
blueprint (Opus=architect · GPT-5.5=reviewer · Gemini=schema · GLM/Kimi=cheap worker).
Validated it mostly matches what we built; closed the real gaps.

## Done
1. **Temperature 0 default** across all 3 crew helpers (`glm-ask.py`, `gemini-ask.py`,
   `glm-judge.py`) — grounded SQL/schema, no creative hallucination. `CREW_TEMP=<n>` env
   override for brainstorming. Verified both APIs accept the field. (arbor-core `06284c2`)
2. **Corrected-blueprint doc** in CREW.md — which models can actually run tool loops on
   *our* box vs Gemini's idealized table. (`7dede79`)
3. **`glm-worker.py`** — NEW cheap GLM file-editing producer tier. Harness does file ops
   (READ/LIST/EDIT/CREATE/CHECK), GLM drives via SEARCH/REPLACE, one action per turn.
   Safety: WORKER_ROOT confinement, timestamped backups, end-of-run diff, read-only CHECK
   allowlist, temp 0. Proven on a scoped CFML WHERE-clause fix. (`58a88a6`)
4. **Codex re-tested = RELIABLE** (v0.135.0). The Jun 25 "flaky/exit143" did NOT reproduce:
   8/8 clean across no-tool / file-read / 5× single-query / heavy 3-query adversarial verify
   (72s, correct PASS, used CONVERT(bit,1), self-cross-checked). The "0" red herring was MY
   prompt leaking a sentence period as a shell arg → `gsql.sh` cat-ed a dir → empty → misread
   as 0. Codex = a 2nd native tool-running agent now; CREW.md + MEMORY.md corrected. (`4fb21a0`)
5. **`gsql.sh` hardened** — guard stray non-file arg (was silently empty, misread as a real
   number). Helps ALL callers. (arbor-stack `af8b295`)

## Lesson
- Don't trust a "model is flaky" verdict without a repeat test — Codex was fine; the failure
  was transient + a prompt-hygiene bug on my end. Always check ground truth before blaming the tool.

## Open / next
- C (pressure-test): run a real task through the full producer→3-judge gate now that we have
  2 tool-runners + a worker tier.
- Kimi K2.6 still untried (option 3) — only if we want a 3rd worker.
