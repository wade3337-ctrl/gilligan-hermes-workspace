
## MuniBot warehouse — overnight delivery + cleanup (14:2x UTC)
- Bulk sync COMPLETED 07:00 UTC: 47G / 27,091 files — full multi-county tree (LA/OC/Riverside/SB/SD). Warehouse now 51G / 29,724 files.
- 4 targeted city drops landed ~05:15-05:18 UTC: Anaheim (610M zip→1.2G/2327f), Long Beach (217M→6.4G/3935f, incl Alamitos Bay nested), Aliso Viejo (219M), Cerritos (133M). Long Beach beat its 10h estimate — arrived same night.
- CLEANUP (Skipper approved): moved Anaheim→'Orange County/Anaheim' and Long Beach→'Los Angeles County/Long Beach' out of loose top-level 'CITY OF' folders; nested Alamitos Bay inside Long Beach; removed 5 stale .lnk stubs now backed by real data (Anaheim, Aliso Viejo, Cerritos, Lake Forest, Long Beach).
- 4 redundant source zips (~1.2G) moved to ~/.munibot/_trash/ (recoverable, not purged — awaiting Skipper OK to purge).
- LEFT: Orange County/Stanton.lnk (no data yet — Skipper checking for Stanton data); LA County 'CustomerInfo (192.168.1.6)' shortcut (source artifact).
- Gotcha: complex multi-step bash script (set -e + loops) tripped a malfunctioning permission-approval layer (ZodError). Individual simple commands went through fine. → prefer discrete simple commands over big scripts when approval prompts misbehave.

## Sales Cockpit — per-column dollar totals (ship #180, play-verified) 15:0x UTC
- Skipper ask: 'at the top of each column in the colored bar, show a total of the dollars represented in each column.' (He first said 'sales pipeline' then corrected: the COCKPIT.)
- File: Dashboard-SalesCockpit.cfm (the LIVE cockpit; ZTest ones retired). 3 edits: .clbl/.csum CSS, csum+s-<stage> span in each of 5 .chd headers, sum loop in render().
- Dollar rule mirrors cardHTML exactly: bidding=c.bidAmt, others=c.val -> header total reconciles with cards. Recomputes live on every filter.
- Deployed via scp to play D:\ web root (Administrator@100.86.97.46, key gstsdb_ed25519, ROOT D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS). No C:\ shadow. Backup-first to Jasonsrepairs + local .pre-coltotals-20260717.bak.
- Render-verified served page (curl + Cookie ZUserID=376 = bootstrap-allowed bot): 432KB, gate holds, 5 csum spans+JS+CSS present, 0 CF errors; payload carries val(279)+bidAmt(299) across 680 cards.
- Deferred hygiene (flagged to Skipper): Reference-RepairsAndScheme.cfm repairRows companion page is batch-behind (last built Jul 13, ~#161); catching up #162-#180 is a separate reconcile. Kanban card not created.

## Repairs ledger catch-up — DONE (ship #181, headliners-only) 15:3x UTC
- After the 180s CLI stall on "catch it up": #180 dollar totals were already live+verified; the ledger (Reference-RepairsAndScheme.cfm repairRows) was the stalled piece — it was behind at n=116/Jul 10, a FULL WEEK (not ~#161 as the earlier note guessed; the page's own n counter runs behind the ship-log #).
- Skipper chose scope B (headliners only). Ran as an isolated background sub-agent (so it can't stall the main session again).
- Added 14 curated rows n=117->130 (RGC Phase1, City Budgets Renewals+Forecasting, Cockpit stage overhaul+property search+dollar totals, SPM Results re-cut, V1.5 deploy package, dashboard single-access, Invoiced-Revenue-by-Market). Collapsed grouped ships; SKIPPED the mobile/pro-tip/color polish sweep per B. Jul16 #177/#179 folded to one row, #178 into Forecasting row.
- Counter tile now "121 repairs logged since Jun 10 2026" (107+14). Backup-first to Jasonsrepairs\Reference-RepairsAndScheme.pre-ledger-catchup-20260717.cfm. Ship-log appended #181 (header bumped to 181/2026-07-17).
- ⚠️ RENDER-VERIFY GOTCHA (capture): public DNS dev.greatscotttreeservice.com -> different box (198.207.148.188); play site is IP-bound to play.greatscotttreeservice.com (173.208.162.142). Curl from THIS workstation over the tailscale IP 404s (host-binding mismatch). Correct verify = run curl FROM the play server against https://play.greatscotttreeservice.com/GSTS/... (ZUserID=376 bot cookie). Verified that way: 200 / 0 CF errors.

## Warehouse: Stanton cleaned + re-litter swept + SOURCE fixed (15:5x UTC)
- Stanton landed (589M/855f) -> moved to Orange County/Stanton, stale Stanton.lnk retired.
- The Stanton sync had RE-LITTERED the top level (re-dropped Anaheim/Long Beach dup folders + Alamitos Bay + 4 source zips + all city .lnk stubs, even though county folders already held them). Verified duplicates by file-count (Anaheim 2327=2327; LongBeach county superset). Swept 9.9G to _trash/sync-relitter-20260717/ (recoverable, awaiting purge OK). Warehouse 61G->52G; top level now = 5 clean county folders only.
- ✅ Skipper then reorganized the SOURCE PC files to match the county structure -> future SyncMuni runs should drop cities into place with LESS/no top-level litter. Still: SPOT-CHECK the next sync to confirm it lands clean before assuming. Post-sync auto-tidy routine offered but NOT built (hold unless strays persist).
- Left as-is: LA County 'CustomerInfo (192.168.1.6) - Shortcut.lnk' (source artifact, not a city).

## Deploy-package walkthrough (pre-Jordan/Travis handoff) — catch #1 (16:3x UTC)
- Skipper running a walkthrough of the 6 V1.5 dashboards before dev handoff ("I keep catching things that could be better"). Punch-list: arbor-stack/release-candidates/WALKTHROUGH-PUNCHLIST-20260717.md.
- #1 Revenue Performance CATCH: CSV export did nothing + HTML5 tooltip under Monthly Goal box. Root cause: Download CSV = type=submit on filtersForm; monthlyGoal input step="1000" but value has cents -> browser blocks submit (export + Apply both). Fix: step="any" + formnovalidate on export btn. Shipped #182, both webroots, backup-first, served-verify 0 CF errors. Awaiting Skipper in-browser confirm.
- LESSON candidate: a type=submit CSV/export button inside a filter form is hostage to EVERY field's HTML5 validation (step/min/max/pattern). Use <a href> exports OR formnovalidate on the export button, and never step-constrain a money field (use step="any").

## Walkthrough catch #2 — RevPerf CSV dates ##### in Excel (16:5x UTC)
- Not a data bug: CSV cells were clean uniform ISO (verified by pulling the actual served CSV). Excel auto-converts ISO->date + default ~8ch column width -> single-digit-day dates fit, double-digit (>=7/10) show ##### -> flips exactly after Jul 9.
- Skipper chose "display properly" (option B). Fix: Period Start/End emitted as Excel text-formula ="mm/dd/yyyy" (bypass csvField, unquoted so Excel evaluates the formula -> text) -> never re-typed as date, no #####. Ship #183, both webroots, backup-first. Awaiting Skipper re-download confirm.
- LESSON candidate: Excel ##### on a CSV date column = Excel auto-date + narrow default width, NOT bad data. To force full display, emit ="mm/dd/yyyy" UNQUOTED (quoted "=..." breaks the formula-eval trick). Text sorts by string not chrono — ISO ="yyyy-mm-dd" if chrono sort matters.

## Walkthrough catch #3 — Cockpit "running dry" false-flags recurring accounts (17:4x UTC)
- Skipper: Newport Coast Shopping Center (Coastal) shows running dry but it's a multi-year Irvine Retail account. Investigated via a throwaway auth-gated diagnostic CFM on play (ZZDiag-RunDry.cfm, deleted after).
- ROOT CAUSE: Proj 1103500 under Co 301642 "Irvine Company - Retail" = ~40 active shopping centers, 34 future WOs, completed work last month. Recurring maint books ~1mo ahead (furthest Nov 12 2026) -> the cockpit's "forward book <3mo + no fresh bid" rule false-flags EVERY recurring monthly account. Contracts on file: only 2, both expired 2021-08-30 -> contract lookup would NOT fix it (the data isn't there).
- FIX (Skipper-approved "for now"): exclude actively-serviced recurring accounts -> added AND (dc.d IS NULL OR dc.d < DATEADD(day,-60,GETDATE())) to RunningDry. Deployed play D:\ (Cockpit is D:-only), backup-first .pre-rundryfix, 0 CF errors, Newport Coast now runningDry:0; 52 still flagged (628 clear) so not zeroed.
- FOLLOW-UPS: (1) re-sync Aspen (canonical running-dry mirror). (2) BIGGER data fix (Skipper): record recurring commercial contracts in TRIM IT for real look-ahead -> wiki/facts/trimit-recurring-contract-lookahead-gap.md.

## Catch #3 FINAL — segment-scoped running-dry (Skipper chose B) (18:1x UTC)
- Iterated 3x live: (1) per-property dc<=60d cleared Newport Coast + monthly-cadence sites but 8 Irvine centers on slower cadence stayed flagged; (2) account-level (company active) cleared all Irvine BUT over-suppressed (52->9): it hid individual HOAs under active mgmt cos (Powerstone/FirstService aggregate many HOAs under one CompanyID); (3) Skipper chose B = scope account-level clearing to COMMERCIAL only.
- Canonical segment = Projects.ProjectTypeID (1/2=Municipal, 3=HOA, 4=Commercial); verified via throwaway diag: Irvine retail=4, the HOAs=3. Confirmed.
- FINAL SQL: RunningDry adds `AND (p.ProjectTypeID <> 4 OR ca.active = 0)` + keeps per-property `dc<60d`. Commercial (type4) clears at account grain via ca.active; HOA/Muni stay per-property. Live play D:, 0 CF errors. All Irvine centers :0; dry HOAs correctly :1; board=32 real. Ship #184 (updated in log). Aspen re-sync still pending. Backups .pre-rundryfix/-2/-3.

## Walkthrough catch #5 (SPM Production) — Regular/OT breakout on Paid Field Hours (21:1x UTC)
- Skipper: break out OT vs regular in the Paid Field Hours card. Data source for the split = dbo.CrewMemberCalendars (per crew member per day); the card total stays dbo.Calendars.TotalHours (Day-Sheet-validated).
- OT rule = the authoritative GSTS/COO overtime def (from anomaly-monitor/monitor.js metric1b): per person per day, OT = max(0, hours-8), AND the entire 6th/7th worked day of the Mon-Sun week is all-OT. Replicated in SQL via ROW_NUMBER() OVER (PARTITION BY CrewMemberID, wkMon ORDER BY d).
- KEY DATA FINDING (diagnostic): CrewMemberCalendars reconciles to Calendars.TotalHours for CLOSED periods (June 0.08%) but LAGS for the open period (last 7d: only 73% of hours posted per-member) because crew timecards post after the day rollup. So Skipper chose A: show the split with an "as entered so far -- N% posted" caveat when memberHrs < 98% of the card total; ties out at close. Ship #187.
- Reusable: dbo.CrewMemberCalendars.TotalHours = per-member per-day paid field hours; sums to dbo.Calendars.TotalHours once timecards are fully posted.

## MuniBot Smart-Bidding Tool — installed + smoke-tested (21:5x UTC)
- Herman (Boss Hermes) emailed 3 scripts (bid_engine.py, competitor_extractor.py, bid_output.py) for MuniBot's Municipal Smart-Bidding Tool. Skipper: help install for MuniBot.
- MuniBot = docker container `munibot` (image hermes-agent) on THIS host; host ~/.munibot <-> container /opt/data. NOT a locally-registered agent (sessions_send agentId=munibot fails); Gilligan->Munibot line not live. Practical dispatch = `docker exec munibot`.
- Did: staged 3 scripts -> ~/.munibot/home (=/opt/data/home), created muni-scratch/bid_output. Then inside container via docker exec: `uv venv /opt/data/.venv` + `uv pip install pymupdf openpyxl`; ran all 4 smoke tests -> ALL PASS (bid_engine pulled 129 TRIM IT items/48 avg/180 nearby/48 competitor files/6 PriceBuddy bands; competitor_extractor pulled full WCA-vs-GSTS line items). chowned venv+outputs to hermes:hermes.
- GOTCHAS: (1) container HOME=/root, NOT /opt/data/home -> run scripts by full path, not ~/. (2) venv MUST be built inside the container (uv venv) so /opt/data/.venv/bin/python -> container's /usr/bin/python3.13; a host-built venv would point at the wrong python. (3) TRIMIT_PIPE=/opt/data/home/trimit-query.sh already correct.
- Reply to Herman drafted for Skipper approval (he said yes). Scripts archived at inbox-pull/munibot-pricing-tool-2026-07-17/.

## Walkthrough #6 — Executive Review -> "Sales Performance" + Crew Performance split (22:2x UTC)
- Skipper: rename Executive Review (it's a sales dashboard) + pull the Crew Performance tab into its own page for production. Chose name "Sales Performance", crew option B (standalone page).
- Frame Executive$Financial$Overview$Frame.cfm: title/H1 -> Sales Performance, dropped crew tab (now 4 tabs). FILE NAME KEPT so AppForms 1104 + RC-AppForms rename SQL untouched. New Dashboard-CrewPerformance.cfm = single-frame clone (same date JS + welcome) iframing Executive$Sales$ByCrewName.cfm, tab-strip hidden. Menu Profile$Main.HiRes: Executive Review->Sales Performance + added Crew Performance after Production Performance (Jason-9 pilot gate). Ship #189, all live, 0 CF errors. Legacy Exec$Fleet$Periods "Executive Review" menu item is unrelated - left alone. Deploy doc updated (Dashboard-CrewPerformance.cfm ADD TO SET).

## MuniBot bid tool v2 patch — installed + smoke-tested (22:4x UTC)
- Herman email #63 "MuniBot Update — Patched Bid Scripts (v2)": fixes 4 issues from MuniBot's first run — (1) no blank lines (Crown Raise=35% Full Prune, Stump=35% Removal auto-populated), (2) $130 TPH labor floor (day $3,120 / emergency $390 day / $488 night), (3) variable discount (10% under WCA on high-vol 13-30" bands, 8% std, 5% low-vol), (4) Date vs Fan palm pricing. Replaces bid_engine.py + bid_output.py; competitor_extractor unchanged.
- Did: saved attachments -> inbox-pull/munibot-pricing-tool-v2-2026-07-17/; backed up in-container v1 (bid_engine.py.v1-bak / bid_output.py.v1-bak); docker cp'd v2 in (engine 12375->14504, output 10727->23647); chowned hermes:hermes.
- Smoke test (docker exec, bid_output.py --signals Long_Beach_signals.json --output test_v2.xlsx): PASS -> "46 line items (no blanks)", "VERIFIED: All 46 line items priced. No blanks.", "VERIFIED: All labor rates at or above $130 TPH floor." Exit 0.
- Reply to Herman drafted for Skipper approval.
