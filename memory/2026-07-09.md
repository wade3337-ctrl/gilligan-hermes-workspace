# 2026-07-09

## Boss Herman Crew Meter → durable URL via Tailscale Serve (Jul 10, DONE)
- Herman's Crew Meter dashboard (token usage / $100-mo budget monitor) ran on container `127.0.0.1:8300` with only a temp trycloudflare quick tunnel. Skipper wanted durable.
- **Cloudflare named-tunnel NOT viable:** no CF account/creds, and neither domain is on Cloudflare (greatscotttreecare.com→MediaTemple, greatscotttreeservice.com→ezhostingserver). Custom-hostname CF tunnel needs the zone on Cloudflare.
- **Chose Tailscale Serve** (Skipper picked option 1). Key: `hermes` container uses **host networking** → container `127.0.0.1:8300` == host `127.0.0.1:8300`, so host `tailscale serve` reaches it directly (no port publish). Result: **`https://gilligan.tail5807bd.ts.net/`**, tailnet-only (his phone+laptop already on tailnet), HTTPS auto. Verified via the ts.net URL: `/`→200, `/healthz`→ok, title "Crew Meter". Auto-starts (tailscaled enabled + serve config persists).
- **Root step:** no passwordless sudo (in sudo group but needs interactive pw). Skipper OK'd doing it via docker-group (root-equiv): ran `tailscale set --operator=wade3337` in a root busybox mounting `/var/run/tailscale`+`/usr/bin/tailscale` → then serve as user. Future tailscale mgmt now works without root.
- **Temp trycloudflare tunnel** (pid in hermes container) still running, NO watchdog (won't respawn). Redundant + PUBLIC — offered to retire it. Tailscaled socket was world-writable (rw-rw-rw-) — noted.

## Boss Herman → direct crew API access (Jul 10, done; keys pushed to his container)
- Herman runs in a **Docker container** `hermes` (image hermes-agent). Bind mount: host `~/.hermes` → container `/opt/data`. hermes user uid 1000 (= host wade3337 uid 1000), home `/opt/data/home`. So container `/opt/data/home/.secrets/` = host **`~/.hermes/home/.secrets/`**. Currently Herman reaches crew by SSH-hop back to host (herman-gateway forced cmd) — cutting that out.
- **Vault-keeper check first:** the prompt's `/opt/data/home/.secrets/` + "hermes user" don't exist on the HOST — traced to the container (didn't blind-copy keys). Reconciled via `docker inspect` mount.
- **Pushed (copies, 600, land as hermes:hermes in-container):** `crew-api-keys.env` (GEMINI_API_KEY, ANTHROPIC_API_KEY [=Fable/crew key from fable.json], CREW_ANTHROPIC_API_KEY alias, KIMI_API_KEY, BRAVE_API_KEY/_SEARCH_) + raw json copies (gemini/anthropic/kimi + brave-search.txt). Source keys in host `~/.secrets/`.
- **Footgun flagged:** crew Anthropic key vs Herman's GLM brain key both = ANTHROPIC_API_KEY → added CREW_ANTHROPIC_API_KEY alias + warning so he doesn't clobber his z.ai brain.
- **Earlier (same session) I staged crew keys+wrappers+skill at host `~/.hermes/.secrets/` + `~/.hermes/crew/` (= container `/opt/data/.secrets`, `/opt/data/crew`)** — that's NOT Herman's home secrets dir; superseded by `/opt/data/home/.secrets/`. Offered Skipper cleanup of the redundant copies (key hygiene). Herman to build his own local crew scripts + verify each connection + cut over. Gemini was returning 1.5 Pro (2 gens back) — model choice now his to set (→2.5 Pro).

## RC-03 City Budgets — Brent reconciliation → dashboard VALIDATED + Uninvoiced tie-line shipped (ship-log #109)
- Brent Beller emailed 12 city budget reports (QuickBooks source); Skipper forwarded to gilligan.gsts inbox. Pulled via `brent-citybudgets-check.js` → `arbor-stack/city-budgets-review/incoming/`.
- **VERDICT: Dashboard-CityBudgets.cfm is working correctly — it reports TRIM IT faithfully.** Invoiced = TRIM IT invoice-masters to the penny (verified Irvine/Newport/SanClemente); budgets 9/12 match; Stanton/Cypress/Fountain Valley tie to Brent exactly.
- **Key reframe (Skipper agreed):** dashboard reads TRIM IT; Brent = QuickBooks. All diffs = (A) **TRIM IT↔QB data divergence** (Irvine TRIM$3.09M/QB$2.84M; Newport TRIM$2.02M/QB$2.18M; SanClemente TRIM$457k/QB$422k) → **defer to future QB API** (Skipper: "keep using TRIM IT for reporting, QB API later"); (B) Brent aggregating across contract/FY boundaries (Industry old+new contract; Long Beach = one combined vs Brent's Marine+Parks split — invoiced $730,432.92 matches combined exactly); (C) contract-budget entry (Aliso -$1.5k, LB "TBD" Parks PO, Westminster old 24/25). **No dashboard logic bugs.**
- **SHIPPED:** added **"Uninvoiced" (Budget − Invoiced)** row to card + detail views of `Dashboard-CityBudgets.cfm` (inline calc, no data-layer change) → ties to Brent's "Remaining" exactly where data agrees. Backup-first (`Jasonsrepairs\...bak-20260709-152457`), deployed to play (D:\ webroot, no C:\ shadow), render-verified (35 Uninvoiced rows, 0 CF errors, values correct). Local committed + `city-budgets-review/RECONCILIATION-2026-07-09.md`.
- Disabled the Brent-watcher cron (7b063122) — done. **RC-03 effectively signed off pending Skipper's final OK; QB reconciliation is the open follow-up (future QB API).**

## ▶️ Carryover from 2026-07-08 (still open): more work on the QUOTE page (One-Stop UI, `QuoteStep`) — Skipper has specific changes; ask first.

## Steve's Sales Win/Loss-by-Year report — 2024 mis-attribution root-caused (Jul 9, IN PROGRESS)
- Steve Guzowski (CFO) emailed Jason 7/8 (fwd to gilligan.gsts uid 96): "Sales Win/Loss Report by Year" (att `Sales Win_Loss Report 07-01-26.xlsx`), flagged **2024 looks off** (2024 was a better year than 2025 but report shows worse). Skipper: **Ethan & Rebekah didn't work here in 2024** — real 2024 reps were Chris Mello, Garrett Cornish, Scott Griffiths, maybe Parker Keil / Stephen Darden.
- **DIAGNOSIS (DB-verified via gsql):** raw `Proposals` 2024 shows the REAL reps (Chris Mello 1130 = 7000 props, Garrett 6878, Scott 5675, Nathan Nevois, Jeremy Indes, Toby Henry). Report shows only Garrett/Scott/Toby/Rebekah/Ethan — Chris Mello DROPPED. Two stacked bugs, historical-years only:
  - **Bug 1 (roster filter):** report rep list = `SalesReps WHERE IsMeasured=1 AND StatusDefID=188` (current active roster) → departed reps (Chris Mello etc.) vanish from past years. **Locus: `Executive$ClosePercentage$ByRep.cfm` lines 144-146.** Same pattern in `$ByMarket`/`$Detail` (RC-01 family).
  - **Bug 2 (attribution drift):** counts join `Proposals p WHERE p.SalesRepID = sr.SalesRepID` — but `Proposals.SalesRepID` gets OVERWRITTEN to the current owner on account reassignment (Rebekah's 106 / Ethan's 5 "2024" props were created 2024 but reassigned to them). Audit table `zDelete-InsiderepChanges` (ProjectID, Old/NewInsideRepID) confirms reassignment happens. Original writer may be recoverable via `ProjectReps` (Role) or that audit log — TBD.
- Report also dedups heavily (raw Garrett 6878 → report 1092) — engine counts via GoAhead-EXISTS win logic, PG=11 city-excluded (Skipper 6/30). Fix must preserve that.
- **CONFIRMED page = Steve's Diligence dash `FinancialReport\FinancialReportDashboard.cfm` (+ `FinancialReportExport.cfm`)** ("the page we built for him"). Both bugs in the SalesPerf query (Dashboard lines 286-298): **Bug 1** = `WHERE sr.IsMeasured=1 AND sr.StatusDefID=188` (roster gate); **Bug 2** = attribution via overwritten `Proposals.SalesRepID`. Treatments queries (lines 341) also key on prop.SalesRepID.
- **Fix 1 quantified:** roster gate DROPS **9,390 of 22,914 2024 proposals (41%)** incl. Chris Mello (IsMeasured=0, StatusDefID=188 — the real 2024 leader). Removing the gate = the dominant correction; it's what actually resolves Steve's "2024 looks worse than 2025."
- **Fix 2 VERDICT — REVISED (gpt-5.6-sol sanity check REFUTED my "unrecoverable"):** original attribution IS recoverable via reconstruction. My dead-end sources (ProjectReps, InsiderepChanges, InsideRepID=support staff, UserID, ProposalLogs) still dead — but the model found: (a) **dated frozen snapshot tables `zDelete-SalesReportNewest008`** (created 2025-01-21, 12,372 rows) covers **all 106/106** disputed Rebekah-2024 props → Chris Mello 100, Scott 3, Garrett/Jeremy/Chase 1 each (I independently VERIFIED this query); earlier snapshots 2023-10 / 2024-01 / 2024-07 exist too; (b) proposal-grain RFP/commission/invoice tables (`RFPs`, `SaleRepCommissionInvoices/CrewSheets`, `Invoices`) recover 24/106 directly, all naming Chris Mello/Nathan/Jeremy/Scott. **Recon precedence:** proposal-grain (RFP/commission/invoice) FIRST → dated project snapshot as explicitly-labeled `HistoricalProjectRep` FALLBACK → surface conflicts (2 found: snapshot vs RFP), don't hide. Snapshot is project-grain not proposal-writer-grain → "materially recoverable, not provably perfect."
- **NUMBER CORRECTION (model caught):** my 9,390/22,914 (41%) was a CREATED-year population; on the report's REAL window (status filter + `ISNULL(ProposalSentDate,ProposalDate)`) it's **1,502 excluded / 4,962 = ~30%**. Bug-1 direction unchanged, magnitude ~30% not 41%.
- **Model test = success** (Skipper wanted to trial gpt-5.6-sol): genuinely adversarial, found real sources I missed, flipped the verdict. NEXT: build Fix 1 (drop roster gate) + Fix 2 (reconstruction w/ precedence) on play, backup-first, reconcile 3 yrs, show Skipper before Steve.
- **Skipper GO: fix 1 AND 2, involve crew.** NEXT: confirm exact by-year page Steve runs (ByRep is single-period T12; his had 2024/25/26 blocks + Treatments cols), then rewrite rep-set (drive from proposals-in-window, not current roster) + attribution (original writer). Backup-first, render-verify, reconcile 2024, THEN show Steve. Work dir `arbor-stack/winloss-fix/`.

## Steve Win/Loss — Fix 1 SHIPPED (play), Fix 2 BLOCKED by crew (Jul 10)
- **Fix 1 (roster gate) DEPLOYED + render-verified** on play (FinancialReportDashboard.cfm + Export; backup `.bak-20260710-010423`; junk-exclusion replaces IsMeasured=1&StatusDefID=188). 2024 now restores Chris Mello $3.43M, Jeremy Indes, Nathan Nevois; served page HTTP200 0-errors. Ship #111 (ongoing).
- **Fix 2 attempt = 3 inheritor reps** (Rebekah 243 pre-start props back to 2010, Omar Sanchez 136, Ethan 6) via corroborated-start boundary. **gpt-5.6-sol pressure-test BLOCKED it (verdict `winloss-fix/FIX2-CREW-VERDICT.md`):**
  - Derived "corroborated start" ≠ hire date. Gap detector also fires on established reps (Scott Andre, Parker Keil, Chris Mello) and MISSES small-book recent hires (Ba Tran, Chase, Jeremy) — "exactly 3" only from unstated roster exclusions.
  - **Rebekah's 2024-02 start is CONTAMINATED — proves Skipper right:** proposal 709255 — RFP 2024-01 = Chris Mello, Chris commission/invoices thru 2025; only a **2026-02-03** Rebekah commission row makes "same-rep" true. Commission DRIFTS. Her real first native RFPs = Jan 2026. She did NOT work here in 2024.
  - Blind precedence RFP>commission>snapshot can pick the WRONG rep on multi-rep proposals (709255 has both Chris & Rebekah commission rows).
  - **Safe design:** need a REVIEWED `RepEffectiveDate` (HR/payroll hire date — SalesReps has NO hire column) per inheritor; override only proposals before effective date w/ unambiguous single-rep proposal-grain evidence; multi-rep = explicit conflict/manual; snapshot only to CONFIRM reassignment (label HistoricalProjectOwner); store full provenance; exception report not silent nightly.
- **BLOCKER = need real start/hire dates for Rebekah, Ethan, Omar Sanchez from Skipper (HR knowledge, not in DB).** Asked. Fix 1 already did the heavy lifting.
- **RESOLVED + Fix 2 COMPLETE (Jul 10):** Skipper gave hire dates — **Rebekah Jan 2026, Ethan Aug 2025, Omar Dec 2022 (branch mgr, non-seller).** Built reviewed `Workbench.dbo.RepEffectiveDate` + `Workbench.dbo.ProposalOriginalRep` (564 overrides: 554 resolved / 10 Former / 9 conflict-flagged; sources RFP 198, commission 8, snapshot 348). Wired `COALESCE(ov.OrigRepName, current)` into ALL 4 win/loss surfaces (summary/detail/treatments/salesperf-export) — detail query ALSO had the roster gate (render-verify caught it). **Verified:** 2024 Rebekah 0/Ethan 0/Chris Mello 1107; 2026 Rebekah 345/Ethan 337 (boundary correct); 0 CF errors. Ship #112. Export gotcha: win/loss export = `FinancialReportExport.cfm?mode=salesperf` (NOT view=2, which is the separate Invoice Details report — same drift, out of scope, offered). **Pending Skipper review before Steve.** Workbench tables survive nightly refresh; NOT auto-regenerated (reviewed/static per crew).

## Steve Win/Loss — Fix-2 BUILD: snapshot fallback FAILS at scale (Jul 10, validated pre-build)
- Backed up FinancialReportDashboard.cfm + Export → `Jasonsrepairs\...bak-20260710-010423`.
- Built Fix-2 mapping as a READ-ONLY SELECT first (didn't materialize). Result: **116,185 overrides (112,338 from snapshot)** — NOT minimal-touch. Diagnosed: snapshot `zDelete-SalesReportNewest008.SalesRep` = **project ACCOUNT-OWNER, not proposal seller** → it would move **35,518 of Scott Griffiths' (current active rep) own proposals**. Blanket snapshot fallback would CORRUPT attribution. **Sanity-check crew's snapshot lead was right for the 106 disputed but does NOT generalize.**
- Proposal-grain sources also impure: RFP 3,724 overrides (1,456 on current-active reps — RFP rep = intake/role rep, ≠ seller); invoice 208; commission 57. No single clean immutable "original proposal seller" field exists.
- **REVISED PLAN:** Fix 1 (roster gate) is clean/safe/dominant — ship it. Fix 2 must NARROW to provably-impossible attributions only (rep credited in a year before they started, e.g. Rebekah/Ethan 2024) + FLAG the ambiguous rest; NO blanket rebuild. Dated snapshots (2023-10/2024-01/2024-07/2025-01) could give per-year rep tenure to define "impossible." Crew to pressure-test targeting. Go-forward cure = snapshot writer-at-creation. Awaiting Skipper steer.

## Play refresh staleness — root-caused + monitor set up (Jul 9)
- Reconciled Nate's 7/8 SPM meeting reports vs our Sales Production Meeting dashboard. Ties within ~1.9% YTD; rep-by-rep differs BY DESIGN (managing-rep vs performing-crew; week-vs-month buckets). No dashboard calc error. Record: `arbor-stack/spm-verify/RECON-2026-07-09-nate-vs-dashboard.md`.
- ROOT CAUSE of the "dashboard looks behind": play GSTS data frozen at 7/3/created (7/7 vintage). Play's `\GSTS DB RESTORE` task (D:\DownloadAndRestoreLatestGSTS.bat) runs fine nightly — it WinSCP-downloads newest GSTS_backup_*.bak from rsync.net (/data1/home/fm2760/database) then restores. Source has NO file after 7/7 03:00 → prod->rsync.net backup upload stopped (likely quota; ~121GB/day, no pruning). Prod-side fix = Jordan. Skipper is telling Jordan.
- Built monitor `arbor-stack/anomaly-monitor/play-refresh-monitor.sh` (reads last-restored backup_finish_date via gsql msdb; STALE if >40h). Cron "play DB refresh staleness monitor" id 1bb319ab-2c59-4963-9e0f-9b8cf7615bee, daily 8 AM PT, sessionTarget main, alerts Skipper only when STALE.
