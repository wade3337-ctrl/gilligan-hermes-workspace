# 2026-07-15

## 🤖 Muni Bot stand-up — Phase 1 (Brent's municipal-work agent) — build session
Skipper greenlit come-up path: **(4) general shell FIRST → (1) budget/PO tracking**. Surface = Telegram channel (Skipper+Brent+Muni Bot) + own Gmail; later into Brent's V1.5. Reusing the [[herman-agent]] stack. Project note: `wiki/projects/brent-agent.md`.

### ✅ #1 RUNTIME — DONE
- New container **`munibot`** (image `hermes-agent`, reused — no rebuild), `net=host`, mount `~/.munibot:/opt/data`, **gateway-only (NO dashboard)** → binds zero inbound ports → **no collision with Herman** (verified: Herman Up 11 days, 9119 still his alone). Compose: `~/munibot/docker-compose.yml`.
- **Brain-only clone:** copied config.yaml + auth.json + .codex + shell profiles. Wrote clean `.env` (brain + brave only, channels OMITTED) + a NEW Track-1 `SOUL.md` (Muni Bot = Brent's assistant). **Did NOT copy** Herman's .ssh / .secrets / vault / TRIM IT login / Telegram — verified absent. Mount = 94M (tiny).
- Brain smoke test PASS: `hermes -z` → "MUNIBOT-BRAIN-OK gpt-5.6-sol".
- **Resources:** box has ~10G RAM avail, 799G disk free, 8 cores idle. Munibot ~1.5G like Herman. Plenty.

### 🔎 FINDING (surface to Skipper): Herman is currently on his FALLBACK brain
Both Herman AND Muni Bot answer as **gpt-5.6-sol** (openai-codex fallback), NOT the GLM/z.ai primary. Herman's `auth.json` anthropic pool base_url = `api.anthropic.com` (no z.ai base-URL set, ANTHROPIC_BASE_URL empty) → z.ai primary fails → Codex answers. Pre-existing Herman condition; Muni Bot inherited it faithfully. NOT fixed (don't touch Herman's brain without Skipper). Worth deciding: repair z.ai primary for both, or stay on Codex.

### ⏳ REMAINING Phase 1: #2 scoped read-only DB access (own key, additive to authorized_keys, NO get-arbor), #3 own TRIM IT play login (mirror herman-trimit-login), #4 seed municipal-knowledge vault. Then #5 Gmail + #6 Telegram (with Skipper).

### ✅ #2 DB ACCESS / #3 TRIM IT LOGIN / #4 VAULT — all DONE + proven (2026-07-15)
- **#2 scoped read-only DB:** Muni Bot's OWN key generated IN its container (`/opt/data/.ssh/gilligan_access`), registered on gilligan (authorized_keys APPENDED, backed up first) with forced-command `~/munibot-gateway/munibot-dispatch.sh`. Dispatch allows: RO query (default) + get-vault + search. **HARD-DENIES `get-arbor` (BLACK/Track-2) + `crew`** — verified denied. RO query reuses HermanRO (read-only). Container tool `~/home/trimit-query.sh`. PROVEN: pulled real City-of customers; write (CREATE TABLE) → permission denied. NO port/collision impact.
- **#3 TRIM IT play login:** `munibot@greatscotttreeservice.com` **UserID 90377**, SecurityLevel 1, **cloned from BRENT (UserID 40)** = least-privilege (46 UserActions, vs Herman's 97). Authenticates OK. Provisioner `~/arbor-stack/tools/reprovision-munibot-trimit-login.sh` (+`--check`); creds `~/.secrets/trimit-munibot-login.env` (host) + `/opt/data/home/.secrets/trimit-web-login.env` (container). ⚠️ play-only → nightly restore wipes it; needs a re-provision cron before the web login is actually used (not used yet).
- **#4 municipal-knowledge vault:** git repo `~/municipal-knowledge` (Track-1 ONLY), 8 seed notes (Brent profile, PO-gated, 178K artifact, city-forecasting, budget methodology, muni query playbook, README, brent-history/_INBOX). Refresh `~/munibot-gateway/refresh-munibot-vaults.sh` (docker cp → container). `OBSIDIAN_VAULT_PATH=/opt/data/home/municipal-knowledge`.
- **🏁 CAPSTONE (agent-does-its-own-work):** Muni Bot one-shot autonomously ran its DB tool (3 real City-of customers) + read its vault (stated PO-gated fact). PASS.

### 🔎 2 findings to surface + open items
- **Herman is on his FALLBACK brain** (gpt-5.6-sol/Codex), z.ai primary unwired (see above). Muni Bot inherited same. Decide: repair z.ai for both or standardize on Codex.
- **Herman's TRIM IT web login is currently WIPED** (nightly play-restore-from-prod removed hermes@; freed UserID 90377 which Muni Bot took). Herman's login needs re-provisioning — separate maintenance.
- **OPEN INPUT (Skipper):** Brent's **massive municipal-history file base** → location + format, to ingest into `~/municipal-knowledge/brent-history/`.
- **NEXT (#5/#6, need Skipper):** Muni Bot Gmail + Telegram channel (Skipper+Brent+Muni Bot).
- **Hardening TODО (when web login used):** cron the login re-provision + vault refresh (mirror Herman's crons).

### 📁 Brent's municipal file-base → sync design (2026-07-15)
- File base lives on the **GSTS "200 server"**, reachable only via **OpenVPN Connect → GSTS_VPN** (gateway 104.56.77.161), login+password. Our hosted DB-box beachhead (100.86.97.46, LAN iface /32, public 173.208.162.142) is a colo box — CANNOT see the office 200 server. `.ovpn` profile not exportable easily (IT set it up; Skipper won't ask them; file search found no .ovpn).
- **Skipper's call:** sync **once a day, run WHILE he's on the office network/VPN** (Plan B). Security win: copy runs on HIS PC using HIS existing session → **no share/VPN creds stored on gilligan**.
- **Architecture:** Brent's folder → (daily sync helper on Skipper's PC, runs when connected) → **private repo `wade3337-ctrl/municipal-history`** (CREATED + cloned to `~/municipal-history`) → Muni Bot pulls. Distinct from the curated `~/municipal-knowledge` vault.
- **NEXT INPUTS from Skipper:** (a) exact UNC folder path to Brent's muni files; (b) can he install software on the work PC? → determines helper delivery (Tailscale+scp vs git/deploy-key bundle vs built-ins only).

### ✅ Two problems solved from Skipper's forwarded emails (2026-07-15)
**Read via Gilligan's own inbox** (gilligan.gsts@gmail.com IMAP — ImapFlow + creds `arbor-stack/anomaly-monitor/.secrets/gmail.json`; no himalaya on gilligan).
- **① File location (Joseph Young email):** Brent's muni file base = **`\\gsts-server200\GSTS\Municipal Bid Data\Jason_Compiled`** (Joseph still compiling — 3 counties done, 2 large ones processing; "astounding amount of data"). → Built **SyncMuni** Windows package (`~/munibot-gateway/SyncMuni/` + `SyncMuni.zip`): robocopy share→local (his login) → `tar -cz | ssh` drop-key → gilligan receiver → repo + Muni Bot. Setup = install Tailscale + run Setup-SyncMuni.cmd (daily 6PM schtask). Delivered to Skipper via Discord.
- **② GLM-5.2 brain fix (Boss Herman email + attached `gilligan-munibot-glm52-brain-setup.md`):** root cause = `provider: anthropic` normalizes `glm-5.2`→`glm-5-2` (dot→hyphen) → z.ai 400 → silent fallback; also missing `base_url`. **FIX applied to `~/.munibot/config.yaml`:** `model.provider: zai-anthropic` + `base_url: https://api.z.ai/api/anthropic` + top-level `providers.zai-anthropic` (key_env ANTHROPIC_API_KEY, api_mode anthropic_messages). **PROVEN** (Herman's procedure): Test1 z.ai direct → glm-5.2/GLM52_OK; Test2 fallback-disabled → replied ZAI_PERIOD_OK + self-id glm-5.2 (nothing to mask). Restarted on fixed config. Muni Bot now genuinely on GLM-5.2. Backup `config.yaml.bak-glm52-*`.
  - ⚠️ **Herman himself has the SAME two bugs** (his email says he fixed his own tonight; verify Herman's config uses zai-anthropic custom provider too — the doc was authored FROM his fix). Muni Bot now matches the correct pattern.
  - Large-data note: first sync of Jason_Compiled may be big; watch git repo bloat — may need git-lfs or index-only if multi-GB.

### 🏁 Muni Bot LIVE on Telegram — Phase 1 COMPLETE (2026-07-15)
- Telegram: bot **@GSTS_MuniBot_bot** (id 8981732401), group **"Muni Bot – Municipal Work"** chat_id **-5529154032**, allowlisted to Skipper (id 8975923324; GATEWAY_ALLOW_ALL_USERS=false). Add Brent's TG id later. Made bot ADMIN so it reads group msgs. (Gotcha: manual getUpdates conflicts with the running poller — 409 = it IS connected; stop poking it.)
- **LIVE capstone PASSED in-channel:** on the Skipper's question, Muni Bot autonomously ran trimit-query.sh (233 "City of" rows → deduped analysis: dept sub-rows, San Diego ×8, "Alsio Viejo" typo), recited PO-gated rule + $2.14M gap, and is now working function #1 (which cities have PO-pending work) after Skipper said "yes." Running on GLM-5.2.
- **Polish applied:** appended a "My tools" section to Muni Bot's SOUL.md (trimit-query.sh, municipal-knowledge, municipal-history) so it stops re-discovering its own tools (it fumbled that once at start).
- **Remaining:** #5 its own Gmail (optional/next). File sync (SyncMuni) running on Skipper's PC — municipal-history filling in.

### ✅ #5 Muni Bot Gmail DONE (2026-07-15) — Phase 1 fully complete
- Mailbox **MuniBot.gsts@gmail.com** (Skipper created + 2FA + app password). himalaya v1.2.0 copied from Herman's mount into munibot; config `/opt/data/.config/himalaya/config.toml` (imap.gmail 993 / smtp.gmail 465, auth.cmd = cat /opt/data/.secrets/gmail-app.txt). App pass also backed up host-side `~/.secrets/munibot-gmail.env` (600).
- **PROVEN:** IMAP folder list OK; SMTP self-send → arrived + read back (MUNIBOT_EMAIL_OK). Send-gate = behavioral (drafts to Skipper for outbound-to-people), added to SOUL tool inventory.
- Muni Bot SOUL now lists all its tools: trimit-query.sh, municipal-knowledge, municipal-history, web search, email. (Takes effect next restart — didn't restart to avoid interrupting its live gap-analysis work.)
- **Future (agent-does-its-own-work):** inbox monitoring should become Muni Bot's OWN scheduled function, not a gilligan cron. Not built yet.

### ✅ Muni Bot brain → Obsidian (two-way sync wired, 2026-07-15)
- Pushed curated vault to private repo **`wade3337-ctrl/municipal-knowledge`** (8 notes) so Skipper can view/edit it in Obsidian (same pattern as trimit/arbor-knowledge).
- **Two-way autosync** `~/munibot-gateway/refresh-munibot-vaults.sh` upgraded: commit gilligan/Muni-Bot edits → `git pull --rebase` (Skipper's Obsidian edits) → push → `docker cp` into container. Cron **`27 * * * *`** (hourly), appended (crontab backed up `~/munibot-gateway/crontab.bak-*`). Proven once live.
- Loop: **Skipper's Obsidian ⇄ GitHub ⇄ Muni Bot container.** So the Skipper can now *teach* Muni Bot by editing the vault, not just watch it. (municipal-history file base is a separate repo; raw files.)

### ➕ Muni Bot got the TRIM IT knowledge base (2026-07-15)
- Skipper: "a lot of TRIM IT knowledge we have that should be there." → Gave Muni Bot the existing **`trimit-knowledge`** vault (101 notes: schema, query playbook, business logic, page docs, data flows) as a **read-only reference** in `/opt/data/home/trimit-knowledge`. Track-1, shareable — the same vault Herman + Skipper's Obsidian already use. **Guard verified: NO arbor-knowledge (Track-2) copied.**
- Refresh script now syncs BOTH: municipal-knowledge (two-way) + trimit-knowledge (read-only, pull+cp), cron `27 * * * *`. SOUL updated to list it.

### ➕➕ Folded ALL hard-won TRIM IT learnings into the shared vault (2026-07-15)
Skipper: "make sure he got all the SOPs and everything we've learned about TRIM IT as we've worked in it." → Audited workspace Track-1 TRIM IT knowledge vs the vault (sub-agent, gap-fill) and authored **7 new notes** into `~/trimit-knowledge` (101→108):
- `sops/repairing-trimit-safely.md` (the repair contract: backup-first/root-cause/propagate-siblings/render-verify/log/dev-handoff)
- `concepts/trimit-environment-and-gotchas.md` (PLAY-vs-PROD nightly refresh, Workbench side-DB, GSTSREADONLY, dual-webroot shadow, SQL/CF gotchas)
- `concepts/sales-rep-attribution-drift.md`
- `references/dashboard-metric-standards.md` (6 canonical metric rules), `references/trimit-ui-standards.md`, `references/lessons-learned-working-in-trimit.md`, `references/proven-techniques-trimit.md`
+ extended sops/concepts MOCs + HOME.
- **SAFETY GATE (I ran it independently):** scanned all changed files for Track-2/BLACK markers + secrets before pushing — clean (only pre-existing arbor pointers, no NEW leaks, no secrets). Shareable-vault guardrail honored.
- Committed + pushed to `wade3337-ctrl/trimit-knowledge` → synced into Muni Bot (108 notes) + reaches **Herman** on his 2h refresh + **Skipper's Obsidian**. One consolidation, three beneficiaries.

### 🚀 Dashboard deploy prep — DRIFT PASS clean (2026-07-15)
Re-ran full pre-handoff verification on the V1.5 6-dashboard DEPLOY-PACKAGE (`arbor-stack/release-candidates/DEPLOY-PACKAGE-V15-DASHBOARDS.md`). **No drift — handoff-ready.**
- Manifest (6 entry pages): missing-on-prod set matches doc exactly. Smoke-test vs play: all assets 200. Render: all 6 dashboards 200 + 0 CF errors. Runtime assets (protips.content.json, mtab-bar-chart-style.js) still prod-404 (migrate). Security gate 403s garbage cookie on pages + SPM$Drill pipeWon branch + aisummary.
- Stamped verification into the deploy package doc.
- **Open Skipper decisions (not drift):** (1) does RGC fold into this deploy or ship later; (2) confirm Boss Herman menu-handoff doc final. Then it's Skipper-OK → hand to Jordan/Travis.

### 🔎 Menu-handoff verify — found + fixed drift (2026-07-15)
Verified Herman's V1.5 menu-handoff doc vs LIVE play `Profile$Main.HiRes.cfm`. **Live menu correct** (Dashboard-SalesCockpit.cfm + Executive$Financial$Overview$Frame.cfm + new "Manage Access…"→Dashboard-Access.cfm, ZUserID EQ 9 gate). **Herman's WRITTEN doc was STALE** on 2 filenames (ZTest-SalesPipeline / Frame$Beta) + missing Access link → **corrected the doc to match live** (`~/.hermes/home/TRIM-IT-V15-PRODUCTION-DEPLOY-HANDOFF.md`, backup .bak-20260715). No deploy risk (menu ships as the file). "Fold in Boss Herman handoff" checklist item RESOLVED. Deploy package stamped.
**Deploy prep now fully clean.** Only open Skipper decision: RGC in/out (Gilligan lean: OUT — ship the 6, RGC as follow-up).

### ✅ RGC leak fix — crew-reviewed GO (2026-07-15)
GLM adversarial NO-GO (over-exclusion from canceled WOs / ghost crew sheets) → proved non-material in live data (only WO statuses 46 Active + 109 InProcess among 2026 approved go-aheads; no canceled/void; no undated-with-crewsheet) → HARDENED anyway (flag now excludes dead statuses 47 Inactive/315 Revised) → identical $520,080.29, controls PASS → Gemini acceptance GO. RGC "needs work" item CLOSED. Fix + hardening deployed to Workbench play; CHANGELOG + ship-log + LESSONS updated. RGC ready to rejoin a future deploy when Skipper says.

### 🔧 glm-judge wrong-DB fix (Codex-written) — 2026-07-15
Root cause: glm-judge's RUNSQL tool runs via gsql.sh (`-d GSTS` default); RGC objects live in Workbench.rgc → judge queried GSTS, found nothing, no verdict. FIX (Codex authored per new standing rule): `JUDGE_DB` env → prepends `USE [db];` + tells GLM its DB. Committed to arbor-core. VERIFIED: re-ran RGC judge `JUDGE_DB=Workbench` → GLM independently DB-verified $520,080.29/71 rows, 0 dead-status WOs → **VERDICT: PASS**. First real use of the Codex-writes-code rule; worked clean.

### 🧠 Muni Bot self-improvement loop — STANDING RULE (Skipper 2026-07-15)
Brent worked Muni Bot hard; Skipper wants it to ALWAYS write new learnings to its wiki (self-improvement). DONE: (1) SOUL standing rule — write new atomic notes for anything learned, in the moment, Track-1 only, NEW notes (not editing curated ones). (2) `refresh-munibot-vaults.sh` upgraded with a RETURN PATH: rsync --ignore-existing container→source rescues Muni Bot's new notes before the docker-cp overwrite, commits+pushes (BLACK-leak guard quarantines). Cron :27 hourly. (3) RESCUED Muni Bot's 8 first-session notes before they'd be clobbered (vault 8→16). SOUL rule activates on next munibot restart; return path live now. Same pattern as Herman's vault-inbox return path.

### 🧠 Self-learn loop → Boss Herman too (Skipper 2026-07-15)
Skipper: the self-learn loop should be standing for Herman too (he'd been telling Herman manually to update files). DONE: added the same SOUL standing rule to Herman (`~/.hermes/SOUL.md`, backup .bak-selfrule-20260715, copied to container) — write new learnings as atomic notes to his **vault-inbox** IN THE MOMENT, TWO-TRACK ROUTED (Track-1 → vault-inbox/trimit/<folder>, Track-2/BLACK → vault-inbox/arbor/<folder>, never mixing; never write the read-only ~/{trimit,arbor}-knowledge copies directly). His return path (collect-herman-notes.sh */20 + 2h refresh) already existed → now capture is automatic. Activates on Herman's next restart (Skipper controls). Logged to arbor-knowledge boss-herman-runtime per the Herman-upgrade rule.

### 📝 NEXT PROJECT scoped — Aspen (repurpose Arduino Herman) — PLAN AWAITING SIGN-OFF
Skipper wants Arduino Herman ("Herman" board, ssh arduino@100.121.177.31) torn down + rebuilt into **Aspen**: customer retention / marketing / BD agent. LOCKED: 3 pillars in sequence (retention→marketing→BD), retention first; metric = churn/at-risk headline + repeat-rate trend; name Aspen; Telegram; audience Skipper+Nate→team; wipe archive-first + preserve RC/FrSky hobby data; keep board+Hermes+GPT; shell-first come-up; crew-built + Codex writes code; same self-learning loop as Muni Bot. Full plan: `wiki/projects/aspen-retention-agent.md`. **DO NOT BUILD until Skipper green-lights.** Reuse Muni Bot infra patterns.

### 🌲 Aspen build STARTED (Skipper green-lit "build aspen" 2026-07-15) — Phase 1, BLOCKED on access
- ✅ Archived board's last-synced state (recovery snapshot + vault) → ~/aspen-archive/pre-wipe-20260715; hobby data (RC/FrSky/arbor-helper) preserved → ~/aspen-archive/PRESERVED-hobby.
- ✅ aspen-knowledge repo scaffolded + pushed (Obsidian). Generated ~/.ssh/gilligan_to_aspen key for board access.
- 🔴 BLOCKED: (1) gilligan has NO inbound SSH to Arduino board (arduino@100.121.177.31) — need pubkey `gilligan_to_aspen.pub` authorized on the board. (2) Need Aspen Telegram bot token (BotFather). Both need Skipper.
- Aspen runs ON the board (Skipper: keep Hermes+GPT on the Arduino). Reuse Muni Bot infra patterns. Plan: wiki/projects/aspen-retention-agent.md.

### 🌲 ASPEN REBORN — Phase 1 shell DONE (2026-07-15, very late)
Repurposed Arduino Herman → **Aspen** (retention/marketing/BD). Both blockers cleared (Skipper authorized gilligan_to_aspen SSH key on board + gave Telegram token). Teardown+rebuild complete on the board (arduino@100.121.177.31, service hermes-gateway, Restart=always → relaunch by ending the arduino-owned python process, no sudo): fresh brain (state.db wiped→recreated), Aspen SOUL, Discord→Telegram (@GSTS_Aspen_Bot, allowlist Skipper 8975923324), aspen-knowledge+trimit-knowledge (109) on board, read-only TRIM IT DB (aspen-dispatch, arbor DENIED, pulled real customers), self-learn return path (refresh-aspen-vault.sh cron 52). Kept Hermes+GPT-5.5. 3 archives + on-board _aspen-wiped. Details: wiki/projects/aspen-retention-agent.md. **REMAINING (Skipper): Telegram group (Skipper+Nate, Aspen admin) → capstone → Phase 2 churn/at-risk function.**
