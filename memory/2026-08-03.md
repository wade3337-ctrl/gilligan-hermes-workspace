# 2026-08-03

## Prod TRIM IT number extraction — PROVEN + reconciled (Fort Point BLACK-adjacent)

- **Prod is now an open, proven data source.** Two working, observe-only paths:
  1. **Dashboard POST (no login wall):** `POST https://www.greatscotttreeservice.com/gsts/Dashboard-RevenuePerformance.cfm` with a `ZUserID=376` service cookie + form body (`startDate/endDate/groupBy/revenueSource/territory/workType/monthlyGoal/targetTPH/browserToday`). Returns HTTP 200 with produced/TPH figures. **Dashboard ignores URL date params** — must drive `#startDate`/`#endDate` date inputs + Apply (or POST body). Defaults to current month (Aug).
  2. **Browser (Playwright)** via `~/.local/devscout/prodnav8.js` — login `jwade` from `~/.openclaw/.secrets/prod-login.json`, drive date inputs, read innertext.
- **The Revenue dashboard is a PRODUCTION dashboard, not an invoiced one** — its default source is **"True Produced Work (day sheet + schedule)."** There is NO "invoiced" source option on it. Invoiced only lives in the invoice register / Steve's spreadsheet.

### July 2026 — three-way reconciliation (all tie out)
| Metric | July | What it is |
|---|---|---|
| **Invoiced** (Steve's register) | **$1,738,143** | actually billed, 222 invoices — the hard, defensible number |
| **True Produced** (prod dashboard) | **$2,078,762.87** | work performed, day-sheet valued (731 jobs, 15,643 hrs, TPH $137) |
| **Gap** | **~$340,000** | produced-but-not-yet-invoiced (WIP; invoicing lags production) |
- Steve invoiced $1.738M + corrected accrual $441k = **$2.18M "July revenue earned"** — apples-to-apples, ties to produced within ~$100k.
- **Deal discipline:** use **INVOICED** for YoY/TTM (that's the −6.4% TTM finding). Produced ($2.08M) is real but softer/higher; if anyone quotes $2.08M as "July revenue" it's PRODUCTION and FTI would recut to invoiced+accrual.

## Daily COO emails — the real state: HELD, not broken

- **All 3 daily emails (COO email, per-salesperson, Nate rollup) have been COMMENTED OUT / dark since 2026-07-25** ("stale data"). That's the "flying blind" — **9 days of no emails**, NOT broken machinery.
- **Monitor mechanism** (`arbor-stack/anomaly-monitor/monitor.js`): curl-based login (no browser) as bot account **`gilligan-bot`** → two data paths, both keyed to `S.host` (currently **play**):
  1. **Token'd JSON endpoint:** `/GSTS/api/MonitorData.ReadOnly.cfm` — serves `revenue`(needs `period=YYYY-MM`)/`tph_day`(`date=`)/`overtime`/`contracts`/`salesperson_jobs`. 607-line CF page, SELECT-only, richer than dashboards expose.
  2. Revenue dashboard POST (with `ZUserID=376` service cookie).
- Secrets: `arbor-stack/anomaly-monitor/.secrets/gilligan-trimit.json` (host, monitorEndpoint.path/.token, serviceUserID=376).
- **`gilligan-bot` account AUTHENTICATES on prod too** (tested — "My Queue" returned). So switch is close to a host change.

### The key DSN subtlety (why deploy-to-prod is the real fix, not a workaround)
- `MonitorData.ReadOnly.cfm` line 6: `dsn = "GSTSREADONLY"` — labeled "LIVE PRODUCTION (Travis read-only DSN, 2026-07-14)".
- **BUT it still returns play-mirror data** (accrued $277,442 matches play DB to the cent; July invoiced $1.69M < prod's $1.74M). Reason: **the file lives on the PLAY box, and play's nightly webroot refresh keeps reverting the DSN back to the play copy.**
- **On the PROD box the prod DSN sticks — nothing reverts nightly.** That's why deploying the endpoint to prod is the durable fix.
- The JSON endpoint is **404 on prod** right now (only ever deployed to play) — that's the actual blocker to a pure host-flip.

### Deploy package — BUILT, ready for Jordan
- Location: **`arbor-stack/anomaly-monitor/deploy-monitordata-prod/`** — the one read-only `.cfm` + `START-HERE.md` (install path `<prod-webroot>\GSTS\api\MonitorData.ReadOnly.cfm`, the one DSN check, verify curl). One file, one DSN check.
- Follows framing rule: files + what they fix + instructions, no internal history.

### OPEN DECISION (awaiting Skipper) — interim distribution A vs B
Skipper said "keep all recipients same, do both (deploy pkg + scrape), I'm flying blind." Then I found the emails are just HELD + endpoint works (reads play mirror). **Correction I owed him: he does NOT need the scrape — I nearly built a workaround for a system that just needs un-holding.** The one call:
- **(A)** Resume to Skipper ONLY now (play-mirror data, ~2–3% off prod), team back on when Jordan's prod deploy confirms live. **← my rec** (he held them *for* stale data; these are deal-sensitive).
- **(B)** Un-hold full team now (Jordan/Jroulson/Sgriffiths) with a one-line "interim: play mirror, live prod pending" header.
- **I did NOT auto-resume** — waiting on A/B. Once he answers: flip the held crons accordingly. Full-live path: drop `.tgz` on prod → flip monitor host play→prod → un-hold all 3.

## Infra reminders reinforced
- **TWO arbor-stack checkouts:** cron/ops + anomaly-monitor + fleet-telematics = `/home/wade3337/arbor-stack` (canonical). Session work sometimes lands in `~/.gilligan-hermes/home/arbor-stack`.
- Prod login secret: `~/.openclaw/.secrets/prod-login.json` (jwade). Devscout scripts: `~/.local/devscout/prodnav*.js` (prodnav8 = July dashboard reader).
- Play DB (via `gsql.sh`) is ~4 days behind prod — different thing than what the monitor endpoint reads.

## Interim distribution decision (13:32 UTC)
- Skipper chose **Option A**: live pulse (`prod-pulse.js`, 6:30am PT) stays **Skipper-only** for now.
- Full-team un-hold of the 3 daily emails waits until the prod deploy lands (verified live data).

## 🔧 Heartbeat find + fix (15:13–15:20 UTC) — gsql.sh temp-file race broke two self-healing crons

**Found on a routine heartbeat log sweep.** `ensure-bot.log` was 104 ERRORs deep; last clean run 2026-07-29.

- **Root cause:** `arbor-stack/production-dashboard/gsql.sh` scp'd every query to ONE fixed server-side path
  `C:\Users\Administrator\gsql_tmp.sql`. Two crons collide on the :00 boundary —
  `ensure-gilligan-bot.sh` (`0 */3 * * *`) and `gsts-gateway/reapply-play-profilepic.sh` (`*/30 * * * *`) —
  so each ran the OTHER's SQL. Tell: bot log said `ERROR: changed` (20×, that's the pic UPDATE's output);
  pic log said `Msg 102 … Incorrect syntax near ','` (7×, that's the bot INSERT).
- **Real damage (not just noise):** `gilligan-bot` was **actually absent from play** — a nightly restore wiped it and
  the self-healer had failed every run since ~2026-07-06. Verified missing by direct SELECT, then reseeded → UserID 90376 present.
- **Fix:** per-invocation remote temp name (`gsql_tmp_$$_$(date +%s%N).sql`) + server-side delete after each run.
  Applied to **both** copies (repair-contract: verify the neighbours) — `arbor-stack/production-dashboard/gsql.sh`
  and `herman-gateway/gsql.sh`. Backups: `gsql.sh.bak-20260803-race` in each dir.
- **Verified this session:** 4 parallel gsql calls each returned their own marker (was: all same);
  both crons re-run clean back-to-back in parallel (`ok` / `-> 0`); no temp accumulation on the server.
  Herman copy is syntax-checked only — its key `~/.ssh/herman_trimit_rw` isn't in this home (pre-existing, unrelated).
- **Note:** `herman-gateway/regrant-hermanro.sh` is a Windows schtask, not gsql — unaffected.
- Lesson captured in `LESSONS.md` (the 2026-06-21 "run them sequentially" note was an operator rule; cron has no operator).

**Otherwise quiet:** bounces 0, replies 0, prod pulse sent 13:30 UTC (6:30am PT) to Skipper only per Option A.

## Steve July report reconcile (19:40 UTC) — email NOT aligned, held
- Steve July 2026 Summary (as of 08-02): Invoiced $1,738,143.22 + Accrual muni $91,372.97 + Accrual comm/HOA $349,626.82 = GROSS $2,179,143.01; June accrual reversal -$287,141 => NET $1,892,002.01.
- My live pull (08-03): Invoiced $1,779,146.36 (+$41K, a day newer); GetPeriodAccrual $379,037.25 (muni $241,704 / comm $137,333) — WRONG engine vs Steve.
- 3 gaps: (1) different accrual engine (report vs function), (2) invoiced a day newer, (3) my email OMITTED the June reversal => produced $2.079M vs Steve NET $1.892M is ~$187K, not $79K.
- ACTION: rebuild Scott email off Steve report numbers; do NOT send as drafted.
