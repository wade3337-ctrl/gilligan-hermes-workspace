# 2026-08-02

## 🚀 Aspen → Bigin sales-pipeline push — PLANNED + PAUSED (Skipper discussing w/ Nate)
New project kicked off (Skipper 2026-08-02): **Aspen reads the Sales Cockpit → pushes to Bigin → maintains the live SALES pipeline in Bigin.** Model: "Gilligan builds, Aspen runs." Canonical build note → `wiki/projects/aspen-cockpit-to-bigin-push.md` ([[aspen-cockpit-to-bigin-push]]).
- **~60% there — both ends of the pipe verified LIVE today:** Bigin API token refreshes cleanly (admin scope; reads 9 users + Deals module) · Aspen already has TRIM IT read via `trimit-ro-query.sh` (read-only HermanRO, Track-1 scoped) · design done + Skipper-approved.
- **Access model (Skipper asked, important):** Aspen is **DIRECT to Bigin** — its OWN OAuth on its OWN runtime, NOT routed through Gilligan (his original call). Gilligan only builds+verifies then hands off creds+scripts. TRIM IT stays autonomous-but-SCOPED through the `aspen-dispatch.sh` gateway (least-privilege, arbor-core/crew hard-denied). Current caveat: Bigin token verified on jdog1 (my box); making it truly Aspen-direct is a handoff step.
- **⏸️ PAUSED — nothing built/written.** Unblocks on: (1) which rep book to pilot (rec Megan/OC or Chad/IE); (2) Nate's input on pipeline structure/ownership; (3) **the AUTONOMY question** — once verified, do Aspen's Bigin writes run fully autonomous or get a review gate like outbound email? It's the team's shared CRM. Rails in design: dry-run before writes · source-of-truth split (Aspen writes only its own fields) · idempotent dedup. Trust-boundary call, for the Nate conversation.
- ⚠️ **Vault-quarantine lesson (re-hit):** first copy of the note written into the aspen-knowledge vault got auto-swept to `_quarantine/` by the 02:52 autosync. **Gilligan-owned build state lives in the WORKSPACE wiki, not Aspen's vault.**
- Commits: `e6c3084` (save PAUSED state) · `83f4f1b` (access model + autonomy question) — this repo.

## 📨 IE weekly drip to Chad — still running; Wave 5 built for Mon 8/3
Confirmed the "IE pipeline weekly push" cron is live (every Mon 08:12 PT → 6 verified IE targets into Chad's "Inland Empire Expansion" Bigin pipeline). Last real push = Wave 4 (7/27, 6/6). Backlog was about to run dry, so built **Wave 5** + cleared held items → `wiki/projects/inland-empire-expansion.md`, commit `2ca660d`.
- **Wave 5 (queued for the Mon 8/3 cron, not pushed manually):** ✅ Sycamore Creek/Corona (FirstService) · ✅ Prado at Eastvale (Keystone) · ✅ Countryside Marketplace/Menifee (First Commercial) · ✅ Crossings at Corona (Cushman/Castle&Cooke) — all HIGH; 🟡 Harveston & 🟡 Roripaugh Ranch (Temecula) mgmt re-verify at push. 3 of 4 HIGH hit known warm bridges.
- **Held items cleared per Skipper:** Los Serranos → DROPPED (no unified HOA). Vintage Hills → re-approach as a fresh IE deal, queued Wave 6 with a distinct name so it won't collide with the old closed-lost deal. Vellano stays held (mgmt unresolved).
- Deliberate: left the LAST-PUSHED marker at Wave 4 so Monday's automated cron advances it (with its own re-verify) and reports the 6 — kept the drip on normal rails, no off-cycle Saturday write.
- Connection: this IE drip is the "existing IE drip" the Aspen→Bigin plan will eventually absorb.

## 🗒️ Note on capture
The Skipper asked me to "update your files so we know about it later" from a fresh cold session. The build notes + IE backlog were already committed earlier tonight, but there was **no daily log and no MEMORY.md startup pointer** — so it wouldn't have surfaced on a cold wake (which had just happened). Filled both. Lesson reinforced: committing a wiki note ≠ it surfacing at startup; the fresh-session pointer + daily log are the layer that makes durable work *findable* next session.

---

## 🔥 FRICTION HIT LIST — new major workstream (Skipper, 2026-08-02 PM)
Skipper wants **one organized register of wasted time / manual re-work / structural drag in Great Scott — cost + fix + status per item** — to attack them in order and to help Fort Point + Scott understand the need. Becomes part of the **5-Year Business Plan**.
- **Home:** `business-plan/FRICTION-HIT-LIST.md` (🔒 Fort Point/owner-tier) — 20 items, 6 sections (Intake·Activation·Production·Invoicing/AR·Municipal·Foundation), 4-wave attack plan sequenced so AGP-earnout (revenue, excl. overhead) comes first. Top line ≈ **$729K/yr / 8.2 FTE**. Wiki pointer: `wiki/projects/friction-hit-list.md`.
- **Working method (Skipper's rule): brain-dump one node at a time → I compare to our files, say what I think, ask only what I don't know, ASSUME NOTHING. → When a node is SETTLED, WRITE IT UP before moving on.** Deep-dives live in `business-plan/friction-modules/`.

### Node 01 — Marketing/BD → `friction-modules/01-marketing-bd.md`
Not demand-gen — **3 field door-openers: Chad (IE), Megan (OC), Rick (LA, new, not yet in commissions).** No standard method; value locked in individuals (Megan's rolodex). Top-of-funnel invisible till an RFP hits TRIM IT. Comp ≈ **$120k base + 1% new-site commission (~$1,500/mo)** → ~$540k/yr loaded, no ROI visibility; commission too weak to drive Bigin logging (that's why Bigin is empty). **Nate hand-reconciles a TRIM IT commission report EVERY PAY PERIOD (biweekly ~26×/yr)** — messy, error-prone. Fix path: Bigin (unused) + Aspen (already wired to these reps) + fix the commission report (triple win). **Email sent to Nate (via Skipper) asking him to detail the commission fixes+time — NO reason disclosed.**

### Node 02 — Sales → `friction-modules/02-sales.md`
3 sales admin (Rosa, Evie, **Lorena** — Luly left) + 4 arborists (**Scott=CEO backfilling while hiring**, Garrett, Ethan, Rebekah; short an arborist). Admin trio = the manual workflow engine; arborists = pricing judgment (no formula). Thesis: **"$50M in sales without adding admin — impossible with today's tools/process/structure."**
- **Committed fix = the Travis/Jordan "WorkPhloem" Field App on dev; arbor-core = exploration/benchmark/fallback (Skipper's call).**
- **WorkPhloem verdict (walked all 5 stages live):** NOT just a reskin, NOT "5× disease survives." The 4 old defects are FIXED (401 trees render). Writes to REAL shared tables (dedup check). **Its genuine value = digitizing inventory/scope CAPTURE.** Two unclosed seams: (1) doesn't create the RFP (old TRIM IT); (2) no one-click proposal in UI (backend `GenerateProposal` exists, UI=Apply/Export). Missing vs our future-state: live-TPH, inline Price Buddy, customer approve-map.
- **🚨 CORRECTION forced (propagate to investor case): "data re-keyed 5×" is REFUTABLE.** DB shows scope FLOWS via generator procs (Worksheet→Proposal via `GenerateProposal`; Proposal→WO via `GenerateWorkOrderLines*`); identity/address flows too (RFP carries project addr; GenerateProposal copies it). Real manual duplication = field capture (photo sheets/Google Earth/counts) + out-of-system artifacts + transcription points (production paper, QuickBooks) + **"created-after-the-fact" back-entry (people work OUTSIDE the tool, bypassing the auto-flow).** FRICTION-HIT-LIST A3 already re-precised.

## 🌐 NEW STANDING CAPABILITY — server-side browser + dev visibility
Built a headless Chromium driver (Playwright-core, user-space, no admin) → can log into the vendor **dev** box as the Skipper (`jwade`/[pw in secrets]) and observe WorkPhloem WITHOUT dev SSH and without the devs knowing. Scripts in `~/.local/devscout/`; creds `~/.openclaw/.secrets/dev-login.json`; shots `workspace/devscout-shots/`. Details + **🚨 security finding: TRIM IT stores passwords in PLAINTEXT** (`flow.Users.Password`) → `wiki/facts/dev-browser-access.md`. **Observe-only discipline** (some links are state-changing GETs; a create-on-load wrote a blank record last time).

## 🛰️ ONESTEPGPS INTEGRATION — Production node foundation (big build today)
Skipper's Production friction = no objective real-time signal on field execution (yard-departure, real job perf, self-report accuracy). **Trucks already have OneStepGPS; it's just siloed from TRIM IT.** He gave me a **Legacy API key** (in `~/.openclaw/.secrets/onestepgps-login.json`) — ⚠️ **it has DESTRUCTIVE power; READ-ONLY posture, ASK before any write/delete (standing).** All his ideas verified present in the payload: drive-status (yard/dwell), input1/2 (PTO/boom), harsh/speed/idle (safety), odometer/engine-hrs/fuel+idle-waste, vehicle metadata.
- **Vehicle SPINE built** (`friction-modules/03-production-gps-spine.md` + `arbor-stack/fleet-telematics/`): 3-way reconcile Spreadsheet(246)↔TRIM IT `dbo.Equipment`(436)↔OSG(100 trackers). **OSG has NO VIN** → bridge is the asset-name scheme (AB/B/C/D). **71 fully reconciled; ~89–95% trackers auto-match** (11 "orphans" are mostly inconsistent OSG DEVICE NAMES: `B47 HW 1`,`C 68 Unit #175`). ⚠️ **CORRECTED my own overclaim** — earlier "no shared key/can't auto-reconcile" was WRONG (too-small sample). Exception CSVs: untracked trucks, orphan/stale trackers (AB8 dead 2021, C1/C3 2024, dup "(New)"), 81 ERP VIN gaps. ⚠️ Don't state "145 trucks no GPS" as fact — inflated by stale ERP+naming; it's a LEAD.
- **Yard-departure MODULE built** (`osg_snapshot.py` poller + `yard_departure.py` engine + `prune_snapshots.py`): OSG **history endpoint errors with our key → built a read-only SNAPSHOT POLLER instead** (captures current fleet every 20 min → `snapshots.jsonl` = the shared feed for ALL GPS modules). Engine = per-truck per-day first yard-exit, **minute-precise via OSG's `drive_status_begin_time`** even at 20-min cadence. **3 yards named in `yards.json`: Stanton (HQ) · Laguna Woods · Irvine.** Nightly rollup→`yard-events.csv` (git-backed) + gzip-prune raw >45d (self-maintaining; ~2.2MB/day, disk is a non-issue vs 693GB free). **First real morning roll-out distribution = TOMORROW AM.**
- ⚠️ **GOTCHA: TWO arbor-stack checkouts exist** — my session started in `~/.gilligan-hermes/home/arbor-stack` but **cron + real ops use `/home/wade3337/arbor-stack`**. Synced (clean ff), gitignored data files, put crons in the canonical path. **fleet-telematics work lives in `/home/wade3337/arbor-stack/fleet-telematics/`.**
- **Crons added** (canonical crontab, backed up): OSG poller every 20 min · yard rollup nightly 01:15 PT.

## ▶️ TODO tomorrow (Skipper)
1. **GPS task #1 (deferred to today→tomorrow):** firm the soft numbers — normalize the ~11 inconsistent OSG device names + clean the ERP-active list so "trucks without GPS" is a FACT not a lead.
2. Read tomorrow's first real yard-departure distribution.
3. Next GPS modules queued: time-on-job vs clock-in/out · boom/PTO hours · safety scorecard. All bolt onto the spine + snapshot feed.
4. Friction nodes still to brain-dump beyond Marketing/Sales/Production (area-manager bandwidth, training, new-hire pipeline, aged-worker retirement, invoicing/AR, municipal, fleet...).
5. `TODO.md` in workspace holds the running list.

---

## 🌲 ArborNote — GSTS has an ACTIVE account (detour, 2026-08-02 PM) — read-only mandate
Skipper gave an ArborNote **API key** (in `~/.openclaw/.secrets/arbornote-api.json`) + later his **web login** jwade@gstsinc.com (`~/.openclaw/.secrets/arbornote-login.json`). **Read-only — do NOT alter the GSTS account (Skipper).**
- **Reveal:** GSTS has a **live ArborNote account (id 1068, since 2023-11)** with **97 projects / 23,283 trees inventoried / 82 Pending**, and a **huge July-2026 spike (52 projects)** — NOT dormant. **Shadow-IT at scale:** **Rebekah** (sales arborist) uses it for a book she **brought from a former employer** — builds pricing there, **hand-transfers to the GSTS pricing worksheet (cumbersome)**. Big **"CZ" community bid in flight** (CZ-Streets 1806 trees, Pinnacle 1100, Oak Knoll 916… 5,000+ trees, Pending). Clients incl. City of Irvine, Gothic OC, + staff-named (Rebekah, Chad Bouck). **Strong friction evidence: our own people vote with their feet because ArborNote does on-site inventory→bid better than TRIM IT.** (ArborNote backend is also ColdFusion — `index.cfm`.)
- **Public API** `https://api.arbor-note.com/v1` (auth: **`x-api-key`** header, case-sensitive → use curl not urllib): **CRM-only** — `clients`, `projects` (name/addr/GPS/market/**treeCount summary**/notes/status), `work-orders` (empty). **NO trees, NO pricing, NO export endpoint** (probed 0). So Rebekah's transfer **can't** be automated via the public API.
- **Internal API** `https://wup8z01em5.execute-api.us-east-1.amazonaws.com/v21/` (**session-auth**, richer): dashboard, projects, work-orders/activity, account/settings, requirements. **Reached it by driving the browser login** (proven: logged in as Jason Wade, admin). **This is the likely automation path** — pull pricing/line-items from v21 with a session token → push to the GSTS worksheet. ⏭️ NEXT: open a CZ project in the browser to capture the exact **pricing/line-item endpoint** (the project-row click needs a better selector).
- Tools: `~/.local/devscout/arbornote_login.js` (browser login+capture), `~/.gilligan-hermes/home/arbor-stack/arbornote/explore.py` + `export_probe.py` (API mappers). Login SPA notes: Angular hash-route (`/ng/#/login`), needs real keystrokes (pressSequentially) + reload after token to initialize; login returns 203 with `{"Success":true, AccessToken}`.
