# 2026-07-16

## 🏛️ MuniBot 200GB Brent-data re-plumb (Skipper approved)
- Skipper copying Brent's muni bid data `\\gsts-server200\GSTS\Municipal Bid Data\Jason_Compiled` (~200GB) GSTS→PC→MuniBot. Last night's robocopy got ~1/3 onto his PC; upload never ran (municipal-history empty here — nothing to clean on box).
- **Problem w/ old pipe** (`munibot-receive.sh`): tar→`git add/push` to GitHub + docker cp = 200GB can't go to GitHub (100MB/file cap) AND ~3-4 copies/cycle (inbox+git working+.git+container) would fill the 798G disk. tar|ssh also re-streams all 200GB every run (no resume).
- **New two-tier design (BUILT + TESTED box-side):**
  - Warehouse = plain files at `~/.munibot/municipal-archive/` = container bind-mount `/opt/data/municipal-archive` → MuniBot reads directly, ZERO dup. 200G→1 copy, ~598G free after.
  - Drop key `munibot-file-drop` forced command re-pointed: `munibot-receive.sh` → `/usr/bin/rrsync -wo /home/wade3337/.munibot/municipal-archive` (write-only restricted rsync; incremental+resumable). authorized_keys backed up (.bak-munibot-replumb-*), single-line merge-edit verified.
  - Proven via loopback rsync: file+subdir land in warehouse, container sees them, DOWNLOAD denied ("reading from write-only server not allowed").
  - Raw files NEVER go to git/GitHub/Obsidian. Backup = treat as re-syncable cache (source=GSTS share). Distilled text notes (off-peak extraction, TODO) feed municipal-knowledge/Obsidian.
- **REMAINING = Windows uploader.** Old SyncMuni.cmd (tar|ssh) backed up. Need rsync on Windows (cwRsync) OR alt — ⚠️ GSTS handbook restricts unapproved software installs → asked Skipper which route (portable cwRsync vs already-approved tool). Keep his existing ~1/3 stage (head start).

## 📇 Apple Contacts ↔ TRIM IT reconciler — vCard landed + Stage-1 built
- Skipper sent Scott's iPhone vCard (6,080 contacts, 68MB) — the long-awaited "answer key." Stored PRIVATE `~/contacts-work/` (outside git/GitHub — personal PII).
- Built read-only reconcile: parse+triage (2548 work/2427 review/1105 personal) → match vs TRIM IT ProjectContacts+Contacts (1,435 matched) → A: 78 typo-merge clusters + 36 shared-inbox (do-not-merge, surname-sim classifier) → B: 475 missing managers at known companies + 1035 new + 140 muni. Zero DB writes. Details → [[apple-contacts-reconciler]].
- NEXT: Stage-2 write-back per db-repair-contract (build+test play, backup, propose-for-review, devs deploy).

## 🎯 Aspen BD-targeting brain BUILT (Phase 1+2) — serves $50M/5yr goal
- Skipper set North Star: $25M→$50M in 5yr; targets IE+LA push + grow OC backyard (weak BD = missed backyard opp). Aspen = the BD engine. Saved [[50m-growth-goal]].
- Built read-only relationship graph from TRIM IT (Projects/Companies/ProjectContacts/WorkOrders) + Scott's reconciled contacts: Property↔owner-Company↔Manager↔mgmt-company-by-domain + value + region + penetration + warm-contact.
- Headline: 1,844 winnable (lapsed/never-won) properties at known mgmt cos, 1,334 warm. Biggest relationships under-penetrated (FirstService 17/92, Powerstone 60/148). Loaded into Aspen (business-development/). Details → [[aspen-retention-agent]].

## 🔌 Dry→Save+Expand wire (Cockpit→Aspen) built
- Skipper asked to wire Sales Cockpit "running dry"/rebid into Aspen BD engine. Built the DRY half: 223 running-dry accounts / $24.5M / 190 warm, each enriched with who-to-call + mgmt-co winnable bundle. In aspen-knowledge/business-development. Details → [[aspen-retention-agent]].
- Gotcha captured: TRIM IT DateCompleted & Proposals.DateApproved are sparsely populated → can't use "not completed/approved" as open-state; use future-dated WOs + activity RECENCY instead. Rebid half needs contract-term data TRIM IT lacks (deferred).

## ✅ Rebid radar built + FULL WIRE capstone PASSED
- Built contract rebid radar from TRIM IT Contracts subsystem (real EndDate/incumbent/value): DEFEND 27/$13.3M, WIN 38/$3.3M(all warm), 60 recently expired. Municipal-heavy → converges with MuniBot + muni contacts. In aspen-knowledge/business-development.
- CAPSTONE 2 PASSED: asked Aspen for top-3 rebid plays → it nailed them (OC Public Works $2.5M WIN/John Dean, OCWD DEFEND, Anaheim UHSD), assessed MuniBot usefulness per target with real nuance (county YES / water+school district LIMITED), CAUGHT the $-verify caveat, cited note line numbers, explained HOA=dry vs municipal=rebid split. Aspen is a working account-strategy engine.

## 🏛️ Municipal Bid Co-Pilot roadmap (MuniBot tool per pipeline stage)
- Skipper: Brent's muni pipeline needs a TOOL per stage (procurement-push, not cold-call). Mapped 7 stages → MuniBot tools (portal monitor, RFP intake/parser, go-no-bid scorer, Q&A drafter, addenda watcher, bid assembler+compliance, award/board monitor, won/lost debrief) + bid-readiness registry (DIR/bond/insurance/prequal). Reuses arbor-core RFP intake agent + pricing engine + MuniBot monitoring. Roadmap in aspen-knowledge/business-development/municipal-bid-copilot-toolmap.md. Sequenced after the Bigin wire.

## 📋 MuniBot plan notes added (Skipper request) + BLACK-guard catch
- Added the municipal BD plan to MuniBot's OWN files: `municipal-knowledge/procedures/municipal-bid-pipeline-and-munibot-tools.md` (7-stage bid pipeline + per-stage MuniBot tools) + `references/growth-plan-and-bigin-routing.md` ($50M context, muni→Brent's Bigin, two-way source-of-truth). Synced to container+GitHub+Obsidian.
- ⚠️ BLACK-guard worked: first draft named "arbor-core" → auto-quarantined on the Aspen sync. Rewrote Track-1 clean (no BLACK refs). Lesson logged.

## ⚠️ Security catch: rsync password-fallback bypassed the write-only drop
- MuniBot upload: cwRsync ssh rejected the key (Windows ACL perms) → password fallback succeeded → forced-command (rrsync -wo warehouse) BYPASSED → rsync hit ~/ instead of warehouse. Caught fast (only empty cygwin stub dirs landed, NO real data). Killed session + rsync procs, removed stubs. Warehouse clean.
- Fix: SyncMuni v3.3 (icacls key-lock + publickey-only, no password fallback). Server hardening PasswordAuthentication no = needs sudo → flagged to Skipper/IT. Lesson logged.

## 🌅 Morning (2026-07-16): MuniBot transfer = v4 one-hop, Skipper runs at office
- Root cause of last night's repeated failures (crew/Fable confirmed): two-hop design staged 200GB on the user's C: → disk filled → window died mid-copy ("past San Bernardino"), no readable error.
- FIX = SyncMuni v4 ONE-HOP: rsync straight from the mapped share (T:) → warehouse, no local staging. + auto-retry + pause + logfile (readable). Canonical zip = `~/munibot-gateway/SyncMuni-v4-onehop.zip` (on-disk .cmd is hook-mangled — don't reuse it). Skipper will run at the office today; watchdog armed. Resume note: [[munibot-data-warehouse]].
- Also last night's security catch (password-fallback bypass) fixed client-side; server `PasswordAuthentication no` owed to IT.

## 📕 Handbook remediation tracker stood up
- Skipper: reconcile the 52 legal findings (Herman's crew review: Claude+GPT-5.6) against what Roxanne's draft already marks for removal. KEY: the md the reviews ran on was FLAT — didn't preserve the DOCX's red-strikethrough(remove)/yellow-highlight(revise) markup, so reviews re-flagged already-removed content.
- Parsed the DOCX: 440 strikethrough runs (~5,447 words REMOVE) + 350 highlighted (new/revise). Built `handbook-remediation/HANDBOOK-REMEDIATION-TRACKER.md`: first-pass status per finding (Removed/Revised-verify/LIVE/LIVE-add). Confirmed removed: COVID leave (#16), duplicate welcome+phone (#31). Confirmed LIVE Tier-1: CFRA 75mi test (#1), New Parent Leave (#3), arrest reporting (#5). 13 missing policies to add.
- ⚠️ 🟨 "revised" bucket is keyword-matched (noisy) — needs a manual per-section eyeball pass. Attorney sign-off on legal items; Roxanne feedback due 7/24. Source reviews saved in handbook-remediation/source-reviews/.

## 🎛️ Sales Cockpit repair (Northwood "can't find my job") — SHIPPED #174
- Root cause: cards show only account name (Nickname), search only name+company+mgr+city → "Northwood Estates" ($107K bid) displayed as "Northwood II HOA", unfindable. NOT the stageOf bug (that's real but didn't hit this one).
- A: added PropName (JobAddress line1) + JobAddrFlat via CROSS APPLY; show property sub-line + add prop/addr to search hay. B: "Bid out $X" badge on working/scheduled cards with an open bid (Skipper chose badge over moving). D: retired 7 ZTest-Cockpit*/SalesPipeline* orphans on play (.ORPHAN + Jasonsrepairs) + archived local. C deferred (widen status filter). Backup-first, render-verified 0 CF errors. Live file = Dashboard-SalesCockpit.cfm (ZTest ones were orphans — Skipper caught it). Ship #174.

## 🔗 Aligned Aspen BD data to the cockpit change (Skipper: "make sure Aspen is included")
- Cockpit fix #174 (property-name display+search) → propagated to Aspen: added `property_name` (JobAddress line1) to running-dry CSV. Flagged JobAddress-line1 imperfection (sometimes a contact/street, e.g. Trailwood→Carissa Andrus) — same as cockpit shows it (secondary line).
- Wrote `aspen-knowledge/business-development/CANONICAL-cockpit-alignment.md`: cockpit = canonical source for open-bid/running-dry/rebid/stage defs; Aspen's AM-built wires were approximations → cockpit wins; full reconciliation = recommended next step. Synced to board+GitHub+Obsidian.

## ✅ Reconciled Aspen running-dry to the cockpit's EXACT definition
- Ran the cockpit's canonical RunningDry SQL (InProcess/Pending + forward book ends <3mo + no fresh open-bid) → 80 accounts (vs my AM approximation's 223 — different measures; cockpit wins). Enriched w/ mgmt-domain + warm-contact + winnable bundle, sorted soonest-to-dry. Regenerated Aspen's running-dry CSV + updated wire + CANONICAL notes. Numbers now tie to the cockpit. Rebid radar unchanged (no cockpit equivalent). Script: contacts-work/recon_dry.py.

## 📊 SPM Results — Multi-Year-by-Market panel SHIPPED #175
- Added 5-year (dynamic) trend by segment (HOA/Commercial/Muni-cities/Muni-other) to SPM Results: same-date YTD + full-yr secondary + YoY, reusing rgc.vProjectMarket. Live-verified 0 CF errors, ties to DB. Story: HOA up-then-flat, muni-cities declining, commercial bumpy. Backup-first, no shadow. (Skipper asked "how are we tracking vs 2023/2024 by segment.")
- Pro-tip spm.results.mktyears added + served-verified (2026-07-16).

- ⏸️ **HELD — Production Perf future-dated crew sheets:** Skipper spotted Irvine showing $81,403.90 "produced" in Aug 2026 (future; today=Jul 16). Root = ~$557K company-wide of Complete+checked-in CrewSheets dated Aug–Oct. Fix designed (cap production at today, all grains in `ProductionPerf.data.cfm`) but **held** — Skipper asking the team why the future dates exist first. Full finding → `wiki/facts/production-perf-future-dated-crewsheets.md`; task #5.
