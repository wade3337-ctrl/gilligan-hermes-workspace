# Session state — 2026-06-16 (handoff for a fresh conversation)

Big day on the TrimIT cluster page (`Maint-ClusterDefs-Page.cfm`) + Customer Leads. Ship Log #25–#47 cover it all (read `arbor-stack/gsts-ship-log.md`).

## 🗺️ Cluster Management — latest = #52 (mobile r4), all live on play, awaiting Skipper phone re-test
- #52: map keeps selected area on return (reapplyHighlight on iframe load); bottom project-list zebra+row-lines+sticky name col on mobile; "?" now opens a real Quick Help panel (was a hidden tap-to-reveal mode).
- Prior cluster: #50 load-once client-side filtering + sortable cols + 5-yr baseline label; #46–49 mobile cards / tooltips / muni-comm→list / accordion. Files: `Maint-ClusterDefs-Page.cfm`, `ClusterDefs-Map.cfm`, `ClusterDefs.GeoJSON.cfm`, `Profile.ClusterDef.Projects.Content.cfm`. Not prod-deployed yet (play only).

## 💰 City Budgets Dashboard (NEW, #51) — PARKED (Skipper Jun 16, working but paused to return to cluster)
- Automates Brent Beller's Municipal Budget Report. Files: `Dashboard-CityBudgets.cfm` (render) + `Dashboard-CityBudgets.Export.cfm` (2-tab .xlsx) + `CityBudgets.data.cfm` (shared data — single source of truth). All on play. Full reverse-engineering + reconciliation in `arbor-stack/budget-report/PROCESS.md`; detail `ship-log/2026-06-16-city-budgets-dashboard.md`.
- Per city + FY (FY shown on page): Budgeted/Invoiced/Call-Ins/Scheduled/Remaining + monthly invoice breakdown + Work-at-Hand WO table + Excel export. **Reconciled exact to Brent (Newport FY25/26):** Budgeted $2,030,649.30, Invoiced $1,800,911.92 (June held like Brent), Scheduled ~match.
- Sources: Contracts.YearNNBudget (Approved Tree-Maint contract, excl. Homeowner-Paid 1246) + InvoiceMasters (Desc 'Invoice Master - %', status 297) + WorkOrders (Progress=CompletedDollars, per-WO Invoiced=SUM Invoices; excl 'Revised'). 16 cities (de-duped), FY 22/23–26/27.
- **Done this session:** export, city de-dupe (Irvine), hold-current-month, dropdown-scope bugfix; **#53** invoiced-$0 fix (dropped Newport-specific desc filter); **#54** city list now mirrors the Contract Dashboard's CURRENT clients (added Cal State LB, Industry, Stanton, East Whittier SD, Huntington Beach SD, Magnolia USD; dropped Cerritos, Lake Forest, County of OC, Laguna Woods, Moulton Niguel, Woodbridge), went **company-level** + **hybrid Invoiced** (masters where present → Newport exact $1,800,911.92; else actual-invoices fallback → Cal State LB $373,145, Magnolia $53,650). All 16 cities show real numbers, 0 CF errors. Reconcile vs Contract Dashboard documented in `budget-report/RECONCILE-vs-ContractDashboard.md` (different by design = option a).
- **OPEN / next when resumed:** (1) more cities beyond the test look; (2) reconcile BUDGETED vs the production Contract Dashboard after tomorrow's play refresh (the dev GenerateContractPeriod fix); (3) Skipper to spot-check several cities; (4) eventual all-cities rollup. Negative accruals on some WOs are legit (over-invoiced vs progress).

## Where we are
- **Cluster Management page** rebuilt into a customer-territory **lead-finder**: Look-back (1–5 yr) + Leads-only filters; map dots + #Cities + list all in sync (server params ZYears/ZLeadsOnly); customers clickable to project; Last Work/Last Bid cols; sad Arbor Helper mascot (Herman's art, shared `css/gsts-icons.css`) on follow-up leads; welcome popup (DashboardPrefs PageKey ClusterManagement); tooltips to Pro Tip spec; map drag disabled; hover-revive bug fixed; **mobile responsive (vertical cards, 860px breakpoint)**; just shipped 3 mobile fixes (#47).
- **#50 (Jun 16 PM):** load-once client-side filtering + sortable columns + mobile r3 (`Maint-ClusterDefs-Page.cfm` + `Profile.ClusterDef.Projects.Content.cfm`). Customers tab loads full 5-yr set once (620 rows), filters look-back/leads/area in-browser (query now carries LastInvoiceDate + Rev1..5 + InvBucket; one `applyFilters()` = single source of truth, replaced 3 server filters + old applyAreaFilter — the duplicated-filter hardening, done as a side effect). Map syncs via debounced iframe refresh. Sortable cols (within-cluster on Customers; flat on projects list). Market Clusters #Cities = 5-yr baseline (labeled). Mobile: seq inline w/ name, panel 340px→62vh. SQL + render validated (0 CF errors, 620 rows). **Awaiting Skipper test.** NOTE: the duplicated leads/WIP SQL hardening the queue mentioned is now largely DONE via this refactor.
- **#49 (Jun 16 PM):** mobile UI polish round 2 — 4 Skipper items across 3 files (`Maint-ClusterDefs-Page.cfm`, `Profile.ClusterDef.Projects.Content.cfm`, `ClusterDefs-Map.cfm`): (1) Market Clusters accordion (collapse others on focus, mobile); (2) Muni/Comm counts → color-coded project LIST filtered via new `ZType` chip-preselect (was a map; ClusterDefID=0=All Clusters); (3) killed sticky touch tooltips (hover-only binding); (4) mobile Pro-Tips "?" help-mode button. + 16px controls (iOS zoom), touch targets, map finger-pan. JS clean, compile-clean, ref page synced. **Awaiting Skipper phone re-test.** After mobile is signed off → the **hardening pass** (kill the 3-way duplicated leads/WIP SQL filter, consolidate scripts) is the agreed next step.
- **#48 (Jun 16 PM):** fixed PC/desktop bug — Customers list with an **area selected** + toggling **Leads only** was wiping the area → showing ALL. Root cause: on-load restore dispatched a synthetic `change` before the filter listener attached (same IIFE, wrong order) → no-op. Extracted hoisted `applyAreaFilter(v)`, called directly from dropdown + restore. Live on play (40880 B), ref page synced. Awaiting Skipper PC hard-refresh test.
- **AWAITING:** Skipper to re-test the cluster page on his **phone** (vertical scroll, map below at 340px, welcome modal fit, touch-to-map scroll, leads-only staying on tab). He must hard-refresh on mobile to drop cached JS.
- **Customer Leads dashboard** — prod-ready; deploy email already sent to jwade@gstsinc.com (3 files + DashboardPrefs table).
- **Cluster page prod deploy** not yet packaged. Offered to draft the IT email (page + ClusterDefs-Map.cfm + ClusterDefs.GeoJSON.cfm + css/gsts-icons.css + css/gsts-protips.css + assets/icons/*.png + DashboardPrefs table).

## Open follow-ups / parked
- Cron reminder set: **Fri Jun 19 9am PT** — check whether IT deployed Customer Leads to prod.
- Long Beach 26/27 IT email (Contract Dashboard) still awaiting Skipper forward (#11).
- Off-machine backup still parked.

## Notes
- Chat errors today = long-conversation strain, not TrimIT. Starting fresh is fine; all work is on the server + logged.
- `cursor:help` (the ugly `?` cursor) swept off 7 pages (#45).
