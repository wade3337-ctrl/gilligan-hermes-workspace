# 2026-06-06

- **Memory reorganized into two domains** (per Skipper): 🛠️ WORK + 🎣 PERSONAL & HOBBIES in MEMORY.md, kept separate. Cross-cutting HOW WE WORK + ENVIRONMENT sections at top.
- **Work env snapshot** written → `/home/wade3337/arbor-stack/gilligan-environment-snapshot.md` (Skipper added to his consolidation).
- **Hobby consolidation filed** → `/home/wade3337/hobby-stack/MASTER_B-rc-hobby.md`; distilled into MEMORY 🎣 section (RC quads/heli, Ethos vs EdgeTX non-portable, AMA Jet Log + Spektrum apps, OpenClaw's RC-influencer origin).
- **⭐ Behavior correction (logged to USER.md):** Skipper wants ONE question at a time, no multipart; hold questions to end of multi-step tasks. I'd been batching — fixed.
- **PENDING:** Skipper has ChatGPT consolidation files to insert later (file into matching WORK/HOBBY domain when they arrive).
- **Resumed:** Anomaly Monitor BUILD phase (all 4 metrics defined; see arbor-stack/anomaly-monitor/METRICS_SPEC.md).
- **⚠️ TrimIT login regression (Sat 6/6):** gilligan-bot login stopped issuing a session — login POST returns SAME result for good/bad password, NO Set-Cookie, /index.cfm bounces to public marketing site. Auth-gated pages (M1 day-frame, M3 contracts) now return 687b login-bounce; non-auth pages (revperf, Exec Periods) still render. Worked earlier today → server-side change suspected. Gave Skipper a Codex prompt to investigate+fix (backup-first, keep bot read-only). **Awaiting Codex result.**
- **Built Metric 2 builder** `arbor-stack/anomaly-monitor/m2-revenue.js` (works now via the 2 non-auth pages): (A) SCHEDULED pacing from Dashboard-RevenuePerformance.cfm POST by territory (company + N/S share); (B) REAL invoiced by month from Exec$Periods$Overview.cfm. Live 6/6: company booked $1.77M = 80% of $2.2M (-$432K, June schedule still filling); May invoiced $1.84M (latest closed); June invoiced $171K (billing catching up). STILL STUBBED (needs login fix + data endpoint): Accrued (dbo.GetPeriodAccrual) + territory split of Earned.
- **Next build:** M1/M3 parsers (blocked on login fix); the read-only JSON endpoint for M2 accrued+territory; then report/digest + scheduling (4am/6:30am).

## ✅ BREAKTHROUGH (Sat 6/6 PM): Monitor engine working end-to-end on live data
- **Root cause of login wipe:** play DB is auto-restored FROM PRODUCTION; gilligan-bot (created in play) gets wiped each restore (UserID 376 vanished, MAX was 375). Devs will add bot to PROD later (durable). 
- **WORKAROUND BUILT (the real fix):** Codex built a read-only JSON endpoint **`/GSTS/api/MonitorData.ReadOnly.cfm`** — a FILE (survives DB restores) + auth-bypass entry in Application.cfc (backup made). Secured by **token + IP allowlist** (my server 76.32.188.157). Token in `.secrets/gilligan-trimit.json` → `monitorEndpoint`. Serves all 4 metrics as JSON: `metric=tph_day&date=`, `overtime&weekStart=`, `revenue&period=`, `contracts&group=11`. **No bot login needed → immune to the backup wipes.** Verified reachable from my server (200 + 403 without token).
- **Monitor engine built:** `/home/wade3337/arbor-stack/anomaly-monitor/monitor.js` consumes the endpoint, applies all rules, prints the report. Verified live on 6/4 data:
  - M1: dollars-pending guard (a≤0/callIn≤0 = held) + **10% too-early floor (Skipper confirmed)** both working; bands vs 130 + scheduled variance.
  - M1b: OT >8/day + 6th-day, week-aware Mon–Sun; company total + per-man >1hr flags.
  - M2: earned = invoiced+accrued, company + N/S (May $2.09M = 95% of goal); + scheduled pacing in `m2-revenue.js`.
  - M3: remaining/%, flag <10% / over-budget, FY-elapsed pace (Irvine + San Clemente over budget).
- **✅ WIRED & LIVE (Sat 6/6):** monitor.js now auto-finds the latest reported day (walks back ≤8 days, handles weekends + play lag; flags staleness), Monday block auto-includes (PT-aware). Bands finalized: 🔴<120, 🟡120–129, 🟢≥130 (130=on the money), 1-decimal TPH display. OT grouped by crew (hottest first, crew totals). Reds show actual TPH + "scheduled" (not "bid"). 
- **Cron job:** `gsts-daily-monitor`, **6:30am PT daily** (tz America/Los_Angeles), runs monitor.js + posts to this DM. **Delivery gotcha (fixed):** first tried sessionTarget=current/agentTurn + explicit delivery{channel:discord,to:userid} → FAILED "OutboundDeliveryError: Unknown Channel" (it resolved the DM userid as a guild channel `channel:...`). **Fix: recreated as sessionTarget="main" + payload.kind="systemEvent"** (id 5158f540-e4d4-4a84-99f8-38e86d221f47) — delivers via normal in-session reply, no explicit channel/to. For DM delivery from cron, DON'T set explicit discord channel/to; use main+systemEvent.
- **⚠️ DATA-FRESHNESS caveat:** the endpoint is on PLAY, which is a periodically-restored copy of PROD → play's data lags (currently stops at 6/4; 6/5–6/6 empty). Monitor reports freshest available day w/ a stale flag. **DURABLE FIX (devs, later): deploy MonitorData.ReadOnly.cfm to PRODUCTION (www) for live "yesterday" data + add gilligan-bot to prod.** Both are the same "play is a prod restore" root issue.

## 📧 → EMAIL CUTOVER (Sat 6/6 PM): reports moved OFF Discord to email (Skipper request — DM was getting messy)
- **Gmail SMTP email channel built** (see MEMORY.md ENVIRONMENT). Skipper made a Gmail App Password; stored `.secrets/gmail.json` (0600). Helper `send-email.js`. Asked good security Q ("does this give access to my whole account?") → explained App Password = mail scope only, revocable, not a master key.
- **Re-verified M1 is LIVE** (I almost rebuilt it — secrets `monitorEndpoint.status` field said "pending" but was STALE; the endpoint is actually live. Lesson: check reality/daily-memory, not the stale status field. Fixed the field → now says "LIVE — verified 2026-06-06"). Live test: 403 w/o token, 6/4 = 42 jobs/31 reported, 6/3 = 46/36.
- **monitor.js tweak:** `BASE`/`TOKEN` now honor env overrides `MONITOR_BASE`/`MONITOR_TOKEN` (fallback = real endpoint) — lets us point at a local mock or prod-vs-play without editing code. Test fixture: `test/mock-endpoint.js`.
- **Delivery wrapper:** `run-and-email.sh` — runs monitor.js, emails report (or emails an error on failure). Manual run always sends; `--guard-pt-morning` flag only proceeds when it's the 6 o'clock hour in PT.
- **Scheduling = SYSTEM CRONTAB (Path A, Skipper chose)**, NOT OpenClaw cron — zero Discord, zero model tokens, deterministic. Fires **13:30 & 14:30 UTC**, guard lets exactly one through at **6:30am PT year-round** (DST-proof; Ubuntu cron 3.0pl1 does NOT support CRON_TZ, so used the two-fire + PT-hour-guard trick). Logs → `anomaly-monitor/logs/cron.log`. Existing backup crontab entries (3:00/3:15) preserved.
- **OpenClaw cron `gsts-daily-monitor` (5158f540…) DISABLED** (not deleted — kept for rollback) so no double Discord delivery.
- Verified real reports emailed to wade3337@gmail.com this session (sample + live 6/4). **Next M1 cron email fires Sun 6/7 6:30am PT** (will show freshest available play day + staleness flag until prod deploy).
- **M2 revenue → moved to DAILY** (Skipper 2026-06-06: wants monthly progress every morning, not weekly). monitor.js: revenue block pulled out of the Monday gate into the daily report; periods now computed dynamically from today (killed hardcoded May/June). Daily shows June MTD earned vs $2.2M + % + day-of-month pace + N/S; last-closed month as reference. M3 contracts stays Monday-only. Verified live. **Booked-to-goal pace ADDED to daily (Skipper said yes):** `bookedPacing()` in monitor.js POSTs the non-auth Dashboard-RevenuePerformance.cfm (ScheduleBoard) and adds `🎯 Booked to goal: $1.77M = 80% of $2.2M (-$432K short) — banked $480K + scheduled $1.29M`. Pairs the banked/earned read (lags) with the forward on-track read. Wrapped try/catch → degrades to one line if revperf hiccups, never breaks the report. So daily revenue block now = EARNED MTD + N/S + 🎯 booked-to-goal + last-closed reference.
- **Email FORMAT reworked (Skipper 6/6 eve):** was Discord markdown w/ alerts buried mid-sentence + literal `##`/`**` junk in email. Now a clean PLAIN-TEXT OUTLINE: `AT A GLANCE` block at top surfacing every alert as its own line, numbered sections (1 TPH / 2 OT / 3 Revenue / 4 Contracts-Mon-only) with rule lines, each alert on its own indented line. Stripped all markdown. money() now formats negatives as -$X.
- **OT crew fix (Skipper 6/6):** crew label = the crew LEADER's name → he was listed twice (header + member). Now leader on header w/ his own OT + crew total, only his guys listed under. Match leader via stripCodes(crewLabel) startsWith member name.
- **WORKING-DAY calendar (Skipper 6/7):** monthly pace now in WORKING days not calendar days. **Work week Mon–Sat.** Paid holidays: New Year's, Good Friday, Memorial, July 4, Labor Day, Thanksgiving + day-after, Christmas Eve, Christmas (2026 dates computed + stored). Rain days logged as reported. Config `anomaly-monitor/config/work-calendar.json` (workDays/holidays/rainDays). `workingDays()` in monitor.js. **June 2026 = 26 working days** (no June holidays fall on his list). Skipper's "24" was just an estimate — **confirmed 26 correct, no rain days to log.** Verified working-day counts: May 25, Jun 26, Jul 26, Nov 23, Dec 25.
- **PHASE 2 PROBE STARTED (Skipper 6/7): per-salesperson job emails.** Read-only schema dig done. **Chain CONFIRMED:** WorkOrders.ProjectID → dbo.Projects.SalesRepID → dbo.SalesReps(61).UserID → flow.Users(name+email). Scheduled dates via CrewSheets.CalendarID → Calendars. TrimIT has dbo.EmailMessages (already does email). Schema docs only carry CURATED column lists → 3 unknowns need LIVE SQL: (1) flow.Users email col + populated for 61 reps? (2) authoritative WO scheduled-START date (WO col vs min CrewSheet date vs ScheduleBoard) (3) is Projects.SalesRepID the rep who SOLD it (spot-check). Wrote **plan** `anomaly-monitor/SALESPERSON_EMAILS_PLAN.md` + **read-only Codex probe** `anomaly-monitor/CODEX_PROBE_salesperson.md` (SELECT-only: column introspection + 5-row chain spot check + upcoming-WO counts). NEXT: run probe → paste results → lock design → build `metric=salesperson_jobs` endpoint branch + `salesperson-monitor.js`. Sending: pilot on Gmail (1-2 reps), then real domain before full rollout (~500/day cap).
