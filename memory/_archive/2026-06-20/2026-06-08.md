# 2026-06-08 (PT Mon)

## Discord security lockdown (Skipper wanted "only I can use you")
- Confirmed already locked: `channels.discord.groupPolicy: allowlist` (one guild/channel), `commands.ownerAllowFrom: ["discord:1301226640130445323"]`, DM `dmPolicy` = default **pairing** (strangers get a pairing challenge they can't pass w/o his approval). Net: nobody can use Gilligan w/o his permission as-is.
- Offered stricter `dmPolicy: "allowlist"` + `allowFrom:["discord:<his id>"]` hardening — Skipper left it as pairing (good enough). NOT applied.
- **Discord portal: bot set to PRIVATE** — Public Bot OFF (had to clear the Installation "Install Link" → None first). Bot name changed to **Gilligan** on dev page (note: chat display name = Bot tab Username field, separate from App name).

## Herman context file
- Skipper has a 2nd bot **Herman** (Arduino work). Wrote portable working-style brief → `files/HERMAN_CONTEXT.md` (his comms prefs + hands-on mechanic/RC background; NO business/credentials).

## PARKED — Gilligan ↔ Herman collaboration on Discord (resume later)
- **Herman = separate bot on the "Hermes" platform tied to an Arduino board; IS on Discord** (own bot presence). NOT an OpenClaw agent in this instance.
- **Agreed approach:** shared dedicated **#workshop** Discord channel where both bots live; **loop-safe via mention-gating.**
- **My side (to do when resumed):** add the workshop channel to `channels.discord.guilds.<guild>.channels`; set `channels.discord.allowBots: "mentions"` (only respond to a bot when @mentioned) + keep `botLoopProtection` backstop (default 20/60s, can tighten to ~8); restart gateway. (Don't even need Herman's bot ID — mention-gating + private channel suffices.)
- **Herman side (Skipper/Hermes):** add Herman to the channel + configure it to read Gilligan with the same "@mention only" rule (bots ignore each other by default).
- **NEXT STEP to resume:** Skipper creates #workshop, adds BOTH bots, sends me the **channel ID** (Discord Developer Mode → right-click channel → Copy ID) → I configure + restart → live mention test.

## PARKED — Voice with Gilligan (set up tomorrow; Skipper wants BOTH, phone-call FIRST)
- **Goal:** Skipper wants to talk to me by voice. Two modes, doing **(1) live "phone call" Discord voice channel FIRST**, then **(2) async voice messages** later.
- **⭐ VOICE PREFERENCE: FEMALE voice (NOT male/"cedar").** Pick a female OpenAI realtime voice — candidates: **shimmer / coral / sage / marin**. Skipper can swap.
- **Platform supports it** (docs: `channels/discord.md` §Voice, ~line 1163). Realtime mode default = `agent-proxy`.
- **MY SIDE (config, backup-first + restart):** add `channels.discord.voice` block — `enabled:true`, `model:"openai/gpt-5.5"`, `realtime:{provider:"openai", model:"gpt-realtime-2", speakerVoice:"<female>"}`, `allowedChannels:[{guildId,channelId}]`; enable native slash-commands (`channels.discord.commands.native` or `commands.native`) for `/vc`. Consider `voice.realtime.requireWakeName:true` + wakeNames ["Gilligan"] so I only speak when addressed.
- **HIS SIDE:** create a Discord **voice channel** (e.g. 🎙️ Gilligan Voice), grant the bot **Connect + Speak + Send Messages + Read History**; I generate a **fresh invite URL with `applications.commands` scope** (bot is now private, only he can re-add) so `/vc` registers; then `/vc join channel:<id>`.
- **⚠️ KEY UNKNOWN:** does his OpenAI **Codex OAuth** cover the **Realtime API** (gpt-realtime-2)? Docs pair "OpenAI Realtime 2 + Codex brain" so likely yes — but VALIDATE on first test call; if not, need Realtime API access / an OpenAI API key.
- **Discord intents:** Message Content Intent (likely already on), Server Members Intent if role allowlists used; voiceStates intent auto-enables when voice block present.
- **NEXT STEP to resume:** Skipper creates the voice channel + sends me its **channel ID** → I back up config, add the voice block (female voice), enable native commands, restart, give him the re-invite link → test `/vc join`.
- **Phase 2 (voice messages):** inbound transcription ~config flip (`tools.media.audio` STT); **outbound voice replies need a TTS** (OpenAI TTS `voice.tts.provider:"openai"` or ElevenLabs). Do after the phone-call mode works.

## Still pending from earlier today
- Prod GSTS backup pipeline fix (data frozen Thu 6/4). Scheduled one-shot check **`play-freshness-check-tonight`** fires 03:30 UTC Tue (8:30pm PT Mon) → will ping Skipper if still stale. Email delivery now fixed (gilligan.gsts@gmail.com → both inboxes).
