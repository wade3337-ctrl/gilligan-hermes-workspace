# 2026-06-18 — daily log
*Condensed Jun 18 2026 (full raw in `_backups/`). Durable detail now lives in the artifact files this points to.*

## V1.5 Roadmap built (AM)
- Master plan `arbor-stack/TRIMIT-1.5-ROADMAP.md` + live "▶ V1.5 Roadmap" tab on `Reference-RepairsAndScheme.cfm`. Grounded in the system-wide Menu Audit (119 links) + our logic audit + strategy.
- **Cost model:** Jordan Kim = IT mgr (salaried, $0 marginal) → menu/AppForms/deploys; Travis/Data Processing = $75/hr, deep DB/security only → whole V1.5 ≈ $1.5–2.5K. (Now in `contracts/dev-handoff-contract.md`.)
- **Priority = SALES first.** Skipper reframed: the flagship sales issue isn't dashboards (surface) — it's **re-engineering the bid/"traveler" creation workflow** (too many people, duplicate entry, admin↔sales back-and-forth). Reference product to copy = **ArborNote**. Sales funnel pages (Pipeline/Command/Overview) likely consolidate into one rep/manager one-stop-shop; menu vision Find→SALES (Sales+Marketing), go-ahead→Production. Design-first talks.

## TREE-6138 WO cleanup — PASSED on prod
- Re-ran the 255-WO reconciliation read-only: Revised=0, Complete-undated=0, Inactive=216, matched=255; proof table `dbo.zBackup_WO_Cleanup_20260613` present; trigger re-enabled. Closed out (REPAIR 2 / Contract Dashboard landed Jun 17).

## Workspace 5-layer restructure — DONE
- Reorganized to the 5-layer model (Identity / Routing / Contracts / Reference / Artifacts). `ROUTING.md` = front door; `contracts/` created; `reference/` (old `files/`) + INDEX. Recorded in MEMORY HOW-WE-WORK.

## Stale-proposal cleanup — scoping + schema landmine
- Approved. Decisions: sent-but-dead → existing **Lost (StatusDefID 141)**; never-sent drafts → new "Abandoned - Draft". Thresholds 180d sent / 365d draft; reversible via `dbo.ProposalArchiveLog`.
- 🚨 **Schema:** proposal **StatusDefID 149 "Locked" = WON**; `Proposals.LastGoAheadDate` UNRELIABLE → only trustworthy won-test = **EXISTS dbo.GoAheads (g.ProposalID=p.ProposalID)**. (Now metric standard #3.)
- Corrected counts (brief's ~57K was bogus): Pop A sent-dead→Lost = **852**; Pop B never-sent drafts→Abandoned = **7,716**; total sweep ≈ 8,568. Real payoff = clearing abandoned-draft pipeline clutter. NEXT: build `dbo.ArchiveStaleProposals` on Skipper's go. (`dev-tasks/stale-proposal-cleanup.md`.)

## 📌 STEVE'S PROJECTS (Steve = CFO) — `arbor-stack/steves-projects/` (+ INDEX)
- **Project A — Dashboard reconciliation vs CFO Financial Report** (`financial-report-reconciliation/`). CFO's `FinancialReportDashboard.cfm` = ground truth: revenue = SUM(Invoices.Total), status IN (InProcess,Pending,Open,Paid,Locked), anchored to accounting PERIOD. 2026 YTD ≈ $9.73M.
  - **RECON-01 — `Exec$Periods$Overview.cfm` = 🔴 CONFIRMED WRONG:** prefers stored `Periods.TotalPrice` (a broken "period close" snapshot) over live invoices. April "closed" mid-month → $1.224M vs real $2.179M (−$955,745); Mar/May never closed. Blast radius mapped (`BLAST-RADIUS-PeriodClose.md`): Tier1 exec dashboards, Tier2 **Exec$Commissions (pay!)**, Tier3 client-facing, year/contract rollups suspect. **RESUME:** Tier 1 first, verify read-pattern then fix to compute live, backup-first, UI we own → test play → IT deploys. NOT STARTED (pinned).
  - **RECON-02 — `Exec$PercentagePerformed2.cfm` / proc `Report$PercentagePerformed_npr2`:** municipal accrual wrong — one percentage-of-completion formula applied to both commercial (valid) and municipal (already billed progressively) → phantom May 2026 $111,204 accounting strips out. **BLOCKED on Steve's rule.** Steve replied: billing cycle is **PER-CITY** (most Upon Completion; Irvine/Newport/Stanton monthly) → fix reframed = billing-cycle-aware accrual driven by a per-city field TRIM IT lacks (schema/UI + logic, long-term). Split: **Brent** confirms per-city cycles; **Steve** confirms accrual treatment. HOLD build until both reply. (Drafts in folder.)

## 📌 Bid-process re-engineering — SOPs + AS-IS map
- 14 SOPs received → `arbor-stack/bid-process-reengineering/` (`sops/`, `AS-IS-WORKFLOW-MAP.md` = end-to-end current state, cast/owners + 7 pain points). Commercial vs municipal = separate tracks; month-end accruals (Dimitry) = owner of the RECON-02 report.
- **Skipper answers:** pricing = judgment (Sales Arborist + history + Price Buddy assist, don't auto-price); lead intake = phone/email (no portal). **GAP before future-state design = ArborNote proposal-flow walkthrough (Jun 19 w/ Skipper).** RESUME: design future-state (enter once, in-system attachments, handoffs→status changes) when ArborNote reference arrives.

## 📐 Dashboard Metric Standards — origin
- New governing doc `arbor-stack/DASHBOARD-METRIC-STANDARDS.md` (6 standards), distilled from SPM board tuning, applies to ALL dashboards/monitors. SPM board (Pipeline/Drill/Sold/Production) conformed on play Jun 19. (Standards now summarized in MEMORY → WORK; propagation tracked Jun 19.)

## 🔑 Hermes laptop gateway-owner watchdog — SSH setup
- Laptop Herman (desktop-4v2p8at @ 100.100.182.83) runs a watchdog that reads a gateway-owner file on Gilligan to decide which device runs the Hermes gateway. Added a dedicated **forced-command + from-IP-locked** key to `~/.ssh/authorized_keys` (read-only that one file, laptop IP only). Their failure root cause = watchdog ran as SYSTEM (read SYSTEM-profile `.ssh`) → fix = self-contained ssh with explicit `-i`/host paths. **Jun 19 update:** repointed the forced command to **`~/herman-store/gateway-owner.txt`** = the canonical owner file (same one Arduino's rrsync key reads via its `/home/wade3337/herman-store` root). Old `/home/wade3337/gateway-owner.txt` = orphaned. (See Jun 19 log for the switchover-test diagnosis.)
