# 2026-06-21 — Daily Log

## TRIM IT documentation hunt → there is none
- Skipper asked (good question): did SOPs / any files tell us what each data point *means*? Answer recovered by audit:
  **TRIM IT has NO data dictionary anywhere.** DB extended properties (MS_Description) = 0 (all 125 are SSMS
  diagram junk); no Help/Glossary/Dictionary/FieldDef tables; proc comments = commented-out dead code + dated
  "took it out" patch notes, not intent; webroot has no docs. The `D:\trimit-analysis\discovery` folder on the
  play server = **our own** prior discovery work (we have it locally in `arbor-stack/Arbor AI/Trim IT Repairs/`).
- Key distinction taught: we have the **structure** (verified); **meaning/intent was never recorded by anyone**
  and must be *recovered*. Jordan/devs don't know it either — they reverse-engineer blind.

## Population fingerprinting (Skipper's idea) — built + ran + emailed
- Skipper's idea: infer a field's MEANING by matching how often it's populated to known funnel counts, confirm
  with co-population. **It works.** Report: `arbor-core/reference-maps/sales-field-meaning-inference.md`.
  Emailed to jwade@gstsinc.com (msgid ef5ce9c6…) with attachment.
- Funnel anchors (live): proposals 265,059 created · 265,055 drafted · **75,860 sent (28.6%)** · **43,637 won (16.5%)**.
- Confirmed: `ProposalSentDate`+`SentCalendarID` = the SENT event (same 75,860); `LastGoAheadDate` = WON; won⊂sent
  (99.7%); `ReviewedByID`+`ReviewedDate` = a review sub-stage (~35k); `RFPID`↔`ProposalDueDate` (due dates ride
  with RFP-origin). **⭐ Only 31.7% of proposals come from an RFP — ~68% are direct/relationship (build BOTH paths).**
- Self-correction win: `ContractID` (43,231) and `FieldRepID` (42,542) count-matched WON but co-population killed
  them (overlap only 19,246 / 16,799) → **NOT** won markers. Lesson: a matching count is a lead, never a verdict.
- `GoAheads`: ~2.1 per won proposal (multi-year/seasonal release); 4 dead columns (WorkRequestID, ColorDefID,
  ProjectPeriodSummaryID, TotalFollowups = 100% NULL). `RFPs`: 1.68M rows but **76.9% are child line-items**
  (ParentRFPID) — a header(~5%)+scope hierarchy, not a flat bid list.
- **3-lab triangulated verification** (Skipper directive: all subs are MAX plans, lean on them): Claude produced;
  **Codex gpt-5.5 + GLM glm-5.2** each independently re-ran the queries → all 5 headline claims CONFIRMED, numbers
  matched exactly. CREW.md quota line updated to "all three are max/flat-rate, don't ration calls."
- New reusable tool: `arbor-core/reference-maps/_tools/profile-cols.sh` (column population profiler, read-only).

## Next
- Schema Architect run can now build on recovered funnel meaning (lifecycle: draft→sent→reviewed→won→released).
- Residue (fields with no funnel signature) → still needs SOP/admin confirmation.

## Schema Architect — sales-engine schema designed + gate-cleared (v1), then v1.1 from Skipper residue walkthrough
- Lead/retention design locked v1 (`arbor-core/design/sales-lead-retention-design.md`): customer-as-spine,
  4 segments, Slipping/win-back, 2 rep roles (Marketing rep / Sales Arborist), universal-RFP handoff, outcome capture.
- **Proved TRIM IT's "node-copy" flaw to the Skipper** (his architecture question): `GenerateGoAhead` copies into a
  new node w/ own lifecycle; **806/1511 customers (53%) have drifted BillToAddress copies**, 483 (32%) drifted
  phones. → **Design Law #1 "reference, don't copy"** is a hard gate (Judge E). Schema fixes it (zero node-copy).
- **Schema v1** (`arbor-core/schema/sales-schema-v1.md`): 18 entities, all on verified SOURCE tables. **Cleared the
  gate** — Codex (grounding + source-not-copy) PASS, fresh Claude reviewer (reference-not-copy + complete + no-guess)
  PASS. **GLM crashed (exit 137, OOM) both tries** — verified by 2 reviewers/2 engines this run; get GLM back next.
- **All 6 residues resolved w/ Skipper (2026-06-21)** → `arbor-core/design/residue-resolutions-2026-06-21.md`:
  - R1: **Municipal Other = DIR-regulated non-city**; DIR = customer-level flag + per-job override; pull `Companies.DIR*` cols back in.
  - R3: **segment cadence DROPPED** (data irrational — go-ahead release-bursts = 2–15 day medians). Replaced w/
    **recurrence_type {one_time|recurring|contract}** set at deal time; one-timers never Slip; recurring=interval;
    contract=renewal-at-risk. Add agreement/recurrence concept.
  - R4: overrides = **inventory QC person** correcting bid errors → model a QC step; **inventory/pricing/QC = known
    swamp, flagged for its own future design lane.**
  - R5: created_by → user/login, not rep. R6: **two-tier lost-reason tree** (outcome: competitor/cancelled/
    no_response/other → why sub-reasons; price is a sub, not top-level).
  - NEW **bid follow-up clock**: sent → 7-day sales follow-up → 60-day rebid/auto-lost.
- v1.1 producer running (folds all resolutions) → re-judge changed parts. Build is still a FUTURE decision (design phase only).
- v1.1 verified: Codex (grounding/law/source) + GLM (law/complete/no-guess) both PASS. **R7 resolved:** `dbo.Contracts`
  = SOURCE for Standard(type1) contracts (stub-create + user-authored; write-path + GLM); System(type2)=DERIVED, excluded.

## GLM CLI fix (money's-worth) + build planning started
- **GLM `claude -p` CLI is UNSTABLE here** (SIGKILL/137 on agentic runs); the box is HEALTHY (12GB free). Fix =
  **direct z.ai API** via `~/arbor-core/crew/glm-ask.py` (Bearer, single-shot, reliable). CREW.md + MEMORY.md updated.
  Codex CLI also started dying (137) on heavy nested runs — sandbox kills fat subprocesses; lightweight (gsql, GLM-API) fine.
- **Build planning** → `arbor-core/build/BUILD-PLAN.md` (foundation-first, agreed). DECIDED: A1 architecture
  (modular monolith + async workers + ONE enforced "brain" for humans & agents; **agent-first UI from v1**; a
  **knowledge layer** — agents pull from clean DATA + curated KNOWLEDGE distilled from TRIM IT; **TRIM IT dual role**
  = data archive + live R&D/idea incubator). A2 data: **LAZY/on-demand migration** (arbor-core owns clean data;
  pull+clean history only as needed; junk left in TRIM IT; new data born clean; one cutover/domain, no dual-write).
- **FOUNDATION A1–A5 LOCKED (2026-06-21):** A3 = **earned/graduated agent-autonomy DIAL** (per-agent, performance-based;
  new agents supervised T2, autonomy earned→T1, revocable, director holds dial; needs an agent performance/track-record
  layer) + **composable roles** (stack, not silos) + **🔑 governance law "no one holds the only keys"** (≥2 holders on
  every critical capability; CEO/COO/CFO **emergency solo break-glass** w/ heavy audit; **knowledge in the SYSTEM not
  heads = anti-TRIM-IT**). A4 = proven-auth (not home-grown) + secrets manager + encryption; **HOSTING = OWNED/controlled
  for v1 → cloud later when growth requires, built cloud-PORTABLE (Docker) = lift-and-shift not a rewrite.**
  A5 = dev→arbor-play→arbor-prod, Docker, CI, backups, private repo, crew-verified.
- **▶ v1 sales+production INTERFACE = evolve the Field App Beta** (`FieldApp/`, Google-Maps GPS + `/API/` JSON) into a
  **one-stop pricing + call-in collection tool** → Track-1 incubator → distill into the clean arbor-core framework.
  (Field apps mapped: `Yellow/`=prod bulk, `FieldApp/`=beta GPS-serialized; detail in `pricing-asis.md`.)
- **Part B STARTED:** slicing = thin **vertical** slices (use+learn early, the incubator way). **First slice =
  PRICING/SALES — "Price a Job"** (Skipper chose pricing-first over call-in collection): customer spine (lazy-pull) +
  GPS work capture (field-app beta already does it) + **price via TRIM-IT-pull at first** (strangler-fig) + clean bid
  → pipeline; the clean configurable pricing engine swapped in **iteratively underneath**. Call-in collection = 2nd slice.
- ⏭️ **RESUME TOMORROW (2026-06-22):** confirm/detail the **"Price a Job" first-slice scope** (anything to add/trim for
  v0.1), then sequence the rest of Part B (→ B2 strangler-fig cutover). **Build = still a FUTURE decision** (design phase).
  Everything captured in `arbor-core/build/BUILD-PLAN.md` (foundation A1–A5 locked + Part B started).

## Pricing/inventory as-is map (while Skipper at breakfast)
- Built `arbor-core/reference-maps/pricing-asis.md` — DB-verified as-is map of the TRIM IT pricing swamp (future
  inventory/pricing design lane, R4). **InventoryDetail (1.79M) = the TREE inventory + per-tree price**
  (size/DBH/condition/loc + OrigBase→Base→Price + Prior01-03/CurrentYear multi-year). InventoryClasses=13 tree types.
  **PricingModels (232k, 9802 proposals, has ProposalID) = the populated pricing table; PricingGroups/Rates/Sizes =
  EMPTY (0 rows) clean size-band engine.** ⚠️ ~16 per-city custom SizeUOMs (Irvine/Long Beach/Stanton/OCWD/MTA…) +
  junk ("DO NOT USE"/"DELETE"/"FUTURE CITY"); hardcoded city pricing procs (`CURSOR$UpdateIrvinePricing$*`); `FakeBasePrice`.
- **GLM (direct API) reviewed → caught 2 over-reaches** (PricingModels "copied/drifts" + PricingGroups "abandoned" =
  inferences not proven) → corrected to evidence-only; added OQ5–OQ9 (96% gap, Base→Price math, app-layer rules,
  Pareto on SizeUOMs, 7M-log churn, read an Irvine proc for the blueprint). First-draft; full crew gate before it feeds design.
