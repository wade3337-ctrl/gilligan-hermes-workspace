# 2026-06-19 — daily log

## Metric-standards propagation pass (the morning's first job)
- **COO daily email — ✅ FIXED #5** (ship-log #65). Verified empirically the `tph_day` board sometimes carries zero-crew-hour billed work (a `[TREATMENT]` inflated a day 121→119). `monitor.js` now computes company TPH from crew jobs only (`hours>0`) + adds a 🌿 Treatments line. Live next 6:30am, no deploy.
- **Salesperson monitor — ✅ N/A** (sums dollars, no Σ$/Σhours ratio). **Customer Leads + Cluster — ✅ N/A** (invoiced totals / lead buckets, no close-rate/TPH/go-ahead logic).
- **Exec close-% — ✅ ByRep/ByMarket/Detail REWRITTEN to cohort** (ship-log #66, #68). Was the textbook violation (go-aheads-in-window ÷ written-in-window → >100%, masked by a toggle). Now cohort: written-in-window proposals that EXIST a won GoAhead (Locked=won, never LastGoAheadDate), ≤100%, +`StatusDefID=188`. Detail drill numerator reconciles (rep 1143 Won34⊆Written79). Deployed + render-verified on play.
  - 🟡 DB-proc level flagged not patched: `Perf$SalesReps$ClosingPercentage` reads precomputed `SalesRep*Performance` rollups → fix belongs in those procs (DB task).
- **IsMeasured — ✅ PROD dev handoff written** (`dev-tasks/ismeasured-managers-fix-PROD.md`): 4 managers (1108/1136/1142/1145) → IsMeasured=0, backup+rollback, no commission impact.
- **⚠️ My error, corrected:** I claimed "deploy channel isn't active." FALSE — it's direct SSH/scp over Tailscale to `gstsdatabase` @ 100.86.97.46 (live since Jun 13). Missed it because that note was in the truncated half of MEMORY.md. **Lesson → drove today's MEMORY.md slim-down (see below).**

## Emails — phone-friendly + trustworthy-data (ship-log #69) ✅ Skipper-verified
- Reformatted COO (`monitor.js`) + salesperson/manager (`salesperson-monitor.js`) + `revenue-block.js` for PHONES (no box rules/indents/column padding; ` · `, emoji-led, short labels). Skipper verified on his phone.
- **ScheduledTPH removed from rep/manager emails** (untrusted bid data); **COO measures vs `TPH_TARGET`=130** (adjustable), scheduled shown as context. Manager (Nate) rollup DOES show scheduled (`opts.rollup`→showSched). Two MEMORY rules saved (phone-friendly; only-trustworthy-data).
- **⚠️ Correction:** salesperson + Nate emails are ALREADY LIVE since ~Jun 11 (`run-salesperson-live.sh` 6:35am, `run-nate-rollup.sh` 6:30am) — NOT preview-only; `liveEnabled:false` only gates an unused old wrapper. So today's changes take effect at the next send automatically.

## Municipal close rate — not a real metric (finding)
- A city under contract requesting work = INTAKE, not winning business. Investigated `dbo.Contracts` (StatusDefID Approved/Active=won, Not Approved=lost, OnBidList flag; links via ProjectID). **Killer: municipal LOST bids aren't recorded** (1 Not-Approved vs 25 Approved) → win-rate uncomputable.
- **DECISION (Skipper, opt B):** Close% by Market made **COMMERCIAL-ONLY** (`NOT EXISTS ProjectGroups pg ProjectGroupDefID=11` on all 4 cohort subqueries). New **"Municipal / Cities" tab** explains why + shows live active-contract count (25) + button to City Budgets (All view). **City Budgets landing now defaults to All-Cities view.** All live + verified.
- Lessons: `production-dashboard/` = canonical mirror (verify md5==live before editing; I edited a stale `closepct-family/` copy by mistake). CFML gotcha: HTML numeric entities inside `<cfoutput>` break (`#`=expr delimiter) → keep entity HTML outside cfoutput or escape `##`.

## Standing rules set today
- **⛔ NO piecemeal prod dashboard deploys** — prod's Exec dashboard is FAR behind play → it ships as the WHOLE dashboard only after Skipper's full review. Staged handoffs HELD in `REVIEW-PILE.md`.
- Welcome modal w/ colored emoji required on every dashboard front page; NO permanent technical text on pages (→ "?" protip popup); .cfm w/ emoji needs UTF-8 BOM.

## Hermes laptop-takeover switchover test (PM)
- Skipper tested auto-switchover (set canonical `herman-store/gateway-owner.txt` laptop→arduino via `printf`). **Laptop never came online.** Diagnosed from Gilligan: owner file + plumbing correct, but **the laptop's poll loop was DEAD (last poll 19:05 UTC, ~2h before the test)** → it never saw the flip; Arduino (alive, polling) correctly stepped down → Herman went dark. Root cause = laptop side asleep/loop exited.
- Briefly mis-diagnosed a "split-brain" (two owner files: `~/gateway-owner.txt`=laptop stale vs `~/herman-store/gateway-owner.txt`=arduino). Herman verified live and it's NOT split-brain: **Arduino's rrsync key root = `/home/wade3337/herman-store`, so its `gilligan:gateway-owner.txt` resolves to `herman-store/gateway-owner.txt`** — both machines read the SAME file. The home-root `~/gateway-owner.txt` is orphaned/unused (delete later). No files I changed caused it. Owner currently = arduino; Arduino gateway active.

## File-structure efficiency work (PM, Skipper-driven)
- **`live-in-prod/` folder created** — new lifecycle stage for pages shipped to prod + collecting feedback (LP-01 Customer Leads, LP-02 Clusters, LP-03 City Sales Budget). One feedback-log file each. Intake = email (I watch inbox) or verbal (Skipper relays). Wired into ROUTING.
- **`arbor-stack/INDEX.md` created** — full table of contents for the Artifacts root (was missing its own map). **ROUTING.md gained a "By topic" lens** (everything on TRIM IT / Arbor AI / GSTS processes). Reference + artifacts indexes cross-linked.
- **MEMORY.md slimmed 45,262 → 11,130 bytes (−75%)** — it was 44KB vs a ~20KB bootstrap cap → **55% was silently dropped every session** (caused the deploy-channel error above). Long-form standing rules moved to pointers into `contracts/` (which hold them in full); every durable fact preserved compressed. Now loads 100%. Backup `memory/_backups/MEMORY.md.20260619-221414.bak`.
- **Daily-memory cleanup** — this file's source (`2026-06-18.md`) had ballooned to 30KB spanning two days (too big to load at startup). Split into per-day files (Jun 18 condensed + this Jun 19 file); redundant auto-gen `-HHMM` session-dump fragments archived to `_backups/`. Going forward: one curated file per day.

## Open / resume
- Skipper's **full Executive-dashboard review** gates all prod deploys (defines the single full-deploy scope).
- Steve's projects (RECON-01 not started; RECON-02 held on Brent+Steve replies); bid-process future-state design awaits ArborNote walkthrough; stale-proposal proc build on Skipper's go.
- Open list snapshot: SPM board, Sales Pipeline (held), City Budgets, Customer Leads, Cluster, Price Buddy; DB: IsMeasured prod, Contract Dashboard LB FY26/27 (verify after IT), Perf rollup procs, stale-proposal archiving, proposals-drill index.
