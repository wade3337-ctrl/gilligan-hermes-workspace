# 2026-06-25 (evening) — Arborist Workbench build sprint + V1.5 login polish

Big session, all Track-1 (TRIM IT play). Ship-log #81–#89.

## Arborist Workbench (Garrett's tool) — the main thrust
- **#81** propagated the GoalSettings crash-guard from SiteMap (#78) to the child `ZTest-Drill.cfm` — same unguarded query. Drove a NEW base rule: *propagate every fix to linked/sibling pages, same session* (added to `contracts/repair-contract.md` + MEMORY). Swept the whole web root for `GoalSettings` → all 5 references now safe.
- **#82 NEW page `ZTest-MyJobs.cfm`** (Garrett's wishlist): his daily salesperson email as an interactive, date-adjustable web view. 5 lifecycle tabs (Upcoming/Starting/In Progress/Done/Re-bid Radar), scoped to one arborist. **"Done" = Skipper's def: WO Complete (StatusDefID 48) + a non-void Invoice exists.** Verified defs live; invoice STATUS is dead data (Paid stops 2014) → show "invoiced" not "paid".
- **#83 Re-bid Radar depth** (chose build-order B = full scaffold first): manager + tap-to-dial phone (99%/95% coverage), sort, aging, and the headline — an **RFP status flag that PERSISTS and drops handled jobs off**. Renewal-gap = Project-level (≥1 Done WO, 0 open WOs). Garrett's real pool: 91 open, 326 done-nothing-booked (49 hot/6mo, 104 warm/18mo, ~80 at the 12mo default).
- **#85 Drill notes now SAVE** — closes the weeks-old "notes don't save" thread. Relationship + per-site notes → `Workbench.dbo.WorkbenchNote`.
- **#87 Lasso dot-click fix** — real root cause (the #77 SVG `pointer-events` hack was a no-op on the CANVAS renderer): the interactive polygon swallowed clicks → fix = `interactive:false`. Skipper confirmed working.
- **#88 Welcome modal + Pro Tips** wired in (were never there). 10 `wb.*` keys merged into shared `protips.content.json`.

## 🗄️ KEY INFRA: the `Workbench` play database (Skipper chose option A)
The unblock for "notes don't save": a SEPARATE db on the play SQL instance that **survives the nightly GSTS refresh** (refresh restores only GSTS). Tables: `RebidStatus`, `WorkbenchNote`, `Todo`. Reached from CF via the GSTS datasource + 3-part names (CF connects as `sa` → cross-DB, no new datasource/grant). Endpoints: `ZTest-MyJobs-Save`, `ZTest-Note-Save`, `ZTest-Todo`. **Skipper: this is the seed of the eventual clean DB restructure.** Technique → PLAYBOOK.

## V1.5 Home
- **#84** linked "My Jobs / Re-bid Radar" on the SALES node (rep-scoped to the logged-in arborist).
- **#86** "Get it done today" is now a functional per-user todo (`Workbench.dbo.Todo`).

## V1.5 Login concept (`ZTest-V15Login.html`, play-only)
- **#89** repointed login from stale static mockups → the live `Dashboard-V15Home.cfm`. Added no-cache meta (Chrome was serving a stale cached copy — that was the "links to first test page" confusion; also he was using `.htm` not `.html`).
- Footer polish: tagline "Preserving trees for future generations" (replaced "greatest tree company there is"), moved it up under the 50th logo (big/bold/italic/green, centered), dates fixed **1976–2026** (50 yrs, founded 1976).

## Process notes / lessons
- Let the **live `Reference-RepairsAndScheme.cfm`** repairRows drift to #75 (markdown ship-log was current) — Skipper caught it. Caught up #76–#89 (now 84 repairs) via byte-level inserts (ASCII-only, the file is UTF-8). LESSON: don't batch that sync.
- `#` literal in ANY double-quoted CFML string (not just cfoutput) = interpolation error — bit me twice (inline style, then a cfset array). Escape `##`.
- Leaflet canvas renderer: DOM pointer-events tricks are no-ops; use `interactive:false`.

## Created RC-05 (Arborist Workbench Suite) — flags the big new prod dependency: this is the first feature that needs a DATABASE in prod, not just .cfm.

## NEXT: Skipper wants a NEW chat about the BLACK project (arbor-core, Track-2, confidential).
