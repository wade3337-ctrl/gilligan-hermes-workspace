# 2026-06-07

## Monitor data-freshness OUTAGE — root cause traced to PROD backup pipeline
- **Symptom:** Sun 6/7 morning email still showed Thursday 6/4 actuals. Endpoint returns 0 jobs for 6/5, 6/6, 6/7.
- **Refresh architecture (learned via 2 read-only Codex diagnostics):** PROD backs up GSTS ~3:00am → uploads `.bak` to **rsync.net** (offsite) → PLAY scheduled task **`\GSTS DB RESTORE`** (WinSCP) downloads newest `.bak` from rsync.net ~3:00am → restores it. Play DB recovery model SIMPLE; restores recorded in play msdb.restorehistory.
- **Diagnosis:** PLAY side is 100% healthy — download task + restore both SUCCEEDED Jun 6 & 7, D: has ~140GB free, no backup errors. Play keeps restoring the **same June 5 backup** (`GSTS_backup_2026_06_05_030002_0846614.bak`, 120GB) because that's the newest file on rsync.net. The June 5 backup only contains reported actuals through **Thu June 4** (actuals are entered each evening, so a 3am backup has the prior day fully reported).
- **ROOT CAUSE: nothing newer than the June 5 03:00 backup has landed on rsync.net.** Break is on the **PROD → rsync.net leg** — either prod stopped CREATING GSTS backups after 6/5, or it's creating them but the UPLOAD to rsync.net stopped. (Also: June 2 backup missing from the chain → job has been flaky before.)
- **BLOCKER:** prod SQL not reachable from play (`www` = 198.207.148.169; TCP 1433/14333/443 all refused/firewalled). Codex can't query prod msdb from the play box. **Decisive check must run ON the production host.** Wrote prod-side diagnostic → `anomaly-monitor/CODEX_DIAG_prod_backup.md` (prod msdb latest backup, backup job history, local .bak + disk, the rsync upload task's last run/log, error log). If prod msdb also stops 6/5 = backup job dead; if prod has 6/6-6/7 = upload leg broke.
- **⚠️ COO-LEVEL RISK flagged to Skipper:** production GSTS has had **no successful offsite backup since June 5** (~2.5 days). Real data-protection gap independent of the monitor — if prod failed now, everything since 6/5 is lost. Needs prod-server owner to fix urgently.
- **STRATEGIC CONVERGENCE:** BOTH the backup fix AND the monitor's durable fix (deploy `MonitorData.ReadOnly.cfm` to production www so monitor reads LIVE data, bypassing the whole backup→rsync→restore chain) require **production server access**. Open question to Skipper: who administers prod / can we loop in the dev who built the rsync backup. Monitor will keep reporting freshest-available day w/ stale flag until then.
- Phase-2 (per-salesperson emails) probe prompt finalized (CalDate confirmed) but NOT yet run — paused by this freshness investigation.
