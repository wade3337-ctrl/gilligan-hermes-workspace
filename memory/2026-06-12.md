# 2026-06-12

## Two new TrimIT projects kicked off (Skipper, Jun 12)

### 1. "Completed & Sold" report → configurable dashboard (PAUSED w/ checkpoint)
- Reproduce contracts-admin Brent's weekly Completed-&-Sold report (YTD work completed + sold, vs last year) as a server dashboard, without Brent. Brent hand-pulled TrimIT→spreadsheet.
- **Commercial engine SOLVED & validated to the dollar** (Codex hunt): WorkOrder-centered; SOLD=Status 'Active'+no DateCompleted by EndDate, COMPLETED=Status 'Complete' by DateCompleted; Commercial = NOT in ProjectGroups group 11 (municipal=11; commercial includes ungrouped+12+14, so IN(12,14) undercounts); Rep=Projects.SalesRepID→SalesReps. June-2026 sold reconciled exactly to $956,466.
- **OPEN:** validate Completed side + crack MUNICIPAL (repeating values 653,406 / 787,003.92 = CompanyYears contract value allocated monthly, NOT job agg) — follow-up Codex prompt READY but not run (paused to focus on dashboard).
- **Full checkpoint + artifacts → `arbor-stack/completed-sold/CHECKPOINT.md`** (source xlsx, Codex findings, follow-up prompt all in that folder).

### 2. Contract Dashboard production fix — risk review (ACTIVE)
- Skipper showed me Codex's dev instructions to fix the prod Contract Dashboard (code + DB: change procs GenerateContractPeriod / GenerateCompanyPeriods$YearID to use CompanyYearID; relink materialized data; restrict reset endpoint). Asked me "will this break anything else."
- Ran a read-only Codex IMPACT ANALYSIS. Verdict: **conditional NO-GO** until SQL-side checks run (Codex's SQL connection failed on PowerShell Windows auth "SSPI context" — fix = use the GSTS datasource login like hunt #1 did).
- **Confirmed collateral risks:** 3 legacy CFM pages read CompanyPeriods WITHOUT CompanyYearID (Contracts-Company-Overview-Content-Periods.cfm, Companies-Company-Overview-Content-Periods.cfm, ...-Slim.cfm) → will show duplicate/merged data for overlapping-calendar companies (Anaheim E/W) after the fix. Remediation WRITES Invoices.ProjectYearLabel (check invoice/label reports). GetPeriodAccrual may read CompanyPeriods → could move OUR monitor's revenue.
- **Relief:** WorkOrderCalendars / ContractCalendars / Projects NOT directly written by remediation. Reset endpoint has confirm-token + auth exclusion.
- **MUST clear before @Commit=1:** SQL Agent jobs (overnight re-corruption risk), CompanyPeriods unique constraints on CompanyID+PeriodID (fix could fail), proc callers, GetPeriodAccrual body, ProjectGroupDef labels, + fix the 3 legacy pages. **Re-run SQL-checks prompt → `arbor-stack/contract-dashboard-fix/Codex-SQL-Checks-Rerun.md`.** Full analysis saved in that folder.
- **Nightly refresh insight (confirmed w/ Skipper):** PLAY = nightly full copy of PRODUCTION → play-only fixes revert every midnight; fix MUST land on production + fix the PROCS (not just data) or prod regeneration re-breaks it. Our future direct-access SANDBOX must be OFF this refresh cycle or our work gets wiped too. (Deferred deeper nightly-refresh discussion per Skipper.)

## Recurring monitoring live (from Jun 11): bounce-watch + rep-reply-forward every 2h. Salesperson emails live to reps 6:35am; COO daily 6:30am (cc jkim/jroulson/sgriffiths); Nate rollup 6:30am. Reports go to jwade@gstsinc.com only.
