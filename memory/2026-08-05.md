# 2026-08-05 (daily log)

## Channel: moved Discord → Telegram (Discord API outage)
The Skipper took me off Discord mid-project because of a Discord API outage; we're on Telegram now. Same session context, just a new surface.

## TRIM IT Sales Pipeline page — PAUSED/pinned (full detail already in [[trimit-sales-pipeline-page]])
Continued yesterday's build. Municipal reconciled to Nate's report this session:
- Walked the Skipper through the 4-revision municipal saga; he chose **(i) sales basis + B (contract-book forward)**.
- FINAL municipal panel (live on play): **invoiced calendar-YTD $4.67M** (earned) · **contract book forward $3.23M** ($7.90M annual book − $4.67M) · annual book $7.90M ties to Brent.
- Nate's municipal (his weekly `Sales_Report_2026_Completed+Scheduled.xlsx`, sheet `07.29.2026`): Completed YTD $4.20M · **Sold $5.06M (MANUAL spread — repeating $876,082/mo, NOT live-reproducible)** · Total $9.26M. Live WO-based sold municipal = only ~$1.9M (call-ins not cut as WOs yet). ~$1.36M gap = Nate's manual forward > entered contract book (PO-pending).
- Pipeline $9.17M contents pulled live: HOA $4.64M · Commercial $2.00M · **Unassigned $1.49M (NULL MarketID, mostly HOA property mgrs — none municipal)** · Retail $0.43M · Other $0.32M · Muni $0.30M. Top bids: Action Property Mgmt $954,575 (proj 1096695/prop 799879/LegacyRef **426155** = Mission Viejo Env Assoc 3-Yr) · Keystone Pacific $895,302 (proj 1100843/prop 793338, **aging, sent 2/9**; its other 15 bids all <$54K) · Powerstone $208,634 (proj 1105432/prop 801789/LegacyRef 428061 = Northpark Sq Pine Removals).
- **View-link recipe:** `Profile.Proposal.Detail.cfm?ZProposalID=` (the bid) · `Profile.Project.Detail.cfm?ZProjectID=` (account). **Displayed proposal # = `Proposals.LegacyRef`**, not internal ProposalID.
- ⏸️ OPEN: coverage panel += muni forward $3.23M · reclassify $1.49M Unassigned · commit + `verify-build.sh`.
- ✅ Committed + pushed both repos; Kanban card #1057 added (alpha col, backup-first).

## 🖼️ Profile-picture-gone-on-dev + deploy-package update (ACTIVE, unfinished — resume here)
Jordan deployed our bug-fix/landing package to **DEV** (their process: deploy→dev→test→push→prod; he did NOT deploy to prod). Two problems surfaced:

**(1) Skipper's profile pic vanished on dev after the patch.**
- ❌ **I first mis-diagnosed it as "play-only, never on dev."** WRONG — the Skipper corrected me: **the pic WAS deliberately deployed to dev AND prod earlier.** Log the correction.
- Real diagnosis: dev's DB pointer for UserID 9 still correctly = `art/profilepictures/jwade001.jpg` (that's why the page requests it → **404**). The **image FILE** is what's missing from dev's `art/profilepictures/` — **Jordan's deploy overwrote/synced (mirror-deleted) that folder** (a plain copy wouldn't delete it). The CFM code is innocent.
- Source of truth found: **`~/.openclaw/workspace/trimit-profile-jwade/`** → `jwade001.jpg` (520×400 JPEG, house naming convention like sgriffiths002.jpg) + `INSTALL.md` (drop file + `UPDATE dbo.Profiles SET ProfilePicturePath=... WHERE UserID=9`).
- **Fix:** re-drop `jwade001.jpg` into dev's `art\profilepictures\` (pointer already set → pic returns). **Prevent recurrence:** exclude `art/profilepictures/` from Jordan's sync or the prod push wipes ALL staff pics (sgriffiths002, jgriffiths003, …).
- ⚠️ Note: play cron `reapply-play-profilepic.sh` repoints PLAY's DB every 30 min because play's nightly restore resets it toward prod's value.

**(2) The "2 sliders" = double scrollbar on the Daily-Close notepad iframe in `Profile$Main.HiRes.cfm`.**
- Last night I fixed it (2 scrollbars → 1, matching prod) but the fix is on **play (D:\) ONLY, not committed to the repo**, so the package Jordan deployed still has the 2-scrollbar version.
- ▶️ TODO: pull the **live play `Profile$Main.HiRes.cfm`** as source of truth, swap the single-scrollbar version into the deploy package (identify which package Jordan used — likely V15-DASHBOARDS-DEPLOY; diff to confirm), per the repair/handoff contract (files + what they fix + instructions only, no internal notes).

**Immediate next actions when resuming:** (a) offer/write Jordan a clean note = `jwade001.jpg` + 2-line install + "don't sync profilepictures" caveat; (b) rebuild the deploy package with the single-scrollbar `Profile$Main.HiRes.cfm`.

## Capability confirmed: dev browser login works
`~/.local/devscout/` scripts log into dev as `jwade` (headless Chromium), observe-only. Landing pages checked this session: `Dashboard-V15Home.cfm` (clean, 4 nodes render) + `Profile$Main.HiRes.cfm` (renders, 0 CF errors, only the profile-pic 404). Shots in `devscout-shots/`. Ref [[dev-browser-access]].
