# 2026-08-05 (daily log)

## Channel: moved Discord → Telegram (Discord API outage)
The Skipper took me off Discord mid-project because of a Discord API outage; we're on Telegram now. Same session context, just a new surface.

## TRIM IT Sales Pipeline page — PAUSED/pinned (full detail already in [[trimit-sales-pipeline-page]])
Continued yesterday's build. Municipal reconciled to Nate's report this session:
- Walked the Skipper through the 4-revision municipal saga; he chose **(i) sales basis + B (contract-book forward)**.
- FINAL municipal panel (live on play): **invoiced calendar-YTD $4.67M** (earned) · **contract book forward $3.23M** ($7.90M annual book − $4.67M) · annual book $7.90M ties to Brent.
- Nate's municipal (his weekly `Sales_Report_2026_Completed+Scheduled.xlsx`, sheet `07.29.2026`): Completed YTD $4.20M · **Sold $5.06M (MANUAL spread — repeating $876,082/mo, NOT live-reproducible)** · Total $9.26M. Live WO-based sold municipal = only ~$1.9M (call-ins not cut as WOs yet). ~$1.36M gap = Nate's manual forward > entered contract book (PO-pending).
- Pipeline $9.17M contents pulled live: HOA $4.64M · Commercial $2.00M · **Unassigned $1.49M (NULL MarketID, mostly HOA property mgrs — none municipal)** · Retail $0.43M · Other $0.32M · Muni $0.30M. Top bids: Action Property Mgmt $954,575 (proj 1096695/prop 799879/LegacyRef **426155** = Mission Viejo Env Assoc 3-Yr) · Keystone Pacific $895,302 (proj 1100843/prop 793338, **aging, sent 2/9**; its other 15 bids all <$54K) · Powerstone $208,634 (proj 1105432/prop 801789/LegacyRef 428061 = Northpark Sq Pine Removals).
- **View-link recipe:** `Profile.Proposal.Detail.cfm?ZProposalID=` (the bid) · `Profile.Project.Detail.cfm?ZProjectID=` (account). **Displayed proposal # = `Proposals.LegacyRef`**, not internal ProposalID.
- ⏸️ OPEN: coverage panel += muni forward $3.23M · reclassify $1.49M Unassigned · commit + `verify-build.sh`.
- ✅ Committed + pushed both repos; Kanban card #1057 added (alpha col, backup-first).

## 🖼️ Profile-picture-gone-on-dev + deploy-package update (ACTIVE, unfinished — resume here)
Jordan deployed our bug-fix/landing package to **DEV** (their process: deploy→dev→test→push→prod; he did NOT deploy to prod). Two problems surfaced:

**(1) Skipper's profile pic vanished on dev after the patch.**
- ❌ **I first mis-diagnosed it as "play-only, never on dev."** WRONG — the Skipper corrected me: **the pic WAS deliberately deployed to dev AND prod earlier.** Log the correction.
- Real diagnosis: dev's DB pointer for UserID 9 still correctly = `art/profilepictures/jwade001.jpg` (that's why the page requests it → **404**). The **image FILE** is what's missing from dev's `art/profilepictures/` — **Jordan's deploy overwrote/synced (mirror-deleted) that folder** (a plain copy wouldn't delete it). The CFM code is innocent.
- Source of truth found: **`~/.openclaw/workspace/trimit-profile-jwade/`** → `jwade001.jpg` (520×400 JPEG, house naming convention like sgriffiths002.jpg) + `INSTALL.md` (drop file + `UPDATE dbo.Profiles SET ProfilePicturePath=... WHERE UserID=9`).
- **Fix:** re-drop `jwade001.jpg` into dev's `art\profilepictures\` (pointer already set → pic returns). **Prevent recurrence:** exclude `art/profilepictures/` from Jordan's sync or the prod push wipes ALL staff pics (sgriffiths002, jgriffiths003, …).
- ⚠️ Note: play cron `reapply-play-profilepic.sh` repoints PLAY's DB every 30 min because play's nightly restore resets it toward prod's value.

**(2) The "2 sliders" = double scrollbar on the Daily-Close notepad iframe in `Profile$Main.HiRes.cfm`.**
- Last night I fixed it (2 scrollbars → 1, matching prod) but the fix is on **play (D:\) ONLY, not committed to the repo**, so the package Jordan deployed still has the 2-scrollbar version.
- ▶️ TODO: pull the **live play `Profile$Main.HiRes.cfm`** as source of truth, swap the single-scrollbar version into the deploy package (identify which package Jordan used — likely V15-DASHBOARDS-DEPLOY; diff to confirm), per the repair/handoff contract (files + what they fix + instructions only, no internal notes).

**Immediate next actions when resuming:** (a) offer/write Jordan a clean note = `jwade001.jpg` + 2-line install + "don't sync profilepictures" caveat; (b) rebuild the deploy package with the single-scrollbar `Profile$Main.HiRes.cfm`.

## Capability confirmed: dev browser login works
`~/.local/devscout/` scripts log into dev as `jwade` (headless Chromium), observe-only. Landing pages checked this session: `Dashboard-V15Home.cfm` (clean, 4 nodes render) + `Profile$Main.HiRes.cfm` (renders, 0 CF errors, only the profile-pic 404). Shots in `devscout-shots/`. Ref [[dev-browser-access]].

## 🛰️ OneStepGPS — fleet device cleanup + morning yard-departure REPORT (built this session)
Continued the GPS work → [[gps-telematics-integration]]. Two deliverables done:

### Device cleanup (23 fixes, all via API, all verified)
- Audited 100 OSG devices: 72 clean, **28 needed attention**. Fixed 23:
  - **11 cosmetic renames** (AB6/AB10/AB20/AB23/B47/B50/B40/C66/C69/C71/C74 → clean `X## - HW #`).
  - **C 68 Unit #175 → C68 - HW 175.**
  - **BA TRAN Unit #169 → S-49 - Ba Tran** — matched by pulling the VIN from OSG (`5TDKDRAH0PS519692`) → fleet master `2026_Fleet_Master_List.xlsx` asset **S49** (2023 Toyota Highlander XLE, plate 9JKK228). NOTE fleet is in the `~/.gilligan-hermes/home/arbor-stack` checkout, not canonical.
  - **5 "(New)" duplicate pairs** (C37/C39/C46/C47/C48): the base name was the DEAD tracker (`active_state=none`, last seen 2021–2024), the "(New)" was live. **Renamed dead → "(RETIRED)", live → clean name.** Reversible (not deleted); Skipper can request full delete later.
- **Left alone (Skipper):** D58 (two live trackers HW117+HW118, unresolved) · C1/C2/C3 (camera test units, only one live in the mechanics truck) — both excluded from the truck report.

### 🔑 NEW CAPABILITY — OSG authenticated session + safe write method
- **OSG login requires MFA** (6-digit email code). Built `~/.local/devscout/osg_mfa_capture.js` → logs in, checks "remember device", saves session to **`~/.local/devscout/osg-auth.json`** → **no MFA for 30 days**. (Gotchas: fresh login sends a NEW code each run — enter the code that matches THIS run; the MFA field is a single visible text input, not 6 boxes; never `pkill -f <scriptname>` from the same shell — it kills the launcher. Redact password from request-capture logs.)
- **Safe write = `PATCH /v3/api/public/device/{device_id}?vuid=6eqEBuimPtXNb-81f07-0F` body `{"display_name":"..."}`** — a PARTIAL update, verified after each that ONLY the name changed (`other-fields-changed: []`). Reusable for any fleet edit. Scripts: `osg_do_rename.js`, `osg_batch_rename.js`, `osg_retire.js`.
- ⚠️ API key still READ-ONLY posture for anything beyond these confirmed-safe display_name PATCHes; ask before broader writes/deletes.

### 📊 Morning yard-departure report — LIVE, both formats, Skipper-only
- Generator **`fleet-telematics/yard_report.py`** reads `yard-events.csv` → writes `/tmp/yard-digest.txt` (email body) + `yard-dashboard.html` (styled, grouped by yard, ⏰ late-flag after 07:30 PT tunable, cleans names + drops cameras/RETIRED).
- **Email:** `yard-send.js` (needs `NODE_PATH=.../anomaly-monitor/node_modules`) → **jwade only**, digest + HTML attached. Sample sent for 8/04 (55 out, first 03:38, last 08:17, 18 stayed, 3 late).
- **Dashboard:** served you-only on Tailscale **http://100.82.161.7:8093/** (`ensure-yard-server.sh` watchdog).
- **Cron (merged onto existing 54 → 56, nothing clobbered):** `45 15 * * *` yard-morning.sh (~8:45 AM PT: rollup→report→email→refresh) + `*/10` server watchdog. Pipeline: 20-min poller → nightly rollup → morning report.
- Caveat: only ~2–3 days data (directional); D58 still shows twice until resolved.

## 🚚 Stop Attribution module — BUILT + SHIPPED into the morning report (this session) → [[fleet-stop-attribution]]
Skipper's ask, walked through in small bites: **record which scheduled job each truck landed on + highlight guys stopping on the way to/from work** (recording, not judging). Full detail in the new note; the arc:
- **b-1 chosen** (match a stop to ANY scheduled job, record it — not judge right/wrong). Built `stop_attribution.py` (reuses `jobsite_dwell` stop-detection) + `fetch_jobs.sh` (**widened job source** = crew sheets ∪ WO span `StartDate≤day≤EndDate` → 37 sites vs 22) + `geocode_jobs.py` (Nominatim, cached) → **97% site coverage**.
- **4-state classify:** `at_job` · `on_the_way` (narrowed in report to commute-window = before first job / after last) · `in_shop` (repair, `shops.json`) · `parked` (≥8h/overnight take-home). **Match radius tuned 300→550m** (near-misses cluster tight at 475–590m).
- **Fleet key (Skipper):** D/B/C=crew · AB=trafficator · BA=crew · C1–3=cameras · T/S not GPS-tracked.
- **Honest finding (chased+killed 3 theories):** bad pins · fleet mix · crew-clustering (only 13/136) all rejected. Truth = TRIM IT pins ~37 sites vs 56 trucks/215 stops → most field stops are **unmapped work, not off-job.** Tool = reliable **job-landing recorder** (82 landings/31 trucks 8/3), NOT a slacking detector. Off-job → *likely unmapped*, never accused. Path-B (pin more work) parked → 23 no-match trucks.
- **Diagnostic that flipped my hypothesis:** reverse-geocoded never-matched trucks → the noise was **take-home parking + an unmapped location**, not bad pins. Santa Fe Springs (B27/B43 all day) = **a REPAIR SHOP** (Skipper) → now `shops.json` → they read *"in shop."*
- **Shipped:** `stop_report.py` → digest section in the Skipper-only morning email (`yard-send.js`) + **dashboard http://100.82.161.7:8093/stops.html** (`yard-morning.sh` runs it; both verified serving 200).

## 🔧 Boom/PTO hours — groundwork + correction (NEXT, resume tomorrow) → [[fleet-stop-attribution]]
- Went to build the next list item (boom/PTO). **Verified engine bus first: 0/100 populated** (eng_hours/idle/rpm/seatbelt all empty even parked = definitive; these are **GPS-only trackers, not J1939-wired**). Corrected the optimistic prior note. Captured to LESSONS.
- **Skipper corrected MY over-call:** boom/PTO **IS buildable** — the bucket trucks' **PTO is on a wired digital input**, visible live in OneStepGPS (*"changes a color on the truck label when they engage the PTO"*). Engine bus irrelevant.
- **Extended `osg_snapshot.py` to capture `in1–in4`** (the only aux data these trackers carry; trimmed the dead engine-bus fields). Committed.
- **30 boom/bucket trucks = all B-units B23–B55** (`boom-trucks.json`, from fleet master Vehicle Schedule). B=bucket/boom, C/D=chipper/dump → crew ≈ B+C+D.
- OSG API doesn't label which input is the PTO (0 "PTO" matches; `device_field_list` null). ▶️ **RESUME tomorrow:** identify the PTO input by **correlation** (input high on B-trucks during daytime job dwells, differs from C/D) once a workday of `in1–4` data lands; then build **PTO-engaged hours/truck/day** (20-min cadence = directional).
- End-of-day: Skipper asked to save all work to the wiki + give this project its own file → created [[fleet-stop-attribution]]. "This one is awesome."

## 💰 Bonus-Eligible (Emergency) payroll report page for Dimitry — BUILT + verified on play (afternoon)
Skipper's ask: Dimitry manually generates the TRIM IT **Day Sheet** for every day of a pay period and eyeballs the **red lines** to find bonus-eligible emergency work for payroll. Replace that with a configurable-date-window page. ⚠️ Needs its OWN wiki note next session (`wiki/projects/` + PROJECTS pointer) — flush rules kept this in the daily only.

### Trigger — CONFIRMED (the "figure out what triggers the red line" ask)
- **Red line = the crew-sheet "Bonus" checkbox = `dbo.UserCalendars.EmergencyTimeEntry = 1`** (a per-crew-member flag on each person's Work Time row). 1,606 flagged rows on play; actively used.
- **Ruled OUT `WorkOrders.IsEmergency`** — it's **0 on all flagged rows** → red is NOT driven by job type or the WO emergency flag, it's the manual per-person checkbox exactly as the Skipper guessed. Also independent of job name (flagged work = stumps/grids/palms/Armstrong, not just "Citywide Limbs").
- The Day Sheet report itself is a ColdFusion **binary `.cfr`** (can't read source) → confirmed via the data model + the checkbox field, same answer.

### Data model (reuse recipe)
`UserCalendars uc` (CrewSheetID, UserID, EmergencyTimeEntry, **ActHours** = the hours) → `CrewSheets cs` (WorkOrderID, CalendarID) → `WorkOrders wo` (Desc1=job, YardTypeID, ProjectID) → `Calendars cal` (**CalDate** = the date, via cs.CalendarID — never WorkDate) → `flow.Users u` (Desc1 = crew-member name) → `Projects p` (GeoMarketID) → `GeoMarkets gm` (market). Yard from `dbo.YardTypes` (**1=North, 2=South**).

### The page — `Dashboard-BonusEligible.cfm`
- **LIVE (play):** https://play.greatscotttreeservice.com/GSTS/Dashboard-BonusEligible.cfm  · webroot `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` · repo `arbor-stack/production-dashboard/bonus-eligible/` (gilligan-arborstack — commit+push separately).
- **Access:** gated to **UserID 9 (Skipper) + 376 (bot) + 228 (Dimitry Rabyy)**; others 403 (verified). Uses `dashboard-auth-gate.cfm` include + explicit allow-list.
- **Two views** (toggle): **Per-employee rollup** (bonus hrs/person for payroll, default) + **Detail lines** (each entry; **job links to the work order** `Profile.WorkOrder.Detail.cfm?ZWorkOrderID=` — verified 200).
- **CSV export** of the *current* view + filters (Content-Disposition attachment; tested 36 rows).
- **Filters:** Yard (North/South) + **Market → 4 buckets** computed from GeoMarkets: **Municipal** (Cities/Counties/Parks/Schools), **HOA**, **Retail**, **Commercial** (everything else incl. Universities/Property Mgmt). Skipper approved this split.
- **Pay period = CONFIGURABLE** (Skipper requirement): config table stores **AnchorEndDate + PeriodDays**; page opens on the **current pay period** (computed from anchor); **◀ Prev / This / Next ▶** nav + inline **⚙ settings editor** (change end date + length, save→persists, no rebuild) + manual From/To still available. **Seeded WEEKLY (7 days)** — Skipper flips to 14 in ⚙ if payroll is biweekly. Verified: current=8/3–8/9, prev=7/27–8/2, settings save→persist→revert (302).
- Style = green brand package (`assets/css/gsts-tokens.css` + inline tokens), app-bar header, matches BOD dashboard look.
- Sample (14-day window): **331.28 bonus hrs / 26 employees / 36 entries** — real play data.

### OPEN / TODO (resume)
- **Deploy to PROD via Jordan** when Skipper approves (so Dimitry runs real payroll off it; currently play-only).
- **Log to `gsts-ship-log.md` + Reference page** per repair contract (not yet done).
- Create the dedicated **wiki project note** for this page.
- Possible enhancement if asked: compute the **dollar bonus amount** (not just hours) — would need the bonus rate/rule from Dimitry.
