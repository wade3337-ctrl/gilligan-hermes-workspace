
## MuniBot pricing-tool REAL TEST setup — 8 city bids from Brent (00:1x UTC)
- Skipper handed a zip: City Bid Pricing Files (2025-2026). Sorted -> inbox-pull/city-bids-2025-2026/: BEFORE-bids/ (8 blank pricing forms = MuniBot input) + WINNING-answers/ (8 GSTS submitted bids = answer key) + MANIFEST.md. Classified by extractable price count.
- Cities: 2025 Bell Gardens/Glendale/Rosemead/West Covina; 2026 Gardena/Norwalk/Pomona/Riverside. Caveat: GLENDALE = scanned image PDFs (0 extractable prices, needs OCR). Riverside answer = RFP_2535_Cost_Summary.pdf (no "(Submitted)" tag).
- Skipper: MuniBot should pull inventories from the WAREHOUSE as part of the test. All 8 cities HAVE warehouse inventory (18/18/24/43/9/22/16/27 files). But bid_engine.extract_inventory_files(rfp_dir) only searches the rfp-dir, NOT the warehouse (only competitor files are warehouse-searched). Skipper chose A: flag to Herman. Emailed Herman (new thread) to add warehouse/<county>/<city>/ inventory search + reuse the county-dir walk. AWAITING Herman's updated bid_engine.py -> then install + run the 8-city test, compare to WINNING-answers.

## MuniBot v5 fix deployed (Herman email #66, "deploy and rerun Long Beach proof")
- Skipper heads-up → installed Herman's `munibot-v5-fix.tar.gz` into the `munibot` container (mount `~/.munibot`→`/opt/data`).
- **Bundle integrity:** all 11 SHA256SUMS OK; Attachment AA = `e0f42c…bf233` matches Herman's declared hash (byte-for-byte City form, untouched).
- Backed up prior runtime scripts → `~/.munibot/home/_backups/pre-v5-20260718-013500/` (bid_engine/bid_output/competitor_extractor).
- `uv pip install -r requirements.txt` (adds rapidocr stack); **15/15 unittest suite PASS**.
- Installed runtime scripts; ran **blind Long Beach proof**: engine exit 0 (Price Buddy 6 bands CycleTimeEach; inventory 87,229; annual cap 20,000). `bid_output.py` exit **2** = intended fail-closed (REVIEW REQUIRED: no verified current LB rate for **Pest Control Advisor** + **Qualified Applicator** → safe fallbacks).
- **Independent verify_long_beach.py on the freshly-generated workbook: PASS** — 46 lines / 64 formulas, watering held $904.49, 6 grid prices all ≥ cycle floors, method `cycle_time_each_qty1_v1`, inventory 87,229 / cap 20,000, AA hash intact.
- Business flag preserved: conservative full-cycle floor puts LB grid materially above WCA historical → reported as infeasible-without-grid-evidence, NOT reverted to trim-only/below-cost.
- **GATE:** nothing released to Brent (PCA/QAC pending Jason review). Workbook staged `/tmp/Long_Beach_MuniBot_FIXED.xlsx` for Skipper. No outbound sent (GILLIGAN_EMAIL_DRAFT was just a copy of the received email).
