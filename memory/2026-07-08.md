# 2026-07-08 — North-yard analysis (paused) + City Budgets FY-picker repair

## North yard 45→40h revenue analysis — PAUSED (to discuss)
- Framework agreed: net = OT pay saved − margin on production given up; hinges on **backlog vs route-constrained** (open Q for Skipper). First cut: North=Yard 1, tagged $4.35M/34.8k hrs/TPH $125, but **66% of WOs are yard-Unset** ($12.1M) → attribution must be fixed first. `YardHours` table stale (ends 2025-10).
- Full state → `wiki/work/analysis-north-yard-hours.md`; todo card on the arbor Kanban (backlog).

## City Budgets report — FY picker FIXED (ship-log #108, live on play)
- **Symptom:** on the Jul-1 fiscal-year turnover the report showed only Long Beach + Anaheim; the FY dropdown wouldn't roll back to 25/26.
- **Root cause:** the city list was gated on `dbo.CompanyYears` having a row **covering today with hours>0** — new-FY hours are 0.0 at rollover so Jul–Jun cities vanished; and the gate used `GETDATE()` not the selected FY (so the picker was inert).
- **Fix (`CityBudgets.data.cfm`):** list now follows the **contract budget for the viewed FY** (dropped the CompanyYears gate in qCities/qSumCity/qSumInvoices; render loop shows a city only if it has budget/activity for that label). Verified on play: 25/26→**11 cities** (rolls back ✓), 26/27→**6** (Newport $2,081,415 ✓), current→**7** (was 2). 0 CF errors, backup-first, no sibling shares the bug.
- **Real, not a bug:** several cities' contracts end at 25/26 (Aliso, Cypress, Fountain Valley, Long Beach, Stanton) → need 26/27 year entered / renewal.
- **Committed to memory + Herman:** arbor-stack commit 5ceb935 (ship-log #108 + detail file + Reference page synced); `LESSONS.md` (recurring FY-turnover bug class); **trimit-knowledge playbook updated + pushed** (962f9b4) + Herman's vault copy refreshed (he now has the gotcha).
- Lesson: any TRIM IT report keyed on `CompanyYears`+today will empty out at FY turnover — check the family.

## North yard 45→40h analysis — COMPLETE (evening)
- **Answer:** cutting to 40 = back to the 2026 budget (which assumes 40h/2080 hrs, ~$219K OT all year). Summer: ~$0 revenue risk (North book ~3 wks fits 40h) + **~$90K OT saved over the trough**. Guardrail: flex back to 45+ for the **Sept pre-holiday surge** (fall +12–16%, deadline work). Seasonal lever, not permanent.
- **Key data (all cross-sourced):** North TPH ~$127, ~$232K/wk, ~1,830 hrs/wk; **loaded labor ~$36/hr** (Dec fin $35.61 / 2026 budget $36.57); **North 16 crews / South 14 / 4 flex** (CrewNames.SiteAssigned); yard-tag only from 2026 (use 2026 window); YardHours/CrewAssignments stale. Full note → `wiki/work/analysis-north-yard-hours.md`; rate fact → `wiki/facts/gsts-field-labor-rate.md`.
- **Herman:** shared method+aggregates → `trimit-knowledge/query-playbook/yard-production-and-labor.md` (individual wages kept OUT of the shared vault for confidentiality).

## Jason Wade TRIM IT access upgraded to full (PLAY — pending prod)
- Skipper's account (UserID 9, jwade) was Fleet/Mechanic, SecurityLevel 1, 52/100 permissions (reflected his old role, not COO). He asked for full 100.
- **Granted on PLAY:** SecurityLevelID 1→5, all UserActions IsAllowed=1 + inserted rows for missing ActionDefs → **100/100 allowed, 0 denied**. Left Role001/group labels as-is (cosmetic, not permission gates).
- **Revertible:** snapshot at `~/arbor-stack/backups/jwade-access-snapshot-20260708-0232.txt` (was level 1, 53 rows).
- **NEXT:** Skipper tests on play (log in as jwade); if good → replicate on PROD (same grant SQL, UserID 9). Method = clone-all-ActionDefs + SecurityLevel 5. Grant SQL: `/tmp/g2test/grant.sql` pattern.
- ⚠️ **Login-stall gotcha (2026-07-08):** after the grant, Skipper couldn't log in ("stuck on welcome page") — it was **stale browser CACHE holding the old session**, NOT the permission change (login validation doesn't read SecurityLevel/UserActions). Fix = clear cache. Briefly reverted from snapshot to rule it out, then re-applied (100/100, level 5). **⚠️ NOTE: I only have PLAY access, NOT prod** — prod changes are a handoff (someone with prod access runs the SQL).
