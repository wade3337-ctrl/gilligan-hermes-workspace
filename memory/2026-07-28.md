# 2026-07-28

## Session (01:24–02:25 UTC) — audit verification + the play box
- **Gateway restarted**; watchdog change survived (resume/fresh 360000, timeoutSeconds 900). The 6-agent stall fix holds.
- **Verified the 6 high-consequence suite-audit findings MYSELF** against source MD5-proven identical to the shipped `V15-DASHBOARDS-DEPLOY-20260719.zip`. **4 confirmed, 2 refuted** → `arbor-stack/predeploy-pkg3/AUDIT-VERIFICATION-RESULTS.md`.
  - Refuted: the access gate does NOT fail open (`dashOK` false before anything can throw); `ByCrewName` has no "actual revenue" label (word appears 0 times). Both would have been wasted fixes.
- **Fixed #1/#3/#4** (Skipper approved; #5/#6 held as his decisions):
  - `realuser-gate.cfm` added to `FinancialReportDashboard.cfm` + `FinancialReportExport.cfm` — both were **fully ungated**, full customer CSV readable with a junk cookie.
  - Close-rate drill now foots to its tile: all 3 status lists byte-identical across Detail/ByRep/ByMarket, city exclusion follows `entityKind`.
  - End-day truncation: **18 sites found, 17 fixed, 1 correctly left** (`SalesPipeline:695` — `<= GETDATE()` carries a time, so it's right). Crew had reported 3.
  - 🚧 **NOT render-verified** — play unreachable. Gate recorded in `OUTSTANDING-FOR-PROD.md` section G.
- **Also corrected**: `OUTSTANDING-FOR-PROD.md` sections A+B were stale (pre-fixlist checksums, B listed as the open gate).
- **The play box:** I called it DOWN — **wrong**. It answers TCP on 22/80/443/14333 in 0.18s and sends zero bytes. **Skipper's read is the better one:** Jordan + Travis are building shared dev-server access + a repository, and the uniform connect time, dead :3389 and his own VPN failing all fit a middlebox change rather than a wedged host. **Standing down on Nocix** (Travis holds that account). → [[play-box-wedge-signature]]
- ⚠️ **Flagged risk:** `Workbench` on play (~564 override rows, BidQueue, DashboardAccess edits, GoalSettings) has **no local data dump**. First action when play returns.
- **Skipper turning in for the night.** Nothing in flight, nothing promised. Resume tomorrow on whatever Jordan/Travis come back with.

## Session part 2 (04:30–06:05 UTC) — play recovered, package SHIPPED, more repairs
- **Play box came back** (Jordan hard-rebooted 02:58 UTC). Ran the flagged first action: **backed up `Workbench`** (COPY_ONLY, 671 KB → `arbor-stack/backups/play-workbench/`, md5 `86cbcf51…`). Confirmed the DSN repoint survived — play still isolated. Cause of the freeze remains **undiagnosed**; the empty 5.5-h event log is the decisive evidence it was the machine, not the network. → [[play-box-wedge-signature]]
- **Render-verified section G on play**, then deployed + verified 3 MORE fixes:
  - **#5 (BLOCKER, was live on prod):** SPM Results auth gate sat inside `<cfif showComm>` — `?ZCustType=municipal` skipped it. Forged cookie got **200 / 19,291 b**; now **403** on all three paths. Hoisting it also fixed **#31** (municipal CSV called `csvField()` from the same skipped include).
  - **#26** stack-trace leak (`?page=abc` rendered `coldfusion.runtime` + `D:\home`), **#23** reflected XSS (18 hrefs + 1 input value), and an unreported unvalidated `?step=` that built a 180KB page.
  - ⚠️ Nearly broke search: my first sweep tried to encode all 33 `URL.search` uses, but **14 are inside `cfqueryparam` and must stay raw**. The assertion caught it pre-write. → encode at the SINK.
- **📦 PACKAGE SHIPPED 05:57 UTC** to jkim CC jwade, `TRIMIT-BUGFIXES-20260728.zip` md5 `b66cc5b1…`, 17 files, accepted/none rejected. Skipper approved the exact draft first (per-email rule honoured).
  - He directed: **"files, descriptions of the fixes and instructions, no fluff"** and **"remove the production is behind references."** Internal `MANIFEST.md` pulled; folders renamed to `ui-files/` + `baseline/`; `START-HERE.md` rewritten neutral. **Leak scan is now part of packaging** — it caught a CFML comment with "per Skipper" + "Jasonsrepairs" in a vendor-bound file, and I proved the reword was comment-only by diffing against the Skipper-confirmed version.
  - Also caught: a **hand-typed byte count** (66,927 vs actual 66,595). Now the table is generated from the staged folder and every row asserted. Same class as the stale-manifest blocker.
- **Rosa's Go-Aheads SOP = duplicate** of Jeanie's 2026-06-23 copy (identical text md5 `807f50265ded`). Did NOT file a duplicate — instead captured what it teaches: **[[goahead-status-lifecycle]]**. 8 go-aheads stuck mid-activation ($121K), incl. 5 near-identical Crystal Cove attempts at $22,649 that likely never reached Scheduling.
- **Open:** `ZUserID=9` → `Dashboard-Access.cfm` (NOT fixed, likely framework-level) · 40 of 51 audit findings unverified · play-box freeze cause unknown.

### 15:00 UTC — Play DB refresh STALE (automated monitor)
`play-refresh-monitor.sh` → `STALE|2026-07-26 03:03:20|59h`. Play GSTS frozen at 2026-07-26 03:03.
- **NEW outage, not the 7/25 one.** Prior alert (7/25) was vintage **7/22**; vintage has since advanced to **7/26** → the prod→rsync.net upload *resumed* 7/23–7/26 and then stopped again after 7/26 03:00. Third occurrence of the same pattern (7/07, 7/22, 7/26) — it's recurring, not a one-off.
- `\GSTS DB RESTORE` on play keeps reporting success; it just re-restores the newest `GSTS_backup_*.bak` on rsync.net (/data1/home/fm2760/database), and there is none after 7/26. Prod-side fix = Jordan.
- **Side signal:** the monitor connected to play SQL fine, so the play box is **reachable again** (contradicts the earlier "play wedged/unreachable" read from this morning). Workbench data-dump gap on play is now actionable.
- Alerted Skipper.

## Session pin — 2026-07-28 (afternoon, Skipper)
- **ColdFusion 2023 vs 2025** researched; I was wrong that CF2025 has no AI — **Update 8 (2026-05-20) is a full AI framework incl. MCP server+client**. Filed [[coldfusion-2025-upgrade-case]]. Then cloned + source-audited **MCPCFC** → [[mcpcfc-coldfusion-mcp-server]]: **does NOT require CF2025**, ~3,069 lines of protocol core, no auth, `sendEmail` demo tool violates our comms policy, SQL denylist proven to reject `VARCHAR(`/`Resp_Party`/`LIKE '%delete%'`.
- **Revenue ledger rerun live.** Two corrections: the deal dashboard's goal falls back silently to a stale constant ($25.05M) because `dbo.GoalSettings` is gone; and "undated sold work" is really **2027** work ($3.14M/238 beyond-2026 vs $53K/9 truly undated, + $153K/25 past-due invisible to both buckets).
- **Reconciled TRIM IT to Dimitry's books** — H1 within **1.28%**. Found my own 7/20 error: H1 recorded as $11.88M because a partial `Jul 26` column was swept in; true $11,222,433, run-rate $22.4M not $23.8M.
- **Built [[path-to-25m-2026]]** — the run-rate + capacity math (see MEMORY pin). Open item: the capacity view.
- **Diagnosed the future-dated crew sheets** (741 correct vs 148 flipped; hours = estimates; City of Industry ~53%). Still held.
- **V1.5 access**: Skipper granted Celeste Armenta (209) + Larry Baldwin (340); documented the search-box gap and the menu-hidden behaviour.
- ▶️ **NEXT: high-level report to the BOARD for their quarterly meeting** — scope question raised (does it include Fort Point / Track-2, or Track-1 operations only?). Nothing drafted yet.

## Board plan — SENT 2026-07-29 (02:03 UTC)
- The Skipper sent the **Q2 2026 COO Operating Plan** to the Board of Directors. Frozen; SENT copy + basis file in `board-reports/`.
- **The document was rebuilt four times as he corrected me**, and every correction was right: (1) the CFO reports the numbers, the board wants his *plans*; (2) 15 hires not 19; (3) use the **fully weighted** rate, not the crew-sheet one; (4) three specific claims cut — the "5.7% recoverable hours" (actually stale crew assignment), the weekly-meeting causation ("NOT TRUE"), and the year-over-year productivity comparison.
- Final basis: **CFO revenue ÷ clocked payroll hours** — deliberately the number nobody can recompute differently. Q1 $118.76 → Q2 $125.10, target $130.
- His real operating win went in: **3 of 4 weekly 30-min safety meetings replaced with 2-min daily videos** = ~47 net min/person/month ≈ 708 crew-hrs/yr ≈ $89K, with safety coverage *increased*.
- Gap-closing plan he chose: 15 hires + **$1.38/paid hr + 5 minutes per person per day**.
