# 2026-06-30 — Session log

Big session. Two arcs: **disaster recovery** + **arbor-core RFP automation (B1→B2→B3)**.

## 🆘 Disaster recovery (NEW capability)
Closed the gap where GitHub backups held the "brain" but no credentials. Built:
- AES-256 gpg **credential bundle** (`workspace/recovery/gilligan-credentials.tar.gz.gpg`) — openclaw.json (Anthropic
  key + Discord token), all `.secrets`, gstsdb SSH key, gh-token, crontab. Passphrase in Skipper's password manager
  ("Gilligan recovery bundle") + on-machine `~/backups/.recovery-pass` (drives nightly auto-refresh 3:28am).
- **`RECOVERY.md`** cold-start runbook; **`recovery/dr-restore-dryrun.sh`** non-destructive test (PASS).
- **USB grab-and-go kit** flashed to "JASON STICK" `/GILLIGAN-RECOVERY` (Windows `START-GILLIGAN.bat`+ps1, also .sh).
  Skipper's spare = Windows → conversational recovery (cron monitors are Linux-only). Mounted the FAT32 stick via a
  `--privileged busybox` docker container (no passwordless sudo for mount; I'm in the docker group).
- Detail → MEMORY.md "DISASTER RECOVERY" bullet. **DR dry-run report auto-lands 6:30am via cron.**

## 🤖 arbor-core RFP automation — full pipeline LIVE (Track-2 black)
Resumed the lost "automated RFP creation" thread (Rosa's email id 37 = 3 real requests: MainPlace/formal_rfp,
Altamar/one_off_work_order, Paradise Palms/new_customer_proposal). Built end-to-end:
- **B1** intake agent → structured records, 2-lab verified (GLM+Gemini). Customer = serviced property (mgr→mgmt_co).
- **B2** = arbor-core DB went LIVE (first running code): customer spine, UUIDv7 PKs, RLS isolation proven; +
  match-or-create with **lazy data-quality-gated import** (D4 — search TRIM IT, pull only what's touched, never bulk).
- **B3** archetype-aware outbound drafting (drafts only, never sent).
- Resume detail → `~/arbor-core/CHECKPOINT.md` (2026-06-30 section). Next frontier = estimating engine for the bid sheet.
- "**Boss Hermes**" = Skipper's name for the arbor-core sandbox/crew (NOT Herman the Arduino). Noted in auto-memory.

## ▶ RESUME NEXT SESSION (Skipper pings "I'm up", morning Jun 30/Jul 1)
**Arbor-core sales engine, Stage 3 = the keystone.** Read first: `arbor-core/docs/PIPELINE-STATUS-MAP.md` (the board) +
`arbor-core/rfp-automation/BID-PACKAGE-electronification.md` (kill-the-PDFs plan) + `arbor-core/CHECKPOINT.md`.
**NEXT BRICK:** convert the **Bid Sheet → a live in-system quote object** (species×qty×season×price, 2027/28/29 perform-
in-year flags, live totals) = arbor-core `proposal`+`proposal_line`. This is BOTH the electronification first step AND
the Stage-3 quote builder. **Open Q to confirm w/ Skipper before building:** season model = "perform-in-year per tree
line" (X per year)? Also: vendor-requirements = structured checklist now or just STORE the PDF for v1?
DB is live (`arbor_core` in `arbor_postgres`, app role `arbor_app`, `SET app.tenant_id='1'`).
**OVERNIGHT JOB (scheduled):** crew adversarial review of the B2 foundation (schema+matcher) runs via SYSTEM cron
09:30 UTC (outside the agent turn → Kimi can finish; GLM smoke-test already found real RLS/`current_setting(...,true)`
issues). Report → `arbor-core/importer/crew-review/CREW-VERIFY-REPORT.md`; OpenClaw cron delivers a digest 13:45 UTC.
**Before building Stage 3, REVIEW that crew report + fix the agreed CRITICAL/MAJOR schema findings first.**
Also pending (when Skipper awake): raise `noOutputTimeoutMs` in openclaw.json (needs gateway restart) so long
in-turn crew calls don't trip the 180s watchdog. Hermes instance IS running on jdog1 (containers `hermes`+`hermes-dashboard`,
hermes-agent image) — candidate test harness, not yet wired to drive crew verification.

### ☀️ MORNING TODO (Skipper pings when up):
1. **3 reports** auto-land: ~6:00 IsMeasured · ~6:30 DR dry-run · ~6:45 crew schema review. Read + present.
2. **Triage crew schema findings** → fix agreed CRITICAL/MAJOR in migrations before building on the foundation.
3. **⭐ Hermes brain = COST PROBLEM (Skipper, Jun 30):** Hermes currently runs its LLM brain on **OpenRouter =
   pay-as-you-go = too expensive.** Move it to a cheaper/SUBSCRIPTION model — Skipper's lean = **GLM** (lean on a
   sub, not metered API). ⚠️ **SCAR TISSUE:** the prior laptop-Hermes-on-GLM attempt DEAD-ENDED — (a) z.ai metered
   endpoint **blocked on the plan**, (b) Hermes↔z.ai **anthropic-endpoint compat wall ("no final response")**, and
   that laptop GLM key was revoked. So GLM-as-Hermes-brain has known walls — re-check if solvable. Other options to
   weigh: **local model** (JASONWORK RTX 5070 Ti llama3.1:8b = free but separate/not-always-on; or jdog1 Ollama
   llama3.2:3b CPU = free/weak), or **lean on the Claude Max sub** (already powers OpenClaw/me — check terms/limits).
   Step 1 = inspect Hermes's current model config (`~/.hermes/`) to see exactly how its brain is wired.
4. **Then:** first Hermes-ownership milestone (B1 intake live vs arbor_core) → then Stage 3 quote builder.
5. (when convenient) raise `noOutputTimeoutMs` in openclaw.json (needs gateway restart) for long in-turn crew calls.

## Lesson
- **"The application did not respond" on Discord = OpenClaw's 180s no-output watchdog killing my turn.** Confirmed in
  gateway log 2026-06-30: `reason=timeout — CLI produced no output for 180s and was terminated` at 01:45 & 01:53,
  both when **kimi-ask ran as a long FOREGROUND blocking Bash call** (slow reasoning model, >180s silent). The turn
  dies mid-reply → Discord shows "application did not respond" (exit 137). NOT message-preemption (my earlier wrong
  guess), NOT a crash/network issue — gateway stayed up. **Fix:** never run a slow model (kimi) as a long blocking
  foreground call; use glm/gemini in-line, or background kimi so it can't starve the turn's output stream.
- gemini-ask reads STDIN only (no file-path arg); glm/kimi accept a numeric max_tokens arg + file path.
