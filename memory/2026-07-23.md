# 2026-07-23 — daily log

A monster session (essentially all TRIM IT + a Kling side-quest). New durable notes: [[goodman-rfp-bid]], [[trimit-gps-import-pipeline]], [[kling-ai]]; updated [[herman-agent]], [[PROJECTS]].

## 🌳 Goodman Portfolio RFP bid (the big one) — [[goodman-rfp-bid]]
- New RFP from Scott (via Rebekah↔Gothic): bid the **Goodman industrial portfolio** as **Gothic's subcontractor** — ~28 properties / 6,400 trees, LA+IE, **DUE 8/3**, per-property awards, cost + labor hours, 3-yr. RFP came with a complete GPS tree survey (Schedule 2).
- **This was a "train Boss Herman" exercise** — I plan/verify, Herman executes. Account structure locked (under Gothic 301382). Herman Tasks 1–6:
  - T1 recon (bill-to≠mgmt-co model), T2 property map, T3 first-write pilot (raw SQL — **failed**: raw inserts skip the setup layer, trees never render).
  - **Pivot:** discovered TRIM IT's **official GPS import subsystem** → the RIGHT way. Reverse-engineered the whole thing → [[trimit-gps-import-pipeline]].
  - T4 official import (81 trees, full FK chain), T5 map visibility (IsNewPlot=1/DevMark, fixed a NULL ZoneDefID), T6 all detail fields + the SizeCode/SizeModelSizeID display trap + Location fields.
  - **PILOT GLC Fullerton Bldg 4 (Proj 1105465) COMPLETE + verified DB + visual.** Herman documented the converter spec + updated his skill. He genuinely leveled up.
- ▶️ **NEXT: pricing (Price Buddy)** — importer doesn't price; then scale the 27. Confirm Anaheim scope w/ Rebekah.
- Gave Herman **full PLAY write** (activated the Jul-10 grant) → [[herman-agent]].

## 🔧 TRIM IT fixes
- **Lat/Long geocoder (0/0 bug):** I diagnosed a dead SQL geocoder (`Locations_LatLong_upr` → retired keyless-Google) + built a Census-based fix + deploy package for **Jordan**. **Jordan's dev fixed it a *different, cleaner* way on PROD** (client-side role gate + null-guard). Both valid (two independent geocoders); theirs kept, mine retired. Lesson logged (find the simplest user-path cause first). Applied their fix to PLAY for parity. History: DB geocoder was original (~2010-13), browser one bolted on later, both rotted.
- **Field-app tree dots:** a hardcoded **`AND 1 = 0`** in the SelectedPoints query hid every project's dots on PLAY. Removed → Alicia's dots returned. Play-only stray; prod fine.
- **Profile pic:** the reapply cron ran 3–6 UTC but the play restore is 3AM PT (~10–11 UTC) → widened to all-day `*/30`. Fixed for good.

## 🎬 Kling AI — [[kling-ai]]
- Wired up video/image generation. Key at `~/.secrets/kling.json` (single Bearer, `api.klingai.com`). Two-wallet gotcha (sub ≠ API; needs prepaid pack). First video generated ✅. Installed static ffmpeg (`~/.local/bin`) to compress clips under Discord's 8MB.

## Ship log entries added: field-dots fix, herman play-write, profile-pic cron, geocoder (superseded), play-parity. LESSONS/PLAYBOOK updated (OLE varchar(8000), 0/0-geocoder, cron TZ, multi-producer diagnosis, census-via-OLE). Kanban: geocoder→shipped, Goodman card 56 updated, herman-write→arbor-core shipped.
