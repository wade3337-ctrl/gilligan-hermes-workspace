# 2026-08-01

## Gilligan voice platform — build session (gilligan-endpoint + OpenJarvis)

Context: continuing the "Gilligan voice platform" thread. Decision from 2026-07-31/08-01:
all voice paths ride on ONE build — expose Gilligan (the Hermes/gbt agent, tools included)
as an OpenAI-compatible endpoint. Then front-end = OpenJarvis (local, GUI, on subscriptions).

### ✅ BUILT: Gilligan-as-an-OpenAI-compatible-endpoint (MVP, PROVEN)
- **File:** `~/gilligan-hermes/gilligan-endpoint/server.py` (stdlib only, ThreadingHTTPServer).
- **URL:** `http://127.0.0.1:8831/v1` — routes `/v1/chat/completions`, `/v1/models`, `/health`.
- **What it does:** each request runs a FULL Gilligan AGENT turn (identity + memory + all 28
  tools, gbt brain) via `docker exec -i ... hermes -z "$(cat)"` — NOT a raw model call.
  Prompt fed via STDIN to dodge host ARG_MAX (same trap as the shim).
- **Confirmed native Hermes has NO OpenAI-compat serve** (`hermes serve` = JSON-RPC/WS gateway
  for its own desktop app), so the thin adapter is required. `hermes -z "$(cat)"` = BARE_STDIN_OK.
- **Proven 3 ways:** identity turn ("I'm Gilligan… GPT-5.6-sol via openai-codex"), a TOOL-USING
  turn (ran a shell cmd → `GILLIGAN_ENDPOINT_TOOL_OK`), and supervised service (`ENDPOINT_SERVICE_OK`).
- **Durable + secure:** systemd --user service `gilligan-endpoint.service` (auto-restart, boot).
  Bound to **127.0.0.1 ONLY** — it drives a tool-using agent (play SSH, email, deploys), never
  face public without auth. Voice front-ends reach it over localhost/tailnet.
- **MVP caveats:** non-streaming (fakes stream as 2 chunks), one-shot per call (no cross-call
  chat memory yet — Hermes still loads durable identity/memory each turn), long histories would
  hit ARG_MAX. Fine for voice; add persistent session before daily use.
- Log: `~/gilligan-hermes/gilligan-endpoint/endpoint.log`. Env: HOST/PORT/CONTAINER/TURN_TIMEOUT(300).
- Committed + pushed `ef6c9f6`. Wiki updated: `wiki/projects/gilligan-voice-platform.md`.

### 🔑 KEY FIX: endpoint must DROP the front-end's system prompt
- First OpenJarvis test replied "I'm OpenJarvis… on jdog1 under Debian" — WRONG. Cause: OpenJarvis
  injects its own "you are OpenJarvis" system prompt; I was passing it through as caller context and
  it overrode Gilligan's real identity.
- **Fix:** `_flatten()` now DROPS the caller's system messages so Gilligan's Hermes-supplied identity
  always wins. Re-tested → "I'm Gilligan, your Hermes Agent assistant, running GPT-5.6-sol via
  OpenAI Codex." ✅ Lesson: when wrapping an agent that already HAS an identity, the front-end's
  system prompt is noise — strip it, don't merge it.

### 🖥️ OpenJarvis — installed on server (jdog1), wired to Gilligan
- **Why server not laptop:** OpenJarvis-on-Windows is still self-described "advanced" (PowerShell
  one-liner install, WSL2 smoother, config-file edit to repoint brain, voice loop young). So install
  on jdog1 (full control + localhost to Gilligan's endpoint), expose its **browser chat UI over the
  tailnet** → user just opens a URL, zero PowerShell on his laptop.
- **Installed OK:** `jarvis` CLI works. One-shot query cmd = **`jarvis ask`** (NOT `jarvis chat`/`query`).
  Ollama came up (unused — we point at Gilligan). jdog1 itself has a GTX 1050 Ti (not needed).
- **Repointed engine to Gilligan:** OpenJarvis config edited so `[engine.vllm] host` (OpenAI-compat)
  → `http://127.0.0.1:8831/v1`. Config backed up first. Proven: `jarvis ask` turn showed up in
  endpoint.log (`turn: promptlen=...`) and answered AS Gilligan after the system-prompt fix.
- **⏳ IN PROGRESS at flush:** `jarvis serve` needs a `server`/web extra installed to serve the
  browser chat UI (`jarvis serve` = API server; chat UI is a separate frontend). Was installing web
  deps when interrupted. NEXT: finish web-UI deps → start web UI → expose over tailnet → hand user a
  URL. Then honestly test the VOICE loop (don't oversell — it's a maturing feature).

### ✅ SHIPPED: browser chat UI live over the tailnet (click-only, no PowerShell)
- **URL for Jason: `https://gilligan.tail5807bd.ts.net/`** (tailnet-only; open in any browser on a
  tailnet device — his Windows `desktop-472uetd` 100.66.14.71 is a member).
- **Stack (3 durable systemd --user services, linger=yes so they survive reboot/logout):**
  1. `gilligan-endpoint.service` — Gilligan OpenAI endpoint on 127.0.0.1:8831.
  2. `openjarvis-serve.service` — `uv run jarvis serve --host 127.0.0.1 --port 8600` (chat UI + OpenAI API).
     Config `~/.openjarvis/config.toml`: `[engine] default=vllm`, `[engine.vllm] host=http://127.0.0.1:8831`,
     `default_model=gilligan`. Serve auto-picked agent=orchestrator.
  3. `tailscale serve --bg --https=443 http://127.0.0.1:8600` → HTTPS tailnet URL (persisted by tailscaled).
- **Chat UI = the built React/Vite app** in `~/.openjarvis/src/frontend` (name `openjarvis-chat`).
  Built with `npm install && npm run build` — outputs static into `src/openjarvis/server/static/`, so
  `jarvis serve` serves it at `/`. (There's also a `desktop/` Tauri variant, unused.)
- **Security:** OpenJarvis refuses a 0.0.0.0 bind without OPENJARVIS_API_KEY (good default). So we keep
  it LOOPBACK and expose only via Tailscale Serve (tailnet-membership auth, HTTPS). No public port, no key.
- **Proven end-to-end:** tailnet URL / → 200 "OpenJarvis" chat UI; POST /v1/chat/completions via the URL
  → "Hi, I'm Gilligan" and endpoint.log logged the turn.
- Logs: `~/oj-serve.log`, `~/oj-frontend-build.log`. Config backup: `config.toml.bak-pregilligan-*`.

### 🎤 VOICE state (honestly tested 2026-08-01)
- **STT (voice INPUT) = WORKING.** faster-whisper was NOT actually installed despite the serve banner
  (`/v1/speech/health` returned available:false → needed `uv sync --extra desktop`). Installed it
  (faster-whisper 1.2.1 + ctranslate2 + onnxruntime), restarted serve → health now `available:true`.
  UI has a **MicButton** (`frontend/src/components/Chat/MicButton.tsx` + `hooks/useSpeech.ts`) that
  records → POST `/v1/speech/transcribe` → text into the chat box. All LOCAL (no metered key).
- **TTS (spoken reply BACK) = NOT wired in this web UI.** Server has only `/v1/speech/transcribe` +
  `/health` routes (grep of `api_routes.py`, THIS session) — NO `/synthesize` or `/tts` route. The
  frontend "synthesis" refs are research-timeline TEXT, not audio playback. TTS engines EXIST in the
  codebase (`speech/kokoro_tts.py` local, `cartesia_tts.py`/`openai_tts.py` cloud) but aren't exposed
  by the serve API or called by the chat UI. So out-of-box = push-to-talk DICTATION in, TEXT out.
- **Verdict:** browser UI gives voice-to-text dictation, not a full spoken conversation. A true
  talk↔listen loop needs TTS wiring (kokoro local endpoint + UI autoplay) OR a purpose-built voice
  front-end. This matches the maturing-voice caveat. → if Jason wants spoken-back, options: (1) wire
  kokoro TTS route + UI playback, (2) ElevenLabs ElevenAgents pointed at the Gilligan endpoint
  (most polished, cloud, ~$0.05-0.10/min), (3) wait for OpenJarvis two-way voice to land.

### 🐛 FIXED: mic "access denied" was OpenJarvis's OWN Permissions-Policy header (not Windows/Chrome)
- Symptom: Jason's Chrome showed "Microphone access denied" even though Windows mic privacy was fully On
  (Chrome listed under "let desktop apps access mic", last-used 7/31), the Chrome site permission was
  Allow (blue toggle), mic works in Telegram, laptop w/ working mic. TEXT chat worked perfectly
  ("Yep, it's me—Gilligan, running on GPT-5.6 Sol through Hermes Agent").
- **Root cause:** OpenJarvis's server security middleware sends `permissions-policy:
  camera=(), microphone=(), geolocation=()` on EVERY response — `microphone=()` = empty allowlist =
  the page's own origin may NOT use the mic. Browser kills getUserMedia BEFORE OS/site perms matter.
  And `hooks/useSpeech.ts` catches ANY getUserMedia error and mislabels it "Microphone access denied"
  (misleading — it's a Permissions-Policy block, not a permission denial). Ironic: ships a mic feature,
  header forbids the mic.
- **Verify the real cause with `curl -sI` and read the Permissions-Policy header** — that's what nailed it
  after 3 layers of user-side settings all checked out. Lesson: when a web app says "mic denied" but OS +
  browser + site all allow, suspect the SERVER's Permissions-Policy header, not the user.
- **Fix:** `~/.openjarvis/src/src/openjarvis/server/middleware.py` — changed BOTH the live header and the
  `SECURITY_HEADERS` constant to `camera=(self), microphone=(self), geolocation=()`. Backup
  `middleware.py.bak-premicfix-*`. Restarted `openjarvis-serve.service`; header now `microphone=(self)`,
  verified via curl (local + tailnet). ⚠️ This patch is in the OpenJarvis SOURCE — `jarvis self-update`
  / `git pull` would revert it; re-apply after any OpenJarvis update.
- User needs a HARD reload (Ctrl+Shift+R) to drop the cached old header, then mic works.

### 🐛 FIXED #2: STT "libcublas.so.12 not found" — faster-whisper defaulted to GPU/CUDA
- After the mic-permission fix, dictation recorded but transcription failed: "Speech transcription
  failed: Library libcublas.so.12 is not found or cannot be loaded."
- **Cause:** faster-whisper/ctranslate2 default `device="auto"` tries CUDA; jdog1's GTX 1050 Ti has no
  CUDA runtime libs (libcublas). 
- **Fix (config, not source):** appended to `~/.openjarvis/config.toml`:
  `[speech]` \ `backend="faster-whisper"` \ `device="cpu"` \ `compute_type="int8"`. Restart. Verified
  with a real POST to `/v1/speech/transcribe` — clean 200 JSON, ZERO cublas errors in fresh log lines.
  Backup `config.toml.bak-precpu-*`. (CPU is plenty for short dictation; whisper base/small on CPU.)
- Lesson: on a box without a working CUDA stack, force `speech.device=cpu` — the "auto" default assumes GPU.

### 🔊 BUILT: local TTS — Gilligan SPEAKS his replies (option 1, kokoro)
- **Goal:** full hands-free loop — talk → Gilligan answers OUT LOUD. OpenJarvis's web UI shipped voice
  INPUT only (no synthesize route, no playback), so this was a real build: install engine + add server
  route + wire the React UI + rebuild.
- **Engine:** `uv pip install kokoro soundfile` into OpenJarvis venv (local, open-source, no metered key;
  model hexgrad/Kokoro-82M auto-downloads first call). Voices: af_heart(default), af_bella, am_adam, am_michael.
- **Server route (SOURCE PATCH):** added to `~/.openjarvis/src/src/openjarvis/server/api_routes.py`:
  `POST /v1/speech/synthesize` (JSON {text,voice_id,speed} → WAV bytes) + `GET /v1/speech/tts/health`.
  Lazy singleton `_get_tts_backend()` via TTSRegistry('kokoro'). Proven: 220KB WAV, 24kHz mono PCM.
- **GPU trap again → fixed globally:** kokoro grabbed the GTX 1050 Ti → `cudaErrorNoKernelImageForDevice`
  (card too old for torch CUDA kernels). Fix: added `Environment=CUDA_VISIBLE_DEVICES=` to
  `openjarvis-serve.service` — hides the GPU so ALL speech (whisper STT + kokoro TTS) runs on CPU.
  Verified in the live process env. (This is the robust umbrella fix; the earlier speech.device=cpu still stands.)
- **Frontend (SOURCE PATCH + rebuild):** `frontend/src/lib/api.ts` +`synthesizeSpeech()` (strips markdown,
  POSTs, returns blob URL) +`fetchTtsHealth()`; `frontend/src/components/Chat/InputArea.tsx` — after the
  assistant reply completes, if `localStorage 'oj_autospeak' !== 'off'`, synthesize + `new Audio().play()`
  (autoplay) + attach `audio.url` so the existing `AudioPlayer` bubble shows a replay control. Best-effort:
  TTS failure never blocks the text reply. Rebuilt (`npm run build` → static into server/static/), serve restarted.
- **⚠️ These are OpenJarvis SOURCE patches** (api_routes.py, middleware.py, frontend/*) — `jarvis self-update`
  / `git pull` reverts them. Re-apply after any OpenJarvis update. Backups: `middleware.py.bak-premicfix-*`,
  `config.toml.bak-*`. Autoplay may be blocked by Chrome until first user gesture — the replay button covers it.
- **Status:** server-side proven (WAV out, tts/health available:true, UI rebuilt+served). PENDING Jason's
  hard-reload browser test to confirm he HEARS it.

### ✅ PIVOT (2026-08-01 ~03:30): OpenJarvis KILLED — voice went to the HERMES TELEGRAM BOT instead
- Skipper called it after OpenJarvis fought us at every layer (mic Permissions-Policy, CUDA x2, and a PWA
  service-worker cache that kept serving the stale UI even with the fix deployed). "this sucks. i give up.
  lets get rid of this whole jarvis path… I was having better luck with voice through hermesagent."
- **Teardown done:** `openjarvis-serve.service` + `gilligan-endpoint.service` stopped+disabled, `tailscale
  serve --https=443 off`, port 8600 closed. Code + wiki notes KEPT (nothing deleted). OpenJarvis install
  still at `~/.openjarvis` if ever revisited (unlikely).
- **✅ VOICE WORKS ON TELEGRAM** (`@Gilligan_gsts_bot`). It was ~90% configured already in
  `~/.gilligan-hermes/config.yaml`: `stt: provider: local` (local whisper, free/no key) + `tts: provider:
  edge` (Microsoft edge-tts GuyNeural, free/no key). The ONLY change needed: `voice.auto_tts: false ->
  true` (line 363) so Gilligan replies with voice. Backed up (`config.yaml.bak-preautotts-*`), restarted
  container. Skipper confirmed: "the voice works on telegram." ⭐ LESSON: the native app (Telegram) with a
  real mic + audio playback beats a browser web UI — no service workers, CUDA, or autoplay gates.
  ⚠️ auto_tts=true means EVERY reply is spoken (even typed ones); offered to make it voice-back-only-on-
  voice-input if it gets noisy — Skipper hasn't asked yet.

### ✅ B DONE (2026-08-01): Telegram Gilligan can now query PLAY (live TRIM IT DB)
- Asked whether Hermes Gilligan has the wiki + outside-world/play access. Findings: **wiki = near-complete
  but STALE copy** (`/opt/data/home/workspace/wiki`, 169/174 .md, frozen ~7/30, a COPY not a live mount —
  today's work absent). **Internet + play network reach = YES** (network_mode host; play 100.86.97.46 over
  tailnet). **Gap was the play DB toolchain.**
- **How play access actually works** (not a local SQL client): `gsql.sh` SSHes into the Windows play box
  (`Administrator@100.86.97.46` over Tailscale) with key `gstsdb_ed25519` and runs `SQLCMD.EXE` THERE
  (`-S localhost,14333 -d GSTS -E`). So the container only needs: the script + the SSH key + ssh/scp +
  tailnet reach — all present. Container already had `/opt/data/.ssh/gstsdb_ed25519`, ssh/scp, and the
  arbor-stack copy.
- **The one fix:** container's `gsql.sh` hardcoded `KEY=/home/wade3337/.ssh/...` (host path, absent in
  container). Patched `/opt/data/home/arbor-stack/production-dashboard/gsql.sh` → portable
  `KEY="${GSTSDB_KEY:-$HOME/.ssh/gstsdb_ed25519}"` + added `StrictHostKeyChecking=accept-new`. Backup
  `gsql.sh.bak-prekeyfix-*`.
- **PROVEN natively (no overrides) from inside the container:** `SELECT COUNT(*) FROM sys.databases` -> 10;
  real TRIM IT tables (AbsenceTypes/AccessCodes/AccountCodes). The Telegram Gilligan now reads live play
  data like this OpenClaw agent.
- ⚠️ READ path proven; WRITE to play still governed by the standing backup-first/repair-contract rules.

### ✅ A DONE (2026-08-01): Telegram Gilligan's wiki/memory now auto-syncs (no longer frozen at 7/30)
- **Layout learned:** Hermes agent workspace = `/opt/data/home/workspace` (host `~/.gilligan-hermes/home/
  workspace`) — holds AGENTS.md, IDENTITY/SOUL, MEMORY.md, wiki, memory/. Its Hermes summary-memory is
  SEPARATE at `/opt/data/memories/MEMORY.md` (char-limited, not the wiki). Container cwd=/opt/data.
- **Was genuinely stale:** container 169 wiki .md vs live 174; missing today's memory + 5 new notes
  (bluejay-falcon-usfs-rfp, claude-local-shim-spike, gilligan-pilot-model-setup, gilligan-voice-platform,
  moa-mixture-of-agents). Reverse-diff = 0 container-unique files, so a mirror is safe.
- **Mechanism (no container surgery):** that workspace is just a host folder under the `.gilligan-hermes`
  mount, so writing it updates the container live. Script `~/backups/sync-workspace-to-gilligan.sh` does a
  one-way `rsync -a --delete --exclude .git --exclude node_modules` from the LIVE OpenClaw workspace →
  container. `.git` excluded (container has its OWN git repo). Source of truth = OpenClaw workspace.
- **Schedule:** systemd --user timer `gilligan-wiki-sync.timer` every 10 min (OnBootSec=2min, Persistent,
  linger=yes → survives reboot). Verified: container now 174 notes + today's memory, confirmed inside container.
- ⚠️ rsync gotcha: `rsync -an` WITHOUT `-v` prints nothing — don't misread empty output as "0 changes";
  use `-avn` for a real dry-run. (Cost me a detour.)
- ⚠️ Confidentiality: workspace incl. BLACK Fort Point files syncs into the container (already had a copy
  — no NEW exposure). Telegram bot is Skipper's DM (allowed_chats ''); keep BLACK out of any shared chat.

### ⭐⭐ PROOF PASSED (2026-08-01 ~03:56): Telegram Gilligan did a real work task END-TO-END
- Skipper gave it a live proof task BY VOICE: pull a real number from play and report it. **"he did it. he
  reported the number accurately."** = voice in → local STT → gbt brain → `gsql.sh` live TRIM IT query →
  correct number → edge-TTS voice out. The full stack works together on a genuine task, not a canned demo.
- **Significance:** this is the first end-to-end validation that the Hermes/Telegram Gilligan can do the
  actual work — the migration path is proven viable, not just theoretically wired. The three enablers
  (voice / live play DB / auto-current wiki) all fired in one real task.

### Next steps (resume pointer)
- Both A + B done + PROVEN by a real voice task. Telegram Gilligan now: voice loop working, live play DB read (gsql.sh), auto-current wiki.
- OPEN: decide when/if the Telegram Gilligan becomes PRIMARY (then sync direction/ownership of memory writes
  needs a rethink — currently OpenClaw workspace is source of truth, one-way). 2 pilot proof tasks still owed.
- (old) OpenJarvis path dead; code kept at ~/.openjarvis + ~/gilligan-hermes/gilligan-endpoint (services off). (faster-whisper STT is active; TTS?) — don't oversell,
   it's maturing (their issue #250 / PR #503). Text chat is solid now; voice is the next proof.
2. Persistent session in the endpoint (cross-call memory) before heavy daily use — MVP is one-shot/turn.
3. Optional: point ElevenLabs custom-LLM at the same endpoint if OpenJarvis voice underwhelms.
4. UI cosmetics: OpenJarvis UI still self-brands "OpenJarvis" in chrome even though the brain answers as
   Gilligan (identity fix is at the endpoint layer). Reskin later if Jason wants.

## Hermes-Gilligan pilot — full WORK parity (Option A) wired + verified (04:20–04:52 UTC)
Skipper: "same privileges you do" → chose **Option A** (full work parity, keep container boundary: NO docker-socket/host-root/host-fs).
- **Crew** ported to `/opt/data/arbor-core/crew/` (secret paths repointed to `/opt/data/.secrets/`); Fable/Gemini/GLM/Kimi each live-tested (returned exact token); sol = native brain + Hermes sub-agents.
- **Hidden path trap fixed:** pilot exec runs as `HOME=/root` (EMPTY); keys/creds live in `/opt/data`. → `~/.secrets` symlink + root-owned `~/.ssh` (strict perms; SSH rejected uid-1000 files). Idempotent recovery: `/opt/data/setup-creds.sh` (run after any RECREATE; plain restart survives).
- **Play access PROVEN:** SSH jump host `Administrator@100.86.97.46` → hostname `GSTSDATABASE`; read-only query via `gsqlhost.sh 198.207.148.168,1433` → `master`. (Tailscale via host-net; key `gstsdb_ed25519`.)
- **Email WIRED + tested:** `send-email.js`/`send-files.js` + `gmail.json` → `/opt/data/arbor-core/mail/`; `npm i nodemailer`; SMTP verify OK as gilligan.gsts; real test send → jwade inbox OK. COMMS-lock still applies (draft→approve→send).
- ⚠️ gmail app-password rendered unmasked in a tool output this session — offered rotation.
- Boundary intact: no docker.sock, no host `/` mount, no host root. Two-track/COMMS locks preserved (copied SOUL lock).

## Pilot skill-safety: sync --delete trap found + closed (04:58 UTC)
Skipper asked: will Hermes-Gilligan's self-learned skills be damaged when I write to git?
- GIT is NOT the risk: pilot has its own repo; my .git excluded from sync; my commits never touch him.
- REAL trap: `sync-workspace-to-gilligan.sh` (systemd timer, every 10min) does one-way `rsync -a --delete` LIVE->pilot `home/workspace/` (his Obsidian vault). --delete would WIPE any file he authors there that isn't on my side.
- Safe zones (outside mirror): his `/opt/data/memories/` (MEMORY/USER) + `/opt/data/skills/` (15 skills). His two workspace skills survived only by luck (also exist on my side).
- FIX: added rsync `--filter='P skills/***'` + `P _pilot-own/***` (protect-on-receiver) so his authored skills/notes are never deleted, while my wiki/memory still copies IN additively. Backup: `sync-workspace-to-gilligan.sh.bak-preprotect-20260801-045800`. Dry-run proved a planted test skill is NOT deleted. Ran live: 0 bad deletes.
- Guidance for him: save authored skills to `/opt/data/skills/` (primary, fully outside mirror) OR workspace `skills/` (now protected). Avoid saving unique work loose in workspace root.

## Codex heavy-lifting for Hermes-Gilligan — Option B (isolated sibling worker) BUILT (05:06–05:50 UTC)
Skipper chose B over the repo-scoped fallback. Native Codex sandbox impossible in his container (MNT_LOCKED — needs rootless/userns-remap = blast radius). Built minimal-mount isolation instead:
- Image `codex-worker:1` (debian:13-slim+git, 66MB). Host watcher (systemd --user `codex-worker.service`) runs a throwaway `docker run` per job mounting ONLY the repo (rw) + per-job codex home (auth+config+binary, ro). cap-drop ALL, no-new-privileges, bridge net, user 1000, mem/pids limits. Codex runs --dangerously-bypass-approvals-and-sandbox (container = the sandbox).
- Queue orchestration (Gilligan has no docker socket): client `codex-worker.sh <repo> "task"` -> `/opt/data/codex-jobs/inbox/` -> host watcher -> `outbox/`. Repo allowlist host-side.
- PROVEN: isolation (worker sees only /work+auth, NOT .secrets/.ssh/memories), read task, WRITE task (file landed on host uid-1000), survived main-container recreate.
- Reverted the surgical seccomp experiment on MAIN container (B doesn't need it) -> back to Docker default. No docker-socket/host-root/host-mount anywhere.
- Files: host ~/codex-worker/*, client /opt/data/arbor-core/crew/codex-worker.sh, doc _pilot-own/CREW-ACCESS.md.

## LESSON (recurrence): the mirror --delete trap bit AGAIN
His CREW-ACCESS.md (written to workspace ROOT) was wiped by the rsync --delete sync within the hour — because my protect filter only covers skills/ and _pilot-own/, and root-loose files not in MY live workspace get deleted. This is the SECOND time the same trap fired today. Fix: his authored docs live in _pilot-own/ (protected) — verified survives sync. Standing rule for pilot docs.

## Codex worker — greenfield mode + gbt-5.6-sol/high-reasoning requirement — ✅ DONE + PROVEN (06:18 UTC)
**STATUS: COMPLETE.** All three shipped and tested end-to-end (the earlier "IN PROGRESS" note below was
superseded — the LLM timeout was only on polling the docker build, which finished fine):
- **Model lock ✅** seeded `~/.gilligan-hermes/.codex/config.toml` has `model="gpt-5.6-sol"` +
  `model_reasoning_effort="high"`, AND run-job.sh pins it again in the codex exec
  (`-m gpt-5.6-sol -c model_reasoning_effort=high`, vars CODEX_MODEL/CODEX_EFFORT). Live run reported
  `reasoning effort: high`. No weaker model can run coding jobs.
- **Greenfield mode ✅** run-job.sh is mode-aware (`mode` field; greenfield = mktemp fresh git-init scratch
  repo in container, no allowlist needed, output to `/opt/data/codex-jobs/builds/<id>-<name>/`). Client
  `codex-worker.sh --new <name> "task"`. Proven: built fizzbuzz.py+README from nothing.
- **Runtime image ✅** `codex-worker:2` (debian:13-slim + python3 + node/npm, 689MB) so Codex can
  EXECUTE/test its own code. Watcher IMAGE flipped `:1`→`:2` + service restarted. FINAL PROOF: greenfield
  job built add.py AND ran python3 to verify add(2,3)==5 → printed `5`, exit 0.
- Bug that was fixed: early `[ -n "$repo" ]` guard + allowlist block fired before greenfield branch
  (multi-block edit had only half-applied); made both mode-aware. Removed an orphan duplicate `.git` check.
- Isolation unchanged: only scratch dir + codex auth mounted; no secrets/play/host-root.
- Files: `~/codex-worker/run-job.sh`, `~/codex-worker/Dockerfile` (new, adds runtimes),
  `/opt/data/arbor-core/crew/codex-worker.sh`. TODO(minor): document `--new` in `_pilot-own/CREW-ACCESS.md`.

---
### (superseded) original in-progress note:
Skipper asked two things after B shipped:
1. **Greenfield mode** — let Hermes-Gilligan prompt Codex to BUILD something new with no pre-existing repo
   (the worker as built REQUIRES an allowlisted git repo; that was a safety choice, not a hard limit).
   Plan: a "scratch" mode where the worker spins up a FRESH EMPTY repo in the isolated container, Codex
   builds from the prompt, output returns to Gilligan. Same isolation (only the scratch dir + codex auth
   mounted).
2. **Model lock:** Codex must run **gpt-5.6-sol with HIGH reasoning** — "I do not want lower models doing
   coding work." Need to pin model + reasoning_effort in the per-job codex home (config.toml) the worker
   seeds, so no weaker model can ever run a coding job.

### State at flush (INCOMPLETE — resume here):
- **BUG found:** greenfield test failed `job missing 'repo'`. Cause: in `~/codex-worker/run-job.sh` the
  early guard `[ -n "$repo" ] || fail "job missing 'repo'"` (line ~46) fires BEFORE any greenfield branch,
  and the repo-resolution block (lines ~50-63) is still the OLD allowlist-only logic — my earlier
  multi-block edit only applied the first block. So mode=greenfield falls through to the allowlist check.
- **FIX NEEDED:** make run-job.sh mode-aware — read a `mode` field from the job JSON; when
  `mode=greenfield`, SKIP the repo requirement + allowlist, `mktemp -d` a fresh scratch repo (git init)
  inside the container instead of mounting an existing repo, run Codex there, return the built files. Also
  add `mode`/scratch support to the client `codex-worker.sh` (a `--new`/greenfield invocation) and to
  `_pilot-own/CREW-ACCESS.md`.
- **Model requirement NOT yet applied:** must verify + pin the seeded `config.toml` (the worker copies
  `/opt/data/.codex/config.toml` into the per-job home) so model=gpt-5.6-sol and reasoning_effort=high are
  forced for every job. Check the current config.toml value first; if it inherits a weaker default, override
  it in run-job.sh when seeding the job home (don't rely on the copied file).
- Was building/using the `codex-worker:1` image (npm/docker build step) when interrupted.
- Files to touch: `~/codex-worker/run-job.sh` (mode-aware + model pin), `/opt/data/arbor-core/crew/codex-worker.sh`
  (greenfield invocation), `~/.gilligan-hermes/home/workspace/_pilot-own/CREW-ACCESS.md` (document both).

## SET UP (2026-08-01 06:52, Skipper) — TRIM IT Deep Audit runway for a FRESH chat tomorrow
Skipper: tomorrow = big project. Really MAP the DB + ColdFusion — go past the high-level pass, understand
what everything does, what's USED vs not, what WORKS vs not, and start CLEANING UP. Attack it in line with
our WORKFLOW — begin at CUSTOMER CREATION. Pull all the knowledge we've already learned + add to it. May do
REAL cleanup on the DB as testing for the going-forward plan. "dont build anything yet just get all of this
set up for tomorrow in a fresh chat."
- Built the runway (NO building of the audit itself):
  - **`wiki/projects/trimit-deep-audit.md`** — the kickoff charter: mission, workflow-first method (7 stages,
    customer creation = stage 1), what we already know (708-file `~/trimit-knowledge/` + [[trimit-db-cleanup]]
    2026-07-20 quantified audit + gotchas/topology notes), access tooling (gsql.sh read-only, live catalog
    lookup), environment realities (play GSTS wiped nightly → need frozen GSTS_cleanup; prod write blocked),
    cleanup guardrails (backup-first/reversible/rehearse), and Stage-1 first-session targets.
  - **`wiki/reference/deep-audit-stage-template.md`** — repeatable per-stage note template (entry points /
    data model verified live / used-vs-dead / works-vs-broken / cleanup candidates / knowledge delta).
  - Registered as the **🔬 NEXT BIG PROJECT** at the top of WORK Track 1 in `wiki/index/PROJECTS.md` so the
    fresh chat surfaces it on startup.
- Key setup finding: the local `arbor-stack` has only dashboard/export customer `.cfm` (Dashboard-CustomerLeads,
  Export-CustomerList, Executive$Sales$Detail$Customer) — the actual customer-CREATE form lives on the PLAY
  WEBROOT, not local. Stage 1 to-do = pull it from play.
- ⚠️ memory_search (semantic) was DOWN this session (embedding provider timeout) — used grep across wiki +
  trimit-knowledge instead. Worth checking the embedding provider config.
