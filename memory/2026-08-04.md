# 2026-08-04

## 🌳 TRIM IT AI Field Suite — full design session (04:03–05:02 UTC) → resume in the morning

Skipper wants to integrate the inventory-import work into TRIM IT "sooner than later" and build a **suite**.
Across the conversation we designed the whole thing and locked every framing decision. **Build starts next session.**
Canonical: **[[trimit-ai-field-suite]]** + `~/arbor-stack/trimit-ai-field-suite/` (own git repo, pushed).

### Decisions locked this session
- **Standalone Track-1 suite that arbor-core calls on later** (Skipper 04:47) — field-facing product that consumes the arbor-core AI engine.
- **AI translation brain, NOT hardcoded** per-format mappings (04:26) — can't code every source × vocabulary; LLM proposes canonical mappings, confidence-scored, human confirms misses, learns back into a synonym store.
- **Build on Boss Herman's engine** (`one-shot-fresh-build.py`, 68 pitfalls), **NOT Jordan/Travis's ArborNote SP** (reference only; Skipper doesn't trust it / unverified). Steal only the `GSTSArborNoteSynonyms` *shape*.
- **Source correction: Goodman = Davey TreeKeeper, not ArborNote.** (I'd anchored on the wrong system.)
- **LiDAR is IN scope** (04:38) — "build LiDAR working in our app" → native iOS/ARKit field app. It's ArborNote's one real hardware moat; owning it makes it a checkbox.
- **Correctness self-validates against TRIM IT ground truth** (04:53) — real InventoryGroup/ServiceType + real 50yr pricing history + renders + sane price. **We do NOT need Brent's hand-mapped file** (it's just one arborist's opinion; ours is anchored to actual pricing history = a harder test). Brent's file = optional cross-check.
- **Design whole, assemble MODULAR** (04:53) — plug-in modules on a shared core; add/remove functions freely. This dissolved the "all at once vs spine-first" question.

### The design (in `ARCHITECTURE.md` + embedded in the wiki note)
- **CORE C1–C7:** C1 Source Adapters · C2 AI Brain · C3 Learning Synonym Store · C4 Pricing Engine (the moat) · C5 Herman Import Engine · C6 self-validating QC · C7 Confidence/Audit.
- **Module contract:** every function declares inputs · core-services-used · outputs · UI surface · enable flag · verify hook. Add = manifest row; disable = flag; remove = drop row. Core untouched.
- **v1 = the 7 ArborNote features M1–M6:** M1 Import (proven spine — Goodman 58/58 species, 9/9 service) · M2 Species-ID vision · M3 Verbal Entry voice · M4 Mass Edit · M5 Conversational · M6 LiDAR (native app, big rock).

### The 7-feature positioning (Skipper gave me back my own ArborNote-announcement read)
🟢 already-ahead: #7 Import (done), #5 Mass Edit, #6 Conversational (AI-over-our-ERP). 🟡 build: #2 Species-ID, #3 Verbal-Entry (vision/voice). 🔴 hardware: #1/#4 LiDAR. **5 of 7 play to our moat (full ERP + 50yr pricing + production data → auto-price); LiDAR is their only lead, now in scope.** Preserved in `FEATURE-MAP.md`.

### ⏸️ Parked (Skipper 05:00 — roadmap, worked later, do NOT lose)
N1 **Inventory→Bid** (the sleeper flagship — priced proposal from inventory, the thing ArborNote structurally can't do; inventory→bid is *why* our arborists flee to ArborNote) · N2 risk/priority score · N3 re-inventory diff · N4 portfolio health rollup · N5 photo→WO · N6 GPS dedup · N7 crew route optimize.

### ▶️ RESUME (morning)
- **Start the build: stand up CORE C1–C7 + land M1 Import** (the spine already needs most of the core).
- Sub-decisions still open (not blocking): LiDAR own-app vs partner; AI-brain runtime (live LLM per import vs pre-built fat synonym store + lookups).
- Kanban: card the suite on the TRIM IT board when the first build starts.
- ⚠️ `arbor-stack` is its own git repo (`gilligan-arborstack`) — commit AND push separately from the workspace.

Skipper going to a new chat; picking this up in the morning.
