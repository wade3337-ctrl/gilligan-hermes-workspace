### 2026-07-29 — Arbor Helper gets a personality (Skipper: "give the lil fella some personality")
- **His complaint:** *"it's easy to ask it a question and have it go to the stock answer... it needs to be more flexible. Like how about the Raiders? It should say Raiders SUCK!"* — and explicitly **not** a big program build.
- **Diagnosis:** `AI-Chat.cfm` routes to **nine data handlers**; everything else fell to one canned line (*"That's outside my canopy"*). llama3.2:3b was already installed and perfectly able to chat — **our code was gagging it.** No download needed; the answer was a system prompt.
- **Built:** `answerBanter()` + `ai-kb/_persona.md`. Persona lives in the KB so **he can rewrite the personality with no code change and no redeploy** (`?kbreload=1` to pick it up). Nine handlers still own every data question.
- 🚨 **My first guard FAILED in testing and I caught it live:** free chat replied *"about 80 folks in the field and around 70 more in the office."* Two faults — I had left "about 80 people in the field" **in the persona file itself** (handed it the ammunition), and the guard was a **blocklist of business nouns** ("crew", "employees") that simply missed "folks" and "office". **A blocklist cannot work: the model always finds a word you didn't think of.** Replaced with **zero digits allowed in banter**, sole exception being OC freeways by name so "the 405 is a nightmare" survives. Persona stripped of every company fact.
- **Also fixed a worse-than-deflecting bug:** the semantic fallback forced unmatched questions into the nearest of nine buckets, so *"how many people work here?"* returned **top crews by TPH** — a confident answer to a different question, with real numbers. Classifier now told that a nearly-fitting category is a serious error and `none` is a good answer (safe now that `none` means banter, not a dead end).
- **Verified live:** Raiders → *"total dumpster fire this season"* · traffic → *"the 405 is like a living thing"* · "how many people work here?" → *"Look it up yourself, I don't have a branch on that info!"* · and every data question still routes to SQL (pace, top rep, least-productive crew, uncovered goal).
- ⚠️ **Known trait:** it will invent *sports* facts (claimed a loss to the Bengals). Harmless — the guard only protects company data — but it is not a sports oracle.
- ⏭️ **Still open:** *"how many crews/trucks do we have?"* still mis-routes to a crew-TPH answer (a deterministic keyword branch grabs it before the classifier). Needs a headcount/roster handler or a narrower keyword test.
- **He laughs at his own jokes now** (Skipper, same session): the canned `fun-joke` handler appends a rotating laugh (`😂` · `🤣 I slay me.` · `😂 ...get it?` · `😂 Timber! Sorry. Sorry.` and four more), and the persona tells banter to do the same. First cut rotated on `minute()` — three jokes in one minute all came back *"I slay me"*, so it now strides `second()*3` to move every second and stay out of lockstep with the joke picker. Verified: *"Root beer. 🍺 😂 ...get it?"* / *"turn over a new leaf. 😂 Timber! Sorry. Sorry."*
- **33 dad jokes added** (Skipper: *"he should have a bunch of dad jokes too"*) — stored in `ai-kb/_persona.md`, **not** the system KB, so he can add his own with no code change. Ask for a "dad joke" or a "tree joke" and you get that kind; ask for "a joke" and both pools are in play (8 + 33 = 41).
- 🔁 **Checking the neighbours caught two more** (the rule adopted an hour earlier, working as intended): the joke **counter** still advertised *"8 tree jokes"* while 41 were loaded, and the **picker was clock-based** — asking twice inside the same second returned the identical joke, which is exactly what a person does when they want another one. Both now driven by an `application.ahJokeN` counter that advances per ask; the laugh rides the same counter so the pairing never repeats either. Verified three consecutive asks → three different jokes, three different laughs.
- Backup `D:\GSTS\Jasonsrepairs\2026-07-29-AI-Chat-prebanter.bak`.

### 2026-07-29 — RGC "Approved · unscheduled" drill listed work already finished (Skipper found it from a screenshot)
- **His report:** the drill for *Approved · unscheduled* showed a work order in progress. He was right, and it was worse than a display glitch.
- **Worst example:** **GA 214331, City of Newport Beach Grid 4 (Fashion Island)** — listed as $86,737 "approved unscheduled" while carrying **29 crew sheets, all Complete, $78,118 already produced**. GA 214745: 32 sheets, 20 complete, $42,822 produced.
- **The tell was in the header, not the rows:** the drill reported **82 rows / $713,281.37** against a tile of **$234,711.16**. The proc's own comment says the header *"must equal the summary tile."* It did not.
- **Root cause:** `rgc.usp_DrillGet`'s APPROVED branch filtered effective dates but **omitted `IsScheduledOrWorked=0`**, the flag `usp_DashboardGet` uses. **The tile was always correct** — 21 go-aheads, $234,711.16, reproduced exactly by adding the predicate.
- **Blast radius checked before fixing** (repair contract): the other three drill types — PENDING **$10,043,740.65**, STALE_PENDING **$11,397,105.84**, FUTURE_YEAR_EXCLUDED **$21,354,539.42** — already reconciled to the cent, so the defect was isolated to one branch and they were left alone.
- **Fixed + verified on play:** APPROVED drill now **21 rows / $234,711.16 = the tile**; all four reconcile; GA 214331 no longer appears; and the surviving 21 have **0 crew sheets and 0 completed work** between them. Data endpoint still 200.
- Proc backed up first to `arbor-stack/backups/play-workbench/usp_DrillGet-before-approved-fix-20260729.sql` (**`Workbench` is not in the nightly restore**).
- 🔑 **No double-count in the headline:** APPROVED_UNSCHEDULED carries `inHardCoverage=0`, so hard coverage and the $9.21M gap were never affected — the damage was confined to the pipeline drill.

### 2026-07-29 — Revenue Goal Close "shows no numbers": diagnosed and fixed — it was goal governance, not code
- **Symptom (Skipper):** every tile an em-dash. **The page has zero `cfquery` tags** — it fetches `RevenueGoalClose.data.cfm` client-side, so the HTML said nothing.
- **Three steps to root cause:** curl the endpoint → **HTTP 422 `RECONCILIATION_FAILED`** · grep the endpoint for that code → it maps a proc THROW containing *"does not reconcile"* · `EXEC Workbench.rgc.usp_DashboardGet` in a try/catch → **err 50022, "RGC: SalesGoal does not reconcile to the approved annual goal."**
- ⚠️ **Trap on the last step:** my first EXEC passed `@Scenario=''` and returned a *different* error ("unknown coverage scenario") that would have sent me the wrong way. **The page's `cfparam` default is `PROTECTED`** — reproduce with the caller's own defaults.
- **The mismatch:** `rgc.Plan.ApprovedAnnualGoal` **$24,000,000** (approved 2026-07-13) vs `dbo.SalesGoal` FY2026 twelve rows summing to **$25,300,976**. The goal was raised after the plan was approved; the plan row never was. **The page was behaving correctly — fail-closed rather than compute coverage against a wrong denominator.**
- **Verified the fix inside `BEGIN TRAN … ROLLBACK` before touching anything** — proc succeeded, all four self-reconciliation controls PASS, rollback confirmed the row unchanged at $24,000,000.
- **Applied on the Skipper's call: $25,300,976.** Old row saved to `arbor-stack/backups/play-workbench/rgc-Plan-before-goal-rebase-20260729.txt` (**`Workbench` is not in the nightly restore — that file is the only copy**). Endpoint now **200 / ok:true**, controls all PASS: produced **$13,343,214** · future scheduled **$2,748,430** · hard coverage **$16,091,644** · **hard gap $9,209,332**.
- 🔑 **Settles the authoritative FY2026 goal at $25,300,976** and retires the $24.0M. Landing-page link stays pulled until he says otherwise.

### 2026-07-29 — BOD Commitment Dashboard built on play — and three basis defects found before it shipped
**What it is:** `Dashboard-BODCommitments.cfm` — a COO cockpit tracking the four commitments in the Q2 2026 operating plan delivered to the Board on 2026-07-29. Play only, gated to **UserID 9 + 376 only** (verified: 340 and 209, who both have V1.5 dashboard access, get **403**; no cookie → 302). Design settled with the Skipper across seven decisions before any code: his cockpit · control panel + lever math · invoiced revenue headline with accrual shadow · editable monthly target ramp · both productivity gaps shown · CFO revenue stored not derived · tile 3 shipped as committed **with** a decomposition panel.
**🚨 Three defects caught during the build — each would have shipped a wrong number to a board-facing metric:**
- **The documented revenue filter returns ZERO rows.** My own spec note said `Invoices … IsProForma=0 AND IsCredit=0`. Both columns are **100% NULL** (0 of 1,501 H1 invoices), so the filter matches nothing and the tile would have read **$0**. Correct basis is `SUM(dbo.Invoices.Total)` joined to `Periods` — reproduces H1 = **$11,078,311.77** exactly. Note corrected.
- **Tile 3 cannot measure what it was committed to measure.** Numerator is `CrewSheets.ActHours` = the schedule estimate. Checked the two fields that might have saved it: **`CompletedHours` is a copy of `ActHours`** (Q2 44,498.60 vs 44,498.72) and **`ScheduledHours` is unused** (1 row of 3,305). Returned field time is invisible to it; closing stale sheets moves it. Hence the mandatory "why it moved" panel + a daily snapshot table so the split is evidence, not assertion.
- **The 5.7% gap was misdiagnosed.** Not crew-assignment drift — **2,640 of the 2,696 Q2 hours sit on 959 sheets still in `Pending`, 902 already checked in.** Steady ~850–950/month all year, 100% checked in. → `wiki/facts/pending-crewsheet-closeout-gap.md`; `crew-assignment-drift` corrected.
- **Bonus:** "revenue-bearing hours" (38,038) drops 6,461 hrs whose work orders **do** have paid sibling sheets (99.4% of them) — multi-visit jobs booking dollars once. Excluding them inflates the on-job rate from ~$137 to ~$155. Page uses the honest denominator.
**Also built:** `Workbench.dbo.BODCommitmentTargets` (editable monthly ramp, 36 rows), `BODCfoRevenue` (Controller's figure stored — ERP drift **swings sign**: Q1 −5.9%, Q2 +2.9%), `BODMetricSnapshot` (daily capture powering the decomposition).
**Render-verified served output, not the file:** clean 200, four tiles correct, Jun 2026 productivity $119.08, booked share 94.7%, monthly table foots. Three render bugs found and fixed in the served page: CF `NumberFormat` has no `"+9.9;-9.9"` mask form; a `>` inside a `<cfset>` expression closes the tag (use `GT`); and `Calendars` future-dated rows made the headcount tile report **3 people for October** until months were capped at the live month.
**Open:** headcount computes **78.1/weekday for Q2** where the board plan said "about 76" — reconcile before it is reported. Lever panel now states plainly when a gap is too big for the levers to cover rather than offering "$33 of rate".


### 2026-07-27 — Root-caused the crew stalls: the 180s watchdog was killing MY process and orphaning its subagents
- **Symptom (Skipper spotted the pattern):** three multi-agent runs each stalled **exactly 6 agents short** — 13/7, 60/54, 43/37. He called it: *"why does the crew keep stalling out at 6."*
- **The 6 is the concurrency cap.** This box has **8 cores**; workflows run `min(16, cores - 2)` = **6 concurrent agents**. Every run lost precisely the batch that was IN FLIGHT — everything already finished, finished. That is not a workflow fault; it is the whole running batch dying at once, which points at the parent.
- **The parent was being killed by the CLI watchdog** after **180s with no output** — the same `CLI subprocess: timed out after 180s (no-output stall)` errors he kept seeing. Long silent stretches (multi-agent waits, slow SSH/`Get-ChildItem -Recurse` scans, big SQL) tripped it, my process died, and the 6 in-flight subagents were orphaned. **Both symptoms were the same event seen from two sides.**
- ⚠️ **The fix was NOT where the error message pointed — two traps found by reading the source instead of guessing:**
  1. The knob is under **`agents.defaults.cliBackends`**, not top-level `cliBackends`; and the watchdog sits under **`reliability.watchdog`**, not `watchdog`.
  2. **Raising `timeoutSeconds` alone would have done NOTHING.** The 180s ceiling is `CLI_RESUME_WATCHDOG_DEFAULTS.maxMs` — a hard cap on the *resume* profile, which is what a continuing conversation uses. `resolveCliNoOutputTimeoutMs` also caps the window at **`timeoutMs - 1000`**, so the turn budget must exceed it too. Setting one without the other is useless.
- **Applied (Skipper-authorised, 6 minutes):** `agents.defaults.timeoutSeconds = 900` · `cliBackends.claude-cli.reliability.watchdog.{fresh,resume}.noOutputTimeoutMs = 360000`. The required `command: "claude"` matches the built-in fallback in `resolveClaudeCliCommand`, so it changes nothing.
- **Method:** `openclaw config patch` (validated recursive merge) with **`--dry-run` first** — no hand-editing. Verified after: **4 keys added, 0 removed, 0 changed** except `meta.lastTouchedAt`. Backup `openclaw.json.bak-prewatchdog-20260727T220041Z`. **Needs a gateway restart** (Skipper doing it).
- 🔒 **Also persisted before the restart:** the 51 suite-audit findings existed ONLY in workflow transcripts. Written to `arbor-stack/predeploy-pkg3/SUITE-AUDIT-FINDINGS.md` with a confidence warning — only 21 of 51 reached a verifier, so they are LEADS, not confirmed defects.

### 2026-07-27 — Revenue Performance fixlist COMPLETE: 31 fixed/closed, 5 deferred with reasons [PLAY ✅ · package restaged]
**Batch 5 (robustness + display) — every item verified against the code first:**
- **#1** no cap on the requested span while `requestTimeOut="6000"` (100 min) → **date range clamped to 3 years**, silently, so the page still renders.
- **#18** `Val(URL.hide)` on an undefined variable — `?action=hideWelcome` with no `hide` param threw → guarded. **Verified HTTP 200.**
- **#20** `csvField` had **no spreadsheet formula-injection guard** → values opening `= + - @` now prefixed with an apostrophe. *(The `="mm/dd/yyyy"` date cells are Excel's text-literal idiom on OUR OWN constant, deliberately left.)*
- **#25** `cfparam form.revenueSource default="ScheduleBoard"` **silently defeated the documented 7/24 intent** — the cfparam always supplied a value so the TrueProduced fallback never fired. Now genuinely TrueProduced.
- **#34** Total tile read *"N hrs; internal excluded M hrs"* while `totalHours` **INCLUDED** those hours — caption contradicted the figure beside it. Now **"281 jobs, 15,233.1 revenue hrs · 322.5 non-productive"**.
- **#19** deprecated `HTMLEditFormat` on user-editable DB text → `EncodeForHTML`, matching file B.
- **#28** a net-negative bucket (credits / posting reversals) produced a **negative SVG height**, so the browser dropped the rect and the bar silently vanished → clamped at 0; tooltip and CSV keep the true signed figure.
- **#36** pro-tip key only split TrueProduced vs everything-else, so under Crew Sheets the tile said "Estimated to date" while its tip described posted actuals → now source-aware.
- **#2** (file B) **row cap 2,000** on the list; the **TOTAL is deliberately left uncapped** so the headline stays correct even if the list truncates.
- **#21/#22/#24/#33** (file B) `showdebugoutput="false"` added · InvoiceID `URLEncodedFormat` · **rows and total now both show cents and FOOT** — $109,686.16.

**⏸️ 5 DEFERRED — verified real, deliberately NOT fixed, reasons recorded in `MORNING-FIXLIST.md`:**
- **#3 CSRF** — real, but needs a token framework, not an end-of-session patch. App is auth-gated and internal; the goal MERGE is POST-only and **#12 (the silent goal overwrite) is already fixed**, so the residual is a forged POST altering Target TPH.
- **#23 partial SalesGoal coverage** — needs a **business answer**: when only some months in a range have a goal row, should the period goal be today's partial sum, or prorated? Changing it would move the goal number on a guess. The all-NULL crash path is already inside the cftry.
- **#26** cosmetic wording on the hours-only internal view · **#27** Weekly/Monthly buckets report their FULL period boundaries (definitional — truncating would misrepresent the bucket) · **#32** an edge case never observed in any test range.

**Final state — all verified on play, 0 CF errors, 0 literal `##` on all three sources:**
Goal $2,200,000.00 · Actual $1,618,204.26 · Projected $441,932.40 · Total $2,060,136.66 · **281 jobs (Group-By independent)** · Unattributed $109,686.16 / 21.
**Package restaged and byte-identical to BOTH play webroots** — `Dashboard-RevenuePerformance.cfm` `8227d6b76a57` (109,592 b) · `Executive$Sales$Unattributed.cfm` `065c5db51f11` (11,765 b). Manifest re-stamped **as the last step**, per the lesson.

### 2026-07-27 — Revenue Performance fixlist, batch 4: posted/unposted now decided PER CREW SHEET [Skipper-directed · PLAY ✅]
**Skipper's calls:** #11 *"unposted should show as projected"* · #10 *"let's try it and see how it presents… I do not want to overstate projections."*
- **#11 — the posted/unposted decision moved from the DAY to the CREW SHEET.** It used to flip the whole day to "actual" the moment ANY crew posted, counting only the posted dollars — so crews that had not posted contributed nothing and the day understated. `qActualChunk` now groups by `CalendarID, CrewSheetID` and each sheet stands on its own: posted → its own actual; not posted → projected from its own scheduled hours. A part-posted day is now part actual, part projected, which is what it actually is. The day-level `dollarRatio`/`hourRatio` apportionment is retired for this branch.
- **#10 — non-productive hours are no longer monetized.** Projected days valued `dayHours x targetTPH` using the day's TOTAL scheduled hours (internal crews included) and handed all of it to revenue jobs.
- ⚠️ **MY FIRST CUT OVERSHOT — caught by measuring, not by review.** Treating every past sheet with no dollars as "unposted" pushed **Projected from $413,585 to $654,767 (+$241K)** and **Productive TPH from $135.44 to $149.21** — the exact overstatement the Skipper warned against. The data said why: **386 past July sheets have no dollars but carry 1,944.9 COMPLETED hours** — the signature of yard time, safety training, OJT and meetings. **Hours recorded with no dollars is ZERO-REVENUE WORK, not unposted work.** Refined to a three-way rule:
  1. dollars posted → **actual**
  2. no dollars but hours recorded → **zero-revenue work**: contributes hours, earns $0 (never projected)
  3. no dollars and no hours → genuinely **not yet posted** → projected from scheduled hours

  | | Original | First cut | **Shipped** |
  |---|---|---|---|
  | Actual to date | $1,618,804.26 | $1,618,204.26 | **$1,618,204.26** |
  | Projected | $413,585.03 | $654,767.10 | **$441,932.40** |
  | Total | $2,032,389.29 | $2,272,971.36 | **$2,060,136.66** |
  | Productive TPH | $135.44 | $149.21 | **$135.24** |

  **+$28,347 of projection** is the genuinely-unposted work now showing — believable; **+$241K was not.**
- ⚠️ **UNRESOLVED, DISCLOSED: "Actual to date" is $600 lower than the raw DB sum** ($1,618,204.26 vs $1,618,804.26, **0.037%**). Mechanism is understood — the old code apportioned each DAY's actual across its visible rows, so money on sheets the page does not display still landed somewhere; per-sheet assignment no longer leaks it. **The specific sheet was not identified.** Ruled out by query: sheets with no work order · "DAY OFF" work orders · non-Active crews · internal crews · dates outside the window · both $600 sheets trace to WO 167882 with two Active, non-internal South crews and nothing disqualifying. **Told the Skipper rather than papering over it.**
- Backup `Jasonsrepairs\persheet-20260727\RP.PRIOR.cfm`. Both webroots, cfclasses cleared, 0 CF errors. **17 of 36 closed.**

### 2026-07-27 — Revenue Performance fixlist, batch 3: chunking + status filter [PLAY ✅ verified · 2 items ESCALATED]
- **#16 status filter — applied, verified NO-OP.** `qActualChunk` read `dbo.CrewSheets` with no status filter while every other number on the page comes through the proc's `Live$CrewSheets$Thin('Active')` path. **Measured first:** July is **$1,618,804.26 / 1,093 sheets with OR without** the Inactive filter, and identical again excluding internal crews — so there is no live discrepancy. Added `AND cs.StatusDefID <> dbo.GetStatus('CrewSheets','Inactive')` as consistency insurance so a voided sheet can never quietly enter "actual produced". **Regression confirms every headline unchanged.**
- **#8/#31 (a failed chunk anchor silently drops 241 days) and #9/#30 (cross-chunk double counting) — INVESTIGATED, NOT MANIFESTING.** Both only bite past 240 days, so I ran the real test: **full-year 2026 (365 days = 2 chunks)**. 0 CF errors, 12 chart buckets, and **"Actual to date" $12,907,730.51 vs DB truth $12,914,500.51 — the dashboard is $6,770 LOWER (0.05%), not higher.** Double counting would read HIGH; a dropped chunk would show a massive shortfall. Neither is happening. The small gap + 5 fewer jobs (1,965 vs 1,970) is the proc's Active-crew filtering, which is by design. **Closed without speculative changes** — exactly the trap #15 taught this morning.
- ⚠️ **#11 and #10 ESCALATED to the Skipper — both move the HEADLINE revenue number and need a business decision, not my guess:**
  - **#11 partially-posted days.** A past day counts as fully Actual the moment ANY crew posts (`actualByCalendar[cal].dollars > 0`); crews that have not posted contribute nothing and their scheduled value is not added. My first measurement said **20 of 20 posted July days are "partial"** — but that test is **too crude to act on**: a crew sheet with $0 is often legitimately $0 (yard, safety training, OJT), not "unposted". Separating "not yet posted" from "genuinely zero revenue" needs the business rule.
  - **#10 projected dollars monetize non-productive hours.** Projected days are valued `dayHours x targetTPH` where `dayHours` is the day's TOTAL scheduled hours — which include internal/non-productive crews — and all of it is then apportioned to revenue jobs. If that is real it inflates Projected and Total. The page already tracks `internalHours` separately, so the correction would be to project on revenue hours only.
- **Running total: 15 of 36 closed.** Remaining are Tier-2 robustness (span cap, CSRF, `Val(URL.hide)`, CSV formula guard) and Tier-3 display/labels.

### 2026-07-27 — Revenue Performance fixlist, batch 2: the JOBS COUNT was inflated ~27% [PLAY ✅ verified]
- **#17 CONFIRMED and PROVEN.** `jobShareByBucket` is keyed **`bucket|workorder`**, so a job spanning buckets got one entry per bucket and the headline summed them. **The same July range reported a different job count depending on the Group By selector:**

  | Group By | Jobs shown |
  |---|---|
  | Daily | **721** |
  | Weekly | **358** ← what the Skipper had been reading |
  | Monthly | **281** |
  | **DB truth — `COUNT(DISTINCT WorkOrderID)`** | **281** |

  Hours stayed constant at 15,326.8 throughout, which is why nobody caught it — only the count moved.
- **FIX:** added a second accumulator keyed by the **job only**; the headline now counts distinct work orders with the territory rule applied once per job over its whole-range hours. **Per-bucket counts left untouched** — "jobs active in this bucket" is the correct meaning for the chart, tooltip and CSV.
- ✅ **Verified:** **281 on Daily, Weekly AND Monthly**, matching the database exactly. 0 CF errors.
- ⚠️ **#15 "North + South ≠ ALL" — INVESTIGATED, NOT A DEFECT, and I reverted my own fix for it.**
  - All crews do have a valid `SiteAssigned` (1×46, 2×14, 3×6) — no NULLs, so the "dropped crew" premise is false.
  - `141 + 137 = 278` vs ALL `281` looked like a gap, so I added a weight-based fallback for zero-hour jobs. It moved the counts to `149 + 143 = 292` — **+14 when only 3 jobs were unattributed. I could not account for the other 11, so I reverted it rather than ship it.**
  - **The premise was wrong anyway:** **49 of July's 281 jobs are worked by crews from BOTH territories**, so a shared job legitimately appears on both sides. "North + South == ALL" is not a valid invariant for a *count*. **HOURS are the reconciling measure and they tie out exactly: 7,721.8 + 7,605.0 = 15,326.8 = ALL.**
  - Left a comment at the call site recording the reverted attempt and why, so nobody re-derives it.
- **Running total: 10 of 36 closed.**

### 2026-07-27 — Revenue Performance fixlist, batch 1 of N: the GOAL & PACE cluster [PLAY ✅ verified]
Working the 36 pre-existing defects so package 3 can ship in one deploy. **Every item verified against the code before touching it.** Batch 1 = the five that make the goal/pace numbers wrong.
- **#4/#14 Target TPH of 0 was accepted.** Line 325 rejected a Monthly Goal of 0 (`LTE 0`) but line 327 only rejected TPH **below** zero (`LT 0`). Projected days are valued `hours x targetTPH`, so 0 silently zeroed EVERY projected dollar, turned every bucket Rainbow — **and persisted via the goal MERGE.** → `LTE 0`.
- **#5 Pace compared a slice of a month against the WHOLE month's goal.** `periodGoal = monthlyGoal x monthCount` regardless of range, so 7/1–7/5 demanded the full **$2.2M** "by today". → periodGoal is now scaled by the range's share of the touched months' working days. **A full-month range is mathematically unchanged (factor 1.0)** — which the regression test confirms.
- **#6 A range with zero working days demanded the full goal.** `pacedTarget` initialised to `periodGoal` and the proration was skipped when `workDaysTotal = 0`, so a single Sunday or lone holiday asked for a whole month's revenue. → `pacedTarget = 0`.
- **#7 The holiday list was hardcoded to 2026.** `holidays2026` = 7 literal dates; any cross-year range counted holidays as working days and **the whole pace calc would have gone stale on 2027-01-01.** → replaced with `holidayListFor(year)` deriving observed US holidays by rule for every year the range touches.
  - ⚠️ **My first rule was WRONG and I caught it by validating against the data it replaced.** I shifted only Sunday→Monday, which produced `2026-07-04`; the hardcoded list had **`2026-07-03`** — proof GSTS shifts a **Saturday** holiday BACK to Friday. With `Sun→Mon AND Sat→Fri` the rule reproduces **all 7 of the 2026 dates exactly**. *(The old list was the only evidence of company observance policy — worth keeping in mind that deleting "hardcoded" data can delete the spec.)*
- **#12 The seasonal read overwrote AND SAVED the user's monthly goal.** `monthlyGoal = qSeasonGoal.AV` replaced the user's figure with an **average** across the selected months; the Monthly Goal input renders `#monthlyGoal#`, so the next bar click / filter change re-POSTed that average and the MERGE **wrote it over their stored goal — corrupting a persisted setting just by browsing.** → the seasonal figures drive `periodGoal` only; `monthlyGoal` stays the user's own value.
- ✅ **Verified on play, 0 CF errors on every case:** full month **$2,200,000.00 (unchanged — the regression that matters)** · 5-day slice **$253,846.15** goal / pace −$3,019 (was −$1.95M) · single Sunday **$0.00 / +$0.00** · cross-year 12/20–1/10 renders clean at **$643,137.25** · Target TPH 0 falls back to 130 with numbers intact.
- Backup `Jasonsrepairs\fixlist-goalpace-20260727\RP.PRIOR.cfm`. Both webroots + cfclasses cleared.

### 2026-07-27 — Sales Cockpit customer pop-out never loads on PROD: another undeployed Workbench table [FIX BUILT + PROVEN · in the batch]
- **Symptom (Skipper, screenshot):** the CUSTOMER pop-out panel on `Dashboard-SalesCockpit.cfm` sits on "Loading…" then "Could not load profile" — **on prod only**. His read: *"I think we may be pointing at something we did not deploy."* **Correct.**
- **Cause:** `Dashboard-SalesCockpit.Profile.cfm` line 20 carries a correlated subquery `(SELECT TOP 1 bq.Status FROM Workbench.dbo.BidQueue …)`. **`Workbench.dbo.BidQueue` was never deployed** — prod's `Workbench` holds exactly one table, `ProposalOriginalRep`, from package 2.
- **Why it fails TOTALLY rather than partially:** the entire endpoint is inside **one** `<cftry>` whose `<cfcatch>` returns `{"ok":false,"error":…}`. One missing table therefore discards the whole profile payload — contacts, properties, everything — not just the `BidStatus` column.
- ✅ **FIX = create the table EMPTY. Proven, not assumed:** only **14** locations on play have a BidQueue row at all, so I called the live endpoint for two companies with none — the same shape an empty table presents — and both returned **`ok=true` with full data** (274 properties / 629 contacts, and 270 / 86) and `BidStatus=null`. An empty table fully restores the pop-out; Bid Status simply renders blank until that feature ships.
- **Schema copied exactly from play** (11 cols, IDENTITY PK, `RequestedAt`/`Status` defaults, + a `LocationID` index). **No play rows ship** — those 16 rows are our working data, not production truth.
- **Folded into the SAME database visit** (Skipper's batching rule): the rgc script was renamed `01-create-workbench-objects-PROD.sql` and now fixes **both** prod faults in one run. **Re-proven from a cold scratch DB** — created `ZZ_WbTest`, ran it in, confirmed the 3 objects, `BidQueue` row count 0, `vProjectMarket` 12,824 rows, and ran **the pop-out's actual line-20 subquery** against the empty table → `BidStatus = NULL`. Scratch DB dropped (verified).
- ⚠️ **TWO false positives I caught before reporting them:** my cftry-depth heuristic flagged `SalesGoal` as unguarded in **both** `Dashboard-RevenuePerformance.cfm` and `Dashboard-SalesGoals.cfm` — **both hits were inside comments** (one a `<!--- --->` block my regex did not span). Had I trusted the scanner I would have told the Skipper a third page was broken when it is not. **Read the call site; do not trust the pattern match.**
- **Verified-safe (by reading each call site):** `SalesGoal`, `DashboardAccess`, `SpmBookSnapshot` all sit in `cftry` blocks that degrade cleanly. **Only SPM Results and this pop-out actually break on prod.**

### 2026-07-27 — iPad landing-page menu bug root-caused: a trapped z-index, not a touch problem [PLAY ✅ deployed · ✅ SKIPPER-CONFIRMED on IPAD **and DESKTOP** 2026-07-27]
- **Symptom (Skipper):** on iPad, opening a left-menu item makes the flyout render **underneath the Daily Close notepad**. Desktop is fine.
- **Structure:** the notepad is **two iframes deep** — shell `Profile$Main.HiRes.cfm` → `#CenterContentDiv` → `iframe ProfileBaseFrame` → `Overview/Quick$Base$WithList.cfm` → **`iframe DailyCloseFrame`** (`Profile.My.DailyCloseActions.Content.cfm`).
- **ROOT CAUSE — two parts:**
  1. **The submenu's own z-index is inert.** Spry sets the flyout to `z-index:1020`, but its container `#ActionMenuDiv` has `position:absolute` **+ `z-index:7`**, which **creates a stacking context**. The 1020 is trapped inside and cannot compete with anything outside the menu. The number that actually competes is **7**.
  2. **Nothing explicitly orders the menu above the content column.** `#CenterContentDiv` was `position:relative` with **NO z-index (auto)**. The menu won only via the implicit "positive beats auto" rule — correct per spec, honoured by Chrome/desktop Safari (**which is exactly why desktop works**), but **iOS Safari promotes iframes to their own compositing layers and does not honour implicit ordering across them.** Compounded here: nested iframes carrying fixed pixel heights (`750px`, `1735`), another long-standing iOS sore point.
  - Checked and **ruled out** the other iOS culprits: no `transform` / `opacity` / `filter` / `will-change` on `body`, `.gsts-shell`, `#LeftNavWrap`, `#ActionMenuDiv` or `#CenterContentDiv`.
- ⚠️ **I fingered the WRONG element first.** I blamed `#apDiv5` (`position:fixed`, bottom-left, `z-index:20`), built a headless-Chrome repro and *proved* the flyout loses to it — then checked what it renders on prod: **"Availability"**, the vestigial widget from 7/23, not the notepad. **The repro was right about a real defect, wrong about this one.**
- 🐛 **BONUS — a second, genuine collision found by that wrong turn:** the menu also loses to `#apDiv5`, and **on desktop too** (menu item ~24 of 46 at 1080px tall; ~item 11 on iPad). Proven via `elementFromPoint` at three viewports. The z-index bump fixes this as well; removing the widget (the 7/23 QoL fix) also kills it independently. ✅ **Skipper confirmed desktop working 18:39** — so the `apDiv5` collision was real and is now closed by the same two-line change. The wrong turn earned its keep.
- **FIX (CSS only, no logic/markup):** `#ActionMenuDiv` **z-index 7 → 100** · `#CenterContentDiv` **gains explicit `z-index: 0`**. Make the ordering explicit on both sides instead of relying on auto. Both rules carry a comment explaining the stacking-context trap and "do not lower below 21".
- **Deployed to play** (single copy — `Get-ChildItem` confirmed **no shadow copy** of this file). **Byte-transparent patch** (latin-1 read/write): **non-ASCII 15 → 15 preserved**, 41,572 → 42,408 b, md5 `256c2e9619216e25aa676f47eb1266bf` — on-disk hash matches what I built. `cfclasses` cleared. Backup: `Jasonsrepairs\ipad-zindex-20260727-1830\Profile-Main-HiRes.cfm.PRIOR`.
- ✅ **Hash-convention checked** (last night's trap): the `<style>` block is at **cfoutput depth 0**, so single `#` is correct — 17 pre-existing single-`#` tokens beside the edit, and **zero `##` introduced**. CF `application.log` shows **no new compile error** after the deploy.
- ⚠️ **NOT self-verifiable:** this shell **bounces the read-only bot to login** (316-byte redirect), so no served-render check is possible — and my browser is **Chrome, which renders the *unfixed* code correctly**, so it cannot reproduce a WebKit-on-iOS compositing bug either. **Verification depended entirely on the Skipper's iPad test — he confirmed it working 18:35 UTC.** The CSS diagnosis held: it was a trapped stacking context, not a touch/event problem.
- **Batch:** folded into `trimit-ui-qol-fixes/bug1+ipad-zindex.diff` — that file now carries BOTH the widget removal and this fix. Section C of `predeploy-pkg3/OUTSTANDING-FOR-PROD.md`.

### 2026-07-27 — PROD CRASH root-caused: SalesProductionMeeting$Results dies on a Revenue-Goal-Close view that was never deployed [FIX BUILT + PROVEN]
- **Symptom (Skipper, on production):** the Sales Production Meeting **Results** tab crashes outright.
- **Error:** `208 Invalid object name 'Workbench.rgc.vProjectMarket'` at `SalesProductionMeeting$Results.cfm:116`.
- **Cause:** that page `LEFT JOIN`s an **RGC-subsystem view** to bucket work orders by market (COMMERCIAL / HOA / MUNI_CITIES / MUNI_OTHER / UNCLASSIFIED). **Revenue Goal Close was deliberately held out of the V1.5 deploy**, so the `rgc` schema was never created on prod — but this page still needs one view from it. The join is **UNGUARDED**, so the page dies instead of degrading. Play works because play's Workbench has the full `rgc` schema (32 objects).
- ⚠️ **MY SCAN WAS TOO NARROW AND MISSED IT.** I first grepped only `Workbench.dbo.` and reported 5 dependencies — `rgc.vProjectMarket` is in a **different schema** so it never appeared. I had even told the Skipper the Workbench gap was "probably not this bug" because `SpmBookSnapshot` (the `dbo` one on that page) *is* try/caught. **The error text he pasted settled it in seconds.** Rule: when scanning for cross-database dependencies, match `Db.<anyschema>.<obj>`, never a hardcoded schema.
- **Full dependency picture (all schemas, package 1):** `dbo.DashboardAccess` ×7 · `dbo.SalesGoal` ×6 · **`rgc.vProjectMarket` ×3** · `dbo.SpmBookSnapshot` ×3 · `dbo.BidQueue` · `dbo.Todo`. **Only `ProposalOriginalRep` (package 2) was ever deployed to prod** — that is why Travis created the `Workbench` DB there on 7/26 13:23.
- **FIX BUILT:** `arbor-stack/dev-tasks/spm-results-prod-fix/01-create-rgc-vProjectMarket-PROD.sql` — creates **only** what that page needs: `rgc` schema → `rgc.MarketMap` (21-row lookup, seeded from the working play table) → `rgc.vProjectMarket`. **Does NOT install Revenue Goal Close.** Idempotent, transactional (`SET XACT_ABORT ON`), seeds only if empty, `THROW`s and rolls back if the row count != 21. `CREATE OR ALTER VIEW` wrapped in `EXEC()` so it stays inside the transaction instead of needing a `GO`.
- ✅ **PROVEN FROM NOTHING, not just re-run where it already worked:** created a scratch database `ZZ_RgcFixTest`, ran the script into it cold, and queried the resulting view — **COMMERCIAL 6,418 · HOA 3,626 · MUNI_OTHER 1,438 · MUNI_CITIES 734 · UNCLASSIFIED 608**. Scratch DB dropped afterwards (verified gone). *(Directly applying my own lesson: validate a script against the state it will actually meet, not the state where it already passes.)*
- ▶️ **Needs from Travis:** run it against **Workbench on prod**, plus a `GRANT SELECT ON SCHEMA::rgc` to whichever login the CF `GSTS` datasource uses — without the grant the page fails with **916** instead of 208. Same shape as the `Workbench` grant Steve's dashboard needs; **bundle them into one database ask.**

### 2026-07-27 — Package 1 landed FILES ONLY: the menu/permission half was never deployed to prod
- **Context:** Skipper noted nothing links to the V1.5 suite from the TRIM IT landing page. Checked prod directly (read-only via `GSTSREADONLY` on `.168`) — **he is right, and here is why.**
- **`dbo.AppForms` on prod has exactly ONE V1.5 entry:** `AppFormID 1287 · "City Budgets" · /GSTS/Dashboard-CityBudgets.cfm`. **The other six dashboards have no menu entry at all** — V15Home, RevenuePerformance, SalesCockpit, CrewPerformance, ProductionPerf, the Exec suite. (City Budgets almost certainly predates this package — RC-03 shipped separately.) So the suite is reachable on prod **only by direct URL**.
- **`GoalSettings` IS present on prod** (the RevenuePerformance page auto-creates it in a `cftry`, so this may be self-healing rather than deployed).
- **`Workbench.dbo.DashboardAccess` — UNDETERMINED.** `GSTSREADONLY` cannot read `Workbench` on prod (error 916), so I cannot see it. **It does not matter much either way:** `dashboard-access-check.cfm` wraps the lookup in `<cftry>` and falls back to a leadership check if the table is absent — verified by reading the file. **No landmine.**
- ⚠️ **TWO WRONG ANSWERS ON THE WAY TO THE RIGHT ONE — both from guessing at schema instead of reading it:**
  1. First checked `OBJECT_ID('dbo.DashboardAccess')` → "MISSING". **Wrong name** — the table lives in **`Workbench`**, which I cannot even read. The "finding" was an artifact.
  2. Then searched `AppForms.Desc1` for page names → "0 entries". **Wrong column** — the path lives in **`ObjectPath`**. Correct answer is 1, not 0.
  **Rule: read the schema (`sys.columns`) BEFORE searching a table you did not build.** Both errors would have been reported as alarming findings if I had not re-checked.
- ▶️ **OPEN for the Travis conversation:** the remaining deploy step for package 1 is the **menu/AppForms wiring** (+ the `DashboardAccess` seed if it is not already there). `release-candidates/RC-AppForms-ExecFrame-rename-PROD.sql` + `RC-DashboardAccess-create-seed-PROD.sql`. Until that runs, the team cannot reach six of the seven dashboards.

### 2026-07-27 — RENDER SWEEP of the whole V1.5 package on play (the `##`-class blind spot) [CLEAN + 2 findings]
- **Why:** the `##` bug reached production because we verified *numbers* and *error logs*, never the rendered page. That blind spot applied to all 40 `.cfm` in package 1, **none of which had been render-checked** — and the team has not opened them yet (Travis deployed today), so first impressions were still recoverable.
- **Swept 28 user-facing pages on play.** Checked served HTML for: CF errors · literal `##` · raw `&##NNNN;` entities · unresolved `#var#` · blank output.
  ✅ **0 errors · 0 literal `##` · 0 raw entities · 0 unresolved variables.** Clean.
- **5 "suspiciously small" responses all verified as CORRECT, not breakage:** `SalesCockpit.Profile`/`.Search` = JSON API endpoints returning valid `{"ok":true}` · `Dashboard-SalesGoals` = **403** + styled restricted card · `Dashboard-Access` = "Access admin is restricted" · `Executive$Sales$Detail$Customer` = **302 redirect** to the Financial Overview frame (0 bytes is the redirect, not a blank page). **Checked the HTTP status rather than reporting a false finding off the byte count.**
- **All 8 package assets serve 200** (gsts-tokens.css · tree-helper-v1.png · protips css/js/json · pro-tips.js · mtab-bar-chart-style.js · **chart.js@4.4.1 205,399 b**). CityBudgets export returns a valid XLSX. *(The 5 SPM assets that 404'd on prod in June are present here.)*
- 🔍 **METHOD CHECK — the sweep tested PLAY's files, so I verified they equal the PACKAGE prod got. 40 compared, 4 differ:**
  - `Dashboard-RevenuePerformance.cfm` + `Executive$Sales$Unattributed.cfm` — **expected**, these are package 3.
  - ⚠️ **`Dashboard-V15Home.cfm` — PROD IS BEHIND.** Play has the Arbor Helper live to-do hook (`window.ahTodoInsert` + the `todo-add` branch) added 2026-07-22; the package was built 7/19. **So on prod, a to-do saved by Arbor Helper will NOT appear in the "Get it done today" widget without a page reload** — a feature we built and shipped past.
  - ⚠️ **`Profile$Main.HiRes.cfm` — PROD IS BEHIND.** Play has the vestigial "Availability / August 1st" widget removed (7/23, QoL Bug 1); the package has it still present. **Travis deployed the 7/19 shell today, so prod still shows the widget the Skipper asked to have removed.** The fix has been sitting staged in `trimit-ui-qol-fixes/` awaiting a go.
- **Caveat stated honestly:** rendered as read-only bot UserID 376, so permission-gated pages showed me their gate rather than their content — those gates render correctly, but their full content is unverified. For the 4 differing files, the sweep exercised play's version, not prod's.

### 2026-07-27 — Revenue Performance: Crew Sheets relabelled so an ESTIMATE stops being shown under the word "Actual" [PLAY ✅ render-verified]
- **The bug (Skipper spotted on prod):** switching Revenue Source to **Crew Sheets** moved the "Actual to date" number. Line 581 applies the actual-completed override to `ScheduleBoard` and `TrueProduced` only — `CrewSheets` falls to the `else` and takes `row.csValue` **raw**: the crew sheet's *scheduled* value, with no posted/pending bucketing and no hours x TPH projection. **It never substitutes posted actuals on past days**, so its "to date" figure is an estimate wearing the label "Actual".
- **Decision (Skipper, option b):** keep CrewSheets as a deliberate **raw lens** and relabel, rather than extend the override. Rationale: the three sources exist to be different views — hiding the difference is how people stop trusting the number later.
- **Changed (labels only — no query, no calculation touched):** KPI tile relabels to **"Estimated to date"** under CrewSheets · sub-caption gains *"· raw crew-sheet estimates, not posted actuals"* · dropdown → **"Crew Sheets (raw estimates — not actuals)"** · `revenueSourceLabel()` → "Crew Sheets (raw estimates)" with a comment recording why it is exempt.
- **Verified by POST on play** (⚠️ the source selector is a **POST form** — a GET query string silently does NOT switch it, which made my first three "renders" identical and nearly read as a failed fix):

  | Source | Tile | Amount |
  |---|---|---|
  | TrueProduced | Actual to date | **$1,618,804** |
  | ScheduleBoard | Actual to date | **$1,618,804** (identical — override working) |
  | CrewSheets | **Estimated to date** | **$1,670,085** |

  **The $51,281 gap is the whole point** — that is what was being displayed as "Actual". 0 CF errors, 0 literal `##`, all three sources.
- Both webroots + package MD5 `92c08806f3de3eb7deb89ec119dad056` (96,505 b). Backup `Jasonsrepairs\crewsheetlabel-20260727-0326\`. `cfclasses` cleared.
- **Also closed:** the "header name bug" the Skipper saw on the Unattributed drill was **prod running the OLD file** — its header is `Unattributed Sales · ##ThisPeriod.Desc1##`, fed by Period/Quarter/Year IDs the V15 parent no longer sends (it links with `ZStartDate`/`ZEndDate`), so the name resolves to nothing. **Package 3's version already fixes it** — renders "Unattributed Sales · 7/1/2026 – 7/26/2026", $109,686 / 21 invoices, 0 errors. No new work needed; it ships with the package.
- 🆕 **Found, not yet fixed:** the drill's *Current Rep* column prints a raw DB flag to users — `RG · Raudel Gutierrez (IsMeasured=0)` (line 152). Awaiting the Skipper's nod.

### 2026-07-27 — Play website repointed to its OWN SQL instance: Steve's dashboard un-broken + play is a sandbox again [Skipper-authorised]
- **Problem:** Steve's `FinancialReport/FinancialReportDashboard.cfm` **500'd on play** (worked on prod). SQL error **916** — `GSTSREADONLY` cannot reach the `Workbench` database. The page carries 3 `LEFT JOIN Workbench.dbo.ProposalOriginalRep` (564 reviewed original-seller overrides), and **that table lives on the play box's own SQL instance, not on `.168`.** Play's copy of the page was NOT stale — byte-identical to our package (`0f4d331d…`); it simply couldn't run.
- **Root cause found by diffing `neo-datasource.xml` against the `.bak` CF wrote at the same second:** the 2026-07-25 18:35 vendor edit changed **exactly one DSN** — `GSTSAPI` from `localhost:14333/sa` → `198.207.148.168/GSTSREADONLY`. **`GSTS` was already on `.168` before that.** So the whole play website was running **read-only against a remote server across the lossy Ayera tunnel** — no writes possible, no `Workbench`, and every query paying the tunnel's packet loss. Skipper: *"It's stupid slow and I can't work with it that way."*
- **Change (authorised 02:28 UTC):** `GSTS` **and** `GSTSAPI` → **`localhost:14333` as `sa`** (the play box's `GSTSDATABASE`: nightly GSTS restore + `Workbench`). **`GSTSREADONLY` left untouched** as the explicit remote read-only link. `sa` password blob copied from the pre-7/25 `GSTSAPI` entry — known-good for this box, nothing invented. Diff confined to **32 fragments**, all inside the two DSN structs; verified before upload.
- **Verified after CF restart — 0 DB errors on all 6:** Steve's **500 → 0.8s**; its Sales-Performance tab (`view=2`) returns 2.1 MB with **override rep names resolving** (Scott Griffiths ×424) = `Workbench` genuinely reachable. RevenuePerformance 1.7s · SPM 0.7s · ByRep$Scope 0.8s · ProductionPerf.Day 1.6s · SalesCommand 13.9s (heavy, pre-existing).
- ⚠️ **Consequence:** play now serves the **nightly restore (data through 7/25)**, not `.168` — expect play numbers to lag. Play can **write** again.
- **Revert:** `bash ~/arbor-stack/dev-tasks/play-dsn-revert/REVERT-play-dsn.sh` (byte-restores the as-found file, backs up first, restarts CF). Backups on the box at `lib\dsn-backup-20260727-0230\` + local copies. Skipper is telling the vendor tomorrow. ⚠️ **The CF restart makes ssh appear to hang ~90s — the restart completes server-side regardless; check the process start time, don't re-issue.**
- 🔓 **Security item for IT (pre-existing, left alone):** the `GSTS` datasource's **`description` field holds a plaintext password**, readable by anyone with file or CF-Admin access. Raise with Travis.

### 2026-07-27 — Revenue Performance `##` render bug fully fixed on PLAY (18 literal `##`, not 13) [PLAY ✅ render-verified · package 3 now proven]
- **What:** the 2026-07-26 `##`→`#` patch (Skipper spotted the raw `&##9662;` / missing brown gradient on PROD) had been written **into the package file only and never deployed to play** — so it had never been rendered anywhere. Package copy differed from live play by exactly those 13 lines / 19 chars.
- **Deployed pass 1 to play (both webroots, backup-first), then fetched the SERVED HTML — still 18 literal `##`.** Pass 1 missed **lines 1381–1384** (the 4 KPI tiles: Productive TPH / True TPH / TPH Drag / Non-Productive — `##6b7280`, `##888`, `##5C743D`, `##92400e`, `##B00020`) and **line 1389** (the `cfloop` emitting the 6 non-productive category `<th>`s → `##bbb` ×6). **Why they hide:** the `<cfoutput>` tags on those lines wrap *only the variable*, so the surrounding HTML is outside `cfoutput` and the doubling survives. Line 1410 looks identical but IS inside `cfoutput` — verified via its rendered output and left alone.
- **Verified (served HTML, `view.sh`):** 0 CF errors · **0 literal `##`** · KPI tiles `color:#5C743D` $135.44 / `color:#92400e` $132.63 · `&#9656;`/`&#9662;`/`&#128338;` resolve · all 6 category headers draw. Both webroots MD5 `7f71dcbc127fc216705d81cdca677790` (95,791 b) == package. `cfclasses` cleared both passes.
- **Backups:** `Jasonsrepairs\hashfix-20260727-0155\` (`.D-drive` + `.C-drive`, 95,823 b each). **Cosmetic only** — no query/number/logic touched.
- **Deploy state audit (same session):** the "first 2" packages are confirmed staged on play at `D:\GSTS-Deploy\` — **`V15-DASHBOARDS-DEPLOY-20260719`** + **`STEVE-FRD-DEPLOY-20260721`**. **Package 3 (`NEXT-DEPLOY-20260724`) has never been staged there.** Its other file, `Executive$Sales$Unattributed.cfm`, checks out clean: package == play live (MD5 `4656485…`), `dashboard-auth-gate.cfm` still line 1 (no security regression from the 7/24 rewrite), and the only caller (`Executive$Sales$ByRep$Scope.cfm`) passes `ZStartDate`/`ZEndDate` — no orphaned Period/Quarter/Year entry point.
- **Manifest corrected** — it advertised a stale MD5 (`fea943a…`) that predated the patch; Travis checksumming against it would have mismatched.

### 2026-07-23 — Project page header overlap fixed: title now flows above the tabs instead of a rigid box (Bug 2 of 2, Skipper) [PLAY ✅ Skipper-confirmed · staged for PROD]
- **What:** on `Profile.Project.Detail.cfm` the project title (`#apDiv`) sat in a **fixed 535×29px absolute box** (Dreamweaver-era), with the tab strip (`#MainDiv`) hard-pinned at `top:43px`. Long/padded project names (e.g. `==Irvine Unified School District Portfolio==`) wrap to a 2nd line, overflow the 29px box, and **spill onto the tab bar**. NOT a play regression — reproduced identically on **prod** (Skipper sent side-by-side play vs prod pics); it's a long-standing fragility that shows on any long-named project.
- **Fix (option A — proper rework):** moved `#apDiv` (title) + `#apDiv3` (HTD) into a new flowing flex bar `#ProjHdrBar` at the top of `<body>` (title `flex:1 1 auto` grows with text, HTD pinned right); changed `#MainDiv` from `absolute top:43` → **`position:relative`** so the tabs flow *below* the header at any title height. Also removed a pre-existing stray `</div>`.
- **Verified (served HTML, bot render of proj 1105465):** 0 CF errors; `#ProjHdrBar` renders before `#MainDiv`; `#apDiv`/`#apDiv3`/`#MainDiv` each once (no dupes); CF vars resolve. Byte-exact scp round-trip, file pure-ASCII (0 non-ASCII), 74530→75253 b. Backup: `Jasonsrepairs\Profile.Project.Detail.cfm.hdroverlap-bak-20260723-135937`.
- **Scope:** PLAY ONLY per Skipper. **Skipper visually confirmed** on worst-case Irvine project (2-line title, tabs sit cleanly below, HTD on right). **Both this + Bug 1 staged for prod** → `trimit-ui-qol-fixes/DEPLOY-PACKAGE.md` (surgical diffs + patched/original files; Jordan applies the DIFFS to prod's own files, NOT blind-copy — play's shell carries other modernization prod lacks). **Prod deploy on HOLD until Skipper says go.**
- **Related/future:** same fixed-box header pattern likely on sibling detail pages (Profile.WorkOrder.Detail, Profile.GPSWorkOrder.Detail, Location) — separate pending play task, not bundled.

### 2026-07-23 — Profile landing: removed the vestigial "Availability / August 1st" widget (Bug 1 of 2, Skipper)
- **What:** the bottom-left "Availability · August 1st" box on the Skipper's TRIM IT profile landing. Traced it to the `ProfileWidgetsFrame` iframe (`<div id="apDiv5">`, fixed bottom-left) in the shell **`Profile$Main.HiRes.cfm`**, which loaded **`Maint-Customer-Broadcast.cfm`** — a "Customer Broadcast" widget that prints one `dbo.SystemLists` row (`Scope='CustomerBroadcast'`, Desc1="August 1st") under an "Availability" label. Only UserID 32 could ever edit it; everyone else just saw it. Vestigial (+ it even had a stray `"` in its markup).
- **Fix:** commented out the `apDiv5`/`ProfileWidgetsFrame` block in the shell (widget file left on disk). **Byte-preserving in-place patch** (Latin1 transparent read/write via `Jasonsrepairs\patch_remove_widget.ps1`) so the file's 15 non-ASCII bytes were untouched — verified 15→15, len 41577→41572, 0 dangling refs (orphan `#apDiv5` CSS rule left, inert).
- **Scope:** widget lived ONLY in `Profile$Main.HiRes.cfm`. The sibling shells (`Profile$CrewLeader$Main.HiRes`, `Pad$Profile$Main.HiRes`, `Profile$Main`) embed a *different* frame (`Profile$Widgets.cfm` = perf widgets, Closing % etc.) — correctly left alone.
- **Verified:** not C:\-shadowed (D:\ serves). Backup: `Jasonsrepairs\Profile$Main.HiRes.cfm.avail-widget-bak-20260723-131809`. PLAY only (.cfm persist nightly). Shell bounces the read-only bot to login, so served-render self-check not possible — Skipper to hard-refresh + confirm.

### 2026-07-23 — Play parity: applied Jordan's Lat/Long client-side fix to the PLAY copy of Profile.Location.Update.New.cfm
- **What:** play still had the OLD broken gate (Jordan's dev shipped their fix to PROD only). Reproduced the 2 core changes on play so our sandbox matches: line-514 role gate broadened `findNoCase("inventoryadmin"…)` → `findNoCase("admin"…)` (+ inventorydataspecialist); null-guarded `codeNewAddress()` so it bails cleanly when the Lat/Long inputs aren't rendered (non-admin) instead of crashing.
- **Not replicated:** their exact async-submit-race handler (`ensureLatLongBeforeSubmit`) — can't reach prod files to byte-copy, and a mis-built onsubmit risks breaking submits. For a true byte-match, get Jordan's actual file and deploy verbatim.
- **Verified:** backup `Jasonsrepairs\Profile.Location.Update.New.cfm.bak-prodparity-<ts>`; deployed; page renders 0 CF errors. PLAY only (.cfm persist nightly).

### 2026-07-23 — Field-app tree dots fixed: `AND 1 = 0` was disabling the tree-dot query for EVERY project
- **What:** `FieldApp/Field.Map.Map.Municipal.Desktop.cfm` — the `SelectedPoints` query that feeds every tree marker (→ `store_locations` → `newTreeMarker` loop) had a hardcoded **`AND 1 = 0`** in its WHERE clause → returned **zero rows for all projects, always** → the field-app map loaded but showed **no tree dots for anyone** (even fully-set-up properties like Alicia Business Center).
- **Why it mattered:** it MASKED all the GPS/import work — made it look like Herman's inventory (missing FK chain) was the problem, when the app itself couldn't draw a single dot. Root-caused by reading the actual render path, not guessing at data.
- **Fix:** removed the `AND 1 = 0` line. Backup: `FieldApp\Jasonsrepairs\Field.Map.Map.Municipal.Desktop.cfm.bak-<ts>`.
- **Verified (play):** Skipper hard-refreshed Alicia → **tree dots returned.** PLAY only (.cfm persist through the nightly DB refresh). **✅ Prod confirmed FINE** (Skipper: crews see dots on prod) — so the `AND 1 = 0` was a stray *play-only* dev edit; this just restored play to match prod. No prod deploy needed.
- **Separate/remaining:** Herman's Fullerton pilot still needs the real GPS import (`Process$IMP$Standard`) to populate the zip-region/season/service FK chain before *its* dots show.

### 2026-07-23 — Boss Herman PLAY write access ACTIVATED (was staged Jul-10, never wired)
- **What:** Fixed the 2 gaps blocking Herman's play-write. His RW key (`herman_trimit_rw`) sat at `/opt/data/home/.ssh/` but `gsql.sh` reads `$HOME/.ssh` (`/opt/data/.ssh`, since HOME=/opt/data) → copied key there (600, hermes); seeded play's host key into the container `known_hosts` (accept-new). Both on the durable `~/.hermes:/opt/data` mount → survive recreate.
- **Verified from the container:** `gsql.sh` reads GSTS; write proven — login `GSTSDATABASE\Administrator`, `HAS_PERMS_BY_NAME` UPDATE dbo.Locations=1, INSERT dbo.Projects=1. Tools: `gsql.sh` (read+**write**), `view.sh` (render-verify), `deploy-to-play.sh` (webroot, auto-backup).
- **Level:** FULL Administrator on **PLAY** (integrated auth = sa) — the Skipper's Jul-10 "same access Gilligan has" grant, reaffirmed 2026-07-23 (play-only). **PLAY ONLY** (prod has no write path anyway). Nightly play revert = safety net. Guardrails doc: `herman-gateway/HERMAN-PLAY-WRITE-SETUP.md` (backup-first · render-verify · play-only · log-it · ask-before-destructive). Revoke = remove Herman's key line from play `administrators_authorized_keys` (Gilligan's key is separate/unaffected).
- **Unblocks:** Herman Stage 3–5 of the Goodman bid build (create/split projects, load RFP inventory, GIS, e-Traveler). Arbor-core runtime upgrade → also fold into `arbor-knowledge` boss-herman-runtime.

### 2026-07-23 — Profile-pic reapply cron: fix timezone window (was missing the restore)
- **What:** widened the `reapply-play-profilepic.sh` cron from `*/30 3-6 * * *` → **`*/30 * * * *`** (every 30 min, all day).
- **Why:** the reapply window was **3–6 UTC**, but the `GSTS DB RESTORE` runs **3 AM Pacific ≈ 10–11 UTC** — *after* the window. So the restore wiped the pointer and nothing re-applied it until the next day's 3–6 UTC (before the next restore) → pic blank all day. Log proof: 07-22 runs 04:34–06:30 UTC all rowcount 0 (pic still intact then); wipe came later, unreapplied. Same all-day pattern as the HermanRO regrant cron (which never had this issue).
- **Verified:** re-applied now → `Profiles.ProfilePicturePath='art/profilepictures/jwade001.jpg'` (UserID 9); image serves HTTP 200 (50,927 b). Crontab preserved (46 lines). Cost: 48 idempotent runs/day (rowcount 0 when not needed) — negligible.

### 2026-07-22 — Location Lat/Long geocoder fix: dead keyless-Google → free US Census [SUPERSEDED 2026-07-23 — see note]
> **⚠️ SUPERSEDED / RETIRED (2026-07-23):** Jordan's dev fixed the *reported* bug on PROD a different (better-targeted) way — **client-side** in `Profile.Location.Update.New.cfm`: broadened the line-514 role gate to any admin + null-guarded `codeNewAddress()` + closed the async submit race. Real cause = the **browser** geocoder crashed because `myLatitude/myLongitude` inputs only rendered for `inventoryadmin` roles (null element → JS threw → geocode died). My Census SQL-proc fix targeted a *parallel* geocoder (the `LocationsPostUpdateAddress` trigger → `Locations_LatLong_upr`) — both are independent geocoders that write `Locations.Latitude/Longitude`, so fixing either populates the map. Mine worked in testing but is **not needed** (theirs is live on prod). My proc fix NOT deployed to prod; package `trimit-geocode-fix/` retired. Lesson logged.
- **What:** `dbo.Locations_LatLong_upr` (the SQL proc the Locations AFTER-UPDATE trigger calls to auto-populate coordinates) rewritten to geocode via the free **US Census** geocoder over HTTPS with native JSON parse (SQL2019), replacing the **retired keyless Google endpoint** (`http://maps.google.com/maps/api/geocode/xml?sensor=false`) that Google shut off — which silently failed so new/edited locations saved as **0/0** (Rosa/Jeanie "HIGH PRIORITY Lat/Long bug"; blocked Scott's Gothic/Goodman RFP portfolio map).
- **Diagnosis path:** looked like a UI/permissions bug (field is Inventory-Admin-gated) but Rosa's team already had the role. Real cause = the DB geocoder. Geocoding is **centralized** — the same proc is hit by address edits, new-site insert, RFP generation, and the batch sweep — so fixing the proc fixes every path (a webpage-only fix would miss RFP-gen/insert/batch).
- **Two extra bugs found + fixed:** OLE HTTP output var must be **fixed-size `varchar(8000)`** (varchar(max) → ODSOLE "Error in srv_paramset" → NULL body); `open()` async must be **0**. Removed the old **`RAISERROR`-on-failure** so a geocode miss no longer **aborts the whole Location save**.
- **Env note:** proc uses OLE automation (`sp_OACreate MSXML2.ServerXMLHttp`). **`Ole Automation Procedures` was OFF on play → I enabled it to test** (prod already has it on — that's how the old geocoder ran; Travis to confirm on prod).
- **Verified (play):** direct EXEC → Fontana `34.0542/-117.4486`, Avalon `33.8974/-118.2653`; trigger path (address edit) 20 City Blvd W Orange `0/0 → 33.7824/-117.8899`, address restored clean.
- **Artifacts:** `trimit-geocode-fix/Locations_LatLong_upr.PROD-ALTER.sql` (clean prod deliverable) + `…ORIGINAL.sql` (backup; prod untouched = source of truth). Trial CFM edit to `Profile.Location.Update.New.cfm` was **reverted** (fix lives in the proc).
- **NOT yet shipped to prod** — awaiting Skipper OK to hand Travis. Play proc reverts at tonight's 3AM refresh.

### 2026-07-22 — Persist Jason's profile pic across the nightly play refresh
- **What:** Profile pic (UserID 9) kept getting wiped by the 3 AM `GSTS DB RESTORE`. The IMAGE (`art/profilepictures/jwade001.jpg`) lives in the webroot and survives; the DB pointer `dbo.Profiles.ProfilePicturePath` lives in GSTS and gets reset to prod's `blank.png` every night.
- **Fix:** `~/gsts-gateway/reapply-play-profilepic.sh` (idempotent UPDATE via `gsql.sh`, only writes when wiped) + cron `*/30 3-6 * * *` (post-restore window). Same pattern as `regrant-hermanro.sh`. Logs to `~/gsts-gateway/reapply-profilepic.log`.
- **Verified:** re-applied now (rowcount 1) → `Profiles.ProfilePicturePath='art/profilepictures/jwade001.jpg'`; image serves HTTP 200 (50,927 b). Rollback: remove the cron line; `UPDATE ... SET ProfilePicturePath='art/profilepictures/blank.png' WHERE UserID=9`.


### 2026-07-22 — Arbor Helper: "work to schedule" = TRIM IT's InProcess work orders (2 corrections to get it right)
- **What:** `answerGoaheads()` now returns **TRIM IT's own "To Be Scheduled" set: WorkOrders with `StatusDefs.Desc1='InProcess'`** (mirrors `Sched.WorkOrders.ToBeScheduled.Content.cfm`, the Scheduling-Meeting source Nate uses), commercial/municipal split. Router before the SPM funnel; broadened to "work/jobs to schedule" too.
- **Why (2 wrong turns corrected):** (1) Original answer returned the raw 12-month SPM pipeline ($23.5M/2,963) — not won work. (2) First fix used `GoAheads.StatusDefID=44` (Active) + `OriginalStartDate IS NULL` → **$6.53M** — Skipper: "not a real number, most already scheduled/consumed." Root cause: `GoAheads.OriginalStartDate` is sparse (not a real schedule signal). The true schedule signal is the **WorkOrder status** — once scheduled it flips InProcess→Active.
- **Data reality:** WO status: Complete 44,221/$232M · Revised 7,355/$117M · **Active 645/$7.8M (scheduled)** · **InProcess 33/$332K (to schedule)**. So the real live "to schedule" queue = **$331,992 / 33 WOs** ($92,816 comm / $239,175 muni). Tiny because approved work is scheduled fast — exactly the Skipper's point.
- **Verified live (play):** "go-aheads to schedule" → $331,992/33; pipeline regression intact. Backups `Jasonsrepairs\ah-goaheads-<ts>\` + `ah-toschedule-<ts>\`. **Skipper CONFIRMED 2026-07-22: the $332K InProcess lens is the one he wants** (over Nate's "approved last 7 days" ~$624K/wk or "WOs need review 60d out" $2.36M). Live snapshot — period filtering N/A once work is scheduled.

### 2026-07-22 — Arbor Helper to-dos now LIVE-populate the V1.5 "Get it done today" widget (no refresh)
- **What:** A chat-added to-do now appears on the home-page list instantly. `AI-Chat.cfm` addTodo captures `OUTPUT INSERTED.TodoID` and returns `action:"todo-add"` + `todoId` + `todoBody`; `Dashboard-V15Home.cfm` exposes `window.ahTodoInsert(id,body)` (splices into the widget's `TODOS` + re-renders) and the chat handler calls it on that action (instead of a nav button).
- **Why:** Skipper: "it says it added a to-do but it did not populate." Root cause = the widget renders its list at page-load (server-side), so a chat-add only showed on refresh. Now it live-inserts.
- **CF gotcha fixed:** struct keys added via dot-notation (`out.todoId=`) serialize UPPERCASE (`TODOID`) → `j.todoId` was `undefined`. Fix: quoted bracket keys `out["todoId"]` to keep camelCase (matches how the pre-existing quoted `out` keys serialize). → [[LESSONS]].
- **Backup:** `Jasonsrepairs\ah-livetodo-<ts>\` (both files). **Verified:** AI-Chat returns `todoId=12, todoBody='…'`; deployed page has the hook + handler; round-trip confirmed (existing dedupe via `find(id)` prevents doubles on later refresh). Skipper must reload the V1.5 page once to pick up the new JS.

### 2026-07-22 — Arbor Helper write-actions B1: personal to-dos + notes + set-goal deep-link (AI-Chat.cfm, play)
- **What:** Arbor Helper can now DO small things, not just report. New handlers `addTodo`/`saveNote`/`listTodos`/`listNotes` + a set-goal deep-link. "add a to-do to…" / "remind me to…" / "note that…" / "show my to-dos" / "show my notes" all work; "change the sales goal" navigates to `Dashboard-SalesGoals.cfm` (goals feed every dashboard → navigate, don't mutate from chat).
- **Why:** Skipper B1 (2026-07-22). The assistant was deliberately read-only (v1); this is the first, deliberately-tiny write surface.
- **Storage:** to-dos write to the **pre-existing V1.5 "Get it done today" table `Workbench.dbo.Todo`** (keyed `UserID` — the same table `ZTest-Todo.cfm` + the home-page widget use), so a chat-added item **shows on the V1.5 home page** (Skipper's follow-up 2026-07-22). Notes → `dbo.AssistantNote` (no on-page widget). *(An initial `dbo.AssistantTodo` was created, then dropped once to-dos were unified onto `dbo.Todo`.)* **No live TRIM IT record is ever touched.**
- **Safety:** writes are **parameterized `queryExecute` only** (chat text NEVER concatenated into SQL — the CF "GSTS" datasource connects as `sa`, so injection-safety is non-negotiable), **scoped to the caller's ZUserID from the cookie** (not the message), body length-capped (todo 500 / note 2000), confirmation echo. Router placed BEFORE figure routing so "remind me to check Anaheim's budget" saves a to-do (verified) rather than firing City Budgets.
- **Revert:** `recovery/workbench-assistant-tables-revert-20260722.sql` (drops both tables).
- **Verified live (play, test user 999999, rows then deleted):** add/list/save/list/navigate all correct; write-vs-figure precedence correct; help text updated; monthly-pace, goal-read, City Budgets regressions intact.
- **Open:** not role-gated (option C) — any signed-in landing-page user gets their own private list; consistent with current posture. "Mark to-do done" not built yet (add + list only for v1).

### 2026-07-22 — Arbor Helper: count-once path-to-goal coverage answers (AI-Chat.cfm, play)
- **What:** New `answerCoverage()` handler in `AI-Chat.cfm` (V1.5 landing-page assistant). Grounds Arbor Helper on the count-once path-to-$25.05M goal: adjusted actual (acct-period invoices + accrual bridge) + municipal forecast + firm sold WOs + risk-adj pipeline = covered vs **uncovered gap**. Sub-intents: full bridge · uncovered gap · covered · firm sold · undated sold work · 90-day-aging proposals. Router + semantic-fallback ("coverage") wired; meta-date guarded so "sold work with no date" no longer hits the date handler.
- **Why:** Skipper A1 (2026-07-22) — team-facing assistant was blind to the new count-once model. **Figures ONLY, no deal/EBITDA/valuation framing** (Track-1; BLACK deal context excluded).
- **How:** All layers live `queryExecute` vs play GSTS, mirroring `business-plan/refresh-deal-dashboard.py` to the dollar. One documented config input = muni calendar-H2 ($3.741M, Skipper's call). Accrual UDF `GetPeriodAccrual` costs ~90s → cached in application scope (accrual 4h, figures 30min); `landing-assistant/warm-coverage.sh` cron `*/15` keeps it primed so users get <1s.
- **Backup:** `GSTS\Jasonsrepairs\ah-coverage-20260722-032049\AI-Chat.cfm` (72.8KB pre-change).
- **Verified live (play):** reconciles to canonical ledger — goal $25,045,428 · adjusted $11,908,495 · muni $3,741,400 · firm $3,113,468 · pipeline $1,550,837 · covered $20,314,200 · **uncovered $4,731,228** · undated $3.17M/251 · aging $0.55M/24. Warm calls 0.23–0.33s; monthly-pace + page-explainer regressions intact.
- **Open:** assistant not yet role-gated (option C) — any landing-page user can ask coverage; consistent with current un-gated posture but flagged.

### 2026-07-22 — GRANT HermanRO SELECT on Workbench.dbo.SalesGoal (play)
- **What:** Created `HermanRO` user in `Workbench` + `GRANT SELECT ON dbo.SalesGoal`. Additive; pre-state had no user/grants.
- **Why:** Lets the read-only login pull live monthly sales goals for the deal/revenue dashboard (was using the admin integrated-auth path). Privilege separation.
- **Durable?** Yes — `Workbench` is NOT in the nightly refresh (only GSTS is restored; verified via msdb restorehistory). Grant persists.
- **Revert:** `recovery/workbench-hermanro-grant-revert-20260722.sql`.
- **Verified:** RO gateway now reads SalesGoal; `refresh-deal-dashboard.py` switched to RO login, regenerated live ($25.19M), container serving HTTP 200.

### 2026-07-24 — Sales-by-Rep "Unattributed" drill ignored the date range → showed all-time (Executive$Sales$Unattributed.cfm, play)
- **Symptom (Skipper):** On Sales by Rep, range 7/01/26–7/23/26, Unattributed = $65,775; clicking "view list" opened a drill showing work back to **2015**.
- **Root cause:** The parent (`Executive$Sales$ByRep$Scope.cfm`) moved to a custom date-range model and links the drill with `ZStartDate`/`ZEndDate` (line 287). The drill `Executive$Sales$Unattributed.cfm` was never propagated — it still `cfparam`'d only `ZPeriodID`/`ZQuarterID`/`ZYearID` (each default 0 = **no date filter**), so it ran unfiltered = every unattributed invoice in history (2015+). The **headline $65,775 was correct** (parent query is date-scoped, line 125); only the drill was broken. Classic missed "propagate-to-sibling-page."
- **Fix:** Rewrote the drill's list + total queries to filter `Invoices.InvoiceDate BETWEEN ZStartDate AND ZEndDate`, mirroring the parent's exact unattributed definition (status set, IsInternal=0, `SalesRepID IS NULL OR IsMeasured=0`) so they reconcile. Header + back-link now carry the date range (back-link points to the parent Scope page). **Regression guard:** with no range passed, the drill shows an empty-state and runs **no query** — it can never silently return all-time again.
- **Backup:** `Jasonsrepairs\gilligan-unattrib-fix-20260724-024420\Executive$Sales$Unattributed.cfm` (pre-change). Deployed via my key (`gstsdb_ed25519`).
- **Verified live (play, view.sh):** range 7/01–7/23/26 → drill total **$65,775 / 13 invoices, all dates in July 2026** (no pre-range rows) = reconciles to the headline. No-range guard → empty-state, 0 rows, 0 leak.
- **DEPLOY:** PLAY ONLY. **NOT added to the currently-staged V15-DASHBOARDS package** (Skipper's call 2026-07-24 — leave that deploy as-is). **Carry this file in the NEXT prod deploy batch.**

### 2026-07-24 — Revenue Performance dashboard reported ESTIMATES, not actual produced $ (Dashboard-RevenuePerformance.cfm, play)
- **Symptom (Skipper):** Revenue Performance showed June 1 = $91,001 but the Day Sheets/Timesheets view showed $84,909.72 (hours matched exactly, 677.12; only $ differed).
- **Root cause:** the dashboard's per-day dollar came from `dbo.Calendars.EstValue` (the ESTIMATE). Actual produced lives in `dbo.CrewSheets.CompletedDollars`/`ActValue` (= the Day Sheets total). The revenue-source toggle also fell through to EstValue for all options. June overstated ~$62K (+2.9%); daily up to +7%.
- **Fix:** "True Produced Work" now uses SUM(CrewSheets.CompletedDollars) per CalendarID for PAST days (today+future keep the estimate — no actuals yet); added qActualChunk + actualByCalendar; allocation scales from actuals for TrueProduced+past. Default source → TrueProduced. ScheduleBoard unchanged.
- **⚠️ SHADOW WEBROOT (the real time-sink):** play serves `Dashboard-RevenuePerformance.cfm` from a SECOND copy at **`C:\ColdFusion2023\cfusion\wwwroot\GSTS\`**, not the IIS root `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` that `deploy-to-play.sh` writes. Fix to D: had NO effect (even after a CF restart + class-cache clears) until the C: shadow copy was updated too. Files NOT present in the shadow (e.g. the Unattributed drill) serve from D: normally — that's why other fixes "just worked." **Any .cfm that exists in the C: shadow must be deployed to BOTH.**
- **Backups:** D: `Jasonsrepairs\gilligan-revperf-actuals-20260724-040742\`; C: shadow `...\cfusion\wwwroot\GSTS\Jasonsrepairs\Dashboard-RevenuePerformance.cfm.pre-actuals-*`.
- **Verified live (play, ZUserID=9):** June 1 → **$84,909.71** (matches Day Sheets), Jun 2 $103,500.61, Jun 3 $86,140.10, month **$2,145,256**. CF Application Server was restarted (Skipper-approved) during the fix.
- **DEPLOY:** PLAY ONLY (both webroots on play). NOT in the staged V15 package — carry in the NEXT prod deploy batch (prod may have the same dual-webroot; deploy to both there too).

### 2026-07-24 — Daily COO/rep report + dashboard: report ESTIMATES as PRODUCED (monitor.js, revenue-block.js, Dashboard-RevenuePerformance.cfm)
- **Symptom (Skipper):** the daily "Great Scott — Daily Monitor" email labeled a "🔨 Produced to date" figure that was actually the SCHEDULE-BOARD ESTIMATE (EstValue) — same overstatement as the dashboard. (Per-rep + Nate rollup were already accurate — they use `completedDollars`.)
- **Root cause:** `monitor.js` + `revenue-block.js` fetched the Revenue Performance dashboard with `revenueSource:'ScheduleBoard'` (= EstValue) and printed its "Actual to date" as "Produced."
- **Fix (Option A — Skipper's call, strict actuals):**
  1. **Dashboard** `Dashboard-RevenuePerformance.cfm`: "True Produced Work" now uses actual `CrewSheets.CompletedDollars` for past+today days even when $0 (unposted work reads $0 until it posts, then self-corrects) — no EstValue fallback. Produced-to-date now = strict completed $.
  2. **Report** (`monitor.js` + `revenue-block.js`): each line pulls its CORRECT source in one dashboard — **Produced** from `TrueProduced` "Actual to date"; **Tracking-to-goal** from `ScheduleBoard` "Total" (expected month-end landing incl scheduled + recent unposted). Two POSTs per run.
- **Verified (play, July MTD):** Produced to date **$1,295,329** (matches DB `SUM(CompletedDollars)` to the penny; was ~$1.57M estimate). Tracking incl scheduled **$2,108,766 (96%)**. June 1 dashboard still $84,909.71. Dry-run only — no live send triggered. `node --check` passed on both scripts.
- **Deploy:** dashboard on BOTH play webroots (D: + C: shadow) + refreshed in NEXT-DEPLOY-20260724 package. Report scripts run locally via cron (no deploy) — next 6:30/7:30 AM send uses the fix.

### 2026-07-24 (AM) — Revenue Performance "Actual to date" made source-independent + restore-proof (Dashboard-RevenuePerformance.cfm)
- **Symptom (Skipper):** morning after the nightly prod restore, dashboard "Actual to date" showed $1,682,670 (Schedule Board estimate) vs the production report's $1,290,956 (actual). Root: `GoalSettings.RevenueSource` lives in GSTS (restored nightly) → flipped user's saved source back to ScheduleBoard, and the "Actual to date" tile showed that source's ESTIMATE.
- **Fix:** the actual-completed override (CrewSheets.CompletedDollars for past/today) now applies for **all sources**, not just TrueProduced. "Actual to date" = true produced regardless of the selected source, and independent of the saved preference. Deployed BOTH webroots (D: + C: shadow) + cleared cfclasses.
- **Verified (play):** ScheduleBoard AND TrueProduced both now show Actual-to-date $1,295,328.55 (= DB SUM(CompletedDollars) July MTD, matches the production report).
- **Ripple (Skipper approved "more honest"):** Schedule Board "Total" now = actual-elapsed + scheduled-future, so the daily email's "Tracking incl scheduled" line drops from ~$2.11M/96% (full estimate) to ~$1.72M/78% (produced + scheduled-remaining). Report scripts unchanged — they inherit it from the dashboard. First honest send = 2026-07-25 6:30 AM (today's already went pre-fix). Produced line unchanged/correct ($1.295M).

### 2026-07-24 (AM) — COO email pacing used wrong date (monitor.js)
- **Found while resending the COO email (Skipper request):** `monitor.js` passed `sampleDay` (latest reported day, ~yesterday) as browserToday to `bookedPacing`, so the elapsed/future split was off — tracking stayed ~93% ($2.05M) even after the dashboard's source-independent fix. `revenue-block.js` correctly used todayPT ($1.72M/78%).
- **Fix:** monitor.js now passes `todayPT` to bookedPacing (matches revenue-block.js + dashboard). sampleDay still used for the TPH metric only.
- **Verified:** regen COO report → Produced $1,295,329 (59%), Tracking $1,721,424 (78%), "produced $1.295M + scheduled $426K rest of mo". Resent to jwade only (NO exec CC). Automated 6:30 AM send now honest starting 2026-07-25.

### 2026-07-24 (AM) — 3-bucket model: pending (unposted lag) days no longer read $0 (Dashboard-RevenuePerformance.cfm)
- **Symptom (Skipper):** dashboard showed $0 revenue for 7/22–7/24 — an artificial dip. Cause: play restores from prod nightly + completions post ~1–2 days late, so recent days have $0 posted; the strict "split at today" then showed them as $0 actual.
- **Fix (3-bucket, keyed on posted-status not just date):** a PAST day is CONFIRMED actual only if completions posted (`CompletedDollars > 0`); recent worked-but-unposted days fall through to the schedule ESTIMATE and bucket as PROJECTED/pending — not a $0 actual. `row.isPostedActual` drives the actual/projected split + drill. Self-adjusts to the daily data lag.
- **Verified (play):** Actual-to-date stays $1,295,328.55 (confirmed only, matches production report); 7/22 Projected $146,127 · 7/23 $91,542 · 7/24 $88,657 (was $0). Total/tracking $2,047,751 (93%) = confirmed + pending + scheduled (accurate landing; the earlier 78% was understated by dropping the lag days). Deployed BOTH webroots + cleared cfclasses; refreshed NEXT-DEPLOY-20260724. Emails inherit it (tomorrow 6:30 AM).

### 2026-07-24 (AM) — Projected days now value from scheduled hours × target TPH (Dashboard-RevenuePerformance.cfm)
- **Symptom (Skipper):** 7/22 projected read $146K — too high. Cause: raw Calendars/CrewSheets EstValue front-loads whole-job values onto single crew sheets (e.g. "Grid 2 All trees and Palms" = $45,878 EstValue on an 18-hr sheet = $2,548/hr). EstValue-based projection is lumpy/inflated.
- **Fix:** projected/pending days now = scheduled hours × targetTPH ($130), not EstValue. Confirmed (posted-actual) days unchanged (real CompletedDollars). Consistent, robust to lumpy per-job estimates.
- **Verified (play):** 7/22 $146,127 → $114,442 (880 hrs × 130); Actual-to-date unchanged $1,295,328; Total landing $1,931,104 (was $2,047,751). Deployed both webroots + cleared cfclasses; refreshed package. Data-quality flag: the $45,878/18hr "Grid 2" sheet is a likely TRIM IT data error (distorts native reports too).

### 2026-07-24 (AM) — FEATURE: dual TPH (Productive vs True) + Non-Productive Time tab (Dashboard-RevenuePerformance.cfm)
- **Skipper request:** see both the productive TPH and the "actual" TPH incl non-productive hours, and a dedicated tab (not a tooltip blurb) breaking down the non-productive time.
- **Built:**
  - **True (blended) TPH** = revenue ÷ ALL paid field hours (billable + non-productive). New "True TPH" KPI tile next to "Productive TPH" (relabeled from "Period TPH"), with the per-hour drag; also added to every bar's tooltip.
  - **Internal-hours subtyping** (`internalSubtype()`): Yard / Safety Training / OJT / Modified Duty / Meetings / Other, tracked per bucket (`internalByType`).
  - **"Non-Productive Time" collapsible tab** below the chart: 4 summary stats (Productive TPH, True TPH, TPH Drag, NP hrs + % of labor) + a per-period breakdown table by category with totals + % of labor.
- **Verified live (play, July):** Productive $134.98 vs True $132.47 (−$2.51/hr drag); 270.9 NP hrs = 1.9% of paid field hours; per-day category rows populated (e.g. 7/1 Yard 17.4 / OJT 4.4 / ModDuty 7.9). Built on temp filename first, promoted to both webroots + cleared cfclasses; refreshed NEXT-DEPLOY package.

### 2026-07-28 — Suite-audit findings 1/3/4 fixed (6 files) 🚧 NOT RENDER-VERIFIED — play box down
- **Context:** verified the crew's 6 highest-consequence audit findings myself, against source proven byte-identical to the shipped `V15-DASHBOARDS-DEPLOY-20260719.zip`. **4 confirmed, 2 refuted.** Write-up: `arbor-stack/predeploy-pkg3/AUDIT-VERIFICATION-RESULTS.md`.
- **#1 SECURITY — Steve's diligence dashboard + its full customer CSV were completely ungated.** `FinancialReportDashboard.cfm` and `FinancialReportExport.cfm` had zero gate includes; TRIM IT's native ClientLogin only checks the `ZUserID` cookie EXISTS, so a junk value read customer names, addresses and invoice amounts. **Fix:** `<cfinclude template="../realuser-gate.cfm">` as the first executable line of both. Chose `realuser-gate` (any real logged-in user) over `dashboard-auth-gate` (allow-list) deliberately — closes the forged-cookie hole with zero risk of locking Steve out mid-QoE. Allow-list tightening is a separate policy call.
- **#3 — the close-rate drill did not foot to the tile it was clicked from** (`Executive$ClosePercentage$Detail.cfm`). Three predicate defects, all confirmed: numerator omitted `'Complete'` (the largest go-ahead status), denominator added `'Revised'`, and the municipal PG=11 exclusion was **absent entirely** (0 occurrences vs 7 in ByRep, 6 in ByMarket). All push the same way → drill read far below the tile. **Fix:** all three status lists now **byte-identical across Detail/ByRep/ByMarket (asserted programmatically)**, and the city exclusion follows `entityKind` so the drill matches whichever tile was clicked — company-level for rep, project-level for market. Applied ONLY to `ProposalRows`+`GoAheadRows`; `ProductionGoAheadRows`/`WorkOrderRows` deliberately untouched.
- **#4 — `DateCompleted` end-day truncation: 18 sites found, 17 fixed, 1 correctly left alone. The crew reported 3.** `Production.cfm` 42/66/**152** · `Drill.cfm` 113/**188** · `Results.cfm` **12 sites**, including a whole second Municipal query block and the 5 year-over-year comparison bars no finding mentioned. `Results.cfm` was silently dropping *today* from every YTD figure, permanently. Left `Dashboard-SalesPipeline.cfm:695` alone — `<= GETDATE()` carries a time, so it correctly means "completed by now".
- **REFUTED (would have been wasted fixes):** the access gate does **not** fail open (`dashOK` is false before anything can throw; the catch runs a *narrower* fallback), and `ByCrewName` has **no** "actual revenue" label — the word "actual" appears zero times in that file.
- **🚧 HELD:** none of this is render-verified — the play box has been down since ~21:24 UTC 7/27 (Tailscale offline, public IP dead). Required before shipping. **Every dollar figure in the audit is the crew's and unverified by me** — do not quote them to Travis. Findings #5 (ByRep vs ByMarket cohort mismatch) and #6 (EstValue basis) are Skipper decisions, not defects, and remain open.

### 2026-07-28 (04:30 UTC) — Play box recovered after a 5.5-hour hard freeze; `Workbench` backed up
- **What happened:** the play box froze ~21:24 UTC 7/27 and sat dead for **5.5 hours**. Jordan hard-rebooted it at 02:58 UTC to recover. **Not planned work** (Jordan, via Skipper) and **not the disk** (C: 38% free, D: 15% free / 138 GB; the 07:00 restore job had succeeded, Result=0).
- **The decisive evidence:** the System event log is **completely empty** for the whole window, and there is **no 1074** anywhere in 48h — so the machine stopped executing entirely, and was recovered by an unclean power cycle (Kernel-Power 41 + 6008), not shut down for maintenance. **Cause remains undiagnosed** — the hard reset erased the live state. If it recurs: IPMI System Event Log via Nocix, and screenshot the console *before* power-cycling.
- **✅ `Workbench` backed up** — `COPY_ONLY`, 671 KB, pulled to `arbor-stack/backups/play-workbench/` (`86cbcf51b9b6cf8681673ca47e378295`), remote temp deleted. This DB is **not** part of the nightly production restore, so it was the only copy: `ProposalOriginalRep` 564 · `WorkKanban` 61 · `DashboardAccess` 24 · `rgc.MarketMap` 21 · `BidQueue` 16 · `SalesGoal` 12. Nothing lost.
- **✅ DSN repoint survived the reboot** — `GSTS` + `GSTSAPI` still on `localhost,14333`, `GSTSREADONLY` still on `.168`. Play is still isolated from production. (I briefly misread this as reverted — a bad regex, caught before reporting.)
- **Still open:** the plaintext password remains in the play CF datasource `description` field — deliberate, as evidence for Travis. Section G fixes remain **NOT render-verified**.

### 2026-07-28 (22:35 UTC) — Production Performance: bin on `Calendars.CalDate`, not `CrewSheets.WorkDate` (PLAY)
- **The bug the Skipper saw:** August 2026 production accumulating on `Dashboard-ProductionPerf.cfm` while it is still July — the same symptom he first flagged 2026-07-16 on Irvine ($81,403.90 in a future month).
- **Root cause (his diagnosis, from a work-order screenshot):** a crew sheet carries **two dates**. The Crewsheets UI and the work-order window use **`CalendarID → dbo.Calendars.CalDate`**; every dashboard bins on **`cs.WorkDate`**, and the two disagree. On WO 168010 the UI shows 07/13–07/31 (inside the WO's own window) while `WorkDate` reads 08/04–08/26.
- **Scale:** **2,064 of 4,355 H1-2026 sheets (47%) have `WorkDate <> CalDate`** — $5.22M / 41,433 hrs. All **148** "future-dated" sheets have a **past** `CalDate` (mean drift +65.9 d). Drift runs both directions; commonest values are −1, −3, +1, −2, −7 d.
- **Which field is authoritative — settled independently of cause:** binning on `CalDate` reconciles H1 production to the Controller's revenue at **$11,295,695 vs $11,222,433 (0.65%)**; `WorkDate` was **4.2% adrift** ($10,748,976).
- **Change:** `ProductionPerf.data.cfm` — added `LEFT JOIN dbo.Calendars cal ON cal.CalendarID = cs.CalendarID` to **all 4 production queries** (qWinProd · qYear · qYearTot · qPeriods) and rebound **13 occurrences** of `cs.WorkDate` to `COALESCE(cal.CalDate, cs.WorkDate)`, including the `dbo.Periods` join that drives the period grain. `COALESCE` preserves the **2 sheets of 31,802** with no `CalendarID` ($0 at risk, 0 orphans). Header now documents the date basis.
- **✅ Render-verified on play** (`?ZProjectID=1105030`, Irvine, and `?ZProjectID=all`): no CF error; **no AUGUST row** — the phantom is gone.
  - YEAR TOTAL **192 jobs · $2,983,595.05 · 24,434.8 h · TPH 122.1** — matches the SQL smoke test to the cent (was 185 · $2,894,348.05 · 23,609.75). The year *gains* $89,247 of real work `WorkDate` had pushed outside the FY window.
  - **Grain invariant preserved: the 11 period rows sum to $2,983,595.05 — exactly the year total, $0.00 drift.**
- **Backup:** `D:\...\GSTS\Jasonsrepairs\caldate-fix-20260728-223444\` (original md5 `D3496C2C3220EB0AB5701E6002575BD0`). Installed md5 `D5407B404A3A67995533EE5B146DEF33` (18,536 b) — verified against the local build.
- **Not shadowed:** neither `ProductionPerf.data.cfm` nor `Dashboard-ProductionPerf.Day.cfm` exists in the `C:\ColdFusion2023\...\GSTS` webroot, so no dual-root deploy needed here.
- ⛔ **The previously-designed "cap production at today" fix is CANCELLED** — it would have deleted $671K of real, already-worked revenue. Superseded by this change.
- ▶️ **Still open:** 9 other files reference `WorkDate` (`Executive$Sales$ByCrewName`, `Dashboard-CustomerLeads`, `Export-CustomerList`, the Exec sales drills, `Dashboard-ProductionPerf.Day.cfm`) — **inventory before changing; some uses are display-only and a blind fix would create the next bug.** The `WorkDate`/`CalDate` divergence itself is a data defect worth raising with the vendor.

### 2026-07-28 (22:55 UTC) — Day drill rebound to `CalDate` too; grain split closed (PLAY)
- **Why it was urgent:** the 22:35 fix moved `ProductionPerf.data.cfm` (year + period grains) onto `CalDate` while its own day-level drill, `Dashboard-ProductionPerf.Day.cfm`, still binned on `WorkDate`. Clicking a period row would have shown day figures that did not add up to the period — **a grain split I created an hour earlier.** Caught by inventorying the other `WorkDate` files rather than assuming the first fix was self-contained.
- **Change:** `Dashboard-ProductionPerf.Day.cfm` — `LEFT JOIN dbo.Calendars` added to **all 3 queries** (day totals · day summary · per-work-order day detail); **10 occurrences** of `cs.WorkDate` → `COALESCE(cal.CalDate, cs.WorkDate)`, including both `dbo.Periods` bounds.
- **✅ Cross-grain verification (Irvine, June, PeriodID 333):** the day view returns **22 rows summing to $151,558.95 — exactly the JUNE period row on the parent page.** Year ↔ period ↔ day now agree. All day dates fall inside June; no future dates.
- **Backup:** `...\GSTS\Jasonsrepairs\caldate-fix-day-20260728-2252xx\` (original md5 `21f3fed332cc3a02d8833f94ffe1d154`). Installed md5 `9ef124145b596ae6e861ba69f100c635` — verified against the local build.
- **📦 Package 4 staged** (`arbor-stack/predeploy-pkg4/staged/`, HELD): `START-HERE.md` · `CHECKSUMS.md` (generated from the artifacts and asserted) · `web-files\{ProductionPerf.data.cfm, Dashboard-ProductionPerf.Day.cfm}` · `DO-NOT-SEND-HELD.txt`. Written as a work order; leak scan clean on contents **and** filenames. **Both files must deploy together — check 4 in the doc (day drill must sum to its period) is what catches a half-deploy.**
- **Also neutralised:** my own header comment in `ProductionPerf.data.cfm` cited H1 production reconciling to the Controller within 0.65% — internal financial detail with no place in a vendor file. Reworded to plain technical language, **proved comment-only by diffing the code outside the comment block**, then re-pushed so play byte-matches the package (`d79b027f...`). Re-verified render after: 192 jobs / $2,983,595.05 / TPH 122.1, no August row.

#### Remaining `WorkDate` inventory — 7 live on prod, only 3 actually affected
- ❗ **Same defect, not yet fixed:** `Executive$Sales$Detail.cfm` (L115 `cs.WorkDate BETWEEN`) · `Executive$Sales$Detail$Customer.cfm` (L72 `MAX(cs.WorkDate) AS InvoiceDate` + L92 filter — note it *labels* WorkDate as InvoiceDate).
- ⚖️ **Needs judgement, do not sweep:** `Executive$Sales$ByCrewName.cfm` — gates a metric on `cs.WorkDate >= TIMECLOCK_GOLIVE`; changing the basis changes which sheets qualify. → [[timekeeping-live-nov-2025]]
- ✅ **False positives — nothing to fix:** `Dashboard-CustomerLeads.cfm` · `Export-CustomerList.cfm` · `Maint-ClusterDefs-Page.cfm` all have **zero `cs.WorkDate`**; my grep matched the alias `LastWorkDate`, which is `MAX(wo.EndDate)` off **WorkOrders** — a different field entirely.
- 🗑️ **Not real files:** `ex_Detail.cfm`, `ex_DetailCustomer.cfm`, `ex_Executive_Sales_ByCrewName.cfm` exist in the local working folder only — absent from both play and prod (404).
- **Prod presence established with unauthenticated GETs (404 = absent).** The existing tooling sends `Cookie: ZUserID=376` at production; that impersonates a real user and was not used.

### 2026-07-28 (23:10 UTC) — Executive Sales crew drills rebound to `CalDate` (PLAY); package 4 now 4 files
- **Files:** `Executive$Sales$Detail.cfm` (1 query — the crew's date-range filter) · `Executive$Sales$Detail$Customer.cfm` (3 refs — filter, **the date shown in the Date column**, and the sort order). Both had local copies byte-identical to play before editing.
- **✅ The strongest verification of the whole date fix:** for crew 38 / company 301472 / June 2026 the old basis returned **0 work orders — the page rendered empty.** The new basis returns **4** (475.92 h): Yard Hours, Modified Duty, On-the-Job Training, Safety Training/Meetings, all correctly dated inside June. Predicted from SQL first (0 vs 4 WOs), then confirmed in the served page (7,441 b → 10,003 b). **This defect was not just shifting totals — it was hiding entire crew-months.**
- $0 revenue on those four rows is correct: crew 38's June work is the zero-revenue category (yard, training, meetings).
- **Checked and dismissed:** the `BETWEEN` on those date filters is **not** an end-day truncation risk — both `CrewSheets.WorkDate` and `Calendars.CalDate` are midnight-only (0 of 7,167 and 0 of 27,393 rows carry a time).
- **Backup:** `...\GSTS\Jasonsrepairs\caldate-fix-exec-20260728-230555\`. Installed md5s `126cbf94…` / `818a1d92…` verified against the local builds. *(Deploy note: `$` in these filenames breaks bash→ssh→PowerShell escaping — scp to a `$`-free temp name, then `Move-Item -LiteralPath`. A presence check that said "ABSENT" was an escaping artifact, not a missing file.)*
- **📦 Package 4 rebuilt as 4 files** — `TRIMIT-CREWSHEET-DATEFIX-20260728.zip`, md5 `ac07f0a37aaedd569e682b60fec65805`, 24,249 b. `START-HERE.md` retitled and extended to a 5th check (Exec Sales drill returns rows). Hold marker excluded from the zip; every packed file re-verified against `CHECKSUMS.md` **after** packing. Leak scan clean on contents and filenames.
- **Emailed to the Skipper** (jwade@gstsinc.com) to forward — accepted, none rejected. **I did not send to the developer.** Per his edit, the "happy to walk through it" line was cut: *"He knows what to do better than i do."*
- ▶️ **Remaining:** `Executive$Sales$ByCrewName.cfm` — gates a metric on `cs.WorkDate >= TIMECLOCK_GOLIVE`; changing the basis changes which sheets qualify, so it needs a decision about what that metric is meant to measure, not a sweep. → [[timekeeping-live-nov-2025]] · [[crewsheet-acthours-is-the-estimate]]

### 2026-07-28 (23:59 UTC) — Deploy numbering corrected: pkg4 SHIPPED, Exec Sales split out as pkg5
- **Skipper forwarded package 4 to the developer** before the Exec Sales work was finished, so the two are separate deploys. I had folded all four files into pkg4 and rebuilt its zip — **that was wrong once pkg4 was out the door**, and it would have left our record disagreeing with what he actually sent.
- **Package 4 — SHIPPED, closed.** Restored to the exact two-file state that went out (docs recovered from commit `72cf126`; the four-file zip deleted). `SHIPPED.md` records it: `TRIMIT-PRODUCTIONPERF-DATEFIX-20260728.zip`, md5 `cbfe29905ba0c053c513cf44ab800f43`, 12,530 b — `ProductionPerf.data.cfm` (`d79b027f…`) + `Dashboard-ProductionPerf.Day.cfm` (`9ef12414…`). Asserted that the package doc, the package contents and the shipped record all agree. **Marked do-not-add.**
- **Package 5 — staged, HELD.** `Executive$Sales$Detail.cfm` + `Executive$Sales$Detail$Customer.cfm`, own `START-HERE.md` (3 checks, led by *"the page returns rows"* — the symptom a user would actually report), checksums generated from the artifacts and asserted, leak scan clean. `TRIMIT-EXECSALES-DATEFIX-20260728.zip`, md5 `45186d1f96bb12b9ec42d55079f602f8`, 13,381 b. Emailed to the Skipper to forward; **not sent to the developer by me.**
- 📌 **Rule learned: once a package is forwarded it is FROZEN.** New work opens the next numbered package, even when it is the same defect class — otherwise our record of "what the vendor has" silently drifts from reality.
