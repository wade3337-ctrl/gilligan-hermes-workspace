---
title: Boss Herman (container) vs Herman (Arduino) — the two agents
type: fact
domain: env
tags: [infra, boss-herman, herman, hermes, agent, arduino, container]
links: ["[[crew-llms-and-helpers]]", "[[env-host-and-tooling]]"]
updated: 2026-07-03
---

# 🤖 Two distinct agents — name them right (Skipper, 2026-07-02)

> [!important] **STANDING RULE (Skipper, 2026-07-03):** Boss Herman **is part of arbor-core**. As we do
> Boss Herman upgrades, log the work + lessons to the arbor-core build doc **`arbor-knowledge/how-it-works/boss-herman-runtime.md`** (BLACK vault) — not just workspace memory. Do it each upgrade.
> **STANDING RULE — BROADENED (Skipper, 2026-07-08):** "Herman **IS Arbor Core** — he should know everything you and I know." So **EVERY arbor-core build / change / decision** also gets written into his **`~/arbor-knowledge`** vault (right folder — architecture / how-it-works / decisions / schema / lessons; **UPDATE the existing note, don't duplicate**), then commit + push + `~/herman-gateway/refresh-herman-vaults.sh`. Keep his arbor-core knowledge continuously current — not just Boss-Herman-runtime upgrades. **BLACK/Track-2 stays in `arbor-knowledge` only, never `trimit-knowledge`.** (Track-1 GSTS/TRIM IT knowledge → `trimit-knowledge`.)
> **DEPLOYMENT GOAL (Skipper, 2026-07-03):** end-state = **self-bootstrapping** — Skipper installs base Hermes on a fresh GSTS server + provisions secrets, hands the agent a **deploy file, and the agent builds arbor-core itself.** Keep **`arbor-knowledge/how-it-works/boss-herman-deployment.md`** current as an *agent-executable* inventory — capture each new piece as a *templated, fetchable, self-verifying artifact* (not prose), so the deploy file assembles over time.

## 👑 Boss Herman — the AI boss (`hermes` Docker container on jdog1)
The one with the TRIM IT + arbor knowledge and live DB access. **Call it "Boss Herman."**
- **Framework:** runs **Hermes** — the Arbor AI agent framework/runtime he IS the brain of (image `hermes-agent`, `config.yaml`, `delegate_task` + maturity-phase system). **NOT OpenClaw.** **Version: v0.18.0 (2026.7.1)** — updated from v0.17.0 on **2026-07-03** (git pull `~/hermes-sandbox` 4ea3096→528159f + `HERMES_UID=1000 HERMES_GID=1000 docker compose up -d --build`). Rollback image tagged `hermes-agent:rollback-20260703`; pre-update config/creds parked at `~/herman-gateway/preupdate-backup-20260703/`. **Update = host-side image rebuild + container recreate** (a container can't replace its own running self — "self-update" for him means a scoped host-side trigger, not in-container). Compose also runs a 2nd container `hermes-dashboard` (127.0.0.1 only). (Gilligan runs OpenClaw — different software; same-family only in that we're both Claude-brained assistants for the Skipper.)
- **Where:** Docker container `hermes` on our box **jdog1**, `net=host` → it shares gilligan's Tailscale IP `100.82.161.7` (that's why the IPs match; NOT a bug). Runs as uid 1000 `hermes`, **HOME=`/opt/data`**, cwd `/opt/hermes`.
- **I reach it directly via `docker exec [-u hermes] hermes …`** (I'm in the `docker` group) — no SSH, no key relay. Logs: **`~/.hermes/logs/agent.log`** (detailed model/fallback events, since v0.18) + `docker logs hermes` (gateway stdout only). Config: `/opt/data/config.yaml` + `/opt/data/.env`.
- **Brain:** primary = GLM `glm-5` via z.ai Anthropic-compat endpoint (`https://api.z.ai/api/anthropic`, subscription, switched 2026-06-30). **Fallback = Codex `gpt-5.5`** — auto-fails-over when z.ai errors/overloads (the 2026-07-02 crash cause). **Wired by Boss Herman himself 2026-07-03 (verified), PROVEN by a live failover test** (primary pointed at a dead addr → log `Fallback activated: glm-5 → gpt-5.5 (openai-codex)` → Codex answered → primary restored). **RE-PROVEN post-v0.18-update 2026-07-03 18:03** (Gilligan-verified from the log): primary broke → HTTP 401 → `Fallback activated: glm-5 → gpt-5.5 (openai-codex)` → Codex answered `latency=4.3s total=15713 tok` → restored to glm-5. Boss Herman saved it as the **`prove-the-failover-drill` skill** (run after every update/auth change).
  - **KEY LESSON (Gilligan's first attempt failed here):** the Hermes `openai-codex` fallback provider needs the ChatGPT OAuth registered in **Hermes's OWN credential store** (`hermes auth add openai-codex`, device-code flow → `hermes auth list` shows `openai-codex-oauth-1`), + `fallback_providers: [{provider: openai-codex, model: gpt-5.5}]` in `/opt/data/config.yaml`. **TWO separate auths:** the Codex CLI login (`/opt/data/.codex/auth.json`, for standalone `codex exec` tool) is NOT the same as the Hermes store credential (for the failover BRAIN) — both use the same ChatGPT sub. Copying the CLI login alone does NOT wire the fallback.
  - Failover latency ~3.5min on dead-host connection timeouts; near-instant on 429/5xx (z.ai's typical 529 = fast failover). Tunable via retry count.
  - ✅ **CORRECTION (2026-07-03) — creds survive recreate:** Hermes store (`/opt/data/auth.json`) + codex CLI login (`/opt/data/.codex/`) live on the **`~/.hermes:/opt/data` bind mount** → survive `docker restart` AND recreate. The codex CLI **binary** now also lives on the mount (`/opt/data/home/.npm-global/bin/codex`, moved off root-owned `/usr/local/bin`) → persists too. What a **version bump** can require is a **re-auth** (v0.18 hardened credential handling — that's what got "lost" on the update, not the files). Crew "Codex" on gilligan = same tool via `~/.codex` (CLI + ChatGPT login, NOT an API key — no codex.json in `.secrets/`).
  - 📍 **v0.18 gotcha — log moved:** detailed model/fallback events now write to **`~/.hermes/logs/agent.log`** (file), NOT `docker logs hermes` (stdout). Verify a failover by grepping the file log — stdout looks empty and will fool you into calling a real flip "unproven."
- **Channel:** Telegram "Boss Hermes", locked to the Skipper only.
- **📧 Email (2026-07-03):** mailbox **`bossherman.gsts@gmail.com`** (Gmail app-pass in `~/.secrets-herman/herman-gmail-apppass.txt` + container `/opt/data/.secrets/gmail-app.txt`). Client **himalaya v1.2.0** (`/opt/data/home/.npm-global/bin`, on PATH). Config `/opt/data/.config/himalaya/config.toml`. IMAP+SMTP proven. **Send-gate = behavioral** (drafts→Skipper approves→send); technical gate is future. In the encrypted recovery bundle. Detail → arbor `[[boss-herman-roadmap]]#F1`.
- **Knowledge:** both vaults at `/opt/data/home/{trimit-knowledge,arbor-knowledge}` — **auto-refreshed every 2h** by `~/herman-gateway/refresh-herman-vaults.sh` (wade3337 cron, `docker cp`). Update the vault on our side → Boss Herman gets it. **Return path (built 2026-07-03):** Boss Herman contributes NEW notes by writing to `/opt/data/home/vault-inbox/{trimit,arbor}/<taxonomy-folder>/<slug>.md` (= gilligan `~/.hermes/home/vault-inbox`, shared via the mount). Collector `~/herman-gateway/collect-herman-notes.sh` (cron `*/20`) folds them into the source vault → commit → push → Skipper's Obsidian → back to Boss Herman on the 2h refresh. **BLACK-leak guard:** a trimit-bound note containing arbor/Track-2/BLACK markers is quarantined, not pushed. Contract: `vault-inbox/HOW-TO.md`.
- **🔄 Self-update (built 2026-07-03, proven):** Boss Herman triggers his own image-swap via a SCOPED forced-command channel (same least-privilege model as his DB access — he never touches Docker/host-root). Container-side: `bash /opt/data/home/hermes-self-update.sh [update|status|log|rollback]` → key `/opt/data/.ssh/herman_selfupdate` → gilligan `~/herman-gateway/herman-update-dispatch.sh` (whitelists those 4 verbs, denies all else) → **`herman-self-update.sh` runs DETACHED on gilligan** (must — the recreate would otherwise kill its own updater): backup-image-tag → `git pull` sandbox → `docker compose up -d --build` → health-gate → **auto-rollback to `hermes-agent:rollback-current` if the new build is unhealthy** (never leaves him down). Status one-liner: `~/herman-gateway/update-status.txt`; log: `update.log`. Boss Herman restarts mid-update → reads `status` after he's back. Verbs proven end-to-end 2026-07-03 (UPTODATE path, confinement DENIED off-list).
- **👥 Crew (2026-07-03):** `echo "PROMPT" | /opt/data/home/crew-ask.sh <gemini|fable|glm|kimi|judge>` → `gilligan_access` key → `herman-dispatch.sh crew <spec>` (whitelisted, extended the DB dispatcher) → `~/arbor-core/crew/<spec>-ask.py`. Keys stay on gilligan. Map/patterns → arbor `[[boss-herman-crew]]`.
  - **⚡ DIRECT crew API keys (2026-07-10, Skipper):** to cut the SSH-hop dependency on gilligan, pushed **copies** of the crew keys into Herman's OWN container store **`/opt/data/home/.secrets/`** (= host `~/.hermes/home/.secrets/`, 0600, lands as `hermes:hermes`): `crew-api-keys.env` (GEMINI/ANTHROPIC/KIMI/BRAVE + `CREW_ANTHROPIC_API_KEY` alias) + raw json copies. **`ANTHROPIC_API_KEY` here = the crew/Fable key — do NOT let it clobber his GLM brain key** (both share the name; use the alias). Herman builds his own local crew scripts + manages model versions (Gemini was pinned to 1.5 via the forced cmd → his to bump to 2.5 now). Copies → re-push from `~/.secrets/` if a provider key rotates.
- **📊 Crew Meter dashboard (durable URL, 2026-07-10):** token-usage / model-cost / $100-mo budget monitor at container `127.0.0.1:8300` (reads a local SQLite). 🚨 **BROKEN 2026-08-01 — the URL is dead, the service is fine.** Tearing down the OpenJarvis voice experiment, `tailscale serve --https=443 off` cleared the **entire** serve config, not just that experiment's `:443` proxy — taking this route with it. Port 8300 still answers `/healthz` 200. **Restore: `tailscale serve --bg 8300`.** *(Lesson: `serve` config is additive in, TOTAL out — capture `tailscale serve status` before changing it.)* → [[gilligan-voice-platform]]. Original routing via **Tailscale Serve on host `gilligan`** → **`https://gilligan.tail5807bd.ts.net/`**, tailnet-only (Jason's phone+laptop already on tailnet), HTTPS, auto-starts on reboot (tailscaled enabled + serve config persists). Works because the container is `net=host` → container-localhost == host-localhost, so host `tailscale serve --bg 8300` reaches it. `/healthz`→ok. (Cloudflare named-tunnel was NOT viable — no CF account + neither GSTS domain is on Cloudflare.) Setup needed root once → done via docker-group (`tailscale set --operator=wade3337`), Skipper-approved. Manage: `tailscale serve status`.
- **Live read-only DB:** `/opt/data/home/trimit-query.sh` (pipe SQL) → key `/opt/data/.ssh/gilligan_access` → gilligan's forced-command gateway → `HermanRO` (db_datareader, play, ~24h behind). Cannot write.
  - **🩹 Nightly-wipe fix (2026-07-03):** the play GSTS is restored from prod at **3:00 AM daily** (task `GSTS DB RESTORE`), which WIPES the HermanRO *database user* (prod has none) → login can't open its default DB → `Login failed for user 'HermanRO'`. The old DB-box schtask `HermanRO-ReGrant` ran as `NT AUTHORITY\SYSTEM` (NOT sysadmin) with `sqlcmd -E` and **no `-b`** → its `CREATE USER` was denied every night but reported success = **silent failure**. **Fix:** disabled that schtask; re-grant now runs **from gilligan** via `~/herman-gateway/regrant-hermanro.sh` (cron `*/30 * * * *`) as Administrator (IS sysadmin), idempotent, `-b`, logs to `regrant.log`. Also set the HermanRO login's default DB → `master` (+ master user) so it no longer hard-fails. **Proven** by simulating the wipe (DROP USER → read fails → re-grant → read works, 923 tables). (KB repos: `wade3337-ctrl/trimit-knowledge` + `/arbor-knowledge`; local `~/trimit-knowledge`, `~/arbor-knowledge`.)

## 💾 Backup & recovery (2026-07-03) — 3 layers
- **L1 self-recovery:** his `self-backup` skill → local rotated snapshots `/opt/data/backups/brain-*.tar.gz` (config+memory+skills; no secrets/vault-copies). Run before risky changes.
- **L2a off-box brain:** gilligan cron `~/herman-gateway/backup-herman-brain.sh` (nightly 03:45) → private repo `wade3337-ctrl/herman-brain-backup` (secret-scan guarded).
- **L2b off-box secrets (encrypted):** his `.env`/`auth.json`/`.codex`/`.ssh` added to `~/backups/refresh-recovery-bundle.sh` (real file: `~/.openclaw/workspace/recovery/…`, it's a symlink) → AES-256 bundle → nightly git push. Restore needs the Skipper's passphrase.
- Detail + redeploy use → arbor `[[boss-herman-deployment]]`.

## 🔌 Herman — the Arduino field companion (separate machine)
- **Herman** = companion agent on the **Arduino board** (`herman@100.121.177.31`, rrsync-only key → `~/herman-store/`), the portable field assistant. Specs: `arbor-stack/herman-agent-specs.md`.

## 💾 Hermes LAPTOP (`desktop-4v2p8at`) — DECOMMISSIONED 2026-06-23 (historical)
- Removed after the GLM-brain experiment dead-ended; failover moot; `~/laptop-store/` + `~/.ssh/laptop_hermes_ed25519` dead; laptop z.ai key REVOKED. Detail: `memory/2026-06-19-2302.md`.

**Three names, don't mix:** 👑 **Boss Herman** (container, the AI boss) · 🔌 **Herman** (Arduino field companion) · 💾 Hermes laptop (dead).

## 🔓 PLAY write access (activated 2026-07-23)
Boss Herman now has **full Administrator write on PLAY** — the Jul-10 "same access as Gilligan" grant, finally wired 2026-07-23 (his RW key was in the wrong `.ssh` dir + play wasn't in his `known_hosts`). Tools in his container `/opt/data/home/`: **`gsql.sh`** (read + **write** SQL), `view.sh` (render-verify), `deploy-to-play.sh` (webroot + auto-backup). Guardrails `herman-gateway/HERMAN-PLAY-WRITE-SETUP.md`: backup-first · render-verify · **PLAY ONLY** · log-it · ask-before-destructive. Revoke = remove his key line from play `administrators_authorized_keys` (Gilligan's is a separate key). **Proven same day:** built the entire Goodman GPS pilot start-to-finish across Tasks 4–6 (careful, disciplined, self-documenting) → [[goodman-rfp-bid]], [[trimit-gps-import-pipeline]].

## ✍️ Task hand-off = copy-paste via chat, NOT an automated line (Skipper, 2026-07-24)
**STANDING PREFERENCE:** dispatch Boss Herman tasks by handing the Skipper a **clean, paste-ready prompt** he pastes into Herman's chat — do **not** stand up / pitch the staged Gilligan→Herman direct hook (`gilligan-to-hermes-line-SETUP.md`) or dispatch via `docker exec`. Rationale: copy-paste through the chat channel **trains Herman to operate the way he really will in production** (chat-driven, no privileged pipe). So: craft the prompt to the repair/build contract, hand it over, and verify Herman's returned output — that's the loop. (Direct line stays deferred; don't re-offer it.)

## Related
- [[crew-llms-and-helpers]] — the LLM crew (distinct from these agents).
