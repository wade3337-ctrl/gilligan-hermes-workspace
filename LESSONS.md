# LESSONS.md — What Didn't Work (gotchas · dead-ends · anti-patterns)

**Purpose:** the ONE place I check *before* a task to avoid repeating a known failure. Paired with `PLAYBOOK.md`
(what works). **🔄 AUTO-UPDATE RULE:** the instant something fails or wastes real time, I add a one-line entry here,
tagged by domain — *in the moment*, not "later." Kept **lean** (consolidate, don't pile). In my lookup path via
`ROUTING.md` + `MEMORY.md`. Format: `- ⚠️ <the trap> → <the fix/avoid>. (date)`

## 🤖 Models / CLI tooling
- ⚠️ **Kimi K3: slow but sol-level — LEAN ON IT as crew (Skipper directive 2026-07-21, upgraded K3 sub).** Validated 2026-07-19 K3 tied `gpt-5.6-sol` 100%/100% on a blind hidden-test coding+reasoning duel; always-on thinking makes it ~7× slower. Per Skipper: use K3 as a **first-class quality/cross-lab verification crew judge and give it time** (900s timeouts, run in background) rather than dropping it for speed. Only route genuinely latency-critical / agentic-coding work to Codex; for quality reviews K3 is a go-to, not a fallback. (updated 2026-07-21)
- ⚠️ **OpenClaw generic Discord error after Opus stops can be Claude Code auth, not Discord/OpenClaw** — logs showed `claude-cli` returning `401 Invalid authentication credentials` while `claude auth status` still said logged in. → check `journalctl --user -u openclaw*` / `/tmp/openclaw/openclaw-YYYY-MM-DD.log`; fix by refreshing Claude CLI auth (`claude auth login`, then `claude setup-token` if needed). (2026-06-29)
- ⚠️ **Opus can have two simultaneous auth blockers** — Claude CLI may be stale (`claude -p` returns `401` even when `claude auth status` says Max/loggedIn), while OpenClaw manual Anthropic token may be valid but blocked by Claude's "third-party apps draw from extra usage" billing gate. → test both paths separately: direct `claude -p` for CLI, `openclaw agent --model anthropic/...` for manual token; fix CLI with `claude auth login`, fix manual token by adding extra usage at `claude.ai/settings/usage` or using a fresh setup token with available balance. (2026-06-29)
- ⚠️ **Ollama `llama3.2:3b` at 131k context can effectively wedge the laptop** — logs showed CPU runner allocating ~14GB KV cache on a 15GB box after Claude/OpenClaw fallback, followed by Docker/Discord timeouts and stuck OpenClaw runs. → keep Ollama CPU-only for the unsupported GTX 1050 Ti and cap local model context around 4k/8k unless intentionally running a memory test. (2026-06-30)
- ⚠️ **GLM (& Codex) agentic `claude -p` CLI is UNRELIABLE here** — it **HANGS (124) OR SIGKILLs (137)** on tool
  loops. Root-caused 2026-06-23: the CLI's **agentic orchestration with the backend** is the broken part, NOT GLM or
  the box (GLM via direct API answers in seconds). → use `~/arbor-core/crew/glm-ask.py` (reasoning) or
  **`glm-judge.py`** (tool-running DB-verification via a stable Python loop). (2026-06-21 / 23)
- ⚠️ **Codex `codex exec` also dies (137) on heavy *nested* runs** — the sandbox kills fat subprocess trees;
  lightweight calls (gsql, GLM-API) are fine. Don't depend on long nested CLI agents. (2026-06-22)
- ⚠️ **Background sub-agents get killed mid-run** (the v1.1 producer was) — the **main session is stable**; do
  consequential long work there, not in detached background agents. (2026-06-22)
- ⚠️ **Kimi empty/truncated output = the HELPER `max_tokens` cap, NOT the account** — K2 is a reasoning model and burns the budget *thinking* before it emits the answer, so "ran out of budget" was `kimi-ask.py`'s cap (raised 8k→16k→**40k**) while the Skipper's Kimi account was ~1% used. Raise `max_tokens` before suspecting billing. (2026-06-28)

## 🗄️ DB / SQL (TRIM IT, sqlcmd via gsql.sh)
- ⚠️ **The GSTS play SQL box is SHARED and gets hammered by daytime SSMS load** — a heavy analytical query (retention sessionization) ran **<1s when the box was quiet** but **timed out (>4–5 min)** minutes later while someone ran heavy work in SSMS. `sqlcmd "Timeout expired"` = a **QUERY** timeout (server too slow), **NOT** a network drop — confirmed the tailnet was direct/0%-loss the whole time. → schedule heavy/analytical runs **OFF-PEAK (3am PT)**; don't reboot the box chasing a network ghost. (2026-07-15)
- ⚠️ **A proc/view "deployed" per the notes may NOT actually be on the server** — `ret.vRetentionTrend` (054) was written + bug-fixed in the .sql file but its `OBJECT_ID` was NULL (never applied). → `SELECT OBJECT_ID('schema.obj')` for every object before validating, and redeploy from the .sql files. (2026-07-15)
- ⚠️ **`gsql.sh` takes a FILE or STDIN, not an inline arg string** (it `cat`s $1) → pipe SQL on stdin. (2026-06-21)
- ⚠️ **`gsql.sh` writes ONE shared remote temp file** → concurrent queries clobber each other → run **sequentially**
  or use a per-process isolated wrapper. (2026-06-21)
- ⚠️ **Reserved words as a column alias break sqlcmd** ("Incorrect syntax near 'proc'") → use a safe alias (`pname`). (2026-06-21)
- ⚠️ **`SUM(CASE WHEN EXISTS(subquery)…)` / aggregate over a subquery → SQL error 130** → compute the per-row flag in a CTE/derived table, then `SUM()` it. (2026-06-25)
- ⚠️ **`dbo.ZoneDefs.ColorCode` TEXT is a useless uniform 'Yellow' on every zone — the REAL per-zone color is `ColorDefID` → `dbo.ColorDefs.ColorCode` hex** (Green/Orange/Blue/Red matching the printed map). First zone-importer preferred the text field → every area came out yellow. → resolve color via ColorDefID, fall back to the text only if the FK is null. (Reverses an earlier wrong call to "trust zd.ColorCode text.") (2026-07-07)
- ⚠️ **sqlcmd (gsql.sh) truncates `varchar(max)` at 256 chars** → a long value (e.g. `ZoneDefs.FusionTablePolygon` KML, up to ~2KB) comes back cut off. → fetch it in ≤160-char **bracket-wrapped** chunks (brackets survive `-W` trailing-trim) + reassemble in Python. (2026-07-07)
- ⚠️ **Some TRIM IT `ZoneDefs` rows are junk** — C2E Townhomes (1283100) had 5 *identical duplicate* polygons + 1 that parses to 0 points, all one color. The pretty multi-color map was hand-drawn onto the image, NOT saved as data. → before trusting a site's zones, check for distinct geometry; don't assume the colorful map == pullable vector data. (2026-07-07)
- ⚠️ **A literal `#` in ANY double-quoted CFML string = "missing ending #" / interpolation error — NOT just inside `<cfoutput>`** (also `<cfset x="...#77...">`, array literals, etc.). Bit me twice in one day (inline `color:#fff`, then `#77` inside a `cfset` repairRows array). → escape as `##`. (2026-06-25)
- ⚠️ **Don't BATCH the live `Reference-RepairsAndScheme.cfm` sync** — update its `repairRows`/`buildRows` when you write each `gsts-ship-log.md` row, not "later." Let it drift 13 entries behind once (Skipper caught it). Edit it byte-level (ASCII-only inserts after a newline) since it's UTF-8 + may have a BOM. (2026-06-25)

## 🗺️ Leaflet / maps
- ⚠️ **Leaflet CANVAS renderer (`preferCanvas:true`): the DOM `pointer-events:none` trick to make an overlay click-through is a NO-OP** — canvas paths have no own DOM element, so `layer.getElement()` returns nothing and the style never applies. A filled *interactive* polygon then swallows clicks meant for markers underneath (bit the workbench lasso twice — #77 "fixed" it this wrong way). → set **`interactive:false`** on the overlay so Leaflet skips it in JS hit-detection; the shape still renders. (2026-06-25)

## 📧 Email (gilligan.gsts gmail)
- ⚠️ **Self From=To lands in Sent, not Inbox** → keep From≠To.
- ⚠️ **Rep daily emails are plain-text only (no HTML part)** → extract `text/plain` when copying/forwarding; grabbing
  the HTML part returns BLANK (I sent the Skipper an empty copy once). (2026-06-22)
- ⚠️ **ImapFlow by-UID lookup** needs `{uid:true}` in the OPTIONS (3rd) arg of `fetchOne`/`fetch`, NOT the query — a
  bare number is a *sequence* number → silent false→crash. (2026-06-20)
- ⚠️ **I can read `gilligan.gsts@gmail.com` only — NOT `jwade@gstsinc.com`** (no M365). Inbound to jwade must be forwarded. (2026-06-22)

## 🌳 TRIM IT / web (ColdFusion)
- ⚠️ **The GSTS app's `.cfm` files aren't in `C:\...\wwwroot\GSTS`** (near-empty) — they serve from the **D: webroot**.
  Don't conclude "missing" from the C path. (2026-06-22)
- ⚠️ **Menu "window" names ≠ filenames** — resolve the real page via `dbo.AppForms.ObjectPath`; an app's JS endpoints
  are **relative to that app's folder** (e.g. `FieldApp/...`), so root-level fetch 404s. (2026-06-22)
- ⚠️ **`.cfm` with emoji/non-ASCII needs a UTF-8 BOM** or ColdFusion serves mojibake (`ssh type` strips it). **Cleaner alt: use HTML numeric entities** (`&#NNNN;`) → ASCII source, no BOM. CF only treats `#` special *inside* `<cfoutput>` → single `#` in static HTML, **double `##` inside cfoutput** (collapses to one on render). (2026-06-25)
- ⚠️ **Substring role-matching trap:** CFML `CONTAINS "coo"` matched "**coo**rdinator" → Sales/Production *Coordinators* mis-tagged as exec. Use **padded whole-word checks** (` coo `/` ceo `) for short tokens; avoid over-broad keywords like bare "director". Always test role logic against real users. (2026-06-25)
- ⚠️ **Dual-webroot shadow:** `C:\ColdFusion...\GSTS` can OVERRIDE `D:\...\GSTS` → render-verify the *served* output.

## ⚙️ Config / infra
- ⚠️ **Changing `model.primary` in `openclaw.json` does NOT move an EXISTING long-running session** — it has a sticky per-session model binding in `~/.openclaw/agents/main/sessions/sessions.json`. After the Opus 5 upgrade + gateway restart, config read `anthropic/claude-opus-5` but the live session still resolved `claude-cli/claude-opus-4-8`. → the config default governs **NEW** sessions/crons/subagents; for the current one re-apply the session override (`session_status` with `model=...`). Don't hand-edit sessions.json (gateway owns it). Also: `openclaw gateway restart` **kills the agent process running it** → the restart command returns exit 137 to me; that's expected, not a failure — confirm via the follow-up system event + `session_status` uptime. (2026-07-24)
- ⚠️ **Adding an auth gate to a TRIM IT dashboard silently breaks automated CONSUMERS that scraped it unauthenticated** — the V1.5 `dashboard-auth-gate.cfm` (2026-07-12) started 403-ing the anomaly-monitor's `bookedPacing()` POST to `Dashboard-RevenuePerformance.cfm` → the daily email quietly showed "Forward pace unavailable (HTTP 403)" for days. Same bug hit `revenue-block.js` (salesperson + Nate emails). → the gate authorizes on a **`ZUserID` cookie** (bootstrap-allowlist 9=Jason, **376=Arbor Helper bot**); fix = send `Cookie: ZUserID=376` on the fetch. LESSON: when hardening a data surface, **grep for every headless/automation fetch of it** and re-authorize them in the same change. (`m2-revenue.js` has the same cookie-less pattern but is unused legacy.) (2026-07-15)
- ⚠️ **`~/.openclaw/openclaw.json` has clobber history** → always back up + merge-patch, never overwrite.
- ⚠️ **Python 3.14 here has no package manager + no passwordless sudo** → use stdlib; Skipper installs system pkgs.
- ⚠️ **No headless browser** → authenticated HTTP fetch (view.sh) for web/ERP pages.
- ⚠️ **MEMORY.md silently clipped at ~20k** → `agents.defaults.bootstrapMaxChars`/`bootstrapTotalMaxChars` were UNSET in `~/.openclaw/openclaw.json` (bootstrap truncated). Set them (40000 / 160000), back up the config first, and restart to apply. (2026-06-28)

## 🧑‍🤝‍🧑 Working with the Skipper / process
- ⚠️ **Don't let background/inter-session chatter drown the user** (`[[subagent-completion-noise]]`) — surface real
  status; never go silent through a direct question.
- ⚠️ **Don't over-claim beyond the evidence** — GLM caught me twice asserting *behavior* from row-counts (pricing map).
  State only what's proven; flag inferences as inferences. (2026-06-21)
- ⚠️ **"Set up" ≠ working** — laptop GLM was reported "set up" but had **NO key/client/config** at all. Always VERIFY
  the config actually persisted (key present? endpoint right? client runs?) before assuming a setup landed. (2026-06-23)
- ⚠️ **Don't run a crew gate where only ONE of us can see the result** — a background-shell run leaves the Skipper blind to it; a `sessions_spawn` sub-agent leaves ME blind to it (supersedes the 06-26 "spawn a sub-agent for long crew work" note). → run gates **FOREGROUND, in-turn**; confirm the Skipper got it, re-send if it doesn't land. (2026-06-28)

## 🪟 Windows / SSH (Hermes laptop)
- ⚠️ **Recursive `dir /s` / `findstr /S` over the user profile or a venv HANGS the SSH session** (timed out 3× in a
  row) → keep Windows recon **NON-recursive**; target specific files/dirs. (2026-06-23)
- ⚠️ **My laptop SSH key can get de-authorized** (it was removed from `administrators_authorized_keys` during a
  Herman setup) → if `Permission denied (publickey)` but host key matches, my key was dropped; have Herman re-add it. (2026-06-23)
- ⚠️ **`icacls … /inheritance:r` may not stick if chained loosely** — run it cleanly and CONFIRM with a follow-up
  `icacls <file>` (look for `(I)` = still inherited). (2026-06-23)
- ⚠️ **Laptop Herman brain = GLM is UNRESOLVED (2026-06-23):** Hermes's built-in **z.ai provider uses the METERED
  `/paas/v4`** (blocked on the coding plan → "insufficient balance"); forcing `glm-5.2` via Hermes's **anthropic**
  provider + the z.ai **anthropic** endpoint produced **"no final response"** (Hermes↔z.ai coding-endpoint
  compatibility gap — needs `hermes` debug-log analysis). Herman was ALREADY pre-set to glm-5.2/metered (= the
  original "GLM not working"); its model config is **NOT** in `~/.hermes/config.yaml`. GLM-as-a-TOOL works (`glm-ask.py`).
- ⚠️ **ColdFusion: literal `#` inside `<cfoutput>` errors** — a hex color (`color:#ffe08a`) or any `#...#` in cfoutput is read as a CF variable → "An error occurred" with debug off. **Escape as `##` (`##ffe08a`)** or move the literal outside cfoutput. Cost ~2 deploys to spot (2026-06-23, ZTest-SiteMap).
- ⚠️ **CF dynamic SQL: avoid `--` line comments inside `<cfquery>`** — if the newline collapses, `--` comments out the rest of the query. Use `/* ... */` block comments. (2026-06-23)
- 💡 **TrimIT geo data has bad geocodes** — active job sites include sign-flipped longitudes (+117 vs -117 → Asia) and mis-geocodes (UK/NM/ID). Any map MUST bound to a SoCal sanity box (lat 32–35.5, lng -121 to -114) and *count+flag* the strays, never silently fit-to-all (4/2906 active blew the view worldwide). Fixable in the geocode backfill pass. (2026-06-23)
- ⚠️ **Leaflet canvas (preferCanvas): don't `removeLayer`/`addTo` circleMarkers to filter** — they fail to repaint on re-add (filter down works, clearing the filter leaves them gone). Instead keep all markers on the map and toggle visibility with `setStyle({radius:0,opacity:0,fillOpacity:0})` for hidden / real style for shown — `setStyle` repaints reliably both directions, and radius:0 = visually off the map + no overlap. (2026-06-23, ZTest-SiteMap)
- ⚠️ **Don't trust a "model X is flaky" verdict without a repeat test.** Codex was written off as flaky (Jun 25 City Budgets, exit143) — re-tested Jun 26, 8/8 clean incl. a heavy 3-query verify. The real causes were (a) a transient blip and (b) MY prompt leaking a sentence period as a shell arg (`gsql.sh .` → cat-ed a dir → empty → model reported "0"). Check ground truth + prompt hygiene before blaming the tool; one bad run ≠ broken. (2026-06-26)
- ⚠️ **Shell-wrapper scripts should fail loudly on a stray/garbage arg**, not silently return empty. `gsql.sh` used `cat "${1:-/dev/stdin}"` → a `.` arg cat-ed a directory → empty SQL → a model misread the blank as a real `0`. Guard: if `$1` is set and not a readable file, error+exit. (2026-06-26)
- ⚠️ **dbo.SalesRepPerformance is a SNAPSHOT, not a source** — populated by `GenerateAllSalesRepPerformance` (INSERT…SELECT from other tables), per-period rollup (PeriodID + InvoiceTotal/NetTotal/TPH cols), 0 triggers. Do NOT build arbor-core sales attribution/perf on it; use dbo.SalesReps (base entity: SalesRepID/IsMeasured/StatusDefID) + transactional sources. Verified by Codex lineage judge 2026-06-26. (2026-06-26)
- ⚠️ **Crew HTTP helpers must use a generous timeout + catch timeouts, not just HTTPError.** `gemini-ask.py` had `timeout=120` and only caught `HTTPError` → big reasoning prompts (3.1-pro on a full schema-gate dump) took ~121s and died with a raw `TimeoutError` traceback (silent `[gemini 1]`, empty output) — looked like "Gemini is down" but the API + key were fine. Fixed: 300s default (`GEMINI_TIMEOUT`) + retry loop catching `URLError`/`TimeoutError`/5xx/429. Apply the same pattern to every `*-ask.py` helper. (2026-06-26)
- ⚠️ **Always feed the crew the CURRENT artifact — stale-prompt = wasted run + muddled report (2026-06-26).** Ran a Kimi schema judge but pointed it at the OLD v1.2 review prompt (a leftover lens file), so it judged v1.2 while the live schema was v1.6 → most of its 12 findings were already fixed (e.g. it flagged the per-entity-transition issue we'd resolved in v1.4). Had to cross-check every finding against current to separate stale from live. Rebuild the evidence bundle from the CURRENT version before every (re-)gate; never reuse an old lens file. (Bright side: it still caught 1 real live gap — an undefined `agent` table — proving the value, but that was luck, not hygiene.)
- ⚠️ **HTML numeric entities (e.g. `&#127807;` 🌿) inside `<cfoutput>` BREAK ColdFusion** — the `#` starts a CF expression, so `&#127807;...$#NumberFormat(...)#` gets parsed as one giant expression → COMPILE error → whole page dies with a generic "error occurred". Cost ~40 min mis-blaming a SQL query (which was fine). **Fix: escape as `&##127807;`** (CF `##`→literal `#`) or paste the raw emoji char. Named entities (`&mdash; &amp;`) are safe (no `#`). When a cfquery page dies and the SQL runs clean standalone, suspect a `#` in the cfoutput HTML before the query. (2026-06-26, FinancialReportDashboard treatment line.)
- ⚠️ **Verify every SSH `copy` backup actually wrote** (read it back / check size) — a chained `if not exist ... & copy` or a bad path fails SILENTLY on cmd.exe; I restored from a "backup" that was actually the original and reverted a dashboard's whole feature set. Always `dir`/`type | grep` the backup after copying, and keep a local `scp`-down copy of anything important. (2026-06-26)

## 🧠 Agent brains / model auth
- ❌ **Don't run an OpenClaw-style agent's brain on a Claude SUBSCRIPTION token (work Enterprise / Max) for real agent work** — Anthropic now meters third-party apps separately: plain chat passes, but a full tool-using request → **HTTP 400 "Third-party apps now draw from your extra usage, not your plan limits."** Hermes's Claude-Code disguise (identity + ua + `mcp_`→`mcp__`) clears the 429 identity wall but NOT the billing classifier. Chasing it = fragile + ToS-gray. Use a sanctioned flat-rate provider (GLM/z.ai sub) instead. (2026-06-30)
- ❌ **z.ai SUBSCRIPTION only authorizes the Anthropic-compat endpoint** (`/api/anthropic` or `open.bigmodel.cn/api/anthropic`), NOT the OpenAI endpoint (`/api/paas/v4` → "Insufficient balance"). So Hermes's *native* `zai` provider (which uses paas/v4) fails on a sub — use `provider: anthropic` + `base_url` override to the z.ai anthropic endpoint. (2026-06-30)
- ❌ **Dotted model ids get mangled under `provider: anthropic`** — Hermes's `normalize_model_for_provider("glm-5.2","anthropic")` → `glm-5-2` (dot→hyphen), which z.ai rejects (`1211 Unknown Model`). Fix: use the **non-dotted alias** (`glm-5` → resolves to glm-5.2). ALWAYS check the model id the GATEWAY actually sends (log `model=…`), not a hardcoded client repro — the repro hid the bug. (2026-06-30)

## 🖥️ Frontend / preact single-file app (arbor-core one-stop UI)
- ❌ **TDZ crash blanks the WHOLE preact app + `node --check` won't catch it** — a `useState` var referenced in a `useEffect(cb,[x])` **dependency array placed ABOVE** its `const [x]=useState()` line throws "Cannot access 'x' before initialization" at RENDER (the dep array is evaluated during render, even though the callback is lazy). One component throwing → entire app renders blank → "nothing loads, clicks do nothing." Syntax-check passes because TDZ is a *runtime* error; symptom was masked because the app resumed on the broken step via localStorage. Fix: declare state BEFORE any effect/expr that names it; catch headlessly with the PLAYBOOK render-harness. (2026-07-01)
- ❌ **Polygon orthogonalize (square-to-axis) SPIKES irregular/concave shapes** — forcing every edge to H/V makes consecutive same-orientation edges collapse into ~0°/~180° "spike" vertices or self-intersections. Looked like "tidy worked on zone 1, broke zone 2" (zone 1 was rectangle-ish, zone 2 wasn't). Any geometry "cleanup" must **validate + fall back** so it never worsens input (drop degenerate turns · reject self-intersection · area-ratio guard · else keep the simplified ring). (2026-07-01)

## 🗄️ Legacy-DB → Postgres pipeline + pricing engine (2026-07-01)
- ❌ **Windows `sqlcmd` output is CRLF → breaks Postgres `\copy … FORMAT csv`** with "unquoted newline found in data" (the trailing `\r` corrupts the last field / row boundary). Always `tr -d '\r'` between the gsql pull and the COPY. (Cost one failed load before spotting it.)
- ❌ **Postgres has NO `round(double precision, int)`** — only `round(numeric, int)`. `ROUND(col * %s, 4)` where `%s` is a Python float makes the product `double` → "function round(double precision, integer) does not exist". Cast: `ROUND((col * %s)::numeric, 4)`. (Python-side `round()` before insert sidesteps it.)
- ❌ **browserless `/function` returns an EMPTY 200 unless the module returns `{data, type}`** (a bare object/value serializes to nothing). And `page.waitForTimeout` is gone in newer Puppeteer → `await new Promise(r=>setTimeout(r,ms))`.
- ❌ **Substring checks on human-readable labels collide.** Fallback-tier logic keyed on `"species" in label` wrongly fired for the label `"size (any species)"` (contains "species"), so a species-agnostic aggregate mislabeled itself with a random `MIN(species)`="Acacia". Use explicit boolean flags per tier, never substring-match a display string for control flow.

## 🖥️ ColdFusion / TrimIT dashboards (2026-07-02)
- ⚠️ **Granting a TRIM IT user ALL permissions BREAKS their login** ("stuck on welcome page"). `ActionDefs` **52 Web Portal Access** + **53 Field App Access** are *interface-routing* flags, not power — turning them on for an internal/desktop user misroutes login toward the external portal / mobile app. → to make an admin, **clone a known-good Developer account's `UserActions` + `SecurityLevelID` exactly** (incl. its intentional denials), never "set everything allowed." (jwade access, 2026-07-08.)
- ⚠️ **Reports that gate their row-LIST on `dbo.CompanyYears` "covering today with hours>0" EMPTY OUT at every fiscal-year turnover** — City Budgets showed only 2 cities on Jul 1 because the new-FY hours are 0.0 (not entered yet), and the `GETDATE()` gate also made the FY dropdown inert (wouldn't roll back). → drive the list off the **contract budget for the VIEWED fiscal year** (approved non-Homeowner contract whose year-label matches the selected/resolved FY), and show a row only if it has budget/activity for that label. Stable across turnover + follows the picker. (ship-log #108, 2026-07-08; a *recurring seasonal* bug class — check any report keyed on `CompanyYears`+today.)
- ❌ **`NumberFormat(v,'___,___')` emits LEADING SPACES for small numbers** ("$   135"), and in a narrow HTML table cell those spaces are line-break points → the `$` wraps ONTO ITS OWN LINE ABOVE the number (Skipper reported it on a TPH column). For HTML money use `NumberFormat(v,',')` → clean "$135"/"$196,343" (comma grouping, no width padding). Belt-and-suspenders: also `white-space:nowrap` the numeric cells. The `'___,___'` mask is for monospaced/`<pre>` alignment, not HTML.
- ❌ **`crew/gemini-ask.py` arg-handling is broken** — it sends `argv[1]` as the prompt (so a file path or a `max_tokens` digit becomes the literal prompt) instead of reading the file / stdin like the template. **Call it via pure stdin with NO positional arg:** `cat prompt.txt | python3 gemini-ask.py`. (GLM/Kimi honor the `file [max_tokens]` convention; Gemini doesn't.)

## 🌙 Overnight Herman KB build (2026-07-02)
- ❌ **`cmd &` background jobs in a Bash tool call get KILLED when the tool returns** — launched 3 crew asks with `&`; only the one that finished before the shell exited survived, the other two were orphaned/killed (0 bytes, no done-flag). Run crew/long jobs FOREGROUND (blocking) or use the Bash tool's `run_in_background`, never bare `&` in a call that returns immediately.
- ❌ **A crew *read-through* review missed a SQL bug the crew's own FIX introduced** — the fix aliased a column `AS Current` (a T-SQL reserved word) → syntax error. Caught only because I RAN every recipe against the live DB. Lesson: for a query playbook, EXECUTE the SQL — don't trust prose review. `Current`/reserved words need `[brackets]`.
- ✅ **`kimi-ask.py` long-prompt timeout FIXED + Kimi K3 is now a first-class crew member (Skipper 2026-07-21, upgraded K3 sub → "lean on it hard").** The old SIGKILL/exit-143 on big bundles was the *caller's* timeout killing a slow model, NOT a Kimi limit. Fix: script timeouts raised 300→**900s** (`KIMI_TIMEOUT` in arbor-core + MuniBot `kimi-ask.py` + `kimi-worker.py`); and **invoke Kimi in the BACKGROUND** (or with the max Bash timeout 600000ms) so slow ≠ killed. K3 = sol-level quality → **lean on it as a crew judge for reviews; don't skip it for slowness — give it time.** (supersedes the old "keep Kimi for short prompts" note)

## 🔐 HermanRO DB access broke nightly, silently (2026-07-03)
- ❌ **`sqlcmd` without `-b` exits 0 even when the SQL errors** — so a Windows scheduled task (or any wrapper checking exit code) shows "success/Result 0" while its `CREATE USER`/`ALTER ROLE` fails every run. The re-grant job "ran fine" for days while Boss Herman's DB user stayed missing. ALWAYS pass `-b` on any sqlcmd that mutates, and log its output — exit code alone lies.
- ❌ **`NT AUTHORITY\SYSTEM` is NOT a SQL sysadmin by default** — a schtask running `sqlcmd -E` as SYSTEM couldn't `CREATE USER`. Don't assume SYSTEM = DB admin. Fix without escalating the box: drive the grant from a host that already has a sysadmin login (we run it from gilligan as Administrator on a cron).
- 🔁 **Restoring a DB from another instance ORPHANS/wipes its db-level users** (server logins survive, database users don't). Anything granted at the *database* level (users, role membership) must be re-applied AFTER every restore. Poll/re-grant on a schedule tighter than the restore cadence, and prove it by simulating the wipe (DROP USER → verify broken → re-grant → verify fixed).

## 🌐 Web research helper bugs (2026-07-03)
- ❌ **`curl … | python3 - <<'PY' … sys.stdin.read() … PY` reads NOTHING** — the heredoc IS stdin, so it becomes the *script* and the piped data never reaches `sys.stdin`. Symptom: helper silently returns empty. Fix: put the parser in a **standalone `.py` file** (`curl … | python3 parser.py args`) or use `python3 -c`. Bit websearch/webfetch/ollama-ask all at once.
- ❌ **Keyless DuckDuckGo scraping throttles fast** — first query works, then the IP is blocked (0 results). Fine for a one-off, NOT for a process. Use a keyed search API (Brave free tier 2000/mo) for anything Boss Herman runs regularly. Reader can stay free (local Ollama llama3.2:3b — proven to summarize real fetched pages well).

## 🤖 Don't run high-volume intake INSIDE the agent's live conversation (2026-07-03)
- ❌ **The Hermes email gateway adapter is a two-way email *assistant*** — it feeds inbound mail into the agent's ONE live conversation and auto-replies to the sender. A 6-email burst tangled the requests ("all three drafted"), spam-looped on our OWN test inbox (verified: 0 real-customer addresses ever), and JAMMED Boss Herman (required a restart). A skill-level "delegate + report to Telegram" rewire could NOT override the adapter's structural behavior.
- ✅ **Fix = a standalone background service** (`rfp-watcher.py` cron + `rfp-intake.py`) that uses the agent's TOOLS/crew but runs ONE isolated OS process per email (parallel-safe), reports to Telegram via the bot API, never touches the customer thread or the agent's live chat. Deterministic pipeline > agent-conversation for volume; keep the agent free for judgment + chat.
- ⚠️ **Rapid docker restarts** of a Telegram-bot gateway → Telegram takes ~1 min to reconnect (getUpdates/DoH dance); don't panic, wait it out. And restarting CLEARS the agent's live session (in-flight chat context lost; skills/memory persist).

## 🌳 Field-App map blank / TrimIT REST + diagnosis discipline (2026-07-04)
- ❌ **Taffy REST routes live in ColdFusion APPLICATION scope** — a CF app restart (or `?reinit` on another page) can leave a route UNregistered → `GSTS/API/index.cfm/rest/<route>/` returns **404 HTML**, `apiCall.cfm`'s cfhttp POST gets HTML not JSON → `DeserializeJSON` throws → 0 inventory → **blank field-app map (null-island)**. Not a Maps/Google bug, not a broken IIS connector. **Fix: hit the API app with `?reinit=true` to re-register routes** (verified the POST then returns 200 + real JSON). Optional hardening: guard `apiCall.cfm`'s DeserializeJSON to fail gracefully. (2026-07-04)
- ⚠️ **Don't ship a defensive "guard" as THE fix before confirming root cause on the LIVE runtime** — I mis-diagnosed the blank map TWICE (first "Google retired Maps v3.64 → DrawingManager throws", then "broken CF↔IIS connector 404") and deployed a harmless guard as if it were the fix, before browser-Claude's live console showed BOTH were wrong. `view.sh` can't run JS → use the headless browser / browser-Claude console to see the ACTUAL runtime error before declaring a fix. (2026-07-04)
- ⚠️ **Verify dev-handoff PROD state before surfacing it as a to-do** — several "pending" dev items were already deployed (Anaheim GenerateContractPeriod #71, Long Beach 26/27, Contract Dashboard #11). Play mirrors prod → check the live proc/data first (the fix present? the number rolling up?) instead of re-flagging phantom work. (2026-07-04)
- ❌ **A project's trees are NOT reliably found by `InventoryDetail.ProjectID`** (unset/NULL on GPS-imported rows) — I queried it, got 0, and wrongly concluded "trees wiped / refresh ate them / publish gate missing." **The rows were there the whole time.** → Resolve a project's inventory via the **join `Projects.LocationID → InventoryDetail.LocationID`** (e.g. Eastvale proj 1105467 → LocationID 1285096 → 313 trees). Confirm before declaring anything "missing." (2026-07-24)
- ⚠️ **GPS "publish" = TWO steps; the GeoJSON generator alone isn't publish.** apiCall.cfm generating the grey `shaded_dot` layer does NOT remove blue new-plot dots — the blue layer is gated `ISNULL(IsNewPlot,0)=1`, so you get blue+grey BOTH until a separate `UPDATE InventoryDetail SET IsNewPlot=NULL WHERE LocationID=<loc> AND IsNewPlot=1` runs. That clear can happen via direct SQL (won't show in webroot .cfm files) — verify by checking `IsNewPlot` state in the DB, not by grepping files. (2026-07-24)
- ⚠️ **`GSTSID` (the card's "tree id") is PER-SITE, not globally unique** — the same value exists as GSTSID on thousands of trees across jobs (customer tag 2043 → 10,365 hits). It's the local tree number. So populating it from a job's survey tags (`LegacyRef`) is correct, not a collision risk. GPS import can leave GSTSID NULL → card shows "0"; copy `LegacyRef`→`GSTSID`. Numeric `Height` can also import NULL while the `HeightRange` bucket is set. (2026-07-24)
- ❌ **`dev.greatscotttreeservice.com` ≠ `play.greatscotttreeservice.com` — THREE separate hosts, and I assessed a vendor's work from the wrong one.** play=**173.208.162.142** (our sandbox, SSH via tailnet 100.86.97.46) · dev=**198.207.148.188** (the VENDOR's build box — no SSH, but **HTTP-reachable**) · www/prod=198.207.148.169. **The trap:** the play box's webroot folder is *named* `D:\home\dev.greatscotttreeservice.com\wwwroot\` so "dev" appears in our own paths while meaning PLAY. I searched play, found nothing since April, and told the Skipper the vendor had produced almost nothing — **wrong, and it could have blown up a vendor confrontation.** The vendor's actual Map Setup page is a substantial build (boundary/vertex editing, artwork-overlay canvas, 3 data APIs). → **Before judging where work does/doesn't exist, resolve the hostname** (`getent hosts …`) and confirm which box serves the page (404 on one + renders on the other = different hosts). **Never infer absence of work from a filesystem you haven't confirmed is the right one.** (2026-07-24)
- ❌ **A `CreatedByID`/`ModifiedBy` audit field that resolves to a real NAME can still be a SERVICE ACCOUNT — never infer "person X does this work" from it.** All 3,033 invoices/yr (50,233 since 2006, incl. this week) stamp `CreatedByID=11` = "Rosanna Baez" — who **left 4 years ago**; account `Inactive`, `Role001="Generate Invoices"` (a *function*, not a title), and **exactly 1 distinct creator every year 2019-2026**. I nearly reported "key-person risk: one person does all billing" — wrong; the routine hard-stamps the ID. → **Tells:** 100% concentration, a role name that's a function, an inactive/departed account, a range spanning decades. Check account status + distinct-creators-per-year **before** attributing work to a human. The real finding was better anyway: **no audit trail on $21.5M/yr of billing.** (2026-07-24)
- ❌ **NEVER derive an org fact (headcount, team size, "who does X") from a distinct-ID count — check it with the human.** `COUNT(DISTINCT CreatedByID)` on bid records returned **24**; the real bid desk is **8** (Skipper). The 24 swept in *former employees* still attached to records in the trailing window (7 flagged `Inactive`), *other functions* that touch a record but don't do the job (production/municipal staff, coordinators, IQC, oversight), and *noise* (8 IDs with 1–7 records, plus NULL and 0). → Join to the user table for **active status + role**, look for the volume cliff, and **confirm with someone who knows the org before publishing**. Same class as inflating "200 fixes" from 77 log files that were mostly iterations — **both caught by the Skipper, not by me.** Better stat anyway: express as **load per person** (≈9 duplicate entries/person/working day), which doesn't depend on a headcount claim. (2026-07-24)
- ❌ **Verify against the ACTUAL DB key set, not a sampled range.** Verifying Herman's Eastvale address fix, I read the sheet for `site_id` 2043–2360 and reported the street split as "Goodman 255 / Bellgrave 30" — the real tag set runs to 2937, so my window was truncated and the true split (Bellgrave 201 / Goodman 112) looked like a Herman error when it was MINE. → pull the real tag/ID list from the DB first, then join; never eyeball a range. (Verification still worked — it caught my own mistake.) (2026-07-24)
- ⚠️ **`InventoryGPSModel` is a GLOBAL staging table — orphaned rows from ANOTHER job sit there and will contaminate the next import.** Found 197 rows tagged ProjectID 1094998 ("USC Contract", LA coords) unrelated to Goodman/Eastvale — the summary-blast reads staging globally. → before ANY GPS import, `SELECT ProjectID, COUNT(*) FROM InventoryGPSModel GROUP BY ProjectID`; isolate/back-up-and-remove foreign rows first (never blind-delete — shared box, could be someone's active load). (2026-07-24)

## 🗄️ Postgres cast + play-DB account survival (2026-07-04/05)
- ❌ **`coalesce(int_col,'')` throws `invalid input syntax for type integer: ""`** — an empty-string default can't cast to the column's INTEGER type. In `georef_map.detect_trees` this Postgres error was SWALLOWED and surfaced as "detection returns 0 trees" (looked like a vision failure). Cast the column `::text` for the match, and don't let DB exceptions be swallowed into a bogus empty result. (2026-07-05)
- ❌ **Play-only DB *application-account rows* get WIPED by every prod→play restore** (distinct from db-user/grant orphaning) — the `gilligan-bot` login (UserID 376) vanished on restore, so `trimit-fetch.sh` got no session cookie and every fetch bounced to ClientLogin (recurred 6/6 & 7/4). **Fix: self-heal cron** (`ensure-gilligan-bot.sh` every 3h) re-creates it if missing, at a **high/synthetic UserID (90376)** so it never collides with a real employee; the ensure-log doubles as a restore-wipe monitor. ⚠️ also `flow.Users.Password` is **varchar(50)** → a >50-char secret silently NEVER matches. (2026-07-04)

- **[arbor-core/leaflet]** circleMarker/path clicks BUBBLE to the map by default → a pin click also fires `map.on("click")`. If the map click drops/creates something (stamp mode), clicking an existing marker silently creates a duplicate. Fix: `bubblingMouseEvents:false` on the marker (Leaflet 1.0+). (2026-07-05, inventory tree-pin dup bug, commit 6063e40)

- **[arbor-core/leaflet]** A bad/degenerate bounds passed to `fitBounds`/`imageOverlay` corner-jams the whole map (tiles freeze top-left). ALWAYS validate bounds (finite, in lat/lng range, non-degenerate) before use, and call `invalidateSize()` after any async op that may have changed the container. (2026-07-06, extract-areas map-jam, commit)

- **[arbor-core/leaflet]** "Map collapses to top-left corner" after an async action = STALE container size, NOT a view pan. Banners/buttons above the map reflow the layout while Leaflet keeps its old cached size → panes paint at 0,0. Chasing fitBounds/setView = wrong tree. FIX: ResizeObserver on the map container → invalidateSize() (rAF-debounced) on any resize, + invalidateSize on the state toggle that triggers the reflow. (2026-07-06, Extract-Trees corner-jam, Codex root-caused, commit cde0f79)

- **[arbor-core/preact+leaflet]** A conditionally-rendered sibling (`${note && <div>}`) inserted ABOVE a ref-held Leaflet map div makes preact RECREATE the map div (container→0×0, map orphaned, jams to corner). Symptom looks like a framing/invalidateSize bug but is DOM destruction. FIX: render such banners ALWAYS (display:none when empty) so the map div is never reconciled away. Reproduce via headless localStorage session inject + measure .leaflet-container size. (2026-07-06, Extract-Trees corner-jam REAL fix, commit 8994c10)

- **[terminology] TPH = "Throughput per Hour" (dollars of production per crew hour), NOT "Trees Per Hour."** Skipper corrected 2026-07-08. The error was seeded in the shared trimit-knowledge vault (START-HERE, production-tph, WorkOrders) → would have propagated to Hermes and into the Scott demo. Fixed at source. Lesson: verify acronym expansions with the Skipper before baking into knowledge; a wrong term in the vault infects every downstream answer.

- **[infra] Shared fixed remote filenames = a time-bomb.** Herman's DB bridge scp'd every query to one fixed name `herman_q.sql`; a single hung `sqlcmd` holding it open locked out ALL future queries ("scp: dest open Failure"), and `set -e`+`2>/dev/null` masked it as "exit 1, no output." Fix pattern: unique per-invocation temp names + a query timeout so nothing hangs + self-cleanup + don't let a filter-grep's empty exit fail the script. Check sibling scripts for the same fixed-name pattern.

- **[workflow/deep-research]** The `deep-research` workflow RESUME is broken: `Workflow({scriptPath, resumeFromRunId})` does NOT re-pass `args` (the research question) → the resumed run dies instantly with "No research question provided." Also seen: the workflow can stall on the FINAL synthesis agent (e.g. 99 started / 98 returned), leaving a 0-byte result + no completion event. FIX/PATTERN: don't rely on resume — recover findings directly from the run journal (`subagents/workflows/<wf>/journal.jsonl`): grep `"claim":"..."` for the verified claims (they carry evidence + refuted:false) and synthesize by hand. (2026-07-08, IE-expansion Round-2)

- **[rigor/reconciliation]** Stated an assumption as FACT and skipped a source doc we already had. Claimed Brent's City-Budgets numbers came from "QuickBooks" (inferred from report format) and built a whole reconciliation + sign-off on it — when Brent's own SOP (`budget-report/PROCESS.md` + his Jun-16 email) said he pulls from **TRIM IT**. Two failures: (1) labeled an inference as verified; (2) didn't re-read the authoritative SOP BEFORE analyzing. RULE: before reconciling/analyzing against an external party's report, FIND + READ their SOP/source-of-truth doc first (search the project files — we usually already have it), and mark every source claim VERIFIED vs INFERRED. The Skipper caught both. (2026-07-09, RC-03 City Budgets)

- **[infra] gsql.sh has the SAME fixed-remote-temp flaw the herman_q.sql lesson warned about.** `gsql.sh` scp's every query to one fixed name `gsql_tmp.sql` on the play box → **concurrent** gsql calls clobber each other's SQL mid-flight → empty/garbled output (a model misreads as "0 rows" or a silent fail). Hit this hard 2026-07-10 (Steve Fix-2) by firing several gsql calls that the harness auto-backgrounded in parallel. FIX pattern: run gsql **one at a time** (wait for each), OR use a unique-per-call remote temp (`gsql_$$_$RANDOM.sql` + self-delete) — see the `/tmp/gsql1.sh` one-shot. The earlier lesson literally said "check sibling scripts for the same pattern" — gsql.sh was that sibling. Still not permanently fixed in gsql.sh itself.
- **[infra] The gsql.sh fixed-temp time-bomb detonated AGAIN 2026-07-15** — this time the HUNG-process variant, not just concurrency. A leftover process on the play box held `gsql_tmp.sql` open → `scp: dest open "gsql_tmp.sql": Failure` AND `del` failed ("used by another process"; attrib = `A`, not read-only). Because gsql.sh does `scp ... 2>/dev/null`, the write failure is SILENT → every subsequent query ran the STALE leftover file (an old aspen retention-scoreboard query) and returned tier-distribution output. This broke the automated **play-refresh-monitor** (ERROR: read "Changed database context to 'Workbench'" instead of a backup vintage) and silently poisons EVERY gsql.sh caller until the handle is released. Couldn't clear the lock safely (would need to kill the holding process / reboot the play box — didn't do unilaterally). PERMANENT FIX now overdue: (a) unique per-invocation remote temp `gsql_$$_$RANDOM.sql` + self-delete, (b) **fail loud on scp non-zero exit** (drop the `2>/dev/null` swallow), (c) short query timeout so nothing hangs holding the file. Third time this exact pattern has bitten (herman_q.sql → gsql concurrency → this).
- **[rigor/db] Validate a bulk transform's SCOPE with a read-only SELECT BEFORE materializing.** The Fix-2 override map, run as a SELECT first, flagged **116k overrides (would move 35k of an active rep's OWN proposals)** — the "snapshot" source turned out to be project-account-owner, not proposal-seller. Materializing blind would've corrupted the report. RULE: for any override/reattribution/mass-update, first `SELECT COUNT + a by-entity breakdown` and sanity-check the magnitude against expectation ("minimal-touch" should be hundreds, not 100k); only `CREATE TABLE ... INTO` / UPDATE after the numbers make sense. (2026-07-10)
- **[verify] Render-verify EVERY served table, not just the primary query.** Fix 1 fixed the Win/Loss *summary* but a *second* per-proposal detail table on the same page had its own copy of the roster gate → the served page still showed the wrong reps (98 detail rows). The `curl` render-check caught it (summary right, detail wrong). RULE: a page can have N queries with the same bug; grep the SERVED HTML for the specific symptom across the whole page, and enumerate `<cfquery>` blocks before declaring a query-level fix complete. (2026-07-10)

- **[paths/startup]** Project work lives at `/home/wade3337/arbor-stack/` — memory/RC pointers like `arbor-stack/...` are relative to **`/home/wade3337/`**, NOT the OpenClaw workspace (`~/.openclaw/workspace/`, which holds only wiki/memory/reference/contracts/skills). At session start, `cd /home/wade3337` for project files. (Cost ~5 tool calls 2026-07-10.)

- **[infra/CRITICAL] Dual-webroot shadow: CF serves `C:\ColdFusion2023\cfusion\wwwroot\GSTS\` OVER the IIS path `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\`.** If a file exists in the C:\ shadow, CF serves THAT (often stale) and silently ignores your D:\ edit; if it does NOT exist in C:\, CF falls back to D:\ (so NEW files work from D:\, which misleads you). Burned ~40 min 2026-07-11 chasing "trusted cache / IIS output cache / CF restart" — none of it; the served file was a 6/27 C:\ shadow. **RULE: when a `.cfm` EDIT doesn't show in served output but a NEW file would, FIRST `Get-ChildItem C:\ -Recurse -Filter <name> -EA SilentlyContinue` (plain `where /r C:\` aborts on a permission error and lies "not found"). If shadowed, deploy to BOTH webroots (back up the C:\ copy first).** 8 GSTS .cfm are shadowed (RevenuePerformance, CustomerLeads, SalesPipeline, 2 Exec, Export-CustomerList, 2 Diag) — any D:\-only edit to those never went live. Audit pending. (2026-07-11)
- **[llm/small-model] Do NOT hand a 3B model raw JSON or let it compare/compute.** llama3.2:3b fed the dashboard's JSON blob garbled numbers ($655K→"$1,655,561"), fixated on one field for every question, and flipped comparisons ("$179 is BELOW $130"). FIX: in ColdFusion pre-select the intent-relevant fields, pre-format money (`"$"&numberFormat(round(n),",")`), and **pre-compute every verdict** (on-pace yes/no+gap, TPH above/below) — hand the model a tiny labelled fact list and tell it to only quote, never calculate. Numbers went exact. (2026-07-11)
- **[llm/small-model] Ollama's default temperature is 0.8 → set `options.temperature=0` for any factual/grounded task**, or the model paraphrases numbers into fiction. AND: a 3B model will **override injected facts with its own (wrong) world-reasoning** — ask "TPH last month", give it facts labelled "June 2026", and it still writes "May 2026: $124.21" because it "reasons" what last month is. Even temp 0. FIX: never let the model reason about dates/periods OR emit the number — **compute the period + compose the exact answer sentence in CF**, let the model only REWORD, and **accept the reword only if it preserves the primary number AND names no month other than the period's own; else serve the CF sentence.** (2026-07-11)
- **[coldfusion/CRITICAL] In a CFML string, `##` is an ESCAPED literal `#`.** So `"## "` at runtime is `"# "` (2 chars, one hash), and `left(ln,3) EQ "## "` NEVER matches a real markdown `## ` heading. Cost ~30 min on the Arbor Helper KB parser (bodies came back empty). **To match a two-hash `## `, write `"#### "` in the source** (four hashes = two literal hashes). A crew review caught it independently. Same class as the earlier BOM/emoji issue. (2026-07-11)
- **[coldfusion] Two more CF gotchas hit the same night:** (1) `var` is ILLEGAL outside a `function{}` — in a top-level `<cfscript>` template body, declare plain (no `var`), only use `var` inside UDFs. (2) `.cfm` files with emoji/non-ASCII render as mojibake unless you add `<cfprocessingdirective pageencoding="utf-8">` as the FIRST line (or a UTF-8 BOM). (3) `find(chr(10) & "## ", raw)` was unreliable for splitting — a line-loop (`for ln in listToArray(raw,chr(10),true)`) is robust. (2026-07-11)

- **[llm/router] A word that means two things will mis-route — test BOTH senses after touching an ambiguous trigger.** Arbor Helper: "value" = company *values* (ideals) AND dollar *value* (money). Broadening the company-identity trigger to catch "what does the company value" risked stealing "how much value is in open bids". FIX: add the ideological sense narrowly (`\bvalues\b` plural + "company value"/"we value"), NOT bare singular `value`; then verify the money side still routes to figures ("open bids" → $40.9M ✓) as well as the fixed miss. RULE: whenever a new keyword overlaps an existing domain term, add a regression for each meaning before declaring done. (2026-07-11)
- **[regex] `\bword\b` does NOT match the plural `words` — a word boundary breaks on the trailing s.** `\bjoke\b` silently failed on "how many tree jokes" (fell to out-of-scope). Use `\bjokes?\b` (or `\bword(s)?\b`) whenever the plural is a natural phrasing. Worth sweeping other `\bword\b` triggers for the same gap. (2026-07-11)
- **[llm/router] Another verb-vs-name collision (like "value"): "chase" = follow-up verb AND rep Chase Schroeder (SalesRepID 1135).** "which accounts should i chase" routes to sales-rep (rep-name match runs before cockpit). By design correct-ish (rep Chase is real). Did NOT add bare "chase" to follow-up detection (would break "how much did Chase sell"). Follow-up uses unambiguous triggers (follow up / need follow-up / need attention / to chase). Same rule as [[value collision]]: test both meanings before adding a keyword. (2026-07-12)
- **[security/coldfusion] A `?param` JSON/grounding branch inherits NO auth — gate it EXPLICITLY.** The `?aisummary=1` branches added to dashboards to feed Arbor Helper emitted financial totals + the raw JSON to ANY caller, even a garbage/forged `ZUserID` cookie (a real `flow.Users` row was never required) = broken access control. Crew caught it during the deploy-package review, not in normal use. FIX: a shared `dashboard-auth-gate.cfm` `<cfinclude>`d at the TOP (before ANY output) of every financial/JSON-emitting surface (20 files) that resolves the cookie to a real authorized `flow.Users` row and **403s HTML *and* JSON** otherwise. RULE: any new `?param` data/JSON branch = add the gate in the SAME commit; also `</`-escape any `SerializeJSON`-into-`<script>` (Cockpit XSS). (2026-07-12)
- **[coldfusion] Two exec-gate traps (2026-07-11):** (1) CFML **`gt`/`gte`/`lt`/`is` are reserved operators, not legal variable names** — a var named `gt` (holding a title) errored; renamed `gtitle`. Same class as the T-SQL `Current` reserved-word trap. (2) **Gate on the login ALLOWLIST, NOT `flow.Users.Title`** — jwade's title is a stale "Fleet Manager" (never updated to COO), so a title-based exec gate locks the Skipper out; login validation doesn't read Title/SecurityLevel anyway. Use an explicit authorized login/UserID set, fail-closed to leadership. (2026-07-11)
- **[security/coldfusion] Gating "the dashboards" ≠ gating the DRILL CHILDREN — enumerate every reachable page, not just the tabs.** The Jul-11 crew audit gated 20 leak surfaces but MISSED `Executive$Sales$Detail.cfm` + `Executive$ClosePercentage$Detail.cfm` — the per-record detail pages the market/rep tabs drill INTO. Both returned 200 (full per-invoice/proposal financials) to a garbage `ZUserID` cookie, and `Sales$Detail` wasn't even in the repo (live on prod, untracked). Found only when a new feature promoted the drill to a first-class path. RULE: when gating a dashboard, walk the ENTIRE click-path (tab → bucket → segment → detail → export) and gate each leaf; a page not in your repo/`deploy-manifest` is a page you didn't audit. `curl --cookie ZUserID=99999999 <page>` every drill target, expect 403. (2026-07-12)
- **[coldfusion/mobile] Two protip systems in TRIM IT — check WHICH one a page uses before assuming a mobile fix applies everywhere.** The 5 newer dashboards use the shared `protips-include.cfm` (`.gsts-protip-badge`, shows on `(pointer:coarse),(max-width:640px)`). **Revenue Performance uses a SEPARATE legacy `data-tip`/`.tip-badge` system** (inline in the page, `css/gsts-protips.css`) whose badge showed only under `@media (hover:none)` — so on a laptop in device-mode (reports `hover:hover`) the "?" badges vanished at phone width. Fix = align the media query to `(hover:none),(pointer:coarse),(max-width:640px)`. RULE: a mobile/protip fix on the shared CSS does NOT touch Revenue; grep the page for `assets/protips/protips` vs `data-tip` to know which system you're in. `@media (hover:none)` alone is a mobile-visibility trap — always OR it with `(max-width:…)` so device-mode/narrow-desktop works too. (2026-07-12)
- **[coldfusion/dual-webroot] A C:\-shadowed page's `<cfinclude>` targets must ALSO live in C:\ — or the page 500s.** Adding `<cfinclude template="protips-include.cfm">` to `Dashboard-RevenuePerformance.cfm` (which is served from the C:\ shadow) 500'd with "Could not find the included template" because protips-include.cfm was only in D:\. Static assets it pulls are URL-fetched (resolve fine from the IIS root), but a SERVER-SIDE cfinclude needs the file physically in the same webroot CF serves the page from. RULE: when you add a `<cfinclude>`/`<cfmodule>` to a shadowed page, deploy the included file to BOTH webroots too, and grep the CF `application.log` (`Could not find the included template`) the moment a page returns a tiny error body. Confirmed on play: Revenue is C:\-served. (2026-07-12)
- **[js/mobile] The shared protip badge's JS injector and its CSS display rule must agree — they didn't.** `protips.js` injected `.gsts-protip-badge` only on true touch (`(hover:none) and (pointer:coarse)`), but `protips.css` displays it on `(pointer:coarse), (max-width:640px)`. Result: a laptop in device-mode (narrow width, hovering pointer) got NO badge (JS never made it) AND no hover-reveal (device-mode suppresses hover) — the Skipper saw nothing on his "computer mobile view" while a real phone worked. Fix = gate the JS injection on the SAME condition as the CSS: `(pointer:coarse) OR (max-width:640px)`. RULE: whenever a JS "should I create this element" check and a CSS "should I show it" media query both exist, they must use identical conditions, or you get an invisible-but-present (or absent-but-styled) element. (2026-07-13)
- **[coldfusion] Inside `<cfoutput>`, ANY bare `#` is a variable delimiter — hex colors AND numeric HTML entities break the compile.** Adding `style="border-bottom:1px dotted #94a3b8"` and `&#9432;` (ⓘ) inside SPM note blocks (which sit in cfoutput) both errored ("`94a3b8` is not a valid identifier" / "invalid construct near `;`"). Fixes: use a color NAME or `rgb()` (no `#`), and paste the LITERAL unicode char (ⓘ, →, ÷) instead of `&#NNNN;` entities — or double them (`##94a3b8`, `&##9432;`). Safest for injected HTML: literal chars + no-`#` colors, so it works whether or not the surrounding block is a cfoutput. (2026-07-13)
- **[js] Adding a stage/enum value = update EVERY hardcoded list of it, and "0 CF errors" does NOT prove the browser renders.** Splitting the Cockpit's "Working" into Working+Scheduled, I updated `STAGES`, `stageOf()`, `stageColor()`, the column HTML, welcome copy — but MISSED the client-side `buckets={followup:[],bidding:[],won:[],working:[],done:[]}` initializer. `stageOf` returned `'scheduled'` → `buckets['scheduled'].push()` threw a `TypeError` → the throw aborted the entire `render()` loop → EVERY column drew empty. Server rendered fine (0 CF errors, aisummary correct, 740 cards in the inline `CARDS` array) — it was a pure browser-JS throw, invisible to server-side checks. The Skipper caught the blank board. RULES: (1) when adding an enum member, grep for every literal that enumerates the old set (arrays, object-key initializers, switch/ternary chains) — not just the obvious `STAGES` list. (2) A dashboard is NOT verified by "0 CF errors" — the cards render CLIENT-side; extract the rendered inline JS and either `node`-execute the render path over the real data or screenshot the populated board. My earlier "verified" screenshot showed 0-count columns and I wrongly blamed a missing cookie — the empty board WAS the bug. (2026-07-13)
- **[security/coldfusion] A gate `<cfinclude>` inside a `<cfcase>` only guards THAT branch — put it before the `<cfswitch>`.** `SalesProductionMeeting$Drill.cfm` (a drill page reachable from every SPM tile) had `<cfinclude template="dashboard-auth-gate.cfm">` stranded inside the `soldApproved` case, so `metric=soldApproved` returned 403 to a garbage cookie but `metric=pipeWon/prodCompleted/soldBacklog/resSold` returned 200 + real customer/$ data. A spot-check that only tested the page with no `metric` (or the one gated metric) SEES 403 and declares it safe — the leak hides in the other branches. RULES: (1) the auth gate must run BEFORE any branching/query/output — top of file, before `<cfswitch>`/`<cfif>` — never inside a case. (2) When a page's behavior forks on a URL param (`metric`, `view`, `tab`, `mode`), security-test EVERY value of that param with a garbage cookie, not just the default. (3) grep new/edited gated pages for the include line's column position — a gate line with leading whitespace is a smell it's nested inside a block. (2026-07-13, caught by the pre-ship crew audit)

- [trimit/rgc/sql] A "pipeline outside coverage" bucket must exclude BOTH future-scheduled AND already-started/produced work — not just future. RGC's approved-unscheduled filter only excluded future ACTIVE(46) WOs, so in-progress/completed jobs (and jobs whose WOs left status 46) leaked in ($1.09M). In TRIM IT a WO is created at approval with NULL StartDate; "unscheduled" = no dated WO + no crew sheet, NOT "no WO". Test a bucket by joining every item back to WorkOrders/CrewSheets and counting how many have started work.

- [crew/judge/db] glm-judge's DB tool loop runs queries via gsql.sh which defaults to `-d GSTS`. Objects in the Workbench side-DB (rgc.*) were invisible → the judge wandered the wrong DB and never reached a verdict. FIX: `JUDGE_DB` env prepends `USE [db];` to each query (+ tells GLM which DB it's in). Run RGC/Workbench reviews with `JUDGE_DB=Workbench python3 glm-judge.py`. Always tell a DB-tool judge which database its objects live in.

- [agents/vault-guard] A BLACK-leak guard that greps the bare word `\bBLACK\b` false-positives on legit prose ("black-box match") AND on a vault's OWN guardrail text ("no Track-2/BLACK") — it quarantined Aspen's start-here README. FIX: match PRECISE Track-2 markers only — `arbor-core|arbor-knowledge|arbor-strategy|BLACK vault` + real secret regexes (`ghp_[A-Za-z0-9]{20}`, `ssh-ed25519 AAAA`, `-----BEGIN … PRIVATE KEY`). Applied to refresh-aspen-vault.sh + refresh-munibot-vaults.sh.

- [trimit/retention/sql] Never build a churn/service-interval metric on raw crew work-days — a single job spans several consecutive days, so the gaps between distinct WorkDates are ~1 day and the customer's MEDIAN interval collapses to 1 day → 92% of customers falsely flag "at-risk." FIX = **sessionize** work-days into VISITS first (a new visit starts when the gap from the prior day > ~14 days), then measure the interval between visit-starts (came out to a believable ~301-day typical cadence). Caught by spiking the query on play before productionizing — always sanity-check the at-risk RATE (76–92% is a tell something's wrong) and the median interval before shipping. Also: the play DB is a partly-stale snapshot (only 150/381 "Other" custs active in 2023+), so anchor the clock with an @AsOf param and verify on the recently-active cohort, else staleness inflates at-risk. (2026-07-15, Aspen retention scoreboard)

## [trimit/db] "Open/incomplete" state can't be read from DateCompleted/DateApproved NULLs (2026-07-16)
Building the running-dry wire, I used `WorkOrders.DateCompleted IS NULL` = "work still open" and `Proposals.DateApproved IS NULL` = "proposal still open" → both flagged ~everyone (counts like 107, 830 per account). Cause: these completion/approval date columns are **sparsely populated** — lots of long-done work/dead proposals never got the date stamped. Same trap as forward-scheduling (only 100/3007 projects have a future-dated WO). **Rule:** for TRIM IT account-state, don't trust "NULL date = still open"; use **activity RECENCY** (last WorkOrder date via `MAX(COALESCE(DateCompleted,StartDate))`, last ProposalDate/LastGoAheadDate) instead. Cost me ~3 iterations (all-zeros results).

## [confidentiality/agents] Track-1 agent vaults auto-quarantine "arbor-core" mentions (2026-07-16)
Authoring a plan note for Aspen/MuniBot (both Track-1, sync to GitHub+Obsidian), I referenced "arbor-core RFP intake agent" → the refresh script's **BLACK-leak guard quarantined the file** (didn't sync it). Correct behavior — arbor-core is Track-2 BLACK, must never surface in team vaults. **Rule:** when writing plan/roadmap notes for team agents, SCRUB all arbor-core/Track-2 references — describe capabilities generically ("an RFP-intake tool to build"), never name the BLACK project or reveal a BLACK prototype exists. Check `_quarantine/` + refresh.log after syncing.

## [security/ssh] rsync key rejected → SSH password fallback BYPASSES the forced-command restriction (2026-07-16)
MuniBot 200GB drop: cwRsync's ssh rejected the drop key (Windows ACLs too open) → **fell back to password auth**, which SUCCEEDED for wade3337 → the per-key `command="rrsync -wo <warehouse>"` restriction **did NOT apply** (forced commands are key-scoped) → rsync ran unconfined against `~/` (dumped empty cygwin stub dirs; caught before real data). **Fixes:** (client) `icacls <key> /inheritance:r /grant:r "%USERNAME%:R"` so ssh accepts the key, AND force `-o PreferredAuthentications=publickey -o BatchMode=yes -o NumberOfPasswordPrompts=0` so it FAILS SAFE instead of using a password. (server) `PasswordAuthentication no` — a key-only box should never allow password login to bypass a write-only drop. **Rule:** any forced-command/restricted-key drop is only as safe as `PasswordAuthentication no`; verify it. Detection: `journalctl -u ssh | grep "Accepted password"`.

- [trimit/dashboards] **Changing a SHARED data structure broke a sibling page.** Reworked `woByCo` in `CityBudgets.data.cfm` (struct-of-labels → array-of-rows) for the scope-guard fix; `CityBudgetsRenewals.data.cfm` *reuses* that same `woByCo` (per its header comment) and still did `StructKeyExists(woByCo[cid], label)` → runtime error at line 49, blanking the whole Renewals tab. **Lesson:** before changing any structure built in one page and consumed by others, `grep` every consumer FIRST and update them in the same change-set + render-verify EACH sibling. The repair contract's "propagate to sibling pages" applies to shared *data shapes*, not just visual layout.

- [security/ssh] **cwRsync bundles can be STRIPPED — don't depend on `cygpath`/`chmod` in the launcher.** MuniBot local-source drop (v5): the Desktop cwRsync bundle had `rsync.exe`+`ssh.exe`+`cygwin1.dll` but NOT `cygpath.exe`/`chmod.exe`. The `.cmd` used `cygpath -u` to translate the key's Windows path → cygwin form; with cygpath missing that step silently produced an EMPTY string → `ssh -i ''` → "Identity file not accessible" → `Permission denied (publickey)`, transfer never authenticated. **Fix:** translate paths in pure batch (`set DL=%P:~0,1%` + `set REST=%P:~2:\=/%` → `/cygdrive/%DL%%REST%`), lowercase the drive letter, drop the `chmod 600` (rely on `icacls /inheritance:r /grant:r user:R` for cygwin-ssh key perms), and BUNDLE `munibot_drop_key` in the zip so it's guaranteed beside the launcher. **Rule:** a Windows rsync launcher should assume only rsync.exe+ssh.exe+cygwin1.dll exist; never call cygpath/chmod/other cygwin coreutils that may be absent.

- [openclaw/config] **`channels.discord.streaming` must be an OBJECT `{mode:"progress"}`, NOT a bare string.** The docs describe it as "takes `off|partial|block|progress`" which reads like a string value — but the schema requires `streaming:{mode:"progress", progress:{toolProgress:true}}`. Setting `streaming:"progress"` passed `json.load` but the gateway ABORTED on restart (`invalid config: must be object`) and did NOT start — left the gateway DOWN until fixed (rsync/other SSH work was unaffected, separate procs). **Rule:** after ANY openclaw.json edit, run `openclaw config validate` BEFORE `openclaw gateway restart` — validate catches schema errors while the old gateway is still up; restarting blind can strand you with a downed gateway. Green-dot presence = `channels.discord.autoPresence:{enabled:true}` (+ optional `activity`/`activityType:4`); working-animation = `streaming.mode:"progress"`.

- [agents/data-plumbing] **"Reachable" ≠ "knows to look there" — verify the agent's PROMPT points at the data path, not just that the mount exists.** MuniBot 42GB ingest: the warehouse was correctly bind-mounted (`~/.munibot → /opt/data`) so data at `/opt/data/municipal-archive/` was physically visible to the container — but MuniBot's SOUL still named `~/home/municipal-history/` (an empty, git-tracked vault dir) as "my raw source material." As instructed it would have looked in the empty folder and found nothing. **Fix:** point the agent's SOUL + MEMORY at the ACTUAL landing path as an ABSOLUTE path (`/opt/data/municipal-archive/`, not `~/...` — `$HOME` shifts inside containers and `~` can double-path), and note the big on-disk warehouse is deliberately NOT in git/the vault (bulk raw data never rides the sync). **Rule:** after wiring a data mount for an agent, grep its SOUL/memory for the path it's actually told to read, reconcile any mismatch, then verify from INSIDE the container (`docker exec`). Agent picks up SOUL/memory changes on its NEXT session, not the running one.

- [data-transfer/rsync] **rsync copies Windows `.lnk` shortcut STUBS, not the folders they point to.** MuniBot warehouse ingest: 7 cities (Long Beach, Anaheim, Aliso Viejo, Cerritos, Lake Forest, Stanton) existed on the source share as `.lnk` shortcuts to folders elsewhere. rsync faithfully copied the ~2KB shortcut files and moved on — those cities landed EMPTY, easy to mistake for "no data exists." **Rule:** before/after any Windows-source sync, scan the source for `*.lnk` (`find … -iname '*.lnk'`); resolve each shortcut's real target and copy the actual folder (or add its resolved path to the sync). And when a dataset says "no history for X," check X isn't a shortcut that never transferred before reporting real absence. Verify contents from inside the destination, don't assume the copy was complete.

- [docker/compose] **Recreating a compose service without its original env vars silently reverts them to defaults — and can break the container.** Adding a mount to the `hermes` service, I ran `docker compose up -d gateway` without `HERMES_UID/HERMES_GID` set; the compose default `${HERMES_UID:-10000}` kicked in (original was 1000, captured only in my pre-change `docker inspect` snapshot, NOT in any `.env`). The boot wrapper chowned `/opt/data` to 10000 and the gateway hit `PermissionError` everywhere → Boss Herman down. **Fix:** recreate with the original values (`HERMES_UID=1000 HERMES_GID=1000 docker compose up -d gateway`); the wrapper chowns ownership back. **Rule:** before recreating a compose service, capture its live env (`docker inspect --format '{{range .Config.Env}}...'`) and pass any non-default values explicitly (or persist them in a project `.env`) — a compose file with `${VAR:-default}` is a landmine if VAR isn't in the environment at up-time.
- [docker/mounts] **Don't nest a read-only bind mount inside a directory the entrypoint `chown -R`s.** I mounted the warehouse at `/opt/data/municipal-archive:ro`, but the hermes boot wrapper runs `chown -R /opt/data` — which collides with the read-only submount and compounded the startup breakage. **Fix:** mount such read-only data OUTSIDE the chowned tree (used `/warehouse:ro`). **Rule:** check the image's entrypoint for `chown -R <datadir>` before mounting anything (esp. `:ro`) inside that datadir; give read-only reference data its own top-level mountpoint.

- **[trimit/cfm-ui] CSV/export button as `type=submit` inside a filter form is hostage to EVERY field's HTML5 validation** (step/min/max/pattern). A money `<input type=number step="1000">` with a cents value silently blocked the whole Revenue Performance form (export AND Apply). Fix pattern: never step-constrain a money field (use `step="any"`), and put `formnovalidate` on any export submit button — or export via a plain `<a href>` link (immune). (ship #182)

- **[trimit/sales-signals] "Running dry" (forward-book<3mo) false-flags recurring maintenance accounts** — they book ~1mo ahead by nature, so a naive forward-book horizon flags healthy recurring customers (Irvine Co Retail's ~40 centers). Discriminator = active recurring CADENCE: recent completed work + a booked next WO = being serviced, NOT dry. Contract lookup did NOT help (recurring contracts aren't recorded in dbo.Contracts — only expired-2021 rows; see wiki/facts/trimit-recurring-contract-lookahead-gap.md).
- **[trimit/debug] To inspect live TRIM IT data, deploy a throwaway auth-gated diagnostic CFM to play, curl it from the play server (bot cookie 376), then DELETE it.** Gotcha: `##qVar#` in cfoutput is a PARSE error (## = escaped literal #, then unbalanced #) → uncatchable by cftry → generic CF error page. Use `#qVar#` for a value; `###qVar#` only if you truly need a literal # prefix.

- **[munibot] Agent workflow ≠ its scripts — verify the AGENT, not just the script (2026-07-18).** Herman's v5 `bid_output.py` fail-closes (exit 2, REVIEW REQUIRED) on Long Beach PCA/QAC when TRIM IT has no verified rate. When MuniBot ran the SAME bid through its `municipal-bid-pricing` SKILL (as the agent, `hermes -z --skills … --yolo`), it did NOT fail-close — it priced PCA $173.76 / QAC $128.63 and only marked "HOLD". A control living in a script isn't a control the agent honors unless it's in SKILL.md. Test-as-prod = drive the actual agent, then diff its output vs the script's. Also agent inventory 87,481 vs script 87,229.
- **[munibot] Driving the hermes agent one-shot: `hermes -z` quoting + perms (2026-07-18).** Nested double-quotes inside the `-z` prompt (a path with spaces) break shell parsing → the next word gets read as a subcommand ("invalid choice: 'Angeles'"). Fix: write the prompt to a file, run `-z "$(cat prompt.txt)"`. And the output/log dir must be `chown hermes:hermes` (a dir made by a default-user `docker exec` is root-owned → `hermes` hits Permission denied). Run the agent as `docker exec -u hermes -e HOME=/opt/data`.

- **[munibot] The warehouse leaks the answer key — our "blind" bid test isn't blind (Skipper, 2026-07-18).** MuniBot prices by pulling each city's history from `/opt/data/municipal-archive/` — which contains BOTH GSTS's own submitted bids AND the award results (WCA winning prices) for the very cities we tried to test him on. So feeding him a city we've already bid (Pomona, Bell Gardens) doesn't test his pricing ability; he finds our prior submission + the winner's numbers and undercuts them. His "sharp" comps ($83 WCA street, $139 our grid, $904K vs $1.16M) were warehouse recall, not pricing. A REAL blind test must withhold the answer: either (a) test a city we've NEVER bid (no GSTS submission in the warehouse), or (b) temporarily quarantine that city's own submitted-bid + results files before the run, or (c) hold out a fresh RFP whose award isn't in the warehouse yet. Warehouse-as-comp-source is correct BY DESIGN for real bidding — it just invalidates the compare-to-answer-key test.

- **[trimit/cfm-ui] Excel `#####` on a CSV date column = Excel auto-date + narrow default width, NOT bad data (ship #183, 2026-07-17).** RevPerf CSV cells were clean uniform ISO (verified against the served CSV), but Excel auto-converts ISO→date + ~8ch default column width → single-digit-day dates fit, double-digit (≥ 7/10) overflow to `#####` — flips exactly after Jul 9. To force full display, emit the date as an Excel text-formula `="mm/dd/yyyy"` **UNQUOTED** (a quoted `"=..."` breaks the formula-eval trick; bypass your normal `csvField` quoting for these cells). Note: text sorts by string not chrono — use ISO `="yyyy-mm-dd"` if chronological sort matters.
- **[coldfusion/debug] The generic CF error page does NOT contain the string "cfml error" — a grep-for-errors that keys on "cfml"/"error" gives FALSE NEGATIVES.** A page can be 500-ing hard while your render-check reports "0 CF errors." Detect it by grepping the served HTML for `<title>Error</title>` (the CF error template's title) instead. (2026-07-14, SPM Results)
- **[coldfusion/db] Two CF-DSN SQL gotchas (2026-07-14):** (1) the CF datasource **blocks multi-statement `IF … INSERT`** batches — rewrite an idempotent insert as a single `INSERT … SELECT … WHERE NOT EXISTS (…)`. (2) A ColdFusion **struct name can collide with a `<cfquery name=>`** (scope is case-insensitive) → silent overwrite/odd behavior; give the struct and the query distinct names.
- **[agents/telegram] NEVER run a manual `getUpdates` against a live Hermes/OpenClaw bot — it steals the poll (409 Conflict) and can knock the bot's own long-poll offline.** A 409 when you poke it actually means the bot IS connected and polling — stop poking it. To inspect what a live bot is receiving, read the gateway journal (`journalctl --user -u …` / the gateway log), don't call the Telegram API directly. (recurred 2026-07-15 ×2, 07-16)
- **[harness/process] A complex multi-step bash script (`set -e` + loops + chained ops) can trip a malfunctioning permission-approval layer (surfaced as a `ZodError`) and fail to run — while the SAME work as discrete simple commands goes through fine.** When an approval prompt misbehaves on a big script, break it into individual simple commands rather than fighting the wrapper. (2026-07-17, warehouse cleanup)
- **[trimit/deploy] The deploy manifest only walks the named ENTRY pages — it MISSES (a) shared libs referenced only by non-entry pages and (b) un-gated deep drill children / bundled legacy add-ons.** 2026-07-20 pre-deploy crew caught `vendor/chart.js@4.4.1` (needed by all 5 Executive chart pages → charts silently blank on prod) AND 3 un-gated financial pages already bundled for prod. Before every ship, run a garbage-cookie (`ZUserID=99999999`) sweep on EVERY .cfm in the bundle + a full dependency grep, not just the entry pages.
- **[comms/email] Gmail refuses `.js` attachments EVEN inside a .zip (it scans archives) → a deploy zip containing .js silently bounces/strips.** Don't email code bundles; stage on the server + email a pointer link instead. (2026-07-20)
- **[trimit/kanban-db] `Workbench.dbo.WorkKanban.CardID` is an IDENTITY column** — inserting a card with an explicit CardID fails (Msg 544 "Cannot insert explicit value for identity column"). Omit CardID on INSERT and let SQL assign it; use `SCOPE_IDENTITY()` to read the new id. (2026-07-20)

- infra/agents — **Codex ChatGPT-oauth can't be cloned into a 2nd container.** Single-use refresh tokens: two copies refreshing independently → the loser gets "refresh token already used" and dies. Symptom: 401 token_expired in a container even though the host codex works. Fix: for containerized agent crews, use **API-key models (Kimi/Gemini/GLM)** — static keys, no race — not codex oauth. (Boss Herman "works fine off the same account" only because it uses API-key crew + has no codex binary.) The codex binary is a static musl build → copies into any glibc container fine; it's the *auth*, not the binary, that doesn't travel. (2026-07-20, wiring MuniBot's bid-check crew)

- trimit/access — **I have WRITE access to PLAY — don't reflexively say "read-only."** My SSH key `~/.ssh/gstsdb_ed25519` logs into the play server (Administrator@100.86.97.46) as **Administrator** → write files to the webroot `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\...` via `scp`, and run **write** SQL on GSTS via `~/arbor-stack/production-dashboard/gsql.sh` (SQLCMD `-E` integrated auth = full perms). The read-only things are *separate*: HermanRO/GSTSREADONLY query logins + the `gilligan-bot` view-only web login. I once told the Skipper I couldn't install a profile pic "because play is read-only" — wrong, cost a round-trip. Follow the repair contract (backup-first + verify) but know the write path is mine. (2026-07-21)

- mna/fort-point — **Route deal questions to the right advisor — the banker ≠ the phantom.** Cam Bryan (Augusta, sell-side banker) owns DEAL ECONOMICS (valuation, gross→net proceeds, fees, structure, earnout measurement, escrow, timeline). He knows the Skipper's phantom exists but did NOT write it and can't interpret it — phantom base/"taxes"/409A go to Gary Vyneman (attorney) + the LTIP plan doc; the adjusted-EBITDA add-back bridge + AGP definition go to Steve (CFO). I misfiled "what's the phantom's deal-price base" onto Cam and the Skipper corrected me. Map: [[fort-point-advisors-and-open-questions]]. (2026-07-21)

- coldfusion/perf — **A slow ColdFusion grounded-answer is usually ONE tall-pole query — time each layer before optimizing.** Arbor Helper's count-once coverage answer took **83–90s**; every layer was ~3s except the accrual UDF `dbo.GetPeriodAccrual` (called twice = ~96s). Fix wasn't SQL tuning — it was **application-scope caching** (accrual 4h, figures 30min) + a `*/15` warm cron (`landing-assistant/warm-coverage.sh`) so warm calls are 0.23–0.33s and no human eats the cold hit. Same cache pattern as the existing weather cache. Also: a 55s curl timeout hid the real result as an empty/JSON-parse error — always check `-w '%{time_total}s %{http_code}'` and raise the timeout before assuming the endpoint is broken. (2026-07-22)

- coldfusion/security — **The CF "GSTS" datasource on play connects as `sa`** (found in `C:\ColdFusion2023\cfusion\lib\neo-datasource.xml`, url `...databaseName=GSTS`, `username=sa`). So ANY query the assistant/pages run has sysadmin rights — cross-db `Workbench.dbo.*` reads/writes "just work" with no grants. Upside: no permission plumbing. Non-negotiable downside: a chat box → `sa` means **every write handler MUST use parameterized `queryExecute` (cfsqltype params) — NEVER concatenate user/chat text into SQL**, and scope writes to the caller's cookie ZUserID (not anything from the message). Built Arbor Helper's to-do/note write-actions (B1) this way. (2026-07-22)

- coldfusion — **Adobe CF `serializeJSON` uppercases struct keys added via DOT notation** (`out.todoId=x` → JSON `"TODOID"`), so a JS client reading `j.todoId` gets `undefined`. Keys created in a `{"foo"=..}` literal (quoted) serialize lowercase and work. FIX: add later keys with quoted bracket notation — `out["todoId"]=x` — to preserve camelCase. Bit the Arbor Helper live-todo wire (action came through, todoId/todoBody were null) until switched to brackets. (2026-07-22)

- trimit/data — **"Work/go-aheads to schedule" = WorkOrders where `StatusDefs.Desc1='InProcess'`** (TRIM IT's own "To Be Scheduled" page `Sched.WorkOrders.ToBeScheduled.Content.cfm`; also `CompanyID<>297309`). WO lifecycle: **InProcess (to schedule) → Active (scheduled/in-progress) → Complete**. Live: Active 645/$7.8M, **InProcess 33/$332K** — the real "to schedule" queue is TINY because approved work is scheduled almost immediately. Two WRONG turns I made first: (a) the SPM "pipeline" ($23.5M/2,963 = all open go-aheads 12mo, un-deduped) and (b) `GoAheads.StatusDefID=44` + `OriginalStartDate IS NULL` → $6.53M (that field is sparse, NOT a schedule signal; Skipper: "most of this is already scheduled/consumed"). LESSON: **scheduling state lives on the WorkOrder status, NOT on GoAheads** — the GoAhead lifecycle (Pending49→Active44→Complete45) is the SALES pipeline. When a metric has a native TRIM IT page, read ITS query first (search webroot `*Schedul*`/`*GoAhead*`) instead of inventing one. Nate's Scheduling-Meeting PDFs (`arbor-stack/sales-review/incoming/`) are the human-facing version. (2026-07-22)

- sql/ole — **Reading an HTTP response body via SQL Server OLE automation returns NULL unless the OUTPUT variable is FIXED-size.** `sp_OACreate 'MSXML2.ServerXMLHttp'` → `sp_OAGetProperty ...,'responseText',@out OUT` yields NULL when `@out` is `varchar(max)`/`nvarchar(max)` — a MAX type as an OLE OUT param throws ODSOLE **`Error in srv_paramset`** (only visible via `sp_OAGetErrorInfo`). Use **`varchar(8000)`**. Also `open()`'s async arg must be integer **`0`** (string `'false'` reads as truthy = async → body not ready → NULL). Cost ~8 diagnostic rounds on the Locations geocoder before spotting it. (2026-07-22)

- trimit/geocode — **A TRIM IT location stuck at 0/0 (map pin on "null island") is the DB geocoder, not the page.** The `LocationsPostUpdateAddress` trigger calls proc `dbo.Locations_LatLong_upr`, which geocoded via the **retired keyless Google endpoint** `http://maps.google.com/maps/api/geocode/xml?sensor=false` (Google killed keyless geocoding) → silent fail → 0/0, on PROD too. The page's client-side `google.maps.Geocoder` (`codeNewAddress()`) is a red herring (Maps JS not even loaded there). Geocoding is centralized — address edit / new-site insert / RFP generation / nightly batch all funnel through that one proc — so fix the proc, not any page. (2026-07-22)

- infra/cron/refresh-timing — **The nightly play `GSTS DB RESTORE` runs 3 AM *Pacific* ≈ 10–11 AM *UTC*, NOT 3 AM UTC.** The openclaw box is on UTC, so any cron that must run *after* the restore (re-apply profile pic, HermanRO regrant, any post-restore reseed) has to target ~10–13 UTC — or, better, just run **all-day `*/30 * * * *`** if it's idempotent. Bit us twice: the reapply-profilepic cron was scoped `*/30 3-6` (UTC) = *before* the restore, so the wipe was never re-applied and the pic went blank all day (log proof 07-22: 04:34–06:30 UTC runs all rowcount 0). Rule of thumb: **`3 AM "local" ≠ 3 AM UTC`; for post-restore reappliers default to all-day, like the HermanRO regrant cron that never had this problem.** (2026-07-23) [[env-host-and-tooling]]

- trimit/diagnosis — **When an outcome has MULTIPLE independent producers, fix the simplest path the user actually triggers before chasing the deeper mechanism.** The Location "Lat/Long won't populate" bug had TWO broken geocoders both writing `Locations.Latitude/Longitude`: (1) client-side JS (`codeNewAddress`) that crashed because `myLatitude` inputs were role-gated (line 514) → null element → JS threw; (2) a SQL trigger geocoder (`Locations_LatLong_upr`) calling a dead keyless-Google endpoint. I *found* the line-514 gate early but over-pursued the SQL geocoder and built a whole Census-proc fix + deploy package. Jordan's dev correctly made the client-side gate/null-guard/async-race the fix and shipped it to prod — cleaner (no SQL→OLE→HTTP). Both fixes "work" because either geocoder populating the DB satisfies the map. LESSON: enumerate ALL producers of the symptom, then pick the one on the actual user action + fewest moving parts — don't fall in love with the deepest root cause you find. (2026-07-23)

- **[trimit/gps-import · pricing]** The GPS/RFP importer creates **NEW `InventoryGroups` in category 197 "UNDEFINED"** matching the survey's species names verbatim (e.g. "LONDON PLANE", "AFGHAN PINE", "Brisbane Box") **instead of matching to the existing GSTS catalog** — so those trees are **orphaned from all invoice/pricing history** (0 HoursEach rows) even though the species has thousands of invoiced trees under its proper name. Cost: on the Goodman pilot, 50/81 trees (62%) fell to the generic size-fallback for pricing. **Fix two places:** (1) pricing — map UNDEFINED dupes to the real catalog species (London Plane→"Sycamore - London Plane", Brisbane Box→"Tristania (Brisbane Box)", S.Magnolia→"Magnolia", Afghan Pine→"Pine - Eldarica"); (2) the converter/import — add a species-name→existing-InventoryGroupID match step so future imports don't create UNDEFINED duplicates. Affects every Goodman property. Detect: `WHERE ig.InventoryCategoryID=197` on a project's inventory. (2026-07-23)

- **[trimit/species-crosswalk]** When matching survey/RFP species names → TRIM IT `InventoryGroups`, **"exact-name match" is NOT good enough** — the catalog has many sparse duplicate entries under the plain common name, while the CANONICAL high-history entry is usually in "Type - Variety" format. Herman's first Goodman crosswalk matched exact names and landed on near-empty dupes for 3 big species: African Sumac 511 trees → "African Sumac" (ID 2019, 2 comps @0-6) instead of "Rhus - African Sumac" (259, 452); Callery Pear 286 → "Pear - Ornamental" (604, 83) instead of "Pear - Bradford" (247, 1381); Elm Hybrid 33 → "Elm - spp." (321, 0) instead of "Elm - Chinese" (287, 1479). **Rule:** among ALL name/botanical/alias matches, pick the InventoryGroupID with the **most 0-6 ServiceClass-1 history** (tiebreak any-size), exclude cat 197. Verify big-tree-count species by hand. Detect a bad match: high tree count + tiny `HoursEach` N. (2026-07-23) [[pricing-true-labor-method]]

- **[trimit/species-crosswalk · refinement]** The "most-0-6-history-wins" rule (above) must be **scoped to the SAME plant** — it over-corrected in Goodman v2 by swapping botanically-correct matches for higher-volume *different* ones: Marina cultivar (Arbutus 'Marina', ID 29, 363 comps) → "Arbutus - Strawberry Tree" (30, a different species, A. unedo); Magnolia 'Little Gem' (dwarf cultivar, 844, 623 comps) → generic "Magnolia" (175). **Correct rule:** match the correct species/cultivar FIRST; among duplicate/spelling-variant/generic entries **of the same plant**, pick the most history — but **never cross to a different species or dissolve a distinct cultivar into a generic just for volume.** If the correct match is thin at 0-6, LEAVE it — the pricing engine's species→size-level fallback already covers thin data. **More history ≠ better if it's a different tree.** (2026-07-23)

- **[trimit/diagnosis · deliverable-maps]** Herman's traveler-package "Field Map" is a **matplotlib scatter he generates himself** from Schedule-2 GPS (caption: "plotted from GPS coordinates | Generated by Boss Hermes"), **NOT the TRIM IT FieldApp Google map.** When Skipper said the traveler map was "blank with dots," I spent a full prompt chasing Google Maps API-key / `v=` version / `.cfm` traveler templates — all irrelevant. The screenshot showed dots on plain graph paper: there was simply no basemap layer in Herman's plot. LESSON: for a "map looks wrong" report, **get the actual image before diagnosing** — a self-generated chart vs. the app's rendered map are totally different code paths. Fix here lives in Herman's Python PDF script (add a real basemap, e.g. contextily + Esri WorldImagery, reproject 4326→3857), not TRIM IT. (2026-07-24)

- **[trimit/coldfusion-cache]** On the play ColdFusion 2023 server, **rendering a .cfm repeatedly DURING diagnosis caches its compiled class in CF's in-memory template cache — and after you deploy a code fix, CF keeps serving the OLD bytecode.** Trusted Cache is OFF (verified in neo-runtime.xml), yet bumping the file mtime (even +5 min future) AND deleting all `cfclasses\*<Template>2ecfm*` class files did NOT force a recompile — the class is pinned in the JVM template cache (templateCacheSize 1024). A fresh FILENAME compiles clean and shows the fix (proved the fix works: `Dashboard-RevenuePerformance-T0724.cfm` → correct $), so the code is right; only the cache is stale. **Fix requires clearing CF's in-memory template cache: CF Admin → "Clear Template Cache", or restart the ColdFusion service.** LESSON: for .cfm repairs, DEPLOY the fix FIRST, then render to verify — do NOT render the page many times pre-deploy, or you'll pin the old compile and need a cache clear/restart. (2026-07-24) [[repair-contract]]

- **[trimit/deploy · CRITICAL]** Play serves some .cfm from a SHADOW webroot **`C:\ColdFusion2023\cfusion\wwwroot\GSTS\`**, NOT only the IIS root `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` that `deploy-to-play.sh`/`gstsdb` scp targets. If a file exists in BOTH, editing only D: does nothing — CF serves the C: copy (survives CF restarts + cfclasses deletion, because the stale SOURCE is what recompiles). This masqueraded as a "CF template cache" problem for ~40 min on the RevenuePerformance fix. **Before concluding "cache," search the whole server for duplicate copies of the file** (`Get-ChildItem D:\,C:\ -Recurse -Filter <name>.cfm`) and deploy to EVERY copy that's live. The DEPLOY-PLAYBOOK's "dual-webroot shadow copy" warning = exactly this. Files absent from the shadow (most) serve from D: fine, which is why it's intermittent and confusing. (2026-07-24) [[repair-contract]]

- **[tooling/detached-crew-runs]** Launching a long crew LLM run (e.g. `kimi-ask.py`) via the harness: (1) `pkill -f "kimi-ask.py"` SELF-KILLS the launching command — `-f` matches the pkill command's OWN cmdline (which contains "kimi-ask.py") → instant exit 144. Never pkill a pattern that matches your own command. (2) The CLI has a **180s no-output watchdog** that SIGKILLs (137) a foreground command that emits nothing — reasoning models buffer silently for minutes → killed. (3) Harness `run_in_background` also killed these long runs. **FIX: launch DETACHED** — `nohup python3 kimi-ask.py <promptfile> > out 2> err < /dev/null &` (no pkill), the launch returns instantly (no stall), Kimi runs decoupled; then poll the PID + output file with quick commands. Kimi K3 = `~/arbor-core/crew/kimi-ask.py`, ~6 min on a 168KB prompt. (2026-07-24)

- **[crew/kimi-k3-reverse-engineering]** Kimi K3 is EXCELLENT at reverse-engineering a subsystem from raw code. Gave it ~2,500 lines (apiJS.cfm + field map + apiCall) + the DB facts I'd established + a precise 4-part question; it correctly traced the grey/published map layer end-to-end (GeoJSON files in /GSTS/API/JSON/, the exact parser property contract, the IsNewPlot gate, the publish sequence, and a PLAY-viable fix bypassing the dead Taffy/proc). Every claim verified against code+server. Pattern that worked: give it ALL relevant files + everything you already know (so it doesn't re-derive) + numbered concrete questions demanding line-number citations. (2026-07-24) [[our-work-kanban]]

- **[metrics/tph · ALWAYS STATE WHICH]** **"TPH" is two different numbers and they RANK SEGMENTS IN OPPOSITE ORDER.** On **productive hours** commercial **$157.53** beats municipal **$146.70** (as it always has). On **blended** TPH — including $0-revenue paid hours — the ranking **inverts**, because commercial carries 27,325 zero-revenue hours (24.4% of its total) vs municipal's 9,716 (14.5%). I reported "municipal beats commercial" off the blended figure; Skipper caught it. **Rule: never say "TPH" unbaptized — say Productive TPH or True/blended TPH, and never compare one to the other.** (The RC-02 dashboard now shows both side by side on purpose.) Related open item, do-not-present: ~21,000 commercial field hours with $0 revenue that T&A only explains ~6,377 of. (2026-07-24) [[trimit-investor-case]] [[rc-02-revenue-performance]]

- **[data-trust/back-entry]** **Before computing ANY duration/aging metric in TRIM IT, exclude the back-entered records first.** Bid-chain timestamps (RFP created / proposal sent / go-ahead) are frequently stamped **within the same second** because the record is created *after* the work happened — 12% inside 60 seconds, 25% inside an hour, 97% of sent→approved same-day. My first turnaround pass reported **median 2 days** and it was garbage; filtering the back-entry set out gave the real answer, **median 6d / p75 14d / p90 36d**, which independently validated Skipper's "about 10 days." Same family: `Invoices.InvoiceDate` is a BACKDATED accounting date (median −3d vs completion) so billing speed must be measured off `Invoices.Created`. **Smell test: if a duration distribution has a huge spike at ~0, you're measuring data entry, not the process.** (2026-07-24) [[trimit-db-gotchas]] [[trimit-investor-case]]

- **[tooling/browser-agent]** **A "crawl this whole app and report" prompt handed to a browser-driving agent will blow the context window — chunk it by section.** The dev-server FieldApp inventory prompt (map every page, follow every link, open the console on each) ran 22 steps and died at **1,411,579 tokens > 1,000,000 max**, and even compaction failed. Re-run in chunks (nav map first, then one subsystem per run) and have it return findings per chunk. Also budget for it making **accidental writes** on a "read-only" browse: buttons named like modals aren't (dev's "Add Contact" hit `Field.Contact.Create.cfm`, which INSERTS a blank row server-side before you type, leaving ContactID 222090 to clean up) and some checkboxes POST straight to an API. **"Don't click Save/Delete/Submit" is not sufficient read-only instruction** — tell it that any button may write, and to report what it touched. (2026-07-24) [[vendor-fieldapp-build]]
- 💥 trimit/play-db — **The nightly prod→play GSTS restore WIPES anything we build in the GSTS db — including whole projects/inventories.** 2026-07-25: the 04:49 UTC restore (of a 7/22 backup) erased all three Goodman builds — projects 1105465 (Fullerton pilot), 1105467 (Eastvale, 313 trees), 1105468 (El Monte, 298 trees) + LocationIDs 1285095/96/97 — plus Herman's in-flight El Monte fixes. `MAX(ProjectID)` was back to 1105464. We spent 3 days building on sand. **Rules going forward: (1) treat `GSTS` on play as EPHEMERAL — any multi-day build must live in `Workbench` (separate db, NOT restored) or in flat files, with a scripted, idempotent replay into GSTS; (2) after every prod→play restore, re-verify anything we "shipped" to the DB still exists before reporting it done; (3) `msdb.dbo.restorehistory` is the fast check.** What saved us: the backup-first rule — `Workbench.dbo.*_bak_*` snapshots survived and hold the full tree rows. (2026-07-25) [[env-host-and-tooling]]
- 📛 tooling/verification — **Don't trust a crontab/config COMMENT as a statement of current behaviour — read the code it invokes.** I told the Skipper the AR collections job was "PREVIEW to Skipper while piloting" because that's what the crontab comment said; the wrapper actually calls `--live` and sends to every rep, Nate and Brent. Comments describe the day they were written, not today. When reporting what an automation *does* (especially who it emails), trace: cron line → wrapper → script → the flag that decides recipients. (2026-07-25) [[async-report-rule]]
- 💥 tooling/email — **Verify a script's dry-run flag NAME before using it on a live emailer.** I ran `ar-collections-monitor.js --dry-run`; the real flag is **`--dry`**, so the unknown flag was ignored and it actually sent (a PREVIEW to the Skipper — contained, but unintended). With any script that can send: `grep` for the flag first, or test with the send transport unreachable. Unknown flags fail OPEN, not closed. (2026-07-25) [[external-comms-contract]]
- 🕵️ data-pipelines — **When one stage RENAMES a file, check every downstream regex that parses the name.** `ar-fetch.js` saved Dimitry's report as `AR-Aging-2026-07-21.xlsx`; `ar-collections-monitor.js` still parsed the week with `/(\d{2}\.\d{2}\.\d{2})/` (his ORIGINAL `07.21.26` naming). It silently matched nothing, so `wk=''` and every AR email to the reps went out **with no date on it** — the one field that would have exposed stale data. Silent-empty is the worst failure mode: it looks like a design choice. Prefer failing loudly (we now emit `(DATE UNKNOWN)` and refuse to send). (2026-07-25) [[only-trustworthy-data]]
- 📭 email-automation — **Don't gate "have I processed this?" on the IMAP `\Seen` flag.** `ar-fetch.js` searched `{seen:false}`, so any human opening the inbox — or our own `inbox-recent.js`, which downloads bodies and thus sets `\Seen` — silently consumed that week's report; **3 of 6 weekly AR reports were lost.** Track processed items in our own state file keyed by **message-id**, and make any inbox-listing tool restore the flags it found. Read state is shared with humans; it is not ours to use as a queue. (2026-07-25) [[async-report-rule]]
- 🔎 openclaw/status — **The `/status` card packs FOUR independent axes onto one line — don't read adjacent fields as one phrase.** `Execution: direct · Runtime: … · Think: off · Fast: off · Reasoning: on · elevated`. **`Reasoning: on`** = whether thinking is *displayed* (`/reasoning`); **`elevated`** = *tool/command permissions* (`/elevated`), unrelated to reasoning — the Skipper read the tail as "reasoning elevated". **`Think:`** = the thinking-effort override, a third thing. Proof method that settled it: toggle one control and **diff two status cards** — `elevated` was present before the reasoning toggle, `Reasoning: on` was not. Also: I misread `Think: off` as "thinking disabled" and theorised on top of it — **read the docs (`docs/tools/thinking.md`) before explaining a config behaviour twice.** Authoritative check is `/think` alone (user must send it; directive-only messages are stripped before I see them). Resolution order: inline → session override → per-agent → `agents.defaults.thinkingDefault` → fallback **medium**. (2026-07-25) [[gilligan-session-settings]]
- 🔗 trimit/schema — **The bid chain joins on `ProposalID`, NOT `RFPID`.** `RFPs.ProposalID → Proposals.ProposalID` and `GoAheads.ProposalID → Proposals.ProposalID` are the real links. `Proposals.RFPID` is a BACK-link populated only ~32% of the time, and `RFPs.GoAheadID` is **0%** populated (2026). I joined on the back-link, found "68% of proposals unlinked", and raised a false alarm that the whole turnaround methodology was survivorship-biased. **Before declaring a data-integrity problem from a join, check whether the link runs the OTHER way** — `sys.columns` on both tables takes 20 seconds. (2026-07-25) [[trimit-db-gotchas]]
- 📐 analysis — **Save the query, not just the number.** Deck figures from 7/24 (population 2,329, p90 36, avg 17, "12% within 60s") could not be reproduced 24h later because the exact filter was never written down — median 6 and p75 14 came back fine, the tail stats didn't. A deck that claims "every figure is checkable" has to ship with the SQL that checks it. Fix: `VERIFY-deck-numbers.sql`, each query marked reproduces/doesn't. (2026-07-25) [[only-trustworthy-data]]
