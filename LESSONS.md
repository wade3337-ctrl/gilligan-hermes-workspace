# LESSONS.md — What Didn't Work (gotchas · dead-ends · anti-patterns)

**Purpose:** the ONE place I check *before* a task to avoid repeating a known failure. Paired with `PLAYBOOK.md`
(what works). **🔄 AUTO-UPDATE RULE:** the instant something fails or wastes real time, I add a one-line entry here,
tagged by domain — *in the moment*, not "later." Kept **lean** (consolidate, don't pile). In my lookup path via
`ROUTING.md` + `MEMORY.md`. Format: `- ⚠️ <the trap> → <the fix/avoid>. (date)`

## 🤖 Models / CLI tooling
- ⚠️ **GPT-5.6 Sol needs both the right OpenClaw ref AND a new enough Codex runtime** — raw `codex debug models` showed `gpt-5.6-sol`, but `openai/gpt-5.6-sol` never resolved through `infer`; the working catalog ref in OpenClaw 2026.6.1 is `codex/gpt-5.6-sol` with alias `sol`. Even then, the gateway returns `The 'gpt-5.6-sol' model requires a newer version of Codex` while `codex-cli` is 0.144.1. → setup order: verify raw catalog, add explicit `models.providers.codex` row + `agents.defaults.models["codex/gpt-5.6-sol"]`, then upgrade OpenClaw/Codex before setting default; live-test with `openclaw infer model run --gateway --model codex/gpt-5.6-sol`. (2026-07-30)
- ⚠️ **Kimi K3: slow but sol-level — LEAN ON IT as crew (Skipper directive 2026-07-21, upgraded K3 sub).** Validated 2026-07-19 K3 tied `gpt-5.6-sol` 100%/100% on a blind hidden-test coding+reasoning duel; always-on thinking makes it ~7× slower. Per Skipper: use K3 as a **first-class quality/cross-lab verification crew judge and give it time** (900s timeouts, run in background) rather than dropping it for speed. Only route genuinely latency-critical / agentic-coding work to Codex; for quality reviews K3 is a go-to, not a fallback. (updated 2026-07-21)
- ⚠️ **OpenClaw generic Discord error after Opus stops can be Claude Code auth, not Discord/OpenClaw** — logs showed `claude-cli` returning `401 Invalid authentication credentials` while `claude auth status` still said logged in. → check `journalctl --user -u openclaw*` / `/tmp/openclaw/openclaw-YYYY-MM-DD.log`; fix by refreshing Claude CLI auth (`claude auth login`, then `claude setup-token` if needed). (2026-06-29)
- ⚠️ **Opus can have two simultaneous auth blockers** — Claude CLI may be stale (`claude -p` returns `401` even when `claude auth status` says Max/loggedIn), while OpenClaw manual Anthropic token may be valid but blocked by Claude's "third-party apps draw from extra usage" billing gate. → test both paths separately: direct `claude -p` for CLI, `openclaw agent --model anthropic/...` for manual token; fix CLI with `claude auth login`, fix manual token by adding extra usage at `claude.ai/settings/usage` or using a fresh setup token with available balance. (2026-06-29)
- ⚠️ **Ollama `llama3.2:3b` at 131k context can effectively wedge the laptop** — logs showed CPU runner allocating ~14GB KV cache on a 15GB box after Claude/OpenClaw fallback, followed by Docker/Discord timeouts and stuck OpenClaw runs. → keep Ollama CPU-only for the unsupported GTX 1050 Ti and cap local model context around 4k/8k unless intentionally running a memory test. (2026-06-30)
- ⚠️ **GLM (& Codex) agentic `claude -p` CLI is UNRELIABLE here** — it **HANGS (124) OR SIGKILLs (137)** on tool
  loops. Root-caused 2026-06-23: the CLI's **agentic orchestration with the backend** is the broken part, NOT GLM or
  the box (GLM via direct API answers in seconds). → use `~/arbor-core/crew/glm-ask.py` (reasoning) or
  **`glm-judge.py`** (tool-running DB-verification via a stable Python loop). (2026-06-21 / 23)
- ⚠️ **Codex `codex exec` also dies (137) on heavy *nested* runs** — the sandbox kills fat subprocess trees;
  lightweight calls (gsql, GLM-API) are fine. Don't depend on long nested CLI agents. (2026-06-22)
- ⚠️ **Background sub-agents get killed mid-run** (the v1.1 producer was) — the **main session is stable**; do
  consequential long work there, not in detached background agents. (2026-06-22)
- ⚠️ **Kimi empty/truncated output = the HELPER `max_tokens` cap, NOT the account** — K2 is a reasoning model and burns the budget *thinking* before it emits the answer, so "ran out of budget" was `kimi-ask.py`'s cap (raised 8k→16k→**40k**) while the Skipper's Kimi account was ~1% used. Raise `max_tokens` before suspecting billing. (2026-06-28)

## 🗄️ DB / SQL (TRIM IT, sqlcmd via gsql.sh)
- ⚠️ **The GSTS play SQL box is SHARED and gets hammered by daytime SSMS load** — a heavy analytical query (retention sessionization) ran **<1s when the box was quiet** but **timed out (>4–5 min)** minutes later while someone ran heavy work in SSMS. `sqlcmd "Timeout expired"` = a **QUERY** timeout (server too slow), **NOT** a network drop — confirmed the tailnet was direct/0%-loss the whole time. → schedule heavy/analytical runs **OFF-PEAK (3am PT)**; don't reboot the box chasing a network ghost. (2026-07-15)
- ⚠️ **A proc/view "deployed" per the notes may NOT actually be on the server** — `ret.vRetentionTrend` (054) was written + bug-fixed in the .sql file but its `OBJECT_ID` was NULL (never applied). → `SELECT OBJECT_ID('schema.obj')` for every object before validating, and redeploy from the .sql files. (2026-07-15)
- ⚠️ **`gsql.sh` takes a FILE or STDIN, not an inline arg string** (it `cat`s $1) → pipe SQL on stdin. (2026-06-21)
- ⚠️ **Play webroot SSH key = `/home/wade3337/.ssh/gstsdb_ed25519`** (Administrator@100.86.97.46). When writing an inline `KEY=…` in an exec command, paste the LITERAL path — a masked/ellipsis `/home/…519` is a real filename that fails with "Identity file not accessible". Cost 3 failed calls in the deep-audit session. `gsql.sh` hardcodes the right path, so DB queries never hit this; only ad-hoc `ssh`/`scp` do. (2026-08-01)
- ⚠️ **Real TRIM IT webroot = `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` (8,645 .cfm), NOT `C:\ColdFusion2023\...\wwwroot\GSTS\`** (that's our 11-file dashboard drop). Pull core create/edit forms from the D: path. (2026-08-01)
- ⚠️ **`gsql.sh` writes ONE shared remote temp file** → concurrent queries clobber each other → run **sequentially**
  or use a per-process isolated wrapper. (2026-06-21)
- ⚠️ **Reserved words as a column alias break sqlcmd** ("Incorrect syntax near 'proc'") → use a safe alias (`pname`). (2026-06-21)
- ⚠️ **`SUM(CASE WHEN EXISTS(subquery)…)` / aggregate over a subquery → SQL error 130** → compute the per-row flag in a CTE/derived table, then `SUM()` it. (2026-06-25)
- ⚠️ **`dbo.ZoneDefs.ColorCode` TEXT is a useless uniform 'Yellow' on every zone — the REAL per-zone color is `ColorDefID` → `dbo.ColorDefs.ColorCode` hex** (Green/Orange/Blue/Red matching the printed map). First zone-importer preferred the text field → every area came out yellow. → resolve color via ColorDefID, fall back to the text only if the FK is null. (Reverses an earlier wrong call to "trust zd.ColorCode text.") (2026-07-07)
- ⚠️ **sqlcmd (gsql.sh) truncates `varchar(max)` at 256 chars** → a long value (e.g. `ZoneDefs.FusionTablePolygon` KML, up to ~2KB) comes back cut off. → fetch it in ≤160-char **bracket-wrapped** chunks (brackets survive `-W` trailing-trim) + reassemble in Python. (2026-07-07)
- ⚠️ **Some TRIM IT `ZoneDefs` rows are junk** — C2E Townhomes (1283100) had 5 *identical duplicate* polygons + 1 that parses to 0 points, all one color. The pretty multi-color map was hand-drawn onto the image, NOT saved as data. → before trusting a site's zones, check for distinct geometry; don't assume the colorful map == pullable vector data. (2026-07-07)
- ⚠️ **A literal `#` in ANY double-quoted CFML string = "missing ending #" / interpolation error — NOT just inside `<cfoutput>`** (also `<cfset x="...#77...">`, array literals, etc.). Bit me twice in one day (inline `color:#fff`, then `#77` inside a `cfset` repairRows array). → escape as `##`. (2026-06-25)
- ⚠️ **Don't BATCH the live `Reference-RepairsAndScheme.cfm` sync** — update its `repairRows`/`buildRows` when you write each `gsts-ship-log.md` row, not "later." Let it drift 13 entries behind once (Skipper caught it). Edit it byte-level (ASCII-only inserts after a newline) since it's UTF-8 + may have a BOM. (2026-06-25)

## 🗺️ Leaflet / maps
- ⚠️ **Leaflet CANVAS renderer (`preferCanvas:true`): the DOM `pointer-events:none` trick to make an overlay click-through is a NO-OP** — canvas paths have no own DOM element, so `layer.getElement()` returns nothing and the style never applies. A filled *interactive* polygon then swallows clicks meant for markers underneath (bit the workbench lasso twice — #77 "fixed" it this wrong way). → set **`interactive:false`** on the overlay so Leaflet skips it in JS hit-detection; the shape still renders. (2026-06-25)

## 📧 Email (gilligan.gsts gmail)
- ⚠️ **Self From=To lands in Sent, not Inbox** → keep From≠To.
- ⚠️ **Rep daily emails are plain-text only (no HTML part)** → extract `text/plain` when copying/forwarding; grabbing
  the HTML part returns BLANK (I sent the Skipper an empty copy once). (2026-06-22)
- ⚠️ **ImapFlow by-UID lookup** needs `{uid:true}` in the OPTIONS (3rd) arg of `fetchOne`/`fetch`, NOT the query — a
  bare number is a *sequence* number → silent false→crash. (2026-06-20)
- ⚠️ **I can read `gilligan.gsts@gmail.com` only — NOT `jwade@gstsinc.com`** (no M365). Inbound to jwade must be forwarded. (2026-06-22)

## 🌳 TRIM IT / web (ColdFusion)
- ⚠️ **The GSTS app's `.cfm` files aren't in `C:\...\wwwroot\GSTS`** (near-empty) — they serve from the **D: webroot**.
  Don't conclude "missing" from the C path. (2026-06-22)
- ⚠️ **Menu "window" names ≠ filenames** — resolve the real page via `dbo.AppForms.ObjectPath`; an app's JS endpoints
  are **relative to that app's folder** (e.g. `FieldApp/...`), so root-level fetch 404s. (2026-06-22)
- ⚠️ **`.cfm` with emoji/non-ASCII needs a UTF-8 BOM** or ColdFusion serves mojibake (`ssh type` strips it). **Cleaner alt: use HTML numeric entities** (`&#NNNN;`) → ASCII source, no BOM. CF only treats `#` special *inside* `<cfoutput>` → single `#` in static HTML, **double `##` inside cfoutput** (collapses to one on render). (2026-06-25)
- ⚠️ **Substring role-matching trap:** CFML `CONTAINS "coo"` matched "**coo**rdinator" → Sales/Production *Coordinators* mis-tagged as exec. Use **padded whole-word checks** (` coo `/` ceo `) for short tokens; avoid over-broad keywords like bare "director". Always test role logic against real users. (2026-06-25)
- ⚠️ **Dual-webroot shadow:** `C:\ColdFusion...\GSTS` can OVERRIDE `D:\...\GSTS` → render-verify the *served* output.

## ⚙️ Config / infra
- ⚠️ **Changing `model.primary` in `openclaw.json` does NOT move an EXISTING long-running session** — it has a sticky per-session model binding in `~/.openclaw/agents/main/sessions/sessions.json`. After the Opus 5 upgrade + gateway restart, config read `anthropic/claude-opus-5` but the live session still resolved `claude-cli/claude-opus-4-8`. → the config default governs **NEW** sessions/crons/subagents; for the current one re-apply the session override (`session_status` with `model=...`). Don't hand-edit sessions.json (gateway owns it). Also: `openclaw gateway restart` **kills the agent process running it** → the restart command returns exit 137 to me; that's expected, not a failure — confirm via the follow-up system event + `session_status` uptime. (2026-07-24)
- ⚠️ **Adding an auth gate to a TRIM IT dashboard silently breaks automated CONSUMERS that scraped it unauthenticated** — the V1.5 `dashboard-auth-gate.cfm` (2026-07-12) started 403-ing the anomaly-monitor's `bookedPacing()` POST to `Dashboard-RevenuePerformance.cfm` → the daily email quietly showed "Forward pace unavailable (HTTP 403)" for days. Same bug hit `revenue-block.js` (salesperson + Nate emails). → the gate authorizes on a **`ZUserID` cookie** (bootstrap-allowlist 9=Jason, **376=Arbor Helper bot**); fix = send `Cookie: ZUserID=376` on the fetch. LESSON: when hardening a data surface, **grep for every headless/automation fetch of it** and re-authorize them in the same change. (`m2-revenue.js` has the same cookie-less pattern but is unused legacy.) (2026-07-15)
- ⚠️ **`~/.openclaw/openclaw.json` has clobber history** → always back up + merge-patch, never overwrite.
- ⚠️ **Python 3.14 here has no package manager + no passwordless sudo** → use stdlib; Skipper installs system pkgs.
- ⚠️ **No headless browser** → authenticated HTTP fetch (view.sh) for web/ERP pages.
- ⚠️ **MEMORY.md silently clipped at ~20k** → `agents.defaults.bootstrapMaxChars`/`bootstrapTotalMaxChars` were UNSET in `~/.openclaw/openclaw.json` (bootstrap truncated). Set them (40000 / 160000), back up the config first, and restart to apply. (2026-06-28)
- ⚠️ **This workspace clone may have no Git author identity configured** — the first commit failed with `unable to auto-detect email address`. Before committing, inspect the latest commit author and repo/global config; set the matching identity repo-locally rather than guessing or changing global config. (2026-07-31)

## 🧑‍🤝‍🧑 Working with the Skipper / process
- ⚠️ **Don't let background/inter-session chatter drown the user** (`[[subagent-completion-noise]]`) — surface real
  status; never go silent through a direct question.
- ⚠️ **Don't over-claim beyond the evidence** — GLM caught me twice asserting *behavior* from row-counts (pricing map).
  State only what's proven; flag inferences as inferences. (2026-06-21)
- ⚠️ **"Set up" ≠ working** — laptop GLM was reported "set up" but had **NO key/client/config** at all. Always VERIFY
  the config actually persisted (key present? endpoint right? client runs?) before assuming a setup landed. (2026-06-23)
- ⚠️ **Don't run a crew gate where only ONE of us can see the result** — a background-shell run leaves the Skipper blind to it; a `sessions_spawn` sub-agent leaves ME blind to it (supersedes the 06-26 "spawn a sub-agent for long crew work" note). → run gates **FOREGROUND, in-turn**; confirm the Skipper got it, re-send if it doesn't land. (2026-06-28)

## 🪟 Windows / SSH (Hermes laptop)
- ⚠️ **Recursive `dir /s` / `findstr /S` over the user profile or a venv HANGS the SSH session** (timed out 3× in a
  row) → keep Windows recon **NON-recursive**; target specific files/dirs. (2026-06-23)
- ⚠️ **My laptop SSH key can get de-authorized** (it was removed from `administrators_authorized_keys` during a
  Herman setup) → if `Permission denied (publickey)` but host key matches, my key was dropped; have Herman re-add it. (2026-06-23)
- ⚠️ **`icacls … /inheritance:r` may not stick if chained loosely** — run it cleanly and CONFIRM with a follow-up
  `icacls <file>` (look for `(I)` = still inherited). (2026-06-23)
- ⚠️ **Laptop Herman brain = GLM is UNRESOLVED (2026-06-23):** Hermes's built-in **z.ai provider uses the METERED
  `/paas/v4`** (blocked on the coding plan → "insufficient balance"); forcing `glm-5.2` via Hermes's **anthropic**
  provider + the z.ai **anthropic** endpoint produced **"no final response"** (Hermes↔z.ai coding-endpoint
  compatibility gap — needs `hermes` debug-log analysis). Herman was ALREADY pre-set to glm-5.2/metered (= the
  original "GLM not working"); its model config is **NOT** in `~/.hermes/config.yaml`. GLM-as-a-TOOL works (`glm-ask.py`).
- ⚠️ **ColdFusion: literal `#` inside `<cfoutput>` errors** — a hex color (`color:#ffe08a`) or any `#...#` in cfoutput is read as a CF variable → "An error occurred" with debug off. **Escape as `##` (`##ffe08a`)** or move the literal outside cfoutput. Cost ~2 deploys to spot (2026-06-23, ZTest-SiteMap).
- ⚠️ **CF dynamic SQL: avoid `--` line comments inside `<cfquery>`** — if the newline collapses, `--` comments out the rest of the query. Use `/* ... */` block comments. (2026-06-23)
- 💡 **TrimIT geo data has bad geocodes** — active job sites include sign-flipped longitudes (+117 vs -117 → Asia) and mis-geocodes (UK/NM/ID). Any map MUST bound to a SoCal sanity box (lat 32–35.5, lng -121 to -114) and *count+flag* the strays, never silently fit-to-all (4/2906 active blew the view worldwide). Fixable in the geocode backfill pass. (2026-06-23)
- ⚠️ **Leaflet canvas (preferCanvas): don't `removeLayer`/`addTo` circleMarkers to filter** — they fail to repaint on re-add (filter down works, clearing the filter leaves them gone). Instead keep all markers on the map and toggle visibility with `setStyle({radius:0,opacity:0,fillOpacity:0})` for hidden / real style for shown — `setStyle` repaints reliably both directions, and radius:0 = visually off the map + no overlap. (2026-06-23, ZTest-SiteMap)
- ⚠️ **Don't trust a "model X is flaky" verdict without a repeat test.** Codex was written off as flaky (Jun 25 City Budgets, exit143) — re-tested Jun 26, 8/8 clean incl. a heavy 3-query verify. The real causes were (a) a transient blip and (b) MY prompt leaking a sentence period as a shell arg (`gsql.sh .` → cat-ed a dir → empty → model reported "0"). Check ground truth + prompt hygiene before blaming the tool; one bad run ≠ broken. (2026-06-26)
- ⚠️ **Shell-wrapper scripts should fail loudly on a stray/garbage arg**, not silently return empty. `gsql.sh` used `cat "${1:-/dev/stdin}"` → a `.` arg cat-ed a directory → empty SQL → a model misread the blank as a real `0`. Guard: if `$1` is set and not a readable file, error+exit. (2026-06-26)
- ⚠️ **dbo.SalesRepPerformance is a SNAPSHOT, not a source** — populated by `GenerateAllSalesRepPerformance` (INSERT…SELECT from other tables), per-period rollup (PeriodID + InvoiceTotal/NetTotal/TPH cols), 0 triggers. Do NOT build arbor-core sales attribution/perf on it; use dbo.SalesReps (base entity: SalesRepID/IsMeasured/StatusDefID) + transactional sources. Verified by Codex lineage judge 2026-06-26. (2026-06-26)
- ⚠️ **Crew HTTP helpers must use a generous timeout + catch timeouts, not just HTTPError.** `gemini-ask.py` had `timeout=120` and only caught `HTTPError` → big reasoning prompts (3.1-pro on a full schema-gate dump) took ~121s and died with a raw `TimeoutError` traceback (silent `[gemini 1]`, empty output) — looked like "Gemini is down" but the API + key were fine. Fixed: 300s default (`GEMINI_TIMEOUT`) + retry loop catching `URLError`/`TimeoutError`/5xx/429. Apply the same pattern to every `*-ask.py` helper. (2026-06-26)
- ⚠️ **Always feed the crew the CURRENT artifact — stale-prompt = wasted run + muddled report (2026-06-26).** Ran a Kimi schema judge but pointed it at the OLD v1.2 review prompt (a leftover lens file), so it judged v1.2 while the live schema was v1.6 → most of its 12 findings were already fixed (e.g. it flagged the per-entity-transition issue we'd resolved in v1.4). Had to cross-check every finding against current to separate stale from live. Rebuild the evidence bundle from the CURRENT version before every (re-)gate; never reuse an old lens file. (Bright side: it still caught 1 real live gap — an undefined `agent` table — proving the value, but that was luck, not hygiene.)
- ⚠️ **HTML numeric entities (e.g. `&#127807;` 🌿) inside `<cfoutput>` BREAK ColdFusion** — the `#` starts a CF expression, so `&#127807;...$#NumberFormat(...)#` gets parsed as one giant expression → COMPILE error → whole page dies with a generic "error occurred". Cost ~40 min mis-blaming a SQL query (which was fine). **Fix: escape as `&##127807;`** (CF `##`→literal `#`) or paste the raw emoji char. Named entities (`&mdash; &amp;`) are safe (no `#`). When a cfquery page dies and the SQL runs clean standalone, suspect a `#` in the cfoutput HTML before the query. (2026-06-26, FinancialReportDashboard treatment line.)
- ⚠️ **Verify every SSH `copy` backup actually wrote** (read it back / check size) — a chained `if not exist ... & copy` or a bad path fails SILENTLY on cmd.exe; I restored from a "backup" that was actually the original and reverted a dashboard's whole feature set. Always `dir`/`type | grep` the backup after copying, and keep a local `scp`-down copy of anything important. (2026-06-26)

## 🧠 Agent brains / model auth
- ❌ **Don't run an OpenClaw-style agent's brain on a Claude SUBSCRIPTION token (work Enterprise / Max) for real agent work** — Anthropic now meters third-party apps separately: plain chat passes, but a full tool-using request → **HTTP 400 "Third-party apps now draw from your extra usage, not your plan limits."** Hermes's Claude-Code disguise (identity + ua + `mcp_`→`mcp__`) clears the 429 identity wall but NOT the billing classifier. Chasing it = fragile + ToS-gray. Use a sanctioned flat-rate provider (GLM/z.ai sub) instead. (2026-06-30)
- ❌ **z.ai SUBSCRIPTION only authorizes the Anthropic-compat endpoint** (`/api/anthropic` or `open.bigmodel.cn/api/anthropic`), NOT the OpenAI endpoint (`/api/paas/v4` → "Insufficient balance"). So Hermes's *native* `zai` provider (which uses paas/v4) fails on a sub — use `provider: anthropic` + `base_url` override to the z.ai anthropic endpoint. (2026-06-30)
- ❌ **Dotted model ids get mangled under `provider: anthropic`** — Hermes's `normalize_model_for_provider("glm-5.2","anthropic")` → `glm-5-2` (dot→hyphen), which z.ai rejects (`1211 Unknown Model`). Fix: use the **non-dotted alias** (`glm-5` → resolves to glm-5.2). ALWAYS check the model id the GATEWAY actually sends (log `model=…`), not a hardcoded client repro — the repro hid the bug. (2026-06-30)

## 🖥️ Frontend / preact single-file app (arbor-core one-stop UI)
- ❌ **TDZ crash blanks the WHOLE preact app + `node --check` won't catch it** — a `useState` var referenced in a `useEffect(cb,[x])` **dependency array placed ABOVE** its `const [x]=useState()` line throws "Cannot access 'x' before initialization" at RENDER (the dep array is evaluated during render, even though the callback is lazy). One component throwing → entire app renders blank → "nothing loads, clicks do nothing." Syntax-check passes because TDZ is a *runtime* error; symptom was masked because the app resumed on the broken step via localStorage. Fix: declare state BEFORE any effect/expr that names it; catch headlessly with the PLAYBOOK render-harness. (2026-07-01)
- ❌ **Polygon orthogonalize (square-to-axis) SPIKES irregular/concave shapes** — forcing every edge to H/V makes consecutive same-orientation edges collapse into ~0°/~180° "spike" vertices or self-intersections. Looked like "tidy worked on zone 1, broke zone 2" (zone 1 was rectangle-ish, zone 2 wasn't). Any geometry "cleanup" must **validate + fall back** so it never worsens input (drop degenerate turns · reject self-intersection · area-ratio guard · else keep the simplified ring). (2026-07-01)

## 🗄️ Legacy-DB → Postgres pipeline + pricing engine (2026-07-01)
- ❌ **Windows `sqlcmd` output is CRLF → breaks Postgres `\copy … FORMAT csv`** with "unquoted newline found in data" (the trailing `\r` corrupts the last field / row boundary). Always `tr -d '\r'` between the gsql pull and the COPY. (Cost one failed load before spotting it.)
- ❌ **Postgres has NO `round(double precision, int)`** — only `round(numeric, int)`. `ROUND(col * %s, 4)` where `%s` is a Python float makes the product `double` → "function round(double precision, integer) does not exist". Cast: `ROUND((col * %s)::numeric, 4)`. (Python-side `round()` before insert sidesteps it.)
- ❌ **browserless `/function` returns an EMPTY 200 unless the module returns `{data, type}`** (a bare object/value serializes to nothing). And `page.waitForTimeout` is gone in newer Puppeteer → `await new Promise(r=>setTimeout(r,ms))`.
- ❌ **Substring checks on human-readable labels collide.** Fallback-tier logic keyed on `"species" in label` wrongly fired for the label `"size (any species)"` (contains "species"), so a species-agnostic aggregate mislabeled itself with a random `MIN(species)`="Acacia". Use explicit boolean flags per tier, never substring-match a display string for control flow.

## 🖥️ ColdFusion / TrimIT dashboards (2026-07-02)
- ⚠️ **Granting a TRIM IT user ALL permissions BREAKS their login** ("stuck on welcome page"). `ActionDefs` **52 Web Portal Access** + **53 Field App Access** are *interface-routing* flags, not power — turning them on for an internal/desktop user misroutes login toward the external portal / mobile app. → to make an admin, **clone a known-good Developer account's `UserActions` + `SecurityLevelID` exactly** (incl. its intentional denials), never "set everything allowed." (jwade access, 2026-07-08.)
- ⚠️ **Reports that gate their row-LIST on `dbo.CompanyYears` "covering today with hours>0" EMPTY OUT at every fiscal-year turnover** — City Budgets showed only 2 cities on Jul 1 because the new-FY hours are 0.0 (not entered yet), and the `GETDATE()` gate also made the FY dropdown inert (wouldn't roll back). → drive the list off the **contract budget for the VIEWED fiscal year** (approved non-Homeowner contract whose year-label matches the selected/resolved FY), and show a row only if it has budget/activity for that label. Stable across turnover + follows the picker. (ship-log #108, 2026-07-08; a *recurring seasonal* bug class — check any report keyed on `CompanyYears`+today.)
- ❌ **`NumberFormat(v,'___,___')` emits LEADING SPACES for small numbers** ("$   135"), and in a narrow HTML table cell those spaces are line-break points → the `$` wraps ONTO ITS OWN LINE ABOVE the number (Skipper reported it on a TPH column). For HTML money use `NumberFormat(v,',')` → clean "$135"/"$196,343" (comma grouping, no width padding). Belt-and-suspenders: also `white-space:nowrap` the numeric cells. The `'___,___'` mask is for monospaced/`<pre>` alignment, not HTML.
- ❌ **`crew/gemini-ask.py` arg-handling is broken** — it sends `argv[1]` as the prompt (so a file path or a `max_tokens` digit becomes the literal prompt) instead of reading the file / stdin like the template. **Call it via pure stdin with NO positional arg:** `cat prompt.txt | python3 gemini-ask.py`. (GLM/Kimi honor the `file [max_tokens]` convention; Gemini doesn't.)

## 🌙 Overnight Herman KB build (2026-07-02)
- ❌ **`cmd &` background jobs in a Bash tool call get KILLED when the tool returns** — launched 3 crew asks with `&`; only the one that finished before the shell exited survived, the other two were orphaned/killed (0 bytes, no done-flag). Run crew/long jobs FOREGROUND (blocking) or use the Bash tool's `run_in_background`, never bare `&` in a call that returns immediately.
- ❌ **A crew *read-through* review missed a SQL bug the crew's own FIX introduced** — the fix aliased a column `AS Current` (a T-SQL reserved word) → syntax error. Caught only because I RAN every recipe against the live DB. Lesson: for a query playbook, EXECUTE the SQL — don't trust prose review. `Current`/reserved words need `[brackets]`.
- ✅ **`kimi-ask.py` long-prompt timeout FIXED + Kimi K3 is now a first-class crew member (Skipper 2026-07-21, upgraded K3 sub → "lean on it hard").** The old SIGKILL/exit-143 on big bundles was the *caller's* timeout killing a slow model, NOT a Kimi limit. Fix: script timeouts raised 300→**900s** (`KIMI_TIMEOUT` in arbor-core + MuniBot `kimi-ask.py` + `kimi-worker.py`); and **invoke Kimi in the BACKGROUND** (or with the max Bash timeout 600000ms) so slow ≠ killed. K3 = sol-level quality → **lean on it as a crew judge for reviews; don't skip it for slowness — give it time.** (supersedes the old "keep Kimi for short prompts" note)

## 🔐 HermanRO DB access broke nightly, silently (2026-07-03)
- ❌ **`sqlcmd` without `-b` exits 0 even when the SQL errors** — so a Windows scheduled task (or any wrapper checking exit code) shows "success/Result 0" while its `CREATE USER`/`ALTER ROLE` fails every run. The re-grant job "ran fine" for days while Boss Herman's DB user stayed missing. ALWAYS pass `-b` on any sqlcmd that mutates, and log its output — exit code alone lies.
- ❌ **`NT AUTHORITY\SYSTEM` is NOT a SQL sysadmin by default** — a schtask running `sqlcmd -E` as SYSTEM couldn't `CREATE USER`. Don't assume SYSTEM = DB admin. Fix without escalating the box: drive the grant from a host that already has a sysadmin login (we run it from gilligan as Administrator on a cron).
- 🔁 **Restoring a DB from another instance ORPHANS/wipes its db-level users** (server logins survive, database users don't). Anything granted at the *database* level (users, role membership) must be re-applied AFTER every restore. Poll/re-grant on a schedule tighter than the restore cadence, and prove it by simulating the wipe (DROP USER → verify broken → re-grant → verify fixed).

## 🌐 Web research helper bugs (2026-07-03)
- ❌ **`curl … | python3 - <<'PY' … sys.stdin.read() … PY` reads NOTHING** — the heredoc IS stdin, so it becomes the *script* and the piped data never reaches `sys.stdin`. Symptom: helper silently returns empty. Fix: put the parser in a **standalone `.py` file** (`curl … | python3 parser.py args`) or use `python3 -c`. Bit websearch/webfetch/ollama-ask all at once.
- ❌ **Keyless DuckDuckGo scraping throttles fast** — first query works, then the IP is blocked (0 results). Fine for a one-off, NOT for a process. Use a keyed search API (Brave free tier 2000/mo) for anything Boss Herman runs regularly. Reader can stay free (local Ollama llama3.2:3b — proven to summarize real fetched pages well).

## 🤖 Don't run high-volume intake INSIDE the agent's live conversation (2026-07-03)
- ❌ **The Hermes email gateway adapter is a two-way email *assistant*** — it feeds inbound mail into the agent's ONE live conversation and auto-replies to the sender. A 6-email burst tangled the requests ("all three drafted"), spam-looped on our OWN test inbox (verified: 0 real-customer addresses ever), and JAMMED Boss Herman (required a restart). A skill-level "delegate + report to Telegram" rewire could NOT override the adapter's structural behavior.
- ✅ **Fix = a standalone background service** (`rfp-watcher.py` cron + `rfp-intake.py`) that uses the agent's TOOLS/crew but runs ONE isolated OS process per email (parallel-safe), reports to Telegram via the bot API, never touches the customer thread or the agent's live chat. Deterministic pipeline > agent-conversation for volume; keep the agent free for judgment + chat.
- ⚠️ **Rapid docker restarts** of a Telegram-bot gateway → Telegram takes ~1 min to reconnect (getUpdates/DoH dance); don't panic, wait it out. And restarting CLEARS the agent's live session (in-flight chat context lost; skills/memory persist).

## 🌳 Field-App map blank / TrimIT REST + diagnosis discipline (2026-07-04)
- ❌ **Taffy REST routes live in ColdFusion APPLICATION scope** — a CF app restart (or `?reinit` on another page) can leave a route UNregistered → `GSTS/API/index.cfm/rest/<route>/` returns **404 HTML**, `apiCall.cfm`'s cfhttp POST gets HTML not JSON → `DeserializeJSON` throws → 0 inventory → **blank field-app map (null-island)**. Not a Maps/Google bug, not a broken IIS connector. **Fix: hit the API app with `?reinit=true` to re-register routes** (verified the POST then returns 200 + real JSON). Optional hardening: guard `apiCall.cfm`'s DeserializeJSON to fail gracefully. (2026-07-04)
- ⚠️ **Don't ship a defensive "guard" as THE fix before confirming root cause on the LIVE runtime** — I mis-diagnosed the blank map TWICE (first "Google retired Maps v3.64 → DrawingManager throws", then "broken CF↔IIS connector 404") and deployed a harmless guard as if it were the fix, before browser-Claude's live console showed BOTH were wrong. `view.sh` can't run JS → use the headless browser / browser-Claude console to see the ACTUAL runtime error before declaring a fix. (2026-07-04)
- ⚠️ **Verify dev-handoff PROD state before surfacing it as a to-do** — several "pending" dev items were already deployed (Anaheim GenerateContractPeriod #71, Long Beach 26/27, Contract Dashboard #11). Play mirrors prod → check the live proc/data first (the fix present? the number rolling up?) instead of re-flagging phantom work. (2026-07-04)
- ❌ **A project's trees are NOT reliably found by `InventoryDetail.ProjectID`** (unset/NULL on GPS-imported rows) — I queried it, got 0, and wrongly concluded "trees wiped / refresh ate them / publish gate missing." **The rows were there the whole time.** → Resolve a project's inventory via the **join `Projects.LocationID → InventoryDetail.LocationID`** (e.g. Eastvale proj 1105467 → LocationID 1285096 → 313 trees). Confirm before declaring anything "missing." (2026-07-24)
- ⚠️ **GPS "publish" = TWO steps; the GeoJSON generator alone isn't publish.** apiCall.cfm generating the grey `shaded_dot` layer does NOT remove blue new-plot dots — the blue layer is gated `ISNULL(IsNewPlot,0)=1`, so you get blue+grey BOTH until a separate `UPDATE InventoryDetail SET IsNewPlot=NULL WHERE LocationID=<loc> AND IsNewPlot=1` runs. That clear can happen via direct SQL (won't show in webroot .cfm files) — verify by checking `IsNewPlot` state in the DB, not by grepping files. (2026-07-24)
- ⚠️ **`GSTSID` (the card's "tree id") is PER-SITE, not globally unique** — the same value exists as GSTSID on thousands of trees across jobs (customer tag 2043 → 10,365 hits). It's the local tree number. So populating it from a job's survey tags (`LegacyRef`) is correct, not a collision risk. GPS import can leave GSTSID NULL → card shows "0"; copy `LegacyRef`→`GSTSID`. Numeric `Height` can also import NULL while the `HeightRange` bucket is set. (2026-07-24)
- ❌ **`dev.greatscotttreeservice.com` ≠ `play.greatscotttreeservice.com` — THREE separate hosts, and I assessed a vendor's work from the wrong one.** play=**173.208.162.142** (our sandbox, SSH via tailnet 100.86.97.46) · dev=**198.207.148.188** (the VENDOR's build box — no SSH, but **HTTP-reachable**) · www/prod=198.207.148.169. **The trap:** the play box's webroot folder is *named* `D:\home\dev.greatscotttreeservice.com\wwwroot\` so "dev" appears in our own paths while meaning PLAY. I searched play, found nothing since April, and told the Skipper the vendor had produced almost nothing — **wrong, and it could have blown up a vendor confrontation.** The vendor's actual Map Setup page is a substantial build (boundary/vertex editing, artwork-overlay canvas, 3 data APIs). → **Before judging where work does/doesn't exist, resolve the hostname** (`getent hosts …`) and confirm which box serves the page (404 on one + renders on the other = different hosts). **Never infer absence of work from a filesystem you haven't confirmed is the right one.** (2026-07-24)
- ❌ **A `CreatedByID`/`ModifiedBy` audit field that resolves to a real NAME can still be a SERVICE ACCOUNT — never infer "person X does this work" from it.** All 3,033 invoices/yr (50,233 since 2006, incl. this week) stamp `CreatedByID=11` = "Rosanna Baez" — who **left 4 years ago**; account `Inactive`, `Role001="Generate Invoices"` (a *function*, not a title), and **exactly 1 distinct creator every year 2019-2026**. I nearly reported "key-person risk: one person does all billing" — wrong; the routine hard-stamps the ID. → **Tells:** 100% concentration, a role name that's a function, an inactive/departed account, a range spanning decades. Check account status + distinct-creators-per-year **before** attributing work to a human. The real finding was better anyway: **no audit trail on $21.5M/yr of billing.** (2026-07-24)
- ❌ **NEVER derive an org fact (headcount, team size, "who does X") from a distinct-ID count — check it with the human.** `COUNT(DISTINCT CreatedByID)` on bid records returned **24**; the real bid desk is **8** (Skipper). The 24 swept in *former employees* still attached to records in the trailing window (7 flagged `Inactive`), *other functions* that touch a record but don't do the job (production/municipal staff, coordinators, IQC, oversight), and *noise* (8 IDs with 1–7 records, plus NULL and 0). → Join to the user table for **active status + role**, look for the volume cliff, and **confirm with someone who knows the org before publishing**. Same class as inflating "200 fixes" from 77 log files that were mostly iterations — **both caught by the Skipper, not by me.** Better stat anyway: express as **load per person** (≈9 duplicate entries/person/working day), which doesn't depend on a headcount claim. (2026-07-24)
- ❌ **Verify against the ACTUAL DB key set, not a sampled range.** Verifying Herman's Eastvale address fix, I read the sheet for `site_id` 2043–2360 and reported the street split as "Goodman 255 / Bellgrave 30" — the real tag set runs to 2937, so my window was truncated and the true split (Bellgrave 201 / Goodman 112) looked like a Herman error when it was MINE. → pull the real tag/ID list from the DB first, then join; never eyeball a range. (Verification still worked — it caught my own mistake.) (2026-07-24)
- ⚠️ **`InventoryGPSModel` is a GLOBAL staging table — orphaned rows from ANOTHER job sit there and will contaminate the next import.** Found 197 rows tagged ProjectID 1094998 ("USC Contract", LA coords) unrelated to Goodman/Eastvale — the summary-blast reads staging globally. → before ANY GPS import, `SELECT ProjectID, COUNT(*) FROM InventoryGPSModel GROUP BY ProjectID`; isolate/back-up-and-remove foreign rows first (never blind-delete — shared box, could be someone's active load). (2026-07-24)

## 🗄️ Postgres cast + play-DB account survival (2026-07-04/05)
- ❌ **`coalesce(int_col,'')` throws `invalid input syntax for type integer: ""`** — an empty-string default can't cast to the column's INTEGER type. In `georef_map.detect_trees` this Postgres error was SWALLOWED and surfaced as "detection returns 0 trees" (looked like a vision failure). Cast the column `::text` for the match, and don't let DB exceptions be swallowed into a bogus empty result. (2026-07-05)
- ❌ **Play-only DB *application-account rows* get WIPED by every prod→play restore** (distinct from db-user/grant orphaning) — the `gilligan-bot` login (UserID 376) vanished on restore, so `trimit-fetch.sh` got no session cookie and every fetch bounced to ClientLogin (recurred 6/6 & 7/4). **Fix: self-heal cron** (`ensure-gilligan-bot.sh` every 3h) re-creates it if missing, at a **high/synthetic UserID (90376)** so it never collides with a real employee; the ensure-log doubles as a restore-wipe monitor. ⚠️ also `flow.Users.Password` is **varchar(50)** → a >50-char secret silently NEVER matches. (2026-07-04)

- **[arbor-core/leaflet]** circleMarker/path clicks BUBBLE to the map by default → a pin click also fires `map.on("click")`. If the map click drops/creates something (stamp mode), clicking an existing marker silently creates a duplicate. Fix: `bubblingMouseEvents:false` on the marker (Leaflet 1.0+). (2026-07-05, inventory tree-pin dup bug, commit 6063e40)

- **[arbor-core/leaflet]** A bad/degenerate bounds passed to `fitBounds`/`imageOverlay` corner-jams the whole map (tiles freeze top-left). ALWAYS validate bounds (finite, in lat/lng range, non-degenerate) before use, and call `invalidateSize()` after any async op that may have changed the container. (2026-07-06, extract-areas map-jam, commit)

- **[arbor-core/leaflet]** "Map collapses to top-left corner" after an async action = STALE container size, NOT a view pan. Banners/buttons above the map reflow the layout while Leaflet keeps its old cached size → panes paint at 0,0. Chasing fitBounds/setView = wrong tree. FIX: ResizeObserver on the map container → invalidateSize() (rAF-debounced) on any resize, + invalidateSize on the state toggle that triggers the reflow. (2026-07-06, Extract-Trees corner-jam, Codex root-caused, commit cde0f79)

- **[arbor-core/preact+leaflet]** A conditionally-rendered sibling (`${note && <div>}`) inserted ABOVE a ref-held Leaflet map div makes preact RECREATE the map div (container→0×0, map orphaned, jams to corner). Symptom looks like a framing/invalidateSize bug but is DOM destruction. FIX: render such banners ALWAYS (display:none when empty) so the map div is never reconciled away. Reproduce via headless localStorage session inject + measure .leaflet-container size. (2026-07-06, Extract-Trees corner-jam REAL fix, commit 8994c10)

- **[terminology] TPH = "Throughput per Hour" (dollars of production per crew hour), NOT "Trees Per Hour."** Skipper corrected 2026-07-08. The error was seeded in the shared trimit-knowledge vault (START-HERE, production-tph, WorkOrders) → would have propagated to Hermes and into the Scott demo. Fixed at source. Lesson: verify acronym expansions with the Skipper before baking into knowledge; a wrong term in the vault infects every downstream answer.

- **[infra] Shared fixed remote filenames = a time-bomb.** Herman's DB bridge scp'd every query to one fixed name `herman_q.sql`; a single hung `sqlcmd` holding it open locked out ALL future queries ("scp: dest open Failure"), and `set -e`+`2>/dev/null` masked it as "exit 1, no output." Fix pattern: unique per-invocation temp names + a query timeout so nothing hangs + self-cleanup + don't let a filter-grep's empty exit fail the script. Check sibling scripts for the same fixed-name pattern.

- **[workflow/deep-research]** The `deep-research` workflow RESUME is broken: `Workflow({scriptPath, resumeFromRunId})` does NOT re-pass `args` (the research question) → the resumed run dies instantly with "No research question provided." Also seen: the workflow can stall on the FINAL synthesis agent (e.g. 99 started / 98 returned), leaving a 0-byte result + no completion event. FIX/PATTERN: don't rely on resume — recover findings directly from the run journal (`subagents/workflows/<wf>/journal.jsonl`): grep `"claim":"..."` for the verified claims (they carry evidence + refuted:false) and synthesize by hand. (2026-07-08, IE-expansion Round-2)

- **[rigor/reconciliation]** Stated an assumption as FACT and skipped a source doc we already had. Claimed Brent's City-Budgets numbers came from "QuickBooks" (inferred from report format) and built a whole reconciliation + sign-off on it — when Brent's own SOP (`budget-report/PROCESS.md` + his Jun-16 email) said he pulls from **TRIM IT**. Two failures: (1) labeled an inference as verified; (2) didn't re-read the authoritative SOP BEFORE analyzing. RULE: before reconciling/analyzing against an external party's report, FIND + READ their SOP/source-of-truth doc first (search the project files — we usually already have it), and mark every source claim VERIFIED vs INFERRED. The Skipper caught both. (2026-07-09, RC-03 City Budgets)

- **[infra] gsql.sh has the SAME fixed-remote-temp flaw the herman_q.sql lesson warned about.** `gsql.sh` scp's every query to one fixed name `gsql_tmp.sql` on the play box → **concurrent** gsql calls clobber each other's SQL mid-flight → empty/garbled output (a model misreads as "0 rows" or a silent fail). Hit this hard 2026-07-10 (Steve Fix-2) by firing several gsql calls that the harness auto-backgrounded in parallel. FIX pattern: run gsql **one at a time** (wait for each), OR use a unique-per-call remote temp (`gsql_$$_$RANDOM.sql` + self-delete) — see the `/tmp/gsql1.sh` one-shot. The earlier lesson literally said "check sibling scripts for the same pattern" — gsql.sh was that sibling. Still not permanently fixed in gsql.sh itself.
- **[infra] The gsql.sh fixed-temp time-bomb detonated AGAIN 2026-07-15** — this time the HUNG-process variant, not just concurrency. A leftover process on the play box held `gsql_tmp.sql` open → `scp: dest open "gsql_tmp.sql": Failure` AND `del` failed ("used by another process"; attrib = `A`, not read-only). Because gsql.sh does `scp ... 2>/dev/null`, the write failure is SILENT → every subsequent query ran the STALE leftover file (an old aspen retention-scoreboard query) and returned tier-distribution output. This broke the automated **play-refresh-monitor** (ERROR: read "Changed database context to 'Workbench'" instead of a backup vintage) and silently poisons EVERY gsql.sh caller until the handle is released. Couldn't clear the lock safely (would need to kill the holding process / reboot the play box — didn't do unilaterally). PERMANENT FIX now overdue: (a) unique per-invocation remote temp `gsql_$$_$RANDOM.sql` + self-delete, (b) **fail loud on scp non-zero exit** (drop the `2>/dev/null` swallow), (c) short query timeout so nothing hangs holding the file. Third time this exact pattern has bitten (herman_q.sql → gsql concurrency → this).
- **[rigor/db] Validate a bulk transform's SCOPE with a read-only SELECT BEFORE materializing.** The Fix-2 override map, run as a SELECT first, flagged **116k overrides (would move 35k of an active rep's OWN proposals)** — the "snapshot" source turned out to be project-account-owner, not proposal-seller. Materializing blind would've corrupted the report. RULE: for any override/reattribution/mass-update, first `SELECT COUNT + a by-entity breakdown` and sanity-check the magnitude against expectation ("minimal-touch" should be hundreds, not 100k); only `CREATE TABLE ... INTO` / UPDATE after the numbers make sense. (2026-07-10)
- **[verify] Render-verify EVERY served table, not just the primary query.** Fix 1 fixed the Win/Loss *summary* but a *second* per-proposal detail table on the same page had its own copy of the roster gate → the served page still showed the wrong reps (98 detail rows). The `curl` render-check caught it (summary right, detail wrong). RULE: a page can have N queries with the same bug; grep the SERVED HTML for the specific symptom across the whole page, and enumerate `<cfquery>` blocks before declaring a query-level fix complete. (2026-07-10)

- **[paths/startup]** Project work lives at `/home/wade3337/arbor-stack/` — memory/RC pointers like `arbor-stack/...` are relative to **`/home/wade3337/`**, NOT the OpenClaw workspace (`~/.openclaw/workspace/`, which holds only wiki/memory/reference/contracts/skills). At session start, `cd /home/wade3337` for project files. (Cost ~5 tool calls 2026-07-10.)

- **[infra/CRITICAL] Dual-webroot shadow: CF serves `C:\ColdFusion2023\cfusion\wwwroot\GSTS\` OVER the IIS path `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\`.** If a file exists in the C:\ shadow, CF serves THAT (often stale) and silently ignores your D:\ edit; if it does NOT exist in C:\, CF falls back to D:\ (so NEW files work from D:\, which misleads you). Burned ~40 min 2026-07-11 chasing "trusted cache / IIS output cache / CF restart" — none of it; the served file was a 6/27 C:\ shadow. **RULE: when a `.cfm` EDIT doesn't show in served output but a NEW file would, FIRST `Get-ChildItem C:\ -Recurse -Filter <name> -EA SilentlyContinue` (plain `where /r C:\` aborts on a permission error and lies "not found"). If shadowed, deploy to BOTH webroots (back up the C:\ copy first).** 8 GSTS .cfm are shadowed (RevenuePerformance, CustomerLeads, SalesPipeline, 2 Exec, Export-CustomerList, 2 Diag) — any D:\-only edit to those never went live. Audit pending. (2026-07-11)
- **[llm/small-model] Do NOT hand a 3B model raw JSON or let it compare/compute.** llama3.2:3b fed the dashboard's JSON blob garbled numbers ($655K→"$1,655,561"), fixated on one field for every question, and flipped comparisons ("$179 is BELOW $130"). FIX: in ColdFusion pre-select the intent-relevant fields, pre-format money (`"$"&numberFormat(round(n),",")`), and **pre-compute every verdict** (on-pace yes/no+gap, TPH above/below) — hand the model a tiny labelled fact list and tell it to only quote, never calculate. Numbers went exact. (2026-07-11)
- **[llm/small-model] Ollama's default temperature is 0.8 → set `options.temperature=0` for any factual/grounded task**, or the model paraphrases numbers into fiction. AND: a 3B model will **override injected facts with its own (wrong) world-reasoning** — ask "TPH last month", give it facts labelled "June 2026", and it still writes "May 2026: $124.21" because it "reasons" what last month is. Even temp 0. FIX: never let the model reason about dates/periods OR emit the number — **compute the period + compose the exact answer sentence in CF**, let the model only REWORD, and **accept the reword only if it preserves the primary number AND names no month other than the period's own; else serve the CF sentence.** (2026-07-11)
- **[coldfusion/CRITICAL] In a CFML string, `##` is an ESCAPED literal `#`.** So `"## "` at runtime is `"# "` (2 chars, one hash), and `left(ln,3) EQ "## "` NEVER matches a real markdown `## ` heading. Cost ~30 min on the Arbor Helper KB parser (bodies came back empty). **To match a two-hash `## `, write `"#### "` in the source** (four hashes = two literal hashes). A crew review caught it independently. Same class as the earlier BOM/emoji issue. (2026-07-11)
- **[coldfusion] Two more CF gotchas hit the same night:** (1) `var` is ILLEGAL outside a `function{}` — in a top-level `<cfscript>` template body, declare plain (no `var`), only use `var` inside UDFs. (2) `.cfm` files with emoji/non-ASCII render as mojibake unless you add `<cfprocessingdirective pageencoding="utf-8">` as the FIRST line (or a UTF-8 BOM). (3) `find(chr(10) & "## ", raw)` was unreliable for splitting — a line-loop (`for ln in listToArray(raw,chr(10),true)`) is robust. (2026-07-11)

- **[llm/router] A word that means two things will mis-route — test BOTH senses after touching an ambiguous trigger.** Arbor Helper: "value" = company *values* (ideals) AND dollar *value* (money). Broadening the company-identity trigger to catch "what does the company value" risked stealing "how much value is in open bids". FIX: add the ideological sense narrowly (`\bvalues\b` plural + "company value"/"we value"), NOT bare singular `value`; then verify the money side still routes to figures ("open bids" → $40.9M ✓) as well as the fixed miss. RULE: whenever a new keyword overlaps an existing domain term, add a regression for each meaning before declaring done. (2026-07-11)
- **[regex] `\bword\b` does NOT match the plural `words` — a word boundary breaks on the trailing s.** `\bjoke\b` silently failed on "how many tree jokes" (fell to out-of-scope). Use `\bjokes?\b` (or `\bword(s)?\b`) whenever the plural is a natural phrasing. Worth sweeping other `\bword\b` triggers for the same gap. (2026-07-11)
- **[llm/router] Another verb-vs-name collision (like "value"): "chase" = follow-up verb AND rep Chase Schroeder (SalesRepID 1135).** "which accounts should i chase" routes to sales-rep (rep-name match runs before cockpit). By design correct-ish (rep Chase is real). Did NOT add bare "chase" to follow-up detection (would break "how much did Chase sell"). Follow-up uses unambiguous triggers (follow up / need follow-up / need attention / to chase). Same rule as [[value collision]]: test both meanings before adding a keyword. (2026-07-12)
- **[security/coldfusion] A `?param` JSON/grounding branch inherits NO auth — gate it EXPLICITLY.** The `?aisummary=1` branches added to dashboards to feed Arbor Helper emitted financial totals + the raw JSON to ANY caller, even a garbage/forged `ZUserID` cookie (a real `flow.Users` row was never required) = broken access control. Crew caught it during the deploy-package review, not in normal use. FIX: a shared `dashboard-auth-gate.cfm` `<cfinclude>`d at the TOP (before ANY output) of every financial/JSON-emitting surface (20 files) that resolves the cookie to a real authorized `flow.Users` row and **403s HTML *and* JSON** otherwise. RULE: any new `?param` data/JSON branch = add the gate in the SAME commit; also `</`-escape any `SerializeJSON`-into-`<script>` (Cockpit XSS). (2026-07-12)
- **[coldfusion] Two exec-gate traps (2026-07-11):** (1) CFML **`gt`/`gte`/`lt`/`is` are reserved operators, not legal variable names** — a var named `gt` (holding a title) errored; renamed `gtitle`. Same class as the T-SQL `Current` reserved-word trap. (2) **Gate on the login ALLOWLIST, NOT `flow.Users.Title`** — jwade's title is a stale "Fleet Manager" (never updated to COO), so a title-based exec gate locks the Skipper out; login validation doesn't read Title/SecurityLevel anyway. Use an explicit authorized login/UserID set, fail-closed to leadership. (2026-07-11)
- **[security/coldfusion] Gating "the dashboards" ≠ gating the DRILL CHILDREN — enumerate every reachable page, not just the tabs.** The Jul-11 crew audit gated 20 leak surfaces but MISSED `Executive$Sales$Detail.cfm` + `Executive$ClosePercentage$Detail.cfm` — the per-record detail pages the market/rep tabs drill INTO. Both returned 200 (full per-invoice/proposal financials) to a garbage `ZUserID` cookie, and `Sales$Detail` wasn't even in the repo (live on prod, untracked). Found only when a new feature promoted the drill to a first-class path. RULE: when gating a dashboard, walk the ENTIRE click-path (tab → bucket → segment → detail → export) and gate each leaf; a page not in your repo/`deploy-manifest` is a page you didn't audit. `curl --cookie ZUserID=99999999 <page>` every drill target, expect 403. (2026-07-12)
- **[coldfusion/mobile] Two protip systems in TRIM IT — check WHICH one a page uses before assuming a mobile fix applies everywhere.** The 5 newer dashboards use the shared `protips-include.cfm` (`.gsts-protip-badge`, shows on `(pointer:coarse),(max-width:640px)`). **Revenue Performance uses a SEPARATE legacy `data-tip`/`.tip-badge` system** (inline in the page, `css/gsts-protips.css`) whose badge showed only under `@media (hover:none)` — so on a laptop in device-mode (reports `hover:hover`) the "?" badges vanished at phone width. Fix = align the media query to `(hover:none),(pointer:coarse),(max-width:640px)`. RULE: a mobile/protip fix on the shared CSS does NOT touch Revenue; grep the page for `assets/protips/protips` vs `data-tip` to know which system you're in. `@media (hover:none)` alone is a mobile-visibility trap — always OR it with `(max-width:…)` so device-mode/narrow-desktop works too. (2026-07-12)
- **[coldfusion/dual-webroot] A C:\-shadowed page's `<cfinclude>` targets must ALSO live in C:\ — or the page 500s.** Adding `<cfinclude template="protips-include.cfm">` to `Dashboard-RevenuePerformance.cfm` (which is served from the C:\ shadow) 500'd with "Could not find the included template" because protips-include.cfm was only in D:\. Static assets it pulls are URL-fetched (resolve fine from the IIS root), but a SERVER-SIDE cfinclude needs the file physically in the same webroot CF serves the page from. RULE: when you add a `<cfinclude>`/`<cfmodule>` to a shadowed page, deploy the included file to BOTH webroots too, and grep the CF `application.log` (`Could not find the included template`) the moment a page returns a tiny error body. Confirmed on play: Revenue is C:\-served. (2026-07-12)
- **[js/mobile] The shared protip badge's JS injector and its CSS display rule must agree — they didn't.** `protips.js` injected `.gsts-protip-badge` only on true touch (`(hover:none) and (pointer:coarse)`), but `protips.css` displays it on `(pointer:coarse), (max-width:640px)`. Result: a laptop in device-mode (narrow width, hovering pointer) got NO badge (JS never made it) AND no hover-reveal (device-mode suppresses hover) — the Skipper saw nothing on his "computer mobile view" while a real phone worked. Fix = gate the JS injection on the SAME condition as the CSS: `(pointer:coarse) OR (max-width:640px)`. RULE: whenever a JS "should I create this element" check and a CSS "should I show it" media query both exist, they must use identical conditions, or you get an invisible-but-present (or absent-but-styled) element. (2026-07-13)
- **[coldfusion] Inside `<cfoutput>`, ANY bare `#` is a variable delimiter — hex colors AND numeric HTML entities break the compile.** Adding `style="border-bottom:1px dotted #94a3b8"` and `&#9432;` (ⓘ) inside SPM note blocks (which sit in cfoutput) both errored ("`94a3b8` is not a valid identifier" / "invalid construct near `;`"). Fixes: use a color NAME or `rgb()` (no `#`), and paste the LITERAL unicode char (ⓘ, →, ÷) instead of `&#NNNN;` entities — or double them (`##94a3b8`, `&##9432;`). Safest for injected HTML: literal chars + no-`#` colors, so it works whether or not the surrounding block is a cfoutput. (2026-07-13)
- **[js] Adding a stage/enum value = update EVERY hardcoded list of it, and "0 CF errors" does NOT prove the browser renders.** Splitting the Cockpit's "Working" into Working+Scheduled, I updated `STAGES`, `stageOf()`, `stageColor()`, the column HTML, welcome copy — but MISSED the client-side `buckets={followup:[],bidding:[],won:[],working:[],done:[]}` initializer. `stageOf` returned `'scheduled'` → `buckets['scheduled'].push()` threw a `TypeError` → the throw aborted the entire `render()` loop → EVERY column drew empty. Server rendered fine (0 CF errors, aisummary correct, 740 cards in the inline `CARDS` array) — it was a pure browser-JS throw, invisible to server-side checks. The Skipper caught the blank board. RULES: (1) when adding an enum member, grep for every literal that enumerates the old set (arrays, object-key initializers, switch/ternary chains) — not just the obvious `STAGES` list. (2) A dashboard is NOT verified by "0 CF errors" — the cards render CLIENT-side; extract the rendered inline JS and either `node`-execute the render path over the real data or screenshot the populated board. My earlier "verified" screenshot showed 0-count columns and I wrongly blamed a missing cookie — the empty board WAS the bug. (2026-07-13)
- **[security/coldfusion] A gate `<cfinclude>` inside a `<cfcase>` only guards THAT branch — put it before the `<cfswitch>`.** `SalesProductionMeeting$Drill.cfm` (a drill page reachable from every SPM tile) had `<cfinclude template="dashboard-auth-gate.cfm">` stranded inside the `soldApproved` case, so `metric=soldApproved` returned 403 to a garbage cookie but `metric=pipeWon/prodCompleted/soldBacklog/resSold` returned 200 + real customer/$ data. A spot-check that only tested the page with no `metric` (or the one gated metric) SEES 403 and declares it safe — the leak hides in the other branches. RULES: (1) the auth gate must run BEFORE any branching/query/output — top of file, before `<cfswitch>`/`<cfif>` — never inside a case. (2) When a page's behavior forks on a URL param (`metric`, `view`, `tab`, `mode`), security-test EVERY value of that param with a garbage cookie, not just the default. (3) grep new/edited gated pages for the include line's column position — a gate line with leading whitespace is a smell it's nested inside a block. (2026-07-13, caught by the pre-ship crew audit)

- [trimit/rgc/sql] A "pipeline outside coverage" bucket must exclude BOTH future-scheduled AND already-started/produced work — not just future. RGC's approved-unscheduled filter only excluded future ACTIVE(46) WOs, so in-progress/completed jobs (and jobs whose WOs left status 46) leaked in ($1.09M). In TRIM IT a WO is created at approval with NULL StartDate; "unscheduled" = no dated WO + no crew sheet, NOT "no WO". Test a bucket by joining every item back to WorkOrders/CrewSheets and counting how many have started work.

- [crew/judge/db] glm-judge's DB tool loop runs queries via gsql.sh which defaults to `-d GSTS`. Objects in the Workbench side-DB (rgc.*) were invisible → the judge wandered the wrong DB and never reached a verdict. FIX: `JUDGE_DB` env prepends `USE [db];` to each query (+ tells GLM which DB it's in). Run RGC/Workbench reviews with `JUDGE_DB=Workbench python3 glm-judge.py`. Always tell a DB-tool judge which database its objects live in.

- [agents/vault-guard] A BLACK-leak guard that greps the bare word `\bBLACK\b` false-positives on legit prose ("black-box match") AND on a vault's OWN guardrail text ("no Track-2/BLACK") — it quarantined Aspen's start-here README. FIX: match PRECISE Track-2 markers only — `arbor-core|arbor-knowledge|arbor-strategy|BLACK vault` + real secret regexes (`ghp_[A-Za-z0-9]{20}`, `ssh-ed25519 AAAA`, `-----BEGIN … PRIVATE KEY`). Applied to refresh-aspen-vault.sh + refresh-munibot-vaults.sh.

- [agents/vault-guard] ⚠️ **The line above was WRONG for 2 weeks — the fix was NEVER applied to `refresh-munibot-vaults.sh`.** It kept the loose pattern and quarantined MuniBot's own `00-start-here/README.md` + `concepts/vault-maintenance-system.md` **every hour since 2026-07-15** (639 log events). Self-sustaining loop: guard moves file → commits+pushes → `docker cp` puts `_quarantine/` in the container → next run's `rsync --ignore-existing` restores it → quarantined again. FIXED 2026-07-29: precise markers + a `vault-guard: policy-doc` HTML-comment exemption for docs that must NAME the boundary, and cleared the container's stale `_quarantine` copies (otherwise the rsync resurrects them). **Two lessons: (1) "filed ≠ applied" — a LESSONS entry saying a fix was applied to N scripts is a CLAIM, verify each one; (2) test a guard with a POSITIVE control (plant a fake key, confirm it still catches) — a guard tuned to stop false-positives can go silently blind.**

- [security/creds] The GitHub PAT was embedded in 9 `.git/config` remote URLs, incl. **4 inside the MuniBot/Hermes containers** (`docker cp "$V"/.` copies `.git/` too) — so agents held an account-wide write token. FIX: strip to clean `https://github.com/...` URLs and let the global helper (`store --file=~/backups/.git-credentials`, 600) supply it — one file to rotate. Container copies self-heal on the next sync once the host remote is clean. **When grepping for a leaked token, grep the ACTUAL token string, not `ghp_` — 5 of my 16 "hits" were the leak-guards' own detection patterns.**

- [infra/git] Every vault-sync script ends its git calls with `|| true` so cron never alerts — which meant **`arbor-knowledge` sat 14 commits ahead / 1 behind since 2026-07-15 and NOBODY KNEW**. Two weeks of Boss Herman's Track-2 work existed only on this box: push was rejected non-fast-forward every 2h and the error was swallowed. Found only because a post-change sync audit compared `rev-list --left-right --count HEAD...@{u}` per repo. **`exit=0` from a script full of `|| true` proves NOTHING — verify the end state (in-sync with origin), not the exit code.** FIXED — `~/backups/git-health-check.sh` + OpenClaw cron `git-health-check` (daily 07:15 PT → Discord).
- [infra/git] **Two clones on one box pushing to ONE shared origin = guaranteed divergence + a MISLEADING alert.** The `.gilligan-hermes/home/workspace` Hermes clone shared the primary's `gilligan-workspace` origin → it went 1-ahead/42-behind, stranded a commit, and `git-health-check` reported it under the bare basename **`workspace`** so it looked like the PRIMARY was stranded (it was 0/0). Two fixes: (1) the monitor now prints `$ROOT`-relative paths so a secondary clone can't impersonate the primary; (2) a live independent clone needs its OWN remote — repointed to `gilligan-hermes-workspace` + a dedicated guarded push timer that REFUSES to run unless origin is the dedicated repo. **Lesson: verify WHICH clone an alert names before believing the primary is at risk; never let two working copies share one origin.** (2026-08-01)

- [security/creds] **I declared "zero tokens on disk" and was WRONG — twice, both times filename blindness.** (1) My sweep used `grep --include="*.sh" --include="*.json"…`, so `~/backups/.gh-token` (no extension, 0600, referenced only inside a 5-minute cron script) survived it and I reported the box clean to the Skipper. (2) Then my cleanup glob `"$B"/*.bak` silently skipped the DOT-prefixed `.hermes_home_*.bak` backups, leaving 4 more copies. **A negative result from a FILTERED search is not evidence of absence — for a secret sweep, scan unfiltered (`--binary-files=without-match`, exclude only huge dirs) and accept that it's slow.** Bash globs never match leading dots; use `find -name` instead.

- [security/creds] **Anything a secret passes THROUGH keeps a copy.** After removing the PAT from 10 live locations, a full unfiltered sweep found 5 remaining copies — all in my own Claude Code session transcripts (`~/.claude/projects/*.jsonl`), because the token appeared in tool output. Harmless once revoked (401), local-only, 0600, not in any repo. But the rule generalises: **a secret is not "removed" until it's REVOKED — deleting files only removes the copies you can find, and transcripts/logs/scrollback are copies you usually can't.** This is also why the Skipper pasting a new PAT into Discord would have been worse than useless: SSH was the right call precisely because the private half never travels. Rotate first, clean up second.

- [security/creds] Credential auth can be silently REINSTATED by a script that hardcodes its own remote. `herman-vault-sync.sh` ran `git remote set-url origin https://…` **every 5 minutes**, so a repo I had converted to SSH was back on token-auth ~15 min later — I only caught it because the new health check flagged the fetch failure. Grep for `set-url` / `URL=https://` before declaring a credential migration done, and re-run the audit AFTER the fast crons have had a cycle, not just immediately after the change.

- [trimit/retention/sql] Never build a churn/service-interval metric on raw crew work-days — a single job spans several consecutive days, so the gaps between distinct WorkDates are ~1 day and the customer's MEDIAN interval collapses to 1 day → 92% of customers falsely flag "at-risk." FIX = **sessionize** work-days into VISITS first (a new visit starts when the gap from the prior day > ~14 days), then measure the interval between visit-starts (came out to a believable ~301-day typical cadence). Caught by spiking the query on play before productionizing — always sanity-check the at-risk RATE (76–92% is a tell something's wrong) and the median interval before shipping. Also: the play DB is a partly-stale snapshot (only 150/381 "Other" custs active in 2023+), so anchor the clock with an @AsOf param and verify on the recently-active cohort, else staleness inflates at-risk. (2026-07-15, Aspen retention scoreboard)

## [trimit/db] "Open/incomplete" state can't be read from DateCompleted/DateApproved NULLs (2026-07-16)
Building the running-dry wire, I used `WorkOrders.DateCompleted IS NULL` = "work still open" and `Proposals.DateApproved IS NULL` = "proposal still open" → both flagged ~everyone (counts like 107, 830 per account). Cause: these completion/approval date columns are **sparsely populated** — lots of long-done work/dead proposals never got the date stamped. Same trap as forward-scheduling (only 100/3007 projects have a future-dated WO). **Rule:** for TRIM IT account-state, don't trust "NULL date = still open"; use **activity RECENCY** (last WorkOrder date via `MAX(COALESCE(DateCompleted,StartDate))`, last ProposalDate/LastGoAheadDate) instead. Cost me ~3 iterations (all-zeros results).

## [confidentiality/agents] Track-1 agent vaults auto-quarantine "arbor-core" mentions (2026-07-16)
Authoring a plan note for Aspen/MuniBot (both Track-1, sync to GitHub+Obsidian), I referenced "arbor-core RFP intake agent" → the refresh script's **BLACK-leak guard quarantined the file** (didn't sync it). Correct behavior — arbor-core is Track-2 BLACK, must never surface in team vaults. **Rule:** when writing plan/roadmap notes for team agents, SCRUB all arbor-core/Track-2 references — describe capabilities generically ("an RFP-intake tool to build"), never name the BLACK project or reveal a BLACK prototype exists. Check `_quarantine/` + refresh.log after syncing.

## [security/ssh] rsync key rejected → SSH password fallback BYPASSES the forced-command restriction (2026-07-16)
MuniBot 200GB drop: cwRsync's ssh rejected the drop key (Windows ACLs too open) → **fell back to password auth**, which SUCCEEDED for wade3337 → the per-key `command="rrsync -wo <warehouse>"` restriction **did NOT apply** (forced commands are key-scoped) → rsync ran unconfined against `~/` (dumped empty cygwin stub dirs; caught before real data). **Fixes:** (client) `icacls <key> /inheritance:r /grant:r "%USERNAME%:R"` so ssh accepts the key, AND force `-o PreferredAuthentications=publickey -o BatchMode=yes -o NumberOfPasswordPrompts=0` so it FAILS SAFE instead of using a password. (server) `PasswordAuthentication no` — a key-only box should never allow password login to bypass a write-only drop. **Rule:** any forced-command/restricted-key drop is only as safe as `PasswordAuthentication no`; verify it. Detection: `journalctl -u ssh | grep "Accepted password"`.

- [trimit/dashboards] **Changing a SHARED data structure broke a sibling page.** Reworked `woByCo` in `CityBudgets.data.cfm` (struct-of-labels → array-of-rows) for the scope-guard fix; `CityBudgetsRenewals.data.cfm` *reuses* that same `woByCo` (per its header comment) and still did `StructKeyExists(woByCo[cid], label)` → runtime error at line 49, blanking the whole Renewals tab. **Lesson:** before changing any structure built in one page and consumed by others, `grep` every consumer FIRST and update them in the same change-set + render-verify EACH sibling. The repair contract's "propagate to sibling pages" applies to shared *data shapes*, not just visual layout.

- [security/ssh] **cwRsync bundles can be STRIPPED — don't depend on `cygpath`/`chmod` in the launcher.** MuniBot local-source drop (v5): the Desktop cwRsync bundle had `rsync.exe`+`ssh.exe`+`cygwin1.dll` but NOT `cygpath.exe`/`chmod.exe`. The `.cmd` used `cygpath -u` to translate the key's Windows path → cygwin form; with cygpath missing that step silently produced an EMPTY string → `ssh -i ''` → "Identity file not accessible" → `Permission denied (publickey)`, transfer never authenticated. **Fix:** translate paths in pure batch (`set DL=%P:~0,1%` + `set REST=%P:~2:\=/%` → `/cygdrive/%DL%%REST%`), lowercase the drive letter, drop the `chmod 600` (rely on `icacls /inheritance:r /grant:r user:R` for cygwin-ssh key perms), and BUNDLE `munibot_drop_key` in the zip so it's guaranteed beside the launcher. **Rule:** a Windows rsync launcher should assume only rsync.exe+ssh.exe+cygwin1.dll exist; never call cygpath/chmod/other cygwin coreutils that may be absent.

- [openclaw/config] **`channels.discord.streaming` must be an OBJECT `{mode:"progress"}`, NOT a bare string.** The docs describe it as "takes `off|partial|block|progress`" which reads like a string value — but the schema requires `streaming:{mode:"progress", progress:{toolProgress:true}}`. Setting `streaming:"progress"` passed `json.load` but the gateway ABORTED on restart (`invalid config: must be object`) and did NOT start — left the gateway DOWN until fixed (rsync/other SSH work was unaffected, separate procs). **Rule:** after ANY openclaw.json edit, run `openclaw config validate` BEFORE `openclaw gateway restart` — validate catches schema errors while the old gateway is still up; restarting blind can strand you with a downed gateway. Green-dot presence = `channels.discord.autoPresence:{enabled:true}` (+ optional `activity`/`activityType:4`); working-animation = `streaming.mode:"progress"`.

- [agents/data-plumbing] **"Reachable" ≠ "knows to look there" — verify the agent's PROMPT points at the data path, not just that the mount exists.** MuniBot 42GB ingest: the warehouse was correctly bind-mounted (`~/.munibot → /opt/data`) so data at `/opt/data/municipal-archive/` was physically visible to the container — but MuniBot's SOUL still named `~/home/municipal-history/` (an empty, git-tracked vault dir) as "my raw source material." As instructed it would have looked in the empty folder and found nothing. **Fix:** point the agent's SOUL + MEMORY at the ACTUAL landing path as an ABSOLUTE path (`/opt/data/municipal-archive/`, not `~/...` — `$HOME` shifts inside containers and `~` can double-path), and note the big on-disk warehouse is deliberately NOT in git/the vault (bulk raw data never rides the sync). **Rule:** after wiring a data mount for an agent, grep its SOUL/memory for the path it's actually told to read, reconcile any mismatch, then verify from INSIDE the container (`docker exec`). Agent picks up SOUL/memory changes on its NEXT session, not the running one.

- [data-transfer/rsync] **rsync copies Windows `.lnk` shortcut STUBS, not the folders they point to.** MuniBot warehouse ingest: 7 cities (Long Beach, Anaheim, Aliso Viejo, Cerritos, Lake Forest, Stanton) existed on the source share as `.lnk` shortcuts to folders elsewhere. rsync faithfully copied the ~2KB shortcut files and moved on — those cities landed EMPTY, easy to mistake for "no data exists." **Rule:** before/after any Windows-source sync, scan the source for `*.lnk` (`find … -iname '*.lnk'`); resolve each shortcut's real target and copy the actual folder (or add its resolved path to the sync). And when a dataset says "no history for X," check X isn't a shortcut that never transferred before reporting real absence. Verify contents from inside the destination, don't assume the copy was complete.

- [docker/compose] **Recreating a compose service without its original env vars silently reverts them to defaults — and can break the container.** Adding a mount to the `hermes` service, I ran `docker compose up -d gateway` without `HERMES_UID/HERMES_GID` set; the compose default `${HERMES_UID:-10000}` kicked in (original was 1000, captured only in my pre-change `docker inspect` snapshot, NOT in any `.env`). The boot wrapper chowned `/opt/data` to 10000 and the gateway hit `PermissionError` everywhere → Boss Herman down. **Fix:** recreate with the original values (`HERMES_UID=1000 HERMES_GID=1000 docker compose up -d gateway`); the wrapper chowns ownership back. **Rule:** before recreating a compose service, capture its live env (`docker inspect --format '{{range .Config.Env}}...'`) and pass any non-default values explicitly (or persist them in a project `.env`) — a compose file with `${VAR:-default}` is a landmine if VAR isn't in the environment at up-time.
- [docker/mounts] **Don't nest a read-only bind mount inside a directory the entrypoint `chown -R`s.** I mounted the warehouse at `/opt/data/municipal-archive:ro`, but the hermes boot wrapper runs `chown -R /opt/data` — which collides with the read-only submount and compounded the startup breakage. **Fix:** mount such read-only data OUTSIDE the chowned tree (used `/warehouse:ro`). **Rule:** check the image's entrypoint for `chown -R <datadir>` before mounting anything (esp. `:ro`) inside that datadir; give read-only reference data its own top-level mountpoint.

- **[trimit/cfm-ui] CSV/export button as `type=submit` inside a filter form is hostage to EVERY field's HTML5 validation** (step/min/max/pattern). A money `<input type=number step="1000">` with a cents value silently blocked the whole Revenue Performance form (export AND Apply). Fix pattern: never step-constrain a money field (use `step="any"`), and put `formnovalidate` on any export submit button — or export via a plain `<a href>` link (immune). (ship #182)

- **[trimit/sales-signals] "Running dry" (forward-book<3mo) false-flags recurring maintenance accounts** — they book ~1mo ahead by nature, so a naive forward-book horizon flags healthy recurring customers (Irvine Co Retail's ~40 centers). Discriminator = active recurring CADENCE: recent completed work + a booked next WO = being serviced, NOT dry. Contract lookup did NOT help (recurring contracts aren't recorded in dbo.Contracts — only expired-2021 rows; see wiki/facts/trimit-recurring-contract-lookahead-gap.md).
- **[trimit/debug] To inspect live TRIM IT data, deploy a throwaway auth-gated diagnostic CFM to play, curl it from the play server (bot cookie 376), then DELETE it.** Gotcha: `##qVar#` in cfoutput is a PARSE error (## = escaped literal #, then unbalanced #) → uncatchable by cftry → generic CF error page. Use `#qVar#` for a value; `###qVar#` only if you truly need a literal # prefix.

- **[munibot] Agent workflow ≠ its scripts — verify the AGENT, not just the script (2026-07-18).** Herman's v5 `bid_output.py` fail-closes (exit 2, REVIEW REQUIRED) on Long Beach PCA/QAC when TRIM IT has no verified rate. When MuniBot ran the SAME bid through its `municipal-bid-pricing` SKILL (as the agent, `hermes -z --skills … --yolo`), it did NOT fail-close — it priced PCA $173.76 / QAC $128.63 and only marked "HOLD". A control living in a script isn't a control the agent honors unless it's in SKILL.md. Test-as-prod = drive the actual agent, then diff its output vs the script's. Also agent inventory 87,481 vs script 87,229.
- **[munibot] Driving the hermes agent one-shot: `hermes -z` quoting + perms (2026-07-18).** Nested double-quotes inside the `-z` prompt (a path with spaces) break shell parsing → the next word gets read as a subcommand ("invalid choice: 'Angeles'"). Fix: write the prompt to a file, run `-z "$(cat prompt.txt)"`. And the output/log dir must be `chown hermes:hermes` (a dir made by a default-user `docker exec` is root-owned → `hermes` hits Permission denied). Run the agent as `docker exec -u hermes -e HOME=/opt/data`.

- **[munibot] The warehouse leaks the answer key — our "blind" bid test isn't blind (Skipper, 2026-07-18).** MuniBot prices by pulling each city's history from `/opt/data/municipal-archive/` — which contains BOTH GSTS's own submitted bids AND the award results (WCA winning prices) for the very cities we tried to test him on. So feeding him a city we've already bid (Pomona, Bell Gardens) doesn't test his pricing ability; he finds our prior submission + the winner's numbers and undercuts them. His "sharp" comps ($83 WCA street, $139 our grid, $904K vs $1.16M) were warehouse recall, not pricing. A REAL blind test must withhold the answer: either (a) test a city we've NEVER bid (no GSTS submission in the warehouse), or (b) temporarily quarantine that city's own submitted-bid + results files before the run, or (c) hold out a fresh RFP whose award isn't in the warehouse yet. Warehouse-as-comp-source is correct BY DESIGN for real bidding — it just invalidates the compare-to-answer-key test.

- **[trimit/cfm-ui] Excel `#####` on a CSV date column = Excel auto-date + narrow default width, NOT bad data (ship #183, 2026-07-17).** RevPerf CSV cells were clean uniform ISO (verified against the served CSV), but Excel auto-converts ISO→date + ~8ch default column width → single-digit-day dates fit, double-digit (≥ 7/10) overflow to `#####` — flips exactly after Jul 9. To force full display, emit the date as an Excel text-formula `="mm/dd/yyyy"` **UNQUOTED** (a quoted `"=..."` breaks the formula-eval trick; bypass your normal `csvField` quoting for these cells). Note: text sorts by string not chrono — use ISO `="yyyy-mm-dd"` if chronological sort matters.
- **[coldfusion/debug] The generic CF error page does NOT contain the string "cfml error" — a grep-for-errors that keys on "cfml"/"error" gives FALSE NEGATIVES.** A page can be 500-ing hard while your render-check reports "0 CF errors." Detect it by grepping the served HTML for `<title>Error</title>` (the CF error template's title) instead. (2026-07-14, SPM Results)
- **[coldfusion/db] Two CF-DSN SQL gotchas (2026-07-14):** (1) the CF datasource **blocks multi-statement `IF … INSERT`** batches — rewrite an idempotent insert as a single `INSERT … SELECT … WHERE NOT EXISTS (…)`. (2) A ColdFusion **struct name can collide with a `<cfquery name=>`** (scope is case-insensitive) → silent overwrite/odd behavior; give the struct and the query distinct names.
- **[agents/telegram] NEVER run a manual `getUpdates` against a live Hermes/OpenClaw bot — it steals the poll (409 Conflict) and can knock the bot's own long-poll offline.** A 409 when you poke it actually means the bot IS connected and polling — stop poking it. To inspect what a live bot is receiving, read the gateway journal (`journalctl --user -u …` / the gateway log), don't call the Telegram API directly. (recurred 2026-07-15 ×2, 07-16)
- **[harness/process] A complex multi-step bash script (`set -e` + loops + chained ops) can trip a malfunctioning permission-approval layer (surfaced as a `ZodError`) and fail to run — while the SAME work as discrete simple commands goes through fine.** When an approval prompt misbehaves on a big script, break it into individual simple commands rather than fighting the wrapper. (2026-07-17, warehouse cleanup)
- **[trimit/deploy] The deploy manifest only walks the named ENTRY pages — it MISSES (a) shared libs referenced only by non-entry pages and (b) un-gated deep drill children / bundled legacy add-ons.** 2026-07-20 pre-deploy crew caught `vendor/chart.js@4.4.1` (needed by all 5 Executive chart pages → charts silently blank on prod) AND 3 un-gated financial pages already bundled for prod. Before every ship, run a garbage-cookie (`ZUserID=99999999`) sweep on EVERY .cfm in the bundle + a full dependency grep, not just the entry pages.
- **[comms/email] Gmail refuses `.js` attachments EVEN inside a .zip (it scans archives) → a deploy zip containing .js silently bounces/strips.** Don't email code bundles; stage on the server + email a pointer link instead. (2026-07-20)
- **[trimit/kanban-db] `Workbench.dbo.WorkKanban.CardID` is an IDENTITY column** — inserting a card with an explicit CardID fails (Msg 544 "Cannot insert explicit value for identity column"). Omit CardID on INSERT and let SQL assign it; use `SCOPE_IDENTITY()` to read the new id. (2026-07-20)

- infra/agents — **Codex ChatGPT-oauth can't be cloned into a 2nd container.** Single-use refresh tokens: two copies refreshing independently → the loser gets "refresh token already used" and dies. Symptom: 401 token_expired in a container even though the host codex works. Fix: for containerized agent crews, use **API-key models (Kimi/Gemini/GLM)** — static keys, no race — not codex oauth. (Boss Herman "works fine off the same account" only because it uses API-key crew + has no codex binary.) The codex binary is a static musl build → copies into any glibc container fine; it's the *auth*, not the binary, that doesn't travel. (2026-07-20, wiring MuniBot's bid-check crew)

- trimit/access — **I have WRITE access to PLAY — don't reflexively say "read-only."** My SSH key `~/.ssh/gstsdb_ed25519` logs into the play server (Administrator@100.86.97.46) as **Administrator** → write files to the webroot `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\...` via `scp`, and run **write** SQL on GSTS via `~/arbor-stack/production-dashboard/gsql.sh` (SQLCMD `-E` integrated auth = full perms). The read-only things are *separate*: HermanRO/GSTSREADONLY query logins + the `gilligan-bot` view-only web login. I once told the Skipper I couldn't install a profile pic "because play is read-only" — wrong, cost a round-trip. Follow the repair contract (backup-first + verify) but know the write path is mine. (2026-07-21)

- mna/fort-point — **Route deal questions to the right advisor — the banker ≠ the phantom.** Cam Bryan (Augusta, sell-side banker) owns DEAL ECONOMICS (valuation, gross→net proceeds, fees, structure, earnout measurement, escrow, timeline). He knows the Skipper's phantom exists but did NOT write it and can't interpret it — phantom base/"taxes"/409A go to Gary Vyneman (attorney) + the LTIP plan doc; the adjusted-EBITDA add-back bridge + AGP definition go to Steve (CFO). I misfiled "what's the phantom's deal-price base" onto Cam and the Skipper corrected me. Map: [[fort-point-advisors-and-open-questions]]. (2026-07-21)

- coldfusion/perf — **A slow ColdFusion grounded-answer is usually ONE tall-pole query — time each layer before optimizing.** Arbor Helper's count-once coverage answer took **83–90s**; every layer was ~3s except the accrual UDF `dbo.GetPeriodAccrual` (called twice = ~96s). Fix wasn't SQL tuning — it was **application-scope caching** (accrual 4h, figures 30min) + a `*/15` warm cron (`landing-assistant/warm-coverage.sh`) so warm calls are 0.23–0.33s and no human eats the cold hit. Same cache pattern as the existing weather cache. Also: a 55s curl timeout hid the real result as an empty/JSON-parse error — always check `-w '%{time_total}s %{http_code}'` and raise the timeout before assuming the endpoint is broken. (2026-07-22)

- coldfusion/security — **The CF "GSTS" datasource on play connects as `sa`** (found in `C:\ColdFusion2023\cfusion\lib\neo-datasource.xml`, url `...databaseName=GSTS`, `username=sa`). So ANY query the assistant/pages run has sysadmin rights — cross-db `Workbench.dbo.*` reads/writes "just work" with no grants. Upside: no permission plumbing. Non-negotiable downside: a chat box → `sa` means **every write handler MUST use parameterized `queryExecute` (cfsqltype params) — NEVER concatenate user/chat text into SQL**, and scope writes to the caller's cookie ZUserID (not anything from the message). Built Arbor Helper's to-do/note write-actions (B1) this way. (2026-07-22)

- coldfusion — **Adobe CF `serializeJSON` uppercases struct keys added via DOT notation** (`out.todoId=x` → JSON `"TODOID"`), so a JS client reading `j.todoId` gets `undefined`. Keys created in a `{"foo"=..}` literal (quoted) serialize lowercase and work. FIX: add later keys with quoted bracket notation — `out["todoId"]=x` — to preserve camelCase. Bit the Arbor Helper live-todo wire (action came through, todoId/todoBody were null) until switched to brackets. (2026-07-22)

- trimit/data — **"Work/go-aheads to schedule" = WorkOrders where `StatusDefs.Desc1='InProcess'`** (TRIM IT's own "To Be Scheduled" page `Sched.WorkOrders.ToBeScheduled.Content.cfm`; also `CompanyID<>297309`). WO lifecycle: **InProcess (to schedule) → Active (scheduled/in-progress) → Complete**. Live: Active 645/$7.8M, **InProcess 33/$332K** — the real "to schedule" queue is TINY because approved work is scheduled almost immediately. Two WRONG turns I made first: (a) the SPM "pipeline" ($23.5M/2,963 = all open go-aheads 12mo, un-deduped) and (b) `GoAheads.StatusDefID=44` + `OriginalStartDate IS NULL` → $6.53M (that field is sparse, NOT a schedule signal; Skipper: "most of this is already scheduled/consumed"). LESSON: **scheduling state lives on the WorkOrder status, NOT on GoAheads** — the GoAhead lifecycle (Pending49→Active44→Complete45) is the SALES pipeline. When a metric has a native TRIM IT page, read ITS query first (search webroot `*Schedul*`/`*GoAhead*`) instead of inventing one. Nate's Scheduling-Meeting PDFs (`arbor-stack/sales-review/incoming/`) are the human-facing version. (2026-07-22)

- sql/ole — **Reading an HTTP response body via SQL Server OLE automation returns NULL unless the OUTPUT variable is FIXED-size.** `sp_OACreate 'MSXML2.ServerXMLHttp'` → `sp_OAGetProperty ...,'responseText',@out OUT` yields NULL when `@out` is `varchar(max)`/`nvarchar(max)` — a MAX type as an OLE OUT param throws ODSOLE **`Error in srv_paramset`** (only visible via `sp_OAGetErrorInfo`). Use **`varchar(8000)`**. Also `open()`'s async arg must be integer **`0`** (string `'false'` reads as truthy = async → body not ready → NULL). Cost ~8 diagnostic rounds on the Locations geocoder before spotting it. (2026-07-22)

- trimit/geocode — **A TRIM IT location stuck at 0/0 (map pin on "null island") is the DB geocoder, not the page.** The `LocationsPostUpdateAddress` trigger calls proc `dbo.Locations_LatLong_upr`, which geocoded via the **retired keyless Google endpoint** `http://maps.google.com/maps/api/geocode/xml?sensor=false` (Google killed keyless geocoding) → silent fail → 0/0, on PROD too. The page's client-side `google.maps.Geocoder` (`codeNewAddress()`) is a red herring (Maps JS not even loaded there). Geocoding is centralized — address edit / new-site insert / RFP generation / nightly batch all funnel through that one proc — so fix the proc, not any page. (2026-07-22)

- infra/cron/refresh-timing — **The nightly play `GSTS DB RESTORE` runs 3 AM *Pacific* ≈ 10–11 AM *UTC*, NOT 3 AM UTC.** The openclaw box is on UTC, so any cron that must run *after* the restore (re-apply profile pic, HermanRO regrant, any post-restore reseed) has to target ~10–13 UTC — or, better, just run **all-day `*/30 * * * *`** if it's idempotent. Bit us twice: the reapply-profilepic cron was scoped `*/30 3-6` (UTC) = *before* the restore, so the wipe was never re-applied and the pic went blank all day (log proof 07-22: 04:34–06:30 UTC runs all rowcount 0). Rule of thumb: **`3 AM "local" ≠ 3 AM UTC`; for post-restore reappliers default to all-day, like the HermanRO regrant cron that never had this problem.** (2026-07-23) [[env-host-and-tooling]]

- trimit/diagnosis — **When an outcome has MULTIPLE independent producers, fix the simplest path the user actually triggers before chasing the deeper mechanism.** The Location "Lat/Long won't populate" bug had TWO broken geocoders both writing `Locations.Latitude/Longitude`: (1) client-side JS (`codeNewAddress`) that crashed because `myLatitude` inputs were role-gated (line 514) → null element → JS threw; (2) a SQL trigger geocoder (`Locations_LatLong_upr`) calling a dead keyless-Google endpoint. I *found* the line-514 gate early but over-pursued the SQL geocoder and built a whole Census-proc fix + deploy package. Jordan's dev correctly made the client-side gate/null-guard/async-race the fix and shipped it to prod — cleaner (no SQL→OLE→HTTP). Both fixes "work" because either geocoder populating the DB satisfies the map. LESSON: enumerate ALL producers of the symptom, then pick the one on the actual user action + fewest moving parts — don't fall in love with the deepest root cause you find. (2026-07-23)

- **[trimit/gps-import · pricing]** The GPS/RFP importer creates **NEW `InventoryGroups` in category 197 "UNDEFINED"** matching the survey's species names verbatim (e.g. "LONDON PLANE", "AFGHAN PINE", "Brisbane Box") **instead of matching to the existing GSTS catalog** — so those trees are **orphaned from all invoice/pricing history** (0 HoursEach rows) even though the species has thousands of invoiced trees under its proper name. Cost: on the Goodman pilot, 50/81 trees (62%) fell to the generic size-fallback for pricing. **Fix two places:** (1) pricing — map UNDEFINED dupes to the real catalog species (London Plane→"Sycamore - London Plane", Brisbane Box→"Tristania (Brisbane Box)", S.Magnolia→"Magnolia", Afghan Pine→"Pine - Eldarica"); (2) the converter/import — add a species-name→existing-InventoryGroupID match step so future imports don't create UNDEFINED duplicates. Affects every Goodman property. Detect: `WHERE ig.InventoryCategoryID=197` on a project's inventory. (2026-07-23)

- **[trimit/species-crosswalk]** When matching survey/RFP species names → TRIM IT `InventoryGroups`, **"exact-name match" is NOT good enough** — the catalog has many sparse duplicate entries under the plain common name, while the CANONICAL high-history entry is usually in "Type - Variety" format. Herman's first Goodman crosswalk matched exact names and landed on near-empty dupes for 3 big species: African Sumac 511 trees → "African Sumac" (ID 2019, 2 comps @0-6) instead of "Rhus - African Sumac" (259, 452); Callery Pear 286 → "Pear - Ornamental" (604, 83) instead of "Pear - Bradford" (247, 1381); Elm Hybrid 33 → "Elm - spp." (321, 0) instead of "Elm - Chinese" (287, 1479). **Rule:** among ALL name/botanical/alias matches, pick the InventoryGroupID with the **most 0-6 ServiceClass-1 history** (tiebreak any-size), exclude cat 197. Verify big-tree-count species by hand. Detect a bad match: high tree count + tiny `HoursEach` N. (2026-07-23) [[pricing-true-labor-method]]

- **[trimit/species-crosswalk · refinement]** The "most-0-6-history-wins" rule (above) must be **scoped to the SAME plant** — it over-corrected in Goodman v2 by swapping botanically-correct matches for higher-volume *different* ones: Marina cultivar (Arbutus 'Marina', ID 29, 363 comps) → "Arbutus - Strawberry Tree" (30, a different species, A. unedo); Magnolia 'Little Gem' (dwarf cultivar, 844, 623 comps) → generic "Magnolia" (175). **Correct rule:** match the correct species/cultivar FIRST; among duplicate/spelling-variant/generic entries **of the same plant**, pick the most history — but **never cross to a different species or dissolve a distinct cultivar into a generic just for volume.** If the correct match is thin at 0-6, LEAVE it — the pricing engine's species→size-level fallback already covers thin data. **More history ≠ better if it's a different tree.** (2026-07-23)

- **[trimit/diagnosis · deliverable-maps]** Herman's traveler-package "Field Map" is a **matplotlib scatter he generates himself** from Schedule-2 GPS (caption: "plotted from GPS coordinates | Generated by Boss Hermes"), **NOT the TRIM IT FieldApp Google map.** When Skipper said the traveler map was "blank with dots," I spent a full prompt chasing Google Maps API-key / `v=` version / `.cfm` traveler templates — all irrelevant. The screenshot showed dots on plain graph paper: there was simply no basemap layer in Herman's plot. LESSON: for a "map looks wrong" report, **get the actual image before diagnosing** — a self-generated chart vs. the app's rendered map are totally different code paths. Fix here lives in Herman's Python PDF script (add a real basemap, e.g. contextily + Esri WorldImagery, reproject 4326→3857), not TRIM IT. (2026-07-24)

- **[trimit/coldfusion-cache]** On the play ColdFusion 2023 server, **rendering a .cfm repeatedly DURING diagnosis caches its compiled class in CF's in-memory template cache — and after you deploy a code fix, CF keeps serving the OLD bytecode.** Trusted Cache is OFF (verified in neo-runtime.xml), yet bumping the file mtime (even +5 min future) AND deleting all `cfclasses\*<Template>2ecfm*` class files did NOT force a recompile — the class is pinned in the JVM template cache (templateCacheSize 1024). A fresh FILENAME compiles clean and shows the fix (proved the fix works: `Dashboard-RevenuePerformance-T0724.cfm` → correct $), so the code is right; only the cache is stale. **Fix requires clearing CF's in-memory template cache: CF Admin → "Clear Template Cache", or restart the ColdFusion service.** LESSON: for .cfm repairs, DEPLOY the fix FIRST, then render to verify — do NOT render the page many times pre-deploy, or you'll pin the old compile and need a cache clear/restart. (2026-07-24) [[repair-contract]]

- **[trimit/deploy · CRITICAL]** Play serves some .cfm from a SHADOW webroot **`C:\ColdFusion2023\cfusion\wwwroot\GSTS\`**, NOT only the IIS root `D:\home\dev.greatscotttreeservice.com\wwwroot\GSTS\` that `deploy-to-play.sh`/`gstsdb` scp targets. If a file exists in BOTH, editing only D: does nothing — CF serves the C: copy (survives CF restarts + cfclasses deletion, because the stale SOURCE is what recompiles). This masqueraded as a "CF template cache" problem for ~40 min on the RevenuePerformance fix. **Before concluding "cache," search the whole server for duplicate copies of the file** (`Get-ChildItem D:\,C:\ -Recurse -Filter <name>.cfm`) and deploy to EVERY copy that's live. The DEPLOY-PLAYBOOK's "dual-webroot shadow copy" warning = exactly this. Files absent from the shadow (most) serve from D: fine, which is why it's intermittent and confusing. (2026-07-24) [[repair-contract]]

- **[tooling/detached-crew-runs]** Launching a long crew LLM run (e.g. `kimi-ask.py`) via the harness: (1) `pkill -f "kimi-ask.py"` SELF-KILLS the launching command — `-f` matches the pkill command's OWN cmdline (which contains "kimi-ask.py") → instant exit 144. Never pkill a pattern that matches your own command. (2) The CLI has a **180s no-output watchdog** that SIGKILLs (137) a foreground command that emits nothing — reasoning models buffer silently for minutes → killed. (3) Harness `run_in_background` also killed these long runs. **FIX: launch DETACHED** — `nohup python3 kimi-ask.py <promptfile> > out 2> err < /dev/null &` (no pkill), the launch returns instantly (no stall), Kimi runs decoupled; then poll the PID + output file with quick commands. Kimi K3 = `~/arbor-core/crew/kimi-ask.py`, ~6 min on a 168KB prompt. (2026-07-24)

- **[crew/kimi-k3-reverse-engineering]** Kimi K3 is EXCELLENT at reverse-engineering a subsystem from raw code. Gave it ~2,500 lines (apiJS.cfm + field map + apiCall) + the DB facts I'd established + a precise 4-part question; it correctly traced the grey/published map layer end-to-end (GeoJSON files in /GSTS/API/JSON/, the exact parser property contract, the IsNewPlot gate, the publish sequence, and a PLAY-viable fix bypassing the dead Taffy/proc). Every claim verified against code+server. Pattern that worked: give it ALL relevant files + everything you already know (so it doesn't re-derive) + numbered concrete questions demanding line-number citations. (2026-07-24) [[our-work-kanban]]

- **[metrics/tph · ALWAYS STATE WHICH]** **"TPH" is two different numbers and they RANK SEGMENTS IN OPPOSITE ORDER.** On **productive hours** commercial **$157.53** beats municipal **$146.70** (as it always has). On **blended** TPH — including $0-revenue paid hours — the ranking **inverts**, because commercial carries 27,325 zero-revenue hours (24.4% of its total) vs municipal's 9,716 (14.5%). I reported "municipal beats commercial" off the blended figure; Skipper caught it. **Rule: never say "TPH" unbaptized — say Productive TPH or True/blended TPH, and never compare one to the other.** (The RC-02 dashboard now shows both side by side on purpose.) Related open item, do-not-present: ~21,000 commercial field hours with $0 revenue that T&A only explains ~6,377 of. (2026-07-24) [[trimit-investor-case]] [[rc-02-revenue-performance]]

- **[data-trust/back-entry]** **Before computing ANY duration/aging metric in TRIM IT, exclude the back-entered records first.** Bid-chain timestamps (RFP created / proposal sent / go-ahead) are frequently stamped **within the same second** because the record is created *after* the work happened — 12% inside 60 seconds, 25% inside an hour, 97% of sent→approved same-day. My first turnaround pass reported **median 2 days** and it was garbage; filtering the back-entry set out gave the real answer, **median 6d / p75 14d / p90 36d**, which independently validated Skipper's "about 10 days." Same family: `Invoices.InvoiceDate` is a BACKDATED accounting date (median −3d vs completion) so billing speed must be measured off `Invoices.Created`. **Smell test: if a duration distribution has a huge spike at ~0, you're measuring data entry, not the process.** (2026-07-24) [[trimit-db-gotchas]] [[trimit-investor-case]]

- **[tooling/browser-agent]** **A "crawl this whole app and report" prompt handed to a browser-driving agent will blow the context window — chunk it by section.** The dev-server FieldApp inventory prompt (map every page, follow every link, open the console on each) ran 22 steps and died at **1,411,579 tokens > 1,000,000 max**, and even compaction failed. Re-run in chunks (nav map first, then one subsystem per run) and have it return findings per chunk. Also budget for it making **accidental writes** on a "read-only" browse: buttons named like modals aren't (dev's "Add Contact" hit `Field.Contact.Create.cfm`, which INSERTS a blank row server-side before you type, leaving ContactID 222090 to clean up) and some checkboxes POST straight to an API. **"Don't click Save/Delete/Submit" is not sufficient read-only instruction** — tell it that any button may write, and to report what it touched. (2026-07-24) [[vendor-fieldapp-build]]
- 💥 trimit/play-db — **The nightly prod→play GSTS restore WIPES anything we build in the GSTS db — including whole projects/inventories.** 2026-07-25: the 04:49 UTC restore (of a 7/22 backup) erased all three Goodman builds — projects 1105465 (Fullerton pilot), 1105467 (Eastvale, 313 trees), 1105468 (El Monte, 298 trees) + LocationIDs 1285095/96/97 — plus Herman's in-flight El Monte fixes. `MAX(ProjectID)` was back to 1105464. We spent 3 days building on sand. **Rules going forward: (1) treat `GSTS` on play as EPHEMERAL — any multi-day build must live in `Workbench` (separate db, NOT restored) or in flat files, with a scripted, idempotent replay into GSTS; (2) after every prod→play restore, re-verify anything we "shipped" to the DB still exists before reporting it done; (3) `msdb.dbo.restorehistory` is the fast check.** What saved us: the backup-first rule — `Workbench.dbo.*_bak_*` snapshots survived and hold the full tree rows. (2026-07-25) [[env-host-and-tooling]]
- 📛 tooling/verification — **Don't trust a crontab/config COMMENT as a statement of current behaviour — read the code it invokes.** I told the Skipper the AR collections job was "PREVIEW to Skipper while piloting" because that's what the crontab comment said; the wrapper actually calls `--live` and sends to every rep, Nate and Brent. Comments describe the day they were written, not today. When reporting what an automation *does* (especially who it emails), trace: cron line → wrapper → script → the flag that decides recipients. (2026-07-25) [[async-report-rule]]
- 💥 tooling/email — **Verify a script's dry-run flag NAME before using it on a live emailer.** I ran `ar-collections-monitor.js --dry-run`; the real flag is **`--dry`**, so the unknown flag was ignored and it actually sent (a PREVIEW to the Skipper — contained, but unintended). With any script that can send: `grep` for the flag first, or test with the send transport unreachable. Unknown flags fail OPEN, not closed. (2026-07-25) [[external-comms-contract]]
- 🕵️ data-pipelines — **When one stage RENAMES a file, check every downstream regex that parses the name.** `ar-fetch.js` saved Dimitry's report as `AR-Aging-2026-07-21.xlsx`; `ar-collections-monitor.js` still parsed the week with `/(\d{2}\.\d{2}\.\d{2})/` (his ORIGINAL `07.21.26` naming). It silently matched nothing, so `wk=''` and every AR email to the reps went out **with no date on it** — the one field that would have exposed stale data. Silent-empty is the worst failure mode: it looks like a design choice. Prefer failing loudly (we now emit `(DATE UNKNOWN)` and refuse to send). (2026-07-25) [[only-trustworthy-data]]
- 📭 email-automation — **Don't gate "have I processed this?" on the IMAP `\Seen` flag.** `ar-fetch.js` searched `{seen:false}`, so any human opening the inbox — or our own `inbox-recent.js`, which downloads bodies and thus sets `\Seen` — silently consumed that week's report; **3 of 6 weekly AR reports were lost.** Track processed items in our own state file keyed by **message-id**, and make any inbox-listing tool restore the flags it found. Read state is shared with humans; it is not ours to use as a queue. (2026-07-25) [[async-report-rule]]
- 🔎 openclaw/status — **The `/status` card packs FOUR independent axes onto one line — don't read adjacent fields as one phrase.** `Execution: direct · Runtime: … · Think: off · Fast: off · Reasoning: on · elevated`. **`Reasoning: on`** = whether thinking is *displayed* (`/reasoning`); **`elevated`** = *tool/command permissions* (`/elevated`), unrelated to reasoning — the Skipper read the tail as "reasoning elevated". **`Think:`** = the thinking-effort override, a third thing. Proof method that settled it: toggle one control and **diff two status cards** — `elevated` was present before the reasoning toggle, `Reasoning: on` was not. Also: I misread `Think: off` as "thinking disabled" and theorised on top of it — **read the docs (`docs/tools/thinking.md`) before explaining a config behaviour twice.** Authoritative check is `/think` alone (user must send it; directive-only messages are stripped before I see them). Resolution order: inline → session override → per-agent → `agents.defaults.thinkingDefault` → fallback **medium**. (2026-07-25) [[gilligan-session-settings]]
- 🔗 trimit/schema — **The bid chain joins on `ProposalID`, NOT `RFPID`.** `RFPs.ProposalID → Proposals.ProposalID` and `GoAheads.ProposalID → Proposals.ProposalID` are the real links. `Proposals.RFPID` is a BACK-link populated only ~32% of the time, and `RFPs.GoAheadID` is **0%** populated (2026). I joined on the back-link, found "68% of proposals unlinked", and raised a false alarm that the whole turnaround methodology was survivorship-biased. **Before declaring a data-integrity problem from a join, check whether the link runs the OTHER way** — `sys.columns` on both tables takes 20 seconds. (2026-07-25) [[trimit-db-gotchas]]
- 📐 analysis — **Save the query, not just the number.** Deck figures from 7/24 (population 2,329, p90 36, avg 17, "12% within 60s") could not be reproduced 24h later because the exact filter was never written down — median 6 and p75 14 came back fine, the tail stats didn't. A deck that claims "every figure is checkable" has to ship with the SQL that checks it. Fix: `VERIFY-deck-numbers.sql`, each query marked reproduces/doesn't. (2026-07-25) [[only-trustworthy-data]]
- 🧮 analysis — **When you re-base a ratio, re-measure the NUMERATOR too — don't just swap the denominator.** Re-basing the bid population 5,145 → 5,004 I "corrected" handoffs-per-bid from 5.6 to 5.8 by division alone. Re-running the actual query gave **27,865 actions / 5,004 bids = 5.57 ≈ 5.6** — the original figure was right and my "fix" was a fabrication. The numerator had been measured on the same population all along. Rule: a ratio has two measured parts; changing one silently invents the other. (Sibling catch: the same re-base left `17,800` duplicate entries *correct*, because that total and its subtraction both moved — so verify each dependent figure rather than assuming they all shift.) (2026-07-25) [[only-trustworthy-data]]
- 🔗 trimit/metrics — **Win/loss lives on the GO-AHEAD, not the proposal.** `Proposals.Approved` and `DateApproved` are dead (0 of 5,015) and the proposal's own "Lost" status is used on just 3.9% — take that route and you compute a **96% win rate** and wrongly conclude losses aren't recorded (I did, and wrote it into the plan and both decks). The real signal is **`GoAheads.StatusDefID`** — Archived/Inactive/Lost/Expired are NOT wins. Canonical definition is in `Executive$ClosePercentage$ByRep.cfm`; excluding municipal recurring (`ProjectGroupDefID=11`) it gives **70.2%**. **Before declaring a metric unmeasurable, check whether we already ship a dashboard that measures it** — the Skipper caught this with one question. (2026-07-25) [[canonical-definition]]
- 🧾 evidence/decks — **Check provenance before calling anything "independent corroboration."** I cited a vendor-authored roadmap in Deck A as an independent party reaching our conclusion ("two analyses, arrived at separately, same answer") — the Skipper had **commissioned it**. In front of a QoE team that line dies the moment someone asks the author how he got there, and it discredits the surrounding measured findings by association. **Ask "who asked for this document, and why?" before it becomes evidence.** Directed agreement is a scope statement, not a second opinion. (2026-07-25) [[only-trustworthy-data]]
- 🚫 fabrication — **NEVER invent a proper noun to fill a field. Leave it blank or ask.** Building the deck PDFs I had "Steve" (no surname) and typed **"Steve Sowers"** onto the title page of a document intended for that person's hands. The correct name — **Steve Guzowski, CFO** — was already in my own wiki (`fort-point-advisors-and-open-questions.md`, with his email). **I had the fact and invented instead of retrieving it**, violating the standing "search before answering from memory" rule. A fabricated name is worse than a blank because it reads as knowledge. **Rule: names, titles, dates and figures are never filled from inference — retrieve or omit.** (Skipper's call: keep names off presentation title pages entirely.) (2026-07-25) [[comms-style-and-ask-first]]
- 📄 pdfkit — **PDFKit retains the last explicit x from any positioned `text(str, x, y)` call.** After rendering a table (each cell positioned at its own column x), every following paragraph inherited the LAST COLUMN's x — shifted right and clipped off the page edge. Same after bullets drawn at x=74. **Fix: reset `doc.x = <left margin>` at the top of every block, and pass an explicit x on headings/paragraphs so they can never inherit.** Verify by rendering with `compress: false` and grepping the content stream for `Td`/`Tm` operators — no text x should exceed `pageWidth − rightMargin`. (Skipper caught it from a screenshot; §3 body was offset and cut off.) (2026-07-25) [[gsts-ui-style-guide]]
- 🕵️ verification — **Verify the RENDERED OUTPUT, not the input.** Twice in one hour the deck PDFs leaked content I believed was excluded: (1) my earlier "0 personal-name hits" check ran `strings` over a **compressed** PDF, so it proved nothing; (2) the markdown **H1** (`# DECK A — Steve & Scott: …` / `# DECK B — Fort Point: …`) had no handler in the renderer, fell through to the paragraph branch, and printed the audience names as body text on page 2. **Correct method: render with `compress: false`, extract the text-showing operators, and grep THAT for every forbidden term.** A cut-list is a claim about the input; only the output is evidence. (Skipper caught it by reading page 2.) (2026-07-25) [[data-freshness-contract]]
- 🎚️ findings/scope — **Don't inflate a records gap into an operational or safety claim.** Finding that `GoAheads` has no creator column, I wrote that "if a crew turns up somewhere they shouldn't, there's no way to establish who sent them." **False** — operations knows precisely who dispatched: scheduling, the crew sheet, the supervisor. The gap is in **the paperwork trail, not the work**. Overstating like that hands the room an easy rebuttal ("no, Manuel scheduled it") and taints the accurate findings beside it. **State exactly what is missing and nothing adjacent to it.** Third instance today of claiming past the evidence — after the fabricated surname and the false "independent corroboration". (2026-07-25) [[only-trustworthy-data]]
- 🔤 pdfkit/typography — **PDFKit's built-in fonts (Helvetica etc.) use WinAnsi encoding — no glyphs for → ≈ ← ⇒ ≥ ≤ ⏱, which render as garbage.** The Skipper spotted one mangled arrow ("sent!'approved"); scanning for *every* non-cp1252 character found **59 instances across 11 characters — ≈ used 30× and → 29×**, i.e. it was scattered through both decks, not a one-off. **Fix: embed a Unicode TTF** — `doc.registerFont('Body','/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf')` — rather than mangling the source text into ASCII. Verify with `compress:false` + check for `FontFile2` (embedded) and absence of `WinAnsiEncoding`. **Generalises: when one bad glyph appears, enumerate the whole character set rather than fixing the instance.** (2026-07-25) [[gsts-ui-style-guide]]
- ✍️ editing — **Fix only what was flagged; don't propagate an edit to a document the user didn't mention.** Asked to fix Deck A's opening ("the last while"), I silently applied the same change to Deck B and swapped in "we instrumented" — jargon the Skipper wouldn't say out loud, in a deck he hadn't asked me to touch. **Two rules: (1) scope the edit to what was raised, or say explicitly that you're extending it; (2) in a script someone has to SPEAK, prefer the plainest verb — "measured" over "instrumented".** If it doesn't sound like them, they'll stumble on it in the room. (2026-07-25) [[comms-style-and-ask-first]]
- 🛡️ decks/privacy — **A "no names" rule needs a machine check, not discipline.** After being told to strip names I removed them from the title page — and three more sat in Deck A's body ("For Steve:", a named municipal bridge person, "APPENDIX — Methodology (Steve will ask)"). Fixed by **enumerating every known name across both decks**, then adding a **hard guard in the renderer that ABORTS the build** if any personal name or the investor firm's name would reach a PDF. ⚠️ First run of the guard fired on a line the renderer already skipped — **the guard and the renderer must operate on the SAME filtered line set**, or you get false alarms and, worse, false confidence. (2026-07-25) [[external-comms-contract]]
- **[analysis/decks]** A four-agent adversarial review of my own decks found ~60 defects, nearly all one habit: **stating a fact about what the RECORDS contain as a fact about what PEOPLE did** ("go-ahead record created" → "customer approval"; "one CreatedByID" → "signed by a ghost"; "8 enterer IDs" → "8 managers"). Findings survive being stated narrowly; they don't survive being dramatised. Write the record-level claim first, then decide if the people-level claim is earned.
- **[analysis]** Reviewers are not oracles — one confidently reported the invoice count as 50,233 vs the deck's 50,283. I queried it: **the deck was right and the source fact-base was stale.** Verify contested numbers against the system before "fixing" them, or a review pass silently injects errors.
- **[git/workspace]** `arbor-stack/` is its **own git repo**, separate from the workspace repo — so `git commit` run from the workspace does NOT save deck/analysis edits, and the guarded 30-min push only covers the workspace. I reported work as "committed" when it wasn't. **Commit in the repo that owns the file, and check `git log -1` in that repo before saying it's saved.**
- **[analysis/trimit]** I explained the ~30,000 zero-revenue field hours as a **multi-visit** artifact. Wrong — the Skipper knew it was **multi-CREW**: when two crews work one job, all production posts to one crew's sheet and the others show hours against nothing (53% of the hours; multi-visit is only 27%). The *number* was right, the *mechanism* was invented. **A pattern in the data does not tell you the cause — ask the person who runs the process before writing the explanation into a deck.**
- **[trimit/sql]** `CrewSheets.Total` is **empty on every row**; production dollars live in **`CrewSheets.NetTotal`**. Querying Total returns a clean, plausible, completely wrong answer (it made every zero-dollar sheet look orphaned).
- **[analysis]** I added a newly-discovered 2,800-hr cost line **on top of** a total that already contained a ≈4,200-hr estimate for the same work (map prep + photo sheets inside the per-bid friction). Inflated the headline before the Skipper caught it. **When new evidence arrives for work already modelled, NET IT OUT — ask "what estimate does this replace?" before adding.**
- **[analysis]** Reported a "13-day" penalty computed from **averages** on a distribution with a 285-day tail; on medians it is ~7 days (2.3 → 9.0). The rest of the deck reports medians for exactly this reason. **Match the statistic to the one already in use, and never quote a mean on a long-tailed distribution.**
- **[decks/render]** The PDF renderer's `clean()` stripped `**bold**` but not `*italic*`, so every italic parenthetical printed with literal asterisks — 7 across the two decks, unnoticed through a dozen renders because I only ever checked the markdown. **When a renderer strips markup, strip BOTH emphasis forms (`/\*{1,2}/`) — and proof the rendered output, not the source.**
- **[decks/render]** Deck A's presenter aside was stripped only because it happened to start `*(Prep note`; Deck B's identical aside printed in every PDF because it started `*(If they ask`. **Prefix-matching internal content is fragile — match on the LANGUAGE.** Added a `COACH` regex guard to the renderer that aborts the build (mirrors the name guard), and verified it fires.
- **[verify]** My first test of that guard "passed" because I appended the test line AFTER the cut marker, so it was stripped before the guard ran. **A guard test that puts the bait outside the guarded region proves nothing** — place the bait exactly where the real leak would sit.
- **[trimit/sql]** `LEFT JOIN dbo.GoAheads ON ProposalID` **fans out** — there can be several go-aheads per proposal, so it silently turned **5,033 bids into 7,244 rows** and inflated every rate I computed off it (I briefly believed 31% of proposals pre-date their request; the true figure is 22%). **Any one-to-many join in the bid chain must be pre-aggregated (`MIN(Created) GROUP BY ProposalID`) — and always `COUNT(*)` vs `COUNT(DISTINCT key)` before trusting a rate.**
- **[analysis/decks]** Deck A §3 asserted the bid chain was built in one sitting "with back-dated content." **It was false — 0 of 5,033 proposals carries a date earlier than the day its row was created.** I had inferred back-dating from a proposal *dated* 8 months before its RFP, never checking that the proposal ROW was also 8 months old (it was — the date is honest). The Skipper caught it by pure logic: *"how can someone create these files backwards if TRIM IT doesn't let you open the screens backwards?"* **An internal contradiction in your own narrative is a bug report — when the story requires the system to do something the system forbids, re-query instead of rewording.**
- **[decks/render]** The renderer takes the cover title/subtitle/audience as **argv**, and that invocation was recorded nowhere — so on a re-render I guessed, got 2 of 3 wrong, and would have rewritten the confidential-audience footer on all 22 pages. Caught it by re-rendering the *previous* markdown with my guessed args and finding the output didn't match the committed PDF. **If a build takes undocumented arguments, recover them from the last good artifact and verify by reproducing it byte-for-byte before shipping a new one.**
- **[verify/pdf]** `pdftotext` and `pypdf` aren't installed here, so my "grep the PDF for the removed line" check returned *no output* and I read that as "not present ✅" — a false pass on a tool that never ran. **A verification that produces empty output is not a pass; confirm the tool exists first.** Working extractor: `npm i pdf-parse` in `/tmp/pdfgen`, then `new PDFParse({data}).getText()` (script at `/tmp/pxfull.js`).
- **[decks/render]** Table/paragraph lead-ins ending in `:` were being stranded at the foot of a page with the thing they introduce on the next one — the renderer protected headings (`need(150)`) but nothing else. **Fixed generally: a line ending in `:` (or any line followed by a table) now reserves room for itself PLUS what follows, and breaks before the lead-in.** Pinned the undocumented cover argv in `RENDER-DECKS.sh` at the same time.
- **[verify]** Twice in one hour a check "passed" because it never ran properly: `grep` on extracted PDF text hit **binary** and printed *"binary file matches"* instead of lines (use `grep -a`), and a `-B3` context window flagged a **false orphan** three lines above a page break. **Every pass/fail check must be validated against a known-bad input first — if it can't detect the defect you already fixed, its "clean" verdict is worthless.**
- **[decks/render]** The deck PDFs were **two-thirds blank pages** and nobody noticed for weeks. The footer drew at `page.height - 46` — **below PDFKit's bottom margin** — so every footer `text()` auto-paginated, and each spawned page then got its own footer. Deck A shipped **28 pages of which 19 were footer-only**; Deck B 19 of which 12 were. Fix: `doc.page.margins.bottom = 0` + `lineBreak: false` before drawing, plus an **abort guard** that fails the build if the footer pass changes the page count. **Writing anything below the bottom margin in PDFKit silently adds pages — and a page count nobody audits will hide it indefinitely.**
- **[verify]** I reported "22 pages", "25 pages", "28 pages" across the morning as if it were a health signal, never asking *what is ON those pages*. The count was growing because of a bug, and I read growth as content. **A metric you quote but never decompose is decoration — when a number moves unexpectedly (25→28 for a net-zero content move), stop and audit it rather than reporting it.**
- **[analysis/trimit]** I read `RegularHours`/`OTHours` going to zero in Oct 2025 as **"TRIM IT stopped tracking overtime"** — a system failure. **Backwards.** The Skipper: *"we just built the time keeping function last year and started using it in october of last year."* The zeros are a **go-live**: the company moved from typed-in summary hours to real clock in/out, and the legacy summary fields were superseded, not broken. Same failure mode as the multi-visit/multi-CREW miss: **a metric dropping to zero on a date is as likely to mean a system was REPLACED as that it broke — check what started on that date before writing that something stopped.** The valuable finding survived, but inverted: the punches are captured and nothing computes the split from them.
- **[trimit/infra]** *"Play is down / I can't log in"* was **not** an outage: the **first TCP connection to the play website's database host (`198.207.148.168:1433`) blackholes for ~21s and fails, then succeeds in ~60ms** until the link idles again. That surfaces as a 20-23s hang on first page load or login POST — a refresh "fixes" it because the retry rides the now-open path. **Diagnose a hang by timing `time_starttransfer`, then testing the DB hop from the web server itself** (`TcpClient.ConnectAsync` in a loop) — pinging the web server proves nothing. ⚠️ Also: `https://<bare-IP>` fails TLS by design (no cert for the IP) — not a fault, it fooled me for a minute.
- **[trimit/cfml]** Shipped `Dashboard-RevenuePerformance.cfm` with **`##` in plain HTML that sits OUTSIDE `<cfoutput>`** — ColdFusion only collapses `##`→`#` *inside* cfoutput, so on production the header printed the raw entities `&##9662; &##128338;` and **~a dozen CSS colours were silently invalid** (no gradient bar, black text instead of white). **Rule: inside `cfoutput` double the hash; outside it, never.** Before shipping a CFML page, scan for `##` while tracking cfoutput depth — 13 offending lines vs 15 legitimate ones in the same file. **⚠️ FOLLOW-UP 2026-07-27: that source-reading pass caught only 13 of 18.** The 5 it missed (lines 1381–1384, 1389) have **inline `<cfoutput>` wrapping ONLY the variable** (`<div style="color:##888;"><cfoutput>#var#</cfoutput></div>`) — they *look* inside cfoutput at a glance, but the surrounding HTML is outside. **Reading source cannot settle this reliably. Deploy to play, fetch the served HTML, and `grep -o '##[0-9a-zA-Z]*'` the OUTPUT — expect zero.** The render also proves which lines are genuinely inside cfoutput (1410 looked identical and was correct — don't "fix" those).
- **[verify/render]** That page passed play verification: correct numbers, 0 CF errors in the log. **A page renders "clean" with invalid CSS and literal HTML entities — nothing errors.** Same failure as the deck PDFs printing literal asterisks. **Data-verify AND render-verify: look at the served page, not just its numbers and its error log.**
- **[windows/service]** `Restart-Service 'ColdFusion 2023 Application Server' -Force` over ssh **stops slowly (~90s) and killed my ssh session (exit 137)** — it looked like a failed restart. **It wasn't: the restart completed server-side.** Don't re-issue the command (risks a half-stopped service). **Check `Get-Process coldfusion | Select StartTime` — a start time of "2 minutes ago" proves the restart happened.** Same pattern for any slow Windows service over ssh: verify by process age, not by the command's exit code. (2026-07-27)
- **[infra/CRITICAL] Static data NEVER proves a database is not live — only a write you control does.** Chasing "is `198.207.148.168` production?", I compared it to our nightly restore and got **identical row counts to the exact row** (RFPs 1,685,753 · Proposals 266,852 · CrewSheets 158,160) plus five matching MAX timestamps, and leaned toward "it's a copy." **It was Sunday evening with no data entered since Friday 16:55** — a live DB on a quiet weekend is indistinguishable from a frozen one. The Skipper created one record in prod; it appeared on .168 in minutes. **It IS production.** I had even flagged the weakness and still leaned wrong. **Ask the owner to make a controlled change and go find it — one write settles what a million rows of agreement cannot.** (2026-07-27)
- **[infra] Server NAMES lie; judge a box by what its data does.** `198.207.148.168` is the PRODUCTION database but its Windows machine is named **`WIN-GSTSDB-DEV`** (SQL `@@SERVERNAME` = `WIN-WBTRXGSTSDB` — the mismatch is the signature of a clone/rename after install). Our play box's webroot is literally `D:\home\dev.greatscotttreeservice.com\`. Two "dev" labels, neither meaning dev. That naming actively reinforced my wrong conclusion above. (2026-07-27)
- **[sql/diagnostic] SQL Server error 208 vs 916 tells you whether a database EXISTS.** Missing DB → **208** `Invalid object name 'X.dbo.Y'`. Exists but no rights → **916** `server principal ... not able to access the database ...`. Play threw 916 on `Workbench`, which correctly said the DB was there and only the grant was missing — confirmed later (Travis had created it 7/26 13:23). **Verified empirically rather than from memory**, by querying a deliberately bogus database name. (2026-07-27)
- **[sql/method] Read the schema BEFORE searching a table you did not build.** Chasing "is package 1's menu deployed to prod?" I produced **two false findings in a row**, both from assuming column/table names: (1) `OBJECT_ID('dbo.DashboardAccess')` → "MISSING", but the table is in **`Workbench`**, which my login cannot even read — the check was meaningless; (2) searched `AppForms.Desc1` for page paths → "0 entries", but paths live in **`ObjectPath`** — real answer was 1. Both would have been reported as alarming. **`SELECT name FROM sys.columns WHERE object_id=OBJECT_ID('x')` costs one query and prevents a confidently wrong answer.** A "MISSING"/"0" result against an unfamiliar schema is more likely my mistake than a real absence. (2026-07-27)
- **[sql/deps] Scanning for cross-database dependencies: match `Db.<ANYSCHEMA>.<obj>`, never a hardcoded schema.** Hunting why `SalesProductionMeeting$Results` crashed on prod I grepped `Workbench.dbo.` and confidently reported 5 dependencies — the actual killer was **`Workbench.rgc.vProjectMarket`**, a view in a different schema, invisible to my pattern. I then told the Skipper the Workbench gap was "probably not this bug." His pasted error text solved it in seconds. **A dependency scan that assumes `dbo` is not a dependency scan.** (2026-07-27)
- **[process] Re-stamp the manifest checksum as the LAST step of any file edit — I let it go stale TWICE in one night.** Package 3's manifest advertised an MD5 the artifact no longer had, both before the `##` patch and again after the crew-review fixes. A crew reviewer flagged the second one as a BLOCKER and was right: a manifest whose checksum does not match makes the deploying vendor believe he received the wrong files. **Editing a shipped artifact and updating its manifest are one action, not two.** (2026-07-27)
- **[workflow] A background crew is not "still running" just because you have not been told it finished — check the clock on its last write.** I reported the pre-deploy crew as in-flight twice while it had actually been dead for 26 minutes (13 agents started, 7 results, journal untouched since 04:20). **Compare last-modified against now before reporting progress**; absence of a completion event is not evidence of life. (2026-07-27)
- **[trimit/CRITICAL] A drill-down and its parent MUST use the identical date predicate — I broke that and a crew caught it.** Fixing `Executive$Sales$Unattributed.cfm` from `BETWEEN` to a half-open range was *individually* more correct, but the parent `Executive$Sales$ByRep$Scope.cfm` still uses `BETWEEN` in FOUR queries. A drill exists to explain its parent's headline; changing one side made it return MORE money than the number it explains. **Reverted to BETWEEN deliberately, with a comment naming the parent's line numbers.** Rule: before "improving" a filter in a child page, grep for every page that must agree with it — and if you change one, change them all in the same deploy or change none. (2026-07-27)
- **[css/CRITICAL] `position` + `z-index` on a container CREATES A STACKING CONTEXT — every z-index inside it is then trapped and cannot compete with anything outside.** TRIM IT's landing menu: Spry sets the flyout to `z-index:1020`, but its container `#ActionMenuDiv` is `position:absolute; z-index:7`, so **the 1020 is inert** — 7 is what actually competes. A huge z-index that "should obviously win" is the tell: look at the ANCESTORS, not the element. (2026-07-27)
- **[css/ios] "Works on desktop, breaks on iPad" over an iframe = IMPLICIT stacking order.** If one side has a positive z-index and the other has `z-index:auto`, the spec says positive wins and Chrome/desktop Safari obey — **iOS Safari promotes iframes to their own compositing layers and does not honour it.** Fix by making BOTH sides explicit (`z-index:0` on the content column, a clearly higher number on the overlay), not by raising one side further. (2026-07-27)
- **[verify/limits] My headless browser is Chrome — it CANNOT reproduce a WebKit-on-iOS bug, and worse, it renders the broken code *correctly*.** Emulating an iPad viewport only changes geometry, not the rendering engine. When the defect is engine-specific, say plainly that verification depends on the user's device and do not imply the fix is proven. (2026-07-27)
- **[method] Hardcoded data is often the only written record of a business rule — validate your replacement AGAINST it before deleting it.** Replacing TRIM IT's `holidays2026` list with a rule, my first version shifted only Sunday→Monday and produced `2026-07-04`. The list said **`2026-07-03`** — revealing that GSTS observes a Saturday holiday on the **preceding Friday**, a policy written nowhere else. Only after adding Sat→Fri did the rule reproduce all 7 dates. **Generate from the rule, diff against the literal, and require an exact match before you delete the literal.** (2026-07-27)
- **[analysis] If a "fix" moves the numbers more than the defect could explain, REVERT it — do not ship arithmetic you cannot account for.** Chasing "North + South ≠ ALL" on Revenue Performance I added a weight fallback for zero-hour jobs. It shifted the territory counts by **+14** when only **3** jobs were unattributed. The 11-job remainder had no explanation, so I reverted and kept only the change I could prove. **The premise was also wrong: 49 of 281 July jobs are worked by BOTH territories, so a shared job counts on both sides and N+S==ALL is not a valid invariant for a COUNT — hours are the measure that reconciles (they tied out exactly).** Check whether the invariant you are enforcing is actually true of the business before enforcing it. (2026-07-27)
- **[trimit/data] In TRIM IT, a crew sheet with COMPLETED HOURS but ZERO dollars is zero-revenue work, NOT "unposted".** 386 of July's past sheets look unposted by a dollars-only test, but they carry 1,944.9 completed hours — yard time, safety training, OJT, meetings. Treating them as unposted and projecting at target TPH invented **$241K** of revenue and pushed TPH from $135 to $149. **The three-way test is: dollars → actual; no dollars but hours → zero-revenue (hours only, never projected); neither → genuinely unposted → project.** Hours are what distinguish "no revenue" from "not yet recorded". (2026-07-27)
- **[openclaw/CRITICAL] The 180s "no-output stall" that kills a CLI turn is `CLI_RESUME_WATCHDOG_DEFAULTS.maxMs`, and raising `agents.defaults.timeoutSeconds` alone does NOT lift it.** Config path is `agents.defaults.cliBackends.<backend>.reliability.watchdog.{fresh,resume}.noOutputTimeoutMs` — note `agents.defaults.` prefix and the `reliability.` level, neither of which the error message mentions. `resolveCliNoOutputTimeoutMs` also caps the window at `timeoutMs - 1000`, so BOTH must be raised together. Defaults: fresh {ratio .8, min 180s, max 600s}, resume {ratio .3, min 60s, **max 180s**}. Use `openclaw config patch --dry-run` (validated recursive merge), never hand-edit. (2026-07-27)
- **[openclaw/model-config] Claude can fail in Discord because the picker points at `claude-cli/...` or `agentRuntime: claude-cli` while direct `anthropic/...` auth is healthy.** Diagnose with `openclaw infer model run --gateway --model anthropic/...` versus `claude-cli/...`; route picker entries to `anthropic/...` when the CLI profile is stale. For Sonnet 5 on OpenClaw 2026.7.1-2, configuring `anthropic/claude-sonnet-5` exposed a `models list` crash (`applyAnthropicSonnet5Cost` assumed `params.model.cost` existed); local hotfix was optional chaining in `dist/register.runtime-*.js`. (2026-07-30)
- **[workflow] A multi-agent run that stalls EXACTLY N short, repeatably, is the concurrency cap — the in-flight batch died together, so look at the PARENT, not the workflow.** N = `min(16, cores - 2)` = 6 on an 8-core box. Three runs stalled 13/7, 60/54, 43/37 — all 6 short. The cause was the parent CLI being watchdog-killed during a long silent wait, orphaning its children. **Emit progress output during long waits; silence is what kills the turn.** (2026-07-27)
- **[method/crew] A confident audit finding can be built on a label that does not exist — grep the file for the words the finding QUOTES before you accept its framing.** The crew reported `Executive$Sales$ByCrewName.cfm` labels a metric "True TPH **actual** revenue" while computing it from `EstValue` (the bid). The string `actual` appears **zero times in that file**; both TPHs are documented and computed as net value and differ only in denominator. It objected to wording it had invented. **Verify the quote, not just the logic.** (2026-07-28)
- **[method/crew] "Fails OPEN" claims invert easily — read where the flag is INITIALIZED, not just the `cfcatch`.** `dashboard-access-check.cfm` was reported as granting access on any SQL exception. It sets `request.dashOK = false` on line 13 *before* anything can throw, and the catch runs a NARROWER leadership fallback whose own inner catch leaves it false. It fails **closed**. **For any auth finding, trace: initial value → every assignment → the value at the abort check.** (2026-07-28)
- **[method/crew] A finding can be RIGHT and its cited evidence WRONG — check the citation separately from the claim.** The `DateCompleted BETWEEN` end-day defect is real, but the supporting line "Sold.cfm and Pipeline.cfm use the correct form" was false: neither file filters on `DateCompleted` at all (they use `NotifiedDate` and `ProposalSentDate`). The house-style argument survived; the attribution would have embarrassed us in a handoff. **Never forward a citation you have not opened.** Same pass: the crew under-counted the defect's blast radius by more than half — **7 sites, not the 3 reported**. A verified finding still needs its own full-codebase sweep. (2026-07-28)
- **[method] Before reasoning about "what production runs" from a local copy, MD5 it against the shipped artifact.** Play was down, so I could not read the servers — but `V15-DASHBOARDS-DEPLOY-20260719.zip` (the archive Travis actually deployed) was on disk. Hashing all 7 audit-relevant files against it proved `production-dashboard/` is byte-identical, which made offline source review legitimate instead of assumed. **The deploy bundle is the evidence; release notes are not.** (This is the same failure that made OUTSTANDING-FOR-PROD section E stale.) (2026-07-28)
- **[method] When a finding names N sites, sweep for the PATTERN across the whole suite before fixing — then judge each hit individually.** The crew reported 3 `DateCompleted BETWEEN` truncations; a `grep -rn` found **18**, including a second Municipal query block and 5 year-over-year bars nobody mentioned. **But the sweep is a starting point, not the fix list:** one of the 18 (`Dashboard-SalesPipeline.cfm:695`, `DateCompleted <= GETDATE()`) is *correct* — `GETDATE()` carries a time, so it means "completed by now", a has-it-happened guard rather than a date-range filter. **Sweep wide, then read every hit.** 17 fixed, 1 left. (2026-07-28)
- **[method/tooling] Put the assertion BEFORE the write, so a broken script changes nothing.** My first bulk-rewrite crashed on a trivial bug (`pattern[:60]` on a compiled regex). Because every `re.subn` was followed by `assert n==expected` and the file write came last, **zero files were touched** — and the crashed run's output incidentally revealed 5 more defect sites. A script that writes as it goes would have left three CFML files half-rewritten. **Count-assert every substitution, write once at the end.** (2026-07-28)
- **[infra/diagnosis] "Fast TCP connect + ZERO bytes back" = a WEDGED host, not a down one — and Tailscale showing `offline` is a SYMPTOM, not evidence.** I told the Skipper the play box was down after testing only Tailscale, ICMP and HTTPS — three paths that fail identically for both cases. It was answering SYNs on 22/80/443/14333 in **0.18 s** the whole time; the kernel was completing handshakes into the accept queue while every userland service (IIS, SQL, sshd, tailscaled, the VPN endpoint) sat hung. **Probe a service that greets you UNPROMPTED — SSH and SQL Server both send a banner on connect. Zero bytes in 15 s is the tell.** HTTP is a poor probe because it waits for a request. Same error class as calling `.168` a copy: concluding about a machine from negative evidence on one path. (2026-07-28)
- **[infra/ops] Know who holds the provider account BEFORE you need a power cycle.** The play box is a rented dedicated server at **WholeSale Internet (nocix.net)** — IPMI console + hard reboot live in their portal, and **Travis holds the login**. A wedged box therefore means waiting on someone else, overnight. **For any box we depend on daily, record the host, the portal, and who can reach the console — and get a second person added.** Also: **screenshot the console BEFORE rebooting** — a blind power cycle fixes tonight and destroys the evidence, and a disk-full cause just re-wedges on the next scheduled job. (2026-07-28)
- **[method/CRITICAL] Don't retro-fit my own evidence to a hypothesis the user offers. Weigh it — then TEST it.** I measured the play box wedged, said so, and the Skipper offered a well-informed alternative ("Jordan and Travis are working on the dev server"). I wrote *"my own evidence favours his read over mine"* and built an argument that the uniform 0.18 s connect fit a middlebox. **He was wrong and so was my argument** — Jordan confirmed nobody was working on it; the box had genuinely frozen and was hard-rebooted to recover. The tell I had not yet looked for was decisive and cheap: **the System event log was empty for the entire 5.5-hour window** — a network change leaves a box logging, a frozen box logs nothing. **When someone with more context offers a competing hypothesis, the move is to find the observation that DISCRIMINATES between the two, not to re-narrate my existing evidence in their favour.** Deference is not analysis. (2026-07-28)
- **[infra/diagnosis] An empty event log across the outage window is the cleanest "the machine stopped executing" signal there is.** Frozen host = no entries at all. Network/firewall/middlebox problem = box keeps running and keeps logging, so the log is populated and boring. Pair it with `1074` (graceful shutdown requested) vs `41`+`6008` (unclean hard reset) to tell a planned reboot from a recovery power-cycle. Check both **before** theorising about cause. (2026-07-28)
- **[method] Never hand-type a checksum or byte count into a handover doc — regenerate the table FROM the artifact and diff it.** I typed 66,927 for a file that was 66,595; the hash was right, so nothing downstream would have caught it, and a vendor verifying by size would have stopped mid-deploy. Now the flow is: list the staged folder with `Get-FileHash` + `Length`, then assert every row of the doc matches. **The same class of error stalled us twice before (stale manifest checksums flagged as a BLOCKER).** If a number in a document describes a file, derive it. (2026-07-28)
- **[method/tooling] Watch that your OUTPUT NOISE FILTER doesn't eat real output.** My SSH wrapper filtered lines matching `Install` to hide an OpenSSH banner; it silently swallowed my own `"installed <hash>"` confirmation, so a deploy looked like it had only cleared cfclasses. Filter on the specific banner text (`openssh.com/pq`, `store in this`), never on a bare common word. A verification step you can't see is not a verification step. (2026-07-28)
- **[security/cfml] Encode at the SINK, and know which sinks must stay raw.** Fixing reflected XSS in `FinancialReportDashboard.cfm`, my first sweep tried to encode all 33 uses of `URL.search` — but 14 are inside `cfqueryparam`, where the RAW value is required or search silently stops matching, and where there was never an injection risk. The right split is three variables: `searchURL` (URLEncodedFormat) for query strings, `searchAttr` (EncodeForHTMLAttribute) for HTML attributes, and the raw value for bound SQL parameters only. **An assertion of "no raw uses remain" was too strict and caught it before the write** — scope the assertion to the sinks that actually need encoding. (2026-07-28)
- **[research] For a product on a monthly patch cadence, the GA "What's new" page is the WRONG source — read the UPDATE release notes.** Asked what CF2025 offers over CF2023, I read Adobe's CF2025 what's-new page and told the Skipper flatly **"No AI/LLM features. None."** He pushed back that he thought there were MCP/AI improvements. **He was right and it was not close: CF2025 Update 8 (20 May 2026) shipped an entire production AI framework** — MCP client *and* server, six LLM providers incl. local Ollama, vector stores, RAG, prompt-injection guardrails, passkey auth. The GA page covers the Feb-2025 release only; fifteen months of updates were invisible to it. **Version pages describe a moment, not a product. Search `<product> <year> update` and check the release-notes index before asserting a capability is absent** — and "there is no X" is a claim that needs a second source, the same as any positive finding. (2026-07-28)
- **[data/CRITICAL] A `try/except`-style FALLBACK CONSTANT in a reporting script is a silent-wrong-number machine — make the fallback FAIL, not substitute.** The deal dashboard's annual goal comes from `dbo.GoalSettings`; that object no longer exists in the GSTS restore (**error 208**), so every run silently used `GOAL_FALLBACK_MONTHLY = 2,087,119 × 12 = $25.05M`. Two compounding errors: the constant was **July's monthly figure from the superseded $24.0M budget**, and *an annual goal derived by multiplying one month's seasonal number by 12 is not a goal at all* — it changes meaning whenever someone edits the dashboard's inline "Monthly Goal" box. The authoritative `Workbench.dbo.SalesGoal` had been revised to **$25.30M**. The board had been quietly understating the gap. **Any constant named `*_FALLBACK` next to a live query deserves an audit: check that the live source still resolves, and prefer a hard error over a plausible number.** (2026-07-28)
- **[data/method] Check what a bucket LABEL actually contains before repeating it as an opportunity.** "$3.20M / 247 undated sold work orders" was carried in my notes as cheap upside — sold work merely needing production dates. The live split: **$3,144,745 / 238 are dated BEYOND 2026** (real 2027 work — multi-season plans, future-year COs), and only **$53,189 / 9 are genuinely undated.** The framing overstated recoverable 2026 revenue by ~60×. The same query also exposed **$153,473 / 25 WOs past-due-but-not-complete that appear in NEITHER bucket**, because firm coverage requires `EndDate >= today` — **when two filters partition a set, query the leftovers too; that is where things hide.** (2026-07-28)
- **[finance/CRITICAL] Never trust a spreadsheet TAB'S TITLE for its date range — print the columns you actually summed.** The sheet `IS 01.01.23 - 06.30.26` **also carries a partial `Jul 26` column**, and my extractor matched `/^([A-Za-z]{3}) 26$/` and swept it into "H1". Result: **2026 H1 revenue recorded as $11.88M when Jan–Jun is $11,222,433** — a $655K overstatement that propagated into the incentive-gate analysis (run-rate stated $23.8M, actually **$22.44M**, making Gate 1 materially harder than I told the Skipper). **Any month-column regex needs an explicit month allow-list plus an echo of the columns consumed.** (2026-07-28)
- **[finance/method] Reconcile the ERP to the BOOKS before quoting either as "how the company is tracking" — and check the GOAL'S basis too.** The 2026 goals come from the exec deck's `Total Income` row (accounting basis) while I was measuring progress from `Invoices`×`Periods` (ERP basis). Those are different systems and the comparison is only valid if they agree. **They do: H1 TRIM IT $11,078,312 vs book $11,222,433 = −$144K / −1.28%**, individual months swinging ±$183K each way on timing. **That reconciliation is what makes the ERP usable for planning — do it once per half and state the residual, rather than assuming or fearing a gap.** (2026-07-28)
- **[reporting/CRITICAL] Never let two bases into one document — the same quarter appeared twice with different numbers.** My first Q2 board draft took the quarterly trend from TRIM IT ($6.07M) and the vs-plan table from the Controller's statements ($5.90M), for **the same quarter, two tables apart**. Any board member doing arithmetic finds it. The Skipper caught it by simply asking "are these from Dimitry or TRIM IT?" **Rule: pick the basis your AUDIENCE already uses (a board reads the Controller's income statement), state it in a basis-of-preparation note at the top, and label any section that must use a different source.** Restating on the book basis moved real numbers: Q2 YoY **+14.2% → +11.2%**, H1 YoY **−0.3% → +0.4%** (the sign flipped), and "highest of six quarters" shrank from a $283K lead to **$37,830** — technically true, but too thin to headline. **A claim that survives only on one basis is not a claim worth putting in front of a board.** (2026-07-28)
- **[data/CRITICAL] A denominator from the wrong table can invert a recommendation — find the PAYROLL-grade source before quoting utilization.** I computed field utilization from `CrewSheets.ActHours` ÷ roster and reported **146 hrs/person/month = 85% of full time**, i.e. "there is headroom, +7 hires might do it." The Skipper said Dimitry's report and the schedule both show ~80 people a day. He was right. `dbo.CrewMemberCalendars` (per-person, per-day, with Yard/Vacation/Holiday/Sick split out) shows **paid** hours: **~76 people per weekday at 8.9 hrs each, 184 hrs/person/month — already over a 173-hour full-time month.** Job sheets capture only **90%** of paid time, and the roster (94) overstates the ~83 who log hours and the ~76 who actually work a weekday. **The corrected ask went from "+7 people" to "+23" — the optimistic end of my range was pure artifact.** Two rules: (1) *utilization needs a paid-time denominator, never a job-booked one*; (2) when a user says an authoritative report disagrees with you, **go find that report's source table** — do not re-defend your own. (2026-07-28)
- **[data/CRITICAL] "Actual" fields are not always actuals — test them against the estimate before building a metric on them.** `CrewSheets.ActHours` equals `EstHours` on **98–99% of sheets in every single month**: the "actual" hours are the schedule carried over, and real labor time is never captured on the crew sheet (it lives in `dbo.CrewMemberCalendars`, per person per day). Consequence: **TPH — the company's central operating metric, target $130 — is revenue per SCHEDULED hour, not per labor hour.** H1 reads **$130.19 reported vs $116.87 on payroll hours, an ~11% overstatement**, and the gap swings 73%–105% month to month. The trend survives (+6.5% reported, +3.7% on payroll) but the *level* cannot be described as labor efficiency. **Dollars were fine — `ActValue = EstValue` only ~20% — so one field on a table can be trustworthy while its neighbour is not. Test each field, not each table.** (2026-07-28)
- **[method/CRITICAL] Compare like with like — same FIELD, same population — or the baseline is fake.** I claimed the future-dated crew sheets were fabricated partly because `ActHours = EstHours` on 144/148 (97%) "versus 20% for real June work." **The 20% was the ActVALUE match rate; the real June ActHOURS baseline is 98.2%.** I had compared future **hours** against past **dollars**, so a perfectly normal number looked damning. The finding survived on its other evidence (741 correct `Pending`/$0 sheets vs 148 flipped), but that supporting line had to be struck from a note the Skipper may act on. **Before quoting a contrast rate, run the baseline on the identical field and filter.** (2026-07-28)
- **[method] Apply the test that just caught you to the REPLACEMENT source, not only the source it discredited.** Having proved `CrewSheets.ActHours` was the estimate, I immediately built a new metric ($116.87) on `CrewMemberCalendars.TotalHours` and called it "payroll-grade" **without running the same estimate-vs-actual test on it.** The Skipper asking "where did 116.87 come from?" forced it. It held up — `ScheduledTotalHours`/`TotalCost` are entirely NULL, and `TotalHours` has **259 distinct values in one month, granular to the minute and clustered near 9 hrs, with only 5% landing exactly on 9.00** — a distribution that is clocked time, not a standard day. **But "it held up" was luck, not method.** Rule: the moment you replace a discredited field, subject the replacement to the identical scrutiny, and state its validation status — a derived internal figure with no external tie-out does not belong in a board document as a precise number. (2026-07-28)
- **[data/CRITICAL] Before trending a metric year over year, check whether the MEASUREMENT SYSTEM changed mid-series.** I reported "paid hours flat YoY on 8 fewer people, so each person is working 16 more hours a month" — a striking finding, and an artifact. GSTS switched on electronic timekeeping in **Nov 2025**: `CrewMemberCalendars.TotalHours` goes from **20 distinct values / 87.9% whole numbers (Jun 2025)** to **233 distinct / 5.8% whole (Nov 2025)**, with October as the transition. Clocked time naturally records more than a hand-entered "8", so the apparent rise in hours per person may be entirely the instrument. **The cheap test for any hours/quantity field: count distinct values and the share of round numbers, by month — clocked ≈200+ distinct and <10% whole; entered ≈<50 and >60% whole.** Conclusions computed *inside* the new era (the +23 headcount ask, the ~$117 rate) survived; only the cross-boundary comparison died. **Ask "when did we start measuring this, and how?" before "what is the trend?"** (2026-07-28)
- **[method/CRITICAL] "Who did this?" is the wrong question until you know how many people did it — a behaviour spread across five staff is a PROCESS, not an incident.** I framed the 148 future-dated crew sheets as a probable bulk action on a route block and recommended shipping the cap-at-today fix. One work order the Skipper sent (WO 168010) broke it open: the WO is Active 7/13–7/31 yet **11 of its 12 sheets are dated 8/4–8/26, past the work order's own end date**, all generated in one batch at 15:03–15:06 on 7/13 — and completion is credited to **five different people** (60/39/29/18/2 sheets). Five independent actors doing the same thing is routine practice. **That inverts the fix: if crews finish ahead of a generated schedule and the sheet keeps its future date, the work is REAL and merely misdated — and capping production at today would delete real revenue rather than correct it.** Also rejected en route: a shared `LastUpdateDate` looked like a batch sweep but covered only 11 sheets among dozens of ordinary same-day timestamps. **Before recommending a fix for anomalous records, count the distinct actors and open one full example end to end — aggregates hide the mechanism.** (2026-07-28)
- **[data/CRITICAL] When the user says "I don't see where the dates are wrong," believe them and go looking for a SECOND date field.** I spent a day treating 148 crew sheets as fabricated future work. The Skipper looked at the same work order and saw nothing wrong — because he wasn't lying and neither was the UI. `CrewSheets` carries **`WorkDate`** (what every dashboard bins on) **and `CalendarID → Calendars.CalDate`** (what the screen shows). On WO 168010 every `CalDate` sat inside the work order's own 7/13–7/31 window in consecutive working days, while `WorkDate` was pushed into August. At scale: **all 148 "future" sheets have a past `CalDate`, mean corruption 65.9 days, and 2,064 of 4,355 H1 sheets (47%) disagree** — $5.2M and 41,433 hours. **The tiebreaker was external: rebinning on `CalDate` reconciles H1 production to the Controller's revenue within 0.65%, versus 4.2% adrift on `WorkDate`.** Three of my conclusions inverted at once — the cap fix would have *deleted* real revenue, production was never overstated, and five named employees were doing nothing wrong. **When two fields could carry the same concept, reconcile each against an INDEPENDENT source before deciding which is authoritative — and never let an employee-attribution query out of your mouth before you have.** (2026-07-28)
- **[method/tooling] Put the DOCUMENT-level staleness check before the write, not just the per-edit assertions.** Rebinning the board report I asserted every one of 20 substitutions found its target — all passed, file written — and *then* swept for superseded figures, which found three: the headline still read `$134.80`, one replacement had produced "about about", and a table still cited the old H1 average. The per-edit asserts prove *each edit landed*; they cannot prove *the old value is gone everywhere*, because the same number appears in the summary, the tables and the prose. **Two different checks: (1) each intended edit matched — before writing; (2) no superseded token survives anywhere in the finished text — also before writing.** A number changed in a report but left in its own headline is exactly the error a reader spots first. (2026-07-28)
- **[reporting/CRITICAL] Never put a number in an executive document until you can answer "what IS that?"** I wrote *"about 5.7% of hours we pay for never reach a crew sheet — the first place to look for recoverable time"* into a board plan. I had computed the gap (47,195 clocked vs 44,499 on sheets) but never asked where it went. The Skipper did, in one line. **The answer destroyed the claim:** ~2,100 of the 2,696 hours sit in four crew records with **zero active members** — one marked Inactive holding **377 member records**, another literally named `DELETE`. It is stale crew-member assignment, not idle time, and the giveaway was sitting in my own output — **several real crews showed MORE sheet hours than clocked, and hours cannot be negative.** A difference between two systems is not a finding; it is a question. **Before a delta goes in front of anyone, decompose it and name what it is made of — and check whether your own data contains an impossible value that falsifies the reading.** → [[crew-assignment-drift]] (2026-07-29)
- **[reporting/CRITICAL] Do not attribute a measured improvement to an initiative unless you can show the initiative caused it.** I wrote that the weekly sales-and-production meeting "is what produced" the 22.8%→17.0% utilization shift. The Skipper: **"NOT TRUE."** I had inferred causation from co-existence — the meeting exists, the number moved, so one caused the other. In a board document that is the claim a director asks you to defend, and it collapses. **Two failures in one line:** inventing a cause, and putting it in someone else's mouth as their own plan. **State what moved and by how much; say plainly that the cause is not yet established; then make finding the cause the action.** That is a stronger plan anyway — "we do not yet know what produced our best quarter" is a legitimate thing for a COO to be working on, and pretending to know is not. (2026-07-29)
- **[reporting/CRITICAL] Before committing to MOVE a metric, prove the metric can OBSERVE the thing you intend to do.** The Skipper told the Board he would take job-booked share from **94.3% to 95.3%** by returning ~5 min/person/day of field time. The numerator is `CrewSheets.ActHours` — the *estimate* (`= EstHours` on 98–99% of sheets). I checked the two fields that might have rescued it: **`CompletedHours` is a byte-for-byte copy of `ActHours`** (Q2 44,498.60 vs 44,498.72) and **`ScheduledHours` is unused** (1 populated row of 3,305). So if every crew genuinely returns five minutes a day, **the committed metric does not move at all** — while it *will* move if a clerk closes out old sheets. A metric that responds to paperwork and is blind to the intervention is worse than no metric: it manufactures a false success story with a board-level audience. **Ask "what physically has to change in the database for this number to move?" before the number goes in a commitment.** → [[pending-crewsheet-closeout-gap]] (2026-07-29)
- **[data/method] Decompose a delta by LIFECYCLE STATUS before blaming joins or attribution.** Same 2,696-hr gap, second look, different answer. I had explained it as stale crew-member assignment ([[crew-assignment-drift]]) — an attribution story, arrived at by slicing per crew. One `GROUP BY StatusDefID` showed **2,640.69 hrs on 959 sheets sitting in Pending (39) rather than Complete (5) — 98% of the gap in one bucket**, with 902 of them checked in. My earlier explanation wasn't wrong so much as *aimed at the wrong axis*: crew drift is real and still poisons per-crew TPH, but it isn't the company-level gap. **Slicing by the dimension you're already suspicious of confirms your suspicion. Group by status first — it's one query and it partitions the whole population.** (2026-07-29)
- **[data/CRITICAL] A "typical day" metric must be a MEDIAN — a mean turns every public holiday into a fake staffing collapse.** I built the board headcount tile on the mean of each month's weekdays and it reported **74.1 people for July**, below the 79 ramp, looking like attrition. The real July weekday runs **77–81**; the mean was dragged down by two days — **6 Jul (July 4th observed) with 18 people and 3 Jul with 60**. Same defect lurking in every month: Feb has a 13-person day, April a **3-person** day. Median July = **78.5**, right on plan. **The board's own "about 76" turned out to be the H1 mean (75.75) — the H1 median is 77.0 — so the number he committed to was itself holiday-contaminated.** Two general rules: **(1) if the metric's name says "typical", the statistic is a median;** (2) before trusting any daily average, print MIN and MAX of the underlying days — a 3 next to an 82 tells you instantly that the mean is measuring the calendar, not the crew. (2026-07-29)
- **[reporting/method] When a commitment is stated two ways, they drift apart the moment you fix the baseline — make the user pick which one is the commitment.** The board plan said *"about 76 today"*, *"15 net field additions"*, **and** *"tracked monthly against 91"*. Those are consistent only at a base of 76. Once the base was corrected to a median (79 pooled for Q2), **"15 adds" lands at 94 while "against 91" needs only ~12** — the same sentence now describes two different targets three people apart. I nearly built the tile against 91 because that is the word in the *Measure:* line. **Asked instead; he chose the 15.** Rule: when correcting a baseline, immediately re-derive *every* figure that was computed from it and list which downstream commitments now conflict — do not silently keep the one that happens to be phrased as the measure. (2026-07-29)
- **[method/CRITICAL] A filter that returns ZERO is a question, not an answer — and stop decomposing on one axis at a time.** Same 2,696-hour gap, **three** explanations in two days: crew-attribution drift (wrong), unclosed paperwork (wrong), and finally **internal non-productive work orders — 96% of it** (Safety/Meetings, Modified Duty, OJT, Yard, booked to a `PRODUCTION` crew record that never completes because there is nothing to bill). Only **115 of 2,640 Q2 hours** were genuinely unclosed sheets. I had *already run* the internal-category classifier lifted from `Dashboard-RevenuePerformance.cfm`, got **0 hours**, and concluded the categories were unused — when in fact I had filtered `StatusDefID=5` (Complete) and every internal sheet lives in status 39 (Pending). **The zero WAS the finding, pointing straight at the answer, and I read it as an absence.** Two rules: **(1) when a filter returns zero rows, invert it and look at what you excluded before concluding anything;** (2) a delta needs decomposing on *several* axes — by actor, by status, and **by what the record IS** — because the first plausible story always arrives before the true one. (2026-07-29)
- **[method/debugging] "It doesn't work" on a JS-fronted page: skip the page, curl the data endpoint, then run the proc by hand.** RGC rendered perfectly with every tile as an em-dash — nothing in the HTML said why, and the page has **zero `cfquery` tags**; it fetches `RevenueGoalClose.data.cfm` client-side. Three steps to root cause: (1) `curl` the endpoint → **HTTP 422 `RECONCILIATION_FAILED`**, (2) grep the endpoint for that code → it maps a proc THROW containing "does not reconcile", (3) `EXEC` the proc in a try/catch → **err 50022, "SalesGoal does not reconcile to the approved annual goal."** ⚠️ On step 3, **pass the same parameter defaults the page uses** — my first run sent `@Scenario=''` and got a *different* error ("unknown coverage scenario") that would have sent me down the wrong path; the page's `cfparam` default is `PROTECTED`. **And verify the fix inside `BEGIN TRAN … ROLLBACK`** — it proved all four self-reconciliation controls pass without persisting anything. (2026-07-29)
- **[data/CRITICAL] Before "re-basing" a plan table, check what the old rows are the ONLY record of.** Asked to re-base `SalesGoal` H2 from $2.175M to the $2.346M actually required, I found the change is impossible in isolation: the RGC guard demands `SUM(monthly rows) = ApprovedAnnualGoal` exactly, so lifting H2 forces rewriting H1 — and the H1 rows are the **only** surviving record of budget variance. **January ran at 63.8% of goal, −$916,823, which is 89% of the entire H1 miss.** Overwrite the goals with actuals and every month reads ~100%: the shortfall still exists in the revenue numbers but the *explanation* is gone from every actual-vs-goal view. **A plan table is a historical record, not a live target** — put the moving requirement in a metrics/target store that recomputes from actuals, and leave the approved budget immutable. Tested both shapes in `BEGIN TRAN … ROLLBACK` before recommending. (2026-07-29)
- **[method/CRITICAL] When a drill-down "looks wrong", compare its TOTAL to the tile before reading a single row — the reconciliation header names the bug.** The Skipper spotted an in-progress work order inside RGC's *Approved · unscheduled* list. The fastest proof was not inspecting work orders: the drill header said **82 rows / $713,281.37** where the tile said **$234,711.16**, and the proc's own comment promised they must be equal. That one comparison localised the defect to the drill (not the tile, not the data) in a single request. Root cause: `usp_DrillGet`'s APPROVED branch omitted the `IsScheduledOrWorked=0` predicate that `usp_DashboardGet` applies — so a go-ahead with **29 completed crew sheets and $78,118 produced** was listed as unscheduled. **Two habits worth keeping:** any drill should publish a reconciliation header so a mismatch is self-announcing; and when one branch of a multi-branch proc is wrong, **test the sibling branches before patching** — here PENDING, STALE_PENDING and FUTURE_YEAR_EXCLUDED all matched to the cent, which proved the fix belonged in exactly one place. (2026-07-29)
- **[ai/CRITICAL] Guarding an LLM's output with a BLOCKLIST of words cannot work — use an allowlist of shapes.** Giving Arbor Helper a chat personality, I guarded free-chat against inventing company figures by rejecting replies where a number sat near a business noun (`crew|employees|accounts|hours`). It shipped, and the very first probe produced *"about 80 folks in the field every day, and around 70 more in the office"* — **"folks" and "office" were not on my list**, and the 80 came from a stray fact I had written into the persona file myself. Two rules out of one failure: **(1) never put a real figure in a persona/system prompt — it becomes ammunition**, the model will quote it as fact in a context where it has no data; **(2) invert the guard — instead of enumerating what is forbidden (unbounded), enumerate what is permitted.** Final rule is "banter may contain NO digits at all, except OC freeway names," which is checkable in one regex and cannot be talked around. **The prompt asks; only code enforces.** (2026-07-29)
- **[ai/method] A router that forces every question into its nearest category is worse than one that deflects.** Before the personality work, *"how many people work here?"* returned **top crews by TPH** and *"is Ruben any good?"* dumped 178 job records — the semantic fallback had nine buckets and no practical escape, so it answered a *different* question using real, authoritative-looking numbers. Users read that as the system being wrong about the thing they asked. **A confident near-miss destroys more trust than "I can't do that."** The fix only became safe once `none` led somewhere useful (conversational reply) rather than to a dead-end canned line — so build the graceful fallback FIRST, then let the classifier be honest. (2026-07-29)
- **[code/ux] Rotating on the CLOCK is not rotation — two asks inside the same tick return the same item.** Bit me twice in one hour on Arbor Helper. The self-laugh picked its line off `minute()`, so three jokes told inside one minute all came back *"I slay me"*; I moved it to `second()*3` and the **joke** picker was still clock-based too — asking twice in the same second returned the identical joke, which is exactly what a person does when they want *another* one. Clock-indexing only looks random when requests are sparse; the failure mode is precisely the burst case a user creates. **Drive per-ask rotation off a counter that advances on the ask** (`application.ahJokeN++`), and if two rotating things pair up, ride them on the *same* counter with different strides so the pairing can't lock step either. (2026-07-29)
- **[process/CRITICAL] Two copies of a governing document do not stay in sync — and the map will point at the stale one.** `ROUTING.md` sent every fresh session to `contracts/repair-contract.md`, a 17-line June copy carrying **none** of the five weeks of hardening that had gone into `wiki/reference/repair-contract.md` (no verify-the-neighbours rule, no verification gate, no sibling propagation). Following the map correctly produced the wrong rules — which is worse than having no map, because nothing feels off. **A duplicated doc is a silent fork the moment either side is edited; keep ONE canonical text and make every other copy a stub that points at it** (`contracts/*.md` are now stubs). Corollary: when you harden a contract, grep the workspace for other copies of its title *before* you finish, and check what the entry-point files actually route to. (2026-07-29)
- **[coldfusion] Two more CFML syntax traps, both found only by rendering the served page.** (1) **A bare `>` inside a `<cfset>` expression closes the tag** — `<cfset x = a > b>` truncates and emits garbage; use the word operator `GT`. (Same family as the `gt`-as-a-variable-name trap, but the opposite direction.) (2) **`NumberFormat()` has no two-part positive/negative mask** — `NumberFormat(v,"+9.9;-9.9")` is not a thing in CF; build the sign yourself. Neither throws in a way that reads as a syntax error at the source; both look like a broken *number* on the page. **Reading the .cfm proves nothing about CFML edge syntax — fetch the rendered output.** (2026-07-29)
- **[openclaw/model-config] A wrong model LABEL in the picker is almost never in `openclaw.json` — it's injected by a provider plugin's `augmentModelCatalog`, and pruning config can't touch it.** The Skipper said every Claude in the Discord `/model` picker read "(Claude CLI)". I "fixed" it twice by editing config (removing `agentRuntime: claude-cli`, cleaning picker recents) and it kept showing, because the entries were being *synthesised at runtime*: `dist/extensions/anthropic/register.runtime.js` declares `augmentModelCatalog: () => buildClaudeCliCatalogEntries()`, and the names are hard-coded in `dist/cli-catalog-*.js` (`CLAUDE_CLI_MODEL_LABELS`), with a separate `claude-cli` **runtime choice** built in `dist/commands-models-*.js` from `listCliRuntimeModelBackendBindings()`. **Three layers, three edits.** Debug recipe that actually localised it: `grep -rln "<the literal label>" dist/` — the label string is the shortest path to the code that invents the row. And **verify the picker by calling its builder, not by reading config**: `node --input-type=module` → `import buildModelsProviderData` → assert the returned providers/runtime choices, which proves what Discord will render without needing a Discord round-trip. ⚠️ All such fixes are in `~/.npm-global/lib/.../dist/` and **die on the next `openclaw update`** — leave a `.bak-pre-*` beside each and write the re-apply steps into the daily note. (2026-07-30)
- **[openclaw/plugins/CRITICAL] "Plugin failed during register" after a core update is usually a TRUST failure, not a version mismatch — and `plugins update` will tell you everything is fine.** After the Skipper updated to core `2026.7.1-2`, the Codex plugin died at load with `TypeError: Cannot read properties of undefined (reading 'openSyncKeyedStore')`, which cascaded into `codex app-server client closed before turn completed`, `EPIPE` on `turn/interrupt`, and a main session marked *"transcript tail is not resumable."* I first chased the obvious story — plugin `2026.7.1-1` vs core `2026.7.1-2` — but `npm view @openclaw/codex version` proved `-1` **is** latest, so there was no newer build to install and `plugins update codex` correctly answered *"up to date."* The real chain: core `-2` gates `runtime.state.openSyncKeyedStore` behind `assertPluginStateAllowed()`, which requires `origin==="bundled" || trustedOfficialInstall===true`; `trustedOfficialInstall` is computed by `matchesInstalledPluginRecord()`, which demands an **install record whose `installPath` contains (or is contained by) the path the plugin actually loaded from**. No matching record → not trusted → the whole `state` API is undefined → register throws. The record was missing because `~/.openclaw/plugins/installs.json` **could not regenerate**: it held a stale `discord` entry (v2026.5.27 at `~/.openclaw/npm/node_modules/@openclaw/discord`, a path that **no longer exists**), and that conflict made every command print *"Left plugin install index in place because shared SQLite state has conflicting plugin install metadata for: discord"* — a notice I'd been treating as cosmetic noise for an hour. **That "harmless" doctor notice WAS the bug.** Fix: move the stale legacy `installs.json` aside, `openclaw plugins registry --refresh`, restart. The file is legacy — the authoritative copy is the `installed_plugin_index` table in `~/.openclaw/state/openclaw.sqlite`, so it correctly never came back. Three rules: **(1) read the doctor notice as a finding, not decoration** — it named the exact blocking plugin; **(2) verify the version story before acting on it** (`npm view <pkg> version`), or you'll "fix" a mismatch that doesn't exist; **(3) when a plugin API is `undefined`, grep core's dist for the symbol and read the guard around it** — the throw site named the permission gate in one grep, which beat any amount of reinstalling. (2026-07-30)
- **[tools/edit/CRITICAL] An `Edit` whose `newText` ends mid-sentence SPLICES into the rest of the original line and silently destroys content.** Adding a new bullet to the top of MEMORY.md's "Fresh session" block, I matched `oldText` = the header + the first 20 chars of the next line, and wrote a `newText` that ended with an open parenthesis — intending to "insert above." The result glued my half-finished sentence onto the tail of the 2026-07-29 migration bullet, producing `...The day's crashes ( — get our work off PLAY onto our own infra**` and orphaning that entry's opening. It looked plausible in the diff summary (`1 file changed`) and I only caught it because I printed the line back. **Never anchor an edit on a partial line, and never write a `newText` that doesn't terminate its own thought.** To insert a line, anchor on a COMPLETE line and re-emit it intact after the new content. Recovery that worked: `git show HEAD:<file>` to pull the pristine line, rebuild the file in node (splice, don't retype), then prove it with `git diff -U0` — the standard is **"1 insertion, 0 deletions"**; any deletion means I ate something. (2026-07-30)
- **[openclaw/exec-ux] A harmless exploratory probe that exits non-zero fires a user-visible ⚠️ "Exec failed" card — and the Skipper reads it as a real fault.** While saving wiki notes I guessed a note lived in `wiki/facts/` when it was actually in `wiki/reference/`; the `ls`/`wc` returned exit 1, OpenClaw surfaced it in Discord, and he came back asking what "the executive failed error" was about (Discord's font makes `Exec` read as *executive*). The probe worked as intended — I found the real path with `find` on the next call and the note was written correctly — but the alarm card cost him a question and me a six-place search to answer it. **Two habits: (1) when probing for existence, use `find`/`grep -l` or append `|| true` / `2>/dev/null` so a negative answer isn't an "error"; (2) don't guess a wiki subfolder — `find wiki -iname "*<slug>*"` resolves it in one call, and the workspace has real notes in `facts/`, `reference/`, AND `projects/`.** Corollary for reports: non-zero exit ≠ failure of the task; say so explicitly when a probe card shows up in a channel he can see. (2026-07-30)
- **[method/CRITICAL] `consecutiveErrors: N` is a RUN counter, not a CALENDAR counter — and I reported data loss that never happened.** Looking into a Discord glitch I found the nightly wiki-distill cron with `consecutiveErrors: 4` and told the Skipper it had *"failed 4 nights in a row"* with four days of learns lost. Wrong on both counts. `cron runs` history showed the four errors were **one scheduled run plus three automatic retries inside 7 minutes on a single night**, and **Jul 26, 27, 28, 29 every one finished `ok`**. Nothing was lost; only that night's run was missed. The state object told me *how many consecutive attempts failed*, and I silently converted attempts into days because the job happens to be nightly. **Before characterising the duration or blast radius of a recurring failure, pull the RUN HISTORY (`cron runs`) — the counter in `state` cannot distinguish one bad night from a bad week.** Same class as quoting a figure from a note: I read a summary field and reported it as a measured fact. (2026-07-30)
- **[security/CRITICAL] Redaction keyed on FIELD NAME leaks; redact on VALUE SHAPE.** Dumping `openclaw.json` to answer a config question, I wrote a masker that redacted anything whose *key* matched `key`-ish patterns and whose value looked like `sk-`. It printed **`gateway.auth.token`, `channels.discord.token` and `hooks.token` in full cleartext into a Discord channel** — three live secrets — because I had enumerated forbidden *names* instead of recognising secret *shapes*. (Exactly the blocklist-vs-allowlist failure I'd already written up for Arbor Helper, in a different costume.) **Never print a config tree at all; select the specific fields you need.** If you must dump, match on the value: any string of ≥20 chars of base64/hex, or with a known prefix (`sk-`, `MT`, `xox`, `ghp_`), gets masked regardless of what its key is called. Afterwards, *measure* the exposure before proposing a fix — `git log --all -S '<secret>'` proved neither token ever reached the pushed repo, and the files were `600` in a `700` dir, which turned a reflexive "rotate everything" into the correct answer of "don't" (rotating the bot token would have blacked out the only enabled channel). **The disclosure and the remediation are two separate judgements; don't let panic on the first dictate the second.** (2026-07-30)
- **[openclaw/CRITICAL] A log field named `provider` tells you the ROUTE, not the WALLET — I told the Skipper he was on metered API billing when he wasn't.** Cron runs that used to record `provider: "claude-cli"` began recording `provider: "anthropic"` after the `2026.7.1-2` upgrade, and I reported it as *"calls now go direct to Anthropic's API"* — which he correctly heard as *"we're paying per token now."* He challenged it; I was wrong. `openclaw models status` shows the profile is **`mode=token`, `oauth=0`, credential prefix `sk-ant-o…` (an `oat` subscription token from `claude setup-token`), expiring in 334d** — a metered key is `sk-ant-api…` and doesn't age like that. **Still the subscription; only the transport changed** (the `claude-cli` OAuth profile is expired, so the same token now goes straight to `api.anthropic.com`). **Billing is read from the AUTH PROFILE (`models status`), never inferred from a routing/provider label** — and when a user pushes back on a claim about their money, re-derive it from the credential rather than restating the log. Bonus mechanism worth keeping: per `providers/anthropic.md`, `service_tier` **only applies to API-key requests — OAuth/subscription traffic never gets one**, so subscription calls have no priority lever when Anthropic sheds load (the 529 `Overloaded` bursts), and the CLI used to absorb those retries invisibly. (2026-07-30)
- **[openclaw/cron] Patching a cron job silently REWRITES `toolsAllow` into native names — and tools with no equivalent just vanish.** Raising a job's `timeoutSeconds` via `cron update`, the returned object came back with `toolsAllow` changed from `[Read, Glob, Grep, Edit, Write, Bash]` to `[read, edit, write, apply_patch, exec]`. The legacy Claude-CLI names were normalised, but **`Glob` and `Grep` have no native counterpart and were dropped entirely** — and that job's prompt instructs it to "Glob memory/*.md" and "Grep wiki/" throughout. Not fatal (it can shell out via `exec`), but it quietly degraded a job I was only trying to give more time. **After any `cron update`, diff the FULL returned job against what you expected to change — the patch surface is larger than the field you touched.** (2026-07-30)
- **[data/parsing/CRITICAL] A spreadsheet row's LABEL is not "every string cell in the row" — annotations to the right of the totals column silently corrupt the name.** Building the CFO derivation script, anchored regexes `^Total Income$` and `^Adjusted Gross Profit$` matched NOTHING, while `^Gross Profit$` and `^Base EBITDA$` matched fine. Cause: Steve parks his FY targets in column T, so row 10's strings are `{C:'Total Income', T:'$24.4M'}` and my naive join produced **`'Total Income $24.4M'`**. The rows that worked were simply the ones he hadn't annotated — so the bug was *invisible in 4 of 6 cases*, which is exactly how it would have reached a deal number. Fix: resolve a row's NAME from string cells **left of the TOTAL column**, and treat everything right of it as annotation — which turned the bug into a feature (the script now reads his stated targets from that column instead of me hardcoding them). **Two rules: (1) when an anchored match fails, print the string you are actually matching against before touching the regex — I nearly loosened `^...$` to a substring match, which would have hidden the problem instead of finding it; (2) a partial-success pattern across similar rows is a parsing bug, not a data quirk.** The script's own controls caught it (exit 2, "row not found") rather than emitting wrong figures — build the controls before trusting the parse. (2026-07-30)
- **[tooling/CRITICAL] Never retype a path or credential that you read off DISPLAYED output — the display may have elided it.** Deploying the deal-strip fix I needed play's SSH key, so I copied `KEY=/home/…519` from an earlier `head -30 gsql.sh` in my own scrollback. That `…` is a **Unicode ellipsis inserted by the display layer**, not part of the path — the real key path was never shown. `ssh` exited 255 (`Identity file not accessible`), which surfaced to the Skipper as a red "Exec failed" card mid-deploy, on the turn where I was closing a data leak. **Fix: resolve secrets/paths from the SOURCE at runtime — `KEY=$(grep -m1 '^KEY=' gsql.sh | cut -d= -f2-)` — and gate on it (`[ -r "$KEY" ] || exit 1`) so a bad path fails before it touches the network, not after.** General rule: terminal output is a *rendering* of data, not the data. Anything I intend to re-execute must come from the file, never from my own transcript. (2026-07-30)
- **[security/verification/CRITICAL] An auth test that compares two REAL USERS proves nothing about an ATTACKER — every auth probe needs a forged-identity case.** `verify-build.sh`'s auth group checks `ALLOW_UID=9` → 200 and `DENY_UID=340` → 403 and reports **PASS 20 / FAIL 0**; I read that and told the Skipper the deal figures on the BOD dashboard "stay gated." One `curl -H "Cookie: ZUserID=9"` — no password, no session token — returned **HTTP 200 with the full COO-only page**, on `play.greatscotttreeservice.com` which resolves to a **public IP** (an external-vantage probe confirmed it answers the open internet). The gate is not broken: it validates the cookie against `DashboardAccess` with a bootstrap allow-list (`9`, `376`) that must always pass so the admin can't be locked out — and TRIM IT's framework only checks the cookie **exists**, never that anyone authenticated. **Login is a screen door; the real fix is session binding, which is framework-level.** Three rules: **(1) an auth probe must include a forged/garbage identity (`ZUserID=9` with no session, `ZUserID=99999999`), not merely a second legitimate user;** (2) "behind a login" is not a confidentiality control — confirm what the check actually tests before repeating its PASS to anyone; (3) treat a host as internet-reachable until an external probe says otherwise — DNS being public and SSH being tailnet-only are independent facts. Consequence now standing: **no deal/Track-2 material renders on play, ever.** → [[play-public-cookie-forgeable]] (2026-07-31)
- **[reporting/dashboards] A tile that silently scores a DIFFERENT PERIOD than the number its audience remembers will be read as a broken tile.** The BOD cockpit's productivity tile scored a **single month** — June, **$116.32** — while the figure the Skipper carried to the Board was the **Q2 quarter, $125.70**. Nothing was wrong with the arithmetic; June is genuinely soft (it holds the most clocked hours of the quarter). But he saw two numbers for "productivity" and had to ask which was real, which is exactly the trust cost a cockpit exists to avoid. There were **four** live figures in circulation for one metric: Q2 $125.70 (board basis), June $116.32, H1 $122.32, and April-alone $128.33 — plus a production-based $128.88 deliberately kept off board material. **Fix: the headline is the basis the commitment was made on (quarter), with the finer grain as a labelled sub-line — and write the roll-forward generically so it moves to H2 as months close, rather than hard-coding the quarter.** General rule: **a commitment tile must be scored on the same period, statistic and source as the commitment itself; any other grain goes underneath, labelled.** (2026-07-31)
- **[tooling/python] An emoji in a Python string written through `-c`/echo can abort the write with an invalid-surrogate error — and the failure looks like nothing happened.** Appending ship-log entry 203 via a Python one-liner, the emoji in the text produced surrogates the encoder refused, the write **silently did not happen**, and I only caught it by reading the file back. Same family as the shell-quoting traps: the content never reached disk while the command looked survivable. **Habits: write file content from a heredoc'd file or `Write`, not through a shell-interpolated `-c` string; keep emoji out of programmatically-generated text unless the encoding is pinned (`encoding='utf-8'` explicitly on the open); and always read back the tail of any file you appended to before reporting it done.** (2026-07-31)
- **[email/CRITICAL] Corporate mail filters eat ZIP-family attachments silently — and `.kmz` IS a zip.** Sending the Blue Jay/Falcon GIS deliverables to `jwade@gstsinc.com`, the message with the `.kmz` never arrived: no bounce, no NDR, just nothing, because the gateway quarantined it. The zip-free follow-up (PDF/PNG only) landed fine. **Rules: (1) if a send to a corporate address goes unacknowledged, suspect attachment-type quarantine before delivery failure — resend PDF/PNG-only rather than re-explaining; (2) for anything the recipient must actually open, ship the flat format first and offer the archive on request; (3) keep verified personal addresses on hand for exactly this case, and take them from real message headers, never from memory.** Second gotcha from the same run: `send-files.js` is **hardwired to jwade** — a recipient-variant helper had to be written, and it **must live in the `anomaly-monitor/` directory where `nodemailer` resolves**; the same script in `/tmp` fails on module resolution. (2026-07-31)
- **[infra/hermes] A Hermes session keeps a STICKY model binding in the DB — config default only governs NEW sessions.** After moving Gilligan's pilot off the claude-shim to gbt, `/status` still read "opus 4.8": the persistent Telegram session (`sessions` table) had `model=claude-opus-4-8` + `model_config.gateway_runtime` pinned to the now-DEAD shim (`custom / 127.0.0.1:8828`) — so it was both mislabeled AND aimed at a dead endpoint. Editing the DB row while the gateway RAN didn't stick (it re-persisted from its in-memory cache on next activity — same reason an earlier `sessions delete` "came back"). **Fix: stop the gateway, edit `state.db` (set model, DROP `model_config.gateway_runtime`, clear billing fields), start — or use the in-session model-switch. Config edits + restart do NOT rebind existing sessions.** Also: chased this as a phantom "permanent fallback pin" for an hour before reading the source — Hermes fallback RE-ACTIVATES per message (`conversation_loop.py:838`), it's not a one-time pin; a session stuck on the fallback means the primary is failing every turn. (2026-07-31)
- **[infra/models] Small-tool tests hid a full-scale failure, and a redundant provider shadowed the real one.** Two shim/model flops in one session: (1) the claude-shim was **10/10 with 1 tool but ~50% with 28** — Claude Code DEFERS large tool sets behind a flaky internal `ToolSearch`; **tool COUNT is the variable, so reliability MUST be measured at the real tool count + context, not a toy prompt.** (2) Adding a **custom** `zai-anthropic` provider created an **empty duplicate that shadowed Hermes's BUILT-IN `zai`** provider in the model picker → Skipper saw "no models under z.ai." **When a built-in provider exists (`zai`, `kimi-coding`), use it — don't add a custom one with the same purpose.** (2026-07-31)

- [infra/hermes-pilot 2026-08-01] The workspace->pilot `rsync --delete` mirror wiped a file I wrote to the pilot's workspace ROOT (CREW-ACCESS.md) within the hour — protect filters only cover `skills/`+`_pilot-own/`. Any pilot-authored file loose in the workspace root, not present in my live workspace, gets deleted. RULE: pilot-authored docs go in `_pilot-own/` (protected), never workspace root.
- **[method/CRITICAL] Before BUILDING a capability, grep the config of the thing you already run — voice was one boolean away the whole time.** I spent a session standing up a Gilligan-as-an-OpenAI-endpoint adapter, installing OpenJarvis, patching its server middleware, its API routes and its React frontend, and fighting three separate platform layers (a `Permissions-Policy` header, CUDA twice, a PWA service-worker cache) to get spoken conversation working in a browser. The Skipper called it — *"this sucks. i give up… I was having better luck with voice through hermesagent."* — and the Hermes/Telegram bot turned out to be **~90% configured already**: `stt: provider: local` (local whisper) and `tts: provider: edge` (edge-tts) were both in `config.yaml`; the ONLY change needed was **`voice.auto_tts: false → true`** on line 363. Hours of build versus one line, and the one line also came with a native mic, native audio playback, no service workers and no autoplay gate. **Two rules: (1) when a capability is "missing", read the config of every runtime you already operate before writing an adapter for it; (2) fighting a stack at three consecutive layers is DATA — treat the third yak as a signal to go looking for the path that isn't fighting you, not as one more thing to shave.** (2026-08-01)
- **[web/debugging/CRITICAL] When a web app says "microphone access denied" but the OS, the browser and the site permission all allow it, suspect the SERVER's own `Permissions-Policy` header.** The Skipper's Chrome refused the mic on our own chat UI while Windows privacy listed Chrome as having used it that day, the site toggle was blue, and the mic worked in Telegram. Cause: OpenJarvis's security middleware sends `permissions-policy: camera=(), microphone=(), geolocation=()` on **every** response — `microphone=()` is an EMPTY allowlist, meaning the page's own origin may not use the mic, so the browser kills `getUserMedia` **before** OS or site permissions are ever consulted. The app shipped a mic feature and a header forbidding the mic. Compounding it, the frontend caught any `getUserMedia` error and relabelled it "Microphone access denied" — **a permission-shaped error message that was not a permission problem**, which is what sent three layers of user-side settings-checking down the wrong path. **`curl -sI <url>` and read the response headers is the one command that settles it** — do that before asking anyone to check their settings again. (2026-08-01)
- **[tooling/ml] A speech/ML library's `device="auto"` assumes a working CUDA stack — and the umbrella fix belongs on the SERVICE UNIT, not in each library's config.** Twice in one session on the same box: faster-whisper died with `Library libcublas.so.12 is not found`, and later kokoro TTS died with `cudaErrorNoKernelImageForDevice` — jdog1 has a GTX 1050 Ti but no CUDA runtime libs, and the card is too old for current torch kernels anyway. I fixed the first one per-library (`[speech] device="cpu", compute_type="int8"`), which left the second one free to make the identical mistake. The fix that covers every present and future library in the process is **`Environment=CUDA_VISIBLE_DEVICES=` on the systemd unit** — hide the GPU once and everything downstream lands on CPU. **Rule: when the same class of failure recurs in a second component of one process, stop patching components and move the fix up to the process boundary.** (CPU is plenty for short dictation and short TTS.) (2026-08-01)
- **[tooling/rsync] `rsync -an` with no `-v` prints NOTHING — an empty dry-run is not "zero changes".** Checking whether the workspace→pilot mirror would touch anything, I read the blank output as a clean no-op and moved on, then had to backtrack. `-n` suppresses the action; only `-v` produces the itemised list. **Any dry-run you intend to read must be `-avn` (or `--itemize-changes`), and "no output" from a diagnostic command should always be verified as "nothing to report" rather than assumed.** (2026-08-01)
- **[infra/tailscale/CRITICAL] `tailscale serve --https=443 off` clears the WHOLE serve config, not just the proxy you added — it silently killed an unrelated dashboard URL that had been up for three weeks.** Tearing down the OpenJarvis experiment I turned off its `:443` proxy; that same config had been carrying **Boss Herman's Crew Meter at `https://gilligan.tail5807bd.ts.net/` → `127.0.0.1:8300`** since 2026-07-10. The service itself never went down (port 8300 still answers `/healthz` 200) — only the route the Skipper actually uses. Nothing errored, and the documented URL is now dead in two wiki notes. **Rules: (1) run `tailscale serve status` and capture it BEFORE changing any serve config, so teardown restores rather than truncates; (2) shared/singleton config surfaces (serve configs, nginx sites, crontabs, allowlists) are additive on the way in and total on the way out — check what else lives there before an `off`; (3) after tearing down an experiment, re-probe every OTHER durable URL the box publishes, not just the one you built.** (2026-08-01)
- **[ai/wrapping] When you put an adapter in front of an agent that ALREADY has an identity, drop the caller's system prompt — don't merge it.** Fronting Gilligan's endpoint with a third-party chat UI, the first reply came back *"I'm OpenJarvis… on jdog1 under Debian"*: the front-end injects its own "you are OpenJarvis" system message, I passed it through as caller context, and it overrode the identity Hermes loads from SOUL/MEMORY every turn. Fix was one line in the message flattener — **discard `role: system` from the caller** so the wrapped agent's own identity always wins. **General rule: a wrapper's system prompt is noise the moment the thing being wrapped is an agent rather than a raw model; the front-end may own the transport and the UI, never the persona.** (2026-08-01)
- **[tools/edit] A multi-block edit that only half-applies reports success and fails at RUNTIME — re-read every block you claimed to change.** Adding greenfield mode to `run-job.sh`, my edit was supposed to make two places mode-aware: the early `[ -n "$repo" ] || fail "job missing 'repo'"` guard and the repo-resolution/allowlist block below it. Only the first landed. Nothing errored at edit time; the failure surfaced later as a greenfield job dying on `job missing 'repo'` — i.e. the code still enforced a requirement I believed I had removed, and it also left an orphan duplicate `.git` check behind. **After any edit touching more than one region of a file, grep the file for the OLD pattern rather than trusting the edit summary — "the edit applied" and "no stale copy of the old logic survives" are two different assertions** (the same pair as the document-level staleness rule). (2026-08-01)
- **[tooling/subprocess/CRITICAL] A big prompt passed as an argv string dies at ~128KB — and it passes every small test first.** The claude-local shim handed `claude -p` the user prompt **and** the SOUL/comms system prompt as CLI arguments. Linux caps a **single** argv entry at `MAX_ARG_STRLEN` ≈ 128KB (a separate, much lower limit than the ~2MB total `ARG_MAX` everyone quotes), so every real Telegram turn — 150-160K prompt, ~20K system — died with `[Errno 7] Argument list too long: 'claude'`, retried three times, and **silently fell through to the gbt fallback**. Lab tests with toy prompts fit inside the limit and passed 100%, which is why it shipped. **Rules: (1) any subprocess fed content whose size scales with a conversation must take it on STDIN (feed it from a daemon thread so the pipe can't deadlock) or via a `--…-file <tmp>` flag — never argv; (2) size-dependent failures cannot be found by functional tests, so exercise the real payload size before calling it proven; (3) a wrapper with a fallback converts a hard crash into a wrong-but-green answer — check *which* backend served the reply, not just that a reply came.** → [[claude-local-shim-spike]] (2026-08-01)
- **[db/method/CRITICAL] A join to `sys.allocation_units` FANS OUT — it inflated the dead-table inventory by 53% before anyone questioned it.** The first TRIM IT cleanup inventory reported **416 dead tables / 44M rows**; the true figure is **271 / 20.4M**. Cause: a table has one allocation unit per IN_ROW / LOB / ROW_OVERFLOW partition, so any table carrying a `varchar(max)`/`text` column gets counted two or three times by a naive `sys.tables → sys.partitions → sys.allocation_units` join. The **size** (5.76 GB) was right the whole time — `SUM(total_pages)` over the same fan-out is correct because each unit's pages are distinct — which is exactly why nothing looked wrong. **Rule: any catalog query that joins through `allocation_units` must `COUNT(DISTINCT object_id)` for counts and aggregate rows with `MAX`/a filtered `index_id IN (0,1)` predicate, never a bare `SUM` over the joined rows.** General form: **when one query yields both a count and a size and only the count is suspicious, suspect a fan-out, not the data** — and re-derive the count from the narrowest table that can answer it. (2026-08-01)
- **[db/method/CRITICAL] You cannot find dead code by its NAME — build a call graph, and check BOTH layers.** Hunting removable procs in TRIM IT, name patterns (`*$Dev`, `*Test*`, `*Backup*`, date-stamped) flagged 164 candidates — and among them **live** procs we had literally watched execute in the Stage-5 cascade (`EvaluateStumpCrew`). The reverse error is just as bad: **"0 proc callers" does NOT mean dead** — the entire `Generate*` create pattern has zero in-DB callers because it is invoked from `.cfm`. Only the two-layer intersection is safe: **164 name-flagged → 103 high-confidence → 90 with no proc caller → cross-checked against all 8,645 `.cfm` → 84 TRUE-DEAD** (13 held for in-DB callers — `$Dev` procs wired into live code, exactly what the Skipper predicted; 6 held for `.cfm` callers). **A name is a hypothesis; a caller is evidence. Removal sets are built from callers, in every layer that can call.** (2026-08-01)
- **[db/tooling] Match proc bodies SERVER-SIDE — `sqlcmd` truncates `varchar(max)` at 256 chars, and the brute-force `LIKE` scan wedged a shared box.** My first dependency pass ran a `LIKE '%<name>%'` over `sys.sql_modules.definition` for every candidate; it hammered the shared play server long enough to time out the chat LLM. Two fixes, both large: use SQL Server's **native, indexed `sys.sql_expression_dependencies`** (26,214 edges, returns instantly) for in-DB callers, and for the file layer do a **single-pass grep** — concatenate all 8,645 `.cfm` once in memory and test every candidate against that one string, rather than one scan per candidate. And because `sqlcmd` will not return a full `varchar(max)` definition to the client, **any text match against proc bodies must happen on the server** — you cannot pull them local and grep. (2026-08-01)
- **[infra/play/CRITICAL] The nightly play refresh is a full DROP+RESTORE — it reverts PROCEDURES, not just data.** Confirmed live 2026-08-01: `GSTS`'s `create_date` rolls to that morning (08:47), while `Workbench` (06-25) and `GSTSBACKUP` (05-18) persist. A restore replaces the **whole database including all 3,628 procs** — so quarantining or dropping a proc on play self-reverts overnight exactly the way a deleted row does. Consequence for the cleanup: **any soak that must survive more than one night needs a frozen `GSTS_cleanup` copy**, not play's `GSTS`. The existing rule ([[play-gsts-is-ephemeral]]) said "rows and objects we create"; the correction is that it applies equally to objects we *remove* and to code we *change*. (2026-08-01)
- **[process/memory/CRITICAL] A committed wiki note is NOT a memory — it is invisible to a cold wake unless the daily log and the fresh-session pointer carry it.** The Skipper opened a new session and asked me to "update your files so we know about it later"; the build notes for that work had **already been committed hours earlier**, but the session had genuinely no idea, because it woke cold and the two surfacing layers were empty. I nearly answered "already done" from the git log. **The wiki is the durable STORE; `memory/YYYY-MM-DD.md` + the `MEMORY.md` fresh-session pointers are the RETRIEVAL layer — a note written to only one of them exists but will not be found.** Rule: every piece of durable work gets three writes — the wiki note, the daily log line, and (if it changes what the next session should do first) the MEMORY.md pointer. (2026-08-02)
- **[agents/vaults] Another agent's knowledge vault is NOT a place to put YOUR build state — the autosync will quarantine it.** Writing the Aspen→Bigin build note straight into the `aspen-knowledge` vault, the 02:52 autosync swept it into `_quarantine/` — the vault syncs **from** the Aspen board and treats anything Gilligan writes directly into it as foreign. (Same family as the BLACK-guard quarantines, different mechanism: this one is direction-of-sync, not content.) **Rule: Gilligan-owned build/project state lives in the workspace wiki; a peer agent's vault holds only what that agent authored or what arrives through its own sync path.** Design/context notes the other agent wrote can stay there and be linked. (2026-08-02)
- **[process/CRITICAL] A system that is HELD is not a system that is BROKEN — check whether it was turned off before you build a replacement for it.** The Skipper said he was *"flying blind"* on the daily COO numbers, and I started designing a scrape to get him data. The actual state: the COO email, the per-salesperson email and the Nate rollup had been **commented out of the crontab on 2026-07-25** with a `# HELD …` prefix (for stale data) and were **9 days dark**. The machinery was fine — the read-only endpoint still authenticated, still returned figures, the send path still worked. I was one `crontab -l | grep HELD` away from the answer and instead nearly shipped a workaround for a disabled system, which would have left two paths to maintain and the original one still off. **Rules: (1) when a recurring output stops arriving, read the SCHEDULER before the code — a commented cron line, a `liveEnabled:false`, a paused timer are all silent and none of them error; (2) "the user isn't getting X" describes a symptom at the last mile, not a fault anywhere in particular; walk the chain backwards from delivery; (3) owe the correction out loud — I told him he needed the workaround before I knew he didn't, and saying so plainly was the whole value of the turn.** Note the reason it was held is still legitimate: the fix is un-holding it onto data worth sending, not un-holding it. (2026-08-03)
- **[data/CRITICAL] A DSN's LABEL is not evidence of what it reads — prove it by matching a figure against BOTH candidate sources.** `MonitorData.ReadOnly.cfm` line 6 reads `dsn = "GSTSREADONLY"` with the comment *"LIVE PRODUCTION (Travis read-only DSN, 2026-07-14)"*, and on that basis its output had been treated as prod. It is not: accrued **$277,442** matches the **play** DB to the cent, and its July invoiced ($1.69M) sits below prod's ($1.74M). The file lives on the play box and the copy being served is the play one. **A one-line comment written by whoever last edited the file is documentation, not instrumentation.** The check that settles it costs one query: pick a figure that must differ between the two sources (a partial-month accrual, a row count, `MAX(id)`), pull it from the endpoint and from each candidate DB directly, and see which one it lands on **to the cent**. Corollary: when the same file exists on two boxes, "which box is serving this?" is a separate question from "which DSN does the source say?" — and deploying to the box where the setting *sticks* beats repointing on the box that reverts. (2026-08-03)
- **[reporting/CRITICAL] "Revenue" is at least three different numbers, and the dashboard hands you the softest one.** July 2026 reads **$1,738,143 invoiced** (Steve's register, 222 invoices — actually billed), **$2,078,762.87 true-produced** (the prod Revenue dashboard, day-sheet valued, 731 jobs / 15,643 hrs), and **$2.18M "earned"** (invoiced + the corrected $441K municipal accrual). All three are correct; the ~$340K invoiced-to-produced gap is real WIP, because invoicing lags production ([[june-invoicing-lag]]). The trap: **the Revenue-Performance dashboard's default source is "True Produced Work (day sheet + schedule)" and it offers NO invoiced option at all** — so anyone reading "July revenue" off the screen gets the production figure, ~20% above the billed one. In a diligence context that is the number a buyer recuts. **Rules: (1) state the BASIS with every revenue figure, never the figure alone; (2) use INVOICED for anything comparative or external — YoY, TTM, deal material (it's what produced the −6.4% TTM finding) — and treat produced as the operating/capacity view; (3) two figures for one month is not a discrepancy to reconcile away, it's two questions being answered — the error is letting them swap silently.** (2026-08-03)
- **[tools/write/CRITICAL] Writing a daily note instead of appending to it silently destroys the rest of the day.** `memory/2026-08-02.md` lost **49 of its 61 lines** — the whole Aspen→Bigin pause, the IE Wave-5 build, the Friction Hit List kickoff, both friction nodes, the dev-browser capability and the entire OneStepGPS build — because a later session wrote the file with only its own new section (ArborNote) instead of appending. Nothing errored; the auto-capture cron faithfully committed the truncated file two minutes later, so the loss looked like a normal commit and survived the weekly hygiene pass. Recovered from git (`git show <pre-clobber-commit>:memory/YYYY-MM-DD.md`), which is the only reason it exists. **Rules: (1) a dated daily note is APPEND-ONLY — `cat >>` or an anchored edit on the last line, never `Write`; (2) if you must rewrite one, read it first and prove your output is a superset (`git diff --stat` should show insertions and ZERO deletions, the same standard as any partial-line edit); (3) a cold session that finds a daily note "empty" should check `git log --stat` on that path before believing it.** (2026-08-03)
- [DB] gsql.sh uses a FIXED remote temp file (gsql_tmp.sql) — running two gsql.sh calls in PARALLEL clobbers one; both return the same result. Run sequentially, or the harness needs a per-call temp name. (2026-08-03)
